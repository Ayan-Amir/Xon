import {
  require_lodash
} from "./chunk-CIP2UIHJ.js";
import {
  Bn,
  Extension,
  F,
  Ko,
  NodeView,
  Plugin,
  PluginKey,
  S,
  dt,
  fo,
  ho,
  isNodeSelection,
  isTextSelection,
  jt,
  mo,
  posToDOMRect,
  xn
} from "./chunk-G77HIUJU.js";
import {
  require_react_dom
} from "./chunk-GSZ7ISAW.js";
import {
  require_hoist_non_react_statics_cjs
} from "./chunk-LXNNWNNO.js";
import "./chunk-AAY5IJNO.js";
import {
  require_react
} from "./chunk-ZGRSIX2Q.js";
import {
  __toESM
} from "./chunk-ROME4SDB.js";

// node_modules/@blocknote/react/dist/blocknote-react.js
var import_react291 = __toESM(require_react());

// node_modules/@mantine/core/node_modules/@mantine/utils/esm/create-polymorphic-component/create-polymorphic-component.js
function createPolymorphicComponent(component) {
  return component;
}

// node_modules/@mantine/core/node_modules/@mantine/utils/esm/find-element-ancestor/find-element-ancestor.js
function findElementAncestor(element, selector) {
  let _element = element;
  while ((_element = _element.parentElement) && !_element.matches(selector))
    ;
  return _element;
}

// node_modules/@mantine/core/node_modules/@mantine/utils/esm/create-scoped-keydown-handler/create-scoped-keydown-handler.js
function getPreviousIndex(current, elements, loop) {
  for (let i = current - 1; i >= 0; i -= 1) {
    if (!elements[i].disabled) {
      return i;
    }
  }
  if (loop) {
    for (let i = elements.length - 1; i > -1; i -= 1) {
      if (!elements[i].disabled) {
        return i;
      }
    }
  }
  return current;
}
function getNextIndex(current, elements, loop) {
  for (let i = current + 1; i < elements.length; i += 1) {
    if (!elements[i].disabled) {
      return i;
    }
  }
  if (loop) {
    for (let i = 0; i < elements.length; i += 1) {
      if (!elements[i].disabled) {
        return i;
      }
    }
  }
  return current;
}
function onSameLevel(target, sibling, parentSelector) {
  return findElementAncestor(target, parentSelector) === findElementAncestor(sibling, parentSelector);
}
function createScopedKeydownHandler({
  parentSelector,
  siblingSelector,
  onKeyDown,
  loop = true,
  activateOnFocus = false,
  dir = "rtl",
  orientation
}) {
  return (event) => {
    var _a;
    onKeyDown == null ? void 0 : onKeyDown(event);
    const elements = Array.from(((_a = findElementAncestor(event.currentTarget, parentSelector)) == null ? void 0 : _a.querySelectorAll(siblingSelector)) || []).filter((node2) => onSameLevel(event.currentTarget, node2, parentSelector));
    const current = elements.findIndex((el) => event.currentTarget === el);
    const _nextIndex = getNextIndex(current, elements, loop);
    const _previousIndex = getPreviousIndex(current, elements, loop);
    const nextIndex = dir === "rtl" ? _previousIndex : _nextIndex;
    const previousIndex = dir === "rtl" ? _nextIndex : _previousIndex;
    switch (event.key) {
      case "ArrowRight": {
        if (orientation === "horizontal") {
          event.stopPropagation();
          event.preventDefault();
          elements[nextIndex].focus();
          activateOnFocus && elements[nextIndex].click();
        }
        break;
      }
      case "ArrowLeft": {
        if (orientation === "horizontal") {
          event.stopPropagation();
          event.preventDefault();
          elements[previousIndex].focus();
          activateOnFocus && elements[previousIndex].click();
        }
        break;
      }
      case "ArrowUp": {
        if (orientation === "vertical") {
          event.stopPropagation();
          event.preventDefault();
          elements[_previousIndex].focus();
          activateOnFocus && elements[_previousIndex].click();
        }
        break;
      }
      case "ArrowDown": {
        if (orientation === "vertical") {
          event.stopPropagation();
          event.preventDefault();
          elements[_nextIndex].focus();
          activateOnFocus && elements[_nextIndex].click();
        }
        break;
      }
      case "Home": {
        event.stopPropagation();
        event.preventDefault();
        !elements[0].disabled && elements[0].focus();
        break;
      }
      case "End": {
        event.stopPropagation();
        event.preventDefault();
        const last = elements.length - 1;
        !elements[last].disabled && elements[last].focus();
        break;
      }
    }
  };
}

// node_modules/@mantine/core/node_modules/@mantine/utils/esm/get-context-item-index/get-context-item-index.js
function getContextItemIndex(elementSelector, parentSelector, node2) {
  var _a;
  if (!node2) {
    return null;
  }
  return Array.from(((_a = findElementAncestor(node2, parentSelector)) == null ? void 0 : _a.querySelectorAll(elementSelector)) || []).findIndex((element) => element === node2);
}

// node_modules/@mantine/core/node_modules/@mantine/utils/esm/create-safe-context/create-safe-context.js
var import_react = __toESM(require_react());
function createSafeContext(errorMessage) {
  const Context = (0, import_react.createContext)(null);
  const useSafeContext = () => {
    const ctx = (0, import_react.useContext)(Context);
    if (ctx === null) {
      throw new Error(errorMessage);
    }
    return ctx;
  };
  const Provider = ({ children, value }) => import_react.default.createElement(Context.Provider, {
    value
  }, children);
  return [Provider, useSafeContext];
}

// node_modules/@mantine/core/node_modules/@mantine/utils/esm/pack-sx/pack-sx.js
function packSx(sx) {
  return Array.isArray(sx) ? sx : [sx];
}

// node_modules/@mantine/core/node_modules/@mantine/utils/esm/get-safe-id/get-safe-id.js
function getSafeId(uid, errorMessage) {
  return (value) => {
    if (typeof value !== "string" || value.trim().length === 0) {
      throw new Error(errorMessage);
    }
    return `${uid}-${value}`;
  };
}

// node_modules/@mantine/core/node_modules/@mantine/utils/esm/noop/noop.js
var noop = () => {
};

// node_modules/@mantine/core/node_modules/@mantine/utils/esm/close-on-escape/close-on-escape.js
function closeOnEscape(callback, options = { active: true }) {
  if (typeof callback !== "function" || !options.active) {
    return options.onKeyDown || noop;
  }
  return (event) => {
    var _a;
    if (event.key === "Escape") {
      callback(event);
      (_a = options.onTrigger) == null ? void 0 : _a.call(options);
    }
  };
}

// node_modules/@mantine/core/node_modules/@mantine/utils/esm/create-event-handler/create-event-handler.js
function createEventHandler(parentEventHandler, eventHandler) {
  return (event) => {
    parentEventHandler == null ? void 0 : parentEventHandler(event);
    eventHandler == null ? void 0 : eventHandler(event);
  };
}

// node_modules/@mantine/core/node_modules/@mantine/utils/esm/keys/keys.js
function keys(object) {
  return Object.keys(object);
}

// node_modules/@mantine/core/node_modules/@mantine/utils/esm/use-hovered/use-hovered.js
var import_react2 = __toESM(require_react());
function useHovered() {
  const [hovered, setHovered] = (0, import_react2.useState)(-1);
  const resetHovered = () => setHovered(-1);
  return [hovered, { setHovered, resetHovered }];
}

// node_modules/@mantine/core/node_modules/@mantine/utils/esm/group-options/group-options.js
function groupOptions({ data }) {
  const sortedData = [];
  const unGroupedData = [];
  const groupedData = data.reduce((acc, item, index6) => {
    if (item.group) {
      if (acc[item.group])
        acc[item.group].push(index6);
      else
        acc[item.group] = [index6];
    } else {
      unGroupedData.push(index6);
    }
    return acc;
  }, {});
  Object.keys(groupedData).forEach((groupName) => {
    sortedData.push(...groupedData[groupName].map((index6) => data[index6]));
  });
  sortedData.push(...unGroupedData.map((itemIndex) => data[itemIndex]));
  return sortedData;
}

// node_modules/@mantine/core/node_modules/@mantine/utils/esm/create-use-external-events/create-use-external-events.js
var import_react3 = __toESM(require_react());

// node_modules/@mantine/core/node_modules/@mantine/utils/esm/is-element/is-element.js
var import_react4 = __toESM(require_react());
function isElement(value) {
  if (Array.isArray(value) || value === null) {
    return false;
  }
  if (typeof value === "object") {
    if (value.type === import_react4.default.Fragment) {
      return false;
    }
    return true;
  }
  return false;
}

// node_modules/clsx/dist/clsx.m.js
function toVal(mix) {
  var k2, y, str = "";
  if (typeof mix === "string" || typeof mix === "number") {
    str += mix;
  } else if (typeof mix === "object") {
    if (Array.isArray(mix)) {
      for (k2 = 0; k2 < mix.length; k2++) {
        if (mix[k2]) {
          if (y = toVal(mix[k2])) {
            str && (str += " ");
            str += y;
          }
        }
      }
    } else {
      for (k2 in mix) {
        if (mix[k2]) {
          str && (str += " ");
          str += k2;
        }
      }
    }
  }
  return str;
}
function clsx_m_default() {
  var i = 0, tmp, x, str = "";
  while (i < arguments.length) {
    if (tmp = arguments[i++]) {
      if (x = toVal(tmp)) {
        str && (str += " ");
        str += x;
      }
    }
  }
  return str;
}

// node_modules/@mantine/styles/esm/theme/default-colors.js
var DEFAULT_COLORS = {
  dark: [
    "#C1C2C5",
    "#A6A7AB",
    "#909296",
    "#5c5f66",
    "#373A40",
    "#2C2E33",
    "#25262b",
    "#1A1B1E",
    "#141517",
    "#101113"
  ],
  gray: [
    "#f8f9fa",
    "#f1f3f5",
    "#e9ecef",
    "#dee2e6",
    "#ced4da",
    "#adb5bd",
    "#868e96",
    "#495057",
    "#343a40",
    "#212529"
  ],
  red: [
    "#fff5f5",
    "#ffe3e3",
    "#ffc9c9",
    "#ffa8a8",
    "#ff8787",
    "#ff6b6b",
    "#fa5252",
    "#f03e3e",
    "#e03131",
    "#c92a2a"
  ],
  pink: [
    "#fff0f6",
    "#ffdeeb",
    "#fcc2d7",
    "#faa2c1",
    "#f783ac",
    "#f06595",
    "#e64980",
    "#d6336c",
    "#c2255c",
    "#a61e4d"
  ],
  grape: [
    "#f8f0fc",
    "#f3d9fa",
    "#eebefa",
    "#e599f7",
    "#da77f2",
    "#cc5de8",
    "#be4bdb",
    "#ae3ec9",
    "#9c36b5",
    "#862e9c"
  ],
  violet: [
    "#f3f0ff",
    "#e5dbff",
    "#d0bfff",
    "#b197fc",
    "#9775fa",
    "#845ef7",
    "#7950f2",
    "#7048e8",
    "#6741d9",
    "#5f3dc4"
  ],
  indigo: [
    "#edf2ff",
    "#dbe4ff",
    "#bac8ff",
    "#91a7ff",
    "#748ffc",
    "#5c7cfa",
    "#4c6ef5",
    "#4263eb",
    "#3b5bdb",
    "#364fc7"
  ],
  blue: [
    "#e7f5ff",
    "#d0ebff",
    "#a5d8ff",
    "#74c0fc",
    "#4dabf7",
    "#339af0",
    "#228be6",
    "#1c7ed6",
    "#1971c2",
    "#1864ab"
  ],
  cyan: [
    "#e3fafc",
    "#c5f6fa",
    "#99e9f2",
    "#66d9e8",
    "#3bc9db",
    "#22b8cf",
    "#15aabf",
    "#1098ad",
    "#0c8599",
    "#0b7285"
  ],
  teal: [
    "#e6fcf5",
    "#c3fae8",
    "#96f2d7",
    "#63e6be",
    "#38d9a9",
    "#20c997",
    "#12b886",
    "#0ca678",
    "#099268",
    "#087f5b"
  ],
  green: [
    "#ebfbee",
    "#d3f9d8",
    "#b2f2bb",
    "#8ce99a",
    "#69db7c",
    "#51cf66",
    "#40c057",
    "#37b24d",
    "#2f9e44",
    "#2b8a3e"
  ],
  lime: [
    "#f4fce3",
    "#e9fac8",
    "#d8f5a2",
    "#c0eb75",
    "#a9e34b",
    "#94d82d",
    "#82c91e",
    "#74b816",
    "#66a80f",
    "#5c940d"
  ],
  yellow: [
    "#fff9db",
    "#fff3bf",
    "#ffec99",
    "#ffe066",
    "#ffd43b",
    "#fcc419",
    "#fab005",
    "#f59f00",
    "#f08c00",
    "#e67700"
  ],
  orange: [
    "#fff4e6",
    "#ffe8cc",
    "#ffd8a8",
    "#ffc078",
    "#ffa94d",
    "#ff922b",
    "#fd7e14",
    "#f76707",
    "#e8590c",
    "#d9480f"
  ]
};

// node_modules/@mantine/styles/esm/theme/functions/fns/font-styles/font-styles.js
function fontStyles(theme) {
  return () => ({ fontFamily: theme.fontFamily || "sans-serif" });
}

// node_modules/@mantine/styles/esm/theme/functions/fns/focus-styles/focus-styles.js
var __defProp = Object.defineProperty;
var __getOwnPropSymbols = Object.getOwnPropertySymbols;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __propIsEnum = Object.prototype.propertyIsEnumerable;
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp.call(b2, prop))
      __defNormalProp(a2, prop, b2[prop]);
  if (__getOwnPropSymbols)
    for (var prop of __getOwnPropSymbols(b2)) {
      if (__propIsEnum.call(b2, prop))
        __defNormalProp(a2, prop, b2[prop]);
    }
  return a2;
};
function focusStyles(theme) {
  return (selector) => ({
    WebkitTapHighlightColor: "transparent",
    [selector || "&:focus"]: __spreadValues({}, theme.focusRing === "always" || theme.focusRing === "auto" ? theme.focusRingStyles.styles(theme) : theme.focusRingStyles.resetStyles(theme)),
    [selector ? selector.replace(":focus", ":focus:not(:focus-visible)") : "&:focus:not(:focus-visible)"]: __spreadValues({}, theme.focusRing === "auto" || theme.focusRing === "never" ? theme.focusRingStyles.resetStyles(theme) : null)
  });
}

// node_modules/@mantine/styles/esm/theme/functions/fns/primary-shade/primary-shade.js
function primaryShade(theme) {
  return (colorScheme) => {
    if (typeof theme.primaryShade === "number") {
      return theme.primaryShade;
    }
    return theme.primaryShade[colorScheme || theme.colorScheme];
  };
}

// node_modules/@mantine/styles/esm/theme/functions/fns/theme-color/theme-color.js
function themeColor(theme) {
  const getPrimaryShade = primaryShade(theme);
  return (color, shade, primaryFallback = true, useSplittedShade = true) => {
    if (typeof color === "string" && color.includes(".")) {
      const [splitterColor, _splittedShade] = color.split(".");
      const splittedShade = parseInt(_splittedShade, 10);
      if (splitterColor in theme.colors && splittedShade >= 0 && splittedShade < 10) {
        return theme.colors[splitterColor][typeof shade === "number" && !useSplittedShade ? shade : splittedShade];
      }
    }
    const _shade = typeof shade === "number" ? shade : getPrimaryShade();
    return color in theme.colors ? theme.colors[color][_shade] : primaryFallback ? theme.colors[theme.primaryColor][_shade] : color;
  };
}

// node_modules/@mantine/styles/esm/theme/functions/fns/gradient/get-gradient-color-stops/get-gradient-color-stops.js
function getGradientColorStops(colors) {
  let stops = "";
  for (let i = 1; i < colors.length - 1; i += 1) {
    stops += `${colors[i]} ${i / (colors.length - 1) * 100}%, `;
  }
  return `${colors[0]} 0%, ${stops}${colors[colors.length - 1]} 100%`;
}

// node_modules/@mantine/styles/esm/theme/functions/fns/gradient/gradient.js
function linearGradient(deg, ...colors) {
  return `linear-gradient(${deg}deg, ${getGradientColorStops(colors)})`;
}
function radialGradient(...colors) {
  return `radial-gradient(circle, ${getGradientColorStops(colors)})`;
}
function gradient(theme) {
  const getThemeColor = themeColor(theme);
  const getPrimaryShade = primaryShade(theme);
  return (payload) => {
    const merged = {
      from: (payload == null ? void 0 : payload.from) || theme.defaultGradient.from,
      to: (payload == null ? void 0 : payload.to) || theme.defaultGradient.to,
      deg: (payload == null ? void 0 : payload.deg) || theme.defaultGradient.deg
    };
    return `linear-gradient(${merged.deg}deg, ${getThemeColor(merged.from, getPrimaryShade(), false)} 0%, ${getThemeColor(merged.to, getPrimaryShade(), false)} 100%)`;
  };
}

// node_modules/@mantine/styles/esm/theme/functions/fns/size/size.js
function size(props) {
  if (typeof props.size === "number") {
    return props.size;
  }
  const computedSize = props.sizes[props.size];
  return computedSize !== void 0 ? computedSize : props.size || props.sizes.md;
}

// node_modules/@mantine/styles/esm/theme/functions/fns/breakpoints/breakpoints.js
function largerThan(theme) {
  return (breakpoint) => `@media (min-width: ${size({ size: breakpoint, sizes: theme.breakpoints })}px)`;
}
function smallerThan(theme) {
  return (breakpoint) => `@media (max-width: ${size({ size: breakpoint, sizes: theme.breakpoints }) - 1}px)`;
}

// node_modules/@mantine/styles/esm/theme/utils/to-rgba/to-rgba.js
function isHexColor(hex) {
  const HEX_REGEXP = /^#?([0-9A-F]{3}){1,2}$/i;
  return HEX_REGEXP.test(hex);
}
function hexToRgba(color) {
  let hexString = color.replace("#", "");
  if (hexString.length === 3) {
    const shorthandHex = hexString.split("");
    hexString = [
      shorthandHex[0],
      shorthandHex[0],
      shorthandHex[1],
      shorthandHex[1],
      shorthandHex[2],
      shorthandHex[2]
    ].join("");
  }
  const parsed = parseInt(hexString, 16);
  const r2 = parsed >> 16 & 255;
  const g = parsed >> 8 & 255;
  const b2 = parsed & 255;
  return {
    r: r2,
    g,
    b: b2,
    a: 1
  };
}
function rgbStringToRgba(color) {
  const [r2, g, b2, a2] = color.replace(/[^0-9,.]/g, "").split(",").map(Number);
  return { r: r2, g, b: b2, a: a2 || 1 };
}
function toRgba(color) {
  if (isHexColor(color)) {
    return hexToRgba(color);
  }
  if (color.startsWith("rgb")) {
    return rgbStringToRgba(color);
  }
  return {
    r: 0,
    g: 0,
    b: 0,
    a: 1
  };
}

// node_modules/@mantine/styles/esm/theme/functions/fns/rgba/rgba.js
function rgba(color, alpha) {
  if (typeof color !== "string" || alpha > 1 || alpha < 0) {
    return "rgba(0, 0, 0, 1)";
  }
  const { r: r2, g, b: b2 } = toRgba(color);
  return `rgba(${r2}, ${g}, ${b2}, ${alpha})`;
}

// node_modules/@mantine/styles/esm/theme/functions/fns/cover/cover.js
function cover(offset3 = 0) {
  return {
    position: "absolute",
    top: offset3,
    right: offset3,
    left: offset3,
    bottom: offset3
  };
}

// node_modules/@mantine/styles/esm/theme/functions/fns/darken/darken.js
function darken(color, alpha) {
  const { r: r2, g, b: b2, a: a2 } = toRgba(color);
  const f = 1 - alpha;
  const dark = (input) => Math.round(input * f);
  return `rgba(${dark(r2)}, ${dark(g)}, ${dark(b2)}, ${a2})`;
}

// node_modules/@mantine/styles/esm/theme/functions/fns/lighten/lighten.js
function lighten(color, alpha) {
  const { r: r2, g, b: b2, a: a2 } = toRgba(color);
  const light = (input) => Math.round(input + (255 - input) * alpha);
  return `rgba(${light(r2)}, ${light(g)}, ${light(b2)}, ${a2})`;
}

// node_modules/@mantine/styles/esm/theme/functions/fns/radius/radius.js
function radius(theme) {
  return (size3) => {
    if (typeof size3 === "number") {
      return size3;
    }
    const defaultRadius = typeof theme.defaultRadius === "number" ? theme.defaultRadius : theme.radius[theme.defaultRadius] || theme.defaultRadius;
    return theme.radius[size3] || size3 || defaultRadius;
  };
}

// node_modules/@mantine/styles/esm/theme/functions/fns/variant/variant.js
function getColorIndexInfo(color, theme) {
  if (typeof color === "string" && color.includes(".")) {
    const [splittedColor, _splittedShade] = color.split(".");
    const splittedShade = parseInt(_splittedShade, 10);
    if (splittedColor in theme.colors && splittedShade >= 0 && splittedShade < 10) {
      return { isSplittedColor: true, key: splittedColor, shade: splittedShade };
    }
  }
  return { isSplittedColor: false };
}
function variant(theme) {
  const getThemeColor = themeColor(theme);
  const getPrimaryShade = primaryShade(theme);
  const getGradient = gradient(theme);
  return ({ variant: variant2, color, gradient: gradient2, primaryFallback }) => {
    const colorInfo = getColorIndexInfo(color, theme);
    switch (variant2) {
      case "light": {
        return {
          border: "transparent",
          background: rgba(getThemeColor(color, theme.colorScheme === "dark" ? 8 : 0, primaryFallback, false), theme.colorScheme === "dark" ? 0.2 : 1),
          color: color === "dark" ? theme.colorScheme === "dark" ? theme.colors.dark[0] : theme.colors.dark[9] : getThemeColor(color, theme.colorScheme === "dark" ? 2 : getPrimaryShade("light")),
          hover: rgba(getThemeColor(color, theme.colorScheme === "dark" ? 7 : 1, primaryFallback, false), theme.colorScheme === "dark" ? 0.25 : 0.65)
        };
      }
      case "subtle": {
        return {
          border: "transparent",
          background: "transparent",
          color: color === "dark" ? theme.colorScheme === "dark" ? theme.colors.dark[0] : theme.colors.dark[9] : getThemeColor(color, theme.colorScheme === "dark" ? 2 : getPrimaryShade("light")),
          hover: rgba(getThemeColor(color, theme.colorScheme === "dark" ? 8 : 0, primaryFallback, false), theme.colorScheme === "dark" ? 0.2 : 1)
        };
      }
      case "outline": {
        return {
          border: getThemeColor(color, theme.colorScheme === "dark" ? 5 : getPrimaryShade("light")),
          background: "transparent",
          color: getThemeColor(color, theme.colorScheme === "dark" ? 5 : getPrimaryShade("light")),
          hover: theme.colorScheme === "dark" ? rgba(getThemeColor(color, 5, primaryFallback, false), 0.05) : rgba(getThemeColor(color, 0, primaryFallback, false), 0.35)
        };
      }
      case "default": {
        return {
          border: theme.colorScheme === "dark" ? theme.colors.dark[4] : theme.colors.gray[4],
          background: theme.colorScheme === "dark" ? theme.colors.dark[6] : theme.white,
          color: theme.colorScheme === "dark" ? theme.white : theme.black,
          hover: theme.colorScheme === "dark" ? theme.colors.dark[5] : theme.colors.gray[0]
        };
      }
      case "white": {
        return {
          border: "transparent",
          background: theme.white,
          color: getThemeColor(color, getPrimaryShade()),
          hover: null
        };
      }
      case "transparent": {
        return {
          border: "transparent",
          color: color === "dark" ? theme.colorScheme === "dark" ? theme.colors.dark[0] : theme.colors.dark[9] : getThemeColor(color, theme.colorScheme === "dark" ? 2 : getPrimaryShade("light")),
          background: "transparent",
          hover: null
        };
      }
      case "gradient": {
        return {
          background: getGradient(gradient2),
          color: theme.white,
          border: "transparent",
          hover: null
        };
      }
      default: {
        const _primaryShade = getPrimaryShade();
        const _shade = colorInfo.isSplittedColor ? colorInfo.shade : _primaryShade;
        const _color = colorInfo.isSplittedColor ? colorInfo.key : color;
        return {
          border: "transparent",
          background: getThemeColor(_color, _shade, primaryFallback),
          color: theme.white,
          hover: getThemeColor(_color, _shade === 9 ? 8 : _shade + 1)
        };
      }
    }
  };
}

// node_modules/@mantine/styles/esm/theme/functions/fns/primary-color/primary-color.js
function primaryColor(theme) {
  return (colorScheme) => {
    const shade = primaryShade(theme)(colorScheme);
    return theme.colors[theme.primaryColor][shade];
  };
}

// node_modules/@mantine/styles/esm/theme/functions/fns/hover/hover.js
function hover(hoverStyle) {
  return {
    "@media (hover: hover)": {
      "&:hover": hoverStyle
    },
    "@media (hover: none)": {
      "&:active": hoverStyle
    }
  };
}

// node_modules/@mantine/styles/esm/theme/functions/fns/placeholder-styles/placeholder-styles.js
function placeholderStyles(theme) {
  return () => ({
    userSelect: "none",
    color: theme.colorScheme === "dark" ? theme.colors.dark[3] : theme.colors.gray[5]
  });
}

// node_modules/@mantine/styles/esm/theme/functions/fns/index.js
var fns = {
  fontStyles,
  themeColor,
  focusStyles,
  linearGradient,
  radialGradient,
  smallerThan,
  largerThan,
  rgba,
  size,
  cover,
  darken,
  lighten,
  radius,
  variant,
  primaryShade,
  hover,
  gradient,
  primaryColor,
  placeholderStyles
};

// node_modules/@mantine/styles/esm/theme/functions/attach-functions.js
var __defProp2 = Object.defineProperty;
var __defProps = Object.defineProperties;
var __getOwnPropDescs = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols2 = Object.getOwnPropertySymbols;
var __hasOwnProp2 = Object.prototype.hasOwnProperty;
var __propIsEnum2 = Object.prototype.propertyIsEnumerable;
var __defNormalProp2 = (obj, key, value) => key in obj ? __defProp2(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues2 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp2.call(b2, prop))
      __defNormalProp2(a2, prop, b2[prop]);
  if (__getOwnPropSymbols2)
    for (var prop of __getOwnPropSymbols2(b2)) {
      if (__propIsEnum2.call(b2, prop))
        __defNormalProp2(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps = (a2, b2) => __defProps(a2, __getOwnPropDescs(b2));
function attachFunctions(themeBase) {
  return __spreadProps(__spreadValues2({}, themeBase), {
    fn: {
      fontStyles: fns.fontStyles(themeBase),
      themeColor: fns.themeColor(themeBase),
      focusStyles: fns.focusStyles(themeBase),
      largerThan: fns.largerThan(themeBase),
      smallerThan: fns.smallerThan(themeBase),
      radialGradient: fns.radialGradient,
      linearGradient: fns.linearGradient,
      gradient: fns.gradient(themeBase),
      rgba: fns.rgba,
      size: fns.size,
      cover: fns.cover,
      lighten: fns.lighten,
      darken: fns.darken,
      primaryShade: fns.primaryShade(themeBase),
      radius: fns.radius(themeBase),
      variant: fns.variant(themeBase),
      hover: fns.hover,
      primaryColor: fns.primaryColor(themeBase),
      placeholderStyles: fns.placeholderStyles(themeBase)
    }
  });
}

// node_modules/@mantine/styles/esm/theme/default-theme.js
var MANTINE_COLORS = Object.keys(DEFAULT_COLORS);
var MANTINE_SIZES = ["xs", "sm", "md", "lg", "xl"];
var _DEFAULT_THEME = {
  dir: "ltr",
  primaryShade: {
    light: 6,
    dark: 8
  },
  focusRing: "auto",
  loader: "oval",
  dateFormat: "MMMM D, YYYY",
  colorScheme: "light",
  white: "#fff",
  black: "#000",
  defaultRadius: "sm",
  transitionTimingFunction: "ease",
  colors: DEFAULT_COLORS,
  lineHeight: 1.55,
  fontFamily: "-apple-system, BlinkMacSystemFont, Segoe UI, Roboto, Helvetica, Arial, sans-serif, Apple Color Emoji, Segoe UI Emoji",
  fontFamilyMonospace: "ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, Liberation Mono, Courier New, monospace",
  primaryColor: "blue",
  respectReducedMotion: true,
  cursorType: "default",
  defaultGradient: {
    from: "indigo",
    to: "cyan",
    deg: 45
  },
  shadows: {
    xs: "0 1px 3px rgba(0, 0, 0, 0.05), 0 1px 2px rgba(0, 0, 0, 0.1)",
    sm: "0 1px 3px rgba(0, 0, 0, 0.05), rgba(0, 0, 0, 0.05) 0px 10px 15px -5px, rgba(0, 0, 0, 0.04) 0px 7px 7px -5px",
    md: "0 1px 3px rgba(0, 0, 0, 0.05), rgba(0, 0, 0, 0.05) 0px 20px 25px -5px, rgba(0, 0, 0, 0.04) 0px 10px 10px -5px",
    lg: "0 1px 3px rgba(0, 0, 0, 0.05), rgba(0, 0, 0, 0.05) 0px 28px 23px -7px, rgba(0, 0, 0, 0.04) 0px 12px 12px -7px",
    xl: "0 1px 3px rgba(0, 0, 0, 0.05), rgba(0, 0, 0, 0.05) 0px 36px 28px -7px, rgba(0, 0, 0, 0.04) 0px 17px 17px -7px"
  },
  fontSizes: {
    xs: 12,
    sm: 14,
    md: 16,
    lg: 18,
    xl: 20
  },
  radius: {
    xs: 2,
    sm: 4,
    md: 8,
    lg: 16,
    xl: 32
  },
  spacing: {
    xs: 10,
    sm: 12,
    md: 16,
    lg: 20,
    xl: 24
  },
  breakpoints: {
    xs: 576,
    sm: 768,
    md: 992,
    lg: 1200,
    xl: 1400
  },
  headings: {
    fontFamily: "-apple-system, BlinkMacSystemFont, Segoe UI, Roboto, Helvetica, Arial, sans-serif, Apple Color Emoji, Segoe UI Emoji",
    fontWeight: 700,
    sizes: {
      h1: { fontSize: 34, lineHeight: 1.3, fontWeight: void 0 },
      h2: { fontSize: 26, lineHeight: 1.35, fontWeight: void 0 },
      h3: { fontSize: 22, lineHeight: 1.4, fontWeight: void 0 },
      h4: { fontSize: 18, lineHeight: 1.45, fontWeight: void 0 },
      h5: { fontSize: 16, lineHeight: 1.5, fontWeight: void 0 },
      h6: { fontSize: 14, lineHeight: 1.5, fontWeight: void 0 }
    }
  },
  other: {},
  components: {},
  activeStyles: { transform: "translateY(1px)" },
  datesLocale: "en",
  globalStyles: void 0,
  focusRingStyles: {
    styles: (theme) => ({
      outlineOffset: 2,
      outline: `2px solid ${theme.colors[theme.primaryColor][theme.colorScheme === "dark" ? 7 : 5]}`
    }),
    resetStyles: () => ({ outline: "none" }),
    inputStyles: (theme) => ({
      outline: "none",
      borderColor: theme.colors[theme.primaryColor][typeof theme.primaryShade === "object" ? theme.primaryShade[theme.colorScheme] : theme.primaryShade]
    })
  }
};
var DEFAULT_THEME = attachFunctions(_DEFAULT_THEME);

// node_modules/@mantine/styles/esm/theme/MantineProvider.js
var import_react12 = __toESM(require_react());

// node_modules/@emotion/react/dist/emotion-element-c39617d8.browser.esm.js
var React4 = __toESM(require_react());
var import_react5 = __toESM(require_react());

// node_modules/@emotion/sheet/dist/emotion-sheet.browser.esm.js
function sheetForTag(tag) {
  if (tag.sheet) {
    return tag.sheet;
  }
  for (var i = 0; i < document.styleSheets.length; i++) {
    if (document.styleSheets[i].ownerNode === tag) {
      return document.styleSheets[i];
    }
  }
}
function createStyleElement(options) {
  var tag = document.createElement("style");
  tag.setAttribute("data-emotion", options.key);
  if (options.nonce !== void 0) {
    tag.setAttribute("nonce", options.nonce);
  }
  tag.appendChild(document.createTextNode(""));
  tag.setAttribute("data-s", "");
  return tag;
}
var StyleSheet = function() {
  function StyleSheet2(options) {
    var _this = this;
    this._insertTag = function(tag) {
      var before;
      if (_this.tags.length === 0) {
        if (_this.insertionPoint) {
          before = _this.insertionPoint.nextSibling;
        } else if (_this.prepend) {
          before = _this.container.firstChild;
        } else {
          before = _this.before;
        }
      } else {
        before = _this.tags[_this.tags.length - 1].nextSibling;
      }
      _this.container.insertBefore(tag, before);
      _this.tags.push(tag);
    };
    this.isSpeedy = options.speedy === void 0 ? false : options.speedy;
    this.tags = [];
    this.ctr = 0;
    this.nonce = options.nonce;
    this.key = options.key;
    this.container = options.container;
    this.prepend = options.prepend;
    this.insertionPoint = options.insertionPoint;
    this.before = null;
  }
  var _proto = StyleSheet2.prototype;
  _proto.hydrate = function hydrate(nodes) {
    nodes.forEach(this._insertTag);
  };
  _proto.insert = function insert(rule) {
    if (this.ctr % (this.isSpeedy ? 65e3 : 1) === 0) {
      this._insertTag(createStyleElement(this));
    }
    var tag = this.tags[this.tags.length - 1];
    if (true) {
      var isImportRule3 = rule.charCodeAt(0) === 64 && rule.charCodeAt(1) === 105;
      if (isImportRule3 && this._alreadyInsertedOrderInsensitiveRule) {
        console.error("You're attempting to insert the following rule:\n" + rule + "\n\n`@import` rules must be before all other types of rules in a stylesheet but other rules have already been inserted. Please ensure that `@import` rules are before all other rules.");
      }
      this._alreadyInsertedOrderInsensitiveRule = this._alreadyInsertedOrderInsensitiveRule || !isImportRule3;
    }
    if (this.isSpeedy) {
      var sheet = sheetForTag(tag);
      try {
        sheet.insertRule(rule, sheet.cssRules.length);
      } catch (e) {
        if (!/:(-moz-placeholder|-moz-focus-inner|-moz-focusring|-ms-input-placeholder|-moz-read-write|-moz-read-only|-ms-clear|-ms-expand|-ms-reveal){/.test(rule)) {
          console.error('There was a problem inserting the following rule: "' + rule + '"', e);
        }
      }
    } else {
      tag.appendChild(document.createTextNode(rule));
    }
    this.ctr++;
  };
  _proto.flush = function flush() {
    this.tags.forEach(function(tag) {
      return tag.parentNode && tag.parentNode.removeChild(tag);
    });
    this.tags = [];
    this.ctr = 0;
    if (true) {
      this._alreadyInsertedOrderInsensitiveRule = false;
    }
  };
  return StyleSheet2;
}();

// node_modules/stylis/src/Enum.js
var MS = "-ms-";
var MOZ = "-moz-";
var WEBKIT = "-webkit-";
var COMMENT = "comm";
var RULESET = "rule";
var DECLARATION = "decl";
var IMPORT = "@import";
var KEYFRAMES = "@keyframes";
var LAYER = "@layer";

// node_modules/stylis/src/Utility.js
var abs = Math.abs;
var from = String.fromCharCode;
var assign = Object.assign;
function hash(value, length2) {
  return charat(value, 0) ^ 45 ? (((length2 << 2 ^ charat(value, 0)) << 2 ^ charat(value, 1)) << 2 ^ charat(value, 2)) << 2 ^ charat(value, 3) : 0;
}
function trim(value) {
  return value.trim();
}
function match(value, pattern) {
  return (value = pattern.exec(value)) ? value[0] : value;
}
function replace(value, pattern, replacement) {
  return value.replace(pattern, replacement);
}
function indexof(value, search) {
  return value.indexOf(search);
}
function charat(value, index6) {
  return value.charCodeAt(index6) | 0;
}
function substr(value, begin, end2) {
  return value.slice(begin, end2);
}
function strlen(value) {
  return value.length;
}
function sizeof(value) {
  return value.length;
}
function append(value, array) {
  return array.push(value), value;
}
function combine(array, callback) {
  return array.map(callback).join("");
}

// node_modules/stylis/src/Tokenizer.js
var line = 1;
var column = 1;
var length = 0;
var position = 0;
var character = 0;
var characters = "";
function node(value, root, parent, type, props, children, length2) {
  return { value, root, parent, type, props, children, line, column, length: length2, return: "" };
}
function copy(root, props) {
  return assign(node("", null, null, "", null, null, 0), root, { length: -root.length }, props);
}
function char() {
  return character;
}
function prev() {
  character = position > 0 ? charat(characters, --position) : 0;
  if (column--, character === 10)
    column = 1, line--;
  return character;
}
function next() {
  character = position < length ? charat(characters, position++) : 0;
  if (column++, character === 10)
    column = 1, line++;
  return character;
}
function peek() {
  return charat(characters, position);
}
function caret() {
  return position;
}
function slice(begin, end2) {
  return substr(characters, begin, end2);
}
function token(type) {
  switch (type) {
    case 0:
    case 9:
    case 10:
    case 13:
    case 32:
      return 5;
    case 33:
    case 43:
    case 44:
    case 47:
    case 62:
    case 64:
    case 126:
    case 59:
    case 123:
    case 125:
      return 4;
    case 58:
      return 3;
    case 34:
    case 39:
    case 40:
    case 91:
      return 2;
    case 41:
    case 93:
      return 1;
  }
  return 0;
}
function alloc(value) {
  return line = column = 1, length = strlen(characters = value), position = 0, [];
}
function dealloc(value) {
  return characters = "", value;
}
function delimit(type) {
  return trim(slice(position - 1, delimiter(type === 91 ? type + 2 : type === 40 ? type + 1 : type)));
}
function whitespace(type) {
  while (character = peek())
    if (character < 33)
      next();
    else
      break;
  return token(type) > 2 || token(character) > 3 ? "" : " ";
}
function escaping(index6, count2) {
  while (--count2 && next())
    if (character < 48 || character > 102 || character > 57 && character < 65 || character > 70 && character < 97)
      break;
  return slice(index6, caret() + (count2 < 6 && peek() == 32 && next() == 32));
}
function delimiter(type) {
  while (next())
    switch (character) {
      case type:
        return position;
      case 34:
      case 39:
        if (type !== 34 && type !== 39)
          delimiter(character);
        break;
      case 40:
        if (type === 41)
          delimiter(type);
        break;
      case 92:
        next();
        break;
    }
  return position;
}
function commenter(type, index6) {
  while (next())
    if (type + character === 47 + 10)
      break;
    else if (type + character === 42 + 42 && peek() === 47)
      break;
  return "/*" + slice(index6, position - 1) + "*" + from(type === 47 ? type : next());
}
function identifier(index6) {
  while (!token(peek()))
    next();
  return slice(index6, position);
}

// node_modules/stylis/src/Parser.js
function compile(value) {
  return dealloc(parse("", null, null, null, [""], value = alloc(value), 0, [0], value));
}
function parse(value, root, parent, rule, rules, rulesets, pseudo, points, declarations) {
  var index6 = 0;
  var offset3 = 0;
  var length2 = pseudo;
  var atrule = 0;
  var property = 0;
  var previous = 0;
  var variable = 1;
  var scanning = 1;
  var ampersand = 1;
  var character2 = 0;
  var type = "";
  var props = rules;
  var children = rulesets;
  var reference2 = rule;
  var characters2 = type;
  while (scanning)
    switch (previous = character2, character2 = next()) {
      case 40:
        if (previous != 108 && charat(characters2, length2 - 1) == 58) {
          if (indexof(characters2 += replace(delimit(character2), "&", "&\f"), "&\f") != -1)
            ampersand = -1;
          break;
        }
      case 34:
      case 39:
      case 91:
        characters2 += delimit(character2);
        break;
      case 9:
      case 10:
      case 13:
      case 32:
        characters2 += whitespace(previous);
        break;
      case 92:
        characters2 += escaping(caret() - 1, 7);
        continue;
      case 47:
        switch (peek()) {
          case 42:
          case 47:
            append(comment(commenter(next(), caret()), root, parent), declarations);
            break;
          default:
            characters2 += "/";
        }
        break;
      case 123 * variable:
        points[index6++] = strlen(characters2) * ampersand;
      case 125 * variable:
      case 59:
      case 0:
        switch (character2) {
          case 0:
          case 125:
            scanning = 0;
          case 59 + offset3:
            if (ampersand == -1)
              characters2 = replace(characters2, /\f/g, "");
            if (property > 0 && strlen(characters2) - length2)
              append(property > 32 ? declaration(characters2 + ";", rule, parent, length2 - 1) : declaration(replace(characters2, " ", "") + ";", rule, parent, length2 - 2), declarations);
            break;
          case 59:
            characters2 += ";";
          default:
            append(reference2 = ruleset(characters2, root, parent, index6, offset3, rules, points, type, props = [], children = [], length2), rulesets);
            if (character2 === 123)
              if (offset3 === 0)
                parse(characters2, root, reference2, reference2, props, rulesets, length2, points, children);
              else
                switch (atrule === 99 && charat(characters2, 3) === 110 ? 100 : atrule) {
                  case 100:
                  case 108:
                  case 109:
                  case 115:
                    parse(value, reference2, reference2, rule && append(ruleset(value, reference2, reference2, 0, 0, rules, points, type, rules, props = [], length2), children), rules, children, length2, points, rule ? props : children);
                    break;
                  default:
                    parse(characters2, reference2, reference2, reference2, [""], children, 0, points, children);
                }
        }
        index6 = offset3 = property = 0, variable = ampersand = 1, type = characters2 = "", length2 = pseudo;
        break;
      case 58:
        length2 = 1 + strlen(characters2), property = previous;
      default:
        if (variable < 1) {
          if (character2 == 123)
            --variable;
          else if (character2 == 125 && variable++ == 0 && prev() == 125)
            continue;
        }
        switch (characters2 += from(character2), character2 * variable) {
          case 38:
            ampersand = offset3 > 0 ? 1 : (characters2 += "\f", -1);
            break;
          case 44:
            points[index6++] = (strlen(characters2) - 1) * ampersand, ampersand = 1;
            break;
          case 64:
            if (peek() === 45)
              characters2 += delimit(next());
            atrule = peek(), offset3 = length2 = strlen(type = characters2 += identifier(caret())), character2++;
            break;
          case 45:
            if (previous === 45 && strlen(characters2) == 2)
              variable = 0;
        }
    }
  return rulesets;
}
function ruleset(value, root, parent, index6, offset3, rules, points, type, props, children, length2) {
  var post = offset3 - 1;
  var rule = offset3 === 0 ? rules : [""];
  var size3 = sizeof(rule);
  for (var i = 0, j2 = 0, k2 = 0; i < index6; ++i)
    for (var x = 0, y = substr(value, post + 1, post = abs(j2 = points[i])), z2 = value; x < size3; ++x)
      if (z2 = trim(j2 > 0 ? rule[x] + " " + y : replace(y, /&\f/g, rule[x])))
        props[k2++] = z2;
  return node(value, root, parent, offset3 === 0 ? RULESET : type, props, children, length2);
}
function comment(value, root, parent) {
  return node(value, root, parent, COMMENT, from(char()), substr(value, 2, -2), 0);
}
function declaration(value, root, parent, length2) {
  return node(value, root, parent, DECLARATION, substr(value, 0, length2), substr(value, length2 + 1, -1), length2);
}

// node_modules/stylis/src/Serializer.js
function serialize(children, callback) {
  var output = "";
  var length2 = sizeof(children);
  for (var i = 0; i < length2; i++)
    output += callback(children[i], i, children, callback) || "";
  return output;
}
function stringify(element, index6, children, callback) {
  switch (element.type) {
    case LAYER:
      if (element.children.length)
        break;
    case IMPORT:
    case DECLARATION:
      return element.return = element.return || element.value;
    case COMMENT:
      return "";
    case KEYFRAMES:
      return element.return = element.value + "{" + serialize(element.children, callback) + "}";
    case RULESET:
      element.value = element.props.join(",");
  }
  return strlen(children = serialize(element.children, callback)) ? element.return = element.value + "{" + children + "}" : "";
}

// node_modules/stylis/src/Middleware.js
function middleware(collection) {
  var length2 = sizeof(collection);
  return function(element, index6, children, callback) {
    var output = "";
    for (var i = 0; i < length2; i++)
      output += collection[i](element, index6, children, callback) || "";
    return output;
  };
}

// node_modules/@emotion/weak-memoize/dist/emotion-weak-memoize.esm.js
var weakMemoize = function weakMemoize2(func) {
  var cache = /* @__PURE__ */ new WeakMap();
  return function(arg) {
    if (cache.has(arg)) {
      return cache.get(arg);
    }
    var ret = func(arg);
    cache.set(arg, ret);
    return ret;
  };
};

// node_modules/@emotion/memoize/dist/emotion-memoize.esm.js
function memoize(fn5) {
  var cache = /* @__PURE__ */ Object.create(null);
  return function(arg) {
    if (cache[arg] === void 0)
      cache[arg] = fn5(arg);
    return cache[arg];
  };
}

// node_modules/@emotion/cache/dist/emotion-cache.browser.esm.js
var identifierWithPointTracking = function identifierWithPointTracking2(begin, points, index6) {
  var previous = 0;
  var character2 = 0;
  while (true) {
    previous = character2;
    character2 = peek();
    if (previous === 38 && character2 === 12) {
      points[index6] = 1;
    }
    if (token(character2)) {
      break;
    }
    next();
  }
  return slice(begin, position);
};
var toRules = function toRules2(parsed, points) {
  var index6 = -1;
  var character2 = 44;
  do {
    switch (token(character2)) {
      case 0:
        if (character2 === 38 && peek() === 12) {
          points[index6] = 1;
        }
        parsed[index6] += identifierWithPointTracking(position - 1, points, index6);
        break;
      case 2:
        parsed[index6] += delimit(character2);
        break;
      case 4:
        if (character2 === 44) {
          parsed[++index6] = peek() === 58 ? "&\f" : "";
          points[index6] = parsed[index6].length;
          break;
        }
      default:
        parsed[index6] += from(character2);
    }
  } while (character2 = next());
  return parsed;
};
var getRules = function getRules2(value, points) {
  return dealloc(toRules(alloc(value), points));
};
var fixedElements = /* @__PURE__ */ new WeakMap();
var compat = function compat2(element) {
  if (element.type !== "rule" || !element.parent || // positive .length indicates that this rule contains pseudo
  // negative .length indicates that this rule has been already prefixed
  element.length < 1) {
    return;
  }
  var value = element.value, parent = element.parent;
  var isImplicitRule = element.column === parent.column && element.line === parent.line;
  while (parent.type !== "rule") {
    parent = parent.parent;
    if (!parent)
      return;
  }
  if (element.props.length === 1 && value.charCodeAt(0) !== 58 && !fixedElements.get(parent)) {
    return;
  }
  if (isImplicitRule) {
    return;
  }
  fixedElements.set(element, true);
  var points = [];
  var rules = getRules(value, points);
  var parentRules = parent.props;
  for (var i = 0, k2 = 0; i < rules.length; i++) {
    for (var j2 = 0; j2 < parentRules.length; j2++, k2++) {
      element.props[k2] = points[i] ? rules[i].replace(/&\f/g, parentRules[j2]) : parentRules[j2] + " " + rules[i];
    }
  }
};
var removeLabel = function removeLabel2(element) {
  if (element.type === "decl") {
    var value = element.value;
    if (
      // charcode for l
      value.charCodeAt(0) === 108 && // charcode for b
      value.charCodeAt(2) === 98
    ) {
      element["return"] = "";
      element.value = "";
    }
  }
};
var ignoreFlag = "emotion-disable-server-rendering-unsafe-selector-warning-please-do-not-use-this-the-warning-exists-for-a-reason";
var isIgnoringComment = function isIgnoringComment2(element) {
  return element.type === "comm" && element.children.indexOf(ignoreFlag) > -1;
};
var createUnsafeSelectorsAlarm = function createUnsafeSelectorsAlarm2(cache) {
  return function(element, index6, children) {
    if (element.type !== "rule" || cache.compat)
      return;
    var unsafePseudoClasses = element.value.match(/(:first|:nth|:nth-last)-child/g);
    if (unsafePseudoClasses) {
      var isNested = !!element.parent;
      var commentContainer = isNested ? element.parent.children : (
        // global rule at the root level
        children
      );
      for (var i = commentContainer.length - 1; i >= 0; i--) {
        var node2 = commentContainer[i];
        if (node2.line < element.line) {
          break;
        }
        if (node2.column < element.column) {
          if (isIgnoringComment(node2)) {
            return;
          }
          break;
        }
      }
      unsafePseudoClasses.forEach(function(unsafePseudoClass) {
        console.error('The pseudo class "' + unsafePseudoClass + '" is potentially unsafe when doing server-side rendering. Try changing it to "' + unsafePseudoClass.split("-child")[0] + '-of-type".');
      });
    }
  };
};
var isImportRule = function isImportRule2(element) {
  return element.type.charCodeAt(1) === 105 && element.type.charCodeAt(0) === 64;
};
var isPrependedWithRegularRules = function isPrependedWithRegularRules2(index6, children) {
  for (var i = index6 - 1; i >= 0; i--) {
    if (!isImportRule(children[i])) {
      return true;
    }
  }
  return false;
};
var nullifyElement = function nullifyElement2(element) {
  element.type = "";
  element.value = "";
  element["return"] = "";
  element.children = "";
  element.props = "";
};
var incorrectImportAlarm = function incorrectImportAlarm2(element, index6, children) {
  if (!isImportRule(element)) {
    return;
  }
  if (element.parent) {
    console.error("`@import` rules can't be nested inside other rules. Please move it to the top level and put it before regular rules. Keep in mind that they can only be used within global styles.");
    nullifyElement(element);
  } else if (isPrependedWithRegularRules(index6, children)) {
    console.error("`@import` rules can't be after other rules. Please put your `@import` rules before your other rules.");
    nullifyElement(element);
  }
};
function prefix2(value, length2) {
  switch (hash(value, length2)) {
    case 5103:
      return WEBKIT + "print-" + value + value;
    case 5737:
    case 4201:
    case 3177:
    case 3433:
    case 1641:
    case 4457:
    case 2921:
    case 5572:
    case 6356:
    case 5844:
    case 3191:
    case 6645:
    case 3005:
    case 6391:
    case 5879:
    case 5623:
    case 6135:
    case 4599:
    case 4855:
    case 4215:
    case 6389:
    case 5109:
    case 5365:
    case 5621:
    case 3829:
      return WEBKIT + value + value;
    case 5349:
    case 4246:
    case 4810:
    case 6968:
    case 2756:
      return WEBKIT + value + MOZ + value + MS + value + value;
    case 6828:
    case 4268:
      return WEBKIT + value + MS + value + value;
    case 6165:
      return WEBKIT + value + MS + "flex-" + value + value;
    case 5187:
      return WEBKIT + value + replace(value, /(\w+).+(:[^]+)/, WEBKIT + "box-$1$2" + MS + "flex-$1$2") + value;
    case 5443:
      return WEBKIT + value + MS + "flex-item-" + replace(value, /flex-|-self/, "") + value;
    case 4675:
      return WEBKIT + value + MS + "flex-line-pack" + replace(value, /align-content|flex-|-self/, "") + value;
    case 5548:
      return WEBKIT + value + MS + replace(value, "shrink", "negative") + value;
    case 5292:
      return WEBKIT + value + MS + replace(value, "basis", "preferred-size") + value;
    case 6060:
      return WEBKIT + "box-" + replace(value, "-grow", "") + WEBKIT + value + MS + replace(value, "grow", "positive") + value;
    case 4554:
      return WEBKIT + replace(value, /([^-])(transform)/g, "$1" + WEBKIT + "$2") + value;
    case 6187:
      return replace(replace(replace(value, /(zoom-|grab)/, WEBKIT + "$1"), /(image-set)/, WEBKIT + "$1"), value, "") + value;
    case 5495:
    case 3959:
      return replace(value, /(image-set\([^]*)/, WEBKIT + "$1$`$1");
    case 4968:
      return replace(replace(value, /(.+:)(flex-)?(.*)/, WEBKIT + "box-pack:$3" + MS + "flex-pack:$3"), /s.+-b[^;]+/, "justify") + WEBKIT + value + value;
    case 4095:
    case 3583:
    case 4068:
    case 2532:
      return replace(value, /(.+)-inline(.+)/, WEBKIT + "$1$2") + value;
    case 8116:
    case 7059:
    case 5753:
    case 5535:
    case 5445:
    case 5701:
    case 4933:
    case 4677:
    case 5533:
    case 5789:
    case 5021:
    case 4765:
      if (strlen(value) - 1 - length2 > 6)
        switch (charat(value, length2 + 1)) {
          case 109:
            if (charat(value, length2 + 4) !== 45)
              break;
          case 102:
            return replace(value, /(.+:)(.+)-([^]+)/, "$1" + WEBKIT + "$2-$3$1" + MOZ + (charat(value, length2 + 3) == 108 ? "$3" : "$2-$3")) + value;
          case 115:
            return ~indexof(value, "stretch") ? prefix2(replace(value, "stretch", "fill-available"), length2) + value : value;
        }
      break;
    case 4949:
      if (charat(value, length2 + 1) !== 115)
        break;
    case 6444:
      switch (charat(value, strlen(value) - 3 - (~indexof(value, "!important") && 10))) {
        case 107:
          return replace(value, ":", ":" + WEBKIT) + value;
        case 101:
          return replace(value, /(.+:)([^;!]+)(;|!.+)?/, "$1" + WEBKIT + (charat(value, 14) === 45 ? "inline-" : "") + "box$3$1" + WEBKIT + "$2$3$1" + MS + "$2box$3") + value;
      }
      break;
    case 5936:
      switch (charat(value, length2 + 11)) {
        case 114:
          return WEBKIT + value + MS + replace(value, /[svh]\w+-[tblr]{2}/, "tb") + value;
        case 108:
          return WEBKIT + value + MS + replace(value, /[svh]\w+-[tblr]{2}/, "tb-rl") + value;
        case 45:
          return WEBKIT + value + MS + replace(value, /[svh]\w+-[tblr]{2}/, "lr") + value;
      }
      return WEBKIT + value + MS + value + value;
  }
  return value;
}
var prefixer = function prefixer2(element, index6, children, callback) {
  if (element.length > -1) {
    if (!element["return"])
      switch (element.type) {
        case DECLARATION:
          element["return"] = prefix2(element.value, element.length);
          break;
        case KEYFRAMES:
          return serialize([copy(element, {
            value: replace(element.value, "@", "@" + WEBKIT)
          })], callback);
        case RULESET:
          if (element.length)
            return combine(element.props, function(value) {
              switch (match(value, /(::plac\w+|:read-\w+)/)) {
                case ":read-only":
                case ":read-write":
                  return serialize([copy(element, {
                    props: [replace(value, /:(read-\w+)/, ":" + MOZ + "$1")]
                  })], callback);
                case "::placeholder":
                  return serialize([copy(element, {
                    props: [replace(value, /:(plac\w+)/, ":" + WEBKIT + "input-$1")]
                  }), copy(element, {
                    props: [replace(value, /:(plac\w+)/, ":" + MOZ + "$1")]
                  }), copy(element, {
                    props: [replace(value, /:(plac\w+)/, MS + "input-$1")]
                  })], callback);
              }
              return "";
            });
      }
  }
};
var defaultStylisPlugins = [prefixer];
var createCache = function createCache2(options) {
  var key = options.key;
  if (!key) {
    throw new Error("You have to configure `key` for your cache. Please make sure it's unique (and not equal to 'css') as it's used for linking styles to your cache.\nIf multiple caches share the same key they might \"fight\" for each other's style elements.");
  }
  if (key === "css") {
    var ssrStyles = document.querySelectorAll("style[data-emotion]:not([data-s])");
    Array.prototype.forEach.call(ssrStyles, function(node2) {
      var dataEmotionAttribute = node2.getAttribute("data-emotion");
      if (dataEmotionAttribute.indexOf(" ") === -1) {
        return;
      }
      document.head.appendChild(node2);
      node2.setAttribute("data-s", "");
    });
  }
  var stylisPlugins = options.stylisPlugins || defaultStylisPlugins;
  if (true) {
    if (/[^a-z-]/.test(key)) {
      throw new Error('Emotion key must only contain lower case alphabetical characters and - but "' + key + '" was passed');
    }
  }
  var inserted = {};
  var container;
  var nodesToHydrate = [];
  {
    container = options.container || document.head;
    Array.prototype.forEach.call(
      // this means we will ignore elements which don't have a space in them which
      // means that the style elements we're looking at are only Emotion 11 server-rendered style elements
      document.querySelectorAll('style[data-emotion^="' + key + ' "]'),
      function(node2) {
        var attrib = node2.getAttribute("data-emotion").split(" ");
        for (var i = 1; i < attrib.length; i++) {
          inserted[attrib[i]] = true;
        }
        nodesToHydrate.push(node2);
      }
    );
  }
  var _insert;
  var omnipresentPlugins = [compat, removeLabel];
  if (true) {
    omnipresentPlugins.push(createUnsafeSelectorsAlarm({
      get compat() {
        return cache.compat;
      }
    }), incorrectImportAlarm);
  }
  {
    var currentSheet;
    var finalizingPlugins = [stringify, true ? function(element) {
      if (!element.root) {
        if (element["return"]) {
          currentSheet.insert(element["return"]);
        } else if (element.value && element.type !== COMMENT) {
          currentSheet.insert(element.value + "{}");
        }
      }
    } : rulesheet(function(rule) {
      currentSheet.insert(rule);
    })];
    var serializer = middleware(omnipresentPlugins.concat(stylisPlugins, finalizingPlugins));
    var stylis = function stylis2(styles2) {
      return serialize(compile(styles2), serializer);
    };
    _insert = function insert(selector, serialized, sheet, shouldCache) {
      currentSheet = sheet;
      if (serialized.map !== void 0) {
        currentSheet = {
          insert: function insert2(rule) {
            sheet.insert(rule + serialized.map);
          }
        };
      }
      stylis(selector ? selector + "{" + serialized.styles + "}" : serialized.styles);
      if (shouldCache) {
        cache.inserted[serialized.name] = true;
      }
    };
  }
  var cache = {
    key,
    sheet: new StyleSheet({
      key,
      container,
      nonce: options.nonce,
      speedy: options.speedy,
      prepend: options.prepend,
      insertionPoint: options.insertionPoint
    }),
    nonce: options.nonce,
    inserted,
    registered: {},
    insert: _insert
  };
  cache.sheet.hydrate(nodesToHydrate);
  return cache;
};

// node_modules/@babel/runtime/helpers/esm/extends.js
function _extends() {
  _extends = Object.assign ? Object.assign.bind() : function(target) {
    for (var i = 1; i < arguments.length; i++) {
      var source = arguments[i];
      for (var key in source) {
        if (Object.prototype.hasOwnProperty.call(source, key)) {
          target[key] = source[key];
        }
      }
    }
    return target;
  };
  return _extends.apply(this, arguments);
}

// node_modules/@emotion/react/_isolated-hnrs/dist/emotion-react-_isolated-hnrs.browser.esm.js
var import_hoist_non_react_statics = __toESM(require_hoist_non_react_statics_cjs());

// node_modules/@emotion/utils/dist/emotion-utils.browser.esm.js
var isBrowser = true;
function getRegisteredStyles(registered, registeredStyles, classNames) {
  var rawClassName = "";
  classNames.split(" ").forEach(function(className) {
    if (registered[className] !== void 0) {
      registeredStyles.push(registered[className] + ";");
    } else {
      rawClassName += className + " ";
    }
  });
  return rawClassName;
}
var registerStyles = function registerStyles2(cache, serialized, isStringTag) {
  var className = cache.key + "-" + serialized.name;
  if (
    // we only need to add the styles to the registered cache if the
    // class name could be used further down
    // the tree but if it's a string tag, we know it won't
    // so we don't have to add it to registered cache.
    // this improves memory usage since we can avoid storing the whole style string
    (isStringTag === false || // we need to always store it if we're in compat mode and
    // in node since emotion-server relies on whether a style is in
    // the registered cache to know whether a style is global or not
    // also, note that this check will be dead code eliminated in the browser
    isBrowser === false) && cache.registered[className] === void 0
  ) {
    cache.registered[className] = serialized.styles;
  }
};
var insertStyles = function insertStyles2(cache, serialized, isStringTag) {
  registerStyles(cache, serialized, isStringTag);
  var className = cache.key + "-" + serialized.name;
  if (cache.inserted[serialized.name] === void 0) {
    var current = serialized;
    do {
      cache.insert(serialized === current ? "." + className : "", current, cache.sheet, true);
      current = current.next;
    } while (current !== void 0);
  }
};

// node_modules/@emotion/hash/dist/emotion-hash.esm.js
function murmur2(str) {
  var h = 0;
  var k2, i = 0, len = str.length;
  for (; len >= 4; ++i, len -= 4) {
    k2 = str.charCodeAt(i) & 255 | (str.charCodeAt(++i) & 255) << 8 | (str.charCodeAt(++i) & 255) << 16 | (str.charCodeAt(++i) & 255) << 24;
    k2 = /* Math.imul(k, m): */
    (k2 & 65535) * 1540483477 + ((k2 >>> 16) * 59797 << 16);
    k2 ^= /* k >>> r: */
    k2 >>> 24;
    h = /* Math.imul(k, m): */
    (k2 & 65535) * 1540483477 + ((k2 >>> 16) * 59797 << 16) ^ /* Math.imul(h, m): */
    (h & 65535) * 1540483477 + ((h >>> 16) * 59797 << 16);
  }
  switch (len) {
    case 3:
      h ^= (str.charCodeAt(i + 2) & 255) << 16;
    case 2:
      h ^= (str.charCodeAt(i + 1) & 255) << 8;
    case 1:
      h ^= str.charCodeAt(i) & 255;
      h = /* Math.imul(h, m): */
      (h & 65535) * 1540483477 + ((h >>> 16) * 59797 << 16);
  }
  h ^= h >>> 13;
  h = /* Math.imul(h, m): */
  (h & 65535) * 1540483477 + ((h >>> 16) * 59797 << 16);
  return ((h ^ h >>> 15) >>> 0).toString(36);
}

// node_modules/@emotion/unitless/dist/emotion-unitless.esm.js
var unitlessKeys = {
  animationIterationCount: 1,
  aspectRatio: 1,
  borderImageOutset: 1,
  borderImageSlice: 1,
  borderImageWidth: 1,
  boxFlex: 1,
  boxFlexGroup: 1,
  boxOrdinalGroup: 1,
  columnCount: 1,
  columns: 1,
  flex: 1,
  flexGrow: 1,
  flexPositive: 1,
  flexShrink: 1,
  flexNegative: 1,
  flexOrder: 1,
  gridRow: 1,
  gridRowEnd: 1,
  gridRowSpan: 1,
  gridRowStart: 1,
  gridColumn: 1,
  gridColumnEnd: 1,
  gridColumnSpan: 1,
  gridColumnStart: 1,
  msGridRow: 1,
  msGridRowSpan: 1,
  msGridColumn: 1,
  msGridColumnSpan: 1,
  fontWeight: 1,
  lineHeight: 1,
  opacity: 1,
  order: 1,
  orphans: 1,
  tabSize: 1,
  widows: 1,
  zIndex: 1,
  zoom: 1,
  WebkitLineClamp: 1,
  // SVG-related properties
  fillOpacity: 1,
  floodOpacity: 1,
  stopOpacity: 1,
  strokeDasharray: 1,
  strokeDashoffset: 1,
  strokeMiterlimit: 1,
  strokeOpacity: 1,
  strokeWidth: 1
};

// node_modules/@emotion/serialize/dist/emotion-serialize.browser.esm.js
var ILLEGAL_ESCAPE_SEQUENCE_ERROR = `You have illegal escape sequence in your template literal, most likely inside content's property value.
Because you write your CSS inside a JavaScript string you actually have to do double escaping, so for example "content: '\\00d7';" should become "content: '\\\\00d7';".
You can read more about this here:
https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Template_literals#ES2018_revision_of_illegal_escape_sequences`;
var UNDEFINED_AS_OBJECT_KEY_ERROR = "You have passed in falsy value as style object's key (can happen when in example you pass unexported component as computed key).";
var hyphenateRegex = /[A-Z]|^ms/g;
var animationRegex = /_EMO_([^_]+?)_([^]*?)_EMO_/g;
var isCustomProperty = function isCustomProperty2(property) {
  return property.charCodeAt(1) === 45;
};
var isProcessableValue = function isProcessableValue2(value) {
  return value != null && typeof value !== "boolean";
};
var processStyleName = memoize(function(styleName) {
  return isCustomProperty(styleName) ? styleName : styleName.replace(hyphenateRegex, "-$&").toLowerCase();
});
var processStyleValue = function processStyleValue2(key, value) {
  switch (key) {
    case "animation":
    case "animationName": {
      if (typeof value === "string") {
        return value.replace(animationRegex, function(match2, p1, p2) {
          cursor = {
            name: p1,
            styles: p2,
            next: cursor
          };
          return p1;
        });
      }
    }
  }
  if (unitlessKeys[key] !== 1 && !isCustomProperty(key) && typeof value === "number" && value !== 0) {
    return value + "px";
  }
  return value;
};
if (true) {
  contentValuePattern = /(var|attr|counters?|url|element|(((repeating-)?(linear|radial))|conic)-gradient)\(|(no-)?(open|close)-quote/;
  contentValues = ["normal", "none", "initial", "inherit", "unset"];
  oldProcessStyleValue = processStyleValue;
  msPattern = /^-ms-/;
  hyphenPattern = /-(.)/g;
  hyphenatedCache = {};
  processStyleValue = function processStyleValue3(key, value) {
    if (key === "content") {
      if (typeof value !== "string" || contentValues.indexOf(value) === -1 && !contentValuePattern.test(value) && (value.charAt(0) !== value.charAt(value.length - 1) || value.charAt(0) !== '"' && value.charAt(0) !== "'")) {
        throw new Error("You seem to be using a value for 'content' without quotes, try replacing it with `content: '\"" + value + "\"'`");
      }
    }
    var processed = oldProcessStyleValue(key, value);
    if (processed !== "" && !isCustomProperty(key) && key.indexOf("-") !== -1 && hyphenatedCache[key] === void 0) {
      hyphenatedCache[key] = true;
      console.error("Using kebab-case for css properties in objects is not supported. Did you mean " + key.replace(msPattern, "ms-").replace(hyphenPattern, function(str, _char) {
        return _char.toUpperCase();
      }) + "?");
    }
    return processed;
  };
}
var contentValuePattern;
var contentValues;
var oldProcessStyleValue;
var msPattern;
var hyphenPattern;
var hyphenatedCache;
var noComponentSelectorMessage = "Component selectors can only be used in conjunction with @emotion/babel-plugin, the swc Emotion plugin, or another Emotion-aware compiler transform.";
function handleInterpolation(mergedProps, registered, interpolation) {
  if (interpolation == null) {
    return "";
  }
  if (interpolation.__emotion_styles !== void 0) {
    if (interpolation.toString() === "NO_COMPONENT_SELECTOR") {
      throw new Error(noComponentSelectorMessage);
    }
    return interpolation;
  }
  switch (typeof interpolation) {
    case "boolean": {
      return "";
    }
    case "object": {
      if (interpolation.anim === 1) {
        cursor = {
          name: interpolation.name,
          styles: interpolation.styles,
          next: cursor
        };
        return interpolation.name;
      }
      if (interpolation.styles !== void 0) {
        var next2 = interpolation.next;
        if (next2 !== void 0) {
          while (next2 !== void 0) {
            cursor = {
              name: next2.name,
              styles: next2.styles,
              next: cursor
            };
            next2 = next2.next;
          }
        }
        var styles2 = interpolation.styles + ";";
        if (interpolation.map !== void 0) {
          styles2 += interpolation.map;
        }
        return styles2;
      }
      return createStringFromObject(mergedProps, registered, interpolation);
    }
    case "function": {
      if (mergedProps !== void 0) {
        var previousCursor = cursor;
        var result = interpolation(mergedProps);
        cursor = previousCursor;
        return handleInterpolation(mergedProps, registered, result);
      } else if (true) {
        console.error("Functions that are interpolated in css calls will be stringified.\nIf you want to have a css call based on props, create a function that returns a css call like this\nlet dynamicStyle = (props) => css`color: ${props.color}`\nIt can be called directly with props or interpolated in a styled call like this\nlet SomeComponent = styled('div')`${dynamicStyle}`");
      }
      break;
    }
    case "string":
      if (true) {
        var matched = [];
        var replaced = interpolation.replace(animationRegex, function(match2, p1, p2) {
          var fakeVarName = "animation" + matched.length;
          matched.push("const " + fakeVarName + " = keyframes`" + p2.replace(/^@keyframes animation-\w+/, "") + "`");
          return "${" + fakeVarName + "}";
        });
        if (matched.length) {
          console.error("`keyframes` output got interpolated into plain string, please wrap it with `css`.\n\nInstead of doing this:\n\n" + [].concat(matched, ["`" + replaced + "`"]).join("\n") + "\n\nYou should wrap it with `css` like this:\n\n" + ("css`" + replaced + "`"));
        }
      }
      break;
  }
  if (registered == null) {
    return interpolation;
  }
  var cached = registered[interpolation];
  return cached !== void 0 ? cached : interpolation;
}
function createStringFromObject(mergedProps, registered, obj) {
  var string = "";
  if (Array.isArray(obj)) {
    for (var i = 0; i < obj.length; i++) {
      string += handleInterpolation(mergedProps, registered, obj[i]) + ";";
    }
  } else {
    for (var _key in obj) {
      var value = obj[_key];
      if (typeof value !== "object") {
        if (registered != null && registered[value] !== void 0) {
          string += _key + "{" + registered[value] + "}";
        } else if (isProcessableValue(value)) {
          string += processStyleName(_key) + ":" + processStyleValue(_key, value) + ";";
        }
      } else {
        if (_key === "NO_COMPONENT_SELECTOR" && true) {
          throw new Error(noComponentSelectorMessage);
        }
        if (Array.isArray(value) && typeof value[0] === "string" && (registered == null || registered[value[0]] === void 0)) {
          for (var _i = 0; _i < value.length; _i++) {
            if (isProcessableValue(value[_i])) {
              string += processStyleName(_key) + ":" + processStyleValue(_key, value[_i]) + ";";
            }
          }
        } else {
          var interpolated = handleInterpolation(mergedProps, registered, value);
          switch (_key) {
            case "animation":
            case "animationName": {
              string += processStyleName(_key) + ":" + interpolated + ";";
              break;
            }
            default: {
              if (_key === "undefined") {
                console.error(UNDEFINED_AS_OBJECT_KEY_ERROR);
              }
              string += _key + "{" + interpolated + "}";
            }
          }
        }
      }
    }
  }
  return string;
}
var labelPattern = /label:\s*([^\s;\n{]+)\s*(;|$)/g;
var sourceMapPattern;
if (true) {
  sourceMapPattern = /\/\*#\ssourceMappingURL=data:application\/json;\S+\s+\*\//g;
}
var cursor;
var serializeStyles = function serializeStyles2(args, registered, mergedProps) {
  if (args.length === 1 && typeof args[0] === "object" && args[0] !== null && args[0].styles !== void 0) {
    return args[0];
  }
  var stringMode = true;
  var styles2 = "";
  cursor = void 0;
  var strings = args[0];
  if (strings == null || strings.raw === void 0) {
    stringMode = false;
    styles2 += handleInterpolation(mergedProps, registered, strings);
  } else {
    if (strings[0] === void 0) {
      console.error(ILLEGAL_ESCAPE_SEQUENCE_ERROR);
    }
    styles2 += strings[0];
  }
  for (var i = 1; i < args.length; i++) {
    styles2 += handleInterpolation(mergedProps, registered, args[i]);
    if (stringMode) {
      if (strings[i] === void 0) {
        console.error(ILLEGAL_ESCAPE_SEQUENCE_ERROR);
      }
      styles2 += strings[i];
    }
  }
  var sourceMap;
  if (true) {
    styles2 = styles2.replace(sourceMapPattern, function(match3) {
      sourceMap = match3;
      return "";
    });
  }
  labelPattern.lastIndex = 0;
  var identifierName = "";
  var match2;
  while ((match2 = labelPattern.exec(styles2)) !== null) {
    identifierName += "-" + // $FlowFixMe we know it's not null
    match2[1];
  }
  var name = murmur2(styles2) + identifierName;
  if (true) {
    return {
      name,
      styles: styles2,
      map: sourceMap,
      next: cursor,
      toString: function toString() {
        return "You have tried to stringify object returned from `css` function. It isn't supposed to be used directly (e.g. as value of the `className` prop), but rather handed to emotion so it can handle it (e.g. as value of `css` prop).";
      }
    };
  }
  return {
    name,
    styles: styles2,
    next: cursor
  };
};

// node_modules/@emotion/use-insertion-effect-with-fallbacks/dist/emotion-use-insertion-effect-with-fallbacks.browser.esm.js
var React3 = __toESM(require_react());
var syncFallback = function syncFallback2(create) {
  return create();
};
var useInsertionEffect2 = React3["useInsertionEffect"] ? React3["useInsertionEffect"] : false;
var useInsertionEffectAlwaysWithSyncFallback = useInsertionEffect2 || syncFallback;
var useInsertionEffectWithLayoutFallback = useInsertionEffect2 || React3.useLayoutEffect;

// node_modules/@emotion/react/dist/emotion-element-c39617d8.browser.esm.js
var isBrowser2 = true;
var hasOwnProperty = {}.hasOwnProperty;
var EmotionCacheContext = React4.createContext(
  // we're doing this to avoid preconstruct's dead code elimination in this one case
  // because this module is primarily intended for the browser and node
  // but it's also required in react native and similar environments sometimes
  // and we could have a special build just for that
  // but this is much easier and the native packages
  // might use a different theme context in the future anyway
  typeof HTMLElement !== "undefined" ? createCache({
    key: "css"
  }) : null
);
if (true) {
  EmotionCacheContext.displayName = "EmotionCacheContext";
}
var CacheProvider = EmotionCacheContext.Provider;
var withEmotionCache = function withEmotionCache2(func) {
  return (0, import_react5.forwardRef)(function(props, ref) {
    var cache = (0, import_react5.useContext)(EmotionCacheContext);
    return func(props, cache, ref);
  });
};
if (!isBrowser2) {
  withEmotionCache = function withEmotionCache3(func) {
    return function(props) {
      var cache = (0, import_react5.useContext)(EmotionCacheContext);
      if (cache === null) {
        cache = createCache({
          key: "css"
        });
        return React4.createElement(EmotionCacheContext.Provider, {
          value: cache
        }, func(props, cache));
      } else {
        return func(props, cache);
      }
    };
  };
}
var ThemeContext = React4.createContext({});
if (true) {
  ThemeContext.displayName = "EmotionThemeContext";
}
var getTheme = function getTheme2(outerTheme, theme) {
  if (typeof theme === "function") {
    var mergedTheme = theme(outerTheme);
    if (mergedTheme == null || typeof mergedTheme !== "object" || Array.isArray(mergedTheme)) {
      throw new Error("[ThemeProvider] Please return an object from your theme function, i.e. theme={() => ({})}!");
    }
    return mergedTheme;
  }
  if (theme == null || typeof theme !== "object" || Array.isArray(theme)) {
    throw new Error("[ThemeProvider] Please make your theme prop a plain object");
  }
  return _extends({}, outerTheme, theme);
};
var createCacheWithTheme = weakMemoize(function(outerTheme) {
  return weakMemoize(function(theme) {
    return getTheme(outerTheme, theme);
  });
});
var ThemeProvider = function ThemeProvider2(props) {
  var theme = React4.useContext(ThemeContext);
  if (props.theme !== theme) {
    theme = createCacheWithTheme(theme)(props.theme);
  }
  return React4.createElement(ThemeContext.Provider, {
    value: theme
  }, props.children);
};
var typePropName = "__EMOTION_TYPE_PLEASE_DO_NOT_USE__";
var labelPropName = "__EMOTION_LABEL_PLEASE_DO_NOT_USE__";
var Insertion = function Insertion2(_ref) {
  var cache = _ref.cache, serialized = _ref.serialized, isStringTag = _ref.isStringTag;
  registerStyles(cache, serialized, isStringTag);
  useInsertionEffectAlwaysWithSyncFallback(function() {
    return insertStyles(cache, serialized, isStringTag);
  });
  return null;
};
var Emotion = withEmotionCache(function(props, cache, ref) {
  var cssProp = props.css;
  if (typeof cssProp === "string" && cache.registered[cssProp] !== void 0) {
    cssProp = cache.registered[cssProp];
  }
  var WrappedComponent = props[typePropName];
  var registeredStyles = [cssProp];
  var className = "";
  if (typeof props.className === "string") {
    className = getRegisteredStyles(cache.registered, registeredStyles, props.className);
  } else if (props.className != null) {
    className = props.className + " ";
  }
  var serialized = serializeStyles(registeredStyles, void 0, React4.useContext(ThemeContext));
  if (serialized.name.indexOf("-") === -1) {
    var labelFromStack = props[labelPropName];
    if (labelFromStack) {
      serialized = serializeStyles([serialized, "label:" + labelFromStack + ";"]);
    }
  }
  className += cache.key + "-" + serialized.name;
  var newProps = {};
  for (var key in props) {
    if (hasOwnProperty.call(props, key) && key !== "css" && key !== typePropName && key !== labelPropName) {
      newProps[key] = props[key];
    }
  }
  newProps.ref = ref;
  newProps.className = className;
  return React4.createElement(React4.Fragment, null, React4.createElement(Insertion, {
    cache,
    serialized,
    isStringTag: typeof WrappedComponent === "string"
  }), React4.createElement(WrappedComponent, newProps));
});
if (true) {
  Emotion.displayName = "EmotionCssPropInternal";
}

// node_modules/@emotion/react/dist/emotion-react.browser.esm.js
var React5 = __toESM(require_react());
var import_hoist_non_react_statics2 = __toESM(require_hoist_non_react_statics_cjs());
var pkg = {
  name: "@emotion/react",
  version: "11.11.1",
  main: "dist/emotion-react.cjs.js",
  module: "dist/emotion-react.esm.js",
  browser: {
    "./dist/emotion-react.esm.js": "./dist/emotion-react.browser.esm.js"
  },
  exports: {
    ".": {
      module: {
        worker: "./dist/emotion-react.worker.esm.js",
        browser: "./dist/emotion-react.browser.esm.js",
        "default": "./dist/emotion-react.esm.js"
      },
      "import": "./dist/emotion-react.cjs.mjs",
      "default": "./dist/emotion-react.cjs.js"
    },
    "./jsx-runtime": {
      module: {
        worker: "./jsx-runtime/dist/emotion-react-jsx-runtime.worker.esm.js",
        browser: "./jsx-runtime/dist/emotion-react-jsx-runtime.browser.esm.js",
        "default": "./jsx-runtime/dist/emotion-react-jsx-runtime.esm.js"
      },
      "import": "./jsx-runtime/dist/emotion-react-jsx-runtime.cjs.mjs",
      "default": "./jsx-runtime/dist/emotion-react-jsx-runtime.cjs.js"
    },
    "./_isolated-hnrs": {
      module: {
        worker: "./_isolated-hnrs/dist/emotion-react-_isolated-hnrs.worker.esm.js",
        browser: "./_isolated-hnrs/dist/emotion-react-_isolated-hnrs.browser.esm.js",
        "default": "./_isolated-hnrs/dist/emotion-react-_isolated-hnrs.esm.js"
      },
      "import": "./_isolated-hnrs/dist/emotion-react-_isolated-hnrs.cjs.mjs",
      "default": "./_isolated-hnrs/dist/emotion-react-_isolated-hnrs.cjs.js"
    },
    "./jsx-dev-runtime": {
      module: {
        worker: "./jsx-dev-runtime/dist/emotion-react-jsx-dev-runtime.worker.esm.js",
        browser: "./jsx-dev-runtime/dist/emotion-react-jsx-dev-runtime.browser.esm.js",
        "default": "./jsx-dev-runtime/dist/emotion-react-jsx-dev-runtime.esm.js"
      },
      "import": "./jsx-dev-runtime/dist/emotion-react-jsx-dev-runtime.cjs.mjs",
      "default": "./jsx-dev-runtime/dist/emotion-react-jsx-dev-runtime.cjs.js"
    },
    "./package.json": "./package.json",
    "./types/css-prop": "./types/css-prop.d.ts",
    "./macro": {
      types: {
        "import": "./macro.d.mts",
        "default": "./macro.d.ts"
      },
      "default": "./macro.js"
    }
  },
  types: "types/index.d.ts",
  files: [
    "src",
    "dist",
    "jsx-runtime",
    "jsx-dev-runtime",
    "_isolated-hnrs",
    "types/*.d.ts",
    "macro.*"
  ],
  sideEffects: false,
  author: "Emotion Contributors",
  license: "MIT",
  scripts: {
    "test:typescript": "dtslint types"
  },
  dependencies: {
    "@babel/runtime": "^7.18.3",
    "@emotion/babel-plugin": "^11.11.0",
    "@emotion/cache": "^11.11.0",
    "@emotion/serialize": "^1.1.2",
    "@emotion/use-insertion-effect-with-fallbacks": "^1.0.1",
    "@emotion/utils": "^1.2.1",
    "@emotion/weak-memoize": "^0.3.1",
    "hoist-non-react-statics": "^3.3.1"
  },
  peerDependencies: {
    react: ">=16.8.0"
  },
  peerDependenciesMeta: {
    "@types/react": {
      optional: true
    }
  },
  devDependencies: {
    "@definitelytyped/dtslint": "0.0.112",
    "@emotion/css": "11.11.0",
    "@emotion/css-prettifier": "1.1.3",
    "@emotion/server": "11.11.0",
    "@emotion/styled": "11.11.0",
    "html-tag-names": "^1.1.2",
    react: "16.14.0",
    "svg-tag-names": "^1.1.1",
    typescript: "^4.5.5"
  },
  repository: "https://github.com/emotion-js/emotion/tree/main/packages/react",
  publishConfig: {
    access: "public"
  },
  "umd:main": "dist/emotion-react.umd.min.js",
  preconstruct: {
    entrypoints: [
      "./index.js",
      "./jsx-runtime.js",
      "./jsx-dev-runtime.js",
      "./_isolated-hnrs.js"
    ],
    umdName: "emotionReact",
    exports: {
      envConditions: [
        "browser",
        "worker"
      ],
      extra: {
        "./types/css-prop": "./types/css-prop.d.ts",
        "./macro": {
          types: {
            "import": "./macro.d.mts",
            "default": "./macro.d.ts"
          },
          "default": "./macro.js"
        }
      }
    }
  }
};
var warnedAboutCssPropForGlobal = false;
var Global = withEmotionCache(function(props, cache) {
  if (!warnedAboutCssPropForGlobal && // check for className as well since the user is
  // probably using the custom createElement which
  // means it will be turned into a className prop
  // $FlowFixMe I don't really want to add it to the type since it shouldn't be used
  (props.className || props.css)) {
    console.error("It looks like you're using the css prop on Global, did you mean to use the styles prop instead?");
    warnedAboutCssPropForGlobal = true;
  }
  var styles2 = props.styles;
  var serialized = serializeStyles([styles2], void 0, React5.useContext(ThemeContext));
  if (!isBrowser2) {
    var _ref;
    var serializedNames = serialized.name;
    var serializedStyles = serialized.styles;
    var next2 = serialized.next;
    while (next2 !== void 0) {
      serializedNames += " " + next2.name;
      serializedStyles += next2.styles;
      next2 = next2.next;
    }
    var shouldCache = cache.compat === true;
    var rules = cache.insert("", {
      name: serializedNames,
      styles: serializedStyles
    }, cache.sheet, shouldCache);
    if (shouldCache) {
      return null;
    }
    return React5.createElement("style", (_ref = {}, _ref["data-emotion"] = cache.key + "-global " + serializedNames, _ref.dangerouslySetInnerHTML = {
      __html: rules
    }, _ref.nonce = cache.sheet.nonce, _ref));
  }
  var sheetRef = React5.useRef();
  useInsertionEffectWithLayoutFallback(function() {
    var key = cache.key + "-global";
    var sheet = new cache.sheet.constructor({
      key,
      nonce: cache.sheet.nonce,
      container: cache.sheet.container,
      speedy: cache.sheet.isSpeedy
    });
    var rehydrating = false;
    var node2 = document.querySelector('style[data-emotion="' + key + " " + serialized.name + '"]');
    if (cache.sheet.tags.length) {
      sheet.before = cache.sheet.tags[0];
    }
    if (node2 !== null) {
      rehydrating = true;
      node2.setAttribute("data-emotion", key);
      sheet.hydrate([node2]);
    }
    sheetRef.current = [sheet, rehydrating];
    return function() {
      sheet.flush();
    };
  }, [cache]);
  useInsertionEffectWithLayoutFallback(function() {
    var sheetRefCurrent = sheetRef.current;
    var sheet = sheetRefCurrent[0], rehydrating = sheetRefCurrent[1];
    if (rehydrating) {
      sheetRefCurrent[1] = false;
      return;
    }
    if (serialized.next !== void 0) {
      insertStyles(cache, serialized.next, true);
    }
    if (sheet.tags.length) {
      var element = sheet.tags[sheet.tags.length - 1].nextElementSibling;
      sheet.before = element;
      sheet.flush();
    }
    cache.insert("", serialized, sheet, false);
  }, [cache, serialized.name]);
  return null;
});
if (true) {
  Global.displayName = "EmotionGlobal";
}
function css() {
  for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
    args[_key] = arguments[_key];
  }
  return serializeStyles(args);
}
var keyframes = function keyframes2() {
  var insertable = css.apply(void 0, arguments);
  var name = "animation-" + insertable.name;
  return {
    name,
    styles: "@keyframes " + name + "{" + insertable.styles + "}",
    anim: 1,
    toString: function toString() {
      return "_EMO_" + this.name + "_" + this.styles + "_EMO_";
    }
  };
};
var classnames = function classnames2(args) {
  var len = args.length;
  var i = 0;
  var cls = "";
  for (; i < len; i++) {
    var arg = args[i];
    if (arg == null)
      continue;
    var toAdd = void 0;
    switch (typeof arg) {
      case "boolean":
        break;
      case "object": {
        if (Array.isArray(arg)) {
          toAdd = classnames2(arg);
        } else {
          if (arg.styles !== void 0 && arg.name !== void 0) {
            console.error("You have passed styles created with `css` from `@emotion/react` package to the `cx`.\n`cx` is meant to compose class names (strings) so you should convert those styles to a class name by passing them to the `css` received from <ClassNames/> component.");
          }
          toAdd = "";
          for (var k2 in arg) {
            if (arg[k2] && k2) {
              toAdd && (toAdd += " ");
              toAdd += k2;
            }
          }
        }
        break;
      }
      default: {
        toAdd = arg;
      }
    }
    if (toAdd) {
      cls && (cls += " ");
      cls += toAdd;
    }
  }
  return cls;
};
function merge(registered, css2, className) {
  var registeredStyles = [];
  var rawClassName = getRegisteredStyles(registered, registeredStyles, className);
  if (registeredStyles.length < 2) {
    return className;
  }
  return rawClassName + css2(registeredStyles);
}
var Insertion3 = function Insertion4(_ref) {
  var cache = _ref.cache, serializedArr = _ref.serializedArr;
  useInsertionEffectAlwaysWithSyncFallback(function() {
    for (var i = 0; i < serializedArr.length; i++) {
      insertStyles(cache, serializedArr[i], false);
    }
  });
  return null;
};
var ClassNames = withEmotionCache(function(props, cache) {
  var hasRendered = false;
  var serializedArr = [];
  var css2 = function css3() {
    if (hasRendered && true) {
      throw new Error("css can only be used during render");
    }
    for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
      args[_key] = arguments[_key];
    }
    var serialized = serializeStyles(args, cache.registered);
    serializedArr.push(serialized);
    registerStyles(cache, serialized, false);
    return cache.key + "-" + serialized.name;
  };
  var cx = function cx2() {
    if (hasRendered && true) {
      throw new Error("cx can only be used during render");
    }
    for (var _len2 = arguments.length, args = new Array(_len2), _key2 = 0; _key2 < _len2; _key2++) {
      args[_key2] = arguments[_key2];
    }
    return merge(cache.registered, css2, classnames(args));
  };
  var content = {
    css: css2,
    cx,
    theme: React5.useContext(ThemeContext)
  };
  var ele = props.children(content);
  hasRendered = true;
  return React5.createElement(React5.Fragment, null, React5.createElement(Insertion3, {
    cache,
    serializedArr
  }), ele);
});
if (true) {
  ClassNames.displayName = "EmotionClassNames";
}
if (true) {
  isBrowser5 = true;
  isTestEnv = typeof jest !== "undefined" || typeof vi !== "undefined";
  if (isBrowser5 && !isTestEnv) {
    globalContext = // $FlowIgnore
    typeof globalThis !== "undefined" ? globalThis : isBrowser5 ? window : global;
    globalKey = "__EMOTION_REACT_" + pkg.version.split(".")[0] + "__";
    if (globalContext[globalKey]) {
      console.warn("You are loading @emotion/react when it is already loaded. Running multiple instances may cause problems. This can happen if multiple versions are used, or if multiple builds of the same version are used.");
    }
    globalContext[globalKey] = true;
  }
}
var isBrowser5;
var isTestEnv;
var globalContext;
var globalKey;

// node_modules/@mantine/styles/esm/theme/GlobalStyles.js
var import_react6 = __toESM(require_react());
var __defProp3 = Object.defineProperty;
var __defProps2 = Object.defineProperties;
var __getOwnPropDescs2 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols3 = Object.getOwnPropertySymbols;
var __hasOwnProp3 = Object.prototype.hasOwnProperty;
var __propIsEnum3 = Object.prototype.propertyIsEnumerable;
var __defNormalProp3 = (obj, key, value) => key in obj ? __defProp3(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues3 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp3.call(b2, prop))
      __defNormalProp3(a2, prop, b2[prop]);
  if (__getOwnPropSymbols3)
    for (var prop of __getOwnPropSymbols3(b2)) {
      if (__propIsEnum3.call(b2, prop))
        __defNormalProp3(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps2 = (a2, b2) => __defProps2(a2, __getOwnPropDescs2(b2));
function GlobalStyles({ theme }) {
  return import_react6.default.createElement(Global, {
    styles: {
      "*, *::before, *::after": {
        boxSizing: "border-box"
      },
      html: {
        colorScheme: theme.colorScheme === "dark" ? "dark" : "light"
      },
      body: __spreadProps2(__spreadValues3({}, theme.fn.fontStyles()), {
        backgroundColor: theme.colorScheme === "dark" ? theme.colors.dark[7] : theme.white,
        color: theme.colorScheme === "dark" ? theme.colors.dark[0] : theme.black,
        lineHeight: theme.lineHeight,
        fontSize: theme.fontSizes.md,
        WebkitFontSmoothing: "antialiased",
        MozOsxFontSmoothing: "grayscale"
      })
    }
  });
}

// node_modules/@mantine/styles/esm/theme/MantineCssVariables.js
var import_react8 = __toESM(require_react());
function assignSizeVariables(variables, sizes24, name) {
  Object.keys(sizes24).forEach((size3) => {
    variables[`--mantine-${name}-${size3}`] = typeof sizes24[size3] === "number" ? `${sizes24[size3]}px` : sizes24[size3];
  });
}
function MantineCssVariables({ theme }) {
  const variables = {
    "--mantine-color-white": theme.white,
    "--mantine-color-black": theme.black,
    "--mantine-transition-timing-function": theme.transitionTimingFunction,
    "--mantine-line-height": `${theme.lineHeight}`,
    "--mantine-font-family": theme.fontFamily,
    "--mantine-font-family-monospace": theme.fontFamilyMonospace,
    "--mantine-font-family-headings": theme.headings.fontFamily,
    "--mantine-heading-font-weight": `${theme.headings.fontWeight}`
  };
  assignSizeVariables(variables, theme.shadows, "shadow");
  assignSizeVariables(variables, theme.fontSizes, "font-size");
  assignSizeVariables(variables, theme.radius, "radius");
  assignSizeVariables(variables, theme.spacing, "spacing");
  Object.keys(theme.colors).forEach((color) => {
    theme.colors[color].forEach((shade, index6) => {
      variables[`--mantine-color-${color}-${index6}`] = shade;
    });
  });
  const headings = theme.headings.sizes;
  Object.keys(headings).forEach((heading) => {
    variables[`--mantine-${heading}-font-size`] = `${headings[heading].fontSize}px`;
    variables[`--mantine-${heading}-line-height`] = `${headings[heading].lineHeight}`;
  });
  return import_react8.default.createElement(Global, {
    styles: {
      ":root": variables
    }
  });
}

// node_modules/@mantine/styles/esm/theme/utils/merge-theme/merge-theme.js
var __defProp4 = Object.defineProperty;
var __defProps3 = Object.defineProperties;
var __getOwnPropDescs3 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols4 = Object.getOwnPropertySymbols;
var __hasOwnProp4 = Object.prototype.hasOwnProperty;
var __propIsEnum4 = Object.prototype.propertyIsEnumerable;
var __defNormalProp4 = (obj, key, value) => key in obj ? __defProp4(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues4 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp4.call(b2, prop))
      __defNormalProp4(a2, prop, b2[prop]);
  if (__getOwnPropSymbols4)
    for (var prop of __getOwnPropSymbols4(b2)) {
      if (__propIsEnum4.call(b2, prop))
        __defNormalProp4(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps3 = (a2, b2) => __defProps3(a2, __getOwnPropDescs3(b2));
function mergeTheme(currentTheme, themeOverride) {
  if (!themeOverride) {
    return currentTheme;
  }
  const result = Object.keys(currentTheme).reduce((acc, key) => {
    if (key === "headings" && themeOverride.headings) {
      const sizes24 = themeOverride.headings.sizes ? Object.keys(currentTheme.headings.sizes).reduce((headingsAcc, h) => {
        headingsAcc[h] = __spreadValues4(__spreadValues4({}, currentTheme.headings.sizes[h]), themeOverride.headings.sizes[h]);
        return headingsAcc;
      }, {}) : currentTheme.headings.sizes;
      return __spreadProps3(__spreadValues4({}, acc), {
        headings: __spreadProps3(__spreadValues4(__spreadValues4({}, currentTheme.headings), themeOverride.headings), {
          sizes: sizes24
        })
      });
    }
    acc[key] = typeof themeOverride[key] === "object" ? __spreadValues4(__spreadValues4({}, currentTheme[key]), themeOverride[key]) : typeof themeOverride[key] === "number" || typeof themeOverride[key] === "boolean" || typeof themeOverride[key] === "function" ? themeOverride[key] : themeOverride[key] || currentTheme[key];
    return acc;
  }, {});
  if (!(result.primaryColor in result.colors)) {
    throw new Error("MantineProvider: Invalid theme.primaryColor, it accepts only key of theme.colors, learn more – https://mantine.dev/theming/colors/#primary-color");
  }
  return result;
}
function mergeThemeWithFunctions(currentTheme, themeOverride) {
  return attachFunctions(mergeTheme(currentTheme, themeOverride));
}

// node_modules/@mantine/styles/esm/theme/utils/filter-props/filter-props.js
function filterProps(props) {
  return Object.keys(props).reduce((acc, key) => {
    if (props[key] !== void 0) {
      acc[key] = props[key];
    }
    return acc;
  }, {});
}

// node_modules/@mantine/styles/esm/theme/NormalizeCSS.js
var import_react10 = __toESM(require_react());
var styles = {
  html: {
    fontFamily: "sans-serif",
    lineHeight: "1.15",
    textSizeAdjust: "100%"
  },
  body: {
    margin: 0
  },
  "article, aside, footer, header, nav, section, figcaption, figure, main": {
    display: "block"
  },
  h1: {
    fontSize: "2em"
  },
  hr: {
    boxSizing: "content-box",
    height: 0,
    overflow: "visible"
  },
  pre: {
    fontFamily: "monospace, monospace",
    fontSize: "1em"
  },
  a: {
    background: "transparent",
    textDecorationSkip: "objects"
  },
  "a:active, a:hover": {
    outlineWidth: 0
  },
  "abbr[title]": {
    borderBottom: "none",
    textDecoration: "underline"
  },
  "b, strong": {
    fontWeight: "bolder"
  },
  "code, kbp, samp": {
    fontFamily: "monospace, monospace",
    fontSize: "1em"
  },
  dfn: {
    fontStyle: "italic"
  },
  mark: {
    backgroundColor: "#ff0",
    color: "#000"
  },
  small: {
    fontSize: "80%"
  },
  "sub, sup": {
    fontSize: "75%",
    lineHeight: 0,
    position: "relative",
    verticalAlign: "baseline"
  },
  sup: {
    top: "-0.5em"
  },
  sub: {
    bottom: "-0.25em"
  },
  "audio, video": {
    display: "inline-block"
  },
  "audio:not([controls])": {
    display: "none",
    height: 0
  },
  img: {
    borderStyle: "none",
    verticalAlign: "middle"
  },
  "svg:not(:root)": {
    overflow: "hidden"
  },
  "button, input, optgroup, select, textarea": {
    fontFamily: "sans-serif",
    fontSize: "100%",
    lineHeight: "1.15",
    margin: 0
  },
  "button, input": {
    overflow: "visible"
  },
  "button, select": {
    textTransform: "none"
  },
  "button, [type=reset], [type=submit]": {
    WebkitAppearance: "button"
  },
  "button::-moz-focus-inner, [type=button]::-moz-focus-inner, [type=reset]::-moz-focus-inner, [type=submit]::-moz-focus-inner": {
    borderStyle: "none",
    padding: 0
  },
  "button:-moz-focusring, [type=button]:-moz-focusring, [type=reset]:-moz-focusring, [type=submit]:-moz-focusring": {
    outline: "1px dotted ButtonText"
  },
  legend: {
    boxSizing: "border-box",
    color: "inherit",
    display: "table",
    maxWidth: "100%",
    padding: 0,
    whiteSpace: "normal"
  },
  progress: {
    display: "inline-block",
    verticalAlign: "baseline"
  },
  textarea: {
    overflow: "auto"
  },
  "[type=checkbox], [type=radio]": {
    boxSizing: "border-box",
    padding: 0
  },
  "[type=number]::-webkit-inner-spin-button, [type=number]::-webkit-outer-spin-button": {
    height: "auto"
  },
  "[type=search]": {
    appearance: "none"
  },
  "[type=search]::-webkit-search-cancel-button, [type=search]::-webkit-search-decoration": {
    appearance: "none"
  },
  "::-webkit-file-upload-button": {
    appearance: "button",
    font: "inherit"
  },
  "details, menu": {
    display: "block"
  },
  summary: {
    display: "list-item"
  },
  canvas: {
    display: "inline-block"
  },
  template: {
    display: "none"
  },
  "[hidden]": {
    display: "none"
  }
};
function NormalizeCSS() {
  return import_react10.default.createElement(Global, {
    styles
  });
}

// node_modules/@mantine/styles/esm/theme/MantineProvider.js
var __defProp5 = Object.defineProperty;
var __getOwnPropSymbols5 = Object.getOwnPropertySymbols;
var __hasOwnProp5 = Object.prototype.hasOwnProperty;
var __propIsEnum5 = Object.prototype.propertyIsEnumerable;
var __defNormalProp5 = (obj, key, value) => key in obj ? __defProp5(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues5 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp5.call(b2, prop))
      __defNormalProp5(a2, prop, b2[prop]);
  if (__getOwnPropSymbols5)
    for (var prop of __getOwnPropSymbols5(b2)) {
      if (__propIsEnum5.call(b2, prop))
        __defNormalProp5(a2, prop, b2[prop]);
    }
  return a2;
};
var MantineProviderContext = (0, import_react12.createContext)({
  theme: DEFAULT_THEME
});
function useMantineTheme() {
  var _a;
  return ((_a = (0, import_react12.useContext)(MantineProviderContext)) == null ? void 0 : _a.theme) || DEFAULT_THEME;
}
function useMantineProviderStyles(component) {
  const theme = useMantineTheme();
  const getStyles2 = (name) => {
    var _a, _b;
    return {
      styles: ((_a = theme.components[name]) == null ? void 0 : _a.styles) || {},
      classNames: ((_b = theme.components[name]) == null ? void 0 : _b.classNames) || {}
    };
  };
  if (Array.isArray(component)) {
    return component.map(getStyles2);
  }
  return [getStyles2(component)];
}
function useMantineEmotionCache() {
  var _a;
  return (_a = (0, import_react12.useContext)(MantineProviderContext)) == null ? void 0 : _a.emotionCache;
}
function useComponentDefaultProps(component, defaultProps118, props) {
  var _a;
  const theme = useMantineTheme();
  const contextPropsPayload = (_a = theme.components[component]) == null ? void 0 : _a.defaultProps;
  const contextProps = typeof contextPropsPayload === "function" ? contextPropsPayload(theme) : contextPropsPayload;
  return __spreadValues5(__spreadValues5(__spreadValues5({}, defaultProps118), contextProps), filterProps(props));
}
function MantineProvider({
  theme,
  emotionCache,
  withNormalizeCSS = false,
  withGlobalStyles = false,
  withCSSVariables = false,
  inherit = false,
  children
}) {
  const ctx = (0, import_react12.useContext)(MantineProviderContext);
  const mergedTheme = mergeThemeWithFunctions(DEFAULT_THEME, inherit ? __spreadValues5(__spreadValues5({}, ctx.theme), theme) : theme);
  return import_react12.default.createElement(ThemeProvider, {
    theme: mergedTheme
  }, import_react12.default.createElement(MantineProviderContext.Provider, {
    value: { theme: mergedTheme, emotionCache }
  }, withNormalizeCSS && import_react12.default.createElement(NormalizeCSS, null), withGlobalStyles && import_react12.default.createElement(GlobalStyles, {
    theme: mergedTheme
  }), withCSSVariables && import_react12.default.createElement(MantineCssVariables, {
    theme: mergedTheme
  }), typeof mergedTheme.globalStyles === "function" && import_react12.default.createElement(Global, {
    styles: mergedTheme.globalStyles(mergedTheme)
  }), children));
}
MantineProvider.displayName = "@mantine/core/MantineProvider";

// node_modules/@mantine/styles/esm/theme/ColorSchemeProvider.js
var import_react14 = __toESM(require_react());
var ColorSchemeContext = (0, import_react14.createContext)(null);
function ColorSchemeProvider({
  colorScheme,
  toggleColorScheme,
  children
}) {
  return import_react14.default.createElement(ColorSchemeContext.Provider, {
    value: { colorScheme, toggleColorScheme }
  }, children);
}
ColorSchemeProvider.displayName = "@mantine/core/ColorSchemeProvider";

// node_modules/@mantine/styles/esm/theme/utils/get-default-z-index/get-default-z-index.js
var elevations = {
  app: 100,
  modal: 200,
  popover: 300,
  overlay: 400,
  max: 9999
};
function getDefaultZIndex(level) {
  return elevations[level];
}

// node_modules/@mantine/styles/esm/tss/utils/use-guaranteed-memo/use-guaranteed-memo.js
var import_react15 = __toESM(require_react());
function useGuaranteedMemo(fn5, deps) {
  const ref = (0, import_react15.useRef)();
  if (!ref.current || deps.length !== ref.current.prevDeps.length || ref.current.prevDeps.map((v, i) => v === deps[i]).indexOf(false) >= 0) {
    ref.current = {
      v: fn5(),
      prevDeps: [...deps]
    };
  }
  return ref.current.v;
}

// node_modules/@mantine/styles/esm/tss/default-emotion-cache.js
var defaultMantineEmotionCache = createCache({ key: "mantine", prepend: true });

// node_modules/@mantine/styles/esm/tss/use-emotion-cache.js
function useEmotionCache() {
  const cache = useMantineEmotionCache();
  return cache || defaultMantineEmotionCache;
}

// node_modules/@mantine/styles/esm/tss/use-css.js
var __defProp6 = Object.defineProperty;
var __getOwnPropSymbols6 = Object.getOwnPropertySymbols;
var __hasOwnProp6 = Object.prototype.hasOwnProperty;
var __propIsEnum6 = Object.prototype.propertyIsEnumerable;
var __defNormalProp6 = (obj, key, value) => key in obj ? __defProp6(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues6 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp6.call(b2, prop))
      __defNormalProp6(a2, prop, b2[prop]);
  if (__getOwnPropSymbols6)
    for (var prop of __getOwnPropSymbols6(b2)) {
      if (__propIsEnum6.call(b2, prop))
        __defNormalProp6(a2, prop, b2[prop]);
    }
  return a2;
};
var refPropertyName = "ref";
function getRef(args) {
  let ref;
  if (args.length !== 1) {
    return { args, ref };
  }
  const [arg] = args;
  if (!(arg instanceof Object)) {
    return { args, ref };
  }
  if (!(refPropertyName in arg)) {
    return { args, ref };
  }
  ref = arg[refPropertyName];
  const argCopy = __spreadValues6({}, arg);
  delete argCopy[refPropertyName];
  return { args: [argCopy], ref };
}
var { cssFactory } = (() => {
  function merge2(registered, css2, className) {
    const registeredStyles = [];
    const rawClassName = getRegisteredStyles(registered, registeredStyles, className);
    if (registeredStyles.length < 2) {
      return className;
    }
    return rawClassName + css2(registeredStyles);
  }
  function _cssFactory(params) {
    const { cache } = params;
    const css2 = (...styles2) => {
      const { ref, args } = getRef(styles2);
      const serialized = serializeStyles(args, cache.registered);
      insertStyles(cache, serialized, false);
      return `${cache.key}-${serialized.name}${ref === void 0 ? "" : ` ${ref}`}`;
    };
    const cx = (...args) => merge2(cache.registered, css2, clsx_m_default(args));
    return { css: css2, cx };
  }
  return { cssFactory: _cssFactory };
})();
function useCss() {
  const cache = useEmotionCache();
  return useGuaranteedMemo(() => cssFactory({ cache }), [cache]);
}

// node_modules/@mantine/styles/esm/tss/utils/merge-class-names/merge-class-names.js
function mergeClassNames({
  cx,
  classes,
  context,
  classNames,
  name,
  cache
}) {
  const contextClassNames = context.reduce((acc, item) => {
    Object.keys(item.classNames).forEach((key) => {
      if (typeof acc[key] !== "string") {
        acc[key] = `${item.classNames[key]}`;
      } else {
        acc[key] = `${acc[key]} ${item.classNames[key]}`;
      }
    });
    return acc;
  }, {});
  return Object.keys(classes).reduce((acc, className) => {
    acc[className] = cx(classes[className], contextClassNames[className], classNames != null && classNames[className], Array.isArray(name) ? name.filter(Boolean).map((part) => `${(cache == null ? void 0 : cache.key) || "mantine"}-${part}-${className}`).join(" ") : name ? `${(cache == null ? void 0 : cache.key) || "mantine"}-${name}-${className}` : null);
    return acc;
  }, {});
}

// node_modules/@mantine/styles/esm/tss/create-styles.js
var __defProp7 = Object.defineProperty;
var __getOwnPropSymbols7 = Object.getOwnPropertySymbols;
var __hasOwnProp7 = Object.prototype.hasOwnProperty;
var __propIsEnum7 = Object.prototype.propertyIsEnumerable;
var __defNormalProp7 = (obj, key, value) => key in obj ? __defProp7(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues7 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp7.call(b2, prop))
      __defNormalProp7(a2, prop, b2[prop]);
  if (__getOwnPropSymbols7)
    for (var prop of __getOwnPropSymbols7(b2)) {
      if (__propIsEnum7.call(b2, prop))
        __defNormalProp7(a2, prop, b2[prop]);
    }
  return a2;
};
function createRef(refName) {
  return `__mantine-ref-${refName || ""}`;
}
function getStyles(styles2, theme, params) {
  const extractStyles = (stylesPartial) => typeof stylesPartial === "function" ? stylesPartial(theme, params || {}) : stylesPartial || {};
  if (Array.isArray(styles2)) {
    return styles2.map((item) => extractStyles(item.styles)).reduce((acc, item) => {
      Object.keys(item).forEach((key) => {
        if (!acc[key]) {
          acc[key] = __spreadValues7({}, item[key]);
        } else {
          acc[key] = __spreadValues7(__spreadValues7({}, acc[key]), item[key]);
        }
      });
      return acc;
    }, {});
  }
  return extractStyles(styles2);
}
function createStyles(input) {
  const getCssObject = typeof input === "function" ? input : () => input;
  function useStyles107(params, options) {
    const theme = useMantineTheme();
    const context = useMantineProviderStyles(options == null ? void 0 : options.name);
    const cache = useMantineEmotionCache();
    const { css: css2, cx } = useCss();
    const cssObject = getCssObject(theme, params, createRef);
    const componentStyles = getStyles(options == null ? void 0 : options.styles, theme, params);
    const providerStyles = getStyles(context, theme, params);
    const classes = Object.fromEntries(Object.keys(cssObject).map((key) => {
      const mergedStyles = cx({ [css2(cssObject[key])]: !(options == null ? void 0 : options.unstyled) }, css2(providerStyles[key]), css2(componentStyles[key]));
      return [key, mergedStyles];
    }));
    return {
      classes: mergeClassNames({
        cx,
        classes,
        context,
        classNames: options == null ? void 0 : options.classNames,
        name: options == null ? void 0 : options.name,
        cache
      }),
      cx,
      theme
    };
  }
  return useStyles107;
}

// node_modules/@mantine/styles/esm/tss/Global.js
var import_react16 = __toESM(require_react());
function Global2({ styles: styles2 }) {
  const theme = useMantineTheme();
  return import_react16.default.createElement(Global, {
    styles: css(typeof styles2 === "function" ? styles2(theme) : styles2)
  });
}

// node_modules/@mantine/core/esm/Transition/transitions.js
var __defProp8 = Object.defineProperty;
var __defProps4 = Object.defineProperties;
var __getOwnPropDescs4 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols8 = Object.getOwnPropertySymbols;
var __hasOwnProp8 = Object.prototype.hasOwnProperty;
var __propIsEnum8 = Object.prototype.propertyIsEnumerable;
var __defNormalProp8 = (obj, key, value) => key in obj ? __defProp8(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues8 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp8.call(b2, prop))
      __defNormalProp8(a2, prop, b2[prop]);
  if (__getOwnPropSymbols8)
    for (var prop of __getOwnPropSymbols8(b2)) {
      if (__propIsEnum8.call(b2, prop))
        __defNormalProp8(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps4 = (a2, b2) => __defProps4(a2, __getOwnPropDescs4(b2));
var popIn = {
  in: { opacity: 1, transform: "scale(1)" },
  out: { opacity: 0, transform: "scale(.9) translateY(10px)" },
  transitionProperty: "transform, opacity"
};
var transitions = {
  fade: {
    in: { opacity: 1 },
    out: { opacity: 0 },
    transitionProperty: "opacity"
  },
  scale: {
    in: { opacity: 1, transform: "scale(1)" },
    out: { opacity: 0, transform: "scale(0)" },
    common: { transformOrigin: "top" },
    transitionProperty: "transform, opacity"
  },
  "scale-y": {
    in: { opacity: 1, transform: "scaleY(1)" },
    out: { opacity: 0, transform: "scaleY(0)" },
    common: { transformOrigin: "top" },
    transitionProperty: "transform, opacity"
  },
  "scale-x": {
    in: { opacity: 1, transform: "scaleX(1)" },
    out: { opacity: 0, transform: "scaleX(0)" },
    common: { transformOrigin: "left" },
    transitionProperty: "transform, opacity"
  },
  "skew-up": {
    in: { opacity: 1, transform: "translateY(0) skew(0deg, 0deg)" },
    out: { opacity: 0, transform: "translateY(-20px) skew(-10deg, -5deg)" },
    common: { transformOrigin: "top" },
    transitionProperty: "transform, opacity"
  },
  "skew-down": {
    in: { opacity: 1, transform: "translateY(0) skew(0deg, 0deg)" },
    out: { opacity: 0, transform: "translateY(20px) skew(-10deg, -5deg)" },
    common: { transformOrigin: "bottom" },
    transitionProperty: "transform, opacity"
  },
  "rotate-left": {
    in: { opacity: 1, transform: "translateY(0) rotate(0deg)" },
    out: { opacity: 0, transform: "translateY(20px) rotate(-5deg)" },
    common: { transformOrigin: "bottom" },
    transitionProperty: "transform, opacity"
  },
  "rotate-right": {
    in: { opacity: 1, transform: "translateY(0) rotate(0deg)" },
    out: { opacity: 0, transform: "translateY(20px) rotate(5deg)" },
    common: { transformOrigin: "top" },
    transitionProperty: "transform, opacity"
  },
  "slide-down": {
    in: { opacity: 1, transform: "translateY(0)" },
    out: { opacity: 0, transform: "translateY(-100%)" },
    common: { transformOrigin: "top" },
    transitionProperty: "transform, opacity"
  },
  "slide-up": {
    in: { opacity: 1, transform: "translateY(0)" },
    out: { opacity: 0, transform: "translateY(100%)" },
    common: { transformOrigin: "bottom" },
    transitionProperty: "transform, opacity"
  },
  "slide-left": {
    in: { opacity: 1, transform: "translateX(0)" },
    out: { opacity: 0, transform: "translateX(100%)" },
    common: { transformOrigin: "left" },
    transitionProperty: "transform, opacity"
  },
  "slide-right": {
    in: { opacity: 1, transform: "translateX(0)" },
    out: { opacity: 0, transform: "translateX(-100%)" },
    common: { transformOrigin: "right" },
    transitionProperty: "transform, opacity"
  },
  pop: __spreadProps4(__spreadValues8({}, popIn), {
    common: { transformOrigin: "center center" }
  }),
  "pop-bottom-left": __spreadProps4(__spreadValues8({}, popIn), {
    common: { transformOrigin: "bottom left" }
  }),
  "pop-bottom-right": __spreadProps4(__spreadValues8({}, popIn), {
    common: { transformOrigin: "bottom right" }
  }),
  "pop-top-left": __spreadProps4(__spreadValues8({}, popIn), {
    common: { transformOrigin: "top left" }
  }),
  "pop-top-right": __spreadProps4(__spreadValues8({}, popIn), {
    common: { transformOrigin: "top right" }
  })
};

// node_modules/@mantine/core/esm/Transition/index.js
var AVAILABLE_TRANSITIONS = Object.keys(transitions);

// node_modules/@mantine/core/esm/Accordion/Accordion.js
var import_react78 = __toESM(require_react());

// node_modules/@mantine/core/esm/Accordion/AccordionProvider.js
var import_react69 = __toESM(require_react());

// node_modules/@mantine/hooks/esm/use-click-outside/use-click-outside.js
var import_react19 = __toESM(require_react());
var DEFAULT_EVENTS = ["mousedown", "touchstart"];
function useClickOutside(handler, events, nodes) {
  const ref = (0, import_react19.useRef)();
  (0, import_react19.useEffect)(() => {
    const listener = (event) => {
      const { target } = event != null ? event : {};
      if (Array.isArray(nodes)) {
        const shouldIgnore = (target == null ? void 0 : target.hasAttribute("data-ignore-outside-clicks")) || !document.body.contains(target) && target.tagName !== "HTML";
        const shouldTrigger = nodes.every((node2) => !!node2 && !event.composedPath().includes(node2));
        shouldTrigger && !shouldIgnore && handler();
      } else if (ref.current && !ref.current.contains(target)) {
        handler();
      }
    };
    (events || DEFAULT_EVENTS).forEach((fn5) => document.addEventListener(fn5, listener));
    return () => {
      (events || DEFAULT_EVENTS).forEach((fn5) => document.removeEventListener(fn5, listener));
    };
  }, [ref, handler, nodes]);
  return ref;
}

// node_modules/@mantine/hooks/esm/use-clipboard/use-clipboard.js
var import_react20 = __toESM(require_react());
function useClipboard({ timeout = 2e3 } = {}) {
  const [error, setError] = (0, import_react20.useState)(null);
  const [copied, setCopied] = (0, import_react20.useState)(false);
  const [copyTimeout, setCopyTimeout] = (0, import_react20.useState)(null);
  const handleCopyResult = (value) => {
    clearTimeout(copyTimeout);
    setCopyTimeout(setTimeout(() => setCopied(false), timeout));
    setCopied(value);
  };
  const copy2 = (valueToCopy) => {
    if ("clipboard" in navigator) {
      navigator.clipboard.writeText(valueToCopy).then(() => handleCopyResult(true)).catch((err) => setError(err));
    } else {
      setError(new Error("useClipboard: navigator.clipboard is not supported"));
    }
  };
  const reset = () => {
    setCopied(false);
    setError(null);
    clearTimeout(copyTimeout);
  };
  return { copy: copy2, reset, error, copied };
}

// node_modules/@mantine/hooks/esm/use-media-query/use-media-query.js
var import_react21 = __toESM(require_react());
function attachMediaListener(query, callback) {
  try {
    query.addEventListener("change", callback);
    return () => query.removeEventListener("change", callback);
  } catch (e) {
    query.addListener(callback);
    return () => query.removeListener(callback);
  }
}
function getInitialValue(query, initialValue) {
  if (typeof initialValue === "boolean") {
    return initialValue;
  }
  if (typeof window !== "undefined" && "matchMedia" in window) {
    return window.matchMedia(query).matches;
  }
  return false;
}
function useMediaQuery(query, initialValue, { getInitialValueInEffect } = {
  getInitialValueInEffect: true
}) {
  const [matches2, setMatches] = (0, import_react21.useState)(getInitialValueInEffect ? initialValue : getInitialValue(query, initialValue));
  const queryRef = (0, import_react21.useRef)();
  (0, import_react21.useEffect)(() => {
    if ("matchMedia" in window) {
      queryRef.current = window.matchMedia(query);
      setMatches(queryRef.current.matches);
      return attachMediaListener(queryRef.current, (event) => setMatches(event.matches));
    }
    return void 0;
  }, [query]);
  return matches2;
}

// node_modules/@mantine/hooks/esm/use-counter/use-counter.js
var import_react22 = __toESM(require_react());

// node_modules/@mantine/hooks/esm/utils/clamp/clamp.js
function clamp(value, min3, max3) {
  return Math.min(Math.max(value, min3), max3);
}

// node_modules/@mantine/hooks/esm/use-debounced-state/use-debounced-state.js
var import_react23 = __toESM(require_react());

// node_modules/@mantine/hooks/esm/use-debounced-value/use-debounced-value.js
var import_react24 = __toESM(require_react());

// node_modules/@mantine/hooks/esm/use-isomorphic-effect/use-isomorphic-effect.js
var import_react25 = __toESM(require_react());
var useIsomorphicEffect = typeof document !== "undefined" ? import_react25.useLayoutEffect : import_react25.useEffect;

// node_modules/@mantine/hooks/esm/use-document-visibility/use-document-visibility.js
var import_react26 = __toESM(require_react());

// node_modules/@mantine/hooks/esm/use-focus-return/use-focus-return.js
var import_react28 = __toESM(require_react());

// node_modules/@mantine/hooks/esm/use-did-update/use-did-update.js
var import_react27 = __toESM(require_react());
function useDidUpdate(fn5, dependencies) {
  const mounted = (0, import_react27.useRef)(false);
  (0, import_react27.useEffect)(() => () => {
    mounted.current = false;
  }, []);
  (0, import_react27.useEffect)(() => {
    if (mounted.current) {
      return fn5();
    }
    mounted.current = true;
    return void 0;
  }, dependencies);
}

// node_modules/@mantine/hooks/esm/use-focus-return/use-focus-return.js
function useFocusReturn({ opened, shouldReturnFocus = true }) {
  const lastActiveElement = (0, import_react28.useRef)();
  const returnFocus = () => {
    var _a;
    if (lastActiveElement.current && "focus" in lastActiveElement.current && typeof lastActiveElement.current.focus === "function") {
      (_a = lastActiveElement.current) == null ? void 0 : _a.focus({ preventScroll: true });
    }
  };
  useDidUpdate(() => {
    let timeout = -1;
    const clearFocusTimeout = (event) => {
      if (event.key === "Tab") {
        window.clearTimeout(timeout);
      }
    };
    document.addEventListener("keydown", clearFocusTimeout);
    if (opened) {
      lastActiveElement.current = document.activeElement;
    } else if (shouldReturnFocus) {
      timeout = window.setTimeout(returnFocus, 10);
    }
    return () => {
      window.clearTimeout(timeout);
      document.removeEventListener("keydown", clearFocusTimeout);
    };
  }, [opened, shouldReturnFocus]);
  return returnFocus;
}

// node_modules/@mantine/hooks/esm/use-focus-trap/use-focus-trap.js
var import_react29 = __toESM(require_react());

// node_modules/@mantine/hooks/esm/use-focus-trap/tabbable.js
var TABBABLE_NODES = /input|select|textarea|button|object/;
var FOCUS_SELECTOR = "a, input, select, textarea, button, object, [tabindex]";
function hidden(element) {
  if (false) {
    return false;
  }
  return element.style.display === "none";
}
function visible(element) {
  const isHidden = element.getAttribute("aria-hidden") || element.getAttribute("hidden") || element.getAttribute("type") === "hidden";
  if (isHidden) {
    return false;
  }
  let parentElement = element;
  while (parentElement) {
    if (parentElement === document.body || parentElement.nodeType === 11) {
      break;
    }
    if (hidden(parentElement)) {
      return false;
    }
    parentElement = parentElement.parentNode;
  }
  return true;
}
function getElementTabIndex(element) {
  let tabIndex = element.getAttribute("tabindex");
  if (tabIndex === null) {
    tabIndex = void 0;
  }
  return parseInt(tabIndex, 10);
}
function focusable(element) {
  const nodeName = element.nodeName.toLowerCase();
  const isTabIndexNotNaN = !Number.isNaN(getElementTabIndex(element));
  const res = TABBABLE_NODES.test(nodeName) && !element.disabled || (element instanceof HTMLAnchorElement ? element.href || isTabIndexNotNaN : isTabIndexNotNaN);
  return res && visible(element);
}
function tabbable(element) {
  const tabIndex = getElementTabIndex(element);
  const isTabIndexNaN = Number.isNaN(tabIndex);
  return (isTabIndexNaN || tabIndex >= 0) && focusable(element);
}
function findTabbableDescendants(element) {
  return Array.from(element.querySelectorAll(FOCUS_SELECTOR)).filter(tabbable);
}

// node_modules/@mantine/hooks/esm/use-focus-trap/scope-tab.js
function scopeTab(node2, event) {
  const tabbable3 = findTabbableDescendants(node2);
  if (!tabbable3.length) {
    event.preventDefault();
    return;
  }
  const finalTabbable = tabbable3[event.shiftKey ? 0 : tabbable3.length - 1];
  const root = node2.getRootNode();
  const leavingFinalTabbable = finalTabbable === root.activeElement || node2 === root.activeElement;
  if (!leavingFinalTabbable) {
    return;
  }
  event.preventDefault();
  const target = tabbable3[event.shiftKey ? tabbable3.length - 1 : 0];
  if (target) {
    target.focus();
  }
}

// node_modules/@mantine/hooks/esm/use-focus-trap/create-aria-hider.js
function createAriaHider(containerNode, selector = "body > :not(script)") {
  const rootNodes = Array.from(document.querySelectorAll(selector)).map((node2) => {
    var _a;
    if (((_a = node2 == null ? void 0 : node2.shadowRoot) == null ? void 0 : _a.contains(containerNode)) || node2.contains(containerNode)) {
      return void 0;
    }
    const ariaHidden = node2.getAttribute("aria-hidden");
    if (ariaHidden === null || ariaHidden === "false") {
      node2.setAttribute("aria-hidden", "true");
    }
    return { node: node2, ariaHidden };
  });
  return () => {
    rootNodes.forEach((item) => {
      if (!item) {
        return;
      }
      if (item.ariaHidden === null) {
        item.node.removeAttribute("aria-hidden");
      } else {
        item.node.setAttribute("aria-hidden", item.ariaHidden);
      }
    });
  };
}

// node_modules/@mantine/hooks/esm/use-focus-trap/use-focus-trap.js
function useFocusTrap(active = true) {
  const ref = (0, import_react29.useRef)();
  const restoreAria = (0, import_react29.useRef)(null);
  const setRef = (0, import_react29.useCallback)((node2) => {
    if (!active) {
      return;
    }
    if (node2 === null) {
      if (restoreAria.current) {
        restoreAria.current();
        restoreAria.current = null;
      }
      return;
    }
    restoreAria.current = createAriaHider(node2);
    if (ref.current === node2) {
      return;
    }
    if (node2) {
      const processNode = () => {
        let focusElement = node2.querySelector("[data-autofocus]");
        if (!focusElement) {
          const children = Array.from(node2.querySelectorAll(FOCUS_SELECTOR));
          focusElement = children.find(tabbable) || children.find(focusable) || null;
          if (!focusElement && focusable(node2))
            focusElement = node2;
        }
        if (focusElement) {
          focusElement.focus({ preventScroll: true });
        } else if (true) {
          console.warn("[@mantine/hooks/use-focus-trap] Failed to find focusable element within provided node", node2);
        }
      };
      setTimeout(() => {
        if (node2.getRootNode()) {
          processNode();
        } else if (true) {
          console.warn("[@mantine/hooks/use-focus-trap] Ref node is not part of the dom", node2);
        }
      });
      ref.current = node2;
    } else {
      ref.current = null;
    }
  }, [active]);
  (0, import_react29.useEffect)(() => {
    if (!active) {
      return void 0;
    }
    const handleKeyDown = (event) => {
      if (event.key === "Tab" && ref.current) {
        scopeTab(ref.current, event);
      }
    };
    document.addEventListener("keydown", handleKeyDown);
    return () => {
      document.removeEventListener("keydown", handleKeyDown);
      if (restoreAria.current) {
        restoreAria.current();
      }
    };
  }, [active]);
  return setRef;
}

// node_modules/@mantine/hooks/esm/use-force-update/use-force-update.js
var import_react30 = __toESM(require_react());

// node_modules/@mantine/hooks/esm/use-id/use-id.js
var import_react31 = __toESM(require_react());
var randomId = () => `mantine-${Math.random().toString(36).slice(2, 11)}`;
var useReactId = import_react31.default["useId".toString()] || (() => void 0);
function useClientId() {
  const [uuid, setUuid] = (0, import_react31.useState)("");
  useIsomorphicEffect(() => {
    setUuid(randomId());
  }, []);
  return uuid;
}
function getReactId() {
  const id = useReactId();
  return id ? `mantine-${id.replace(/:/g, "")}` : "";
}
function useId(staticId) {
  return typeof staticId === "string" ? staticId : getReactId() || useClientId();
}

// node_modules/@mantine/hooks/esm/use-idle/use-idle.js
var import_react32 = __toESM(require_react());

// node_modules/@mantine/hooks/esm/use-interval/use-interval.js
var import_react33 = __toESM(require_react());

// node_modules/@mantine/hooks/esm/use-list-state/use-list-state.js
var import_react34 = __toESM(require_react());

// node_modules/@mantine/hooks/esm/use-local-storage/create-storage.js
var import_react36 = __toESM(require_react());

// node_modules/@mantine/hooks/esm/use-window-event/use-window-event.js
var import_react35 = __toESM(require_react());
function useWindowEvent(type, listener, options) {
  (0, import_react35.useEffect)(() => {
    window.addEventListener(type, listener, options);
    return () => window.removeEventListener(type, listener, options);
  }, [type, listener]);
}

// node_modules/@mantine/hooks/esm/use-merged-ref/use-merged-ref.js
var import_react37 = __toESM(require_react());

// node_modules/@mantine/hooks/esm/utils/assign-ref/assign-ref.js
function assignRef(ref, value) {
  if (typeof ref === "function") {
    ref(value);
  } else if (typeof ref === "object" && ref !== null && "current" in ref) {
    ref.current = value;
  }
}

// node_modules/@mantine/hooks/esm/use-merged-ref/use-merged-ref.js
function mergeRefs(...refs) {
  return (node2) => {
    refs.forEach((ref) => assignRef(ref, node2));
  };
}
function useMergedRef(...refs) {
  return (0, import_react37.useCallback)(mergeRefs(...refs), refs);
}

// node_modules/@mantine/hooks/esm/use-mouse/use-mouse.js
var import_react38 = __toESM(require_react());

// node_modules/@mantine/hooks/esm/use-move/use-move.js
var import_react39 = __toESM(require_react());
var clampUseMovePosition = (position2) => ({
  x: clamp(position2.x, 0, 1),
  y: clamp(position2.y, 0, 1)
});
function useMove(onChange, handlers, dir = "ltr") {
  const ref = (0, import_react39.useRef)();
  const mounted = (0, import_react39.useRef)(false);
  const isSliding = (0, import_react39.useRef)(false);
  const frame = (0, import_react39.useRef)(0);
  const [active, setActive] = (0, import_react39.useState)(false);
  (0, import_react39.useEffect)(() => {
    mounted.current = true;
  }, []);
  (0, import_react39.useEffect)(() => {
    const onScrub = ({ x, y }) => {
      cancelAnimationFrame(frame.current);
      frame.current = requestAnimationFrame(() => {
        if (mounted.current && ref.current) {
          ref.current.style.userSelect = "none";
          const rect = ref.current.getBoundingClientRect();
          if (rect.width && rect.height) {
            const _x = clamp((x - rect.left) / rect.width, 0, 1);
            onChange({
              x: dir === "ltr" ? _x : 1 - _x,
              y: clamp((y - rect.top) / rect.height, 0, 1)
            });
          }
        }
      });
    };
    const bindEvents = () => {
      document.addEventListener("mousemove", onMouseMove);
      document.addEventListener("mouseup", stopScrubbing);
      document.addEventListener("touchmove", onTouchMove);
      document.addEventListener("touchend", stopScrubbing);
    };
    const unbindEvents = () => {
      document.removeEventListener("mousemove", onMouseMove);
      document.removeEventListener("mouseup", stopScrubbing);
      document.removeEventListener("touchmove", onTouchMove);
      document.removeEventListener("touchend", stopScrubbing);
    };
    const startScrubbing = () => {
      if (!isSliding.current && mounted.current) {
        isSliding.current = true;
        typeof (handlers == null ? void 0 : handlers.onScrubStart) === "function" && handlers.onScrubStart();
        setActive(true);
        bindEvents();
      }
    };
    const stopScrubbing = () => {
      if (isSliding.current && mounted.current) {
        isSliding.current = false;
        setActive(false);
        unbindEvents();
        setTimeout(() => {
          typeof (handlers == null ? void 0 : handlers.onScrubEnd) === "function" && handlers.onScrubEnd();
        }, 0);
      }
    };
    const onMouseDown = (event) => {
      startScrubbing();
      event.preventDefault();
      onMouseMove(event);
    };
    const onMouseMove = (event) => onScrub({ x: event.clientX, y: event.clientY });
    const onTouchStart = (event) => {
      if (event.cancelable) {
        event.preventDefault();
      }
      startScrubbing();
      onTouchMove(event);
    };
    const onTouchMove = (event) => {
      if (event.cancelable) {
        event.preventDefault();
      }
      onScrub({ x: event.changedTouches[0].clientX, y: event.changedTouches[0].clientY });
    };
    ref.current.addEventListener("mousedown", onMouseDown);
    ref.current.addEventListener("touchstart", onTouchStart, { passive: false });
    return () => {
      if (ref.current) {
        ref.current.removeEventListener("mousedown", onMouseDown);
        ref.current.removeEventListener("touchstart", onTouchStart);
      }
    };
  }, [dir, onChange]);
  return { ref, active };
}

// node_modules/@mantine/hooks/esm/use-pagination/use-pagination.js
var import_react41 = __toESM(require_react());

// node_modules/@mantine/hooks/esm/use-uncontrolled/use-uncontrolled.js
var import_react40 = __toESM(require_react());
function useUncontrolled({
  value,
  defaultValue,
  finalValue,
  onChange = () => {
  }
}) {
  const [uncontrolledValue, setUncontrolledValue] = (0, import_react40.useState)(defaultValue !== void 0 ? defaultValue : finalValue);
  const handleUncontrolledChange = (val) => {
    setUncontrolledValue(val);
    onChange == null ? void 0 : onChange(val);
  };
  if (value !== void 0) {
    return [value, onChange, true];
  }
  return [uncontrolledValue, handleUncontrolledChange, false];
}

// node_modules/@mantine/hooks/esm/utils/range/range.js
function range(start2, end2) {
  const length2 = end2 - start2 + 1;
  return Array.from({ length: length2 }, (_, index6) => index6 + start2);
}

// node_modules/@mantine/hooks/esm/use-pagination/use-pagination.js
var DOTS = "dots";
function usePagination({
  total,
  siblings = 1,
  boundaries = 1,
  page,
  initialPage = 1,
  onChange
}) {
  const _total = Math.max(Math.trunc(total), 0);
  const [activePage, setActivePage] = useUncontrolled({
    value: page,
    onChange,
    defaultValue: initialPage,
    finalValue: initialPage
  });
  const setPage = (pageNumber) => {
    if (pageNumber <= 0) {
      setActivePage(1);
    } else if (pageNumber > _total) {
      setActivePage(_total);
    } else {
      setActivePage(pageNumber);
    }
  };
  const next2 = () => setPage(activePage + 1);
  const previous = () => setPage(activePage - 1);
  const first = () => setPage(1);
  const last = () => setPage(_total);
  const paginationRange = (0, import_react41.useMemo)(() => {
    const totalPageNumbers = siblings * 2 + 3 + boundaries * 2;
    if (totalPageNumbers >= _total) {
      return range(1, _total);
    }
    const leftSiblingIndex = Math.max(activePage - siblings, boundaries);
    const rightSiblingIndex = Math.min(activePage + siblings, _total - boundaries);
    const shouldShowLeftDots = leftSiblingIndex > boundaries + 2;
    const shouldShowRightDots = rightSiblingIndex < _total - (boundaries + 1);
    if (!shouldShowLeftDots && shouldShowRightDots) {
      const leftItemCount = siblings * 2 + boundaries + 2;
      return [...range(1, leftItemCount), DOTS, ...range(_total - (boundaries - 1), _total)];
    }
    if (shouldShowLeftDots && !shouldShowRightDots) {
      const rightItemCount = boundaries + 1 + 2 * siblings;
      return [...range(1, boundaries), DOTS, ...range(_total - rightItemCount, _total)];
    }
    return [
      ...range(1, boundaries),
      DOTS,
      ...range(leftSiblingIndex, rightSiblingIndex),
      DOTS,
      ...range(_total - boundaries + 1, _total)
    ];
  }, [_total, siblings, activePage]);
  return {
    range: paginationRange,
    active: activePage,
    setPage,
    next: next2,
    previous,
    first,
    last
  };
}

// node_modules/@mantine/hooks/esm/use-queue/use-queue.js
var import_react42 = __toESM(require_react());

// node_modules/@mantine/hooks/esm/use-page-leave/use-page-leave.js
var import_react43 = __toESM(require_react());

// node_modules/@mantine/hooks/esm/use-reduced-motion/use-reduced-motion.js
function useReducedMotion(initialValue, options) {
  return useMediaQuery("(prefers-reduced-motion: reduce)", initialValue, options);
}

// node_modules/@mantine/hooks/esm/use-scroll-into-view/use-scroll-into-view.js
var import_react44 = __toESM(require_react());

// node_modules/@mantine/hooks/esm/use-scroll-into-view/utils/ease-in-out-quad.js
var easeInOutQuad = (t) => t < 0.5 ? 2 * t * t : -1 + (4 - 2 * t) * t;

// node_modules/@mantine/hooks/esm/use-scroll-into-view/utils/get-relative-position.js
var getRelativePosition = ({
  axis,
  target,
  parent,
  alignment,
  offset: offset3,
  isList
}) => {
  if (!target || !parent && typeof document === "undefined") {
    return 0;
  }
  const isCustomParent = !!parent;
  const parentElement = parent || document.body;
  const parentPosition = parentElement.getBoundingClientRect();
  const targetPosition = target.getBoundingClientRect();
  const getDiff = (property) => targetPosition[property] - parentPosition[property];
  if (axis === "y") {
    const diff = getDiff("top");
    if (diff === 0)
      return 0;
    if (alignment === "start") {
      const distance = diff - offset3;
      const shouldScroll = distance <= targetPosition.height * (isList ? 0 : 1) || !isList;
      return shouldScroll ? distance : 0;
    }
    const parentHeight = isCustomParent ? parentPosition.height : window.innerHeight;
    if (alignment === "end") {
      const distance = diff + offset3 - parentHeight + targetPosition.height;
      const shouldScroll = distance >= -targetPosition.height * (isList ? 0 : 1) || !isList;
      return shouldScroll ? distance : 0;
    }
    if (alignment === "center") {
      return diff - parentHeight / 2 + targetPosition.height / 2;
    }
    return 0;
  }
  if (axis === "x") {
    const diff = getDiff("left");
    if (diff === 0)
      return 0;
    if (alignment === "start") {
      const distance = diff - offset3;
      const shouldScroll = distance <= targetPosition.width || !isList;
      return shouldScroll ? distance : 0;
    }
    const parentWidth = isCustomParent ? parentPosition.width : window.innerWidth;
    if (alignment === "end") {
      const distance = diff + offset3 - parentWidth + targetPosition.width;
      const shouldScroll = distance >= -targetPosition.width || !isList;
      return shouldScroll ? distance : 0;
    }
    if (alignment === "center") {
      return diff - parentWidth / 2 + targetPosition.width / 2;
    }
    return 0;
  }
  return 0;
};

// node_modules/@mantine/hooks/esm/use-scroll-into-view/utils/get-scroll-start.js
var getScrollStart = ({ axis, parent }) => {
  if (!parent && typeof document === "undefined") {
    return 0;
  }
  const method = axis === "y" ? "scrollTop" : "scrollLeft";
  if (parent) {
    return parent[method];
  }
  const { body, documentElement } = document;
  return body[method] + documentElement[method];
};

// node_modules/@mantine/hooks/esm/use-scroll-into-view/utils/set-scroll-param.js
var setScrollParam = ({ axis, parent, distance }) => {
  if (!parent && typeof document === "undefined") {
    return;
  }
  const method = axis === "y" ? "scrollTop" : "scrollLeft";
  if (parent) {
    parent[method] = distance;
  } else {
    const { body, documentElement } = document;
    body[method] = distance;
    documentElement[method] = distance;
  }
};

// node_modules/@mantine/hooks/esm/use-scroll-into-view/use-scroll-into-view.js
function useScrollIntoView({
  duration = 1250,
  axis = "y",
  onScrollFinish,
  easing = easeInOutQuad,
  offset: offset3 = 0,
  cancelable = true,
  isList = false
} = {}) {
  const frameID = (0, import_react44.useRef)(0);
  const startTime = (0, import_react44.useRef)(0);
  const shouldStop = (0, import_react44.useRef)(false);
  const scrollableRef = (0, import_react44.useRef)(null);
  const targetRef = (0, import_react44.useRef)(null);
  const reducedMotion = useReducedMotion();
  const cancel = () => {
    if (frameID.current) {
      cancelAnimationFrame(frameID.current);
    }
  };
  const scrollIntoView = (0, import_react44.useCallback)(({ alignment = "start" } = {}) => {
    var _a;
    shouldStop.current = false;
    if (frameID.current) {
      cancel();
    }
    const start2 = (_a = getScrollStart({ parent: scrollableRef.current, axis })) != null ? _a : 0;
    const change = getRelativePosition({
      parent: scrollableRef.current,
      target: targetRef.current,
      axis,
      alignment,
      offset: offset3,
      isList
    }) - (scrollableRef.current ? 0 : start2);
    function animateScroll() {
      if (startTime.current === 0) {
        startTime.current = performance.now();
      }
      const now = performance.now();
      const elapsed = now - startTime.current;
      const t = reducedMotion || duration === 0 ? 1 : elapsed / duration;
      const distance = start2 + change * easing(t);
      setScrollParam({
        parent: scrollableRef.current,
        axis,
        distance
      });
      if (!shouldStop.current && t < 1) {
        frameID.current = requestAnimationFrame(animateScroll);
      } else {
        typeof onScrollFinish === "function" && onScrollFinish();
        startTime.current = 0;
        frameID.current = 0;
        cancel();
      }
    }
    animateScroll();
  }, [axis, duration, easing, isList, offset3, onScrollFinish, reducedMotion]);
  const handleStop = () => {
    if (cancelable) {
      shouldStop.current = true;
    }
  };
  useWindowEvent("wheel", handleStop, {
    passive: true
  });
  useWindowEvent("touchmove", handleStop, {
    passive: true
  });
  (0, import_react44.useEffect)(() => cancel, []);
  return {
    scrollableRef,
    targetRef,
    scrollIntoView,
    cancel
  };
}

// node_modules/@mantine/hooks/esm/use-resize-observer/use-resize-observer.js
var import_react45 = __toESM(require_react());
var defaultState = {
  x: 0,
  y: 0,
  width: 0,
  height: 0,
  top: 0,
  left: 0,
  bottom: 0,
  right: 0
};
function useResizeObserver() {
  const frameID = (0, import_react45.useRef)(0);
  const ref = (0, import_react45.useRef)(null);
  const [rect, setRect] = (0, import_react45.useState)(defaultState);
  const observer = (0, import_react45.useMemo)(() => typeof window !== "undefined" ? new ResizeObserver((entries) => {
    const entry = entries[0];
    if (entry) {
      cancelAnimationFrame(frameID.current);
      frameID.current = requestAnimationFrame(() => {
        if (ref.current) {
          setRect(entry.contentRect);
        }
      });
    }
  }) : null, []);
  (0, import_react45.useEffect)(() => {
    if (ref.current) {
      observer.observe(ref.current);
    }
    return () => {
      observer.disconnect();
      if (frameID.current) {
        cancelAnimationFrame(frameID.current);
      }
    };
  }, [ref.current]);
  return [ref, rect];
}
function useElementSize() {
  const [ref, { width, height }] = useResizeObserver();
  return { ref, width, height };
}

// node_modules/@mantine/hooks/esm/use-scroll-lock/use-scroll-lock.js
var import_react46 = __toESM(require_react());

// node_modules/@mantine/hooks/esm/use-scroll-lock/utils/get-scroll-width.js
function getScrollWidth() {
  if (typeof window === "undefined" || typeof document === "undefined")
    return 0;
  const paddingRight = parseInt(window.getComputedStyle(document.body).paddingRight, 10);
  const scrollbarWidth = window.innerWidth - document.documentElement.clientWidth;
  return paddingRight + scrollbarWidth;
}

// node_modules/@mantine/hooks/esm/use-scroll-lock/utils/get-lock-styles.js
var getLockStyles = ({ disableBodyPadding }) => {
  const scrollWidth = disableBodyPadding ? null : getScrollWidth();
  const styles2 = `body {
        --removed-scroll-width: ${scrollWidth}px;
        touch-action: none;
        overflow: hidden !important;
        position: relative !important;
        ${scrollWidth ? "padding-right: var(--removed-scroll-width) !important;" : ""}
        `;
  return styles2;
};

// node_modules/@mantine/hooks/esm/use-scroll-lock/utils/inject-style-tag.js
function injectStyles(tag, css2) {
  if (tag.styleSheet) {
    tag.styleSheet.cssText = css2;
  } else {
    tag.appendChild(document.createTextNode(css2));
  }
}

// node_modules/@mantine/hooks/esm/use-scroll-lock/utils/insert-style-tag.js
function insertStyleTag(tag) {
  const head = document.head || document.getElementsByTagName("head")[0];
  head.appendChild(tag);
}

// node_modules/@mantine/hooks/esm/use-scroll-lock/utils/make-style-tag.js
function makeStyleTag() {
  const tag = document.createElement("style");
  tag.type = "text/css";
  tag.setAttribute("mantine-scroll-lock", "");
  return tag;
}

// node_modules/@mantine/hooks/esm/use-scroll-lock/use-scroll-lock.js
function useScrollLock(lock, options = {
  disableBodyPadding: false
}) {
  const [scrollLocked, setScrollLocked] = (0, import_react46.useState)(lock || false);
  const scrollTop = (0, import_react46.useRef)(0);
  const { disableBodyPadding } = options;
  const stylesheet = (0, import_react46.useRef)(null);
  const lockScroll = () => {
    scrollTop.current = window.scrollY;
    const styles2 = getLockStyles({ disableBodyPadding });
    const sheet = makeStyleTag();
    injectStyles(sheet, styles2);
    insertStyleTag(sheet);
    stylesheet.current = sheet;
  };
  const unlockScroll = () => {
    if (!(stylesheet == null ? void 0 : stylesheet.current))
      return;
    stylesheet.current.parentNode.removeChild(stylesheet.current);
    stylesheet.current = null;
  };
  (0, import_react46.useEffect)(() => {
    if (scrollLocked) {
      lockScroll();
    } else {
      unlockScroll();
    }
    return unlockScroll;
  }, [scrollLocked]);
  (0, import_react46.useEffect)(() => {
    if (lock !== void 0) {
      setScrollLocked(lock);
    }
  }, [lock]);
  (0, import_react46.useEffect)(() => {
    if (lock === void 0 && typeof window !== "undefined") {
      window.document.body.style.overflow === "hidden" && setScrollLocked(true);
    }
  }, [setScrollLocked]);
  return [scrollLocked, setScrollLocked];
}

// node_modules/@mantine/hooks/esm/use-shallow-effect/use-shallow-effect.js
var import_react47 = __toESM(require_react());

// node_modules/@mantine/hooks/esm/use-toggle/use-toggle.js
var import_react48 = __toESM(require_react());

// node_modules/@mantine/hooks/esm/use-viewport-size/use-viewport-size.js
var import_react49 = __toESM(require_react());

// node_modules/@mantine/hooks/esm/use-window-scroll/use-window-scroll.js
var import_react50 = __toESM(require_react());

// node_modules/@mantine/hooks/esm/use-intersection/use-intersection.js
var import_react51 = __toESM(require_react());

// node_modules/@mantine/hooks/esm/use-hash/use-hash.js
var import_react52 = __toESM(require_react());

// node_modules/@mantine/hooks/esm/use-hotkeys/use-hotkeys.js
var import_react53 = __toESM(require_react());

// node_modules/@mantine/hooks/esm/use-fullscreen/use-fullscreen.js
var import_react54 = __toESM(require_react());

// node_modules/@mantine/hooks/esm/use-logger/use-logger.js
var import_react55 = __toESM(require_react());

// node_modules/@mantine/hooks/esm/use-hover/use-hover.js
var import_react56 = __toESM(require_react());

// node_modules/@mantine/hooks/esm/use-validated-state/use-validated-state.js
var import_react57 = __toESM(require_react());

// node_modules/@mantine/hooks/esm/use-os/use-os.js
function getOS() {
  const { userAgent } = window.navigator;
  const macosPlatforms = /(Macintosh)|(MacIntel)|(MacPPC)|(Mac68K)/i;
  const windowsPlatforms = /(Win32)|(Win64)|(Windows)|(WinCE)/i;
  const iosPlatforms = /(iPhone)|(iPad)|(iPod)/i;
  if (macosPlatforms.test(userAgent)) {
    return "macos";
  }
  if (iosPlatforms.test(userAgent)) {
    return "ios";
  }
  if (windowsPlatforms.test(userAgent)) {
    return "windows";
  }
  if (/Android/i.test(userAgent)) {
    return "android";
  }
  if (/Linux/i.test(userAgent)) {
    return "linux";
  }
  return "undetermined";
}
function useOs() {
  if (typeof window !== "undefined") {
    return getOS();
  }
  return "undetermined";
}

// node_modules/@mantine/hooks/esm/use-set-state/use-set-state.js
var import_react58 = __toESM(require_react());

// node_modules/@mantine/hooks/esm/use-input-state/use-input-state.js
var import_react59 = __toESM(require_react());

// node_modules/@mantine/hooks/esm/use-event-listener/use-event-listener.js
var import_react60 = __toESM(require_react());

// node_modules/@mantine/hooks/esm/use-disclosure/use-disclosure.js
var import_react61 = __toESM(require_react());
function useDisclosure(initialState = false, callbacks) {
  const { onOpen, onClose } = callbacks || {};
  const [opened, setOpened] = (0, import_react61.useState)(initialState);
  const open = (0, import_react61.useCallback)(() => {
    setOpened((isOpened) => {
      if (!isOpened) {
        onOpen == null ? void 0 : onOpen();
        return true;
      }
      return isOpened;
    });
  }, [onOpen]);
  const close = (0, import_react61.useCallback)(() => {
    setOpened((isOpened) => {
      if (isOpened) {
        onClose == null ? void 0 : onClose();
        return false;
      }
      return isOpened;
    });
  }, [onClose]);
  const toggle = (0, import_react61.useCallback)(() => {
    opened ? close() : open();
  }, [close, open, opened]);
  return [opened, { open, close, toggle }];
}

// node_modules/@mantine/hooks/esm/use-focus-within/use-focus-within.js
var import_react62 = __toESM(require_react());

// node_modules/@mantine/hooks/esm/use-network/use-network.js
var import_react63 = __toESM(require_react());

// node_modules/@mantine/hooks/esm/use-timeout/use-timeout.js
var import_react64 = __toESM(require_react());

// node_modules/@mantine/hooks/esm/use-text-selection/use-text-selection.js
var import_react65 = __toESM(require_react());

// node_modules/@mantine/hooks/esm/use-previous/use-previous.js
var import_react66 = __toESM(require_react());
function usePrevious(value) {
  const ref = (0, import_react66.useRef)();
  (0, import_react66.useEffect)(() => {
    ref.current = value;
  }, [value]);
  return ref.current;
}

// node_modules/@mantine/hooks/esm/use-favicon/use-favicon.js
var import_react67 = __toESM(require_react());

// node_modules/@mantine/hooks/esm/use-eye-dropper/use-eye-dropper.js
var import_react68 = __toESM(require_react());
function useEyeDropper() {
  const [supported, setSupported] = (0, import_react68.useState)(false);
  useIsomorphicEffect(() => {
    setSupported(typeof window !== "undefined" && "EyeDropper" in window);
  }, []);
  const open = (0, import_react68.useCallback)((options = {}) => {
    if (supported) {
      const eyeDropper = new window.EyeDropper();
      return eyeDropper.open(options);
    }
    return void 0;
  }, [supported]);
  return { supported, open };
}

// node_modules/@mantine/hooks/esm/utils/random-id/random-id.js
function randomId2() {
  return `mantine-${Math.random().toString(36).slice(2, 11)}`;
}

// node_modules/@mantine/core/esm/Accordion/Accordion.errors.js
var ACCORDION_ERRORS = {
  context: "Accordion component was not found in the tree",
  itemContext: "Accordion.Item component was not found in the tree",
  value: "Accordion.Item component was rendered with invalid value or without value"
};

// node_modules/@mantine/core/esm/Accordion/Accordion.context.js
var [AccordionContextProvider, useAccordionContext] = createSafeContext(ACCORDION_ERRORS.context);

// node_modules/@mantine/core/esm/Accordion/AccordionProvider.js
function AccordionProvider({
  children,
  multiple,
  value,
  defaultValue,
  onChange,
  id,
  loop,
  transitionDuration,
  disableChevronRotation,
  chevronPosition,
  chevronSize,
  order: order2,
  chevron,
  variant: variant2,
  radius: radius2,
  classNames,
  styles: styles2,
  unstyled
}) {
  const uid = useId(id);
  const [_value, handleChange] = useUncontrolled({
    value,
    defaultValue,
    finalValue: multiple ? [] : null,
    onChange
  });
  const isItemActive = (itemValue) => Array.isArray(_value) ? _value.includes(itemValue) : itemValue === _value;
  const handleItemChange = (itemValue) => {
    const nextValue = Array.isArray(_value) ? _value.includes(itemValue) ? _value.filter((selectedValue) => selectedValue !== itemValue) : [..._value, itemValue] : itemValue === _value ? null : itemValue;
    handleChange(nextValue);
  };
  return import_react69.default.createElement(AccordionContextProvider, {
    value: {
      isItemActive,
      onChange: handleItemChange,
      getControlId: getSafeId(`${uid}-control`, ACCORDION_ERRORS.value),
      getRegionId: getSafeId(`${uid}-panel`, ACCORDION_ERRORS.value),
      transitionDuration,
      disableChevronRotation,
      chevronPosition,
      chevronSize,
      order: order2,
      chevron,
      loop,
      variant: variant2,
      radius: radius2,
      classNames,
      styles: styles2,
      unstyled
    }
  }, children);
}

// node_modules/@mantine/core/esm/Accordion/AccordionItem/AccordionItem.js
var import_react71 = __toESM(require_react());

// node_modules/@mantine/core/esm/Accordion/AccordionItem.context.js
var [AccordionItemContextProvider, useAccordionItemContext] = createSafeContext(ACCORDION_ERRORS.itemContext);

// node_modules/@mantine/core/esm/Accordion/AccordionItem/AccordionItem.styles.js
function getVariantStyles(theme, { variant: variant2, radius: radius2 }) {
  const borderColor = theme.colorScheme === "dark" ? theme.colors.dark[4] : theme.colors.gray[3];
  const filledColor = theme.colorScheme === "dark" ? theme.colors.dark[6] : theme.colors.gray[0];
  const borderRadius = theme.fn.radius(radius2);
  if (variant2 === "default") {
    return {
      color: theme.colorScheme === "dark" ? theme.colors.dark[0] : theme.black,
      borderBottom: `1px solid ${borderColor}`
    };
  }
  if (variant2 === "contained") {
    return {
      border: `1px solid ${borderColor}`,
      transition: "background-color 150ms ease",
      "&[data-active]": {
        backgroundColor: filledColor
      },
      "&:first-of-type": {
        borderTopRightRadius: borderRadius,
        borderTopLeftRadius: borderRadius,
        "& > [data-accordion-control]": {
          borderTopRightRadius: borderRadius,
          borderTopLeftRadius: borderRadius
        }
      },
      "&:last-of-type": {
        borderBottomRightRadius: borderRadius,
        borderBottomLeftRadius: borderRadius,
        "& > [data-accordion-control]": {
          borderBottomRightRadius: borderRadius,
          borderBottomLeftRadius: borderRadius
        }
      },
      "& + &": {
        borderTop: 0
      }
    };
  }
  if (variant2 === "filled") {
    return {
      borderRadius,
      "&[data-active]": {
        backgroundColor: filledColor
      }
    };
  }
  if (variant2 === "separated") {
    return {
      borderRadius,
      backgroundColor: filledColor,
      border: "1px solid transparent",
      transition: "background-color 150ms ease",
      "& + &": {
        marginTop: theme.spacing.md
      },
      "&[data-active]": {
        backgroundColor: theme.colorScheme === "dark" ? theme.colors.dark[7] : theme.white,
        borderColor
      }
    };
  }
  return {};
}
var useStyles = createStyles((theme, params) => ({
  item: getVariantStyles(theme, params)
}));
var AccordionItem_styles_default = useStyles;

// node_modules/@mantine/core/esm/Box/Box.js
var import_react70 = __toESM(require_react());

// node_modules/@mantine/core/esm/Box/style-system-props/extract-system-styles/extract-system-styles.js
var __getOwnPropSymbols9 = Object.getOwnPropertySymbols;
var __hasOwnProp9 = Object.prototype.hasOwnProperty;
var __propIsEnum9 = Object.prototype.propertyIsEnumerable;
var __objRest = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp9.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols9)
    for (var prop of __getOwnPropSymbols9(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum9.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
function extractSystemStyles(others) {
  const _a = others, {
    m,
    mx,
    my,
    mt: mt2,
    mb,
    ml,
    mr,
    p,
    px,
    py,
    pt: pt2,
    pb,
    pl,
    pr,
    bg,
    c,
    opacity,
    ff,
    fz,
    fw,
    lts,
    ta,
    lh,
    fs,
    tt,
    td,
    w,
    miw,
    maw,
    h,
    mih,
    mah,
    bgsz,
    bgp,
    bgr,
    bga,
    pos,
    top: top2,
    left: left2,
    bottom: bottom2,
    right: right2,
    inset,
    display
  } = _a, rest = __objRest(_a, [
    "m",
    "mx",
    "my",
    "mt",
    "mb",
    "ml",
    "mr",
    "p",
    "px",
    "py",
    "pt",
    "pb",
    "pl",
    "pr",
    "bg",
    "c",
    "opacity",
    "ff",
    "fz",
    "fw",
    "lts",
    "ta",
    "lh",
    "fs",
    "tt",
    "td",
    "w",
    "miw",
    "maw",
    "h",
    "mih",
    "mah",
    "bgsz",
    "bgp",
    "bgr",
    "bga",
    "pos",
    "top",
    "left",
    "bottom",
    "right",
    "inset",
    "display"
  ]);
  const systemStyles = filterProps({
    m,
    mx,
    my,
    mt: mt2,
    mb,
    ml,
    mr,
    p,
    px,
    py,
    pt: pt2,
    pb,
    pl,
    pr,
    bg,
    c,
    opacity,
    ff,
    fz,
    fw,
    lts,
    ta,
    lh,
    fs,
    tt,
    td,
    w,
    miw,
    maw,
    h,
    mih,
    mah,
    bgsz,
    bgp,
    bgr,
    bga,
    pos,
    top: top2,
    left: left2,
    bottom: bottom2,
    right: right2,
    inset,
    display
  });
  return { systemStyles, rest };
}

// node_modules/@mantine/core/esm/Box/style-system-props/get-responsive-value/get-responsive-value.js
function getSortedKeys(value, theme) {
  const sorted = Object.keys(value).filter((breakpoint) => breakpoint !== "base").sort((a2, b2) => theme.fn.size({ size: a2, sizes: theme.breakpoints }) - theme.fn.size({ size: b2, sizes: theme.breakpoints }));
  return "base" in value ? ["base", ...sorted] : sorted;
}
function getResponsiveValue({ value, theme, getValue, property }) {
  if (value == null) {
    return void 0;
  }
  if (typeof value === "object") {
    const result = getSortedKeys(value, theme).reduce((acc, breakpointKey) => {
      if (breakpointKey === "base" && value.base !== void 0) {
        const baseValue = getValue(value.base, theme);
        if (Array.isArray(property)) {
          property.forEach((prop) => {
            acc[prop] = baseValue;
          });
          return acc;
        }
        acc[property] = baseValue;
        return acc;
      }
      const breakpointValue = getValue(value[breakpointKey], theme);
      if (Array.isArray(property)) {
        acc[theme.fn.largerThan(breakpointKey)] = {};
        property.forEach((prop) => {
          acc[theme.fn.largerThan(breakpointKey)][prop] = breakpointValue;
        });
        return acc;
      }
      acc[theme.fn.largerThan(breakpointKey)] = {
        [property]: breakpointValue
      };
      return acc;
    }, {});
    return result;
  }
  const cssValue = getValue(value, theme);
  if (Array.isArray(property)) {
    return property.reduce((acc, prop) => {
      acc[prop] = cssValue;
      return acc;
    }, {});
  }
  return { [property]: cssValue };
}

// node_modules/@mantine/core/esm/Box/style-system-props/value-getters/get-color-value.js
function getColorValue(color, theme) {
  if (color === "dimmed") {
    return theme.colorScheme === "dark" ? theme.colors.dark[2] : theme.colors.gray[6];
  }
  return theme.fn.variant({ variant: "filled", color, primaryFallback: false }).background;
}

// node_modules/@mantine/core/esm/Box/style-system-props/value-getters/get-default-value.js
function getDefaultValue(value) {
  return value;
}

// node_modules/@mantine/core/esm/Box/style-system-props/value-getters/get-font-size-value.js
function getFontSizeValue(size3, theme) {
  return theme.fn.size({ size: size3, sizes: theme.fontSizes });
}

// node_modules/@mantine/core/esm/Box/style-system-props/value-getters/get-spacing-value.js
var NEGATIVE_VALUES = ["-xs", "-sm", "-md", "-lg", "-xl"];
function getSpacingValue(size3, theme) {
  if (NEGATIVE_VALUES.includes(size3)) {
    return theme.fn.size({ size: size3.replace("-", ""), sizes: theme.spacing }) * -1;
  }
  return theme.fn.size({ size: size3, sizes: theme.spacing });
}

// node_modules/@mantine/core/esm/Box/style-system-props/value-getters/value-getters.js
var valueGetters = {
  color: getColorValue,
  default: getDefaultValue,
  fontSize: getFontSizeValue,
  spacing: getSpacingValue
};

// node_modules/@mantine/core/esm/Box/style-system-props/system-props/system-props.js
var SYSTEM_PROPS = {
  m: { type: "spacing", property: "margin" },
  mt: { type: "spacing", property: "marginTop" },
  mb: { type: "spacing", property: "marginBottom" },
  ml: { type: "spacing", property: "marginLeft" },
  mr: { type: "spacing", property: "marginRight" },
  mx: { type: "spacing", property: ["marginRight", "marginLeft"] },
  my: { type: "spacing", property: ["marginTop", "marginBottom"] },
  p: { type: "spacing", property: "padding" },
  pt: { type: "spacing", property: "paddingTop" },
  pb: { type: "spacing", property: "paddingBottom" },
  pl: { type: "spacing", property: "paddingLeft" },
  pr: { type: "spacing", property: "paddingRight" },
  px: { type: "spacing", property: ["paddingRight", "paddingLeft"] },
  py: { type: "spacing", property: ["paddingTop", "paddingBottom"] },
  bg: { type: "color", property: "background" },
  c: { type: "color", property: "color" },
  opacity: { type: "default", property: "opacity" },
  ff: { type: "default", property: "fontFamily" },
  fz: { type: "fontSize", property: "fontSize" },
  fw: { type: "default", property: "fontWeight" },
  lts: { type: "default", property: "letterSpacing" },
  ta: { type: "default", property: "textAlign" },
  lh: { type: "default", property: "lineHeight" },
  fs: { type: "default", property: "fontStyle" },
  tt: { type: "default", property: "textTransform" },
  td: { type: "default", property: "textDecoration" },
  w: { type: "spacing", property: "width" },
  miw: { type: "spacing", property: "minWidth" },
  maw: { type: "spacing", property: "maxWidth" },
  h: { type: "spacing", property: "height" },
  mih: { type: "spacing", property: "minHeight" },
  mah: { type: "spacing", property: "maxHeight" },
  bgsz: { type: "default", property: "background-size" },
  bgp: { type: "default", property: "background-position" },
  bgr: { type: "default", property: "background-repeat" },
  bga: { type: "default", property: "background-attachment" },
  pos: { type: "default", property: "position" },
  top: { type: "default", property: "top" },
  left: { type: "default", property: "left" },
  bottom: { type: "default", property: "bottom" },
  right: { type: "default", property: "right" },
  inset: { type: "default", property: "inset" },
  display: { type: "default", property: "display" }
};

// node_modules/@mantine/core/esm/Box/style-system-props/get-system-styles/get-system-styles.js
var __defProp9 = Object.defineProperty;
var __getOwnPropSymbols10 = Object.getOwnPropertySymbols;
var __hasOwnProp10 = Object.prototype.hasOwnProperty;
var __propIsEnum10 = Object.prototype.propertyIsEnumerable;
var __defNormalProp9 = (obj, key, value) => key in obj ? __defProp9(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues9 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp10.call(b2, prop))
      __defNormalProp9(a2, prop, b2[prop]);
  if (__getOwnPropSymbols10)
    for (var prop of __getOwnPropSymbols10(b2)) {
      if (__propIsEnum10.call(b2, prop))
        __defNormalProp9(a2, prop, b2[prop]);
    }
  return a2;
};
function getSystemStyles(systemStyles, theme, systemProps = SYSTEM_PROPS) {
  const styles2 = Object.keys(systemProps).reduce((acc, systemProp) => {
    if (systemProp in systemStyles && systemStyles[systemProp] !== void 0) {
      acc.push(getResponsiveValue({
        value: systemStyles[systemProp],
        getValue: valueGetters[systemProps[systemProp].type],
        property: systemProps[systemProp].property,
        theme
      }));
    }
    return acc;
  }, []);
  return styles2.reduce((acc, stylesPartial) => {
    Object.keys(stylesPartial).forEach((property) => {
      if (typeof stylesPartial[property] === "object" && stylesPartial[property] !== null) {
        if (!(property in acc)) {
          acc[property] = stylesPartial[property];
        } else {
          acc[property] = __spreadValues9(__spreadValues9({}, acc[property]), stylesPartial[property]);
        }
      } else {
        acc[property] = stylesPartial[property];
      }
    });
    return acc;
  }, {});
}

// node_modules/@mantine/core/esm/Box/use-sx/use-sx.js
function extractSx(sx, theme) {
  return typeof sx === "function" ? sx(theme) : sx;
}
function useSx(sx, systemProps, className) {
  const theme = useMantineTheme();
  const { css: css2, cx } = useCss();
  if (Array.isArray(sx)) {
    return cx(className, css2(getSystemStyles(systemProps, theme)), sx.map((partial) => css2(extractSx(partial, theme))));
  }
  return cx(className, css2(extractSx(sx, theme)), css2(getSystemStyles(systemProps, theme)));
}

// node_modules/@mantine/core/esm/Box/Box.js
var __defProp10 = Object.defineProperty;
var __getOwnPropSymbols11 = Object.getOwnPropertySymbols;
var __hasOwnProp11 = Object.prototype.hasOwnProperty;
var __propIsEnum11 = Object.prototype.propertyIsEnumerable;
var __defNormalProp10 = (obj, key, value) => key in obj ? __defProp10(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues10 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp11.call(b2, prop))
      __defNormalProp10(a2, prop, b2[prop]);
  if (__getOwnPropSymbols11)
    for (var prop of __getOwnPropSymbols11(b2)) {
      if (__propIsEnum11.call(b2, prop))
        __defNormalProp10(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest2 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp11.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols11)
    for (var prop of __getOwnPropSymbols11(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum11.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var _Box = (0, import_react70.forwardRef)((_a, ref) => {
  var _b = _a, { className, component, style, sx } = _b, others = __objRest2(_b, ["className", "component", "style", "sx"]);
  const { systemStyles, rest } = extractSystemStyles(others);
  const Element2 = component || "div";
  return import_react70.default.createElement(Element2, __spreadValues10({
    ref,
    className: useSx(sx, systemStyles, className),
    style
  }, rest));
});
_Box.displayName = "@mantine/core/Box";
var Box = createPolymorphicComponent(_Box);

// node_modules/@mantine/core/esm/Accordion/AccordionItem/AccordionItem.js
var __defProp11 = Object.defineProperty;
var __getOwnPropSymbols12 = Object.getOwnPropertySymbols;
var __hasOwnProp12 = Object.prototype.hasOwnProperty;
var __propIsEnum12 = Object.prototype.propertyIsEnumerable;
var __defNormalProp11 = (obj, key, value) => key in obj ? __defProp11(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues11 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp12.call(b2, prop))
      __defNormalProp11(a2, prop, b2[prop]);
  if (__getOwnPropSymbols12)
    for (var prop of __getOwnPropSymbols12(b2)) {
      if (__propIsEnum12.call(b2, prop))
        __defNormalProp11(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest3 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp12.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols12)
    for (var prop of __getOwnPropSymbols12(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum12.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps = {};
var AccordionItem = (0, import_react71.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("AccordionItem", defaultProps, props), { children, className, value } = _a, others = __objRest3(_a, ["children", "className", "value"]);
  const ctx = useAccordionContext();
  const { classes, cx } = AccordionItem_styles_default({ variant: ctx.variant, radius: ctx.radius }, { name: "Accordion", classNames: ctx.classNames, styles: ctx.styles, unstyled: ctx.unstyled });
  return import_react71.default.createElement(AccordionItemContextProvider, {
    value: { value }
  }, import_react71.default.createElement(Box, __spreadValues11({
    ref,
    className: cx(classes.item, className),
    "data-active": ctx.isItemActive(value) || void 0
  }, others), children));
});
AccordionItem.displayName = "@mantine/core/AccordionItem";

// node_modules/@mantine/core/esm/Accordion/AccordionControl/AccordionControl.js
var import_react73 = __toESM(require_react());

// node_modules/@mantine/core/esm/Accordion/AccordionControl/AccordionControl.styles.js
var __defProp12 = Object.defineProperty;
var __defProps5 = Object.defineProperties;
var __getOwnPropDescs5 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols13 = Object.getOwnPropertySymbols;
var __hasOwnProp13 = Object.prototype.hasOwnProperty;
var __propIsEnum13 = Object.prototype.propertyIsEnumerable;
var __defNormalProp12 = (obj, key, value) => key in obj ? __defProp12(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues12 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp13.call(b2, prop))
      __defNormalProp12(a2, prop, b2[prop]);
  if (__getOwnPropSymbols13)
    for (var prop of __getOwnPropSymbols13(b2)) {
      if (__propIsEnum13.call(b2, prop))
        __defNormalProp12(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps5 = (a2, b2) => __defProps5(a2, __getOwnPropDescs5(b2));
var __objRest4 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp13.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols13)
    for (var prop of __getOwnPropSymbols13(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum13.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
function getVariantStyles2(theme, { variant: variant2 }) {
  if (variant2 === "default" || variant2 === "contained") {
    return theme.fn.hover({
      backgroundColor: theme.colorScheme === "dark" ? theme.colors.dark[6] : theme.colors.gray[0]
    });
  }
  return {};
}
var useStyles2 = createStyles((theme, _a) => {
  var _b = _a, { transitionDuration, chevronPosition, chevronSize } = _b, params = __objRest4(_b, ["transitionDuration", "chevronPosition", "chevronSize"]);
  return {
    icon: {
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      marginRight: chevronPosition === "left" ? 0 : theme.spacing.sm,
      marginLeft: chevronPosition === "left" ? theme.spacing.lg : 0
    },
    chevron: {
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      transition: `transform ${transitionDuration}ms ease`,
      marginRight: chevronPosition === "right" ? 0 : theme.spacing.sm,
      marginLeft: chevronPosition === "right" ? theme.spacing.lg : 0,
      width: chevronSize,
      minWidth: chevronSize,
      "&[data-rotate]": {
        transform: "rotate(180deg)"
      }
    },
    label: {
      color: "inherit",
      fontWeight: 400,
      flex: 1,
      overflow: "hidden",
      textOverflow: "ellipsis"
    },
    itemTitle: {
      margin: 0,
      padding: 0
    },
    control: __spreadProps5(__spreadValues12(__spreadValues12(__spreadValues12({}, theme.fn.focusStyles()), theme.fn.fontStyles()), getVariantStyles2(theme, params)), {
      width: "100%",
      display: "flex",
      alignItems: "center",
      flexDirection: chevronPosition === "right" ? "row-reverse" : "row",
      padding: `${theme.spacing.md}px ${theme.spacing.md / 2}px`,
      paddingLeft: chevronPosition === "right" ? `calc(${theme.spacing.sm}px + 4px)` : null,
      textAlign: "left",
      color: theme.colorScheme === "dark" ? theme.colors.dark[0] : theme.black,
      "&:disabled": __spreadValues12({
        opacity: 0.4,
        cursor: "not-allowed"
      }, theme.fn.hover({ backgroundColor: "transparent" }))
    })
  };
});
var AccordionControl_styles_default = useStyles2;

// node_modules/@mantine/core/esm/UnstyledButton/UnstyledButton.js
var import_react72 = __toESM(require_react());

// node_modules/@mantine/core/esm/UnstyledButton/UnstyledButton.styles.js
var __defProp13 = Object.defineProperty;
var __defProps6 = Object.defineProperties;
var __getOwnPropDescs6 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols14 = Object.getOwnPropertySymbols;
var __hasOwnProp14 = Object.prototype.hasOwnProperty;
var __propIsEnum14 = Object.prototype.propertyIsEnumerable;
var __defNormalProp13 = (obj, key, value) => key in obj ? __defProp13(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues13 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp14.call(b2, prop))
      __defNormalProp13(a2, prop, b2[prop]);
  if (__getOwnPropSymbols14)
    for (var prop of __getOwnPropSymbols14(b2)) {
      if (__propIsEnum14.call(b2, prop))
        __defNormalProp13(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps6 = (a2, b2) => __defProps6(a2, __getOwnPropDescs6(b2));
var useStyles3 = createStyles((theme) => ({
  root: __spreadProps6(__spreadValues13(__spreadValues13({}, theme.fn.focusStyles()), theme.fn.fontStyles()), {
    cursor: "pointer",
    border: 0,
    padding: 0,
    appearance: "none",
    fontSize: theme.fontSizes.md,
    backgroundColor: "transparent",
    textAlign: "left",
    color: theme.colorScheme === "dark" ? theme.colors.dark[0] : theme.black,
    textDecoration: "none",
    boxSizing: "border-box"
  })
}));
var UnstyledButton_styles_default = useStyles3;

// node_modules/@mantine/core/esm/UnstyledButton/UnstyledButton.js
var __defProp14 = Object.defineProperty;
var __getOwnPropSymbols15 = Object.getOwnPropertySymbols;
var __hasOwnProp15 = Object.prototype.hasOwnProperty;
var __propIsEnum15 = Object.prototype.propertyIsEnumerable;
var __defNormalProp14 = (obj, key, value) => key in obj ? __defProp14(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues14 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp15.call(b2, prop))
      __defNormalProp14(a2, prop, b2[prop]);
  if (__getOwnPropSymbols15)
    for (var prop of __getOwnPropSymbols15(b2)) {
      if (__propIsEnum15.call(b2, prop))
        __defNormalProp14(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest5 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp15.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols15)
    for (var prop of __getOwnPropSymbols15(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum15.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var _UnstyledButton = (0, import_react72.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("UnstyledButton", {}, props), {
    className,
    component = "button",
    unstyled
  } = _a, others = __objRest5(_a, [
    "className",
    "component",
    "unstyled"
  ]);
  const { classes, cx } = UnstyledButton_styles_default(null, { name: "UnstyledButton", unstyled });
  return import_react72.default.createElement(Box, __spreadValues14({
    component,
    ref,
    className: cx(classes.root, className),
    type: component === "button" ? "button" : void 0
  }, others));
});
_UnstyledButton.displayName = "@mantine/core/UnstyledButton";
var UnstyledButton = createPolymorphicComponent(_UnstyledButton);

// node_modules/@mantine/core/esm/Accordion/AccordionControl/AccordionControl.js
var __defProp15 = Object.defineProperty;
var __defProps7 = Object.defineProperties;
var __getOwnPropDescs7 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols16 = Object.getOwnPropertySymbols;
var __hasOwnProp16 = Object.prototype.hasOwnProperty;
var __propIsEnum16 = Object.prototype.propertyIsEnumerable;
var __defNormalProp15 = (obj, key, value) => key in obj ? __defProp15(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues15 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp16.call(b2, prop))
      __defNormalProp15(a2, prop, b2[prop]);
  if (__getOwnPropSymbols16)
    for (var prop of __getOwnPropSymbols16(b2)) {
      if (__propIsEnum16.call(b2, prop))
        __defNormalProp15(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps7 = (a2, b2) => __defProps7(a2, __getOwnPropDescs7(b2));
var __objRest6 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp16.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols16)
    for (var prop of __getOwnPropSymbols16(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum16.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps2 = {};
var AccordionControl = (0, import_react73.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("AccordionControl", defaultProps2, props), { disabled, onKeyDown, onClick, chevron, children, className, icon } = _a, others = __objRest6(_a, ["disabled", "onKeyDown", "onClick", "chevron", "children", "className", "icon"]);
  const ctx = useAccordionContext();
  const { value } = useAccordionItemContext();
  const { classes, cx } = AccordionControl_styles_default({
    transitionDuration: ctx.transitionDuration,
    chevronPosition: ctx.chevronPosition,
    chevronSize: ctx.chevronSize,
    variant: ctx.variant,
    radius: ctx.radius
  }, { name: "Accordion", classNames: ctx.classNames, styles: ctx.styles, unstyled: ctx.unstyled });
  const isActive = ctx.isItemActive(value);
  const shouldWrapWithHeading = typeof ctx.order === "number";
  const Heading = `h${ctx.order}`;
  const content = import_react73.default.createElement(UnstyledButton, __spreadProps7(__spreadValues15({}, others), {
    ref,
    "data-accordion-control": true,
    disabled,
    className: cx(classes.control, className),
    onClick: (event) => {
      onClick == null ? void 0 : onClick(event);
      ctx.onChange(value);
    },
    type: "button",
    "data-active": isActive || void 0,
    "aria-expanded": isActive,
    "aria-controls": ctx.getRegionId(value),
    id: ctx.getControlId(value),
    unstyled: ctx.unstyled,
    onKeyDown: createScopedKeydownHandler({
      siblingSelector: "[data-accordion-control]",
      parentSelector: "[data-accordion]",
      activateOnFocus: false,
      loop: ctx.loop,
      orientation: "vertical",
      onKeyDown
    })
  }), import_react73.default.createElement("div", {
    className: classes.chevron,
    "data-rotate": !ctx.disableChevronRotation && isActive || void 0
  }, chevron || ctx.chevron), import_react73.default.createElement("div", {
    className: classes.label
  }, children), icon && import_react73.default.createElement("div", {
    className: classes.icon
  }, icon));
  return shouldWrapWithHeading ? import_react73.default.createElement(Heading, {
    className: classes.itemTitle
  }, content) : content;
});
AccordionControl.displayName = "@mantine/core/AccordionControl";

// node_modules/@mantine/core/esm/Accordion/AccordionPanel/AccordionPanel.js
var import_react76 = __toESM(require_react());

// node_modules/@mantine/core/esm/Accordion/AccordionPanel/AccordionPanel.styles.js
var __defProp16 = Object.defineProperty;
var __defProps8 = Object.defineProperties;
var __getOwnPropDescs8 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols17 = Object.getOwnPropertySymbols;
var __hasOwnProp17 = Object.prototype.hasOwnProperty;
var __propIsEnum17 = Object.prototype.propertyIsEnumerable;
var __defNormalProp16 = (obj, key, value) => key in obj ? __defProp16(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues16 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp17.call(b2, prop))
      __defNormalProp16(a2, prop, b2[prop]);
  if (__getOwnPropSymbols17)
    for (var prop of __getOwnPropSymbols17(b2)) {
      if (__propIsEnum17.call(b2, prop))
        __defNormalProp16(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps8 = (a2, b2) => __defProps8(a2, __getOwnPropDescs8(b2));
var useStyles4 = createStyles((theme, _params) => ({
  panel: __spreadProps8(__spreadValues16({}, theme.fn.fontStyles()), {
    wordBreak: "break-word",
    lineHeight: theme.lineHeight
  }),
  content: {
    padding: theme.spacing.md,
    paddingTop: `calc(${theme.spacing.xs}px / 2)`
  }
}));
var AccordionPanel_styles_default = useStyles4;

// node_modules/@mantine/core/esm/Collapse/Collapse.js
var import_react75 = __toESM(require_react());

// node_modules/@mantine/core/esm/Collapse/use-collapse.js
var import_react74 = __toESM(require_react());
var import_react_dom = __toESM(require_react_dom());
var __defProp17 = Object.defineProperty;
var __defProps9 = Object.defineProperties;
var __getOwnPropDescs9 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols18 = Object.getOwnPropertySymbols;
var __hasOwnProp18 = Object.prototype.hasOwnProperty;
var __propIsEnum18 = Object.prototype.propertyIsEnumerable;
var __defNormalProp17 = (obj, key, value) => key in obj ? __defProp17(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues17 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp18.call(b2, prop))
      __defNormalProp17(a2, prop, b2[prop]);
  if (__getOwnPropSymbols18)
    for (var prop of __getOwnPropSymbols18(b2)) {
      if (__propIsEnum18.call(b2, prop))
        __defNormalProp17(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps9 = (a2, b2) => __defProps9(a2, __getOwnPropDescs9(b2));
var __objRest7 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp18.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols18)
    for (var prop of __getOwnPropSymbols18(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum18.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
function getAutoHeightDuration(height) {
  if (!height || typeof height === "string") {
    return 0;
  }
  const constant = height / 36;
  return Math.round((4 + 15 * constant ** 0.25 + constant / 5) * 10);
}
function getElementHeight(el) {
  return (el == null ? void 0 : el.current) ? el.current.scrollHeight : "auto";
}
var raf = typeof window !== "undefined" && window.requestAnimationFrame;
function useCollapse({
  transitionDuration,
  transitionTimingFunction = "ease",
  onTransitionEnd = () => {
  },
  opened
}) {
  const el = (0, import_react74.useRef)(null);
  const collapsedHeight = "0px";
  const collapsedStyles = {
    display: "none",
    height: "0px",
    overflow: "hidden"
  };
  const [styles2, setStylesRaw] = (0, import_react74.useState)(opened ? {} : collapsedStyles);
  const setStyles = (newStyles) => {
    (0, import_react_dom.flushSync)(() => setStylesRaw(newStyles));
  };
  const mergeStyles = (newStyles) => {
    setStyles((oldStyles) => __spreadValues17(__spreadValues17({}, oldStyles), newStyles));
  };
  function getTransitionStyles2(height) {
    const _duration = transitionDuration || getAutoHeightDuration(height);
    return {
      transition: `height ${_duration}ms ${transitionTimingFunction}`
    };
  }
  useDidUpdate(() => {
    if (opened) {
      raf(() => {
        mergeStyles({ willChange: "height", display: "block", overflow: "hidden" });
        raf(() => {
          const height = getElementHeight(el);
          mergeStyles(__spreadProps9(__spreadValues17({}, getTransitionStyles2(height)), { height }));
        });
      });
    } else {
      raf(() => {
        const height = getElementHeight(el);
        mergeStyles(__spreadProps9(__spreadValues17({}, getTransitionStyles2(height)), { willChange: "height", height }));
        raf(() => mergeStyles({ height: collapsedHeight, overflow: "hidden" }));
      });
    }
  }, [opened]);
  const handleTransitionEnd = (e) => {
    if (e.target !== el.current || e.propertyName !== "height") {
      return;
    }
    if (opened) {
      const height = getElementHeight(el);
      if (height === styles2.height) {
        setStyles({});
      } else {
        mergeStyles({ height });
      }
      onTransitionEnd();
    } else if (styles2.height === collapsedHeight) {
      setStyles(collapsedStyles);
      onTransitionEnd();
    }
  };
  function getCollapseProps(_a = {}) {
    var _b = _a, { style = {}, refKey = "ref" } = _b, rest = __objRest7(_b, ["style", "refKey"]);
    const theirRef = rest[refKey];
    return __spreadProps9(__spreadValues17({
      "aria-hidden": !opened
    }, rest), {
      [refKey]: mergeRefs(el, theirRef),
      onTransitionEnd: handleTransitionEnd,
      style: __spreadValues17(__spreadValues17({ boxSizing: "border-box" }, style), styles2)
    });
  }
  return getCollapseProps;
}

// node_modules/@mantine/core/esm/Collapse/Collapse.js
var __defProp18 = Object.defineProperty;
var __getOwnPropSymbols19 = Object.getOwnPropertySymbols;
var __hasOwnProp19 = Object.prototype.hasOwnProperty;
var __propIsEnum19 = Object.prototype.propertyIsEnumerable;
var __defNormalProp18 = (obj, key, value) => key in obj ? __defProp18(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues18 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp19.call(b2, prop))
      __defNormalProp18(a2, prop, b2[prop]);
  if (__getOwnPropSymbols19)
    for (var prop of __getOwnPropSymbols19(b2)) {
      if (__propIsEnum19.call(b2, prop))
        __defNormalProp18(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest8 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp19.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols19)
    for (var prop of __getOwnPropSymbols19(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum19.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps3 = {
  transitionDuration: 200,
  transitionTimingFunction: "ease",
  animateOpacity: true
};
var Collapse = (0, import_react75.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("Collapse", defaultProps3, props), {
    children,
    in: opened,
    transitionDuration,
    transitionTimingFunction,
    style,
    onTransitionEnd,
    animateOpacity
  } = _a, others = __objRest8(_a, [
    "children",
    "in",
    "transitionDuration",
    "transitionTimingFunction",
    "style",
    "onTransitionEnd",
    "animateOpacity"
  ]);
  const theme = useMantineTheme();
  const shouldReduceMotion = useReducedMotion();
  const reduceMotion = theme.respectReducedMotion ? shouldReduceMotion : false;
  const duration = reduceMotion ? 0 : transitionDuration;
  const { systemStyles, rest } = extractSystemStyles(others);
  const getCollapseProps = useCollapse({
    opened,
    transitionDuration: duration,
    transitionTimingFunction,
    onTransitionEnd
  });
  if (duration === 0) {
    return opened ? import_react75.default.createElement(Box, __spreadValues18({}, rest), children) : null;
  }
  return import_react75.default.createElement(Box, __spreadValues18({}, getCollapseProps(__spreadValues18(__spreadValues18({ style, ref }, rest), systemStyles))), import_react75.default.createElement("div", {
    style: {
      opacity: opened || !animateOpacity ? 1 : 0,
      transition: animateOpacity ? `opacity ${duration}ms ${transitionTimingFunction}` : "none"
    }
  }, children));
});
Collapse.displayName = "@mantine/core/Collapse";

// node_modules/@mantine/core/esm/Accordion/AccordionPanel/AccordionPanel.js
var __defProp19 = Object.defineProperty;
var __defProps10 = Object.defineProperties;
var __getOwnPropDescs10 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols20 = Object.getOwnPropertySymbols;
var __hasOwnProp20 = Object.prototype.hasOwnProperty;
var __propIsEnum20 = Object.prototype.propertyIsEnumerable;
var __defNormalProp19 = (obj, key, value) => key in obj ? __defProp19(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues19 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp20.call(b2, prop))
      __defNormalProp19(a2, prop, b2[prop]);
  if (__getOwnPropSymbols20)
    for (var prop of __getOwnPropSymbols20(b2)) {
      if (__propIsEnum20.call(b2, prop))
        __defNormalProp19(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps10 = (a2, b2) => __defProps10(a2, __getOwnPropDescs10(b2));
var __objRest9 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp20.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols20)
    for (var prop of __getOwnPropSymbols20(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum20.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps4 = {};
function AccordionPanel(props) {
  const _a = useComponentDefaultProps("AccordionPanel", defaultProps4, props), { children, className } = _a, others = __objRest9(_a, ["children", "className"]);
  const ctx = useAccordionContext();
  const { value } = useAccordionItemContext();
  const { classNames, styles: styles2, unstyled } = useAccordionContext();
  const { classes, cx } = AccordionPanel_styles_default({ variant: ctx.variant, radius: ctx.radius }, { name: "Accordion", classNames, styles: styles2, unstyled });
  return import_react76.default.createElement(Collapse, __spreadProps10(__spreadValues19({}, others), {
    className: cx(classes.panel, className),
    in: ctx.isItemActive(value),
    transitionDuration: ctx.transitionDuration,
    role: "region",
    id: ctx.getRegionId(value),
    "aria-labelledby": ctx.getControlId(value)
  }), import_react76.default.createElement("div", {
    className: classes.content
  }, children));
}
AccordionPanel.displayName = "@mantine/core/AccordionPanel";

// node_modules/@mantine/core/esm/Accordion/ChevronIcon.js
var import_react77 = __toESM(require_react());
var __defProp20 = Object.defineProperty;
var __getOwnPropSymbols21 = Object.getOwnPropertySymbols;
var __hasOwnProp21 = Object.prototype.hasOwnProperty;
var __propIsEnum21 = Object.prototype.propertyIsEnumerable;
var __defNormalProp20 = (obj, key, value) => key in obj ? __defProp20(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues20 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp21.call(b2, prop))
      __defNormalProp20(a2, prop, b2[prop]);
  if (__getOwnPropSymbols21)
    for (var prop of __getOwnPropSymbols21(b2)) {
      if (__propIsEnum21.call(b2, prop))
        __defNormalProp20(a2, prop, b2[prop]);
    }
  return a2;
};
function ChevronIcon(props) {
  return import_react77.default.createElement("svg", __spreadValues20({
    viewBox: "0 0 15 15",
    fill: "none",
    xmlns: "http://www.w3.org/2000/svg",
    width: 16,
    height: 16
  }, props), import_react77.default.createElement("path", {
    d: "M3.13523 6.15803C3.3241 5.95657 3.64052 5.94637 3.84197 6.13523L7.5 9.56464L11.158 6.13523C11.3595 5.94637 11.6759 5.95657 11.8648 6.15803C12.0536 6.35949 12.0434 6.67591 11.842 6.86477L7.84197 10.6148C7.64964 10.7951 7.35036 10.7951 7.15803 10.6148L3.15803 6.86477C2.95657 6.67591 2.94637 6.35949 3.13523 6.15803Z",
    fill: "currentColor",
    fillRule: "evenodd",
    clipRule: "evenodd"
  }));
}

// node_modules/@mantine/core/esm/Accordion/Accordion.js
var __defProp21 = Object.defineProperty;
var __defProps11 = Object.defineProperties;
var __getOwnPropDescs11 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols22 = Object.getOwnPropertySymbols;
var __hasOwnProp22 = Object.prototype.hasOwnProperty;
var __propIsEnum22 = Object.prototype.propertyIsEnumerable;
var __defNormalProp21 = (obj, key, value) => key in obj ? __defProp21(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues21 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp22.call(b2, prop))
      __defNormalProp21(a2, prop, b2[prop]);
  if (__getOwnPropSymbols22)
    for (var prop of __getOwnPropSymbols22(b2)) {
      if (__propIsEnum22.call(b2, prop))
        __defNormalProp21(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps11 = (a2, b2) => __defProps11(a2, __getOwnPropDescs11(b2));
var __objRest10 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp22.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols22)
    for (var prop of __getOwnPropSymbols22(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum22.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps5 = {
  multiple: false,
  disableChevronRotation: false,
  transitionDuration: 200,
  chevronPosition: "right",
  variant: "default",
  chevronSize: 24,
  chevron: import_react78.default.createElement(ChevronIcon, null)
};
function Accordion(props) {
  const _a = useComponentDefaultProps("Accordion", defaultProps5, props), {
    id,
    loop,
    children,
    multiple,
    value,
    defaultValue,
    onChange,
    transitionDuration,
    disableChevronRotation,
    chevronPosition,
    chevronSize,
    order: order2,
    chevron,
    classNames,
    styles: styles2,
    unstyled,
    variant: variant2,
    radius: radius2
  } = _a, others = __objRest10(_a, [
    "id",
    "loop",
    "children",
    "multiple",
    "value",
    "defaultValue",
    "onChange",
    "transitionDuration",
    "disableChevronRotation",
    "chevronPosition",
    "chevronSize",
    "order",
    "chevron",
    "classNames",
    "styles",
    "unstyled",
    "variant",
    "radius"
  ]);
  return import_react78.default.createElement(AccordionProvider, {
    id,
    multiple,
    value,
    defaultValue,
    onChange,
    loop,
    transitionDuration,
    disableChevronRotation,
    chevronPosition,
    chevronSize,
    order: order2,
    chevron,
    variant: variant2,
    radius: radius2,
    classNames,
    styles: styles2,
    unstyled
  }, import_react78.default.createElement(Box, __spreadProps11(__spreadValues21({}, others), {
    "data-accordion": true
  }), children));
}
Accordion.Item = AccordionItem;
Accordion.Control = AccordionControl;
Accordion.Panel = AccordionPanel;
Accordion.displayName = "@mantine/core/Accordion";

// node_modules/@mantine/core/esm/ActionIcon/ActionIcon.js
var import_react83 = __toESM(require_react());

// node_modules/@mantine/core/esm/ActionIcon/ActionIcon.styles.js
var __defProp22 = Object.defineProperty;
var __defProps12 = Object.defineProperties;
var __getOwnPropDescs12 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols23 = Object.getOwnPropertySymbols;
var __hasOwnProp23 = Object.prototype.hasOwnProperty;
var __propIsEnum23 = Object.prototype.propertyIsEnumerable;
var __defNormalProp22 = (obj, key, value) => key in obj ? __defProp22(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues22 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp23.call(b2, prop))
      __defNormalProp22(a2, prop, b2[prop]);
  if (__getOwnPropSymbols23)
    for (var prop of __getOwnPropSymbols23(b2)) {
      if (__propIsEnum23.call(b2, prop))
        __defNormalProp22(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps12 = (a2, b2) => __defProps12(a2, __getOwnPropDescs12(b2));
var sizes = {
  xs: 18,
  sm: 22,
  md: 28,
  lg: 34,
  xl: 44
};
function getVariantStyles3({ variant: variant2, theme, color, gradient: gradient2 }) {
  const colors = theme.fn.variant({ color, variant: variant2, gradient: gradient2 });
  if (variant2 === "gradient") {
    return {
      border: 0,
      backgroundImage: colors.background,
      color: colors.color,
      "&:hover": theme.fn.hover({
        backgroundSize: "200%"
      })
    };
  }
  return __spreadValues22({
    border: `1px solid ${colors.border}`,
    backgroundColor: colors.background,
    color: colors.color
  }, theme.fn.hover({
    backgroundColor: colors.hover
  }));
}
var useStyles5 = createStyles((theme, { color, size: size3, radius: radius2, variant: variant2, gradient: gradient2 }) => ({
  root: __spreadProps12(__spreadValues22({}, getVariantStyles3({ variant: variant2, theme, color, gradient: gradient2 })), {
    position: "relative",
    height: theme.fn.size({ size: size3, sizes }),
    minHeight: theme.fn.size({ size: size3, sizes }),
    width: theme.fn.size({ size: size3, sizes }),
    minWidth: theme.fn.size({ size: size3, sizes }),
    borderRadius: theme.fn.radius(radius2),
    padding: 0,
    lineHeight: 1,
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    "&:active": theme.activeStyles,
    "&:disabled, &[data-disabled]": {
      color: theme.colors.gray[theme.colorScheme === "dark" ? 6 : 4],
      cursor: "not-allowed",
      backgroundColor: variant2 === "transparent" ? void 0 : theme.fn.themeColor("gray", theme.colorScheme === "dark" ? 8 : 1),
      borderColor: variant2 === "transparent" ? void 0 : theme.fn.themeColor("gray", theme.colorScheme === "dark" ? 8 : 1),
      backgroundImage: "none",
      pointerEvents: "none",
      "&:active": {
        transform: "none"
      }
    },
    "&[data-loading]": {
      pointerEvents: "none",
      "&::before": {
        content: '""',
        position: "absolute",
        top: -1,
        left: -1,
        right: -1,
        bottom: -1,
        backgroundColor: theme.colorScheme === "dark" ? theme.fn.rgba(theme.colors.dark[7], 0.5) : "rgba(255, 255, 255, .5)",
        borderRadius: theme.fn.radius(radius2),
        cursor: "not-allowed"
      }
    }
  })
}));
var ActionIcon_styles_default = useStyles5;

// node_modules/@mantine/core/esm/Loader/Loader.js
var import_react82 = __toESM(require_react());

// node_modules/@mantine/core/esm/Loader/loaders/Bars.js
var import_react79 = __toESM(require_react());
var __defProp23 = Object.defineProperty;
var __getOwnPropSymbols24 = Object.getOwnPropertySymbols;
var __hasOwnProp24 = Object.prototype.hasOwnProperty;
var __propIsEnum24 = Object.prototype.propertyIsEnumerable;
var __defNormalProp23 = (obj, key, value) => key in obj ? __defProp23(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues23 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp24.call(b2, prop))
      __defNormalProp23(a2, prop, b2[prop]);
  if (__getOwnPropSymbols24)
    for (var prop of __getOwnPropSymbols24(b2)) {
      if (__propIsEnum24.call(b2, prop))
        __defNormalProp23(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest11 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp24.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols24)
    for (var prop of __getOwnPropSymbols24(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum24.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
function Bars(_a) {
  var _b = _a, { size: size3, color } = _b, others = __objRest11(_b, ["size", "color"]);
  return import_react79.default.createElement("svg", __spreadValues23({
    viewBox: "0 0 135 140",
    xmlns: "http://www.w3.org/2000/svg",
    fill: color,
    width: `${size3}px`
  }, others), import_react79.default.createElement("rect", {
    y: "10",
    width: "15",
    height: "120",
    rx: "6"
  }, import_react79.default.createElement("animate", {
    attributeName: "height",
    begin: "0.5s",
    dur: "1s",
    values: "120;110;100;90;80;70;60;50;40;140;120",
    calcMode: "linear",
    repeatCount: "indefinite"
  }), import_react79.default.createElement("animate", {
    attributeName: "y",
    begin: "0.5s",
    dur: "1s",
    values: "10;15;20;25;30;35;40;45;50;0;10",
    calcMode: "linear",
    repeatCount: "indefinite"
  })), import_react79.default.createElement("rect", {
    x: "30",
    y: "10",
    width: "15",
    height: "120",
    rx: "6"
  }, import_react79.default.createElement("animate", {
    attributeName: "height",
    begin: "0.25s",
    dur: "1s",
    values: "120;110;100;90;80;70;60;50;40;140;120",
    calcMode: "linear",
    repeatCount: "indefinite"
  }), import_react79.default.createElement("animate", {
    attributeName: "y",
    begin: "0.25s",
    dur: "1s",
    values: "10;15;20;25;30;35;40;45;50;0;10",
    calcMode: "linear",
    repeatCount: "indefinite"
  })), import_react79.default.createElement("rect", {
    x: "60",
    width: "15",
    height: "140",
    rx: "6"
  }, import_react79.default.createElement("animate", {
    attributeName: "height",
    begin: "0s",
    dur: "1s",
    values: "120;110;100;90;80;70;60;50;40;140;120",
    calcMode: "linear",
    repeatCount: "indefinite"
  }), import_react79.default.createElement("animate", {
    attributeName: "y",
    begin: "0s",
    dur: "1s",
    values: "10;15;20;25;30;35;40;45;50;0;10",
    calcMode: "linear",
    repeatCount: "indefinite"
  })), import_react79.default.createElement("rect", {
    x: "90",
    y: "10",
    width: "15",
    height: "120",
    rx: "6"
  }, import_react79.default.createElement("animate", {
    attributeName: "height",
    begin: "0.25s",
    dur: "1s",
    values: "120;110;100;90;80;70;60;50;40;140;120",
    calcMode: "linear",
    repeatCount: "indefinite"
  }), import_react79.default.createElement("animate", {
    attributeName: "y",
    begin: "0.25s",
    dur: "1s",
    values: "10;15;20;25;30;35;40;45;50;0;10",
    calcMode: "linear",
    repeatCount: "indefinite"
  })), import_react79.default.createElement("rect", {
    x: "120",
    y: "10",
    width: "15",
    height: "120",
    rx: "6"
  }, import_react79.default.createElement("animate", {
    attributeName: "height",
    begin: "0.5s",
    dur: "1s",
    values: "120;110;100;90;80;70;60;50;40;140;120",
    calcMode: "linear",
    repeatCount: "indefinite"
  }), import_react79.default.createElement("animate", {
    attributeName: "y",
    begin: "0.5s",
    dur: "1s",
    values: "10;15;20;25;30;35;40;45;50;0;10",
    calcMode: "linear",
    repeatCount: "indefinite"
  })));
}

// node_modules/@mantine/core/esm/Loader/loaders/Oval.js
var import_react80 = __toESM(require_react());
var __defProp24 = Object.defineProperty;
var __getOwnPropSymbols25 = Object.getOwnPropertySymbols;
var __hasOwnProp25 = Object.prototype.hasOwnProperty;
var __propIsEnum25 = Object.prototype.propertyIsEnumerable;
var __defNormalProp24 = (obj, key, value) => key in obj ? __defProp24(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues24 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp25.call(b2, prop))
      __defNormalProp24(a2, prop, b2[prop]);
  if (__getOwnPropSymbols25)
    for (var prop of __getOwnPropSymbols25(b2)) {
      if (__propIsEnum25.call(b2, prop))
        __defNormalProp24(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest12 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp25.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols25)
    for (var prop of __getOwnPropSymbols25(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum25.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
function Oval(_a) {
  var _b = _a, { size: size3, color } = _b, others = __objRest12(_b, ["size", "color"]);
  return import_react80.default.createElement("svg", __spreadValues24({
    width: `${size3}px`,
    height: `${size3}px`,
    viewBox: "0 0 38 38",
    xmlns: "http://www.w3.org/2000/svg",
    stroke: color
  }, others), import_react80.default.createElement("g", {
    fill: "none",
    fillRule: "evenodd"
  }, import_react80.default.createElement("g", {
    transform: "translate(2.5 2.5)",
    strokeWidth: "5"
  }, import_react80.default.createElement("circle", {
    strokeOpacity: ".5",
    cx: "16",
    cy: "16",
    r: "16"
  }), import_react80.default.createElement("path", {
    d: "M32 16c0-9.94-8.06-16-16-16"
  }, import_react80.default.createElement("animateTransform", {
    attributeName: "transform",
    type: "rotate",
    from: "0 16 16",
    to: "360 16 16",
    dur: "1s",
    repeatCount: "indefinite"
  })))));
}

// node_modules/@mantine/core/esm/Loader/loaders/Dots.js
var import_react81 = __toESM(require_react());
var __defProp25 = Object.defineProperty;
var __getOwnPropSymbols26 = Object.getOwnPropertySymbols;
var __hasOwnProp26 = Object.prototype.hasOwnProperty;
var __propIsEnum26 = Object.prototype.propertyIsEnumerable;
var __defNormalProp25 = (obj, key, value) => key in obj ? __defProp25(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues25 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp26.call(b2, prop))
      __defNormalProp25(a2, prop, b2[prop]);
  if (__getOwnPropSymbols26)
    for (var prop of __getOwnPropSymbols26(b2)) {
      if (__propIsEnum26.call(b2, prop))
        __defNormalProp25(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest13 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp26.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols26)
    for (var prop of __getOwnPropSymbols26(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum26.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
function Dots(_a) {
  var _b = _a, { size: size3, color } = _b, others = __objRest13(_b, ["size", "color"]);
  return import_react81.default.createElement("svg", __spreadValues25({
    width: `${size3}px`,
    height: `${size3 / 4}px`,
    viewBox: "0 0 120 30",
    xmlns: "http://www.w3.org/2000/svg",
    fill: color
  }, others), import_react81.default.createElement("circle", {
    cx: "15",
    cy: "15",
    r: "15"
  }, import_react81.default.createElement("animate", {
    attributeName: "r",
    from: "15",
    to: "15",
    begin: "0s",
    dur: "0.8s",
    values: "15;9;15",
    calcMode: "linear",
    repeatCount: "indefinite"
  }), import_react81.default.createElement("animate", {
    attributeName: "fill-opacity",
    from: "1",
    to: "1",
    begin: "0s",
    dur: "0.8s",
    values: "1;.5;1",
    calcMode: "linear",
    repeatCount: "indefinite"
  })), import_react81.default.createElement("circle", {
    cx: "60",
    cy: "15",
    r: "9",
    fillOpacity: "0.3"
  }, import_react81.default.createElement("animate", {
    attributeName: "r",
    from: "9",
    to: "9",
    begin: "0s",
    dur: "0.8s",
    values: "9;15;9",
    calcMode: "linear",
    repeatCount: "indefinite"
  }), import_react81.default.createElement("animate", {
    attributeName: "fill-opacity",
    from: "0.5",
    to: "0.5",
    begin: "0s",
    dur: "0.8s",
    values: ".5;1;.5",
    calcMode: "linear",
    repeatCount: "indefinite"
  })), import_react81.default.createElement("circle", {
    cx: "105",
    cy: "15",
    r: "15"
  }, import_react81.default.createElement("animate", {
    attributeName: "r",
    from: "15",
    to: "15",
    begin: "0s",
    dur: "0.8s",
    values: "15;9;15",
    calcMode: "linear",
    repeatCount: "indefinite"
  }), import_react81.default.createElement("animate", {
    attributeName: "fill-opacity",
    from: "1",
    to: "1",
    begin: "0s",
    dur: "0.8s",
    values: "1;.5;1",
    calcMode: "linear",
    repeatCount: "indefinite"
  })));
}

// node_modules/@mantine/core/esm/Loader/Loader.js
var __defProp26 = Object.defineProperty;
var __getOwnPropSymbols27 = Object.getOwnPropertySymbols;
var __hasOwnProp27 = Object.prototype.hasOwnProperty;
var __propIsEnum27 = Object.prototype.propertyIsEnumerable;
var __defNormalProp26 = (obj, key, value) => key in obj ? __defProp26(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues26 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp27.call(b2, prop))
      __defNormalProp26(a2, prop, b2[prop]);
  if (__getOwnPropSymbols27)
    for (var prop of __getOwnPropSymbols27(b2)) {
      if (__propIsEnum27.call(b2, prop))
        __defNormalProp26(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest14 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp27.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols27)
    for (var prop of __getOwnPropSymbols27(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum27.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var LOADERS = {
  bars: Bars,
  oval: Oval,
  dots: Dots
};
var sizes2 = {
  xs: 18,
  sm: 22,
  md: 36,
  lg: 44,
  xl: 58
};
var defaultProps6 = {
  size: "md"
};
function Loader(props) {
  const _a = useComponentDefaultProps("Loader", defaultProps6, props), { size: size3, color, variant: variant2 } = _a, others = __objRest14(_a, ["size", "color", "variant"]);
  const theme = useMantineTheme();
  const defaultLoader = variant2 in LOADERS ? variant2 : theme.loader;
  return import_react82.default.createElement(Box, __spreadValues26({
    role: "presentation",
    component: LOADERS[defaultLoader] || LOADERS.bars,
    size: theme.fn.size({ size: size3, sizes: sizes2 }),
    color: theme.fn.variant({
      variant: "filled",
      primaryFallback: false,
      color: color || theme.primaryColor
    }).background
  }, others));
}
Loader.displayName = "@mantine/core/Loader";

// node_modules/@mantine/core/esm/ActionIcon/ActionIcon.js
var __defProp27 = Object.defineProperty;
var __getOwnPropSymbols28 = Object.getOwnPropertySymbols;
var __hasOwnProp28 = Object.prototype.hasOwnProperty;
var __propIsEnum28 = Object.prototype.propertyIsEnumerable;
var __defNormalProp27 = (obj, key, value) => key in obj ? __defProp27(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues27 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp28.call(b2, prop))
      __defNormalProp27(a2, prop, b2[prop]);
  if (__getOwnPropSymbols28)
    for (var prop of __getOwnPropSymbols28(b2)) {
      if (__propIsEnum28.call(b2, prop))
        __defNormalProp27(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest15 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp28.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols28)
    for (var prop of __getOwnPropSymbols28(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum28.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps7 = {
  color: "gray",
  size: "md",
  variant: "subtle",
  loading: false
};
var _ActionIcon = (0, import_react83.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("ActionIcon", defaultProps7, props), {
    className,
    color,
    children,
    radius: radius2,
    size: size3,
    variant: variant2,
    gradient: gradient2,
    disabled,
    loaderProps,
    loading,
    unstyled
  } = _a, others = __objRest15(_a, [
    "className",
    "color",
    "children",
    "radius",
    "size",
    "variant",
    "gradient",
    "disabled",
    "loaderProps",
    "loading",
    "unstyled"
  ]);
  const { classes, cx, theme } = ActionIcon_styles_default({ size: size3, radius: radius2, color, variant: variant2, gradient: gradient2 }, { name: "ActionIcon", unstyled });
  const colors = theme.fn.variant({ color, variant: variant2 });
  const loader = import_react83.default.createElement(Loader, __spreadValues27({
    color: colors.color,
    size: theme.fn.size({ size: size3, sizes }) - 12
  }, loaderProps));
  return import_react83.default.createElement(UnstyledButton, __spreadValues27({
    className: cx(classes.root, className),
    ref,
    disabled,
    "data-disabled": disabled || void 0,
    "data-loading": loading || void 0,
    unstyled
  }, others), loading ? loader : children);
});
_ActionIcon.displayName = "@mantine/core/ActionIcon";
var ActionIcon = createPolymorphicComponent(_ActionIcon);

// node_modules/@mantine/core/esm/Affix/Affix.js
var import_react86 = __toESM(require_react());

// node_modules/@mantine/core/esm/Portal/OptionalPortal.js
var import_react85 = __toESM(require_react());

// node_modules/@mantine/core/esm/Portal/Portal.js
var import_react84 = __toESM(require_react());
var import_react_dom2 = __toESM(require_react_dom());
function Portal(props) {
  const { children, target, className } = useComponentDefaultProps("Portal", {}, props);
  const theme = useMantineTheme();
  const [mounted, setMounted] = (0, import_react84.useState)(false);
  const ref = (0, import_react84.useRef)();
  useIsomorphicEffect(() => {
    setMounted(true);
    ref.current = !target ? document.createElement("div") : typeof target === "string" ? document.querySelector(target) : target;
    if (!target) {
      document.body.appendChild(ref.current);
    }
    return () => {
      !target && document.body.removeChild(ref.current);
    };
  }, [target]);
  if (!mounted) {
    return null;
  }
  return (0, import_react_dom2.createPortal)(import_react84.default.createElement("div", {
    className,
    dir: theme.dir
  }, children), ref.current);
}
Portal.displayName = "@mantine/core/Portal";

// node_modules/@mantine/core/esm/Portal/OptionalPortal.js
var __defProp28 = Object.defineProperty;
var __getOwnPropSymbols29 = Object.getOwnPropertySymbols;
var __hasOwnProp29 = Object.prototype.hasOwnProperty;
var __propIsEnum29 = Object.prototype.propertyIsEnumerable;
var __defNormalProp28 = (obj, key, value) => key in obj ? __defProp28(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues28 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp29.call(b2, prop))
      __defNormalProp28(a2, prop, b2[prop]);
  if (__getOwnPropSymbols29)
    for (var prop of __getOwnPropSymbols29(b2)) {
      if (__propIsEnum29.call(b2, prop))
        __defNormalProp28(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest16 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp29.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols29)
    for (var prop of __getOwnPropSymbols29(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum29.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
function OptionalPortal(_a) {
  var _b = _a, { withinPortal = true, children } = _b, others = __objRest16(_b, ["withinPortal", "children"]);
  if (withinPortal) {
    return import_react85.default.createElement(Portal, __spreadValues28({}, others), children);
  }
  return import_react85.default.createElement(import_react85.default.Fragment, null, children);
}
OptionalPortal.displayName = "@mantine/core/OptionalPortal";

// node_modules/@mantine/core/esm/Affix/Affix.js
var __defProp29 = Object.defineProperty;
var __getOwnPropSymbols30 = Object.getOwnPropertySymbols;
var __hasOwnProp30 = Object.prototype.hasOwnProperty;
var __propIsEnum30 = Object.prototype.propertyIsEnumerable;
var __defNormalProp29 = (obj, key, value) => key in obj ? __defProp29(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues29 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp30.call(b2, prop))
      __defNormalProp29(a2, prop, b2[prop]);
  if (__getOwnPropSymbols30)
    for (var prop of __getOwnPropSymbols30(b2)) {
      if (__propIsEnum30.call(b2, prop))
        __defNormalProp29(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest17 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp30.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols30)
    for (var prop of __getOwnPropSymbols30(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum30.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps8 = {
  position: { bottom: 0, right: 0 },
  zIndex: getDefaultZIndex("modal"),
  withinPortal: true
};
var Affix = (0, import_react86.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("Affix", defaultProps8, props), { target, position: position2, zIndex, sx, withinPortal } = _a, others = __objRest17(_a, ["target", "position", "zIndex", "sx", "withinPortal"]);
  return import_react86.default.createElement(OptionalPortal, {
    withinPortal,
    target
  }, import_react86.default.createElement(Box, __spreadValues29({
    sx: [__spreadValues29({ position: "fixed", zIndex }, position2), ...packSx(sx)],
    ref
  }, others)));
});
Affix.displayName = "@mantine/core/Affix";

// node_modules/@mantine/core/esm/Alert/Alert.js
var import_react89 = __toESM(require_react());

// node_modules/@mantine/core/esm/Alert/Alert.styles.js
var __defProp30 = Object.defineProperty;
var __defProps13 = Object.defineProperties;
var __getOwnPropDescs13 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols31 = Object.getOwnPropertySymbols;
var __hasOwnProp31 = Object.prototype.hasOwnProperty;
var __propIsEnum31 = Object.prototype.propertyIsEnumerable;
var __defNormalProp30 = (obj, key, value) => key in obj ? __defProp30(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues30 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp31.call(b2, prop))
      __defNormalProp30(a2, prop, b2[prop]);
  if (__getOwnPropSymbols31)
    for (var prop of __getOwnPropSymbols31(b2)) {
      if (__propIsEnum31.call(b2, prop))
        __defNormalProp30(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps13 = (a2, b2) => __defProps13(a2, __getOwnPropDescs13(b2));
function getVariantStyles4({ variant: variant2, color, theme }) {
  if (variant2 === "filled") {
    const colors2 = theme.fn.variant({ variant: "filled", color });
    return {
      backgroundColor: colors2.background,
      color: theme.white
    };
  }
  if (variant2 === "outline") {
    const colors2 = theme.fn.variant({ variant: "outline", color });
    return {
      color: colors2.color,
      borderColor: colors2.border,
      backgroundColor: theme.colorScheme === "dark" ? theme.colors.dark[6] : theme.white
    };
  }
  const colors = theme.fn.variant({ variant: "light", color });
  return {
    backgroundColor: colors.background,
    color: colors.color
  };
}
var useStyles6 = createStyles((theme, { color, radius: radius2, variant: variant2 }) => ({
  root: __spreadValues30(__spreadProps13(__spreadValues30({}, theme.fn.fontStyles()), {
    position: "relative",
    overflow: "hidden",
    padding: `${theme.spacing.sm}px ${theme.spacing.md}px`,
    borderRadius: theme.fn.radius(radius2),
    border: "1px solid transparent"
  }), getVariantStyles4({ variant: variant2, color, theme })),
  wrapper: {
    display: "flex"
  },
  body: {
    flex: 1
  },
  title: {
    boxSizing: "border-box",
    margin: 0,
    marginBottom: 7,
    display: "flex",
    alignItems: "center",
    justifyContent: "space-between",
    lineHeight: theme.lineHeight,
    fontSize: theme.fontSizes.sm,
    fontWeight: 700,
    "&[data-with-close-button]": {
      paddingRight: theme.spacing.md
    }
  },
  label: {
    display: "block",
    overflow: "hidden",
    textOverflow: "ellipsis"
  },
  icon: {
    lineHeight: 1,
    width: 20,
    height: 20,
    display: "flex",
    alignItems: "center",
    justifyContent: "flex-start",
    marginRight: theme.spacing.md,
    marginTop: 1
  },
  message: __spreadProps13(__spreadValues30({}, theme.fn.fontStyles()), {
    lineHeight: theme.lineHeight,
    textOverflow: "ellipsis",
    overflow: "hidden",
    fontSize: theme.fontSizes.sm,
    color: variant2 === "filled" ? theme.white : theme.colorScheme === "dark" ? variant2 === "light" ? theme.white : theme.colors.dark[0] : theme.black
  }),
  closeButton: {
    position: "absolute",
    top: theme.spacing.sm,
    right: theme.spacing.sm,
    color: "inherit"
  }
}));
var Alert_styles_default = useStyles6;

// node_modules/@mantine/core/esm/CloseButton/CloseButton.js
var import_react88 = __toESM(require_react());

// node_modules/@mantine/core/esm/CloseButton/CloseIcon.js
var import_react87 = __toESM(require_react());
var __defProp31 = Object.defineProperty;
var __getOwnPropSymbols32 = Object.getOwnPropertySymbols;
var __hasOwnProp32 = Object.prototype.hasOwnProperty;
var __propIsEnum32 = Object.prototype.propertyIsEnumerable;
var __defNormalProp31 = (obj, key, value) => key in obj ? __defProp31(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues31 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp32.call(b2, prop))
      __defNormalProp31(a2, prop, b2[prop]);
  if (__getOwnPropSymbols32)
    for (var prop of __getOwnPropSymbols32(b2)) {
      if (__propIsEnum32.call(b2, prop))
        __defNormalProp31(a2, prop, b2[prop]);
    }
  return a2;
};
function CloseIcon(props) {
  return import_react87.default.createElement("svg", __spreadValues31({
    viewBox: "0 0 15 15",
    fill: "none",
    xmlns: "http://www.w3.org/2000/svg"
  }, props), import_react87.default.createElement("path", {
    d: "M11.7816 4.03157C12.0062 3.80702 12.0062 3.44295 11.7816 3.2184C11.5571 2.99385 11.193 2.99385 10.9685 3.2184L7.50005 6.68682L4.03164 3.2184C3.80708 2.99385 3.44301 2.99385 3.21846 3.2184C2.99391 3.44295 2.99391 3.80702 3.21846 4.03157L6.68688 7.49999L3.21846 10.9684C2.99391 11.193 2.99391 11.557 3.21846 11.7816C3.44301 12.0061 3.80708 12.0061 4.03164 11.7816L7.50005 8.31316L10.9685 11.7816C11.193 12.0061 11.5571 12.0061 11.7816 11.7816C12.0062 11.557 12.0062 11.193 11.7816 10.9684L8.31322 7.49999L11.7816 4.03157Z",
    fill: "currentColor",
    fillRule: "evenodd",
    clipRule: "evenodd"
  }));
}
CloseIcon.displayName = "@mantine/core/CloseIcon";

// node_modules/@mantine/core/esm/CloseButton/CloseButton.js
var __defProp32 = Object.defineProperty;
var __getOwnPropSymbols33 = Object.getOwnPropertySymbols;
var __hasOwnProp33 = Object.prototype.hasOwnProperty;
var __propIsEnum33 = Object.prototype.propertyIsEnumerable;
var __defNormalProp32 = (obj, key, value) => key in obj ? __defProp32(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues32 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp33.call(b2, prop))
      __defNormalProp32(a2, prop, b2[prop]);
  if (__getOwnPropSymbols33)
    for (var prop of __getOwnPropSymbols33(b2)) {
      if (__propIsEnum33.call(b2, prop))
        __defNormalProp32(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest18 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp33.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols33)
    for (var prop of __getOwnPropSymbols33(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum33.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var iconSizes = {
  xs: 12,
  sm: 14,
  md: 16,
  lg: 20,
  xl: 24
};
var defaultProps9 = {
  size: "md"
};
var _CloseButton = (0, import_react88.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("CloseButton", defaultProps9, props), {
    iconSize,
    size: size3 = "md"
  } = _a, others = __objRest18(_a, [
    "iconSize",
    "size"
  ]);
  const theme = useMantineTheme();
  const _iconSize = iconSize || theme.fn.size({ size: size3, sizes: iconSizes });
  return import_react88.default.createElement(ActionIcon, __spreadValues32({
    size: size3,
    ref
  }, others), import_react88.default.createElement(CloseIcon, {
    width: _iconSize,
    height: _iconSize
  }));
});
_CloseButton.displayName = "@mantine/core/CloseButton";
var CloseButton = createPolymorphicComponent(_CloseButton);

// node_modules/@mantine/core/esm/Alert/Alert.js
var __defProp33 = Object.defineProperty;
var __getOwnPropSymbols34 = Object.getOwnPropertySymbols;
var __hasOwnProp34 = Object.prototype.hasOwnProperty;
var __propIsEnum34 = Object.prototype.propertyIsEnumerable;
var __defNormalProp33 = (obj, key, value) => key in obj ? __defProp33(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues33 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp34.call(b2, prop))
      __defNormalProp33(a2, prop, b2[prop]);
  if (__getOwnPropSymbols34)
    for (var prop of __getOwnPropSymbols34(b2)) {
      if (__propIsEnum34.call(b2, prop))
        __defNormalProp33(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest19 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp34.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols34)
    for (var prop of __getOwnPropSymbols34(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum34.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps10 = {
  variant: "light"
};
var Alert = (0, import_react89.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("Alert", defaultProps10, props), {
    id,
    className,
    title,
    variant: variant2,
    children,
    color,
    classNames,
    icon,
    styles: styles2,
    onClose,
    radius: radius2,
    withCloseButton,
    closeButtonLabel,
    unstyled
  } = _a, others = __objRest19(_a, [
    "id",
    "className",
    "title",
    "variant",
    "children",
    "color",
    "classNames",
    "icon",
    "styles",
    "onClose",
    "radius",
    "withCloseButton",
    "closeButtonLabel",
    "unstyled"
  ]);
  const { classes, cx } = Alert_styles_default({ color, radius: radius2, variant: variant2 }, { classNames, styles: styles2, unstyled, name: "Alert" });
  const rootId = useId(id);
  const titleId = title && `${rootId}-title`;
  const bodyId = `${rootId}-body`;
  return import_react89.default.createElement(Box, __spreadValues33({
    id: rootId,
    role: "alert",
    "aria-labelledby": titleId,
    "aria-describedby": bodyId,
    className: cx(classes.root, classes[variant2], className),
    ref
  }, others), import_react89.default.createElement("div", {
    className: classes.wrapper
  }, icon && import_react89.default.createElement("div", {
    className: classes.icon
  }, icon), import_react89.default.createElement("div", {
    className: classes.body
  }, title && import_react89.default.createElement("div", {
    className: classes.title,
    "data-with-close-button": withCloseButton || void 0
  }, import_react89.default.createElement("span", {
    id: titleId,
    className: classes.label
  }, title)), withCloseButton && import_react89.default.createElement(CloseButton, {
    className: classes.closeButton,
    onClick: onClose,
    variant: "transparent",
    size: 16,
    iconSize: 16,
    "aria-label": closeButtonLabel
  }), import_react89.default.createElement("div", {
    id: bodyId,
    className: classes.message
  }, children))));
});
Alert.displayName = "@mantine/core/Alert";

// node_modules/@mantine/core/esm/Anchor/Anchor.js
var import_react91 = __toESM(require_react());

// node_modules/@mantine/core/esm/Text/Text.js
var import_react90 = __toESM(require_react());

// node_modules/@mantine/core/esm/Text/Text.styles.js
var __defProp34 = Object.defineProperty;
var __defProps14 = Object.defineProperties;
var __getOwnPropDescs14 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols35 = Object.getOwnPropertySymbols;
var __hasOwnProp35 = Object.prototype.hasOwnProperty;
var __propIsEnum35 = Object.prototype.propertyIsEnumerable;
var __defNormalProp34 = (obj, key, value) => key in obj ? __defProp34(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues34 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp35.call(b2, prop))
      __defNormalProp34(a2, prop, b2[prop]);
  if (__getOwnPropSymbols35)
    for (var prop of __getOwnPropSymbols35(b2)) {
      if (__propIsEnum35.call(b2, prop))
        __defNormalProp34(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps14 = (a2, b2) => __defProps14(a2, __getOwnPropDescs14(b2));
function getTextDecoration({
  underline,
  strikethrough
}) {
  const styles2 = [];
  if (underline) {
    styles2.push("underline");
  }
  if (strikethrough) {
    styles2.push("line-through");
  }
  return styles2.length > 0 ? styles2.join(" ") : "none";
}
function getTextColor({ theme, color, variant: variant2 }) {
  if (color === "dimmed") {
    return theme.colorScheme === "dark" ? theme.colors.dark[2] : theme.colors.gray[6];
  }
  return typeof color === "string" && (color in theme.colors || color.split(".")[0] in theme.colors) ? theme.fn.variant({ variant: "filled", color }).background : variant2 === "link" ? theme.colors[theme.primaryColor][theme.colorScheme === "dark" ? 4 : 7] : color || "inherit";
}
function getLineClamp(lineClamp) {
  if (typeof lineClamp === "number") {
    return {
      overflow: "hidden",
      textOverflow: "ellipsis",
      display: "-webkit-box",
      WebkitLineClamp: lineClamp,
      WebkitBoxOrient: "vertical"
    };
  }
  return null;
}
function getTruncate({ theme, truncate }) {
  if (truncate === "start") {
    return {
      overflow: "hidden",
      textOverflow: "ellipsis",
      whiteSpace: "nowrap",
      direction: theme.dir === "ltr" ? "rtl" : "ltr",
      textAlign: theme.dir === "ltr" ? "right" : "left"
    };
  }
  if (truncate) {
    return {
      overflow: "hidden",
      textOverflow: "ellipsis",
      whiteSpace: "nowrap"
    };
  }
  return null;
}
var useStyles7 = createStyles((theme, {
  color,
  variant: variant2,
  size: size3,
  lineClamp,
  truncate,
  inline: inline2,
  inherit,
  underline,
  gradient: gradient2,
  weight,
  transform,
  align,
  strikethrough,
  italic
}) => {
  const colors = theme.fn.variant({ variant: "gradient", gradient: gradient2 });
  return {
    root: __spreadValues34(__spreadProps14(__spreadValues34(__spreadValues34(__spreadValues34(__spreadValues34({}, theme.fn.fontStyles()), theme.fn.focusStyles()), getLineClamp(lineClamp)), getTruncate({ theme, truncate })), {
      color: getTextColor({ color, theme, variant: variant2 }),
      fontFamily: inherit ? "inherit" : theme.fontFamily,
      fontSize: inherit || size3 === void 0 ? "inherit" : theme.fn.size({ size: size3, sizes: theme.fontSizes }),
      lineHeight: inherit ? "inherit" : inline2 ? 1 : theme.lineHeight,
      textDecoration: getTextDecoration({ underline, strikethrough }),
      WebkitTapHighlightColor: "transparent",
      fontWeight: inherit ? "inherit" : weight,
      textTransform: transform,
      textAlign: align,
      fontStyle: italic ? "italic" : void 0
    }), theme.fn.hover(variant2 === "link" && underline === void 0 ? {
      textDecoration: "underline"
    } : void 0)),
    gradient: {
      backgroundImage: colors.background,
      WebkitBackgroundClip: "text",
      WebkitTextFillColor: "transparent"
    }
  };
});
var Text_styles_default = useStyles7;

// node_modules/@mantine/core/esm/Text/Text.js
var __defProp35 = Object.defineProperty;
var __getOwnPropSymbols36 = Object.getOwnPropertySymbols;
var __hasOwnProp36 = Object.prototype.hasOwnProperty;
var __propIsEnum36 = Object.prototype.propertyIsEnumerable;
var __defNormalProp35 = (obj, key, value) => key in obj ? __defProp35(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues35 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp36.call(b2, prop))
      __defNormalProp35(a2, prop, b2[prop]);
  if (__getOwnPropSymbols36)
    for (var prop of __getOwnPropSymbols36(b2)) {
      if (__propIsEnum36.call(b2, prop))
        __defNormalProp35(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest20 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp36.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols36)
    for (var prop of __getOwnPropSymbols36(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum36.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps11 = {
  variant: "text"
};
var _Text = (0, import_react90.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("Text", defaultProps11, props), {
    className,
    size: size3,
    weight,
    transform,
    color,
    align,
    variant: variant2,
    lineClamp,
    truncate,
    gradient: gradient2,
    inline: inline2,
    inherit,
    underline,
    strikethrough,
    italic,
    classNames,
    styles: styles2,
    unstyled,
    span
  } = _a, others = __objRest20(_a, [
    "className",
    "size",
    "weight",
    "transform",
    "color",
    "align",
    "variant",
    "lineClamp",
    "truncate",
    "gradient",
    "inline",
    "inherit",
    "underline",
    "strikethrough",
    "italic",
    "classNames",
    "styles",
    "unstyled",
    "span"
  ]);
  const { classes, cx } = Text_styles_default({
    variant: variant2,
    color,
    size: size3,
    lineClamp,
    truncate,
    inline: inline2,
    inherit,
    underline,
    strikethrough,
    italic,
    weight,
    transform,
    align,
    gradient: gradient2
  }, { unstyled, name: "Text" });
  return import_react90.default.createElement(Box, __spreadValues35({
    ref,
    className: cx(classes.root, { [classes.gradient]: variant2 === "gradient" }, className),
    component: span ? "span" : "div"
  }, others));
});
_Text.displayName = "@mantine/core/Text";
var Text = createPolymorphicComponent(_Text);

// node_modules/@mantine/core/esm/Anchor/Anchor.styles.js
var useStyles8 = createStyles(() => ({
  root: {
    backgroundColor: "transparent",
    cursor: "pointer",
    padding: 0,
    border: 0
  }
}));
var Anchor_styles_default = useStyles8;

// node_modules/@mantine/core/esm/Anchor/Anchor.js
var __defProp36 = Object.defineProperty;
var __getOwnPropSymbols37 = Object.getOwnPropertySymbols;
var __hasOwnProp37 = Object.prototype.hasOwnProperty;
var __propIsEnum37 = Object.prototype.propertyIsEnumerable;
var __defNormalProp36 = (obj, key, value) => key in obj ? __defProp36(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues36 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp37.call(b2, prop))
      __defNormalProp36(a2, prop, b2[prop]);
  if (__getOwnPropSymbols37)
    for (var prop of __getOwnPropSymbols37(b2)) {
      if (__propIsEnum37.call(b2, prop))
        __defNormalProp36(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest21 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp37.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols37)
    for (var prop of __getOwnPropSymbols37(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum37.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps12 = {};
var _Anchor = (0, import_react91.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("Anchor", defaultProps12, props), { component, className, unstyled } = _a, others = __objRest21(_a, ["component", "className", "unstyled"]);
  const { classes, cx } = Anchor_styles_default(null, { name: "Anchor", unstyled });
  const buttonProps = component === "button" ? { type: "button" } : null;
  return import_react91.default.createElement(Text, __spreadValues36(__spreadValues36({
    component: component || "a",
    variant: "link",
    ref,
    className: cx(classes.root, className)
  }, buttonProps), others));
});
_Anchor.displayName = "@mantine/core/Anchor";
var Anchor = createPolymorphicComponent(_Anchor);

// node_modules/@mantine/core/esm/AppShell/Navbar/Navbar.js
var import_react95 = __toESM(require_react());

// node_modules/@mantine/core/esm/AppShell/HorizontalSection/HorizontalSection.js
var import_react93 = __toESM(require_react());

// node_modules/@mantine/core/esm/AppShell/AppShell.context.js
var import_react92 = __toESM(require_react());
var AppShellContext = (0, import_react92.createContext)({
  zIndex: 1e3,
  fixed: false,
  layout: "default"
});
var AppShellProvider = AppShellContext.Provider;
function useAppShellContext() {
  return (0, import_react92.useContext)(AppShellContext);
}

// node_modules/@mantine/core/esm/AppShell/HorizontalSection/get-sorted-breakpoints/get-sorted-breakpoints.js
function getSortedBreakpoints(breakpoints, theme) {
  if (!breakpoints) {
    return [];
  }
  const values = Object.keys(breakpoints).filter((breakpoint) => breakpoint !== "base").map((breakpoint) => [
    theme.fn.size({ size: breakpoint, sizes: theme.breakpoints }),
    breakpoints[breakpoint]
  ]);
  values.sort((a2, b2) => a2[0] - b2[0]);
  return values;
}

// node_modules/@mantine/core/esm/AppShell/HorizontalSection/HorizontalSection.styles.js
var __defProp37 = Object.defineProperty;
var __defProps15 = Object.defineProperties;
var __getOwnPropDescs15 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols38 = Object.getOwnPropertySymbols;
var __hasOwnProp38 = Object.prototype.hasOwnProperty;
var __propIsEnum38 = Object.prototype.propertyIsEnumerable;
var __defNormalProp37 = (obj, key, value) => key in obj ? __defProp37(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues37 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp38.call(b2, prop))
      __defNormalProp37(a2, prop, b2[prop]);
  if (__getOwnPropSymbols38)
    for (var prop of __getOwnPropSymbols38(b2)) {
      if (__propIsEnum38.call(b2, prop))
        __defNormalProp37(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps15 = (a2, b2) => __defProps15(a2, __getOwnPropDescs15(b2));
var useStyles9 = createStyles((theme, {
  height,
  width,
  fixed,
  position: position2,
  hiddenBreakpoint,
  zIndex,
  section,
  withBorder,
  layout
}) => {
  const breakpoints = typeof width === "object" && width !== null ? getSortedBreakpoints(width, theme).reduce((acc, [breakpoint, breakpointSize]) => {
    acc[`@media (min-width: ${breakpoint}px)`] = {
      width: breakpointSize,
      minWidth: breakpointSize
    };
    return acc;
  }, {}) : null;
  const borderStyles = withBorder ? {
    [section === "navbar" ? "borderRight" : "borderLeft"]: `1px solid ${theme.colorScheme === "dark" ? theme.colors.dark[5] : theme.colors.gray[2]}`
  } : {};
  return {
    root: __spreadProps15(__spreadValues37(__spreadValues37(__spreadProps15(__spreadValues37(__spreadValues37({}, theme.fn.fontStyles()), position2), {
      top: layout === "alt" ? 0 : (position2 == null ? void 0 : position2.top) || "var(--mantine-header-height)",
      bottom: 0,
      zIndex,
      height: height || (layout === "alt" ? "auto" : "calc(100vh - var(--mantine-header-height, 0px) - var(--mantine-footer-height, 0px))"),
      width: (width == null ? void 0 : width.base) || "100%",
      position: fixed ? "fixed" : "static",
      boxSizing: "border-box",
      display: "flex",
      flexDirection: "column",
      backgroundColor: theme.colorScheme === "dark" ? theme.colors.dark[7] : theme.white
    }), borderStyles), breakpoints), {
      "&[data-hidden]": {
        [`@media (max-width: ${theme.fn.size({
          size: hiddenBreakpoint,
          sizes: theme.breakpoints
        }) - 1}px)`]: {
          display: "none"
        }
      }
    })
  };
});
var HorizontalSection_styles_default = useStyles9;

// node_modules/@mantine/core/esm/AppShell/HorizontalSection/HorizontalSection.js
var __defProp38 = Object.defineProperty;
var __getOwnPropSymbols39 = Object.getOwnPropertySymbols;
var __hasOwnProp39 = Object.prototype.hasOwnProperty;
var __propIsEnum39 = Object.prototype.propertyIsEnumerable;
var __defNormalProp38 = (obj, key, value) => key in obj ? __defProp38(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues38 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp39.call(b2, prop))
      __defNormalProp38(a2, prop, b2[prop]);
  if (__getOwnPropSymbols39)
    for (var prop of __getOwnPropSymbols39(b2)) {
      if (__propIsEnum39.call(b2, prop))
        __defNormalProp38(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest22 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp39.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols39)
    for (var prop of __getOwnPropSymbols39(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum39.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var HorizontalSection = (0, import_react93.forwardRef)((_a, ref) => {
  var _b = _a, {
    width,
    height,
    fixed = false,
    position: position2,
    zIndex,
    hiddenBreakpoint = "md",
    hidden: hidden2 = false,
    withBorder = true,
    className,
    classNames,
    styles: styles2,
    children,
    section,
    __staticSelector,
    unstyled
  } = _b, others = __objRest22(_b, [
    "width",
    "height",
    "fixed",
    "position",
    "zIndex",
    "hiddenBreakpoint",
    "hidden",
    "withBorder",
    "className",
    "classNames",
    "styles",
    "children",
    "section",
    "__staticSelector",
    "unstyled"
  ]);
  const ctx = useAppShellContext();
  const { classes, cx, theme } = HorizontalSection_styles_default({
    width,
    height,
    fixed: ctx.fixed || fixed,
    position: position2,
    hiddenBreakpoint,
    zIndex: zIndex || ctx.zIndex || getDefaultZIndex("app"),
    section,
    withBorder,
    layout: ctx.layout
  }, { classNames, styles: styles2, name: __staticSelector, unstyled });
  const breakpoints = getSortedBreakpoints(width, theme).reduce((acc, [breakpoint, breakpointSize]) => {
    acc[`@media (min-width: ${breakpoint}px)`] = {
      [`--mantine-${section}-width`]: `${breakpointSize}px`
    };
    return acc;
  }, {});
  return import_react93.default.createElement(Box, __spreadValues38({
    component: section === "navbar" ? "nav" : "aside",
    ref,
    "data-hidden": hidden2 || void 0,
    className: cx(classes.root, className)
  }, others), children, import_react93.default.createElement(Global2, {
    styles: () => ({
      ":root": __spreadValues38({
        [`--mantine-${section}-width`]: (width == null ? void 0 : width.base) ? `${width.base}px` : "0px"
      }, breakpoints)
    })
  }));
});
HorizontalSection.displayName = "@mantine/core/HorizontalSection";

// node_modules/@mantine/core/esm/AppShell/HorizontalSection/Section/Section.js
var import_react94 = __toESM(require_react());
var __defProp39 = Object.defineProperty;
var __getOwnPropSymbols40 = Object.getOwnPropertySymbols;
var __hasOwnProp40 = Object.prototype.hasOwnProperty;
var __propIsEnum40 = Object.prototype.propertyIsEnumerable;
var __defNormalProp39 = (obj, key, value) => key in obj ? __defProp39(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues39 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp40.call(b2, prop))
      __defNormalProp39(a2, prop, b2[prop]);
  if (__getOwnPropSymbols40)
    for (var prop of __getOwnPropSymbols40(b2)) {
      if (__propIsEnum40.call(b2, prop))
        __defNormalProp39(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest23 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp40.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols40)
    for (var prop of __getOwnPropSymbols40(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum40.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var _Section = (0, import_react94.forwardRef)((_a, ref) => {
  var _b = _a, { children, grow = false, sx } = _b, others = __objRest23(_b, ["children", "grow", "sx"]);
  return import_react94.default.createElement(Box, __spreadValues39({
    ref,
    sx: [{ flex: grow ? 1 : 0, boxSizing: "border-box" }, ...packSx(sx)]
  }, others), children);
});
_Section.displayName = "@mantine/core/Section";
var Section = createPolymorphicComponent(_Section);

// node_modules/@mantine/core/esm/AppShell/Navbar/Navbar.js
var __defProp40 = Object.defineProperty;
var __getOwnPropSymbols41 = Object.getOwnPropertySymbols;
var __hasOwnProp41 = Object.prototype.hasOwnProperty;
var __propIsEnum41 = Object.prototype.propertyIsEnumerable;
var __defNormalProp40 = (obj, key, value) => key in obj ? __defProp40(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues40 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp41.call(b2, prop))
      __defNormalProp40(a2, prop, b2[prop]);
  if (__getOwnPropSymbols41)
    for (var prop of __getOwnPropSymbols41(b2)) {
      if (__propIsEnum41.call(b2, prop))
        __defNormalProp40(a2, prop, b2[prop]);
    }
  return a2;
};
var defaultProps13 = {
  fixed: false,
  position: { top: 0, left: 0 },
  hiddenBreakpoint: "md",
  hidden: false
};
var Navbar = (0, import_react95.forwardRef)((props, ref) => {
  const _props = useComponentDefaultProps("Navbar", defaultProps13, props);
  return import_react95.default.createElement(HorizontalSection, __spreadValues40({
    section: "navbar",
    __staticSelector: "Navbar",
    ref
  }, _props));
});
Navbar.Section = Section;
Navbar.displayName = "@mantine/core/Navbar";

// node_modules/@mantine/core/esm/AppShell/Header/Header.js
var import_react97 = __toESM(require_react());

// node_modules/@mantine/core/esm/AppShell/VerticalSection/VerticalSection.js
var import_react96 = __toESM(require_react());

// node_modules/@mantine/core/esm/AppShell/VerticalSection/VerticalSection.styles.js
var __defProp41 = Object.defineProperty;
var __defProps16 = Object.defineProperties;
var __getOwnPropDescs16 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols42 = Object.getOwnPropertySymbols;
var __hasOwnProp42 = Object.prototype.hasOwnProperty;
var __propIsEnum42 = Object.prototype.propertyIsEnumerable;
var __defNormalProp41 = (obj, key, value) => key in obj ? __defProp41(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues41 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp42.call(b2, prop))
      __defNormalProp41(a2, prop, b2[prop]);
  if (__getOwnPropSymbols42)
    for (var prop of __getOwnPropSymbols42(b2)) {
      if (__propIsEnum42.call(b2, prop))
        __defNormalProp41(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps16 = (a2, b2) => __defProps16(a2, __getOwnPropDescs16(b2));
var useStyles10 = createStyles((theme, { height, fixed, position: position2, zIndex, borderPosition, layout }) => {
  const breakpoints = typeof height === "object" && height !== null ? getSortedBreakpoints(height, theme).reduce((acc, [breakpoint, breakpointSize]) => {
    acc[`@media (min-width: ${breakpoint}px)`] = {
      height: breakpointSize,
      minHeight: breakpointSize
    };
    return acc;
  }, {}) : null;
  return {
    root: __spreadProps16(__spreadValues41(__spreadProps16(__spreadValues41(__spreadValues41({}, theme.fn.fontStyles()), position2), {
      zIndex,
      left: layout === "alt" ? "var(--mantine-navbar-width, 0)" : 0,
      right: layout === "alt" ? "var(--mantine-aside-width, 0)" : 0,
      height: typeof height === "object" ? (height == null ? void 0 : height.base) || "100%" : height,
      maxHeight: typeof height === "object" ? (height == null ? void 0 : height.base) || "100%" : height,
      position: fixed ? "fixed" : "static",
      boxSizing: "border-box",
      backgroundColor: theme.colorScheme === "dark" ? theme.colors.dark[7] : theme.white
    }), breakpoints), {
      borderBottom: borderPosition === "bottom" ? `1px solid ${theme.colorScheme === "dark" ? theme.colors.dark[5] : theme.colors.gray[2]}` : void 0,
      borderTop: borderPosition === "top" ? `1px solid ${theme.colorScheme === "dark" ? theme.colors.dark[5] : theme.colors.gray[2]}` : void 0
    })
  };
});
var VerticalSection_styles_default = useStyles10;

// node_modules/@mantine/core/esm/AppShell/VerticalSection/VerticalSection.js
var __defProp42 = Object.defineProperty;
var __getOwnPropSymbols43 = Object.getOwnPropertySymbols;
var __hasOwnProp43 = Object.prototype.hasOwnProperty;
var __propIsEnum43 = Object.prototype.propertyIsEnumerable;
var __defNormalProp42 = (obj, key, value) => key in obj ? __defProp42(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues42 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp43.call(b2, prop))
      __defNormalProp42(a2, prop, b2[prop]);
  if (__getOwnPropSymbols43)
    for (var prop of __getOwnPropSymbols43(b2)) {
      if (__propIsEnum43.call(b2, prop))
        __defNormalProp42(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest24 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp43.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols43)
    for (var prop of __getOwnPropSymbols43(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum43.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var VerticalSection = (0, import_react96.forwardRef)((_a, ref) => {
  var _b = _a, {
    children,
    className,
    classNames,
    styles: styles2,
    height,
    fixed = false,
    withBorder = true,
    position: position2,
    zIndex,
    section,
    unstyled,
    __staticSelector
  } = _b, others = __objRest24(_b, [
    "children",
    "className",
    "classNames",
    "styles",
    "height",
    "fixed",
    "withBorder",
    "position",
    "zIndex",
    "section",
    "unstyled",
    "__staticSelector"
  ]);
  const ctx = useAppShellContext();
  const _zIndex = zIndex || ctx.zIndex || getDefaultZIndex("app");
  const { classes, cx, theme } = VerticalSection_styles_default({
    height,
    fixed: ctx.fixed || fixed,
    position: position2,
    zIndex: typeof _zIndex === "number" && ctx.layout === "default" ? _zIndex + 1 : _zIndex,
    layout: ctx.layout,
    borderPosition: withBorder ? section === "header" ? "bottom" : "top" : "none"
  }, { name: __staticSelector, classNames, styles: styles2, unstyled });
  const breakpoints = typeof height === "object" && height !== null ? getSortedBreakpoints(height, theme).reduce((acc, [breakpoint, breakpointSize]) => {
    acc[`@media (min-width: ${breakpoint}px)`] = {
      [`--mantine-${section}-height`]: `${breakpointSize}px`
    };
    return acc;
  }, {}) : null;
  return import_react96.default.createElement(Box, __spreadValues42({
    component: section === "header" ? "header" : "footer",
    className: cx(classes.root, className),
    ref
  }, others), children, import_react96.default.createElement(Global2, {
    styles: () => ({
      ":root": __spreadValues42({
        [`--mantine-${section}-height`]: typeof height === "object" ? `${height == null ? void 0 : height.base}px` || "100%" : `${height}px`
      }, breakpoints)
    })
  }));
});
VerticalSection.displayName = "@mantine/core/VerticalSection";

// node_modules/@mantine/core/esm/AppShell/Header/Header.js
var __defProp43 = Object.defineProperty;
var __defProps17 = Object.defineProperties;
var __getOwnPropDescs17 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols44 = Object.getOwnPropertySymbols;
var __hasOwnProp44 = Object.prototype.hasOwnProperty;
var __propIsEnum44 = Object.prototype.propertyIsEnumerable;
var __defNormalProp43 = (obj, key, value) => key in obj ? __defProp43(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues43 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp44.call(b2, prop))
      __defNormalProp43(a2, prop, b2[prop]);
  if (__getOwnPropSymbols44)
    for (var prop of __getOwnPropSymbols44(b2)) {
      if (__propIsEnum44.call(b2, prop))
        __defNormalProp43(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps17 = (a2, b2) => __defProps17(a2, __getOwnPropDescs17(b2));
var defaultProps14 = {
  fixed: false,
  position: { top: 0, left: 0, right: 0 }
};
var Header = (0, import_react97.forwardRef)((props, ref) => {
  const _props = useComponentDefaultProps("Header", defaultProps14, props);
  return import_react97.default.createElement(VerticalSection, __spreadProps17(__spreadValues43({
    section: "header",
    __staticSelector: "Header"
  }, _props), {
    ref
  }));
});
Header.displayName = "@mantine/core/Header";

// node_modules/@mantine/core/esm/AppShell/Aside/Aside.js
var import_react98 = __toESM(require_react());
var __defProp44 = Object.defineProperty;
var __getOwnPropSymbols45 = Object.getOwnPropertySymbols;
var __hasOwnProp45 = Object.prototype.hasOwnProperty;
var __propIsEnum45 = Object.prototype.propertyIsEnumerable;
var __defNormalProp44 = (obj, key, value) => key in obj ? __defProp44(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues44 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp45.call(b2, prop))
      __defNormalProp44(a2, prop, b2[prop]);
  if (__getOwnPropSymbols45)
    for (var prop of __getOwnPropSymbols45(b2)) {
      if (__propIsEnum45.call(b2, prop))
        __defNormalProp44(a2, prop, b2[prop]);
    }
  return a2;
};
var defaultProps15 = {
  fixed: false,
  position: { top: 0, right: 0 },
  hiddenBreakpoint: "md",
  hidden: false
};
var Aside = (0, import_react98.forwardRef)((props, ref) => {
  const _props = useComponentDefaultProps("Aside", defaultProps15, props);
  return import_react98.default.createElement(HorizontalSection, __spreadValues44({
    section: "aside",
    __staticSelector: "Aside",
    ref
  }, _props));
});
Aside.Section = Section;
Aside.displayName = "@mantine/core/Aside";

// node_modules/@mantine/core/esm/AppShell/Footer/Footer.js
var import_react99 = __toESM(require_react());
var __defProp45 = Object.defineProperty;
var __defProps18 = Object.defineProperties;
var __getOwnPropDescs18 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols46 = Object.getOwnPropertySymbols;
var __hasOwnProp46 = Object.prototype.hasOwnProperty;
var __propIsEnum46 = Object.prototype.propertyIsEnumerable;
var __defNormalProp45 = (obj, key, value) => key in obj ? __defProp45(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues45 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp46.call(b2, prop))
      __defNormalProp45(a2, prop, b2[prop]);
  if (__getOwnPropSymbols46)
    for (var prop of __getOwnPropSymbols46(b2)) {
      if (__propIsEnum46.call(b2, prop))
        __defNormalProp45(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps18 = (a2, b2) => __defProps18(a2, __getOwnPropDescs18(b2));
var defaultProps16 = {
  fixed: false,
  position: { bottom: 0, left: 0, right: 0 }
};
var Footer = (0, import_react99.forwardRef)((props, ref) => {
  const _props = useComponentDefaultProps("Footer", defaultProps16, props);
  return import_react99.default.createElement(VerticalSection, __spreadProps18(__spreadValues45({
    section: "footer",
    __staticSelector: "Footer"
  }, _props), {
    ref
  }));
});
Footer.displayName = "@mantine/core/Footer";

// node_modules/@mantine/core/esm/AppShell/AppShell.js
var import_react100 = __toESM(require_react());

// node_modules/@mantine/core/esm/AppShell/AppShell.styles.js
var __defProp46 = Object.defineProperty;
var __getOwnPropSymbols47 = Object.getOwnPropertySymbols;
var __hasOwnProp47 = Object.prototype.hasOwnProperty;
var __propIsEnum47 = Object.prototype.propertyIsEnumerable;
var __defNormalProp46 = (obj, key, value) => key in obj ? __defProp46(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues46 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp47.call(b2, prop))
      __defNormalProp46(a2, prop, b2[prop]);
  if (__getOwnPropSymbols47)
    for (var prop of __getOwnPropSymbols47(b2)) {
      if (__propIsEnum47.call(b2, prop))
        __defNormalProp46(a2, prop, b2[prop]);
    }
  return a2;
};
function getPositionStyles(props, theme) {
  const padding2 = theme.fn.size({ size: props.padding, sizes: theme.spacing });
  const navbarOffset = props.navbarOffsetBreakpoint ? theme.fn.size({ size: props.navbarOffsetBreakpoint, sizes: theme.breakpoints }) : null;
  const asideOffset = props.asideOffsetBreakpoint ? theme.fn.size({ size: props.asideOffsetBreakpoint, sizes: theme.breakpoints }) : null;
  if (!props.fixed) {
    return { padding: padding2 };
  }
  return {
    minHeight: "100vh",
    paddingTop: `calc(var(--mantine-header-height, 0px) + ${padding2}px)`,
    paddingBottom: `calc(var(--mantine-footer-height, 0px) + ${padding2}px)`,
    paddingLeft: `calc(var(--mantine-navbar-width, 0px) + ${padding2}px)`,
    paddingRight: `calc(var(--mantine-aside-width, 0px) + ${padding2}px)`,
    [`@media (max-width: ${navbarOffset - 1}px)`]: {
      paddingLeft: padding2
    },
    [`@media (max-width: ${asideOffset - 1}px)`]: {
      paddingRight: padding2
    }
  };
}
var useStyles11 = createStyles((theme, props) => ({
  root: {
    boxSizing: "border-box"
  },
  body: {
    display: "flex",
    boxSizing: "border-box"
  },
  main: __spreadValues46({
    flex: 1,
    width: "100vw",
    boxSizing: "border-box"
  }, getPositionStyles(props, theme))
}));
var AppShell_styles_default = useStyles11;

// node_modules/@mantine/core/esm/AppShell/AppShell.js
var __defProp47 = Object.defineProperty;
var __getOwnPropSymbols48 = Object.getOwnPropertySymbols;
var __hasOwnProp48 = Object.prototype.hasOwnProperty;
var __propIsEnum48 = Object.prototype.propertyIsEnumerable;
var __defNormalProp47 = (obj, key, value) => key in obj ? __defProp47(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues47 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp48.call(b2, prop))
      __defNormalProp47(a2, prop, b2[prop]);
  if (__getOwnPropSymbols48)
    for (var prop of __getOwnPropSymbols48(b2)) {
      if (__propIsEnum48.call(b2, prop))
        __defNormalProp47(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest25 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp48.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols48)
    for (var prop of __getOwnPropSymbols48(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum48.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps17 = {
  fixed: true,
  padding: "md"
};
var AppShell = (0, import_react100.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("AppShell", defaultProps17, props), {
    children,
    navbar,
    header,
    footer,
    aside,
    fixed,
    zIndex,
    padding: padding2,
    navbarOffsetBreakpoint,
    asideOffsetBreakpoint,
    className,
    styles: styles2,
    classNames,
    unstyled,
    hidden: hidden2,
    layout
  } = _a, others = __objRest25(_a, [
    "children",
    "navbar",
    "header",
    "footer",
    "aside",
    "fixed",
    "zIndex",
    "padding",
    "navbarOffsetBreakpoint",
    "asideOffsetBreakpoint",
    "className",
    "styles",
    "classNames",
    "unstyled",
    "hidden",
    "layout"
  ]);
  const { classes, cx } = AppShell_styles_default({ padding: padding2, fixed, navbarOffsetBreakpoint, asideOffsetBreakpoint }, { styles: styles2, classNames, unstyled, name: "AppShell" });
  if (hidden2) {
    return import_react100.default.createElement(import_react100.default.Fragment, null, children);
  }
  return import_react100.default.createElement(AppShellProvider, {
    value: { fixed, zIndex, layout }
  }, import_react100.default.createElement(Box, __spreadValues47({
    className: cx(classes.root, className),
    ref
  }, others), header, import_react100.default.createElement("div", {
    className: classes.body
  }, navbar, import_react100.default.createElement("main", {
    className: classes.main
  }, children), aside), footer));
});
AppShell.displayName = "@mantine/core/AppShell";

// node_modules/@mantine/core/esm/AspectRatio/AspectRatio.js
var import_react101 = __toESM(require_react());

// node_modules/@mantine/core/esm/AspectRatio/AspectRatio.styles.js
var useStyles12 = createStyles((_theme, { ratio }) => ({
  root: {
    position: "relative",
    maxWidth: "100%",
    "&::before": {
      content: '""',
      height: 0,
      display: "block",
      paddingBottom: `${1 / ratio * 100}%`
    },
    "&::after": {
      content: '""',
      display: "table",
      clear: "both"
    },
    "& > *:not(style)": {
      overflow: "hidden",
      position: "absolute",
      top: 0,
      right: 0,
      bottom: 0,
      left: 0,
      display: "flex",
      justifyContent: "center",
      alignItems: "center",
      width: "100%",
      height: "100%"
    },
    "& > img, & > video": {
      objectFit: "cover"
    }
  }
}));
var AspectRatio_styles_default = useStyles12;

// node_modules/@mantine/core/esm/AspectRatio/AspectRatio.js
var __defProp48 = Object.defineProperty;
var __getOwnPropSymbols49 = Object.getOwnPropertySymbols;
var __hasOwnProp49 = Object.prototype.hasOwnProperty;
var __propIsEnum49 = Object.prototype.propertyIsEnumerable;
var __defNormalProp48 = (obj, key, value) => key in obj ? __defProp48(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues48 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp49.call(b2, prop))
      __defNormalProp48(a2, prop, b2[prop]);
  if (__getOwnPropSymbols49)
    for (var prop of __getOwnPropSymbols49(b2)) {
      if (__propIsEnum49.call(b2, prop))
        __defNormalProp48(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest26 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp49.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols49)
    for (var prop of __getOwnPropSymbols49(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum49.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var AspectRatio = (0, import_react101.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("AspectRatio", {}, props), { className, ratio, children, unstyled } = _a, others = __objRest26(_a, ["className", "ratio", "children", "unstyled"]);
  const { classes, cx } = AspectRatio_styles_default({ ratio }, { name: "AspectRatio", unstyled });
  return import_react101.default.createElement(Box, __spreadValues48({
    ref,
    className: cx(classes.root, className)
  }, others), children);
});
AspectRatio.displayName = "@mantine/core/AspectRatio";

// node_modules/@mantine/core/esm/Autocomplete/Autocomplete.js
var import_react136 = __toESM(require_react());

// node_modules/@mantine/core/esm/Select/SelectItems/SelectItems.js
var import_react103 = __toESM(require_react());

// node_modules/@mantine/core/esm/Divider/Divider.js
var import_react102 = __toESM(require_react());

// node_modules/@mantine/core/esm/Divider/Divider.styles.js
var sizes3 = {
  xs: 1,
  sm: 2,
  md: 3,
  lg: 4,
  xl: 5
};
function getColor(theme, color) {
  const themeColor2 = theme.fn.variant({ variant: "outline", color }).border;
  return typeof color === "string" && (color in theme.colors || color.split(".")[0] in theme.colors) ? themeColor2 : color === void 0 ? theme.colorScheme === "dark" ? theme.colors.dark[4] : theme.colors.gray[4] : color;
}
var useStyles13 = createStyles((theme, { size: size3, variant: variant2, color }) => ({
  root: {},
  withLabel: {
    borderTop: "0 !important"
  },
  left: {
    "&::before": {
      display: "none"
    }
  },
  right: {
    "&::after": {
      display: "none"
    }
  },
  label: {
    display: "flex",
    alignItems: "center",
    "&::before": {
      content: '""',
      flex: 1,
      height: 1,
      borderTop: `${theme.fn.size({ size: size3, sizes: sizes3 })}px ${variant2} ${getColor(theme, color)}`,
      marginRight: theme.spacing.xs
    },
    "&::after": {
      content: '""',
      flex: 1,
      borderTop: `${theme.fn.size({ size: size3, sizes: sizes3 })}px ${variant2} ${getColor(theme, color)}`,
      marginLeft: theme.spacing.xs
    }
  },
  labelDefaultStyles: {
    color: color === "dark" ? theme.colors.dark[1] : theme.fn.themeColor(color, theme.colorScheme === "dark" ? 5 : theme.fn.primaryShade(), false)
  },
  horizontal: {
    border: 0,
    borderTopWidth: theme.fn.size({ size: size3, sizes: sizes3 }),
    borderTopColor: getColor(theme, color),
    borderTopStyle: variant2,
    margin: 0
  },
  vertical: {
    border: 0,
    alignSelf: "stretch",
    height: "auto",
    borderLeftWidth: theme.fn.size({ size: size3, sizes: sizes3 }),
    borderLeftColor: getColor(theme, color),
    borderLeftStyle: variant2
  }
}));
var Divider_styles_default = useStyles13;

// node_modules/@mantine/core/esm/Divider/Divider.js
var __defProp49 = Object.defineProperty;
var __defProps19 = Object.defineProperties;
var __getOwnPropDescs19 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols50 = Object.getOwnPropertySymbols;
var __hasOwnProp50 = Object.prototype.hasOwnProperty;
var __propIsEnum50 = Object.prototype.propertyIsEnumerable;
var __defNormalProp49 = (obj, key, value) => key in obj ? __defProp49(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues49 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp50.call(b2, prop))
      __defNormalProp49(a2, prop, b2[prop]);
  if (__getOwnPropSymbols50)
    for (var prop of __getOwnPropSymbols50(b2)) {
      if (__propIsEnum50.call(b2, prop))
        __defNormalProp49(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps19 = (a2, b2) => __defProps19(a2, __getOwnPropDescs19(b2));
var __objRest27 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp50.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols50)
    for (var prop of __getOwnPropSymbols50(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum50.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps18 = {
  orientation: "horizontal",
  size: "xs",
  labelPosition: "left",
  variant: "solid"
};
var Divider = (0, import_react102.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("Divider", defaultProps18, props), {
    className,
    color,
    orientation,
    size: size3,
    label,
    labelPosition,
    labelProps,
    variant: variant2,
    styles: styles2,
    classNames,
    unstyled
  } = _a, others = __objRest27(_a, [
    "className",
    "color",
    "orientation",
    "size",
    "label",
    "labelPosition",
    "labelProps",
    "variant",
    "styles",
    "classNames",
    "unstyled"
  ]);
  const { classes, cx } = Divider_styles_default({ color, size: size3, variant: variant2 }, { classNames, styles: styles2, unstyled, name: "Divider" });
  const vertical = orientation === "vertical";
  const horizontal = orientation === "horizontal";
  const withLabel = !!label && horizontal;
  const useLabelDefaultStyles = !(labelProps == null ? void 0 : labelProps.color);
  return import_react102.default.createElement(Box, __spreadValues49({
    ref,
    className: cx(classes.root, {
      [classes.vertical]: vertical,
      [classes.horizontal]: horizontal,
      [classes.withLabel]: withLabel
    }, className),
    role: "separator"
  }, others), withLabel && import_react102.default.createElement(Text, __spreadProps19(__spreadValues49({}, labelProps), {
    size: (labelProps == null ? void 0 : labelProps.size) || "xs",
    sx: { marginTop: 2 },
    className: cx(classes.label, classes[labelPosition], {
      [classes.labelDefaultStyles]: useLabelDefaultStyles
    })
  }), label));
});
Divider.displayName = "@mantine/core/Divider";

// node_modules/@mantine/core/esm/Select/SelectItems/SelectItems.styles.js
var __defProp50 = Object.defineProperty;
var __defProps20 = Object.defineProperties;
var __getOwnPropDescs20 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols51 = Object.getOwnPropertySymbols;
var __hasOwnProp51 = Object.prototype.hasOwnProperty;
var __propIsEnum51 = Object.prototype.propertyIsEnumerable;
var __defNormalProp50 = (obj, key, value) => key in obj ? __defProp50(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues50 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp51.call(b2, prop))
      __defNormalProp50(a2, prop, b2[prop]);
  if (__getOwnPropSymbols51)
    for (var prop of __getOwnPropSymbols51(b2)) {
      if (__propIsEnum51.call(b2, prop))
        __defNormalProp50(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps20 = (a2, b2) => __defProps20(a2, __getOwnPropDescs20(b2));
var useStyles14 = createStyles((theme, { size: size3 }) => ({
  item: __spreadProps20(__spreadValues50({}, theme.fn.fontStyles()), {
    boxSizing: "border-box",
    textAlign: "left",
    width: "100%",
    padding: `${theme.fn.size({ size: size3, sizes: theme.spacing }) / 1.5}px ${theme.fn.size({
      size: size3,
      sizes: theme.spacing
    })}px`,
    cursor: "pointer",
    fontSize: theme.fn.size({ size: size3, sizes: theme.fontSizes }),
    color: theme.colorScheme === "dark" ? theme.colors.dark[0] : theme.black,
    borderRadius: theme.fn.radius(),
    "&[data-hovered]": {
      backgroundColor: theme.colorScheme === "dark" ? theme.colors.dark[4] : theme.colors.gray[1]
    },
    "&[data-selected]": __spreadValues50({
      backgroundColor: theme.fn.variant({ variant: "filled" }).background,
      color: theme.fn.variant({ variant: "filled" }).color
    }, theme.fn.hover({ backgroundColor: theme.fn.variant({ variant: "filled" }).hover })),
    "&[data-disabled]": {
      cursor: "default",
      color: theme.colors.dark[2]
    }
  }),
  nothingFound: {
    boxSizing: "border-box",
    color: theme.colors.gray[6],
    paddingTop: theme.fn.size({ size: size3, sizes: theme.spacing }) / 2,
    paddingBottom: theme.fn.size({ size: size3, sizes: theme.spacing }) / 2,
    textAlign: "center"
  },
  separator: {
    boxSizing: "border-box",
    textAlign: "left",
    width: "100%",
    padding: `${theme.fn.size({ size: size3, sizes: theme.spacing }) / 1.5}px ${theme.fn.size({
      size: size3,
      sizes: theme.spacing
    })}px`
  },
  separatorLabel: {
    color: theme.colorScheme === "dark" ? theme.colors.dark[3] : theme.colors.gray[5]
  }
}));
var SelectItems_styles_default = useStyles14;

// node_modules/@mantine/core/esm/Select/SelectItems/SelectItems.js
var __defProp51 = Object.defineProperty;
var __getOwnPropSymbols52 = Object.getOwnPropertySymbols;
var __hasOwnProp52 = Object.prototype.hasOwnProperty;
var __propIsEnum52 = Object.prototype.propertyIsEnumerable;
var __defNormalProp51 = (obj, key, value) => key in obj ? __defProp51(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues51 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp52.call(b2, prop))
      __defNormalProp51(a2, prop, b2[prop]);
  if (__getOwnPropSymbols52)
    for (var prop of __getOwnPropSymbols52(b2)) {
      if (__propIsEnum52.call(b2, prop))
        __defNormalProp51(a2, prop, b2[prop]);
    }
  return a2;
};
function SelectItems({
  data,
  hovered,
  classNames,
  styles: styles2,
  isItemSelected,
  uuid,
  __staticSelector,
  onItemHover,
  onItemSelect,
  itemsRefs,
  itemComponent: Item,
  size: size3,
  nothingFound,
  creatable,
  createLabel,
  unstyled
}) {
  const { classes } = SelectItems_styles_default({ size: size3 }, { classNames, styles: styles2, unstyled, name: __staticSelector });
  const unGroupedItems = [];
  const groupedItems = [];
  let creatableDataIndex = null;
  const constructItemComponent = (item, index6) => {
    const selected = typeof isItemSelected === "function" ? isItemSelected(item.value) : false;
    return import_react103.default.createElement(Item, __spreadValues51({
      key: item.value,
      className: classes.item,
      "data-disabled": item.disabled || void 0,
      "data-hovered": !item.disabled && hovered === index6 || void 0,
      "data-selected": !item.disabled && selected || void 0,
      selected,
      onMouseEnter: () => onItemHover(index6),
      id: `${uuid}-${index6}`,
      role: "option",
      tabIndex: -1,
      "aria-selected": hovered === index6,
      ref: (node2) => {
        if (itemsRefs && itemsRefs.current) {
          itemsRefs.current[item.value] = node2;
        }
      },
      onMouseDown: !item.disabled ? (event) => {
        event.preventDefault();
        onItemSelect(item);
      } : null,
      disabled: item.disabled
    }, item));
  };
  let groupName = null;
  data.forEach((item, index6) => {
    if (item.creatable) {
      creatableDataIndex = index6;
    } else if (!item.group) {
      unGroupedItems.push(constructItemComponent(item, index6));
    } else {
      if (groupName !== item.group) {
        groupName = item.group;
        groupedItems.push(import_react103.default.createElement("div", {
          className: classes.separator,
          key: `__mantine-divider-${index6}`
        }, import_react103.default.createElement(Divider, {
          classNames: { label: classes.separatorLabel },
          label: item.group
        })));
      }
      groupedItems.push(constructItemComponent(item, index6));
    }
  });
  if (creatable) {
    const creatableDataItem = data[creatableDataIndex];
    unGroupedItems.push(import_react103.default.createElement("div", {
      key: randomId2(),
      className: classes.item,
      "data-hovered": hovered === creatableDataIndex || void 0,
      onMouseEnter: () => onItemHover(creatableDataIndex),
      onMouseDown: (event) => {
        event.preventDefault();
        onItemSelect(creatableDataItem);
      },
      tabIndex: -1,
      ref: (node2) => {
        if (itemsRefs && itemsRefs.current) {
          itemsRefs.current[creatableDataItem.value] = node2;
        }
      }
    }, createLabel));
  }
  if (groupedItems.length > 0 && unGroupedItems.length > 0) {
    unGroupedItems.unshift(import_react103.default.createElement("div", {
      className: classes.separator,
      key: "empty-group-separator"
    }, import_react103.default.createElement(Divider, null)));
  }
  return groupedItems.length > 0 || unGroupedItems.length > 0 ? import_react103.default.createElement(import_react103.default.Fragment, null, groupedItems, unGroupedItems) : import_react103.default.createElement(Text, {
    size: size3,
    unstyled,
    className: classes.nothingFound
  }, nothingFound);
}
SelectItems.displayName = "@mantine/core/SelectItems";

// node_modules/@mantine/core/esm/Select/DefaultItem/DefaultItem.js
var import_react104 = __toESM(require_react());
var __defProp52 = Object.defineProperty;
var __getOwnPropSymbols53 = Object.getOwnPropertySymbols;
var __hasOwnProp53 = Object.prototype.hasOwnProperty;
var __propIsEnum53 = Object.prototype.propertyIsEnumerable;
var __defNormalProp52 = (obj, key, value) => key in obj ? __defProp52(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues52 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp53.call(b2, prop))
      __defNormalProp52(a2, prop, b2[prop]);
  if (__getOwnPropSymbols53)
    for (var prop of __getOwnPropSymbols53(b2)) {
      if (__propIsEnum53.call(b2, prop))
        __defNormalProp52(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest28 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp53.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols53)
    for (var prop of __getOwnPropSymbols53(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum53.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var DefaultItem = (0, import_react104.forwardRef)((_a, ref) => {
  var _b = _a, { label, value } = _b, others = __objRest28(_b, ["label", "value"]);
  return import_react104.default.createElement("div", __spreadValues52({
    ref
  }, others), label || value);
});
DefaultItem.displayName = "@mantine/core/DefaultItem";

// node_modules/@mantine/core/esm/Select/SelectPopover/SelectPopover.js
var import_react128 = __toESM(require_react());

// node_modules/@mantine/core/esm/Select/SelectScrollArea/SelectScrollArea.js
var import_react115 = __toESM(require_react());

// node_modules/@mantine/core/esm/ScrollArea/ScrollArea.js
var import_react114 = __toESM(require_react());

// node_modules/@radix-ui/react-scroll-area/dist/index.module.js
var import_react113 = __toESM(require_react());

// node_modules/@radix-ui/react-primitive/dist/index.module.js
var import_react107 = __toESM(require_react());
var import_react_dom3 = __toESM(require_react_dom());

// node_modules/@radix-ui/react-slot/dist/index.module.js
var import_react106 = __toESM(require_react());

// node_modules/@radix-ui/react-compose-refs/dist/index.module.js
var import_react105 = __toESM(require_react());
function $6ed0406888f73fc4$var$setRef(ref, value) {
  if (typeof ref === "function")
    ref(value);
  else if (ref !== null && ref !== void 0)
    ref.current = value;
}
function $6ed0406888f73fc4$export$43e446d32b3d21af(...refs) {
  return (node2) => refs.forEach(
    (ref) => $6ed0406888f73fc4$var$setRef(ref, node2)
  );
}
function $6ed0406888f73fc4$export$c7b2cbe3552a0d05(...refs) {
  return (0, import_react105.useCallback)($6ed0406888f73fc4$export$43e446d32b3d21af(...refs), refs);
}

// node_modules/@radix-ui/react-slot/dist/index.module.js
var $5e63c961fc1ce211$export$8c6ed5c666ac1360 = (0, import_react106.forwardRef)((props, forwardedRef) => {
  const { children, ...slotProps } = props;
  const childrenArray = import_react106.Children.toArray(children);
  const slottable = childrenArray.find($5e63c961fc1ce211$var$isSlottable);
  if (slottable) {
    const newElement = slottable.props.children;
    const newChildren = childrenArray.map((child) => {
      if (child === slottable) {
        if (import_react106.Children.count(newElement) > 1)
          return import_react106.Children.only(null);
        return (0, import_react106.isValidElement)(newElement) ? newElement.props.children : null;
      } else
        return child;
    });
    return (0, import_react106.createElement)($5e63c961fc1ce211$var$SlotClone, _extends({}, slotProps, {
      ref: forwardedRef
    }), (0, import_react106.isValidElement)(newElement) ? (0, import_react106.cloneElement)(newElement, void 0, newChildren) : null);
  }
  return (0, import_react106.createElement)($5e63c961fc1ce211$var$SlotClone, _extends({}, slotProps, {
    ref: forwardedRef
  }), children);
});
$5e63c961fc1ce211$export$8c6ed5c666ac1360.displayName = "Slot";
var $5e63c961fc1ce211$var$SlotClone = (0, import_react106.forwardRef)((props, forwardedRef) => {
  const { children, ...slotProps } = props;
  if ((0, import_react106.isValidElement)(children))
    return (0, import_react106.cloneElement)(children, {
      ...$5e63c961fc1ce211$var$mergeProps(slotProps, children.props),
      ref: $6ed0406888f73fc4$export$43e446d32b3d21af(forwardedRef, children.ref)
    });
  return import_react106.Children.count(children) > 1 ? import_react106.Children.only(null) : null;
});
$5e63c961fc1ce211$var$SlotClone.displayName = "SlotClone";
var $5e63c961fc1ce211$export$d9f1ccf0bdb05d45 = ({ children }) => {
  return (0, import_react106.createElement)(import_react106.Fragment, null, children);
};
function $5e63c961fc1ce211$var$isSlottable(child) {
  return (0, import_react106.isValidElement)(child) && child.type === $5e63c961fc1ce211$export$d9f1ccf0bdb05d45;
}
function $5e63c961fc1ce211$var$mergeProps(slotProps, childProps) {
  const overrideProps = {
    ...childProps
  };
  for (const propName in childProps) {
    const slotPropValue = slotProps[propName];
    const childPropValue = childProps[propName];
    const isHandler = /^on[A-Z]/.test(propName);
    if (isHandler) {
      if (slotPropValue && childPropValue)
        overrideProps[propName] = (...args) => {
          childPropValue(...args);
          slotPropValue(...args);
        };
      else if (slotPropValue)
        overrideProps[propName] = slotPropValue;
    } else if (propName === "style")
      overrideProps[propName] = {
        ...slotPropValue,
        ...childPropValue
      };
    else if (propName === "className")
      overrideProps[propName] = [
        slotPropValue,
        childPropValue
      ].filter(Boolean).join(" ");
  }
  return {
    ...slotProps,
    ...overrideProps
  };
}

// node_modules/@radix-ui/react-primitive/dist/index.module.js
var $8927f6f2acc4f386$var$NODES = [
  "a",
  "button",
  "div",
  "h2",
  "h3",
  "img",
  "label",
  "li",
  "nav",
  "ol",
  "p",
  "span",
  "svg",
  "ul"
];
var $8927f6f2acc4f386$export$250ffa63cdc0d034 = $8927f6f2acc4f386$var$NODES.reduce((primitive, node2) => {
  const Node2 = (0, import_react107.forwardRef)((props, forwardedRef) => {
    const { asChild, ...primitiveProps } = props;
    const Comp = asChild ? $5e63c961fc1ce211$export$8c6ed5c666ac1360 : node2;
    (0, import_react107.useEffect)(() => {
      window[Symbol.for("radix-ui")] = true;
    }, []);
    return (0, import_react107.createElement)(Comp, _extends({}, primitiveProps, {
      ref: forwardedRef
    }));
  });
  Node2.displayName = `Primitive.${node2}`;
  return {
    ...primitive,
    [node2]: Node2
  };
}, {});

// node_modules/@radix-ui/react-presence/dist/index.module.js
var import_react109 = __toESM(require_react());
var import_react_dom4 = __toESM(require_react_dom());

// node_modules/@radix-ui/react-use-layout-effect/dist/index.module.js
var import_react108 = __toESM(require_react());
var $9f79659886946c16$export$e5c5a5f917a5871c = Boolean(globalThis === null || globalThis === void 0 ? void 0 : globalThis.document) ? import_react108.useLayoutEffect : () => {
};

// node_modules/@radix-ui/react-presence/dist/index.module.js
function $fe963b355347cc68$export$3e6543de14f8614f(initialState, machine) {
  return (0, import_react109.useReducer)((state, event) => {
    const nextState = machine[state][event];
    return nextState !== null && nextState !== void 0 ? nextState : state;
  }, initialState);
}
var $921a889cee6df7e8$export$99c2b779aa4e8b8b = (props) => {
  const { present, children } = props;
  const presence = $921a889cee6df7e8$var$usePresence(present);
  const child = typeof children === "function" ? children({
    present: presence.isPresent
  }) : import_react109.Children.only(children);
  const ref = $6ed0406888f73fc4$export$c7b2cbe3552a0d05(presence.ref, child.ref);
  const forceMount = typeof children === "function";
  return forceMount || presence.isPresent ? (0, import_react109.cloneElement)(child, {
    ref
  }) : null;
};
$921a889cee6df7e8$export$99c2b779aa4e8b8b.displayName = "Presence";
function $921a889cee6df7e8$var$usePresence(present) {
  const [node1, setNode] = (0, import_react109.useState)();
  const stylesRef = (0, import_react109.useRef)({});
  const prevPresentRef = (0, import_react109.useRef)(present);
  const prevAnimationNameRef = (0, import_react109.useRef)("none");
  const initialState = present ? "mounted" : "unmounted";
  const [state, send] = $fe963b355347cc68$export$3e6543de14f8614f(initialState, {
    mounted: {
      UNMOUNT: "unmounted",
      ANIMATION_OUT: "unmountSuspended"
    },
    unmountSuspended: {
      MOUNT: "mounted",
      ANIMATION_END: "unmounted"
    },
    unmounted: {
      MOUNT: "mounted"
    }
  });
  (0, import_react109.useEffect)(() => {
    const currentAnimationName = $921a889cee6df7e8$var$getAnimationName(stylesRef.current);
    prevAnimationNameRef.current = state === "mounted" ? currentAnimationName : "none";
  }, [
    state
  ]);
  $9f79659886946c16$export$e5c5a5f917a5871c(() => {
    const styles2 = stylesRef.current;
    const wasPresent = prevPresentRef.current;
    const hasPresentChanged = wasPresent !== present;
    if (hasPresentChanged) {
      const prevAnimationName = prevAnimationNameRef.current;
      const currentAnimationName = $921a889cee6df7e8$var$getAnimationName(styles2);
      if (present)
        send("MOUNT");
      else if (currentAnimationName === "none" || (styles2 === null || styles2 === void 0 ? void 0 : styles2.display) === "none")
        send("UNMOUNT");
      else {
        const isAnimating = prevAnimationName !== currentAnimationName;
        if (wasPresent && isAnimating)
          send("ANIMATION_OUT");
        else
          send("UNMOUNT");
      }
      prevPresentRef.current = present;
    }
  }, [
    present,
    send
  ]);
  $9f79659886946c16$export$e5c5a5f917a5871c(() => {
    if (node1) {
      const handleAnimationEnd = (event) => {
        const currentAnimationName = $921a889cee6df7e8$var$getAnimationName(stylesRef.current);
        const isCurrentAnimation = currentAnimationName.includes(event.animationName);
        if (event.target === node1 && isCurrentAnimation)
          (0, import_react_dom4.flushSync)(
            () => send("ANIMATION_END")
          );
      };
      const handleAnimationStart = (event) => {
        if (event.target === node1)
          prevAnimationNameRef.current = $921a889cee6df7e8$var$getAnimationName(stylesRef.current);
      };
      node1.addEventListener("animationstart", handleAnimationStart);
      node1.addEventListener("animationcancel", handleAnimationEnd);
      node1.addEventListener("animationend", handleAnimationEnd);
      return () => {
        node1.removeEventListener("animationstart", handleAnimationStart);
        node1.removeEventListener("animationcancel", handleAnimationEnd);
        node1.removeEventListener("animationend", handleAnimationEnd);
      };
    } else
      send("ANIMATION_END");
  }, [
    node1,
    send
  ]);
  return {
    isPresent: [
      "mounted",
      "unmountSuspended"
    ].includes(state),
    ref: (0, import_react109.useCallback)((node2) => {
      if (node2)
        stylesRef.current = getComputedStyle(node2);
      setNode(node2);
    }, [])
  };
}
function $921a889cee6df7e8$var$getAnimationName(styles2) {
  return (styles2 === null || styles2 === void 0 ? void 0 : styles2.animationName) || "none";
}

// node_modules/@radix-ui/react-context/dist/index.module.js
var import_react110 = __toESM(require_react());
function $c512c27ab02ef895$export$50c7b4e9d9f19c1(scopeName, createContextScopeDeps = []) {
  let defaultContexts = [];
  function $c512c27ab02ef895$export$fd42f52fd3ae1109(rootComponentName, defaultContext) {
    const BaseContext = (0, import_react110.createContext)(defaultContext);
    const index6 = defaultContexts.length;
    defaultContexts = [
      ...defaultContexts,
      defaultContext
    ];
    function Provider(props) {
      const { scope, children, ...context } = props;
      const Context = (scope === null || scope === void 0 ? void 0 : scope[scopeName][index6]) || BaseContext;
      const value = (0, import_react110.useMemo)(
        () => context,
        Object.values(context)
      );
      return (0, import_react110.createElement)(Context.Provider, {
        value
      }, children);
    }
    function useContext18(consumerName, scope) {
      const Context = (scope === null || scope === void 0 ? void 0 : scope[scopeName][index6]) || BaseContext;
      const context = (0, import_react110.useContext)(Context);
      if (context)
        return context;
      if (defaultContext !== void 0)
        return defaultContext;
      throw new Error(`\`${consumerName}\` must be used within \`${rootComponentName}\``);
    }
    Provider.displayName = rootComponentName + "Provider";
    return [
      Provider,
      useContext18
    ];
  }
  const createScope = () => {
    const scopeContexts = defaultContexts.map((defaultContext) => {
      return (0, import_react110.createContext)(defaultContext);
    });
    return function useScope(scope) {
      const contexts = (scope === null || scope === void 0 ? void 0 : scope[scopeName]) || scopeContexts;
      return (0, import_react110.useMemo)(
        () => ({
          [`__scope${scopeName}`]: {
            ...scope,
            [scopeName]: contexts
          }
        }),
        [
          scope,
          contexts
        ]
      );
    };
  };
  createScope.scopeName = scopeName;
  return [
    $c512c27ab02ef895$export$fd42f52fd3ae1109,
    $c512c27ab02ef895$var$composeContextScopes(createScope, ...createContextScopeDeps)
  ];
}
function $c512c27ab02ef895$var$composeContextScopes(...scopes) {
  const baseScope = scopes[0];
  if (scopes.length === 1)
    return baseScope;
  const createScope1 = () => {
    const scopeHooks = scopes.map(
      (createScope) => ({
        useScope: createScope(),
        scopeName: createScope.scopeName
      })
    );
    return function useComposedScopes(overrideScopes) {
      const nextScopes1 = scopeHooks.reduce((nextScopes, { useScope, scopeName }) => {
        const scopeProps = useScope(overrideScopes);
        const currentScope = scopeProps[`__scope${scopeName}`];
        return {
          ...nextScopes,
          ...currentScope
        };
      }, {});
      return (0, import_react110.useMemo)(
        () => ({
          [`__scope${baseScope.scopeName}`]: nextScopes1
        }),
        [
          nextScopes1
        ]
      );
    };
  };
  createScope1.scopeName = baseScope.scopeName;
  return createScope1;
}

// node_modules/@radix-ui/react-use-callback-ref/dist/index.module.js
var import_react111 = __toESM(require_react());
function $b1b2314f5f9a1d84$export$25bec8c6f54ee79a(callback) {
  const callbackRef = (0, import_react111.useRef)(callback);
  (0, import_react111.useEffect)(() => {
    callbackRef.current = callback;
  });
  return (0, import_react111.useMemo)(
    () => (...args) => {
      var _callbackRef$current;
      return (_callbackRef$current = callbackRef.current) === null || _callbackRef$current === void 0 ? void 0 : _callbackRef$current.call(callbackRef, ...args);
    },
    []
  );
}

// node_modules/@radix-ui/react-direction/dist/index.module.js
var import_react112 = __toESM(require_react());
var $f631663db3294ace$var$DirectionContext = (0, import_react112.createContext)(void 0);
function $f631663db3294ace$export$b39126d51d94e6f3(localDir) {
  const globalDir = (0, import_react112.useContext)($f631663db3294ace$var$DirectionContext);
  return localDir || globalDir || "ltr";
}

// node_modules/@radix-ui/number/dist/index.module.js
function $ae6933e535247d3d$export$7d15b64cf5a3a4c4(value, [min3, max3]) {
  return Math.min(max3, Math.max(min3, value));
}

// node_modules/@radix-ui/primitive/dist/index.module.js
function $e42e1063c40fb3ef$export$b9ecd428b558ff10(originalEventHandler, ourEventHandler, { checkForDefaultPrevented = true } = {}) {
  return function handleEvent(event) {
    originalEventHandler === null || originalEventHandler === void 0 || originalEventHandler(event);
    if (checkForDefaultPrevented === false || !event.defaultPrevented)
      return ourEventHandler === null || ourEventHandler === void 0 ? void 0 : ourEventHandler(event);
  };
}

// node_modules/@radix-ui/react-scroll-area/dist/index.module.js
function $6c2e24571c90391f$export$3e6543de14f8614f(initialState, machine) {
  return (0, import_react113.useReducer)((state, event) => {
    const nextState = machine[state][event];
    return nextState !== null && nextState !== void 0 ? nextState : state;
  }, initialState);
}
var $57acba87d6e25586$var$SCROLL_AREA_NAME = "ScrollArea";
var [$57acba87d6e25586$var$createScrollAreaContext, $57acba87d6e25586$export$488468afe3a6f2b1] = $c512c27ab02ef895$export$50c7b4e9d9f19c1($57acba87d6e25586$var$SCROLL_AREA_NAME);
var [$57acba87d6e25586$var$ScrollAreaProvider, $57acba87d6e25586$var$useScrollAreaContext] = $57acba87d6e25586$var$createScrollAreaContext($57acba87d6e25586$var$SCROLL_AREA_NAME);
var $57acba87d6e25586$export$ccf8d8d7bbf3c2cc = (0, import_react113.forwardRef)((props, forwardedRef) => {
  const { __scopeScrollArea, type = "hover", dir, scrollHideDelay = 600, ...scrollAreaProps } = props;
  const [scrollArea, setScrollArea] = (0, import_react113.useState)(null);
  const [viewport2, setViewport] = (0, import_react113.useState)(null);
  const [content, setContent2] = (0, import_react113.useState)(null);
  const [scrollbarX, setScrollbarX] = (0, import_react113.useState)(null);
  const [scrollbarY, setScrollbarY] = (0, import_react113.useState)(null);
  const [cornerWidth, setCornerWidth] = (0, import_react113.useState)(0);
  const [cornerHeight, setCornerHeight] = (0, import_react113.useState)(0);
  const [scrollbarXEnabled, setScrollbarXEnabled] = (0, import_react113.useState)(false);
  const [scrollbarYEnabled, setScrollbarYEnabled] = (0, import_react113.useState)(false);
  const composedRefs = $6ed0406888f73fc4$export$c7b2cbe3552a0d05(
    forwardedRef,
    (node2) => setScrollArea(node2)
  );
  const direction = $f631663db3294ace$export$b39126d51d94e6f3(dir);
  return (0, import_react113.createElement)($57acba87d6e25586$var$ScrollAreaProvider, {
    scope: __scopeScrollArea,
    type,
    dir: direction,
    scrollHideDelay,
    scrollArea,
    viewport: viewport2,
    onViewportChange: setViewport,
    content,
    onContentChange: setContent2,
    scrollbarX,
    onScrollbarXChange: setScrollbarX,
    scrollbarXEnabled,
    onScrollbarXEnabledChange: setScrollbarXEnabled,
    scrollbarY,
    onScrollbarYChange: setScrollbarY,
    scrollbarYEnabled,
    onScrollbarYEnabledChange: setScrollbarYEnabled,
    onCornerWidthChange: setCornerWidth,
    onCornerHeightChange: setCornerHeight
  }, (0, import_react113.createElement)($8927f6f2acc4f386$export$250ffa63cdc0d034.div, _extends({
    dir: direction
  }, scrollAreaProps, {
    ref: composedRefs,
    style: {
      position: "relative",
      // Pass corner sizes as CSS vars to reduce re-renders of context consumers
      ["--radix-scroll-area-corner-width"]: cornerWidth + "px",
      ["--radix-scroll-area-corner-height"]: cornerHeight + "px",
      ...props.style
    }
  })));
});
Object.assign($57acba87d6e25586$export$ccf8d8d7bbf3c2cc, {
  displayName: $57acba87d6e25586$var$SCROLL_AREA_NAME
});
var $57acba87d6e25586$var$VIEWPORT_NAME = "ScrollAreaViewport";
var $57acba87d6e25586$export$a21cbf9f11fca853 = (0, import_react113.forwardRef)((props, forwardedRef) => {
  const { __scopeScrollArea, children, ...viewportProps } = props;
  const context = $57acba87d6e25586$var$useScrollAreaContext($57acba87d6e25586$var$VIEWPORT_NAME, __scopeScrollArea);
  const ref = (0, import_react113.useRef)(null);
  const composedRefs = $6ed0406888f73fc4$export$c7b2cbe3552a0d05(forwardedRef, ref, context.onViewportChange);
  return (0, import_react113.createElement)(import_react113.Fragment, null, (0, import_react113.createElement)("style", {
    dangerouslySetInnerHTML: {
      __html: `[data-radix-scroll-area-viewport]{scrollbar-width:none;-ms-overflow-style:none;-webkit-overflow-scrolling:touch;}[data-radix-scroll-area-viewport]::-webkit-scrollbar{display:none}`
    }
  }), (0, import_react113.createElement)($8927f6f2acc4f386$export$250ffa63cdc0d034.div, _extends({
    "data-radix-scroll-area-viewport": ""
  }, viewportProps, {
    ref: composedRefs,
    style: {
      /**
      * We don't support `visible` because the intention is to have at least one scrollbar
      * if this component is used and `visible` will behave like `auto` in that case
      * https://developer.mozilla.org/en-US/docs/Web/CSS/overflowed#description
      *
      * We don't handle `auto` because the intention is for the native implementation
      * to be hidden if using this component. We just want to ensure the node is scrollable
      * so could have used either `scroll` or `auto` here. We picked `scroll` to prevent
      * the browser from having to work out whether to render native scrollbars or not,
      * we tell it to with the intention of hiding them in CSS.
      */
      overflowX: context.scrollbarXEnabled ? "scroll" : "hidden",
      overflowY: context.scrollbarYEnabled ? "scroll" : "hidden",
      ...props.style
    }
  }), (0, import_react113.createElement)("div", {
    ref: context.onContentChange,
    style: {
      minWidth: "100%",
      display: "table"
    }
  }, children)));
});
Object.assign($57acba87d6e25586$export$a21cbf9f11fca853, {
  displayName: $57acba87d6e25586$var$VIEWPORT_NAME
});
var $57acba87d6e25586$var$SCROLLBAR_NAME = "ScrollAreaScrollbar";
var $57acba87d6e25586$export$2fabd85d0eba3c57 = (0, import_react113.forwardRef)((props, forwardedRef) => {
  const { forceMount, ...scrollbarProps } = props;
  const context = $57acba87d6e25586$var$useScrollAreaContext($57acba87d6e25586$var$SCROLLBAR_NAME, props.__scopeScrollArea);
  const { onScrollbarXEnabledChange, onScrollbarYEnabledChange } = context;
  const isHorizontal = props.orientation === "horizontal";
  (0, import_react113.useEffect)(() => {
    isHorizontal ? onScrollbarXEnabledChange(true) : onScrollbarYEnabledChange(true);
    return () => {
      isHorizontal ? onScrollbarXEnabledChange(false) : onScrollbarYEnabledChange(false);
    };
  }, [
    isHorizontal,
    onScrollbarXEnabledChange,
    onScrollbarYEnabledChange
  ]);
  return context.type === "hover" ? (0, import_react113.createElement)($57acba87d6e25586$var$ScrollAreaScrollbarHover, _extends({}, scrollbarProps, {
    ref: forwardedRef,
    forceMount
  })) : context.type === "scroll" ? (0, import_react113.createElement)($57acba87d6e25586$var$ScrollAreaScrollbarScroll, _extends({}, scrollbarProps, {
    ref: forwardedRef,
    forceMount
  })) : context.type === "auto" ? (0, import_react113.createElement)($57acba87d6e25586$var$ScrollAreaScrollbarAuto, _extends({}, scrollbarProps, {
    ref: forwardedRef,
    forceMount
  })) : context.type === "always" ? (0, import_react113.createElement)($57acba87d6e25586$var$ScrollAreaScrollbarVisible, _extends({}, scrollbarProps, {
    ref: forwardedRef
  })) : null;
});
Object.assign($57acba87d6e25586$export$2fabd85d0eba3c57, {
  displayName: $57acba87d6e25586$var$SCROLLBAR_NAME
});
var $57acba87d6e25586$var$ScrollAreaScrollbarHover = (0, import_react113.forwardRef)((props, forwardedRef) => {
  const { forceMount, ...scrollbarProps } = props;
  const context = $57acba87d6e25586$var$useScrollAreaContext($57acba87d6e25586$var$SCROLLBAR_NAME, props.__scopeScrollArea);
  const [visible2, setVisible] = (0, import_react113.useState)(false);
  (0, import_react113.useEffect)(() => {
    const scrollArea = context.scrollArea;
    let hideTimer = 0;
    if (scrollArea) {
      const handlePointerEnter = () => {
        window.clearTimeout(hideTimer);
        setVisible(true);
      };
      const handlePointerLeave = () => {
        hideTimer = window.setTimeout(
          () => setVisible(false),
          context.scrollHideDelay
        );
      };
      scrollArea.addEventListener("pointerenter", handlePointerEnter);
      scrollArea.addEventListener("pointerleave", handlePointerLeave);
      return () => {
        window.clearTimeout(hideTimer);
        scrollArea.removeEventListener("pointerenter", handlePointerEnter);
        scrollArea.removeEventListener("pointerleave", handlePointerLeave);
      };
    }
  }, [
    context.scrollArea,
    context.scrollHideDelay
  ]);
  return (0, import_react113.createElement)($921a889cee6df7e8$export$99c2b779aa4e8b8b, {
    present: forceMount || visible2
  }, (0, import_react113.createElement)($57acba87d6e25586$var$ScrollAreaScrollbarAuto, _extends({
    "data-state": visible2 ? "visible" : "hidden"
  }, scrollbarProps, {
    ref: forwardedRef
  })));
});
var $57acba87d6e25586$var$ScrollAreaScrollbarScroll = (0, import_react113.forwardRef)((props, forwardedRef) => {
  const { forceMount, ...scrollbarProps } = props;
  const context = $57acba87d6e25586$var$useScrollAreaContext($57acba87d6e25586$var$SCROLLBAR_NAME, props.__scopeScrollArea);
  const isHorizontal = props.orientation === "horizontal";
  const debounceScrollEnd = $57acba87d6e25586$var$useDebounceCallback(
    () => send("SCROLL_END"),
    100
  );
  const [state, send] = $6c2e24571c90391f$export$3e6543de14f8614f("hidden", {
    hidden: {
      SCROLL: "scrolling"
    },
    scrolling: {
      SCROLL_END: "idle",
      POINTER_ENTER: "interacting"
    },
    interacting: {
      SCROLL: "interacting",
      POINTER_LEAVE: "idle"
    },
    idle: {
      HIDE: "hidden",
      SCROLL: "scrolling",
      POINTER_ENTER: "interacting"
    }
  });
  (0, import_react113.useEffect)(() => {
    if (state === "idle") {
      const hideTimer = window.setTimeout(
        () => send("HIDE"),
        context.scrollHideDelay
      );
      return () => window.clearTimeout(hideTimer);
    }
  }, [
    state,
    context.scrollHideDelay,
    send
  ]);
  (0, import_react113.useEffect)(() => {
    const viewport2 = context.viewport;
    const scrollDirection = isHorizontal ? "scrollLeft" : "scrollTop";
    if (viewport2) {
      let prevScrollPos = viewport2[scrollDirection];
      const handleScroll = () => {
        const scrollPos = viewport2[scrollDirection];
        const hasScrollInDirectionChanged = prevScrollPos !== scrollPos;
        if (hasScrollInDirectionChanged) {
          send("SCROLL");
          debounceScrollEnd();
        }
        prevScrollPos = scrollPos;
      };
      viewport2.addEventListener("scroll", handleScroll);
      return () => viewport2.removeEventListener("scroll", handleScroll);
    }
  }, [
    context.viewport,
    isHorizontal,
    send,
    debounceScrollEnd
  ]);
  return (0, import_react113.createElement)($921a889cee6df7e8$export$99c2b779aa4e8b8b, {
    present: forceMount || state !== "hidden"
  }, (0, import_react113.createElement)($57acba87d6e25586$var$ScrollAreaScrollbarVisible, _extends({
    "data-state": state === "hidden" ? "hidden" : "visible"
  }, scrollbarProps, {
    ref: forwardedRef,
    onPointerEnter: $e42e1063c40fb3ef$export$b9ecd428b558ff10(
      props.onPointerEnter,
      () => send("POINTER_ENTER")
    ),
    onPointerLeave: $e42e1063c40fb3ef$export$b9ecd428b558ff10(
      props.onPointerLeave,
      () => send("POINTER_LEAVE")
    )
  })));
});
var $57acba87d6e25586$var$ScrollAreaScrollbarAuto = (0, import_react113.forwardRef)((props, forwardedRef) => {
  const context = $57acba87d6e25586$var$useScrollAreaContext($57acba87d6e25586$var$SCROLLBAR_NAME, props.__scopeScrollArea);
  const { forceMount, ...scrollbarProps } = props;
  const [visible2, setVisible] = (0, import_react113.useState)(false);
  const isHorizontal = props.orientation === "horizontal";
  const handleResize = $57acba87d6e25586$var$useDebounceCallback(() => {
    if (context.viewport) {
      const isOverflowX = context.viewport.offsetWidth < context.viewport.scrollWidth;
      const isOverflowY = context.viewport.offsetHeight < context.viewport.scrollHeight;
      setVisible(isHorizontal ? isOverflowX : isOverflowY);
    }
  }, 10);
  $57acba87d6e25586$var$useResizeObserver(context.viewport, handleResize);
  $57acba87d6e25586$var$useResizeObserver(context.content, handleResize);
  return (0, import_react113.createElement)($921a889cee6df7e8$export$99c2b779aa4e8b8b, {
    present: forceMount || visible2
  }, (0, import_react113.createElement)($57acba87d6e25586$var$ScrollAreaScrollbarVisible, _extends({
    "data-state": visible2 ? "visible" : "hidden"
  }, scrollbarProps, {
    ref: forwardedRef
  })));
});
var $57acba87d6e25586$var$ScrollAreaScrollbarVisible = (0, import_react113.forwardRef)((props, forwardedRef) => {
  const { orientation = "vertical", ...scrollbarProps } = props;
  const context = $57acba87d6e25586$var$useScrollAreaContext($57acba87d6e25586$var$SCROLLBAR_NAME, props.__scopeScrollArea);
  const thumbRef = (0, import_react113.useRef)(null);
  const pointerOffsetRef = (0, import_react113.useRef)(0);
  const [sizes24, setSizes] = (0, import_react113.useState)({
    content: 0,
    viewport: 0,
    scrollbar: {
      size: 0,
      paddingStart: 0,
      paddingEnd: 0
    }
  });
  const thumbRatio = $57acba87d6e25586$var$getThumbRatio(sizes24.viewport, sizes24.content);
  const commonProps = {
    ...scrollbarProps,
    sizes: sizes24,
    onSizesChange: setSizes,
    hasThumb: Boolean(thumbRatio > 0 && thumbRatio < 1),
    onThumbChange: (thumb) => thumbRef.current = thumb,
    onThumbPointerUp: () => pointerOffsetRef.current = 0,
    onThumbPointerDown: (pointerPos) => pointerOffsetRef.current = pointerPos
  };
  function getScrollPosition(pointerPos, dir) {
    return $57acba87d6e25586$var$getScrollPositionFromPointer(pointerPos, pointerOffsetRef.current, sizes24, dir);
  }
  if (orientation === "horizontal")
    return (0, import_react113.createElement)($57acba87d6e25586$var$ScrollAreaScrollbarX, _extends({}, commonProps, {
      ref: forwardedRef,
      onThumbPositionChange: () => {
        if (context.viewport && thumbRef.current) {
          const scrollPos = context.viewport.scrollLeft;
          const offset3 = $57acba87d6e25586$var$getThumbOffsetFromScroll(scrollPos, sizes24, context.dir);
          thumbRef.current.style.transform = `translate3d(${offset3}px, 0, 0)`;
        }
      },
      onWheelScroll: (scrollPos) => {
        if (context.viewport)
          context.viewport.scrollLeft = scrollPos;
      },
      onDragScroll: (pointerPos) => {
        if (context.viewport)
          context.viewport.scrollLeft = getScrollPosition(pointerPos, context.dir);
      }
    }));
  if (orientation === "vertical")
    return (0, import_react113.createElement)($57acba87d6e25586$var$ScrollAreaScrollbarY, _extends({}, commonProps, {
      ref: forwardedRef,
      onThumbPositionChange: () => {
        if (context.viewport && thumbRef.current) {
          const scrollPos = context.viewport.scrollTop;
          const offset3 = $57acba87d6e25586$var$getThumbOffsetFromScroll(scrollPos, sizes24);
          thumbRef.current.style.transform = `translate3d(0, ${offset3}px, 0)`;
        }
      },
      onWheelScroll: (scrollPos) => {
        if (context.viewport)
          context.viewport.scrollTop = scrollPos;
      },
      onDragScroll: (pointerPos) => {
        if (context.viewport)
          context.viewport.scrollTop = getScrollPosition(pointerPos);
      }
    }));
  return null;
});
var $57acba87d6e25586$var$ScrollAreaScrollbarX = (0, import_react113.forwardRef)((props, forwardedRef) => {
  const { sizes: sizes24, onSizesChange, ...scrollbarProps } = props;
  const context = $57acba87d6e25586$var$useScrollAreaContext($57acba87d6e25586$var$SCROLLBAR_NAME, props.__scopeScrollArea);
  const [computedStyle, setComputedStyle] = (0, import_react113.useState)();
  const ref = (0, import_react113.useRef)(null);
  const composeRefs = $6ed0406888f73fc4$export$c7b2cbe3552a0d05(forwardedRef, ref, context.onScrollbarXChange);
  (0, import_react113.useEffect)(() => {
    if (ref.current)
      setComputedStyle(getComputedStyle(ref.current));
  }, [
    ref
  ]);
  return (0, import_react113.createElement)($57acba87d6e25586$var$ScrollAreaScrollbarImpl, _extends({
    "data-orientation": "horizontal"
  }, scrollbarProps, {
    ref: composeRefs,
    sizes: sizes24,
    style: {
      bottom: 0,
      left: context.dir === "rtl" ? "var(--radix-scroll-area-corner-width)" : 0,
      right: context.dir === "ltr" ? "var(--radix-scroll-area-corner-width)" : 0,
      ["--radix-scroll-area-thumb-width"]: $57acba87d6e25586$var$getThumbSize(sizes24) + "px",
      ...props.style
    },
    onThumbPointerDown: (pointerPos) => props.onThumbPointerDown(pointerPos.x),
    onDragScroll: (pointerPos) => props.onDragScroll(pointerPos.x),
    onWheelScroll: (event, maxScrollPos) => {
      if (context.viewport) {
        const scrollPos = context.viewport.scrollLeft + event.deltaX;
        props.onWheelScroll(scrollPos);
        if ($57acba87d6e25586$var$isScrollingWithinScrollbarBounds(scrollPos, maxScrollPos))
          event.preventDefault();
      }
    },
    onResize: () => {
      if (ref.current && context.viewport && computedStyle)
        onSizesChange({
          content: context.viewport.scrollWidth,
          viewport: context.viewport.offsetWidth,
          scrollbar: {
            size: ref.current.clientWidth,
            paddingStart: $57acba87d6e25586$var$toInt(computedStyle.paddingLeft),
            paddingEnd: $57acba87d6e25586$var$toInt(computedStyle.paddingRight)
          }
        });
    }
  }));
});
var $57acba87d6e25586$var$ScrollAreaScrollbarY = (0, import_react113.forwardRef)((props, forwardedRef) => {
  const { sizes: sizes24, onSizesChange, ...scrollbarProps } = props;
  const context = $57acba87d6e25586$var$useScrollAreaContext($57acba87d6e25586$var$SCROLLBAR_NAME, props.__scopeScrollArea);
  const [computedStyle, setComputedStyle] = (0, import_react113.useState)();
  const ref = (0, import_react113.useRef)(null);
  const composeRefs = $6ed0406888f73fc4$export$c7b2cbe3552a0d05(forwardedRef, ref, context.onScrollbarYChange);
  (0, import_react113.useEffect)(() => {
    if (ref.current)
      setComputedStyle(getComputedStyle(ref.current));
  }, [
    ref
  ]);
  return (0, import_react113.createElement)($57acba87d6e25586$var$ScrollAreaScrollbarImpl, _extends({
    "data-orientation": "vertical"
  }, scrollbarProps, {
    ref: composeRefs,
    sizes: sizes24,
    style: {
      top: 0,
      right: context.dir === "ltr" ? 0 : void 0,
      left: context.dir === "rtl" ? 0 : void 0,
      bottom: "var(--radix-scroll-area-corner-height)",
      ["--radix-scroll-area-thumb-height"]: $57acba87d6e25586$var$getThumbSize(sizes24) + "px",
      ...props.style
    },
    onThumbPointerDown: (pointerPos) => props.onThumbPointerDown(pointerPos.y),
    onDragScroll: (pointerPos) => props.onDragScroll(pointerPos.y),
    onWheelScroll: (event, maxScrollPos) => {
      if (context.viewport) {
        const scrollPos = context.viewport.scrollTop + event.deltaY;
        props.onWheelScroll(scrollPos);
        if ($57acba87d6e25586$var$isScrollingWithinScrollbarBounds(scrollPos, maxScrollPos))
          event.preventDefault();
      }
    },
    onResize: () => {
      if (ref.current && context.viewport && computedStyle)
        onSizesChange({
          content: context.viewport.scrollHeight,
          viewport: context.viewport.offsetHeight,
          scrollbar: {
            size: ref.current.clientHeight,
            paddingStart: $57acba87d6e25586$var$toInt(computedStyle.paddingTop),
            paddingEnd: $57acba87d6e25586$var$toInt(computedStyle.paddingBottom)
          }
        });
    }
  }));
});
var [$57acba87d6e25586$var$ScrollbarProvider, $57acba87d6e25586$var$useScrollbarContext] = $57acba87d6e25586$var$createScrollAreaContext($57acba87d6e25586$var$SCROLLBAR_NAME);
var $57acba87d6e25586$var$ScrollAreaScrollbarImpl = (0, import_react113.forwardRef)((props, forwardedRef) => {
  const { __scopeScrollArea, sizes: sizes24, hasThumb, onThumbChange, onThumbPointerUp, onThumbPointerDown, onThumbPositionChange, onDragScroll, onWheelScroll, onResize, ...scrollbarProps } = props;
  const context = $57acba87d6e25586$var$useScrollAreaContext($57acba87d6e25586$var$SCROLLBAR_NAME, __scopeScrollArea);
  const [scrollbar, setScrollbar] = (0, import_react113.useState)(null);
  const composeRefs = $6ed0406888f73fc4$export$c7b2cbe3552a0d05(
    forwardedRef,
    (node2) => setScrollbar(node2)
  );
  const rectRef = (0, import_react113.useRef)(null);
  const prevWebkitUserSelectRef = (0, import_react113.useRef)("");
  const viewport2 = context.viewport;
  const maxScrollPos = sizes24.content - sizes24.viewport;
  const handleWheelScroll = $b1b2314f5f9a1d84$export$25bec8c6f54ee79a(onWheelScroll);
  const handleThumbPositionChange = $b1b2314f5f9a1d84$export$25bec8c6f54ee79a(onThumbPositionChange);
  const handleResize = $57acba87d6e25586$var$useDebounceCallback(onResize, 10);
  function handleDragScroll(event) {
    if (rectRef.current) {
      const x = event.clientX - rectRef.current.left;
      const y = event.clientY - rectRef.current.top;
      onDragScroll({
        x,
        y
      });
    }
  }
  (0, import_react113.useEffect)(() => {
    const handleWheel = (event) => {
      const element = event.target;
      const isScrollbarWheel = scrollbar === null || scrollbar === void 0 ? void 0 : scrollbar.contains(element);
      if (isScrollbarWheel)
        handleWheelScroll(event, maxScrollPos);
    };
    document.addEventListener("wheel", handleWheel, {
      passive: false
    });
    return () => document.removeEventListener("wheel", handleWheel, {
      passive: false
    });
  }, [
    viewport2,
    scrollbar,
    maxScrollPos,
    handleWheelScroll
  ]);
  (0, import_react113.useEffect)(handleThumbPositionChange, [
    sizes24,
    handleThumbPositionChange
  ]);
  $57acba87d6e25586$var$useResizeObserver(scrollbar, handleResize);
  $57acba87d6e25586$var$useResizeObserver(context.content, handleResize);
  return (0, import_react113.createElement)($57acba87d6e25586$var$ScrollbarProvider, {
    scope: __scopeScrollArea,
    scrollbar,
    hasThumb,
    onThumbChange: $b1b2314f5f9a1d84$export$25bec8c6f54ee79a(onThumbChange),
    onThumbPointerUp: $b1b2314f5f9a1d84$export$25bec8c6f54ee79a(onThumbPointerUp),
    onThumbPositionChange: handleThumbPositionChange,
    onThumbPointerDown: $b1b2314f5f9a1d84$export$25bec8c6f54ee79a(onThumbPointerDown)
  }, (0, import_react113.createElement)($8927f6f2acc4f386$export$250ffa63cdc0d034.div, _extends({}, scrollbarProps, {
    ref: composeRefs,
    style: {
      position: "absolute",
      ...scrollbarProps.style
    },
    onPointerDown: $e42e1063c40fb3ef$export$b9ecd428b558ff10(props.onPointerDown, (event) => {
      const mainPointer = 0;
      if (event.button === mainPointer) {
        const element = event.target;
        element.setPointerCapture(event.pointerId);
        rectRef.current = scrollbar.getBoundingClientRect();
        prevWebkitUserSelectRef.current = document.body.style.webkitUserSelect;
        document.body.style.webkitUserSelect = "none";
        handleDragScroll(event);
      }
    }),
    onPointerMove: $e42e1063c40fb3ef$export$b9ecd428b558ff10(props.onPointerMove, handleDragScroll),
    onPointerUp: $e42e1063c40fb3ef$export$b9ecd428b558ff10(props.onPointerUp, (event) => {
      const element = event.target;
      if (element.hasPointerCapture(event.pointerId))
        element.releasePointerCapture(event.pointerId);
      document.body.style.webkitUserSelect = prevWebkitUserSelectRef.current;
      rectRef.current = null;
    })
  })));
});
var $57acba87d6e25586$var$THUMB_NAME = "ScrollAreaThumb";
var $57acba87d6e25586$export$9fba1154677d7cd2 = (0, import_react113.forwardRef)((props, forwardedRef) => {
  const { forceMount, ...thumbProps } = props;
  const scrollbarContext = $57acba87d6e25586$var$useScrollbarContext($57acba87d6e25586$var$THUMB_NAME, props.__scopeScrollArea);
  return (0, import_react113.createElement)($921a889cee6df7e8$export$99c2b779aa4e8b8b, {
    present: forceMount || scrollbarContext.hasThumb
  }, (0, import_react113.createElement)($57acba87d6e25586$var$ScrollAreaThumbImpl, _extends({
    ref: forwardedRef
  }, thumbProps)));
});
var $57acba87d6e25586$var$ScrollAreaThumbImpl = (0, import_react113.forwardRef)((props, forwardedRef) => {
  const { __scopeScrollArea, style, ...thumbProps } = props;
  const scrollAreaContext = $57acba87d6e25586$var$useScrollAreaContext($57acba87d6e25586$var$THUMB_NAME, __scopeScrollArea);
  const scrollbarContext = $57acba87d6e25586$var$useScrollbarContext($57acba87d6e25586$var$THUMB_NAME, __scopeScrollArea);
  const { onThumbPositionChange } = scrollbarContext;
  const composedRef = $6ed0406888f73fc4$export$c7b2cbe3552a0d05(
    forwardedRef,
    (node2) => scrollbarContext.onThumbChange(node2)
  );
  const removeUnlinkedScrollListenerRef = (0, import_react113.useRef)();
  const debounceScrollEnd = $57acba87d6e25586$var$useDebounceCallback(() => {
    if (removeUnlinkedScrollListenerRef.current) {
      removeUnlinkedScrollListenerRef.current();
      removeUnlinkedScrollListenerRef.current = void 0;
    }
  }, 100);
  (0, import_react113.useEffect)(() => {
    const viewport2 = scrollAreaContext.viewport;
    if (viewport2) {
      const handleScroll = () => {
        debounceScrollEnd();
        if (!removeUnlinkedScrollListenerRef.current) {
          const listener = $57acba87d6e25586$var$addUnlinkedScrollListener(viewport2, onThumbPositionChange);
          removeUnlinkedScrollListenerRef.current = listener;
          onThumbPositionChange();
        }
      };
      onThumbPositionChange();
      viewport2.addEventListener("scroll", handleScroll);
      return () => viewport2.removeEventListener("scroll", handleScroll);
    }
  }, [
    scrollAreaContext.viewport,
    debounceScrollEnd,
    onThumbPositionChange
  ]);
  return (0, import_react113.createElement)($8927f6f2acc4f386$export$250ffa63cdc0d034.div, _extends({
    "data-state": scrollbarContext.hasThumb ? "visible" : "hidden"
  }, thumbProps, {
    ref: composedRef,
    style: {
      width: "var(--radix-scroll-area-thumb-width)",
      height: "var(--radix-scroll-area-thumb-height)",
      ...style
    },
    onPointerDownCapture: $e42e1063c40fb3ef$export$b9ecd428b558ff10(props.onPointerDownCapture, (event) => {
      const thumb = event.target;
      const thumbRect = thumb.getBoundingClientRect();
      const x = event.clientX - thumbRect.left;
      const y = event.clientY - thumbRect.top;
      scrollbarContext.onThumbPointerDown({
        x,
        y
      });
    }),
    onPointerUp: $e42e1063c40fb3ef$export$b9ecd428b558ff10(props.onPointerUp, scrollbarContext.onThumbPointerUp)
  }));
});
Object.assign($57acba87d6e25586$export$9fba1154677d7cd2, {
  displayName: $57acba87d6e25586$var$THUMB_NAME
});
var $57acba87d6e25586$var$CORNER_NAME = "ScrollAreaCorner";
var $57acba87d6e25586$export$56969d565df7cc4b = (0, import_react113.forwardRef)((props, forwardedRef) => {
  const context = $57acba87d6e25586$var$useScrollAreaContext($57acba87d6e25586$var$CORNER_NAME, props.__scopeScrollArea);
  const hasBothScrollbarsVisible = Boolean(context.scrollbarX && context.scrollbarY);
  const hasCorner = context.type !== "scroll" && hasBothScrollbarsVisible;
  return hasCorner ? (0, import_react113.createElement)($57acba87d6e25586$var$ScrollAreaCornerImpl, _extends({}, props, {
    ref: forwardedRef
  })) : null;
});
Object.assign($57acba87d6e25586$export$56969d565df7cc4b, {
  displayName: $57acba87d6e25586$var$CORNER_NAME
});
var $57acba87d6e25586$var$ScrollAreaCornerImpl = (0, import_react113.forwardRef)((props, forwardedRef) => {
  const { __scopeScrollArea, ...cornerProps } = props;
  const context = $57acba87d6e25586$var$useScrollAreaContext($57acba87d6e25586$var$CORNER_NAME, __scopeScrollArea);
  const [width1, setWidth] = (0, import_react113.useState)(0);
  const [height1, setHeight] = (0, import_react113.useState)(0);
  const hasSize = Boolean(width1 && height1);
  $57acba87d6e25586$var$useResizeObserver(context.scrollbarX, () => {
    var _context$scrollbarX;
    const height = ((_context$scrollbarX = context.scrollbarX) === null || _context$scrollbarX === void 0 ? void 0 : _context$scrollbarX.offsetHeight) || 0;
    context.onCornerHeightChange(height);
    setHeight(height);
  });
  $57acba87d6e25586$var$useResizeObserver(context.scrollbarY, () => {
    var _context$scrollbarY;
    const width = ((_context$scrollbarY = context.scrollbarY) === null || _context$scrollbarY === void 0 ? void 0 : _context$scrollbarY.offsetWidth) || 0;
    context.onCornerWidthChange(width);
    setWidth(width);
  });
  return hasSize ? (0, import_react113.createElement)($8927f6f2acc4f386$export$250ffa63cdc0d034.div, _extends({}, cornerProps, {
    ref: forwardedRef,
    style: {
      width: width1,
      height: height1,
      position: "absolute",
      right: context.dir === "ltr" ? 0 : void 0,
      left: context.dir === "rtl" ? 0 : void 0,
      bottom: 0,
      ...props.style
    }
  })) : null;
});
function $57acba87d6e25586$var$toInt(value) {
  return value ? parseInt(value, 10) : 0;
}
function $57acba87d6e25586$var$getThumbRatio(viewportSize, contentSize) {
  const ratio = viewportSize / contentSize;
  return isNaN(ratio) ? 0 : ratio;
}
function $57acba87d6e25586$var$getThumbSize(sizes24) {
  const ratio = $57acba87d6e25586$var$getThumbRatio(sizes24.viewport, sizes24.content);
  const scrollbarPadding = sizes24.scrollbar.paddingStart + sizes24.scrollbar.paddingEnd;
  const thumbSize = (sizes24.scrollbar.size - scrollbarPadding) * ratio;
  return Math.max(thumbSize, 18);
}
function $57acba87d6e25586$var$getScrollPositionFromPointer(pointerPos, pointerOffset, sizes24, dir = "ltr") {
  const thumbSizePx = $57acba87d6e25586$var$getThumbSize(sizes24);
  const thumbCenter = thumbSizePx / 2;
  const offset3 = pointerOffset || thumbCenter;
  const thumbOffsetFromEnd = thumbSizePx - offset3;
  const minPointerPos = sizes24.scrollbar.paddingStart + offset3;
  const maxPointerPos = sizes24.scrollbar.size - sizes24.scrollbar.paddingEnd - thumbOffsetFromEnd;
  const maxScrollPos = sizes24.content - sizes24.viewport;
  const scrollRange = dir === "ltr" ? [
    0,
    maxScrollPos
  ] : [
    maxScrollPos * -1,
    0
  ];
  const interpolate = $57acba87d6e25586$var$linearScale([
    minPointerPos,
    maxPointerPos
  ], scrollRange);
  return interpolate(pointerPos);
}
function $57acba87d6e25586$var$getThumbOffsetFromScroll(scrollPos, sizes24, dir = "ltr") {
  const thumbSizePx = $57acba87d6e25586$var$getThumbSize(sizes24);
  const scrollbarPadding = sizes24.scrollbar.paddingStart + sizes24.scrollbar.paddingEnd;
  const scrollbar = sizes24.scrollbar.size - scrollbarPadding;
  const maxScrollPos = sizes24.content - sizes24.viewport;
  const maxThumbPos = scrollbar - thumbSizePx;
  const scrollClampRange = dir === "ltr" ? [
    0,
    maxScrollPos
  ] : [
    maxScrollPos * -1,
    0
  ];
  const scrollWithoutMomentum = $ae6933e535247d3d$export$7d15b64cf5a3a4c4(scrollPos, scrollClampRange);
  const interpolate = $57acba87d6e25586$var$linearScale([
    0,
    maxScrollPos
  ], [
    0,
    maxThumbPos
  ]);
  return interpolate(scrollWithoutMomentum);
}
function $57acba87d6e25586$var$linearScale(input, output) {
  return (value) => {
    if (input[0] === input[1] || output[0] === output[1])
      return output[0];
    const ratio = (output[1] - output[0]) / (input[1] - input[0]);
    return output[0] + ratio * (value - input[0]);
  };
}
function $57acba87d6e25586$var$isScrollingWithinScrollbarBounds(scrollPos, maxScrollPos) {
  return scrollPos > 0 && scrollPos < maxScrollPos;
}
var $57acba87d6e25586$var$addUnlinkedScrollListener = (node2, handler = () => {
}) => {
  let prevPosition = {
    left: node2.scrollLeft,
    top: node2.scrollTop
  };
  let rAF = 0;
  (function loop() {
    const position2 = {
      left: node2.scrollLeft,
      top: node2.scrollTop
    };
    const isHorizontalScroll = prevPosition.left !== position2.left;
    const isVerticalScroll = prevPosition.top !== position2.top;
    if (isHorizontalScroll || isVerticalScroll)
      handler();
    prevPosition = position2;
    rAF = window.requestAnimationFrame(loop);
  })();
  return () => window.cancelAnimationFrame(rAF);
};
function $57acba87d6e25586$var$useDebounceCallback(callback, delay) {
  const handleCallback = $b1b2314f5f9a1d84$export$25bec8c6f54ee79a(callback);
  const debounceTimerRef = (0, import_react113.useRef)(0);
  (0, import_react113.useEffect)(
    () => () => window.clearTimeout(debounceTimerRef.current),
    []
  );
  return (0, import_react113.useCallback)(() => {
    window.clearTimeout(debounceTimerRef.current);
    debounceTimerRef.current = window.setTimeout(handleCallback, delay);
  }, [
    handleCallback,
    delay
  ]);
}
function $57acba87d6e25586$var$useResizeObserver(element, onResize) {
  const handleResize = $b1b2314f5f9a1d84$export$25bec8c6f54ee79a(onResize);
  $9f79659886946c16$export$e5c5a5f917a5871c(() => {
    let rAF = 0;
    if (element) {
      const resizeObserver = new ResizeObserver(() => {
        cancelAnimationFrame(rAF);
        rAF = window.requestAnimationFrame(handleResize);
      });
      resizeObserver.observe(element);
      return () => {
        window.cancelAnimationFrame(rAF);
        resizeObserver.unobserve(element);
      };
    }
  }, [
    element,
    handleResize
  ]);
}
var $57acba87d6e25586$export$be92b6f5f03c0fe9 = $57acba87d6e25586$export$ccf8d8d7bbf3c2cc;
var $57acba87d6e25586$export$d5c6c08dc2d3ca7 = $57acba87d6e25586$export$a21cbf9f11fca853;
var $57acba87d6e25586$export$9a4e88b92edfce6b = $57acba87d6e25586$export$2fabd85d0eba3c57;
var $57acba87d6e25586$export$6521433ed15a34db = $57acba87d6e25586$export$9fba1154677d7cd2;
var $57acba87d6e25586$export$ac61190d9fc311a9 = $57acba87d6e25586$export$56969d565df7cc4b;

// node_modules/@mantine/core/esm/ScrollArea/ScrollArea.styles.js
var useStyles15 = createStyles((theme, { scrollbarSize, offsetScrollbars, scrollbarHovered, hidden: hidden2 }, getRef2) => ({
  root: {
    overflow: "hidden"
  },
  viewport: {
    width: "100%",
    height: "100%",
    paddingRight: offsetScrollbars ? scrollbarSize : void 0,
    paddingBottom: offsetScrollbars ? scrollbarSize : void 0
  },
  scrollbar: {
    display: hidden2 ? "none" : "flex",
    userSelect: "none",
    touchAction: "none",
    boxSizing: "border-box",
    padding: scrollbarSize / 5,
    transition: "background-color 150ms ease, opacity 150ms ease",
    "&:hover": {
      backgroundColor: theme.colorScheme === "dark" ? theme.colors.dark[8] : theme.colors.gray[0],
      [`& .${getRef2("thumb")}`]: {
        backgroundColor: theme.colorScheme === "dark" ? theme.fn.rgba(theme.white, 0.5) : theme.fn.rgba(theme.black, 0.5)
      }
    },
    '&[data-orientation="vertical"]': {
      width: scrollbarSize
    },
    '&[data-orientation="horizontal"]': {
      flexDirection: "column",
      height: scrollbarSize
    },
    '&[data-state="hidden"]': {
      display: "none",
      opacity: 0
    }
  },
  thumb: {
    ref: getRef2("thumb"),
    flex: 1,
    backgroundColor: theme.colorScheme === "dark" ? theme.fn.rgba(theme.white, 0.4) : theme.fn.rgba(theme.black, 0.4),
    borderRadius: scrollbarSize,
    position: "relative",
    transition: "background-color 150ms ease",
    display: hidden2 ? "none" : void 0,
    overflow: "hidden",
    "&::before": {
      content: '""',
      position: "absolute",
      top: "50%",
      left: "50%",
      transform: "translate(-50%, -50%)",
      width: "100%",
      height: "100%",
      minWidth: 44,
      minHeight: 44
    }
  },
  corner: {
    backgroundColor: theme.colorScheme === "dark" ? theme.colors.dark[6] : theme.colors.gray[0],
    transition: "opacity 150ms ease",
    opacity: scrollbarHovered ? 1 : 0,
    display: hidden2 ? "none" : void 0
  }
}));
var ScrollArea_styles_default = useStyles15;

// node_modules/@mantine/core/esm/ScrollArea/ScrollArea.js
var __defProp53 = Object.defineProperty;
var __defProps21 = Object.defineProperties;
var __getOwnPropDescs21 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols54 = Object.getOwnPropertySymbols;
var __hasOwnProp54 = Object.prototype.hasOwnProperty;
var __propIsEnum54 = Object.prototype.propertyIsEnumerable;
var __defNormalProp53 = (obj, key, value) => key in obj ? __defProp53(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues53 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp54.call(b2, prop))
      __defNormalProp53(a2, prop, b2[prop]);
  if (__getOwnPropSymbols54)
    for (var prop of __getOwnPropSymbols54(b2)) {
      if (__propIsEnum54.call(b2, prop))
        __defNormalProp53(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps21 = (a2, b2) => __defProps21(a2, __getOwnPropDescs21(b2));
var __objRest29 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp54.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols54)
    for (var prop of __getOwnPropSymbols54(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum54.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps19 = {
  scrollbarSize: 12,
  scrollHideDelay: 1e3,
  type: "hover",
  offsetScrollbars: false
};
var _ScrollArea = (0, import_react114.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("ScrollArea", defaultProps19, props), {
    children,
    className,
    classNames,
    styles: styles2,
    scrollbarSize,
    scrollHideDelay,
    type,
    dir,
    offsetScrollbars,
    viewportRef,
    onScrollPositionChange,
    unstyled,
    viewportProps
  } = _a, others = __objRest29(_a, [
    "children",
    "className",
    "classNames",
    "styles",
    "scrollbarSize",
    "scrollHideDelay",
    "type",
    "dir",
    "offsetScrollbars",
    "viewportRef",
    "onScrollPositionChange",
    "unstyled",
    "viewportProps"
  ]);
  const [scrollbarHovered, setScrollbarHovered] = (0, import_react114.useState)(false);
  const theme = useMantineTheme();
  const { classes, cx } = ScrollArea_styles_default({ scrollbarSize, offsetScrollbars, scrollbarHovered, hidden: type === "never" }, { name: "ScrollArea", classNames, styles: styles2, unstyled });
  return import_react114.default.createElement($57acba87d6e25586$export$be92b6f5f03c0fe9, {
    type: type === "never" ? "always" : type,
    scrollHideDelay,
    dir: dir || theme.dir,
    ref,
    asChild: true
  }, import_react114.default.createElement(Box, __spreadValues53({
    className: cx(classes.root, className)
  }, others), import_react114.default.createElement($57acba87d6e25586$export$d5c6c08dc2d3ca7, __spreadProps21(__spreadValues53({}, viewportProps), {
    className: classes.viewport,
    ref: viewportRef,
    onScroll: typeof onScrollPositionChange === "function" ? ({ currentTarget }) => onScrollPositionChange({
      x: currentTarget.scrollLeft,
      y: currentTarget.scrollTop
    }) : void 0
  }), children), import_react114.default.createElement($57acba87d6e25586$export$9a4e88b92edfce6b, {
    orientation: "horizontal",
    className: classes.scrollbar,
    forceMount: true,
    onMouseEnter: () => setScrollbarHovered(true),
    onMouseLeave: () => setScrollbarHovered(false)
  }, import_react114.default.createElement($57acba87d6e25586$export$6521433ed15a34db, {
    className: classes.thumb
  })), import_react114.default.createElement($57acba87d6e25586$export$9a4e88b92edfce6b, {
    orientation: "vertical",
    className: classes.scrollbar,
    forceMount: true,
    onMouseEnter: () => setScrollbarHovered(true),
    onMouseLeave: () => setScrollbarHovered(false)
  }, import_react114.default.createElement($57acba87d6e25586$export$6521433ed15a34db, {
    className: classes.thumb
  })), import_react114.default.createElement($57acba87d6e25586$export$ac61190d9fc311a9, {
    className: classes.corner
  })));
});
var ScrollAreaAutosize = (0, import_react114.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("ScrollAreaAutosize", defaultProps19, props), {
    maxHeight,
    children,
    classNames,
    styles: styles2,
    scrollbarSize,
    scrollHideDelay,
    type,
    dir,
    offsetScrollbars,
    viewportRef,
    onScrollPositionChange,
    unstyled,
    sx
  } = _a, others = __objRest29(_a, [
    "maxHeight",
    "children",
    "classNames",
    "styles",
    "scrollbarSize",
    "scrollHideDelay",
    "type",
    "dir",
    "offsetScrollbars",
    "viewportRef",
    "onScrollPositionChange",
    "unstyled",
    "sx"
  ]);
  return import_react114.default.createElement(Box, __spreadProps21(__spreadValues53({}, others), {
    ref,
    sx: [{ display: "flex", maxHeight }, ...packSx(sx)]
  }), import_react114.default.createElement(Box, {
    sx: { display: "flex", flexDirection: "column", flex: 1 }
  }, import_react114.default.createElement(_ScrollArea, {
    classNames,
    styles: styles2,
    scrollHideDelay,
    scrollbarSize,
    type,
    dir,
    offsetScrollbars,
    viewportRef,
    onScrollPositionChange,
    unstyled
  }, children)));
});
ScrollAreaAutosize.displayName = "@mantine/core/ScrollAreaAutosize";
_ScrollArea.displayName = "@mantine/core/ScrollArea";
_ScrollArea.Autosize = ScrollAreaAutosize;
var ScrollArea = _ScrollArea;

// node_modules/@mantine/core/esm/Select/SelectScrollArea/SelectScrollArea.js
var __defProp54 = Object.defineProperty;
var __defProps22 = Object.defineProperties;
var __getOwnPropDescs22 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols55 = Object.getOwnPropertySymbols;
var __hasOwnProp55 = Object.prototype.hasOwnProperty;
var __propIsEnum55 = Object.prototype.propertyIsEnumerable;
var __defNormalProp54 = (obj, key, value) => key in obj ? __defProp54(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues54 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp55.call(b2, prop))
      __defNormalProp54(a2, prop, b2[prop]);
  if (__getOwnPropSymbols55)
    for (var prop of __getOwnPropSymbols55(b2)) {
      if (__propIsEnum55.call(b2, prop))
        __defNormalProp54(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps22 = (a2, b2) => __defProps22(a2, __getOwnPropDescs22(b2));
var __objRest30 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp55.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols55)
    for (var prop of __getOwnPropSymbols55(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum55.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var SelectScrollArea = (0, import_react115.forwardRef)((_a, ref) => {
  var _b = _a, { style } = _b, others = __objRest30(_b, ["style"]);
  return import_react115.default.createElement(ScrollArea, __spreadProps22(__spreadValues54({}, others), {
    style: __spreadValues54({ width: "100%" }, style),
    viewportRef: ref
  }), others.children);
});
SelectScrollArea.displayName = "@mantine/core/SelectScrollArea";

// node_modules/@mantine/core/esm/Select/SelectPopover/SelectPopover.styles.js
var useStyles16 = createStyles(() => ({
  dropdown: {},
  itemsWrapper: {
    padding: 4,
    display: "flex",
    width: "100%",
    boxSizing: "border-box"
  }
}));
var SelectPopover_styles_default = useStyles16;

// node_modules/@mantine/core/esm/Popover/Popover.js
var import_react127 = __toESM(require_react());

// node_modules/@floating-ui/react/dist/floating-ui.react.esm.js
var React50 = __toESM(require_react());
var import_react117 = __toESM(require_react());

// node_modules/tabbable/dist/index.esm.js
var candidateSelectors = ["input:not([inert])", "select:not([inert])", "textarea:not([inert])", "a[href]:not([inert])", "button:not([inert])", "[tabindex]:not(slot):not([inert])", "audio[controls]:not([inert])", "video[controls]:not([inert])", '[contenteditable]:not([contenteditable="false"]):not([inert])', "details>summary:first-of-type:not([inert])", "details:not([inert])"];
var candidateSelector = candidateSelectors.join(",");
var NoElement = typeof Element === "undefined";
var matches = NoElement ? function() {
} : Element.prototype.matches || Element.prototype.msMatchesSelector || Element.prototype.webkitMatchesSelector;
var getRootNode = !NoElement && Element.prototype.getRootNode ? function(element) {
  var _element$getRootNode;
  return element === null || element === void 0 ? void 0 : (_element$getRootNode = element.getRootNode) === null || _element$getRootNode === void 0 ? void 0 : _element$getRootNode.call(element);
} : function(element) {
  return element === null || element === void 0 ? void 0 : element.ownerDocument;
};
var focusableCandidateSelector = candidateSelectors.concat("iframe").join(",");

// node_modules/@floating-ui/react/dist/floating-ui.react.esm.js
var import_react_dom5 = __toESM(require_react_dom());

// node_modules/@floating-ui/utils/dist/floating-ui.utils.mjs
var sides = ["top", "right", "bottom", "left"];
var alignments = ["start", "end"];
var placements = sides.reduce((acc, side) => acc.concat(side, side + "-" + alignments[0], side + "-" + alignments[1]), []);
var min = Math.min;
var max = Math.max;
var round = Math.round;
var floor = Math.floor;
var createCoords = (v) => ({
  x: v,
  y: v
});
var oppositeSideMap = {
  left: "right",
  right: "left",
  bottom: "top",
  top: "bottom"
};
var oppositeAlignmentMap = {
  start: "end",
  end: "start"
};
function clamp2(start2, value, end2) {
  return max(start2, min(value, end2));
}
function evaluate(value, param) {
  return typeof value === "function" ? value(param) : value;
}
function getSide(placement) {
  return placement.split("-")[0];
}
function getAlignment(placement) {
  return placement.split("-")[1];
}
function getOppositeAxis(axis) {
  return axis === "x" ? "y" : "x";
}
function getAxisLength(axis) {
  return axis === "y" ? "height" : "width";
}
function getSideAxis(placement) {
  return ["top", "bottom"].includes(getSide(placement)) ? "y" : "x";
}
function getAlignmentAxis(placement) {
  return getOppositeAxis(getSideAxis(placement));
}
function getAlignmentSides(placement, rects, rtl) {
  if (rtl === void 0) {
    rtl = false;
  }
  const alignment = getAlignment(placement);
  const alignmentAxis = getAlignmentAxis(placement);
  const length2 = getAxisLength(alignmentAxis);
  let mainAlignmentSide = alignmentAxis === "x" ? alignment === (rtl ? "end" : "start") ? "right" : "left" : alignment === "start" ? "bottom" : "top";
  if (rects.reference[length2] > rects.floating[length2]) {
    mainAlignmentSide = getOppositePlacement(mainAlignmentSide);
  }
  return [mainAlignmentSide, getOppositePlacement(mainAlignmentSide)];
}
function getExpandedPlacements(placement) {
  const oppositePlacement = getOppositePlacement(placement);
  return [getOppositeAlignmentPlacement(placement), oppositePlacement, getOppositeAlignmentPlacement(oppositePlacement)];
}
function getOppositeAlignmentPlacement(placement) {
  return placement.replace(/start|end/g, (alignment) => oppositeAlignmentMap[alignment]);
}
function getSideList(side, isStart, rtl) {
  const lr = ["left", "right"];
  const rl = ["right", "left"];
  const tb = ["top", "bottom"];
  const bt = ["bottom", "top"];
  switch (side) {
    case "top":
    case "bottom":
      if (rtl)
        return isStart ? rl : lr;
      return isStart ? lr : rl;
    case "left":
    case "right":
      return isStart ? tb : bt;
    default:
      return [];
  }
}
function getOppositeAxisPlacements(placement, flipAlignment, direction, rtl) {
  const alignment = getAlignment(placement);
  let list = getSideList(getSide(placement), direction === "start", rtl);
  if (alignment) {
    list = list.map((side) => side + "-" + alignment);
    if (flipAlignment) {
      list = list.concat(list.map(getOppositeAlignmentPlacement));
    }
  }
  return list;
}
function getOppositePlacement(placement) {
  return placement.replace(/left|right|bottom|top/g, (side) => oppositeSideMap[side]);
}
function expandPaddingObject(padding2) {
  return {
    top: 0,
    right: 0,
    bottom: 0,
    left: 0,
    ...padding2
  };
}
function getPaddingObject(padding2) {
  return typeof padding2 !== "number" ? expandPaddingObject(padding2) : {
    top: padding2,
    right: padding2,
    bottom: padding2,
    left: padding2
  };
}
function rectToClientRect(rect) {
  return {
    ...rect,
    top: rect.y,
    left: rect.x,
    right: rect.x + rect.width,
    bottom: rect.y + rect.height
  };
}

// node_modules/@floating-ui/core/dist/floating-ui.core.mjs
function computeCoordsFromPlacement(_ref, placement, rtl) {
  let {
    reference: reference2,
    floating
  } = _ref;
  const sideAxis = getSideAxis(placement);
  const alignmentAxis = getAlignmentAxis(placement);
  const alignLength = getAxisLength(alignmentAxis);
  const side = getSide(placement);
  const isVertical = sideAxis === "y";
  const commonX = reference2.x + reference2.width / 2 - floating.width / 2;
  const commonY = reference2.y + reference2.height / 2 - floating.height / 2;
  const commonAlign = reference2[alignLength] / 2 - floating[alignLength] / 2;
  let coords;
  switch (side) {
    case "top":
      coords = {
        x: commonX,
        y: reference2.y - floating.height
      };
      break;
    case "bottom":
      coords = {
        x: commonX,
        y: reference2.y + reference2.height
      };
      break;
    case "right":
      coords = {
        x: reference2.x + reference2.width,
        y: commonY
      };
      break;
    case "left":
      coords = {
        x: reference2.x - floating.width,
        y: commonY
      };
      break;
    default:
      coords = {
        x: reference2.x,
        y: reference2.y
      };
  }
  switch (getAlignment(placement)) {
    case "start":
      coords[alignmentAxis] -= commonAlign * (rtl && isVertical ? -1 : 1);
      break;
    case "end":
      coords[alignmentAxis] += commonAlign * (rtl && isVertical ? -1 : 1);
      break;
  }
  return coords;
}
var computePosition = async (reference2, floating, config) => {
  const {
    placement = "bottom",
    strategy = "absolute",
    middleware: middleware2 = [],
    platform: platform2
  } = config;
  const validMiddleware = middleware2.filter(Boolean);
  const rtl = await (platform2.isRTL == null ? void 0 : platform2.isRTL(floating));
  let rects = await platform2.getElementRects({
    reference: reference2,
    floating,
    strategy
  });
  let {
    x,
    y
  } = computeCoordsFromPlacement(rects, placement, rtl);
  let statefulPlacement = placement;
  let middlewareData = {};
  let resetCount = 0;
  for (let i = 0; i < validMiddleware.length; i++) {
    const {
      name,
      fn: fn5
    } = validMiddleware[i];
    const {
      x: nextX,
      y: nextY,
      data,
      reset
    } = await fn5({
      x,
      y,
      initialPlacement: placement,
      placement: statefulPlacement,
      strategy,
      middlewareData,
      rects,
      platform: platform2,
      elements: {
        reference: reference2,
        floating
      }
    });
    x = nextX != null ? nextX : x;
    y = nextY != null ? nextY : y;
    middlewareData = {
      ...middlewareData,
      [name]: {
        ...middlewareData[name],
        ...data
      }
    };
    if (reset && resetCount <= 50) {
      resetCount++;
      if (typeof reset === "object") {
        if (reset.placement) {
          statefulPlacement = reset.placement;
        }
        if (reset.rects) {
          rects = reset.rects === true ? await platform2.getElementRects({
            reference: reference2,
            floating,
            strategy
          }) : reset.rects;
        }
        ({
          x,
          y
        } = computeCoordsFromPlacement(rects, statefulPlacement, rtl));
      }
      i = -1;
      continue;
    }
  }
  return {
    x,
    y,
    placement: statefulPlacement,
    strategy,
    middlewareData
  };
};
async function detectOverflow(state, options) {
  var _await$platform$isEle;
  if (options === void 0) {
    options = {};
  }
  const {
    x,
    y,
    platform: platform2,
    rects,
    elements,
    strategy
  } = state;
  const {
    boundary = "clippingAncestors",
    rootBoundary = "viewport",
    elementContext = "floating",
    altBoundary = false,
    padding: padding2 = 0
  } = evaluate(options, state);
  const paddingObject = getPaddingObject(padding2);
  const altContext = elementContext === "floating" ? "reference" : "floating";
  const element = elements[altBoundary ? altContext : elementContext];
  const clippingClientRect = rectToClientRect(await platform2.getClippingRect({
    element: ((_await$platform$isEle = await (platform2.isElement == null ? void 0 : platform2.isElement(element))) != null ? _await$platform$isEle : true) ? element : element.contextElement || await (platform2.getDocumentElement == null ? void 0 : platform2.getDocumentElement(elements.floating)),
    boundary,
    rootBoundary,
    strategy
  }));
  const rect = elementContext === "floating" ? {
    ...rects.floating,
    x,
    y
  } : rects.reference;
  const offsetParent = await (platform2.getOffsetParent == null ? void 0 : platform2.getOffsetParent(elements.floating));
  const offsetScale = await (platform2.isElement == null ? void 0 : platform2.isElement(offsetParent)) ? await (platform2.getScale == null ? void 0 : platform2.getScale(offsetParent)) || {
    x: 1,
    y: 1
  } : {
    x: 1,
    y: 1
  };
  const elementClientRect = rectToClientRect(platform2.convertOffsetParentRelativeRectToViewportRelativeRect ? await platform2.convertOffsetParentRelativeRectToViewportRelativeRect({
    rect,
    offsetParent,
    strategy
  }) : rect);
  return {
    top: (clippingClientRect.top - elementClientRect.top + paddingObject.top) / offsetScale.y,
    bottom: (elementClientRect.bottom - clippingClientRect.bottom + paddingObject.bottom) / offsetScale.y,
    left: (clippingClientRect.left - elementClientRect.left + paddingObject.left) / offsetScale.x,
    right: (elementClientRect.right - clippingClientRect.right + paddingObject.right) / offsetScale.x
  };
}
var arrow = (options) => ({
  name: "arrow",
  options,
  async fn(state) {
    const {
      x,
      y,
      placement,
      rects,
      platform: platform2,
      elements,
      middlewareData
    } = state;
    const {
      element,
      padding: padding2 = 0
    } = evaluate(options, state) || {};
    if (element == null) {
      return {};
    }
    const paddingObject = getPaddingObject(padding2);
    const coords = {
      x,
      y
    };
    const axis = getAlignmentAxis(placement);
    const length2 = getAxisLength(axis);
    const arrowDimensions = await platform2.getDimensions(element);
    const isYAxis = axis === "y";
    const minProp = isYAxis ? "top" : "left";
    const maxProp = isYAxis ? "bottom" : "right";
    const clientProp = isYAxis ? "clientHeight" : "clientWidth";
    const endDiff = rects.reference[length2] + rects.reference[axis] - coords[axis] - rects.floating[length2];
    const startDiff = coords[axis] - rects.reference[axis];
    const arrowOffsetParent = await (platform2.getOffsetParent == null ? void 0 : platform2.getOffsetParent(element));
    let clientSize = arrowOffsetParent ? arrowOffsetParent[clientProp] : 0;
    if (!clientSize || !await (platform2.isElement == null ? void 0 : platform2.isElement(arrowOffsetParent))) {
      clientSize = elements.floating[clientProp] || rects.floating[length2];
    }
    const centerToReference = endDiff / 2 - startDiff / 2;
    const largestPossiblePadding = clientSize / 2 - arrowDimensions[length2] / 2 - 1;
    const minPadding = min(paddingObject[minProp], largestPossiblePadding);
    const maxPadding = min(paddingObject[maxProp], largestPossiblePadding);
    const min$1 = minPadding;
    const max3 = clientSize - arrowDimensions[length2] - maxPadding;
    const center = clientSize / 2 - arrowDimensions[length2] / 2 + centerToReference;
    const offset3 = clamp2(min$1, center, max3);
    const shouldAddOffset = !middlewareData.arrow && getAlignment(placement) != null && center != offset3 && rects.reference[length2] / 2 - (center < min$1 ? minPadding : maxPadding) - arrowDimensions[length2] / 2 < 0;
    const alignmentOffset = shouldAddOffset ? center < min$1 ? center - min$1 : center - max3 : 0;
    return {
      [axis]: coords[axis] + alignmentOffset,
      data: {
        [axis]: offset3,
        centerOffset: center - offset3 - alignmentOffset,
        ...shouldAddOffset && {
          alignmentOffset
        }
      },
      reset: shouldAddOffset
    };
  }
});
var flip = function(options) {
  if (options === void 0) {
    options = {};
  }
  return {
    name: "flip",
    options,
    async fn(state) {
      var _middlewareData$arrow, _middlewareData$flip;
      const {
        placement,
        middlewareData,
        rects,
        initialPlacement,
        platform: platform2,
        elements
      } = state;
      const {
        mainAxis: checkMainAxis = true,
        crossAxis: checkCrossAxis = true,
        fallbackPlacements: specifiedFallbackPlacements,
        fallbackStrategy = "bestFit",
        fallbackAxisSideDirection = "none",
        flipAlignment = true,
        ...detectOverflowOptions
      } = evaluate(options, state);
      if ((_middlewareData$arrow = middlewareData.arrow) != null && _middlewareData$arrow.alignmentOffset) {
        return {};
      }
      const side = getSide(placement);
      const isBasePlacement = getSide(initialPlacement) === initialPlacement;
      const rtl = await (platform2.isRTL == null ? void 0 : platform2.isRTL(elements.floating));
      const fallbackPlacements = specifiedFallbackPlacements || (isBasePlacement || !flipAlignment ? [getOppositePlacement(initialPlacement)] : getExpandedPlacements(initialPlacement));
      if (!specifiedFallbackPlacements && fallbackAxisSideDirection !== "none") {
        fallbackPlacements.push(...getOppositeAxisPlacements(initialPlacement, flipAlignment, fallbackAxisSideDirection, rtl));
      }
      const placements3 = [initialPlacement, ...fallbackPlacements];
      const overflow = await detectOverflow(state, detectOverflowOptions);
      const overflows = [];
      let overflowsData = ((_middlewareData$flip = middlewareData.flip) == null ? void 0 : _middlewareData$flip.overflows) || [];
      if (checkMainAxis) {
        overflows.push(overflow[side]);
      }
      if (checkCrossAxis) {
        const sides2 = getAlignmentSides(placement, rects, rtl);
        overflows.push(overflow[sides2[0]], overflow[sides2[1]]);
      }
      overflowsData = [...overflowsData, {
        placement,
        overflows
      }];
      if (!overflows.every((side2) => side2 <= 0)) {
        var _middlewareData$flip2, _overflowsData$filter;
        const nextIndex = (((_middlewareData$flip2 = middlewareData.flip) == null ? void 0 : _middlewareData$flip2.index) || 0) + 1;
        const nextPlacement = placements3[nextIndex];
        if (nextPlacement) {
          return {
            data: {
              index: nextIndex,
              overflows: overflowsData
            },
            reset: {
              placement: nextPlacement
            }
          };
        }
        let resetPlacement = (_overflowsData$filter = overflowsData.filter((d2) => d2.overflows[0] <= 0).sort((a2, b2) => a2.overflows[1] - b2.overflows[1])[0]) == null ? void 0 : _overflowsData$filter.placement;
        if (!resetPlacement) {
          switch (fallbackStrategy) {
            case "bestFit": {
              var _overflowsData$map$so;
              const placement2 = (_overflowsData$map$so = overflowsData.map((d2) => [d2.placement, d2.overflows.filter((overflow2) => overflow2 > 0).reduce((acc, overflow2) => acc + overflow2, 0)]).sort((a2, b2) => a2[1] - b2[1])[0]) == null ? void 0 : _overflowsData$map$so[0];
              if (placement2) {
                resetPlacement = placement2;
              }
              break;
            }
            case "initialPlacement":
              resetPlacement = initialPlacement;
              break;
          }
        }
        if (placement !== resetPlacement) {
          return {
            reset: {
              placement: resetPlacement
            }
          };
        }
      }
      return {};
    }
  };
};
function getBoundingRect(rects) {
  const minX = min(...rects.map((rect) => rect.left));
  const minY = min(...rects.map((rect) => rect.top));
  const maxX = max(...rects.map((rect) => rect.right));
  const maxY = max(...rects.map((rect) => rect.bottom));
  return {
    x: minX,
    y: minY,
    width: maxX - minX,
    height: maxY - minY
  };
}
function getRectsByLine(rects) {
  const sortedRects = rects.slice().sort((a2, b2) => a2.y - b2.y);
  const groups = [];
  let prevRect = null;
  for (let i = 0; i < sortedRects.length; i++) {
    const rect = sortedRects[i];
    if (!prevRect || rect.y - prevRect.y > prevRect.height / 2) {
      groups.push([rect]);
    } else {
      groups[groups.length - 1].push(rect);
    }
    prevRect = rect;
  }
  return groups.map((rect) => rectToClientRect(getBoundingRect(rect)));
}
var inline = function(options) {
  if (options === void 0) {
    options = {};
  }
  return {
    name: "inline",
    options,
    async fn(state) {
      const {
        placement,
        elements,
        rects,
        platform: platform2,
        strategy
      } = state;
      const {
        padding: padding2 = 2,
        x,
        y
      } = evaluate(options, state);
      const nativeClientRects = Array.from(await (platform2.getClientRects == null ? void 0 : platform2.getClientRects(elements.reference)) || []);
      const clientRects = getRectsByLine(nativeClientRects);
      const fallback = rectToClientRect(getBoundingRect(nativeClientRects));
      const paddingObject = getPaddingObject(padding2);
      function getBoundingClientRect3() {
        if (clientRects.length === 2 && clientRects[0].left > clientRects[1].right && x != null && y != null) {
          return clientRects.find((rect) => x > rect.left - paddingObject.left && x < rect.right + paddingObject.right && y > rect.top - paddingObject.top && y < rect.bottom + paddingObject.bottom) || fallback;
        }
        if (clientRects.length >= 2) {
          if (getSideAxis(placement) === "y") {
            const firstRect = clientRects[0];
            const lastRect = clientRects[clientRects.length - 1];
            const isTop = getSide(placement) === "top";
            const top3 = firstRect.top;
            const bottom3 = lastRect.bottom;
            const left3 = isTop ? firstRect.left : lastRect.left;
            const right3 = isTop ? firstRect.right : lastRect.right;
            const width2 = right3 - left3;
            const height2 = bottom3 - top3;
            return {
              top: top3,
              bottom: bottom3,
              left: left3,
              right: right3,
              width: width2,
              height: height2,
              x: left3,
              y: top3
            };
          }
          const isLeftSide = getSide(placement) === "left";
          const maxRight = max(...clientRects.map((rect) => rect.right));
          const minLeft = min(...clientRects.map((rect) => rect.left));
          const measureRects = clientRects.filter((rect) => isLeftSide ? rect.left === minLeft : rect.right === maxRight);
          const top2 = measureRects[0].top;
          const bottom2 = measureRects[measureRects.length - 1].bottom;
          const left2 = minLeft;
          const right2 = maxRight;
          const width = right2 - left2;
          const height = bottom2 - top2;
          return {
            top: top2,
            bottom: bottom2,
            left: left2,
            right: right2,
            width,
            height,
            x: left2,
            y: top2
          };
        }
        return fallback;
      }
      const resetRects = await platform2.getElementRects({
        reference: {
          getBoundingClientRect: getBoundingClientRect3
        },
        floating: elements.floating,
        strategy
      });
      if (rects.reference.x !== resetRects.reference.x || rects.reference.y !== resetRects.reference.y || rects.reference.width !== resetRects.reference.width || rects.reference.height !== resetRects.reference.height) {
        return {
          reset: {
            rects: resetRects
          }
        };
      }
      return {};
    }
  };
};
async function convertValueToCoords(state, options) {
  const {
    placement,
    platform: platform2,
    elements
  } = state;
  const rtl = await (platform2.isRTL == null ? void 0 : platform2.isRTL(elements.floating));
  const side = getSide(placement);
  const alignment = getAlignment(placement);
  const isVertical = getSideAxis(placement) === "y";
  const mainAxisMulti = ["left", "top"].includes(side) ? -1 : 1;
  const crossAxisMulti = rtl && isVertical ? -1 : 1;
  const rawValue = evaluate(options, state);
  let {
    mainAxis,
    crossAxis,
    alignmentAxis
  } = typeof rawValue === "number" ? {
    mainAxis: rawValue,
    crossAxis: 0,
    alignmentAxis: null
  } : {
    mainAxis: 0,
    crossAxis: 0,
    alignmentAxis: null,
    ...rawValue
  };
  if (alignment && typeof alignmentAxis === "number") {
    crossAxis = alignment === "end" ? alignmentAxis * -1 : alignmentAxis;
  }
  return isVertical ? {
    x: crossAxis * crossAxisMulti,
    y: mainAxis * mainAxisMulti
  } : {
    x: mainAxis * mainAxisMulti,
    y: crossAxis * crossAxisMulti
  };
}
var offset = function(options) {
  if (options === void 0) {
    options = 0;
  }
  return {
    name: "offset",
    options,
    async fn(state) {
      const {
        x,
        y
      } = state;
      const diffCoords = await convertValueToCoords(state, options);
      return {
        x: x + diffCoords.x,
        y: y + diffCoords.y,
        data: diffCoords
      };
    }
  };
};
var shift = function(options) {
  if (options === void 0) {
    options = {};
  }
  return {
    name: "shift",
    options,
    async fn(state) {
      const {
        x,
        y,
        placement
      } = state;
      const {
        mainAxis: checkMainAxis = true,
        crossAxis: checkCrossAxis = false,
        limiter = {
          fn: (_ref) => {
            let {
              x: x2,
              y: y2
            } = _ref;
            return {
              x: x2,
              y: y2
            };
          }
        },
        ...detectOverflowOptions
      } = evaluate(options, state);
      const coords = {
        x,
        y
      };
      const overflow = await detectOverflow(state, detectOverflowOptions);
      const crossAxis = getSideAxis(getSide(placement));
      const mainAxis = getOppositeAxis(crossAxis);
      let mainAxisCoord = coords[mainAxis];
      let crossAxisCoord = coords[crossAxis];
      if (checkMainAxis) {
        const minSide = mainAxis === "y" ? "top" : "left";
        const maxSide = mainAxis === "y" ? "bottom" : "right";
        const min3 = mainAxisCoord + overflow[minSide];
        const max3 = mainAxisCoord - overflow[maxSide];
        mainAxisCoord = clamp2(min3, mainAxisCoord, max3);
      }
      if (checkCrossAxis) {
        const minSide = crossAxis === "y" ? "top" : "left";
        const maxSide = crossAxis === "y" ? "bottom" : "right";
        const min3 = crossAxisCoord + overflow[minSide];
        const max3 = crossAxisCoord - overflow[maxSide];
        crossAxisCoord = clamp2(min3, crossAxisCoord, max3);
      }
      const limitedCoords = limiter.fn({
        ...state,
        [mainAxis]: mainAxisCoord,
        [crossAxis]: crossAxisCoord
      });
      return {
        ...limitedCoords,
        data: {
          x: limitedCoords.x - x,
          y: limitedCoords.y - y
        }
      };
    }
  };
};
var limitShift = function(options) {
  if (options === void 0) {
    options = {};
  }
  return {
    options,
    fn(state) {
      const {
        x,
        y,
        placement,
        rects,
        middlewareData
      } = state;
      const {
        offset: offset3 = 0,
        mainAxis: checkMainAxis = true,
        crossAxis: checkCrossAxis = true
      } = evaluate(options, state);
      const coords = {
        x,
        y
      };
      const crossAxis = getSideAxis(placement);
      const mainAxis = getOppositeAxis(crossAxis);
      let mainAxisCoord = coords[mainAxis];
      let crossAxisCoord = coords[crossAxis];
      const rawOffset = evaluate(offset3, state);
      const computedOffset = typeof rawOffset === "number" ? {
        mainAxis: rawOffset,
        crossAxis: 0
      } : {
        mainAxis: 0,
        crossAxis: 0,
        ...rawOffset
      };
      if (checkMainAxis) {
        const len = mainAxis === "y" ? "height" : "width";
        const limitMin = rects.reference[mainAxis] - rects.floating[len] + computedOffset.mainAxis;
        const limitMax = rects.reference[mainAxis] + rects.reference[len] - computedOffset.mainAxis;
        if (mainAxisCoord < limitMin) {
          mainAxisCoord = limitMin;
        } else if (mainAxisCoord > limitMax) {
          mainAxisCoord = limitMax;
        }
      }
      if (checkCrossAxis) {
        var _middlewareData$offse, _middlewareData$offse2;
        const len = mainAxis === "y" ? "width" : "height";
        const isOriginSide = ["top", "left"].includes(getSide(placement));
        const limitMin = rects.reference[crossAxis] - rects.floating[len] + (isOriginSide ? ((_middlewareData$offse = middlewareData.offset) == null ? void 0 : _middlewareData$offse[crossAxis]) || 0 : 0) + (isOriginSide ? 0 : computedOffset.crossAxis);
        const limitMax = rects.reference[crossAxis] + rects.reference[len] + (isOriginSide ? 0 : ((_middlewareData$offse2 = middlewareData.offset) == null ? void 0 : _middlewareData$offse2[crossAxis]) || 0) - (isOriginSide ? computedOffset.crossAxis : 0);
        if (crossAxisCoord < limitMin) {
          crossAxisCoord = limitMin;
        } else if (crossAxisCoord > limitMax) {
          crossAxisCoord = limitMax;
        }
      }
      return {
        [mainAxis]: mainAxisCoord,
        [crossAxis]: crossAxisCoord
      };
    }
  };
};
var size2 = function(options) {
  if (options === void 0) {
    options = {};
  }
  return {
    name: "size",
    options,
    async fn(state) {
      const {
        placement,
        rects,
        platform: platform2,
        elements
      } = state;
      const {
        apply = () => {
        },
        ...detectOverflowOptions
      } = evaluate(options, state);
      const overflow = await detectOverflow(state, detectOverflowOptions);
      const side = getSide(placement);
      const alignment = getAlignment(placement);
      const isYAxis = getSideAxis(placement) === "y";
      const {
        width,
        height
      } = rects.floating;
      let heightSide;
      let widthSide;
      if (side === "top" || side === "bottom") {
        heightSide = side;
        widthSide = alignment === (await (platform2.isRTL == null ? void 0 : platform2.isRTL(elements.floating)) ? "start" : "end") ? "left" : "right";
      } else {
        widthSide = side;
        heightSide = alignment === "end" ? "top" : "bottom";
      }
      const overflowAvailableHeight = height - overflow[heightSide];
      const overflowAvailableWidth = width - overflow[widthSide];
      const noShift = !state.middlewareData.shift;
      let availableHeight = overflowAvailableHeight;
      let availableWidth = overflowAvailableWidth;
      if (isYAxis) {
        const maximumClippingWidth = width - overflow.left - overflow.right;
        availableWidth = alignment || noShift ? min(overflowAvailableWidth, maximumClippingWidth) : maximumClippingWidth;
      } else {
        const maximumClippingHeight = height - overflow.top - overflow.bottom;
        availableHeight = alignment || noShift ? min(overflowAvailableHeight, maximumClippingHeight) : maximumClippingHeight;
      }
      if (noShift && !alignment) {
        const xMin = max(overflow.left, 0);
        const xMax = max(overflow.right, 0);
        const yMin = max(overflow.top, 0);
        const yMax = max(overflow.bottom, 0);
        if (isYAxis) {
          availableWidth = width - 2 * (xMin !== 0 || xMax !== 0 ? xMin + xMax : max(overflow.left, overflow.right));
        } else {
          availableHeight = height - 2 * (yMin !== 0 || yMax !== 0 ? yMin + yMax : max(overflow.top, overflow.bottom));
        }
      }
      await apply({
        ...state,
        availableWidth,
        availableHeight
      });
      const nextDimensions = await platform2.getDimensions(elements.floating);
      if (width !== nextDimensions.width || height !== nextDimensions.height) {
        return {
          reset: {
            rects: true
          }
        };
      }
      return {};
    }
  };
};

// node_modules/@floating-ui/utils/dom/dist/floating-ui.utils.dom.mjs
function getNodeName(node2) {
  if (isNode(node2)) {
    return (node2.nodeName || "").toLowerCase();
  }
  return "#document";
}
function getWindow(node2) {
  var _node$ownerDocument;
  return (node2 == null ? void 0 : (_node$ownerDocument = node2.ownerDocument) == null ? void 0 : _node$ownerDocument.defaultView) || window;
}
function getDocumentElement(node2) {
  var _ref;
  return (_ref = (isNode(node2) ? node2.ownerDocument : node2.document) || window.document) == null ? void 0 : _ref.documentElement;
}
function isNode(value) {
  return value instanceof Node || value instanceof getWindow(value).Node;
}
function isElement2(value) {
  return value instanceof Element || value instanceof getWindow(value).Element;
}
function isHTMLElement(value) {
  return value instanceof HTMLElement || value instanceof getWindow(value).HTMLElement;
}
function isShadowRoot(value) {
  if (typeof ShadowRoot === "undefined") {
    return false;
  }
  return value instanceof ShadowRoot || value instanceof getWindow(value).ShadowRoot;
}
function isOverflowElement(element) {
  const {
    overflow,
    overflowX,
    overflowY,
    display
  } = getComputedStyle2(element);
  return /auto|scroll|overlay|hidden|clip/.test(overflow + overflowY + overflowX) && !["inline", "contents"].includes(display);
}
function isTableElement(element) {
  return ["table", "td", "th"].includes(getNodeName(element));
}
function isContainingBlock(element) {
  const webkit = isWebKit();
  const css2 = getComputedStyle2(element);
  return css2.transform !== "none" || css2.perspective !== "none" || (css2.containerType ? css2.containerType !== "normal" : false) || !webkit && (css2.backdropFilter ? css2.backdropFilter !== "none" : false) || !webkit && (css2.filter ? css2.filter !== "none" : false) || ["transform", "perspective", "filter"].some((value) => (css2.willChange || "").includes(value)) || ["paint", "layout", "strict", "content"].some((value) => (css2.contain || "").includes(value));
}
function getContainingBlock(element) {
  let currentNode = getParentNode(element);
  while (isHTMLElement(currentNode) && !isLastTraversableNode(currentNode)) {
    if (isContainingBlock(currentNode)) {
      return currentNode;
    } else {
      currentNode = getParentNode(currentNode);
    }
  }
  return null;
}
function isWebKit() {
  if (typeof CSS === "undefined" || !CSS.supports)
    return false;
  return CSS.supports("-webkit-backdrop-filter", "none");
}
function isLastTraversableNode(node2) {
  return ["html", "body", "#document"].includes(getNodeName(node2));
}
function getComputedStyle2(element) {
  return getWindow(element).getComputedStyle(element);
}
function getNodeScroll(element) {
  if (isElement2(element)) {
    return {
      scrollLeft: element.scrollLeft,
      scrollTop: element.scrollTop
    };
  }
  return {
    scrollLeft: element.pageXOffset,
    scrollTop: element.pageYOffset
  };
}
function getParentNode(node2) {
  if (getNodeName(node2) === "html") {
    return node2;
  }
  const result = (
    // Step into the shadow DOM of the parent of a slotted node.
    node2.assignedSlot || // DOM Element detected.
    node2.parentNode || // ShadowRoot detected.
    isShadowRoot(node2) && node2.host || // Fallback.
    getDocumentElement(node2)
  );
  return isShadowRoot(result) ? result.host : result;
}
function getNearestOverflowAncestor(node2) {
  const parentNode = getParentNode(node2);
  if (isLastTraversableNode(parentNode)) {
    return node2.ownerDocument ? node2.ownerDocument.body : node2.body;
  }
  if (isHTMLElement(parentNode) && isOverflowElement(parentNode)) {
    return parentNode;
  }
  return getNearestOverflowAncestor(parentNode);
}
function getOverflowAncestors(node2, list, traverseIframes) {
  var _node$ownerDocument2;
  if (list === void 0) {
    list = [];
  }
  if (traverseIframes === void 0) {
    traverseIframes = true;
  }
  const scrollableAncestor = getNearestOverflowAncestor(node2);
  const isBody = scrollableAncestor === ((_node$ownerDocument2 = node2.ownerDocument) == null ? void 0 : _node$ownerDocument2.body);
  const win = getWindow(scrollableAncestor);
  if (isBody) {
    return list.concat(win, win.visualViewport || [], isOverflowElement(scrollableAncestor) ? scrollableAncestor : [], win.frameElement && traverseIframes ? getOverflowAncestors(win.frameElement) : []);
  }
  return list.concat(scrollableAncestor, getOverflowAncestors(scrollableAncestor, [], traverseIframes));
}

// node_modules/@floating-ui/dom/dist/floating-ui.dom.mjs
function getCssDimensions(element) {
  const css2 = getComputedStyle2(element);
  let width = parseFloat(css2.width) || 0;
  let height = parseFloat(css2.height) || 0;
  const hasOffset = isHTMLElement(element);
  const offsetWidth = hasOffset ? element.offsetWidth : width;
  const offsetHeight = hasOffset ? element.offsetHeight : height;
  const shouldFallback = round(width) !== offsetWidth || round(height) !== offsetHeight;
  if (shouldFallback) {
    width = offsetWidth;
    height = offsetHeight;
  }
  return {
    width,
    height,
    $: shouldFallback
  };
}
function unwrapElement(element) {
  return !isElement2(element) ? element.contextElement : element;
}
function getScale(element) {
  const domElement = unwrapElement(element);
  if (!isHTMLElement(domElement)) {
    return createCoords(1);
  }
  const rect = domElement.getBoundingClientRect();
  const {
    width,
    height,
    $
  } = getCssDimensions(domElement);
  let x = ($ ? round(rect.width) : rect.width) / width;
  let y = ($ ? round(rect.height) : rect.height) / height;
  if (!x || !Number.isFinite(x)) {
    x = 1;
  }
  if (!y || !Number.isFinite(y)) {
    y = 1;
  }
  return {
    x,
    y
  };
}
var noOffsets = createCoords(0);
function getVisualOffsets(element) {
  const win = getWindow(element);
  if (!isWebKit() || !win.visualViewport) {
    return noOffsets;
  }
  return {
    x: win.visualViewport.offsetLeft,
    y: win.visualViewport.offsetTop
  };
}
function shouldAddVisualOffsets(element, isFixed, floatingOffsetParent) {
  if (isFixed === void 0) {
    isFixed = false;
  }
  if (!floatingOffsetParent || isFixed && floatingOffsetParent !== getWindow(element)) {
    return false;
  }
  return isFixed;
}
function getBoundingClientRect(element, includeScale, isFixedStrategy, offsetParent) {
  if (includeScale === void 0) {
    includeScale = false;
  }
  if (isFixedStrategy === void 0) {
    isFixedStrategy = false;
  }
  const clientRect = element.getBoundingClientRect();
  const domElement = unwrapElement(element);
  let scale = createCoords(1);
  if (includeScale) {
    if (offsetParent) {
      if (isElement2(offsetParent)) {
        scale = getScale(offsetParent);
      }
    } else {
      scale = getScale(element);
    }
  }
  const visualOffsets = shouldAddVisualOffsets(domElement, isFixedStrategy, offsetParent) ? getVisualOffsets(domElement) : createCoords(0);
  let x = (clientRect.left + visualOffsets.x) / scale.x;
  let y = (clientRect.top + visualOffsets.y) / scale.y;
  let width = clientRect.width / scale.x;
  let height = clientRect.height / scale.y;
  if (domElement) {
    const win = getWindow(domElement);
    const offsetWin = offsetParent && isElement2(offsetParent) ? getWindow(offsetParent) : offsetParent;
    let currentIFrame = win.frameElement;
    while (currentIFrame && offsetParent && offsetWin !== win) {
      const iframeScale = getScale(currentIFrame);
      const iframeRect = currentIFrame.getBoundingClientRect();
      const css2 = getComputedStyle2(currentIFrame);
      const left2 = iframeRect.left + (currentIFrame.clientLeft + parseFloat(css2.paddingLeft)) * iframeScale.x;
      const top2 = iframeRect.top + (currentIFrame.clientTop + parseFloat(css2.paddingTop)) * iframeScale.y;
      x *= iframeScale.x;
      y *= iframeScale.y;
      width *= iframeScale.x;
      height *= iframeScale.y;
      x += left2;
      y += top2;
      currentIFrame = getWindow(currentIFrame).frameElement;
    }
  }
  return rectToClientRect({
    width,
    height,
    x,
    y
  });
}
function convertOffsetParentRelativeRectToViewportRelativeRect(_ref) {
  let {
    rect,
    offsetParent,
    strategy
  } = _ref;
  const isOffsetParentAnElement = isHTMLElement(offsetParent);
  const documentElement = getDocumentElement(offsetParent);
  if (offsetParent === documentElement) {
    return rect;
  }
  let scroll = {
    scrollLeft: 0,
    scrollTop: 0
  };
  let scale = createCoords(1);
  const offsets = createCoords(0);
  if (isOffsetParentAnElement || !isOffsetParentAnElement && strategy !== "fixed") {
    if (getNodeName(offsetParent) !== "body" || isOverflowElement(documentElement)) {
      scroll = getNodeScroll(offsetParent);
    }
    if (isHTMLElement(offsetParent)) {
      const offsetRect = getBoundingClientRect(offsetParent);
      scale = getScale(offsetParent);
      offsets.x = offsetRect.x + offsetParent.clientLeft;
      offsets.y = offsetRect.y + offsetParent.clientTop;
    }
  }
  return {
    width: rect.width * scale.x,
    height: rect.height * scale.y,
    x: rect.x * scale.x - scroll.scrollLeft * scale.x + offsets.x,
    y: rect.y * scale.y - scroll.scrollTop * scale.y + offsets.y
  };
}
function getClientRects(element) {
  return Array.from(element.getClientRects());
}
function getWindowScrollBarX(element) {
  return getBoundingClientRect(getDocumentElement(element)).left + getNodeScroll(element).scrollLeft;
}
function getDocumentRect(element) {
  const html = getDocumentElement(element);
  const scroll = getNodeScroll(element);
  const body = element.ownerDocument.body;
  const width = max(html.scrollWidth, html.clientWidth, body.scrollWidth, body.clientWidth);
  const height = max(html.scrollHeight, html.clientHeight, body.scrollHeight, body.clientHeight);
  let x = -scroll.scrollLeft + getWindowScrollBarX(element);
  const y = -scroll.scrollTop;
  if (getComputedStyle2(body).direction === "rtl") {
    x += max(html.clientWidth, body.clientWidth) - width;
  }
  return {
    width,
    height,
    x,
    y
  };
}
function getViewportRect(element, strategy) {
  const win = getWindow(element);
  const html = getDocumentElement(element);
  const visualViewport = win.visualViewport;
  let width = html.clientWidth;
  let height = html.clientHeight;
  let x = 0;
  let y = 0;
  if (visualViewport) {
    width = visualViewport.width;
    height = visualViewport.height;
    const visualViewportBased = isWebKit();
    if (!visualViewportBased || visualViewportBased && strategy === "fixed") {
      x = visualViewport.offsetLeft;
      y = visualViewport.offsetTop;
    }
  }
  return {
    width,
    height,
    x,
    y
  };
}
function getInnerBoundingClientRect(element, strategy) {
  const clientRect = getBoundingClientRect(element, true, strategy === "fixed");
  const top2 = clientRect.top + element.clientTop;
  const left2 = clientRect.left + element.clientLeft;
  const scale = isHTMLElement(element) ? getScale(element) : createCoords(1);
  const width = element.clientWidth * scale.x;
  const height = element.clientHeight * scale.y;
  const x = left2 * scale.x;
  const y = top2 * scale.y;
  return {
    width,
    height,
    x,
    y
  };
}
function getClientRectFromClippingAncestor(element, clippingAncestor, strategy) {
  let rect;
  if (clippingAncestor === "viewport") {
    rect = getViewportRect(element, strategy);
  } else if (clippingAncestor === "document") {
    rect = getDocumentRect(getDocumentElement(element));
  } else if (isElement2(clippingAncestor)) {
    rect = getInnerBoundingClientRect(clippingAncestor, strategy);
  } else {
    const visualOffsets = getVisualOffsets(element);
    rect = {
      ...clippingAncestor,
      x: clippingAncestor.x - visualOffsets.x,
      y: clippingAncestor.y - visualOffsets.y
    };
  }
  return rectToClientRect(rect);
}
function hasFixedPositionAncestor(element, stopNode) {
  const parentNode = getParentNode(element);
  if (parentNode === stopNode || !isElement2(parentNode) || isLastTraversableNode(parentNode)) {
    return false;
  }
  return getComputedStyle2(parentNode).position === "fixed" || hasFixedPositionAncestor(parentNode, stopNode);
}
function getClippingElementAncestors(element, cache) {
  const cachedResult = cache.get(element);
  if (cachedResult) {
    return cachedResult;
  }
  let result = getOverflowAncestors(element, [], false).filter((el) => isElement2(el) && getNodeName(el) !== "body");
  let currentContainingBlockComputedStyle = null;
  const elementIsFixed = getComputedStyle2(element).position === "fixed";
  let currentNode = elementIsFixed ? getParentNode(element) : element;
  while (isElement2(currentNode) && !isLastTraversableNode(currentNode)) {
    const computedStyle = getComputedStyle2(currentNode);
    const currentNodeIsContaining = isContainingBlock(currentNode);
    if (!currentNodeIsContaining && computedStyle.position === "fixed") {
      currentContainingBlockComputedStyle = null;
    }
    const shouldDropCurrentNode = elementIsFixed ? !currentNodeIsContaining && !currentContainingBlockComputedStyle : !currentNodeIsContaining && computedStyle.position === "static" && !!currentContainingBlockComputedStyle && ["absolute", "fixed"].includes(currentContainingBlockComputedStyle.position) || isOverflowElement(currentNode) && !currentNodeIsContaining && hasFixedPositionAncestor(element, currentNode);
    if (shouldDropCurrentNode) {
      result = result.filter((ancestor) => ancestor !== currentNode);
    } else {
      currentContainingBlockComputedStyle = computedStyle;
    }
    currentNode = getParentNode(currentNode);
  }
  cache.set(element, result);
  return result;
}
function getClippingRect(_ref) {
  let {
    element,
    boundary,
    rootBoundary,
    strategy
  } = _ref;
  const elementClippingAncestors = boundary === "clippingAncestors" ? getClippingElementAncestors(element, this._c) : [].concat(boundary);
  const clippingAncestors = [...elementClippingAncestors, rootBoundary];
  const firstClippingAncestor = clippingAncestors[0];
  const clippingRect = clippingAncestors.reduce((accRect, clippingAncestor) => {
    const rect = getClientRectFromClippingAncestor(element, clippingAncestor, strategy);
    accRect.top = max(rect.top, accRect.top);
    accRect.right = min(rect.right, accRect.right);
    accRect.bottom = min(rect.bottom, accRect.bottom);
    accRect.left = max(rect.left, accRect.left);
    return accRect;
  }, getClientRectFromClippingAncestor(element, firstClippingAncestor, strategy));
  return {
    width: clippingRect.right - clippingRect.left,
    height: clippingRect.bottom - clippingRect.top,
    x: clippingRect.left,
    y: clippingRect.top
  };
}
function getDimensions(element) {
  return getCssDimensions(element);
}
function getRectRelativeToOffsetParent(element, offsetParent, strategy) {
  const isOffsetParentAnElement = isHTMLElement(offsetParent);
  const documentElement = getDocumentElement(offsetParent);
  const isFixed = strategy === "fixed";
  const rect = getBoundingClientRect(element, true, isFixed, offsetParent);
  let scroll = {
    scrollLeft: 0,
    scrollTop: 0
  };
  const offsets = createCoords(0);
  if (isOffsetParentAnElement || !isOffsetParentAnElement && !isFixed) {
    if (getNodeName(offsetParent) !== "body" || isOverflowElement(documentElement)) {
      scroll = getNodeScroll(offsetParent);
    }
    if (isOffsetParentAnElement) {
      const offsetRect = getBoundingClientRect(offsetParent, true, isFixed, offsetParent);
      offsets.x = offsetRect.x + offsetParent.clientLeft;
      offsets.y = offsetRect.y + offsetParent.clientTop;
    } else if (documentElement) {
      offsets.x = getWindowScrollBarX(documentElement);
    }
  }
  return {
    x: rect.left + scroll.scrollLeft - offsets.x,
    y: rect.top + scroll.scrollTop - offsets.y,
    width: rect.width,
    height: rect.height
  };
}
function getTrueOffsetParent(element, polyfill) {
  if (!isHTMLElement(element) || getComputedStyle2(element).position === "fixed") {
    return null;
  }
  if (polyfill) {
    return polyfill(element);
  }
  return element.offsetParent;
}
function getOffsetParent(element, polyfill) {
  const window2 = getWindow(element);
  if (!isHTMLElement(element)) {
    return window2;
  }
  let offsetParent = getTrueOffsetParent(element, polyfill);
  while (offsetParent && isTableElement(offsetParent) && getComputedStyle2(offsetParent).position === "static") {
    offsetParent = getTrueOffsetParent(offsetParent, polyfill);
  }
  if (offsetParent && (getNodeName(offsetParent) === "html" || getNodeName(offsetParent) === "body" && getComputedStyle2(offsetParent).position === "static" && !isContainingBlock(offsetParent))) {
    return window2;
  }
  return offsetParent || getContainingBlock(element) || window2;
}
var getElementRects = async function(_ref) {
  let {
    reference: reference2,
    floating,
    strategy
  } = _ref;
  const getOffsetParentFn = this.getOffsetParent || getOffsetParent;
  const getDimensionsFn = this.getDimensions;
  return {
    reference: getRectRelativeToOffsetParent(reference2, await getOffsetParentFn(floating), strategy),
    floating: {
      x: 0,
      y: 0,
      ...await getDimensionsFn(floating)
    }
  };
};
function isRTL(element) {
  return getComputedStyle2(element).direction === "rtl";
}
var platform = {
  convertOffsetParentRelativeRectToViewportRelativeRect,
  getDocumentElement,
  getClippingRect,
  getOffsetParent,
  getElementRects,
  getClientRects,
  getDimensions,
  getScale,
  isElement: isElement2,
  isRTL
};
function observeMove(element, onMove) {
  let io = null;
  let timeoutId2;
  const root = getDocumentElement(element);
  function cleanup() {
    clearTimeout(timeoutId2);
    io && io.disconnect();
    io = null;
  }
  function refresh(skip, threshold) {
    if (skip === void 0) {
      skip = false;
    }
    if (threshold === void 0) {
      threshold = 1;
    }
    cleanup();
    const {
      left: left2,
      top: top2,
      width,
      height
    } = element.getBoundingClientRect();
    if (!skip) {
      onMove();
    }
    if (!width || !height) {
      return;
    }
    const insetTop = floor(top2);
    const insetRight = floor(root.clientWidth - (left2 + width));
    const insetBottom = floor(root.clientHeight - (top2 + height));
    const insetLeft = floor(left2);
    const rootMargin = -insetTop + "px " + -insetRight + "px " + -insetBottom + "px " + -insetLeft + "px";
    const options = {
      rootMargin,
      threshold: max(0, min(1, threshold)) || 1
    };
    let isFirstUpdate = true;
    function handleObserve(entries) {
      const ratio = entries[0].intersectionRatio;
      if (ratio !== threshold) {
        if (!isFirstUpdate) {
          return refresh();
        }
        if (!ratio) {
          timeoutId2 = setTimeout(() => {
            refresh(false, 1e-7);
          }, 100);
        } else {
          refresh(false, ratio);
        }
      }
      isFirstUpdate = false;
    }
    try {
      io = new IntersectionObserver(handleObserve, {
        ...options,
        // Handle <iframe>s
        root: root.ownerDocument
      });
    } catch (e) {
      io = new IntersectionObserver(handleObserve, options);
    }
    io.observe(element);
  }
  refresh(true);
  return cleanup;
}
function autoUpdate(reference2, floating, update, options) {
  if (options === void 0) {
    options = {};
  }
  const {
    ancestorScroll = true,
    ancestorResize = true,
    elementResize = typeof ResizeObserver === "function",
    layoutShift = typeof IntersectionObserver === "function",
    animationFrame = false
  } = options;
  const referenceEl = unwrapElement(reference2);
  const ancestors = ancestorScroll || ancestorResize ? [...referenceEl ? getOverflowAncestors(referenceEl) : [], ...getOverflowAncestors(floating)] : [];
  ancestors.forEach((ancestor) => {
    ancestorScroll && ancestor.addEventListener("scroll", update, {
      passive: true
    });
    ancestorResize && ancestor.addEventListener("resize", update);
  });
  const cleanupIo = referenceEl && layoutShift ? observeMove(referenceEl, update) : null;
  let reobserveFrame = -1;
  let resizeObserver = null;
  if (elementResize) {
    resizeObserver = new ResizeObserver((_ref) => {
      let [firstEntry] = _ref;
      if (firstEntry && firstEntry.target === referenceEl && resizeObserver) {
        resizeObserver.unobserve(floating);
        cancelAnimationFrame(reobserveFrame);
        reobserveFrame = requestAnimationFrame(() => {
          resizeObserver && resizeObserver.observe(floating);
        });
      }
      update();
    });
    if (referenceEl && !animationFrame) {
      resizeObserver.observe(referenceEl);
    }
    resizeObserver.observe(floating);
  }
  let frameId;
  let prevRefRect = animationFrame ? getBoundingClientRect(reference2) : null;
  if (animationFrame) {
    frameLoop();
  }
  function frameLoop() {
    const nextRefRect = getBoundingClientRect(reference2);
    if (prevRefRect && (nextRefRect.x !== prevRefRect.x || nextRefRect.y !== prevRefRect.y || nextRefRect.width !== prevRefRect.width || nextRefRect.height !== prevRefRect.height)) {
      update();
    }
    prevRefRect = nextRefRect;
    frameId = requestAnimationFrame(frameLoop);
  }
  update();
  return () => {
    ancestors.forEach((ancestor) => {
      ancestorScroll && ancestor.removeEventListener("scroll", update);
      ancestorResize && ancestor.removeEventListener("resize", update);
    });
    cleanupIo && cleanupIo();
    resizeObserver && resizeObserver.disconnect();
    resizeObserver = null;
    if (animationFrame) {
      cancelAnimationFrame(frameId);
    }
  };
}
var computePosition2 = (reference2, floating, options) => {
  const cache = /* @__PURE__ */ new Map();
  const mergedOptions = {
    platform,
    ...options
  };
  const platformWithCache = {
    ...mergedOptions.platform,
    _c: cache
  };
  return computePosition(reference2, floating, {
    ...mergedOptions,
    platform: platformWithCache
  });
};

// node_modules/@floating-ui/react-dom/dist/floating-ui.react-dom.esm.js
var React49 = __toESM(require_react());
var import_react116 = __toESM(require_react());
var ReactDOM = __toESM(require_react_dom());
var arrow2 = (options) => {
  const {
    element,
    padding: padding2
  } = options;
  function isRef(value) {
    return Object.prototype.hasOwnProperty.call(value, "current");
  }
  return {
    name: "arrow",
    options,
    fn(args) {
      if (isRef(element)) {
        if (element.current != null) {
          return arrow({
            element: element.current,
            padding: padding2
          }).fn(args);
        }
        return {};
      } else if (element) {
        return arrow({
          element,
          padding: padding2
        }).fn(args);
      }
      return {};
    }
  };
};
var index = typeof document !== "undefined" ? import_react116.useLayoutEffect : import_react116.useEffect;
function deepEqual(a2, b2) {
  if (a2 === b2) {
    return true;
  }
  if (typeof a2 !== typeof b2) {
    return false;
  }
  if (typeof a2 === "function" && a2.toString() === b2.toString()) {
    return true;
  }
  let length2, i, keys2;
  if (a2 && b2 && typeof a2 == "object") {
    if (Array.isArray(a2)) {
      length2 = a2.length;
      if (length2 != b2.length)
        return false;
      for (i = length2; i-- !== 0; ) {
        if (!deepEqual(a2[i], b2[i])) {
          return false;
        }
      }
      return true;
    }
    keys2 = Object.keys(a2);
    length2 = keys2.length;
    if (length2 !== Object.keys(b2).length) {
      return false;
    }
    for (i = length2; i-- !== 0; ) {
      if (!Object.prototype.hasOwnProperty.call(b2, keys2[i])) {
        return false;
      }
    }
    for (i = length2; i-- !== 0; ) {
      const key = keys2[i];
      if (key === "_owner" && a2.$$typeof) {
        continue;
      }
      if (!deepEqual(a2[key], b2[key])) {
        return false;
      }
    }
    return true;
  }
  return a2 !== a2 && b2 !== b2;
}
function useLatestRef(value) {
  const ref = React49.useRef(value);
  index(() => {
    ref.current = value;
  });
  return ref;
}
function useFloating(options) {
  if (options === void 0) {
    options = {};
  }
  const {
    placement = "bottom",
    strategy = "absolute",
    middleware: middleware2 = [],
    platform: platform2,
    whileElementsMounted,
    open
  } = options;
  const [data, setData] = React49.useState({
    x: null,
    y: null,
    strategy,
    placement,
    middlewareData: {},
    isPositioned: false
  });
  const [latestMiddleware, setLatestMiddleware] = React49.useState(middleware2);
  if (!deepEqual(latestMiddleware, middleware2)) {
    setLatestMiddleware(middleware2);
  }
  const referenceRef = React49.useRef(null);
  const floatingRef = React49.useRef(null);
  const dataRef = React49.useRef(data);
  const whileElementsMountedRef = useLatestRef(whileElementsMounted);
  const platformRef = useLatestRef(platform2);
  const [reference2, _setReference] = React49.useState(null);
  const [floating, _setFloating] = React49.useState(null);
  const setReference = React49.useCallback((node2) => {
    if (referenceRef.current !== node2) {
      referenceRef.current = node2;
      _setReference(node2);
    }
  }, []);
  const setFloating = React49.useCallback((node2) => {
    if (floatingRef.current !== node2) {
      floatingRef.current = node2;
      _setFloating(node2);
    }
  }, []);
  const update = React49.useCallback(() => {
    if (!referenceRef.current || !floatingRef.current) {
      return;
    }
    const config = {
      placement,
      strategy,
      middleware: latestMiddleware
    };
    if (platformRef.current) {
      config.platform = platformRef.current;
    }
    computePosition2(referenceRef.current, floatingRef.current, config).then((data2) => {
      const fullData = {
        ...data2,
        isPositioned: true
      };
      if (isMountedRef.current && !deepEqual(dataRef.current, fullData)) {
        dataRef.current = fullData;
        ReactDOM.flushSync(() => {
          setData(fullData);
        });
      }
    });
  }, [latestMiddleware, placement, strategy, platformRef]);
  index(() => {
    if (open === false && dataRef.current.isPositioned) {
      dataRef.current.isPositioned = false;
      setData((data2) => ({
        ...data2,
        isPositioned: false
      }));
    }
  }, [open]);
  const isMountedRef = React49.useRef(false);
  index(() => {
    isMountedRef.current = true;
    return () => {
      isMountedRef.current = false;
    };
  }, []);
  index(() => {
    if (reference2 && floating) {
      if (whileElementsMountedRef.current) {
        return whileElementsMountedRef.current(reference2, floating, update);
      } else {
        update();
      }
    }
  }, [reference2, floating, update, whileElementsMountedRef]);
  const refs = React49.useMemo(() => ({
    reference: referenceRef,
    floating: floatingRef,
    setReference,
    setFloating
  }), [setReference, setFloating]);
  const elements = React49.useMemo(() => ({
    reference: reference2,
    floating
  }), [reference2, floating]);
  return React49.useMemo(() => ({
    ...data,
    update,
    refs,
    elements,
    reference: setReference,
    floating: setFloating
  }), [data, update, refs, elements, setReference, setFloating]);
}

// node_modules/@floating-ui/react/dist/floating-ui.react.esm.js
var index2 = typeof document !== "undefined" ? import_react117.useLayoutEffect : import_react117.useEffect;
var serverHandoffComplete = false;
var count = 0;
var genId = () => "floating-ui-" + count++;
function useFloatingId() {
  const [id, setId] = React50.useState(() => serverHandoffComplete ? genId() : void 0);
  index2(() => {
    if (id == null) {
      setId(genId());
    }
  }, []);
  React50.useEffect(() => {
    if (!serverHandoffComplete) {
      serverHandoffComplete = true;
    }
  }, []);
  return id;
}
var useReactId2 = React50["useId".toString()];
var useId2 = useReactId2 || useFloatingId;
function createPubSub() {
  const map = /* @__PURE__ */ new Map();
  return {
    emit(event, data) {
      var _map$get;
      (_map$get = map.get(event)) == null ? void 0 : _map$get.forEach((handler) => handler(data));
    },
    on(event, listener) {
      map.set(event, [...map.get(event) || [], listener]);
    },
    off(event, listener) {
      map.set(event, (map.get(event) || []).filter((l) => l !== listener));
    }
  };
}
var FloatingNodeContext = React50.createContext(null);
var FloatingTreeContext = React50.createContext(null);
var useFloatingParentNodeId = () => {
  var _React$useContext;
  return ((_React$useContext = React50.useContext(FloatingNodeContext)) == null ? void 0 : _React$useContext.id) || null;
};
var useFloatingTree = () => React50.useContext(FloatingTreeContext);
function getDocument(node2) {
  return (node2 == null ? void 0 : node2.ownerDocument) || document;
}
function getPlatform() {
  const uaData = navigator.userAgentData;
  if (uaData != null && uaData.platform) {
    return uaData.platform;
  }
  return navigator.platform;
}
function getUserAgent() {
  const uaData = navigator.userAgentData;
  if (uaData && Array.isArray(uaData.brands)) {
    return uaData.brands.map((_ref) => {
      let {
        brand,
        version
      } = _ref;
      return brand + "/" + version;
    }).join(" ");
  }
  return navigator.userAgent;
}
function getWindow2(value) {
  return getDocument(value).defaultView || window;
}
function isElement3(value) {
  return value ? value instanceof getWindow2(value).Element : false;
}
function isHTMLElement2(value) {
  return value ? value instanceof getWindow2(value).HTMLElement : false;
}
function isShadowRoot2(node2) {
  if (typeof ShadowRoot === "undefined") {
    return false;
  }
  const OwnElement = getWindow2(node2).ShadowRoot;
  return node2 instanceof OwnElement || node2 instanceof ShadowRoot;
}
function isVirtualClick(event) {
  if (event.mozInputSource === 0 && event.isTrusted) {
    return true;
  }
  const androidRe = /Android/i;
  if ((androidRe.test(getPlatform()) || androidRe.test(getUserAgent())) && event.pointerType) {
    return event.type === "click" && event.buttons === 1;
  }
  return event.detail === 0 && !event.pointerType;
}
function isVirtualPointerEvent(event) {
  return event.width === 0 && event.height === 0 || event.width === 1 && event.height === 1 && event.pressure === 0 && event.detail === 0 && event.pointerType !== "mouse" || // iOS VoiceOver returns 0.333• for width/height.
  event.width < 1 && event.height < 1 && event.pressure === 0 && event.detail === 0;
}
function isSafari() {
  return /apple/i.test(navigator.vendor);
}
function isMac() {
  return getPlatform().toLowerCase().startsWith("mac") && !navigator.maxTouchPoints;
}
function isMouseLikePointerType(pointerType, strict) {
  const values = ["mouse", "pen"];
  if (!strict) {
    values.push("", void 0);
  }
  return values.includes(pointerType);
}
function useLatestRef2(value) {
  const ref = (0, import_react117.useRef)(value);
  index2(() => {
    ref.current = value;
  });
  return ref;
}
var safePolygonIdentifier = "data-floating-ui-safe-polygon";
function getDelay(value, prop, pointerType) {
  if (pointerType && !isMouseLikePointerType(pointerType)) {
    return 0;
  }
  if (typeof value === "number") {
    return value;
  }
  return value == null ? void 0 : value[prop];
}
var useHover2 = function(context, _temp) {
  let {
    enabled = true,
    delay = 0,
    handleClose = null,
    mouseOnly = false,
    restMs = 0,
    move = true
  } = _temp === void 0 ? {} : _temp;
  const {
    open,
    onOpenChange,
    dataRef,
    events,
    elements: {
      domReference,
      floating
    },
    refs
  } = context;
  const tree = useFloatingTree();
  const parentId = useFloatingParentNodeId();
  const handleCloseRef = useLatestRef2(handleClose);
  const delayRef = useLatestRef2(delay);
  const pointerTypeRef = React50.useRef();
  const timeoutRef = React50.useRef();
  const handlerRef = React50.useRef();
  const restTimeoutRef = React50.useRef();
  const blockMouseMoveRef = React50.useRef(true);
  const performedPointerEventsMutationRef = React50.useRef(false);
  const unbindMouseMoveRef = React50.useRef(() => {
  });
  const isHoverOpen = React50.useCallback(() => {
    var _dataRef$current$open;
    const type = (_dataRef$current$open = dataRef.current.openEvent) == null ? void 0 : _dataRef$current$open.type;
    return (type == null ? void 0 : type.includes("mouse")) && type !== "mousedown";
  }, [dataRef]);
  React50.useEffect(() => {
    if (!enabled) {
      return;
    }
    function onDismiss() {
      clearTimeout(timeoutRef.current);
      clearTimeout(restTimeoutRef.current);
      blockMouseMoveRef.current = true;
    }
    events.on("dismiss", onDismiss);
    return () => {
      events.off("dismiss", onDismiss);
    };
  }, [enabled, events]);
  React50.useEffect(() => {
    if (!enabled || !handleCloseRef.current || !open) {
      return;
    }
    function onLeave() {
      if (isHoverOpen()) {
        onOpenChange(false);
      }
    }
    const html = getDocument(floating).documentElement;
    html.addEventListener("mouseleave", onLeave);
    return () => {
      html.removeEventListener("mouseleave", onLeave);
    };
  }, [floating, open, onOpenChange, enabled, handleCloseRef, dataRef, isHoverOpen]);
  const closeWithDelay = React50.useCallback(function(runElseBranch) {
    if (runElseBranch === void 0) {
      runElseBranch = true;
    }
    const closeDelay = getDelay(delayRef.current, "close", pointerTypeRef.current);
    if (closeDelay && !handlerRef.current) {
      clearTimeout(timeoutRef.current);
      timeoutRef.current = setTimeout(() => onOpenChange(false), closeDelay);
    } else if (runElseBranch) {
      clearTimeout(timeoutRef.current);
      onOpenChange(false);
    }
  }, [delayRef, onOpenChange]);
  const cleanupMouseMoveHandler = React50.useCallback(() => {
    unbindMouseMoveRef.current();
    handlerRef.current = void 0;
  }, []);
  const clearPointerEvents = React50.useCallback(() => {
    if (performedPointerEventsMutationRef.current) {
      const body = getDocument(refs.floating.current).body;
      body.style.pointerEvents = "";
      body.removeAttribute(safePolygonIdentifier);
      performedPointerEventsMutationRef.current = false;
    }
  }, [refs]);
  React50.useEffect(() => {
    if (!enabled) {
      return;
    }
    function isClickLikeOpenEvent() {
      return dataRef.current.openEvent ? ["click", "mousedown"].includes(dataRef.current.openEvent.type) : false;
    }
    function onMouseEnter(event) {
      clearTimeout(timeoutRef.current);
      blockMouseMoveRef.current = false;
      if (mouseOnly && !isMouseLikePointerType(pointerTypeRef.current) || restMs > 0 && getDelay(delayRef.current, "open") === 0) {
        return;
      }
      dataRef.current.openEvent = event;
      const openDelay = getDelay(delayRef.current, "open", pointerTypeRef.current);
      if (openDelay) {
        timeoutRef.current = setTimeout(() => {
          onOpenChange(true);
        }, openDelay);
      } else {
        onOpenChange(true);
      }
    }
    function onMouseLeave(event) {
      if (isClickLikeOpenEvent()) {
        return;
      }
      unbindMouseMoveRef.current();
      const doc = getDocument(floating);
      clearTimeout(restTimeoutRef.current);
      if (handleCloseRef.current) {
        if (!open) {
          clearTimeout(timeoutRef.current);
        }
        handlerRef.current = handleCloseRef.current({
          ...context,
          tree,
          x: event.clientX,
          y: event.clientY,
          onClose() {
            clearPointerEvents();
            cleanupMouseMoveHandler();
            closeWithDelay();
          }
        });
        const handler = handlerRef.current;
        doc.addEventListener("mousemove", handler);
        unbindMouseMoveRef.current = () => {
          doc.removeEventListener("mousemove", handler);
        };
        return;
      }
      closeWithDelay();
    }
    function onScrollMouseLeave(event) {
      if (isClickLikeOpenEvent()) {
        return;
      }
      handleCloseRef.current == null ? void 0 : handleCloseRef.current({
        ...context,
        tree,
        x: event.clientX,
        y: event.clientY,
        onClose() {
          clearPointerEvents();
          cleanupMouseMoveHandler();
          closeWithDelay();
        }
      })(event);
    }
    if (isElement3(domReference)) {
      const ref = domReference;
      open && ref.addEventListener("mouseleave", onScrollMouseLeave);
      floating == null ? void 0 : floating.addEventListener("mouseleave", onScrollMouseLeave);
      move && ref.addEventListener("mousemove", onMouseEnter, {
        once: true
      });
      ref.addEventListener("mouseenter", onMouseEnter);
      ref.addEventListener("mouseleave", onMouseLeave);
      return () => {
        open && ref.removeEventListener("mouseleave", onScrollMouseLeave);
        floating == null ? void 0 : floating.removeEventListener("mouseleave", onScrollMouseLeave);
        move && ref.removeEventListener("mousemove", onMouseEnter);
        ref.removeEventListener("mouseenter", onMouseEnter);
        ref.removeEventListener("mouseleave", onMouseLeave);
      };
    }
  }, [domReference, floating, enabled, context, mouseOnly, restMs, move, closeWithDelay, cleanupMouseMoveHandler, clearPointerEvents, onOpenChange, open, tree, delayRef, handleCloseRef, dataRef]);
  index2(() => {
    var _handleCloseRef$curre;
    if (!enabled) {
      return;
    }
    if (open && (_handleCloseRef$curre = handleCloseRef.current) != null && _handleCloseRef$curre.__options.blockPointerEvents && isHoverOpen()) {
      const body = getDocument(floating).body;
      body.setAttribute(safePolygonIdentifier, "");
      body.style.pointerEvents = "none";
      performedPointerEventsMutationRef.current = true;
      if (isElement3(domReference) && floating) {
        var _tree$nodesRef$curren, _tree$nodesRef$curren2;
        const ref = domReference;
        const parentFloating = tree == null ? void 0 : (_tree$nodesRef$curren = tree.nodesRef.current.find((node2) => node2.id === parentId)) == null ? void 0 : (_tree$nodesRef$curren2 = _tree$nodesRef$curren.context) == null ? void 0 : _tree$nodesRef$curren2.elements.floating;
        if (parentFloating) {
          parentFloating.style.pointerEvents = "";
        }
        ref.style.pointerEvents = "auto";
        floating.style.pointerEvents = "auto";
        return () => {
          ref.style.pointerEvents = "";
          floating.style.pointerEvents = "";
        };
      }
    }
  }, [enabled, open, parentId, floating, domReference, tree, handleCloseRef, dataRef, isHoverOpen]);
  index2(() => {
    if (!open) {
      pointerTypeRef.current = void 0;
      cleanupMouseMoveHandler();
      clearPointerEvents();
    }
  }, [open, cleanupMouseMoveHandler, clearPointerEvents]);
  React50.useEffect(() => {
    return () => {
      cleanupMouseMoveHandler();
      clearTimeout(timeoutRef.current);
      clearTimeout(restTimeoutRef.current);
      clearPointerEvents();
    };
  }, [enabled, cleanupMouseMoveHandler, clearPointerEvents]);
  return React50.useMemo(() => {
    if (!enabled) {
      return {};
    }
    function setPointerRef(event) {
      pointerTypeRef.current = event.pointerType;
    }
    return {
      reference: {
        onPointerDown: setPointerRef,
        onPointerEnter: setPointerRef,
        onMouseMove() {
          if (open || restMs === 0) {
            return;
          }
          clearTimeout(restTimeoutRef.current);
          restTimeoutRef.current = setTimeout(() => {
            if (!blockMouseMoveRef.current) {
              onOpenChange(true);
            }
          }, restMs);
        }
      },
      floating: {
        onMouseEnter() {
          clearTimeout(timeoutRef.current);
        },
        onMouseLeave() {
          events.emit("dismiss", {
            type: "mouseLeave",
            data: {
              returnFocus: false
            }
          });
          closeWithDelay(false);
        }
      }
    };
  }, [events, enabled, restMs, open, onOpenChange, closeWithDelay]);
};
var FloatingDelayGroupContext = React50.createContext({
  delay: 0,
  initialDelay: 0,
  timeoutMs: 0,
  currentId: null,
  setCurrentId: () => {
  },
  setState: () => {
  },
  isInstantPhase: false
});
var useDelayGroupContext = () => React50.useContext(FloatingDelayGroupContext);
var FloatingDelayGroup = (_ref) => {
  let {
    children,
    delay,
    timeoutMs = 0
  } = _ref;
  const [state, setState] = React50.useReducer((prev2, next2) => ({
    ...prev2,
    ...next2
  }), {
    delay,
    timeoutMs,
    initialDelay: delay,
    currentId: null,
    isInstantPhase: false
  });
  const initialCurrentIdRef = React50.useRef(null);
  const setCurrentId = React50.useCallback((currentId) => {
    setState({
      currentId
    });
  }, []);
  index2(() => {
    if (state.currentId) {
      if (initialCurrentIdRef.current === null) {
        initialCurrentIdRef.current = state.currentId;
      } else {
        setState({
          isInstantPhase: true
        });
      }
    } else {
      setState({
        isInstantPhase: false
      });
      initialCurrentIdRef.current = null;
    }
  }, [state.currentId]);
  return React50.createElement(FloatingDelayGroupContext.Provider, {
    value: React50.useMemo(() => ({
      ...state,
      setState,
      setCurrentId
    }), [state, setState, setCurrentId])
  }, children);
};
var useDelayGroup = (_ref2, _ref3) => {
  let {
    open,
    onOpenChange
  } = _ref2;
  let {
    id
  } = _ref3;
  const {
    currentId,
    setCurrentId,
    initialDelay,
    setState,
    timeoutMs
  } = useDelayGroupContext();
  React50.useEffect(() => {
    if (currentId) {
      setState({
        delay: {
          open: 1,
          close: getDelay(initialDelay, "close")
        }
      });
      if (currentId !== id) {
        onOpenChange(false);
      }
    }
  }, [id, onOpenChange, setState, currentId, initialDelay]);
  React50.useEffect(() => {
    function unset() {
      onOpenChange(false);
      setState({
        delay: initialDelay,
        currentId: null
      });
    }
    if (!open && currentId === id) {
      if (timeoutMs) {
        const timeout = window.setTimeout(unset, timeoutMs);
        return () => {
          clearTimeout(timeout);
        };
      } else {
        unset();
      }
    }
  }, [open, setState, currentId, id, onOpenChange, initialDelay, timeoutMs]);
  React50.useEffect(() => {
    if (open) {
      setCurrentId(id);
    }
  }, [open, setCurrentId, id]);
};
function _extends2() {
  _extends2 = Object.assign || function(target) {
    for (var i = 1; i < arguments.length; i++) {
      var source = arguments[i];
      for (var key in source) {
        if (Object.prototype.hasOwnProperty.call(source, key)) {
          target[key] = source[key];
        }
      }
    }
    return target;
  };
  return _extends2.apply(this, arguments);
}
function activeElement$1(doc) {
  let activeElement2 = doc.activeElement;
  while (((_activeElement = activeElement2) == null ? void 0 : (_activeElement$shadow = _activeElement.shadowRoot) == null ? void 0 : _activeElement$shadow.activeElement) != null) {
    var _activeElement, _activeElement$shadow;
    activeElement2 = activeElement2.shadowRoot.activeElement;
  }
  return activeElement2;
}
function contains(parent, child) {
  if (!parent || !child) {
    return false;
  }
  const rootNode = child.getRootNode && child.getRootNode();
  if (parent.contains(child)) {
    return true;
  } else if (rootNode && isShadowRoot2(rootNode)) {
    let next2 = child;
    do {
      if (next2 && parent === next2) {
        return true;
      }
      next2 = next2.parentNode || next2.host;
    } while (next2);
  }
  return false;
}
function getChildren(nodes, id) {
  let allChildren = nodes.filter((node2) => {
    var _node$context;
    return node2.parentId === id && ((_node$context = node2.context) == null ? void 0 : _node$context.open);
  }) || [];
  let currentChildren = allChildren;
  while (currentChildren.length) {
    currentChildren = nodes.filter((node2) => {
      var _currentChildren;
      return (_currentChildren = currentChildren) == null ? void 0 : _currentChildren.some((n2) => {
        var _node$context2;
        return node2.parentId === n2.id && ((_node$context2 = node2.context) == null ? void 0 : _node$context2.open);
      });
    }) || [];
    allChildren = allChildren.concat(currentChildren);
  }
  return allChildren;
}
function getTarget(event) {
  if ("composedPath" in event) {
    return event.composedPath()[0];
  }
  return event.target;
}
var useInsertionEffect3 = React50["useInsertionEffect".toString()];
var useSafeInsertionEffect = useInsertionEffect3 || ((fn5) => fn5());
function useEvent(callback) {
  const ref = React50.useRef(() => {
    if (true) {
      throw new Error("Cannot call an event handler while rendering.");
    }
  });
  useSafeInsertionEffect(() => {
    ref.current = callback;
  });
  return React50.useCallback(function() {
    for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
      args[_key] = arguments[_key];
    }
    return ref.current == null ? void 0 : ref.current(...args);
  }, []);
}
var HIDDEN_STYLES = {
  border: 0,
  clip: "rect(0 0 0 0)",
  height: "1px",
  margin: "-1px",
  overflow: "hidden",
  padding: 0,
  position: "fixed",
  whiteSpace: "nowrap",
  width: "1px",
  top: 0,
  left: 0
};
var activeElement;
var timeoutId;
function setActiveElementOnTab(event) {
  if (event.key === "Tab") {
    activeElement = event.target;
    clearTimeout(timeoutId);
  }
}
function isTabFocus(event) {
  const result = activeElement === event.relatedTarget;
  activeElement = event.relatedTarget;
  clearTimeout(timeoutId);
  return result;
}
var FocusGuard = React50.forwardRef(function FocusGuard2(props, ref) {
  const onFocus = useEvent(props.onFocus);
  const [role, setRole] = React50.useState();
  index2(() => {
    if (isSafari()) {
      setRole("button");
    }
    document.addEventListener("keydown", setActiveElementOnTab);
    return () => {
      document.removeEventListener("keydown", setActiveElementOnTab);
    };
  }, []);
  return React50.createElement("span", _extends2({}, props, {
    ref,
    tabIndex: 0,
    role,
    "aria-hidden": role ? void 0 : true,
    "data-floating-ui-focus-guard": "",
    style: HIDDEN_STYLES,
    onFocus: (event) => {
      if (isSafari() && isMac() && !isTabFocus(event)) {
        event.persist();
        timeoutId = window.setTimeout(() => {
          onFocus(event);
        }, 50);
      } else {
        onFocus(event);
      }
    }
  }));
});
var PortalContext = React50.createContext(null);
var VisuallyHiddenDismiss = React50.forwardRef(function VisuallyHiddenDismiss2(props, ref) {
  return React50.createElement("button", _extends2({}, props, {
    type: "button",
    ref,
    tabIndex: -1,
    style: HIDDEN_STYLES
  }));
});
var identifier2 = "data-floating-ui-scroll-lock";
var FloatingOverlay = React50.forwardRef(function FloatingOverlay2(_ref, ref) {
  let {
    lockScroll = false,
    ...rest
  } = _ref;
  index2(() => {
    var _window$visualViewpor, _window$visualViewpor2;
    if (!lockScroll) {
      return;
    }
    const alreadyLocked = document.body.hasAttribute(identifier2);
    if (alreadyLocked) {
      return;
    }
    document.body.setAttribute(identifier2, "");
    const scrollbarX = Math.round(document.documentElement.getBoundingClientRect().left) + document.documentElement.scrollLeft;
    const paddingProp = scrollbarX ? "paddingLeft" : "paddingRight";
    const scrollbarWidth = window.innerWidth - document.documentElement.clientWidth;
    if (!/iP(hone|ad|od)|iOS/.test(getPlatform())) {
      Object.assign(document.body.style, {
        overflow: "hidden",
        [paddingProp]: scrollbarWidth + "px"
      });
      return () => {
        document.body.removeAttribute(identifier2);
        Object.assign(document.body.style, {
          overflow: "",
          [paddingProp]: ""
        });
      };
    }
    const offsetLeft = ((_window$visualViewpor = window.visualViewport) == null ? void 0 : _window$visualViewpor.offsetLeft) || 0;
    const offsetTop = ((_window$visualViewpor2 = window.visualViewport) == null ? void 0 : _window$visualViewpor2.offsetTop) || 0;
    const scrollX = window.pageXOffset;
    const scrollY = window.pageYOffset;
    Object.assign(document.body.style, {
      position: "fixed",
      overflow: "hidden",
      top: -(scrollY - Math.floor(offsetTop)) + "px",
      left: -(scrollX - Math.floor(offsetLeft)) + "px",
      right: "0",
      [paddingProp]: scrollbarWidth + "px"
    });
    return () => {
      Object.assign(document.body.style, {
        position: "",
        overflow: "",
        top: "",
        left: "",
        right: "",
        [paddingProp]: ""
      });
      document.body.removeAttribute(identifier2);
      window.scrollTo(scrollX, scrollY);
    };
  }, [lockScroll]);
  return React50.createElement("div", _extends2({
    ref
  }, rest, {
    style: {
      position: "fixed",
      overflow: "auto",
      top: 0,
      right: 0,
      bottom: 0,
      left: 0,
      ...rest.style
    }
  }));
});
function isEventTargetWithin(event, node2) {
  if (node2 == null) {
    return false;
  }
  if ("composedPath" in event) {
    return event.composedPath().includes(node2);
  }
  const e = event;
  return e.target != null && node2.contains(e.target);
}
var bubbleHandlerKeys = {
  pointerdown: "onPointerDown",
  mousedown: "onMouseDown",
  click: "onClick"
};
var captureHandlerKeys = {
  pointerdown: "onPointerDownCapture",
  mousedown: "onMouseDownCapture",
  click: "onClickCapture"
};
var normalizeBubblesProp = function(bubbles) {
  var _bubbles$escapeKey, _bubbles$outsidePress;
  if (bubbles === void 0) {
    bubbles = true;
  }
  return {
    escapeKeyBubbles: typeof bubbles === "boolean" ? bubbles : (_bubbles$escapeKey = bubbles.escapeKey) != null ? _bubbles$escapeKey : true,
    outsidePressBubbles: typeof bubbles === "boolean" ? bubbles : (_bubbles$outsidePress = bubbles.outsidePress) != null ? _bubbles$outsidePress : true
  };
};
var useDismiss = function(_ref, _temp) {
  let {
    open,
    onOpenChange,
    events,
    nodeId,
    elements: {
      reference: reference2,
      domReference,
      floating
    },
    dataRef
  } = _ref;
  let {
    enabled = true,
    escapeKey = true,
    outsidePress: unstable_outsidePress = true,
    outsidePressEvent = "pointerdown",
    referencePress = false,
    referencePressEvent = "pointerdown",
    ancestorScroll = false,
    bubbles = true
  } = _temp === void 0 ? {} : _temp;
  const tree = useFloatingTree();
  const nested = useFloatingParentNodeId() != null;
  const outsidePressFn = useEvent(typeof unstable_outsidePress === "function" ? unstable_outsidePress : () => false);
  const outsidePress = typeof unstable_outsidePress === "function" ? outsidePressFn : unstable_outsidePress;
  const insideReactTreeRef = React50.useRef(false);
  const {
    escapeKeyBubbles,
    outsidePressBubbles
  } = normalizeBubblesProp(bubbles);
  React50.useEffect(() => {
    if (!open || !enabled) {
      return;
    }
    dataRef.current.__escapeKeyBubbles = escapeKeyBubbles;
    dataRef.current.__outsidePressBubbles = outsidePressBubbles;
    function onKeyDown(event) {
      if (event.key === "Escape") {
        const children = tree ? getChildren(tree.nodesRef.current, nodeId) : [];
        if (children.length > 0) {
          let shouldDismiss = true;
          children.forEach((child) => {
            var _child$context;
            if ((_child$context = child.context) != null && _child$context.open && !child.context.dataRef.current.__escapeKeyBubbles) {
              shouldDismiss = false;
              return;
            }
          });
          if (!shouldDismiss) {
            return;
          }
        }
        events.emit("dismiss", {
          type: "escapeKey",
          data: {
            returnFocus: {
              preventScroll: false
            }
          }
        });
        onOpenChange(false);
      }
    }
    function onOutsidePress(event) {
      const insideReactTree = insideReactTreeRef.current;
      insideReactTreeRef.current = false;
      if (insideReactTree) {
        return;
      }
      if (typeof outsidePress === "function" && !outsidePress(event)) {
        return;
      }
      const target = getTarget(event);
      if (isHTMLElement2(target) && floating) {
        const win = floating.ownerDocument.defaultView || window;
        const canScrollX = target.scrollWidth > target.clientWidth;
        const canScrollY = target.scrollHeight > target.clientHeight;
        let xCond = canScrollY && event.offsetX > target.clientWidth;
        if (canScrollY) {
          const isRTL2 = win.getComputedStyle(target).direction === "rtl";
          if (isRTL2) {
            xCond = event.offsetX <= target.offsetWidth - target.clientWidth;
          }
        }
        if (xCond || canScrollX && event.offsetY > target.clientHeight) {
          return;
        }
      }
      const targetIsInsideChildren = tree && getChildren(tree.nodesRef.current, nodeId).some((node2) => {
        var _node$context;
        return isEventTargetWithin(event, (_node$context = node2.context) == null ? void 0 : _node$context.elements.floating);
      });
      if (isEventTargetWithin(event, floating) || isEventTargetWithin(event, domReference) || targetIsInsideChildren) {
        return;
      }
      const children = tree ? getChildren(tree.nodesRef.current, nodeId) : [];
      if (children.length > 0) {
        let shouldDismiss = true;
        children.forEach((child) => {
          var _child$context2;
          if ((_child$context2 = child.context) != null && _child$context2.open && !child.context.dataRef.current.__outsidePressBubbles) {
            shouldDismiss = false;
            return;
          }
        });
        if (!shouldDismiss) {
          return;
        }
      }
      events.emit("dismiss", {
        type: "outsidePress",
        data: {
          returnFocus: nested ? {
            preventScroll: true
          } : isVirtualClick(event) || isVirtualPointerEvent(event)
        }
      });
      onOpenChange(false);
    }
    function onScroll() {
      onOpenChange(false);
    }
    const doc = getDocument(floating);
    escapeKey && doc.addEventListener("keydown", onKeyDown);
    outsidePress && doc.addEventListener(outsidePressEvent, onOutsidePress);
    let ancestors = [];
    if (ancestorScroll) {
      if (isElement3(domReference)) {
        ancestors = getOverflowAncestors(domReference);
      }
      if (isElement3(floating)) {
        ancestors = ancestors.concat(getOverflowAncestors(floating));
      }
      if (!isElement3(reference2) && reference2 && reference2.contextElement) {
        ancestors = ancestors.concat(getOverflowAncestors(reference2.contextElement));
      }
    }
    ancestors = ancestors.filter((ancestor) => {
      var _doc$defaultView;
      return ancestor !== ((_doc$defaultView = doc.defaultView) == null ? void 0 : _doc$defaultView.visualViewport);
    });
    ancestors.forEach((ancestor) => {
      ancestor.addEventListener("scroll", onScroll, {
        passive: true
      });
    });
    return () => {
      escapeKey && doc.removeEventListener("keydown", onKeyDown);
      outsidePress && doc.removeEventListener(outsidePressEvent, onOutsidePress);
      ancestors.forEach((ancestor) => {
        ancestor.removeEventListener("scroll", onScroll);
      });
    };
  }, [dataRef, floating, domReference, reference2, escapeKey, outsidePress, outsidePressEvent, events, tree, nodeId, open, onOpenChange, ancestorScroll, enabled, escapeKeyBubbles, outsidePressBubbles, nested]);
  React50.useEffect(() => {
    insideReactTreeRef.current = false;
  }, [outsidePress, outsidePressEvent]);
  return React50.useMemo(() => {
    if (!enabled) {
      return {};
    }
    return {
      reference: {
        [bubbleHandlerKeys[referencePressEvent]]: () => {
          if (referencePress) {
            events.emit("dismiss", {
              type: "referencePress",
              data: {
                returnFocus: false
              }
            });
            onOpenChange(false);
          }
        }
      },
      floating: {
        [captureHandlerKeys[outsidePressEvent]]: () => {
          insideReactTreeRef.current = true;
        }
      }
    };
  }, [enabled, events, referencePress, outsidePressEvent, referencePressEvent, onOpenChange]);
};
var useFocus = function(_ref, _temp) {
  let {
    open,
    onOpenChange,
    dataRef,
    events,
    refs,
    elements: {
      floating,
      domReference
    }
  } = _ref;
  let {
    enabled = true,
    keyboardOnly = true
  } = _temp === void 0 ? {} : _temp;
  const pointerTypeRef = React50.useRef("");
  const blockFocusRef = React50.useRef(false);
  const timeoutRef = React50.useRef();
  React50.useEffect(() => {
    if (!enabled) {
      return;
    }
    const doc = getDocument(floating);
    const win = doc.defaultView || window;
    function onBlur() {
      if (!open && isHTMLElement2(domReference) && domReference === activeElement$1(getDocument(domReference))) {
        blockFocusRef.current = true;
      }
    }
    win.addEventListener("blur", onBlur);
    return () => {
      win.removeEventListener("blur", onBlur);
    };
  }, [floating, domReference, open, enabled]);
  React50.useEffect(() => {
    if (!enabled) {
      return;
    }
    function onDismiss(payload) {
      if (payload.type === "referencePress" || payload.type === "escapeKey") {
        blockFocusRef.current = true;
      }
    }
    events.on("dismiss", onDismiss);
    return () => {
      events.off("dismiss", onDismiss);
    };
  }, [events, enabled]);
  React50.useEffect(() => {
    return () => {
      clearTimeout(timeoutRef.current);
    };
  }, []);
  return React50.useMemo(() => {
    if (!enabled) {
      return {};
    }
    return {
      reference: {
        onPointerDown(_ref2) {
          let {
            pointerType
          } = _ref2;
          pointerTypeRef.current = pointerType;
          blockFocusRef.current = !!(pointerType && keyboardOnly);
        },
        onMouseLeave() {
          blockFocusRef.current = false;
        },
        onFocus(event) {
          var _dataRef$current$open;
          if (blockFocusRef.current) {
            return;
          }
          if (event.type === "focus" && ((_dataRef$current$open = dataRef.current.openEvent) == null ? void 0 : _dataRef$current$open.type) === "mousedown" && dataRef.current.openEvent && isEventTargetWithin(dataRef.current.openEvent, domReference)) {
            return;
          }
          dataRef.current.openEvent = event.nativeEvent;
          onOpenChange(true);
        },
        onBlur(event) {
          blockFocusRef.current = false;
          const relatedTarget = event.relatedTarget;
          const movedToFocusGuard = isElement3(relatedTarget) && relatedTarget.hasAttribute("data-floating-ui-focus-guard") && relatedTarget.getAttribute("data-type") === "outside";
          timeoutRef.current = setTimeout(() => {
            if (contains(refs.floating.current, relatedTarget) || contains(domReference, relatedTarget) || movedToFocusGuard) {
              return;
            }
            onOpenChange(false);
          });
        }
      }
    };
  }, [enabled, keyboardOnly, domReference, refs, dataRef, onOpenChange]);
};
var useRole = function(_ref, _temp) {
  let {
    open
  } = _ref;
  let {
    enabled = true,
    role = "dialog"
  } = _temp === void 0 ? {} : _temp;
  const rootId = useId2();
  const referenceId = useId2();
  return React50.useMemo(() => {
    const floatingProps = {
      id: rootId,
      role
    };
    if (!enabled) {
      return {};
    }
    if (role === "tooltip") {
      return {
        reference: {
          "aria-describedby": open ? rootId : void 0
        },
        floating: floatingProps
      };
    }
    return {
      reference: {
        "aria-expanded": open ? "true" : "false",
        "aria-haspopup": role === "alertdialog" ? "dialog" : role,
        "aria-controls": open ? rootId : void 0,
        ...role === "listbox" && {
          role: "combobox"
        },
        ...role === "menu" && {
          id: referenceId
        }
      },
      floating: {
        ...floatingProps,
        ...role === "menu" && {
          "aria-labelledby": referenceId
        }
      }
    };
  }, [enabled, role, open, rootId, referenceId]);
};
function useFloating2(options) {
  if (options === void 0) {
    options = {};
  }
  const {
    open = false,
    onOpenChange: unstable_onOpenChange,
    nodeId
  } = options;
  const position2 = useFloating(options);
  const tree = useFloatingTree();
  const domReferenceRef = React50.useRef(null);
  const dataRef = React50.useRef({});
  const events = React50.useState(() => createPubSub())[0];
  const [domReference, setDomReference] = React50.useState(null);
  const setPositionReference = React50.useCallback((node2) => {
    const positionReference = isElement3(node2) ? {
      getBoundingClientRect: () => node2.getBoundingClientRect(),
      contextElement: node2
    } : node2;
    position2.refs.setReference(positionReference);
  }, [position2.refs]);
  const setReference = React50.useCallback((node2) => {
    if (isElement3(node2) || node2 === null) {
      domReferenceRef.current = node2;
      setDomReference(node2);
    }
    if (isElement3(position2.refs.reference.current) || position2.refs.reference.current === null || // Don't allow setting virtual elements using the old technique back to
    // `null` to support `positionReference` + an unstable `reference`
    // callback ref.
    node2 !== null && !isElement3(node2)) {
      position2.refs.setReference(node2);
    }
  }, [position2.refs]);
  const refs = React50.useMemo(() => ({
    ...position2.refs,
    setReference,
    setPositionReference,
    domReference: domReferenceRef
  }), [position2.refs, setReference, setPositionReference]);
  const elements = React50.useMemo(() => ({
    ...position2.elements,
    domReference
  }), [position2.elements, domReference]);
  const onOpenChange = useEvent(unstable_onOpenChange);
  const context = React50.useMemo(() => ({
    ...position2,
    refs,
    elements,
    dataRef,
    nodeId,
    events,
    open,
    onOpenChange
  }), [position2, nodeId, events, open, onOpenChange, refs, elements]);
  index2(() => {
    const node2 = tree == null ? void 0 : tree.nodesRef.current.find((node3) => node3.id === nodeId);
    if (node2) {
      node2.context = context;
    }
  });
  return React50.useMemo(() => ({
    ...position2,
    context,
    refs,
    reference: setReference,
    positionReference: setPositionReference
  }), [position2, refs, context, setReference, setPositionReference]);
}
function mergeProps(userProps, propsList, elementKey) {
  const map = /* @__PURE__ */ new Map();
  return {
    ...elementKey === "floating" && {
      tabIndex: -1
    },
    ...userProps,
    ...propsList.map((value) => value ? value[elementKey] : null).concat(userProps).reduce((acc, props) => {
      if (!props) {
        return acc;
      }
      Object.entries(props).forEach((_ref) => {
        let [key, value] = _ref;
        if (key.indexOf("on") === 0) {
          if (!map.has(key)) {
            map.set(key, []);
          }
          if (typeof value === "function") {
            var _map$get;
            (_map$get = map.get(key)) == null ? void 0 : _map$get.push(value);
            acc[key] = function() {
              var _map$get2;
              for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
                args[_key] = arguments[_key];
              }
              (_map$get2 = map.get(key)) == null ? void 0 : _map$get2.forEach((fn5) => fn5(...args));
            };
          }
        } else {
          acc[key] = value;
        }
      });
      return acc;
    }, {})
  };
}
var useInteractions = function(propsList) {
  if (propsList === void 0) {
    propsList = [];
  }
  const deps = propsList;
  const getReferenceProps = React50.useCallback(
    (userProps) => mergeProps(userProps, propsList, "reference"),
    // eslint-disable-next-line react-hooks/exhaustive-deps
    deps
  );
  const getFloatingProps = React50.useCallback(
    (userProps) => mergeProps(userProps, propsList, "floating"),
    // eslint-disable-next-line react-hooks/exhaustive-deps
    deps
  );
  const getItemProps = React50.useCallback(
    (userProps) => mergeProps(userProps, propsList, "item"),
    // Granularly check for `item` changes, because the `getItemProps` getter
    // should be as referentially stable as possible since it may be passed as
    // a prop to many components. All `item` key values must therefore be
    // memoized.
    // eslint-disable-next-line react-hooks/exhaustive-deps
    propsList.map((key) => key == null ? void 0 : key.item)
  );
  return React50.useMemo(() => ({
    getReferenceProps,
    getFloatingProps,
    getItemProps
  }), [getReferenceProps, getFloatingProps, getItemProps]);
};

// node_modules/@mantine/core/esm/Floating/use-floating-auto-update.js
var import_react118 = __toESM(require_react());
function useFloatingAutoUpdate({ opened, floating, positionDependencies }) {
  const [delayedUpdate, setDelayedUpdate] = (0, import_react118.useState)(0);
  (0, import_react118.useEffect)(() => {
    if (floating.refs.reference.current && floating.refs.floating.current) {
      return autoUpdate(floating.refs.reference.current, floating.refs.floating.current, floating.update);
    }
    return void 0;
  }, [floating.refs.reference.current, floating.refs.floating.current, opened, delayedUpdate]);
  useDidUpdate(() => {
    floating.update();
  }, positionDependencies);
  useDidUpdate(() => {
    setDelayedUpdate((c) => c + 1);
  }, [opened]);
}

// node_modules/@mantine/core/esm/Popover/use-popover.js
function getPopoverMiddlewares(options) {
  const middlewares = [offset(options.offset)];
  if (options.middlewares.shift) {
    middlewares.push(shift({ limiter: limitShift() }));
  }
  if (options.middlewares.flip) {
    middlewares.push(flip());
  }
  if (options.middlewares.inline) {
    middlewares.push(inline());
  }
  middlewares.push(arrow2({ element: options.arrowRef, padding: options.arrowOffset }));
  return middlewares;
}
function usePopover(options) {
  const [_opened, setOpened] = useUncontrolled({
    value: options.opened,
    defaultValue: options.defaultOpened,
    finalValue: false,
    onChange: options.onChange
  });
  const onClose = () => {
    var _a;
    (_a = options.onClose) == null ? void 0 : _a.call(options);
    setOpened(false);
  };
  const onToggle = () => {
    var _a, _b;
    if (_opened) {
      (_a = options.onClose) == null ? void 0 : _a.call(options);
      setOpened(false);
    } else {
      (_b = options.onOpen) == null ? void 0 : _b.call(options);
      setOpened(true);
    }
  };
  const floating = useFloating2({
    placement: options.position,
    middleware: [
      ...getPopoverMiddlewares(options),
      ...options.width === "target" ? [
        size2({
          apply({ rects }) {
            var _a, _b;
            Object.assign((_b = (_a = floating.refs.floating.current) == null ? void 0 : _a.style) != null ? _b : {}, {
              width: `${rects.reference.width}px`
            });
          }
        })
      ] : []
    ]
  });
  useFloatingAutoUpdate({
    opened: options.opened,
    positionDependencies: options.positionDependencies,
    floating
  });
  useDidUpdate(() => {
    var _a;
    (_a = options.onPositionChange) == null ? void 0 : _a.call(options, floating.placement);
  }, [floating.placement]);
  return {
    floating,
    controlled: typeof options.opened === "boolean",
    opened: _opened,
    onClose,
    onToggle
  };
}

// node_modules/@mantine/core/esm/Popover/Popover.errors.js
var POPOVER_ERRORS = {
  context: "Popover component was not found in the tree",
  children: "Popover.Target component children should be an element or a component that accepts ref, fragments, strings, numbers and other primitive values are not supported"
};

// node_modules/@mantine/core/esm/Popover/Popover.context.js
var [PopoverContextProvider, usePopoverContext] = createSafeContext(POPOVER_ERRORS.context);

// node_modules/@mantine/core/esm/Popover/PopoverTarget/PopoverTarget.js
var import_react121 = __toESM(require_react());
var __defProp55 = Object.defineProperty;
var __defProps23 = Object.defineProperties;
var __getOwnPropDescs23 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols56 = Object.getOwnPropertySymbols;
var __hasOwnProp56 = Object.prototype.hasOwnProperty;
var __propIsEnum56 = Object.prototype.propertyIsEnumerable;
var __defNormalProp55 = (obj, key, value) => key in obj ? __defProp55(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues55 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp56.call(b2, prop))
      __defNormalProp55(a2, prop, b2[prop]);
  if (__getOwnPropSymbols56)
    for (var prop of __getOwnPropSymbols56(b2)) {
      if (__propIsEnum56.call(b2, prop))
        __defNormalProp55(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps23 = (a2, b2) => __defProps23(a2, __getOwnPropDescs23(b2));
var __objRest31 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp56.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols56)
    for (var prop of __getOwnPropSymbols56(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum56.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps20 = {
  refProp: "ref",
  popupType: "dialog"
};
var PopoverTarget = (0, import_react121.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("PopoverTarget", defaultProps20, props), { children, refProp, popupType } = _a, others = __objRest31(_a, ["children", "refProp", "popupType"]);
  if (!isElement(children)) {
    throw new Error(POPOVER_ERRORS.children);
  }
  const forwardedProps = others;
  const ctx = usePopoverContext();
  const targetRef = useMergedRef(ctx.reference, children.ref, ref);
  const accessibleProps = ctx.withRoles ? {
    "aria-haspopup": popupType,
    "aria-expanded": ctx.opened,
    "aria-controls": ctx.getDropdownId(),
    id: ctx.getTargetId()
  } : {};
  return (0, import_react121.cloneElement)(children, __spreadValues55(__spreadProps23(__spreadValues55(__spreadValues55(__spreadValues55({}, forwardedProps), accessibleProps), ctx.targetProps), {
    className: clsx_m_default(ctx.targetProps.className, forwardedProps.className, children.props.className),
    [refProp]: targetRef
  }), !ctx.controlled ? { onClick: ctx.onToggle } : null));
});
PopoverTarget.displayName = "@mantine/core/PopoverTarget";

// node_modules/@mantine/core/esm/Popover/PopoverDropdown/PopoverDropdown.js
var import_react126 = __toESM(require_react());

// node_modules/@mantine/core/esm/Popover/PopoverDropdown/PopoverDropdown.styles.js
var useStyles17 = createStyles((theme, { radius: radius2, shadow }) => ({
  dropdown: {
    position: "absolute",
    backgroundColor: theme.white,
    background: theme.colorScheme === "dark" ? theme.colors.dark[6] : theme.white,
    border: `1px solid ${theme.colorScheme === "dark" ? theme.colors.dark[4] : theme.colors.gray[2]}`,
    padding: `${theme.spacing.sm}px ${theme.spacing.md}px`,
    boxShadow: theme.shadows[shadow] || shadow || "none",
    borderRadius: theme.fn.radius(radius2),
    "&:focus": {
      outline: 0
    }
  },
  arrow: {
    backgroundColor: "inherit",
    border: `1px solid ${theme.colorScheme === "dark" ? theme.colors.dark[4] : theme.colors.gray[2]}`,
    zIndex: 1
  }
}));
var PopoverDropdown_styles_default = useStyles17;

// node_modules/@mantine/core/esm/Transition/Transition.js
var import_react123 = __toESM(require_react());

// node_modules/@mantine/core/esm/Transition/get-transition-styles/get-transition-styles.js
var __defProp56 = Object.defineProperty;
var __getOwnPropSymbols57 = Object.getOwnPropertySymbols;
var __hasOwnProp57 = Object.prototype.hasOwnProperty;
var __propIsEnum57 = Object.prototype.propertyIsEnumerable;
var __defNormalProp56 = (obj, key, value) => key in obj ? __defProp56(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues56 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp57.call(b2, prop))
      __defNormalProp56(a2, prop, b2[prop]);
  if (__getOwnPropSymbols57)
    for (var prop of __getOwnPropSymbols57(b2)) {
      if (__propIsEnum57.call(b2, prop))
        __defNormalProp56(a2, prop, b2[prop]);
    }
  return a2;
};
var transitionStatuses = {
  entering: "in",
  entered: "in",
  exiting: "out",
  exited: "out",
  "pre-exiting": "out",
  "pre-entering": "out"
};
function getTransitionStyles({
  transition,
  state,
  duration,
  timingFunction
}) {
  const shared = {
    transitionDuration: `${duration}ms`,
    transitionTimingFunction: timingFunction
  };
  if (typeof transition === "string") {
    if (!(transition in transitions)) {
      return null;
    }
    return __spreadValues56(__spreadValues56(__spreadValues56({
      transitionProperty: transitions[transition].transitionProperty
    }, shared), transitions[transition].common), transitions[transition][transitionStatuses[state]]);
  }
  return __spreadValues56(__spreadValues56(__spreadValues56({
    transitionProperty: transition.transitionProperty
  }, shared), transition.common), transition[transitionStatuses[state]]);
}

// node_modules/@mantine/core/esm/Transition/use-transition.js
var import_react122 = __toESM(require_react());
function useTransition({
  duration,
  exitDuration,
  timingFunction,
  mounted,
  onEnter,
  onExit,
  onEntered,
  onExited
}) {
  const theme = useMantineTheme();
  const shouldReduceMotion = useReducedMotion();
  const reduceMotion = theme.respectReducedMotion ? shouldReduceMotion : false;
  const [transitionStatus, setStatus] = (0, import_react122.useState)(mounted ? "entered" : "exited");
  let transitionDuration = reduceMotion ? 0 : duration;
  const timeoutRef = (0, import_react122.useRef)(-1);
  const handleStateChange = (shouldMount) => {
    const preHandler = shouldMount ? onEnter : onExit;
    const handler = shouldMount ? onEntered : onExited;
    setStatus(shouldMount ? "pre-entering" : "pre-exiting");
    window.clearTimeout(timeoutRef.current);
    transitionDuration = reduceMotion ? 0 : shouldMount ? duration : exitDuration;
    if (transitionDuration === 0) {
      typeof preHandler === "function" && preHandler();
      typeof handler === "function" && handler();
      setStatus(shouldMount ? "entered" : "exited");
    } else {
      const preStateTimeout = window.setTimeout(() => {
        typeof preHandler === "function" && preHandler();
        setStatus(shouldMount ? "entering" : "exiting");
      }, 10);
      timeoutRef.current = window.setTimeout(() => {
        window.clearTimeout(preStateTimeout);
        typeof handler === "function" && handler();
        setStatus(shouldMount ? "entered" : "exited");
      }, transitionDuration);
    }
  };
  useDidUpdate(() => {
    handleStateChange(mounted);
  }, [mounted]);
  (0, import_react122.useEffect)(() => () => window.clearTimeout(timeoutRef.current), []);
  return {
    transitionDuration,
    transitionStatus,
    transitionTimingFunction: timingFunction || theme.transitionTimingFunction
  };
}

// node_modules/@mantine/core/esm/Transition/Transition.js
function Transition({
  transition,
  duration = 250,
  exitDuration = duration,
  mounted,
  children,
  timingFunction,
  onExit,
  onEntered,
  onEnter,
  onExited
}) {
  const { transitionDuration, transitionStatus, transitionTimingFunction } = useTransition({
    mounted,
    exitDuration,
    duration,
    timingFunction,
    onExit,
    onEntered,
    onEnter,
    onExited
  });
  if (transitionDuration === 0) {
    return mounted ? import_react123.default.createElement(import_react123.default.Fragment, null, children({})) : null;
  }
  return transitionStatus === "exited" ? null : import_react123.default.createElement(import_react123.default.Fragment, null, children(getTransitionStyles({
    transition,
    duration: transitionDuration,
    state: transitionStatus,
    timingFunction: transitionTimingFunction
  })));
}
Transition.displayName = "@mantine/core/Transition";

// node_modules/@mantine/core/esm/FocusTrap/FocusTrap.js
var import_react124 = __toESM(require_react());
function FocusTrap({
  children,
  active = true,
  refProp = "ref"
}) {
  const focusTrapRef = useFocusTrap(active);
  const ref = useMergedRef(focusTrapRef, children == null ? void 0 : children.ref);
  if (!isElement(children)) {
    return children;
  }
  return (0, import_react124.cloneElement)(children, { [refProp]: ref });
}
FocusTrap.displayName = "@mantine/core/FocusTrap";

// node_modules/@mantine/core/esm/Floating/FloatingArrow/FloatingArrow.js
var import_react125 = __toESM(require_react());

// node_modules/@mantine/core/esm/Floating/FloatingArrow/get-arrow-position-styles.js
var __defProp57 = Object.defineProperty;
var __defProps24 = Object.defineProperties;
var __getOwnPropDescs24 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols58 = Object.getOwnPropertySymbols;
var __hasOwnProp58 = Object.prototype.hasOwnProperty;
var __propIsEnum58 = Object.prototype.propertyIsEnumerable;
var __defNormalProp57 = (obj, key, value) => key in obj ? __defProp57(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues57 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp58.call(b2, prop))
      __defNormalProp57(a2, prop, b2[prop]);
  if (__getOwnPropSymbols58)
    for (var prop of __getOwnPropSymbols58(b2)) {
      if (__propIsEnum58.call(b2, prop))
        __defNormalProp57(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps24 = (a2, b2) => __defProps24(a2, __getOwnPropDescs24(b2));
function horizontalSide(placement, arrowY, arrowOffset, arrowPosition) {
  if (placement === "center" || arrowPosition === "center") {
    return { top: arrowY };
  }
  if (placement === "end") {
    return { bottom: arrowOffset };
  }
  if (placement === "start") {
    return { top: arrowOffset };
  }
  return {};
}
function verticalSide(placement, arrowX, arrowOffset, arrowPosition, dir) {
  if (placement === "center" || arrowPosition === "center") {
    return { left: arrowX };
  }
  if (placement === "end") {
    return { [dir === "ltr" ? "right" : "left"]: arrowOffset };
  }
  if (placement === "start") {
    return { [dir === "ltr" ? "left" : "right"]: arrowOffset };
  }
  return {};
}
var radiusByFloatingSide = {
  bottom: "borderTopLeftRadius",
  left: "borderTopRightRadius",
  right: "borderBottomLeftRadius",
  top: "borderBottomRightRadius"
};
function getArrowPositionStyles({
  position: position2,
  withBorder,
  arrowSize,
  arrowOffset,
  arrowRadius,
  arrowPosition,
  arrowX,
  arrowY,
  dir
}) {
  const [side, placement = "center"] = position2.split("-");
  const baseStyles = {
    width: arrowSize,
    height: arrowSize,
    transform: "rotate(45deg)",
    position: "absolute",
    [radiusByFloatingSide[side]]: arrowRadius
  };
  const arrowPlacement = withBorder ? -arrowSize / 2 - 1 : -arrowSize / 2;
  if (side === "left") {
    return __spreadProps24(__spreadValues57(__spreadValues57({}, baseStyles), horizontalSide(placement, arrowY, arrowOffset, arrowPosition)), {
      right: arrowPlacement,
      borderLeft: 0,
      borderBottom: 0
    });
  }
  if (side === "right") {
    return __spreadProps24(__spreadValues57(__spreadValues57({}, baseStyles), horizontalSide(placement, arrowY, arrowOffset, arrowPosition)), {
      left: arrowPlacement,
      borderRight: 0,
      borderTop: 0
    });
  }
  if (side === "top") {
    return __spreadProps24(__spreadValues57(__spreadValues57({}, baseStyles), verticalSide(placement, arrowX, arrowOffset, arrowPosition, dir)), {
      bottom: arrowPlacement,
      borderTop: 0,
      borderLeft: 0
    });
  }
  if (side === "bottom") {
    return __spreadProps24(__spreadValues57(__spreadValues57({}, baseStyles), verticalSide(placement, arrowX, arrowOffset, arrowPosition, dir)), {
      top: arrowPlacement,
      borderBottom: 0,
      borderRight: 0
    });
  }
  return {};
}

// node_modules/@mantine/core/esm/Floating/FloatingArrow/FloatingArrow.js
var __defProp58 = Object.defineProperty;
var __defProps25 = Object.defineProperties;
var __getOwnPropDescs25 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols59 = Object.getOwnPropertySymbols;
var __hasOwnProp59 = Object.prototype.hasOwnProperty;
var __propIsEnum59 = Object.prototype.propertyIsEnumerable;
var __defNormalProp58 = (obj, key, value) => key in obj ? __defProp58(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues58 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp59.call(b2, prop))
      __defNormalProp58(a2, prop, b2[prop]);
  if (__getOwnPropSymbols59)
    for (var prop of __getOwnPropSymbols59(b2)) {
      if (__propIsEnum59.call(b2, prop))
        __defNormalProp58(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps25 = (a2, b2) => __defProps25(a2, __getOwnPropDescs25(b2));
var __objRest32 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp59.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols59)
    for (var prop of __getOwnPropSymbols59(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum59.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var FloatingArrow = (0, import_react125.forwardRef)((_a, ref) => {
  var _b = _a, {
    withBorder,
    position: position2,
    arrowSize,
    arrowOffset,
    arrowRadius,
    arrowPosition,
    visible: visible2,
    arrowX,
    arrowY
  } = _b, others = __objRest32(_b, [
    "withBorder",
    "position",
    "arrowSize",
    "arrowOffset",
    "arrowRadius",
    "arrowPosition",
    "visible",
    "arrowX",
    "arrowY"
  ]);
  const theme = useMantineTheme();
  if (!visible2) {
    return null;
  }
  return import_react125.default.createElement("div", __spreadProps25(__spreadValues58({}, others), {
    ref,
    style: getArrowPositionStyles({
      withBorder,
      position: position2,
      arrowSize,
      arrowOffset,
      arrowRadius,
      arrowPosition,
      dir: theme.dir,
      arrowX,
      arrowY
    })
  }));
});
FloatingArrow.displayName = "@mantine/core/FloatingArrow";

// node_modules/@mantine/core/esm/Popover/PopoverDropdown/PopoverDropdown.js
var __defProp59 = Object.defineProperty;
var __defProps26 = Object.defineProperties;
var __getOwnPropDescs26 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols60 = Object.getOwnPropertySymbols;
var __hasOwnProp60 = Object.prototype.hasOwnProperty;
var __propIsEnum60 = Object.prototype.propertyIsEnumerable;
var __defNormalProp59 = (obj, key, value) => key in obj ? __defProp59(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues59 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp60.call(b2, prop))
      __defNormalProp59(a2, prop, b2[prop]);
  if (__getOwnPropSymbols60)
    for (var prop of __getOwnPropSymbols60(b2)) {
      if (__propIsEnum60.call(b2, prop))
        __defNormalProp59(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps26 = (a2, b2) => __defProps26(a2, __getOwnPropDescs26(b2));
var __objRest33 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp60.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols60)
    for (var prop of __getOwnPropSymbols60(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum60.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps21 = {};
function PopoverDropdown(props) {
  const _a = useComponentDefaultProps("PopoverDropdown", defaultProps21, props), { style, className, children, onKeyDownCapture } = _a, others = __objRest33(_a, ["style", "className", "children", "onKeyDownCapture"]);
  const ctx = usePopoverContext();
  const { classes, cx } = PopoverDropdown_styles_default({ radius: ctx.radius, shadow: ctx.shadow }, {
    name: ctx.__staticSelector,
    classNames: ctx.classNames,
    styles: ctx.styles,
    unstyled: ctx.unstyled
  });
  const returnFocus = useFocusReturn({
    opened: ctx.opened,
    shouldReturnFocus: ctx.returnFocus
  });
  const accessibleProps = ctx.withRoles ? {
    "aria-labelledby": ctx.getTargetId(),
    id: ctx.getDropdownId(),
    role: "dialog"
  } : {};
  if (ctx.disabled) {
    return null;
  }
  return import_react126.default.createElement(OptionalPortal, {
    withinPortal: ctx.withinPortal
  }, import_react126.default.createElement(Transition, {
    mounted: ctx.opened,
    transition: ctx.transition,
    duration: ctx.transitionDuration,
    exitDuration: typeof ctx.exitTransitionDuration === "number" ? ctx.exitTransitionDuration : ctx.transitionDuration
  }, (transitionStyles) => {
    var _a2, _b;
    return import_react126.default.createElement(FocusTrap, {
      active: ctx.trapFocus
    }, import_react126.default.createElement(Box, __spreadValues59(__spreadProps26(__spreadValues59({}, accessibleProps), {
      tabIndex: -1,
      key: ctx.placement,
      ref: ctx.floating,
      style: __spreadProps26(__spreadValues59(__spreadValues59({}, style), transitionStyles), {
        zIndex: ctx.zIndex,
        top: (_a2 = ctx.y) != null ? _a2 : 0,
        left: (_b = ctx.x) != null ? _b : 0,
        width: ctx.width === "target" ? void 0 : ctx.width
      }),
      className: cx(classes.dropdown, className),
      onKeyDownCapture: closeOnEscape(ctx.onClose, {
        active: ctx.closeOnEscape,
        onTrigger: returnFocus,
        onKeyDown: onKeyDownCapture
      }),
      "data-position": ctx.placement
    }), others), children, import_react126.default.createElement(FloatingArrow, {
      ref: ctx.arrowRef,
      arrowX: ctx.arrowX,
      arrowY: ctx.arrowY,
      visible: ctx.withArrow,
      withBorder: true,
      position: ctx.placement,
      arrowSize: ctx.arrowSize,
      arrowRadius: ctx.arrowRadius,
      arrowOffset: ctx.arrowOffset,
      arrowPosition: ctx.arrowPosition,
      className: classes.arrow
    })));
  }));
}
PopoverDropdown.displayName = "@mantine/core/PopoverDropdown";

// node_modules/@mantine/core/esm/Floating/get-floating-position/get-floating-position.js
function getFloatingPosition(dir, position2) {
  if (dir === "rtl" && (position2.includes("right") || position2.includes("left"))) {
    const [side, placement] = position2.split("-");
    const flippedPosition = side === "right" ? "left" : "right";
    return placement === void 0 ? flippedPosition : `${flippedPosition}-${placement}`;
  }
  return position2;
}

// node_modules/@mantine/core/esm/Popover/Popover.js
var __getOwnPropSymbols61 = Object.getOwnPropertySymbols;
var __hasOwnProp61 = Object.prototype.hasOwnProperty;
var __propIsEnum61 = Object.prototype.propertyIsEnumerable;
var __objRest34 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp61.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols61)
    for (var prop of __getOwnPropSymbols61(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum61.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps22 = {
  position: "bottom",
  offset: 8,
  positionDependencies: [],
  transition: "fade",
  transitionDuration: 150,
  middlewares: { flip: true, shift: true, inline: false },
  arrowSize: 7,
  arrowOffset: 5,
  arrowRadius: 0,
  arrowPosition: "side",
  closeOnClickOutside: true,
  withinPortal: false,
  closeOnEscape: true,
  trapFocus: false,
  withRoles: true,
  returnFocus: false,
  clickOutsideEvents: ["mousedown", "touchstart"],
  zIndex: getDefaultZIndex("popover"),
  __staticSelector: "Popover",
  width: "max-content"
};
function Popover(props) {
  var _b, _c, _d, _e2, _f, _g;
  const arrowRef = (0, import_react127.useRef)(null);
  const _a = useComponentDefaultProps("Popover", defaultProps22, props), {
    children,
    position: position2,
    offset: offset3,
    onPositionChange,
    positionDependencies,
    opened,
    transition,
    transitionDuration,
    width,
    middlewares,
    withArrow,
    arrowSize,
    arrowOffset,
    arrowRadius,
    arrowPosition,
    unstyled,
    classNames,
    styles: styles2,
    closeOnClickOutside,
    withinPortal,
    closeOnEscape: closeOnEscape2,
    clickOutsideEvents,
    trapFocus,
    onClose,
    onOpen,
    onChange,
    zIndex,
    radius: radius2,
    shadow,
    id,
    defaultOpened,
    exitTransitionDuration,
    __staticSelector,
    withRoles,
    disabled,
    returnFocus
  } = _a, others = __objRest34(_a, [
    "children",
    "position",
    "offset",
    "onPositionChange",
    "positionDependencies",
    "opened",
    "transition",
    "transitionDuration",
    "width",
    "middlewares",
    "withArrow",
    "arrowSize",
    "arrowOffset",
    "arrowRadius",
    "arrowPosition",
    "unstyled",
    "classNames",
    "styles",
    "closeOnClickOutside",
    "withinPortal",
    "closeOnEscape",
    "clickOutsideEvents",
    "trapFocus",
    "onClose",
    "onOpen",
    "onChange",
    "zIndex",
    "radius",
    "shadow",
    "id",
    "defaultOpened",
    "exitTransitionDuration",
    "__staticSelector",
    "withRoles",
    "disabled",
    "returnFocus"
  ]);
  const [targetNode, setTargetNode] = (0, import_react127.useState)(null);
  const [dropdownNode, setDropdownNode] = (0, import_react127.useState)(null);
  const uid = useId(id);
  const theme = useMantineTheme();
  const popover = usePopover({
    middlewares,
    width,
    position: getFloatingPosition(theme.dir, position2),
    offset: offset3 + (withArrow ? arrowSize / 2 : 0),
    arrowRef,
    arrowOffset,
    onPositionChange,
    positionDependencies,
    opened,
    defaultOpened,
    onChange,
    onOpen,
    onClose
  });
  useClickOutside(() => closeOnClickOutside && popover.onClose(), clickOutsideEvents, [
    targetNode,
    dropdownNode
  ]);
  const reference2 = (0, import_react127.useCallback)((node2) => {
    setTargetNode(node2);
    popover.floating.reference(node2);
  }, [popover.floating.reference]);
  const floating = (0, import_react127.useCallback)((node2) => {
    setDropdownNode(node2);
    popover.floating.floating(node2);
  }, [popover.floating.floating]);
  return import_react127.default.createElement(PopoverContextProvider, {
    value: {
      returnFocus,
      disabled,
      controlled: popover.controlled,
      reference: reference2,
      floating,
      x: popover.floating.x,
      y: popover.floating.y,
      arrowX: (_d = (_c = (_b = popover.floating) == null ? void 0 : _b.middlewareData) == null ? void 0 : _c.arrow) == null ? void 0 : _d.x,
      arrowY: (_g = (_f = (_e2 = popover.floating) == null ? void 0 : _e2.middlewareData) == null ? void 0 : _f.arrow) == null ? void 0 : _g.y,
      opened: popover.opened,
      arrowRef,
      transition,
      transitionDuration,
      exitTransitionDuration,
      width,
      withArrow,
      arrowSize,
      arrowOffset,
      arrowRadius,
      arrowPosition,
      placement: popover.floating.placement,
      trapFocus,
      withinPortal,
      zIndex,
      radius: radius2,
      shadow,
      closeOnEscape: closeOnEscape2,
      onClose: popover.onClose,
      onToggle: popover.onToggle,
      getTargetId: () => `${uid}-target`,
      getDropdownId: () => `${uid}-dropdown`,
      withRoles,
      targetProps: others,
      __staticSelector,
      classNames,
      styles: styles2,
      unstyled
    }
  }, children);
}
Popover.Target = PopoverTarget;
Popover.Dropdown = PopoverDropdown;
Popover.displayName = "@mantine/core/Popover";

// node_modules/@mantine/core/esm/Select/SelectPopover/SelectPopover.js
var __defProp60 = Object.defineProperty;
var __getOwnPropSymbols62 = Object.getOwnPropertySymbols;
var __hasOwnProp62 = Object.prototype.hasOwnProperty;
var __propIsEnum62 = Object.prototype.propertyIsEnumerable;
var __defNormalProp60 = (obj, key, value) => key in obj ? __defProp60(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues60 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp62.call(b2, prop))
      __defNormalProp60(a2, prop, b2[prop]);
  if (__getOwnPropSymbols62)
    for (var prop of __getOwnPropSymbols62(b2)) {
      if (__propIsEnum62.call(b2, prop))
        __defNormalProp60(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest35 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp62.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols62)
    for (var prop of __getOwnPropSymbols62(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum62.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
function SelectPopoverDropdown(_a) {
  var _b = _a, {
    children,
    component = "div",
    maxHeight = 220,
    direction = "column",
    id,
    innerRef,
    __staticSelector,
    styles: styles2,
    classNames,
    unstyled
  } = _b, others = __objRest35(_b, [
    "children",
    "component",
    "maxHeight",
    "direction",
    "id",
    "innerRef",
    "__staticSelector",
    "styles",
    "classNames",
    "unstyled"
  ]);
  const { classes } = SelectPopover_styles_default(null, { name: __staticSelector, styles: styles2, classNames, unstyled });
  return import_react128.default.createElement(Popover.Dropdown, __spreadValues60({
    p: 0,
    onMouseDown: (event) => event.preventDefault()
  }, others), import_react128.default.createElement("div", {
    style: { maxHeight, display: "flex" }
  }, import_react128.default.createElement(Box, {
    component: component || "div",
    id: `${id}-items`,
    "aria-labelledby": `${id}-label`,
    role: "listbox",
    onMouseDown: (event) => event.preventDefault(),
    style: { flex: 1, overflowY: component !== SelectScrollArea ? "auto" : void 0 },
    "data-combobox-popover": true,
    ref: innerRef
  }, import_react128.default.createElement("div", {
    className: classes.itemsWrapper,
    style: { flexDirection: direction }
  }, children))));
}
function SelectPopover({
  opened,
  transition = "fade",
  transitionDuration = 0,
  shadow,
  withinPortal,
  children,
  __staticSelector,
  onDirectionChange,
  switchDirectionOnFlip,
  zIndex,
  dropdownPosition,
  positionDependencies = [],
  classNames,
  styles: styles2,
  unstyled,
  readOnly
}) {
  return import_react128.default.createElement(Popover, {
    unstyled,
    classNames,
    styles: styles2,
    width: "target",
    withRoles: false,
    opened,
    middlewares: { flip: dropdownPosition === "flip", shift: false },
    position: dropdownPosition === "flip" ? "bottom" : dropdownPosition,
    positionDependencies,
    zIndex,
    __staticSelector,
    withinPortal,
    transition,
    transitionDuration,
    shadow,
    disabled: readOnly,
    onPositionChange: (nextPosition) => switchDirectionOnFlip && (onDirectionChange == null ? void 0 : onDirectionChange(nextPosition === "top" ? "column-reverse" : "column"))
  }, children);
}
SelectPopover.Target = Popover.Target;
SelectPopover.Dropdown = SelectPopoverDropdown;

// node_modules/@mantine/core/esm/Autocomplete/filter-data/filter-data.js
function filterData({ data, limit, value, filter }) {
  const result = [];
  for (let i = 0; i < data.length; i += 1) {
    if (filter(value, data[i])) {
      result.push(data[i]);
    }
    if (result.length >= limit) {
      break;
    }
  }
  return result;
}

// node_modules/@mantine/core/esm/Autocomplete/Autocomplete.styles.js
var useStyles18 = createStyles(() => ({
  wrapper: {
    position: "relative"
  }
}));
var Autocomplete_styles_default = useStyles18;

// node_modules/@mantine/core/esm/Input/use-input-props.js
var __defProp61 = Object.defineProperty;
var __defProps27 = Object.defineProperties;
var __getOwnPropDescs27 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols63 = Object.getOwnPropertySymbols;
var __hasOwnProp63 = Object.prototype.hasOwnProperty;
var __propIsEnum63 = Object.prototype.propertyIsEnumerable;
var __defNormalProp61 = (obj, key, value) => key in obj ? __defProp61(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues61 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp63.call(b2, prop))
      __defNormalProp61(a2, prop, b2[prop]);
  if (__getOwnPropSymbols63)
    for (var prop of __getOwnPropSymbols63(b2)) {
      if (__propIsEnum63.call(b2, prop))
        __defNormalProp61(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps27 = (a2, b2) => __defProps27(a2, __getOwnPropDescs27(b2));
var __objRest36 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp63.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols63)
    for (var prop of __getOwnPropSymbols63(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum63.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
function useInputProps(component, defaultProps118, props) {
  const _a = useComponentDefaultProps(component, defaultProps118, props), {
    label,
    description,
    error,
    required,
    classNames,
    styles: styles2,
    className,
    unstyled,
    __staticSelector,
    sx,
    errorProps,
    labelProps,
    descriptionProps,
    wrapperProps: _wrapperProps,
    id,
    size: size3,
    style,
    inputContainer,
    inputWrapperOrder,
    withAsterisk
  } = _a, others = __objRest36(_a, [
    "label",
    "description",
    "error",
    "required",
    "classNames",
    "styles",
    "className",
    "unstyled",
    "__staticSelector",
    "sx",
    "errorProps",
    "labelProps",
    "descriptionProps",
    "wrapperProps",
    "id",
    "size",
    "style",
    "inputContainer",
    "inputWrapperOrder",
    "withAsterisk"
  ]);
  const uid = useId(id);
  const { systemStyles, rest } = extractSystemStyles(others);
  const wrapperProps = __spreadValues61({
    label,
    description,
    error,
    required,
    classNames,
    className,
    __staticSelector,
    sx,
    errorProps,
    labelProps,
    descriptionProps,
    unstyled,
    styles: styles2,
    id: uid,
    size: size3,
    style,
    inputContainer,
    inputWrapperOrder,
    withAsterisk
  }, _wrapperProps);
  return __spreadProps27(__spreadValues61({}, rest), {
    classNames,
    styles: styles2,
    unstyled,
    wrapperProps: __spreadValues61(__spreadValues61({}, wrapperProps), systemStyles),
    inputProps: {
      required,
      classNames,
      styles: styles2,
      unstyled,
      id: uid,
      size: size3,
      __staticSelector,
      invalid: !!error
    }
  });
}

// node_modules/@mantine/core/esm/Input/Input.js
var import_react135 = __toESM(require_react());

// node_modules/@mantine/core/esm/Input/InputWrapper/InputWrapper.js
var import_react133 = __toESM(require_react());

// node_modules/@mantine/core/esm/Input/InputLabel/InputLabel.js
var import_react129 = __toESM(require_react());

// node_modules/@mantine/core/esm/Input/InputLabel/InputLabel.styles.js
var useStyles19 = createStyles((theme, { size: size3 }) => ({
  label: {
    display: "inline-block",
    fontSize: theme.fn.size({ size: size3, sizes: theme.fontSizes }),
    fontWeight: 500,
    color: theme.colorScheme === "dark" ? theme.colors.dark[0] : theme.colors.gray[9],
    wordBreak: "break-word",
    cursor: "default",
    WebkitTapHighlightColor: "transparent"
  },
  required: {
    color: theme.fn.variant({ variant: "filled", color: "red" }).background
  }
}));
var InputLabel_styles_default = useStyles19;

// node_modules/@mantine/core/esm/Input/InputLabel/InputLabel.js
var __defProp62 = Object.defineProperty;
var __getOwnPropSymbols64 = Object.getOwnPropertySymbols;
var __hasOwnProp64 = Object.prototype.hasOwnProperty;
var __propIsEnum64 = Object.prototype.propertyIsEnumerable;
var __defNormalProp62 = (obj, key, value) => key in obj ? __defProp62(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues62 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp64.call(b2, prop))
      __defNormalProp62(a2, prop, b2[prop]);
  if (__getOwnPropSymbols64)
    for (var prop of __getOwnPropSymbols64(b2)) {
      if (__propIsEnum64.call(b2, prop))
        __defNormalProp62(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest37 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp64.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols64)
    for (var prop of __getOwnPropSymbols64(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum64.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps23 = {
  labelElement: "label",
  size: "sm"
};
var InputLabel = (0, import_react129.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("InputLabel", defaultProps23, props), {
    labelElement,
    children,
    required,
    size: size3,
    classNames,
    styles: styles2,
    unstyled,
    className,
    htmlFor,
    __staticSelector
  } = _a, others = __objRest37(_a, [
    "labelElement",
    "children",
    "required",
    "size",
    "classNames",
    "styles",
    "unstyled",
    "className",
    "htmlFor",
    "__staticSelector"
  ]);
  const { classes, cx } = InputLabel_styles_default({ size: size3 }, { name: ["InputWrapper", __staticSelector], classNames, styles: styles2, unstyled });
  return import_react129.default.createElement(Box, __spreadValues62({
    component: labelElement,
    ref,
    className: cx(classes.label, className),
    htmlFor: labelElement === "label" ? htmlFor : void 0
  }, others), children, required && import_react129.default.createElement("span", {
    className: classes.required,
    "aria-hidden": true
  }, " *"));
});
InputLabel.displayName = "@mantine/core/InputLabel";

// node_modules/@mantine/core/esm/Input/InputError/InputError.js
var import_react130 = __toESM(require_react());

// node_modules/@mantine/core/esm/Input/InputError/InputError.styles.js
var useStyles20 = createStyles((theme, { size: size3 }) => ({
  error: {
    wordBreak: "break-word",
    color: theme.fn.variant({ variant: "filled", color: "red" }).background,
    fontSize: theme.fn.size({ size: size3, sizes: theme.fontSizes }) - 2,
    lineHeight: 1.2,
    display: "block"
  }
}));
var InputError_styles_default = useStyles20;

// node_modules/@mantine/core/esm/Input/InputError/InputError.js
var __defProp63 = Object.defineProperty;
var __getOwnPropSymbols65 = Object.getOwnPropertySymbols;
var __hasOwnProp65 = Object.prototype.hasOwnProperty;
var __propIsEnum65 = Object.prototype.propertyIsEnumerable;
var __defNormalProp63 = (obj, key, value) => key in obj ? __defProp63(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues63 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp65.call(b2, prop))
      __defNormalProp63(a2, prop, b2[prop]);
  if (__getOwnPropSymbols65)
    for (var prop of __getOwnPropSymbols65(b2)) {
      if (__propIsEnum65.call(b2, prop))
        __defNormalProp63(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest38 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp65.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols65)
    for (var prop of __getOwnPropSymbols65(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum65.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps24 = {
  size: "sm"
};
var InputError = (0, import_react130.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("InputError", defaultProps24, props), { children, className, classNames, styles: styles2, unstyled, size: size3, __staticSelector } = _a, others = __objRest38(_a, ["children", "className", "classNames", "styles", "unstyled", "size", "__staticSelector"]);
  const { classes, cx } = InputError_styles_default({ size: size3 }, { name: ["InputWrapper", __staticSelector], classNames, styles: styles2, unstyled });
  return import_react130.default.createElement(Text, __spreadValues63({
    className: cx(classes.error, className),
    ref
  }, others), children);
});
InputError.displayName = "@mantine/core/InputError";

// node_modules/@mantine/core/esm/Input/InputDescription/InputDescription.js
var import_react131 = __toESM(require_react());

// node_modules/@mantine/core/esm/Input/InputDescription/InputDescription.styles.js
var useStyles21 = createStyles((theme, { size: size3 }) => ({
  description: {
    wordBreak: "break-word",
    color: theme.colorScheme === "dark" ? theme.colors.dark[2] : theme.colors.gray[6],
    fontSize: theme.fn.size({ size: size3, sizes: theme.fontSizes }) - 2,
    lineHeight: 1.2,
    display: "block"
  }
}));
var InputDescription_styles_default = useStyles21;

// node_modules/@mantine/core/esm/Input/InputDescription/InputDescription.js
var __defProp64 = Object.defineProperty;
var __getOwnPropSymbols66 = Object.getOwnPropertySymbols;
var __hasOwnProp66 = Object.prototype.hasOwnProperty;
var __propIsEnum66 = Object.prototype.propertyIsEnumerable;
var __defNormalProp64 = (obj, key, value) => key in obj ? __defProp64(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues64 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp66.call(b2, prop))
      __defNormalProp64(a2, prop, b2[prop]);
  if (__getOwnPropSymbols66)
    for (var prop of __getOwnPropSymbols66(b2)) {
      if (__propIsEnum66.call(b2, prop))
        __defNormalProp64(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest39 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp66.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols66)
    for (var prop of __getOwnPropSymbols66(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum66.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps25 = {
  size: "sm"
};
var InputDescription = (0, import_react131.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("InputDescription", defaultProps25, props), { children, className, classNames, styles: styles2, unstyled, size: size3, __staticSelector } = _a, others = __objRest39(_a, ["children", "className", "classNames", "styles", "unstyled", "size", "__staticSelector"]);
  const { classes, cx } = InputDescription_styles_default({ size: size3 }, { name: ["InputWrapper", __staticSelector], classNames, styles: styles2, unstyled });
  return import_react131.default.createElement(Text, __spreadValues64({
    color: "dimmed",
    className: cx(classes.description, className),
    ref,
    unstyled
  }, others), children);
});
InputDescription.displayName = "@mantine/core/InputDescription";

// node_modules/@mantine/core/esm/Input/InputWrapper.context.js
var import_react132 = __toESM(require_react());
var InputWrapperContext = (0, import_react132.createContext)({
  offsetBottom: false,
  offsetTop: false,
  describedBy: void 0
});
var InputWrapperProvider = InputWrapperContext.Provider;
var useInputWrapperContext = () => (0, import_react132.useContext)(InputWrapperContext);

// node_modules/@mantine/core/esm/Input/InputWrapper/get-input-offsets.js
function getInputOffsets(inputWrapperOrder, { hasDescription, hasError }) {
  const inputIndex = inputWrapperOrder.findIndex((part) => part === "input");
  const aboveInput = inputWrapperOrder[inputIndex - 1];
  const belowInput = inputWrapperOrder[inputIndex + 1];
  const offsetTop = hasDescription && aboveInput === "description" || hasError && aboveInput === "error";
  const offsetBottom = hasDescription && belowInput === "description" || hasError && belowInput === "error";
  return { offsetBottom, offsetTop };
}

// node_modules/@mantine/core/esm/Input/InputWrapper/InputWrapper.styles.js
var __defProp65 = Object.defineProperty;
var __defProps28 = Object.defineProperties;
var __getOwnPropDescs28 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols67 = Object.getOwnPropertySymbols;
var __hasOwnProp67 = Object.prototype.hasOwnProperty;
var __propIsEnum67 = Object.prototype.propertyIsEnumerable;
var __defNormalProp65 = (obj, key, value) => key in obj ? __defProp65(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues65 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp67.call(b2, prop))
      __defNormalProp65(a2, prop, b2[prop]);
  if (__getOwnPropSymbols67)
    for (var prop of __getOwnPropSymbols67(b2)) {
      if (__propIsEnum67.call(b2, prop))
        __defNormalProp65(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps28 = (a2, b2) => __defProps28(a2, __getOwnPropDescs28(b2));
var useStyles22 = createStyles((theme) => ({
  root: __spreadProps28(__spreadValues65({}, theme.fn.fontStyles()), {
    lineHeight: theme.lineHeight
  })
}));
var InputWrapper_styles_default = useStyles22;

// node_modules/@mantine/core/esm/Input/InputWrapper/InputWrapper.js
var __defProp66 = Object.defineProperty;
var __defProps29 = Object.defineProperties;
var __getOwnPropDescs29 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols68 = Object.getOwnPropertySymbols;
var __hasOwnProp68 = Object.prototype.hasOwnProperty;
var __propIsEnum68 = Object.prototype.propertyIsEnumerable;
var __defNormalProp66 = (obj, key, value) => key in obj ? __defProp66(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues66 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp68.call(b2, prop))
      __defNormalProp66(a2, prop, b2[prop]);
  if (__getOwnPropSymbols68)
    for (var prop of __getOwnPropSymbols68(b2)) {
      if (__propIsEnum68.call(b2, prop))
        __defNormalProp66(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps29 = (a2, b2) => __defProps29(a2, __getOwnPropDescs29(b2));
var __objRest40 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp68.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols68)
    for (var prop of __getOwnPropSymbols68(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum68.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps26 = {
  labelElement: "label",
  size: "sm",
  inputContainer: (children) => children,
  inputWrapperOrder: ["label", "description", "input", "error"]
};
var InputWrapper = (0, import_react133.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("InputWrapper", defaultProps26, props), {
    className,
    label,
    children,
    required,
    id,
    error,
    description,
    labelElement,
    labelProps,
    descriptionProps,
    errorProps,
    classNames,
    styles: styles2,
    size: size3,
    inputContainer,
    __staticSelector,
    unstyled,
    inputWrapperOrder,
    withAsterisk
  } = _a, others = __objRest40(_a, [
    "className",
    "label",
    "children",
    "required",
    "id",
    "error",
    "description",
    "labelElement",
    "labelProps",
    "descriptionProps",
    "errorProps",
    "classNames",
    "styles",
    "size",
    "inputContainer",
    "__staticSelector",
    "unstyled",
    "inputWrapperOrder",
    "withAsterisk"
  ]);
  const { classes, cx } = InputWrapper_styles_default(null, {
    classNames,
    styles: styles2,
    name: ["InputWrapper", __staticSelector],
    unstyled
  });
  const sharedProps = {
    classNames,
    styles: styles2,
    unstyled,
    size: size3,
    __staticSelector
  };
  const isRequired = typeof withAsterisk === "boolean" ? withAsterisk : required;
  const errorId = id ? `${id}-error` : errorProps == null ? void 0 : errorProps.id;
  const descriptionId = id ? `${id}-description` : descriptionProps == null ? void 0 : descriptionProps.id;
  const hasError = !!error && typeof error !== "boolean";
  const _describedBy = `${hasError ? errorId : ""} ${description ? descriptionId : ""}`;
  const describedBy = _describedBy.trim().length > 0 ? _describedBy.trim() : void 0;
  const _label = label && import_react133.default.createElement(InputLabel, __spreadValues66(__spreadValues66({
    key: "label",
    labelElement,
    id: id ? `${id}-label` : void 0,
    htmlFor: id,
    required: isRequired
  }, sharedProps), labelProps), label);
  const _description = description && import_react133.default.createElement(InputDescription, __spreadProps29(__spreadValues66(__spreadValues66({
    key: "description"
  }, descriptionProps), sharedProps), {
    size: (descriptionProps == null ? void 0 : descriptionProps.size) || sharedProps.size,
    id: (descriptionProps == null ? void 0 : descriptionProps.id) || descriptionId
  }), description);
  const _input = import_react133.default.createElement(import_react133.Fragment, {
    key: "input"
  }, inputContainer(children));
  const _error = typeof error !== "boolean" && error && import_react133.default.createElement(InputError, __spreadProps29(__spreadValues66(__spreadValues66({}, errorProps), sharedProps), {
    size: (errorProps == null ? void 0 : errorProps.size) || sharedProps.size,
    key: "error",
    id: (errorProps == null ? void 0 : errorProps.id) || errorId
  }), error);
  const content = inputWrapperOrder.map((part) => {
    switch (part) {
      case "label":
        return _label;
      case "input":
        return _input;
      case "description":
        return _description;
      case "error":
        return _error;
      default:
        return null;
    }
  });
  return import_react133.default.createElement(InputWrapperProvider, {
    value: __spreadValues66({
      describedBy
    }, getInputOffsets(inputWrapperOrder, {
      hasDescription: !!_description,
      hasError: !!_error
    }))
  }, import_react133.default.createElement(Box, __spreadValues66({
    className: cx(classes.root, className),
    ref
  }, others), content));
});
InputWrapper.displayName = "@mantine/core/InputWrapper";

// node_modules/@mantine/core/esm/Input/InputPlaceholder/InputPlaceholder.js
var import_react134 = __toESM(require_react());
var __defProp67 = Object.defineProperty;
var __getOwnPropSymbols69 = Object.getOwnPropertySymbols;
var __hasOwnProp69 = Object.prototype.hasOwnProperty;
var __propIsEnum69 = Object.prototype.propertyIsEnumerable;
var __defNormalProp67 = (obj, key, value) => key in obj ? __defProp67(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues67 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp69.call(b2, prop))
      __defNormalProp67(a2, prop, b2[prop]);
  if (__getOwnPropSymbols69)
    for (var prop of __getOwnPropSymbols69(b2)) {
      if (__propIsEnum69.call(b2, prop))
        __defNormalProp67(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest41 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp69.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols69)
    for (var prop of __getOwnPropSymbols69(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum69.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps27 = {};
var InputPlaceholder = (0, import_react134.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("InputPlaceholder", defaultProps27, props), { sx } = _a, others = __objRest41(_a, ["sx"]);
  return import_react134.default.createElement(Box, __spreadValues67({
    component: "span",
    sx: [(theme) => theme.fn.placeholderStyles(), ...packSx(sx)],
    ref
  }, others));
});
InputPlaceholder.displayName = "@mantine/core/InputPlaceholder";

// node_modules/@mantine/core/esm/Input/Input.styles.js
var __defProp68 = Object.defineProperty;
var __defProps30 = Object.defineProperties;
var __getOwnPropDescs30 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols70 = Object.getOwnPropertySymbols;
var __hasOwnProp70 = Object.prototype.hasOwnProperty;
var __propIsEnum70 = Object.prototype.propertyIsEnumerable;
var __defNormalProp68 = (obj, key, value) => key in obj ? __defProp68(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues68 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp70.call(b2, prop))
      __defNormalProp68(a2, prop, b2[prop]);
  if (__getOwnPropSymbols70)
    for (var prop of __getOwnPropSymbols70(b2)) {
      if (__propIsEnum70.call(b2, prop))
        __defNormalProp68(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps30 = (a2, b2) => __defProps30(a2, __getOwnPropDescs30(b2));
var sizes4 = {
  xs: 30,
  sm: 36,
  md: 42,
  lg: 50,
  xl: 60
};
function getVariantStyles5({ theme, variant: variant2 }) {
  if (variant2 === "default") {
    return {
      border: `1px solid ${theme.colorScheme === "dark" ? theme.colors.dark[4] : theme.colors.gray[4]}`,
      backgroundColor: theme.colorScheme === "dark" ? theme.colors.dark[6] : theme.white,
      transition: "border-color 100ms ease",
      "&:focus, &:focus-within": theme.focusRingStyles.inputStyles(theme)
    };
  }
  if (variant2 === "filled") {
    return {
      border: "1px solid transparent",
      backgroundColor: theme.colorScheme === "dark" ? theme.colors.dark[5] : theme.colors.gray[1],
      "&:focus, &:focus-within": theme.focusRingStyles.inputStyles(theme)
    };
  }
  return {
    borderWidth: 0,
    color: theme.colorScheme === "dark" ? theme.colors.dark[0] : theme.black,
    backgroundColor: "transparent",
    minHeight: 28,
    outline: 0,
    "&:focus, &:focus-within": {
      outline: "none",
      borderColor: "transparent"
    },
    "&:disabled": {
      backgroundColor: "transparent",
      "&:focus, &:focus-within": {
        outline: "none",
        borderColor: "transparent"
      }
    }
  };
}
var useStyles23 = createStyles((theme, {
  size: size3,
  multiline,
  radius: radius2,
  variant: variant2,
  invalid,
  rightSectionWidth,
  withRightSection,
  iconWidth,
  offsetBottom,
  offsetTop,
  pointer
}) => {
  const invalidColor = theme.fn.variant({ variant: "filled", color: "red" }).background;
  const sizeStyles = variant2 === "default" || variant2 === "filled" ? {
    minHeight: theme.fn.size({ size: size3, sizes: sizes4 }),
    paddingLeft: theme.fn.size({ size: size3, sizes: sizes4 }) / 3,
    paddingRight: withRightSection ? rightSectionWidth : theme.fn.size({ size: size3, sizes: sizes4 }) / 3,
    borderRadius: theme.fn.radius(radius2)
  } : null;
  return {
    wrapper: {
      position: "relative",
      marginTop: offsetTop ? `calc(${theme.spacing.xs}px / 2)` : void 0,
      marginBottom: offsetBottom ? `calc(${theme.spacing.xs}px / 2)` : void 0
    },
    input: __spreadValues68(__spreadProps30(__spreadValues68(__spreadProps30(__spreadValues68({}, theme.fn.fontStyles()), {
      height: multiline ? variant2 === "unstyled" ? void 0 : "auto" : theme.fn.size({ size: size3, sizes: sizes4 }),
      WebkitTapHighlightColor: "transparent",
      lineHeight: multiline ? theme.lineHeight : `${theme.fn.size({ size: size3, sizes: sizes4 }) - 2}px`,
      appearance: "none",
      resize: "none",
      boxSizing: "border-box",
      fontSize: theme.fn.size({ size: size3, sizes: theme.fontSizes }),
      width: "100%",
      color: theme.colorScheme === "dark" ? theme.colors.dark[0] : theme.black,
      display: "block",
      textAlign: "left",
      cursor: pointer ? "pointer" : void 0
    }), sizeStyles), {
      "&:disabled": {
        backgroundColor: theme.colorScheme === "dark" ? theme.colors.dark[6] : theme.colors.gray[1],
        color: theme.colors.dark[2],
        opacity: 0.6,
        cursor: "not-allowed",
        "&::placeholder": {
          color: theme.colors.dark[2]
        }
      },
      "&::placeholder": __spreadProps30(__spreadValues68({}, theme.fn.placeholderStyles()), {
        opacity: 1
      }),
      "&::-webkit-inner-spin-button, &::-webkit-outer-spin-button, &::-webkit-search-decoration, &::-webkit-search-cancel-button, &::-webkit-search-results-button, &::-webkit-search-results-decoration": {
        appearance: "none"
      },
      "&[type=number]": {
        MozAppearance: "textfield"
      }
    }), getVariantStyles5({ theme, variant: variant2 })),
    withIcon: {
      paddingLeft: typeof iconWidth === "number" ? iconWidth : theme.fn.size({ size: size3, sizes: sizes4 })
    },
    invalid: {
      color: invalidColor,
      borderColor: invalidColor,
      "&::placeholder": {
        opacity: 1,
        color: invalidColor
      }
    },
    disabled: {
      backgroundColor: theme.colorScheme === "dark" ? theme.colors.dark[6] : theme.colors.gray[1],
      color: theme.colors.dark[2],
      opacity: 0.6,
      cursor: "not-allowed",
      "&::placeholder": {
        color: theme.colors.dark[2]
      }
    },
    icon: {
      pointerEvents: "none",
      position: "absolute",
      zIndex: 1,
      left: 0,
      top: 0,
      bottom: 0,
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      width: typeof iconWidth === "number" ? iconWidth : theme.fn.size({ size: size3, sizes: sizes4 }),
      color: invalid ? theme.colors.red[theme.colorScheme === "dark" ? 6 : 7] : theme.colorScheme === "dark" ? theme.colors.dark[2] : theme.colors.gray[5]
    },
    rightSection: {
      position: "absolute",
      top: 0,
      bottom: 0,
      right: 0,
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      width: rightSectionWidth
    }
  };
});
var Input_styles_default = useStyles23;

// node_modules/@mantine/core/esm/Input/Input.js
var __defProp69 = Object.defineProperty;
var __defProps31 = Object.defineProperties;
var __getOwnPropDescs31 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols71 = Object.getOwnPropertySymbols;
var __hasOwnProp71 = Object.prototype.hasOwnProperty;
var __propIsEnum71 = Object.prototype.propertyIsEnumerable;
var __defNormalProp69 = (obj, key, value) => key in obj ? __defProp69(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues69 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp71.call(b2, prop))
      __defNormalProp69(a2, prop, b2[prop]);
  if (__getOwnPropSymbols71)
    for (var prop of __getOwnPropSymbols71(b2)) {
      if (__propIsEnum71.call(b2, prop))
        __defNormalProp69(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps31 = (a2, b2) => __defProps31(a2, __getOwnPropDescs31(b2));
var __objRest42 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp71.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols71)
    for (var prop of __getOwnPropSymbols71(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum71.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps28 = {
  rightSectionWidth: 36,
  size: "sm",
  variant: "default"
};
var _Input = (0, import_react135.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("Input", defaultProps28, props), {
    className,
    invalid,
    required,
    disabled,
    variant: variant2,
    icon,
    style,
    rightSectionWidth,
    iconWidth,
    rightSection,
    rightSectionProps,
    radius: radius2,
    size: size3,
    wrapperProps,
    classNames,
    styles: styles2,
    __staticSelector,
    multiline,
    sx,
    unstyled,
    pointer
  } = _a, others = __objRest42(_a, [
    "className",
    "invalid",
    "required",
    "disabled",
    "variant",
    "icon",
    "style",
    "rightSectionWidth",
    "iconWidth",
    "rightSection",
    "rightSectionProps",
    "radius",
    "size",
    "wrapperProps",
    "classNames",
    "styles",
    "__staticSelector",
    "multiline",
    "sx",
    "unstyled",
    "pointer"
  ]);
  const { offsetBottom, offsetTop, describedBy } = useInputWrapperContext();
  const { classes, cx } = Input_styles_default({
    radius: radius2,
    size: size3,
    multiline,
    variant: variant2,
    invalid,
    rightSectionWidth,
    iconWidth,
    withRightSection: !!rightSection,
    offsetBottom,
    offsetTop,
    pointer
  }, { classNames, styles: styles2, name: ["Input", __staticSelector], unstyled });
  const { systemStyles, rest } = extractSystemStyles(others);
  return import_react135.default.createElement(Box, __spreadValues69(__spreadValues69({
    className: cx(classes.wrapper, className),
    sx,
    style
  }, systemStyles), wrapperProps), icon && import_react135.default.createElement("div", {
    className: classes.icon
  }, icon), import_react135.default.createElement(Box, __spreadProps31(__spreadValues69({
    component: "input"
  }, rest), {
    ref,
    required,
    "aria-invalid": invalid,
    "aria-describedby": describedBy,
    disabled,
    className: cx(classes[`${variant2}Variant`], classes.input, {
      [classes.withIcon]: icon,
      [classes.invalid]: invalid,
      [classes.disabled]: disabled
    })
  })), rightSection && import_react135.default.createElement("div", __spreadProps31(__spreadValues69({}, rightSectionProps), {
    className: classes.rightSection
  }), rightSection));
});
_Input.displayName = "@mantine/core/Input";
_Input.Wrapper = InputWrapper;
_Input.Label = InputLabel;
_Input.Description = InputDescription;
_Input.Error = InputError;
_Input.Placeholder = InputPlaceholder;
var Input = createPolymorphicComponent(_Input);

// node_modules/@mantine/core/esm/Autocomplete/Autocomplete.js
var __defProp70 = Object.defineProperty;
var __defProps32 = Object.defineProperties;
var __getOwnPropDescs32 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols72 = Object.getOwnPropertySymbols;
var __hasOwnProp72 = Object.prototype.hasOwnProperty;
var __propIsEnum72 = Object.prototype.propertyIsEnumerable;
var __defNormalProp70 = (obj, key, value) => key in obj ? __defProp70(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues70 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp72.call(b2, prop))
      __defNormalProp70(a2, prop, b2[prop]);
  if (__getOwnPropSymbols72)
    for (var prop of __getOwnPropSymbols72(b2)) {
      if (__propIsEnum72.call(b2, prop))
        __defNormalProp70(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps32 = (a2, b2) => __defProps32(a2, __getOwnPropDescs32(b2));
var __objRest43 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp72.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols72)
    for (var prop of __getOwnPropSymbols72(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum72.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
function defaultFilter(value, item) {
  return item.value.toLowerCase().trim().includes(value.toLowerCase().trim());
}
var defaultProps29 = {
  required: false,
  size: "sm",
  shadow: "sm",
  limit: 5,
  itemComponent: DefaultItem,
  transition: "pop",
  transitionDuration: 0,
  initiallyOpened: false,
  filter: defaultFilter,
  switchDirectionOnFlip: false,
  zIndex: getDefaultZIndex("popover"),
  dropdownPosition: "flip",
  maxDropdownHeight: "auto",
  positionDependencies: []
};
var Autocomplete = (0, import_react136.forwardRef)((props, ref) => {
  const _a = useInputProps("Autocomplete", defaultProps29, props), {
    inputProps,
    wrapperProps,
    shadow,
    data,
    limit,
    value,
    defaultValue,
    onChange,
    unstyled,
    itemComponent,
    onItemSubmit,
    onKeyDown,
    onFocus,
    onBlur,
    onClick,
    transition,
    transitionDuration,
    initiallyOpened,
    transitionTimingFunction,
    classNames,
    styles: styles2,
    filter,
    nothingFound,
    onDropdownClose,
    onDropdownOpen,
    withinPortal,
    switchDirectionOnFlip,
    zIndex,
    dropdownPosition,
    maxDropdownHeight,
    dropdownComponent,
    positionDependencies,
    readOnly,
    hoverOnSearchChange
  } = _a, others = __objRest43(_a, [
    "inputProps",
    "wrapperProps",
    "shadow",
    "data",
    "limit",
    "value",
    "defaultValue",
    "onChange",
    "unstyled",
    "itemComponent",
    "onItemSubmit",
    "onKeyDown",
    "onFocus",
    "onBlur",
    "onClick",
    "transition",
    "transitionDuration",
    "initiallyOpened",
    "transitionTimingFunction",
    "classNames",
    "styles",
    "filter",
    "nothingFound",
    "onDropdownClose",
    "onDropdownOpen",
    "withinPortal",
    "switchDirectionOnFlip",
    "zIndex",
    "dropdownPosition",
    "maxDropdownHeight",
    "dropdownComponent",
    "positionDependencies",
    "readOnly",
    "hoverOnSearchChange"
  ]);
  const { classes } = Autocomplete_styles_default(null, { classNames, styles: styles2, name: "Autocomplete", unstyled });
  const [dropdownOpened, _setDropdownOpened] = (0, import_react136.useState)(initiallyOpened);
  const [hovered, setHovered] = (0, import_react136.useState)(-1);
  const [direction, setDirection] = (0, import_react136.useState)("column");
  const inputRef = (0, import_react136.useRef)(null);
  const [IMEOpen, setIMEOpen] = (0, import_react136.useState)(false);
  const [_value, handleChange] = useUncontrolled({
    value,
    defaultValue,
    finalValue: "",
    onChange
  });
  const setDropdownOpened = (opened) => {
    _setDropdownOpened(opened);
    const handler = opened ? onDropdownOpen : onDropdownClose;
    typeof handler === "function" && handler();
  };
  useDidUpdate(() => {
    if (hoverOnSearchChange && _value) {
      setHovered(0);
    } else {
      setHovered(-1);
    }
  }, [_value, hoverOnSearchChange]);
  const handleItemClick = (item) => {
    handleChange(item.value);
    typeof onItemSubmit === "function" && onItemSubmit(item);
    setDropdownOpened(false);
  };
  const formattedData = data.map((item) => typeof item === "string" ? { value: item } : item);
  const filteredData = groupOptions({
    data: filterData({ data: formattedData, value: _value, limit, filter })
  });
  const handleInputKeydown = (event) => {
    if (IMEOpen) {
      return;
    }
    typeof onKeyDown === "function" && onKeyDown(event);
    const isColumn = direction === "column";
    const handleNext = () => {
      setHovered((current) => current < filteredData.length - 1 ? current + 1 : current);
    };
    const handlePrevious = () => {
      setHovered((current) => current > 0 ? current - 1 : current);
    };
    switch (event.key) {
      case "ArrowUp": {
        event.preventDefault();
        isColumn ? handlePrevious() : handleNext();
        break;
      }
      case "ArrowDown": {
        event.preventDefault();
        isColumn ? handleNext() : handlePrevious();
        break;
      }
      case "Enter": {
        if (filteredData[hovered] && dropdownOpened) {
          event.preventDefault();
          handleChange(filteredData[hovered].value);
          typeof onItemSubmit === "function" && onItemSubmit(filteredData[hovered]);
          setDropdownOpened(false);
        }
        break;
      }
      case "Escape": {
        if (dropdownOpened) {
          event.preventDefault();
          setDropdownOpened(false);
        }
      }
    }
  };
  const handleInputFocus = (event) => {
    typeof onFocus === "function" && onFocus(event);
    setDropdownOpened(true);
  };
  const handleInputBlur = (event) => {
    typeof onBlur === "function" && onBlur(event);
    setDropdownOpened(false);
  };
  const handleInputClick = (event) => {
    typeof onClick === "function" && onClick(event);
    setDropdownOpened(true);
  };
  const shouldRenderDropdown = dropdownOpened && (filteredData.length > 0 || filteredData.length === 0 && !!nothingFound);
  return import_react136.default.createElement(Input.Wrapper, __spreadProps32(__spreadValues70({}, wrapperProps), {
    __staticSelector: "Autocomplete"
  }), import_react136.default.createElement(SelectPopover, {
    opened: shouldRenderDropdown,
    transition,
    transitionDuration,
    shadow: "sm",
    withinPortal,
    __staticSelector: "Autocomplete",
    onDirectionChange: setDirection,
    switchDirectionOnFlip,
    zIndex,
    dropdownPosition,
    positionDependencies,
    classNames,
    styles: styles2,
    unstyled,
    readOnly
  }, import_react136.default.createElement(SelectPopover.Target, null, import_react136.default.createElement("div", {
    className: classes.wrapper,
    role: "combobox",
    "aria-haspopup": "listbox",
    "aria-owns": shouldRenderDropdown ? `${inputProps.id}-items` : null,
    "aria-controls": inputProps.id,
    "aria-expanded": shouldRenderDropdown,
    onMouseLeave: () => setHovered(-1),
    tabIndex: -1
  }, import_react136.default.createElement(Input, __spreadProps32(__spreadValues70(__spreadValues70({
    type: "search",
    autoComplete: "off"
  }, inputProps), others), {
    readOnly,
    "data-mantine-stop-propagation": dropdownOpened,
    ref: useMergedRef(ref, inputRef),
    onKeyDown: handleInputKeydown,
    classNames,
    styles: styles2,
    __staticSelector: "Autocomplete",
    value: _value,
    onChange: (event) => {
      handleChange(event.currentTarget.value);
      setDropdownOpened(true);
    },
    onFocus: handleInputFocus,
    onBlur: handleInputBlur,
    onClick: handleInputClick,
    onCompositionStart: () => setIMEOpen(true),
    onCompositionEnd: () => setIMEOpen(false),
    "aria-autocomplete": "list",
    "aria-controls": shouldRenderDropdown ? `${inputProps.id}-items` : null,
    "aria-activedescendant": hovered >= 0 ? `${inputProps.id}-${hovered}` : null
  })))), import_react136.default.createElement(SelectPopover.Dropdown, {
    component: dropdownComponent || SelectScrollArea,
    maxHeight: maxDropdownHeight,
    direction,
    id: inputProps.id,
    __staticSelector: "Autocomplete",
    classNames,
    styles: styles2
  }, import_react136.default.createElement(SelectItems, {
    data: filteredData,
    hovered,
    classNames,
    styles: styles2,
    uuid: inputProps.id,
    __staticSelector: "Autocomplete",
    onItemHover: setHovered,
    onItemSelect: handleItemClick,
    itemComponent,
    size: inputProps.size,
    nothingFound
  }))));
});
Autocomplete.displayName = "@mantine/core/Autocomplete";

// node_modules/@mantine/core/esm/Avatar/Avatar.js
var import_react140 = __toESM(require_react());

// node_modules/@mantine/core/esm/Avatar/AvatarPlaceholderIcon.js
var import_react137 = __toESM(require_react());
var __defProp71 = Object.defineProperty;
var __defProps33 = Object.defineProperties;
var __getOwnPropDescs33 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols73 = Object.getOwnPropertySymbols;
var __hasOwnProp73 = Object.prototype.hasOwnProperty;
var __propIsEnum73 = Object.prototype.propertyIsEnumerable;
var __defNormalProp71 = (obj, key, value) => key in obj ? __defProp71(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues71 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp73.call(b2, prop))
      __defNormalProp71(a2, prop, b2[prop]);
  if (__getOwnPropSymbols73)
    for (var prop of __getOwnPropSymbols73(b2)) {
      if (__propIsEnum73.call(b2, prop))
        __defNormalProp71(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps33 = (a2, b2) => __defProps33(a2, __getOwnPropDescs33(b2));
function AvatarPlaceholderIcon(props) {
  return import_react137.default.createElement("svg", __spreadProps33(__spreadValues71({}, props), {
    width: "15",
    height: "15",
    viewBox: "0 0 15 15",
    fill: "none",
    xmlns: "http://www.w3.org/2000/svg"
  }), import_react137.default.createElement("path", {
    d: "M0.877014 7.49988C0.877014 3.84219 3.84216 0.877045 7.49985 0.877045C11.1575 0.877045 14.1227 3.84219 14.1227 7.49988C14.1227 11.1575 11.1575 14.1227 7.49985 14.1227C3.84216 14.1227 0.877014 11.1575 0.877014 7.49988ZM7.49985 1.82704C4.36683 1.82704 1.82701 4.36686 1.82701 7.49988C1.82701 8.97196 2.38774 10.3131 3.30727 11.3213C4.19074 9.94119 5.73818 9.02499 7.50023 9.02499C9.26206 9.02499 10.8093 9.94097 11.6929 11.3208C12.6121 10.3127 13.1727 8.97172 13.1727 7.49988C13.1727 4.36686 10.6328 1.82704 7.49985 1.82704ZM10.9818 11.9787C10.2839 10.7795 8.9857 9.97499 7.50023 9.97499C6.01458 9.97499 4.71624 10.7797 4.01845 11.9791C4.97952 12.7272 6.18765 13.1727 7.49985 13.1727C8.81227 13.1727 10.0206 12.727 10.9818 11.9787ZM5.14999 6.50487C5.14999 5.207 6.20212 4.15487 7.49999 4.15487C8.79786 4.15487 9.84999 5.207 9.84999 6.50487C9.84999 7.80274 8.79786 8.85487 7.49999 8.85487C6.20212 8.85487 5.14999 7.80274 5.14999 6.50487ZM7.49999 5.10487C6.72679 5.10487 6.09999 5.73167 6.09999 6.50487C6.09999 7.27807 6.72679 7.90487 7.49999 7.90487C8.27319 7.90487 8.89999 7.27807 8.89999 6.50487C8.89999 5.73167 8.27319 5.10487 7.49999 5.10487Z",
    fill: "currentColor",
    fillRule: "evenodd",
    clipRule: "evenodd"
  }));
}

// node_modules/@mantine/core/esm/Avatar/AvatarGroup/AvatarGroup.js
var import_react139 = __toESM(require_react());

// node_modules/@mantine/core/esm/Avatar/AvatarGroup/AvatarGroup.context.js
var import_react138 = __toESM(require_react());
var __defProp72 = Object.defineProperty;
var __defProps34 = Object.defineProperties;
var __getOwnPropDescs34 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols74 = Object.getOwnPropertySymbols;
var __hasOwnProp74 = Object.prototype.hasOwnProperty;
var __propIsEnum74 = Object.prototype.propertyIsEnumerable;
var __defNormalProp72 = (obj, key, value) => key in obj ? __defProp72(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues72 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp74.call(b2, prop))
      __defNormalProp72(a2, prop, b2[prop]);
  if (__getOwnPropSymbols74)
    for (var prop of __getOwnPropSymbols74(b2)) {
      if (__propIsEnum74.call(b2, prop))
        __defNormalProp72(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps34 = (a2, b2) => __defProps34(a2, __getOwnPropDescs34(b2));
var AvatarGroupContext = (0, import_react138.createContext)(null);
function AvatarGroupProvider({ spacing, children }) {
  return import_react138.default.createElement(AvatarGroupContext.Provider, {
    value: { spacing }
  }, children);
}
function useAvatarGroupContext() {
  const ctx = (0, import_react138.useContext)(AvatarGroupContext);
  if (ctx) {
    return __spreadProps34(__spreadValues72({}, ctx), { withinGroup: true });
  }
  return { spacing: null, withinGroup: false };
}

// node_modules/@mantine/core/esm/Avatar/AvatarGroup/AvatarGroup.styles.js
var useStyles24 = createStyles((theme, { spacing }) => ({
  root: {
    display: "flex",
    paddingLeft: theme.fn.size({ size: spacing, sizes: theme.spacing })
  }
}));
var AvatarGroup_styles_default = useStyles24;

// node_modules/@mantine/core/esm/Avatar/AvatarGroup/AvatarGroup.js
var __defProp73 = Object.defineProperty;
var __getOwnPropSymbols75 = Object.getOwnPropertySymbols;
var __hasOwnProp75 = Object.prototype.hasOwnProperty;
var __propIsEnum75 = Object.prototype.propertyIsEnumerable;
var __defNormalProp73 = (obj, key, value) => key in obj ? __defProp73(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues73 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp75.call(b2, prop))
      __defNormalProp73(a2, prop, b2[prop]);
  if (__getOwnPropSymbols75)
    for (var prop of __getOwnPropSymbols75(b2)) {
      if (__propIsEnum75.call(b2, prop))
        __defNormalProp73(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest44 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp75.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols75)
    for (var prop of __getOwnPropSymbols75(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum75.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps30 = {};
var AvatarGroup = (0, import_react139.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("AvatarGroup", defaultProps30, props), {
    children,
    spacing = "sm",
    unstyled,
    className
  } = _a, others = __objRest44(_a, [
    "children",
    "spacing",
    "unstyled",
    "className"
  ]);
  const { classes, cx } = AvatarGroup_styles_default({ spacing }, { name: "AvatarGroup", unstyled });
  return import_react139.default.createElement(AvatarGroupProvider, {
    spacing
  }, import_react139.default.createElement(Box, __spreadValues73({
    ref,
    className: cx(classes.root, className)
  }, others), children));
});
AvatarGroup.displayName = "@mantine/core/AvatarGroup";

// node_modules/@mantine/core/esm/Avatar/Avatar.styles.js
var __defProp74 = Object.defineProperty;
var __defProps35 = Object.defineProperties;
var __getOwnPropDescs35 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols76 = Object.getOwnPropertySymbols;
var __hasOwnProp76 = Object.prototype.hasOwnProperty;
var __propIsEnum76 = Object.prototype.propertyIsEnumerable;
var __defNormalProp74 = (obj, key, value) => key in obj ? __defProp74(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues74 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp76.call(b2, prop))
      __defNormalProp74(a2, prop, b2[prop]);
  if (__getOwnPropSymbols76)
    for (var prop of __getOwnPropSymbols76(b2)) {
      if (__propIsEnum76.call(b2, prop))
        __defNormalProp74(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps35 = (a2, b2) => __defProps35(a2, __getOwnPropDescs35(b2));
var sizes5 = {
  xs: 16,
  sm: 26,
  md: 38,
  lg: 56,
  xl: 84
};
function getGroupStyles({ withinGroup, spacing, theme }) {
  if (!withinGroup) {
    return null;
  }
  return {
    marginLeft: -theme.fn.size({ size: spacing, sizes: theme.spacing }),
    backgroundColor: `${theme.colorScheme === "dark" ? theme.colors.dark[7] : theme.white}`,
    border: `2px solid ${theme.colorScheme === "dark" ? theme.colors.dark[7] : theme.white}`
  };
}
var useStyles25 = createStyles((theme, { size: size3, radius: radius2, color, withinGroup, spacing, variant: variant2, gradient: gradient2 }) => {
  const colors = theme.fn.variant({ variant: variant2, color, gradient: gradient2 });
  return {
    root: __spreadValues74(__spreadProps35(__spreadValues74({}, theme.fn.focusStyles()), {
      WebkitTapHighlightColor: "transparent",
      boxSizing: "border-box",
      position: "relative",
      display: "block",
      userSelect: "none",
      overflow: "hidden",
      width: theme.fn.size({ size: size3, sizes: sizes5 }),
      minWidth: theme.fn.size({ size: size3, sizes: sizes5 }),
      height: theme.fn.size({ size: size3, sizes: sizes5 }),
      borderRadius: theme.fn.radius(radius2),
      textDecoration: "none",
      border: 0,
      backgroundColor: "transparent",
      padding: 0
    }), getGroupStyles({ withinGroup, spacing, theme })),
    image: {
      objectFit: "cover",
      width: "100%",
      height: "100%",
      display: "block"
    },
    placeholder: __spreadProps35(__spreadValues74({}, theme.fn.fontStyles()), {
      fontSize: theme.fn.size({ size: size3, sizes: sizes5 }) / 2.5,
      color: colors.color,
      fontWeight: 700,
      backgroundColor: colors.background,
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      width: "100%",
      height: "100%",
      userSelect: "none",
      backgroundImage: variant2 === "gradient" ? colors.background : void 0,
      border: `${variant2 === "gradient" ? 0 : 1}px solid ${colors.border}`,
      borderRadius: theme.fn.radius(radius2)
    }),
    placeholderIcon: {
      width: "70%",
      height: "70%",
      color: colors.color
    }
  };
});
var Avatar_styles_default = useStyles25;

// node_modules/@mantine/core/esm/Avatar/Avatar.js
var __defProp75 = Object.defineProperty;
var __defProps36 = Object.defineProperties;
var __getOwnPropDescs36 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols77 = Object.getOwnPropertySymbols;
var __hasOwnProp77 = Object.prototype.hasOwnProperty;
var __propIsEnum77 = Object.prototype.propertyIsEnumerable;
var __defNormalProp75 = (obj, key, value) => key in obj ? __defProp75(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues75 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp77.call(b2, prop))
      __defNormalProp75(a2, prop, b2[prop]);
  if (__getOwnPropSymbols77)
    for (var prop of __getOwnPropSymbols77(b2)) {
      if (__propIsEnum77.call(b2, prop))
        __defNormalProp75(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps36 = (a2, b2) => __defProps36(a2, __getOwnPropDescs36(b2));
var __objRest45 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp77.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols77)
    for (var prop of __getOwnPropSymbols77(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum77.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps31 = {
  size: "md",
  color: "gray",
  variant: "light"
};
var _Avatar = (0, import_react140.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("Avatar", defaultProps31, props), {
    className,
    size: size3,
    src,
    alt,
    radius: radius2,
    children,
    color,
    variant: variant2,
    gradient: gradient2,
    classNames,
    styles: styles2,
    imageProps,
    unstyled
  } = _a, others = __objRest45(_a, [
    "className",
    "size",
    "src",
    "alt",
    "radius",
    "children",
    "color",
    "variant",
    "gradient",
    "classNames",
    "styles",
    "imageProps",
    "unstyled"
  ]);
  const ctx = useAvatarGroupContext();
  const [error, setError] = (0, import_react140.useState)(!src);
  const { classes, cx } = Avatar_styles_default({ color, radius: radius2, size: size3, withinGroup: ctx.withinGroup, spacing: ctx.spacing, variant: variant2, gradient: gradient2 }, { classNames, styles: styles2, unstyled, name: "Avatar" });
  (0, import_react140.useEffect)(() => {
    !src ? setError(true) : setError(false);
  }, [src]);
  return import_react140.default.createElement(Box, __spreadValues75({
    component: "div",
    className: cx(classes.root, className),
    ref
  }, others), error ? import_react140.default.createElement("div", {
    className: classes.placeholder,
    title: alt
  }, children || import_react140.default.createElement(AvatarPlaceholderIcon, {
    className: classes.placeholderIcon
  })) : import_react140.default.createElement("img", __spreadProps36(__spreadValues75({}, imageProps), {
    className: classes.image,
    src,
    alt,
    onError: () => setError(true)
  })));
});
_Avatar.displayName = "@mantine/core/Avatar";
_Avatar.Group = AvatarGroup;
var Avatar = createPolymorphicComponent(_Avatar);

// node_modules/@mantine/core/esm/BackgroundImage/BackgroundImage.js
var import_react141 = __toESM(require_react());
var __defProp76 = Object.defineProperty;
var __defProps37 = Object.defineProperties;
var __getOwnPropDescs37 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols78 = Object.getOwnPropertySymbols;
var __hasOwnProp78 = Object.prototype.hasOwnProperty;
var __propIsEnum78 = Object.prototype.propertyIsEnumerable;
var __defNormalProp76 = (obj, key, value) => key in obj ? __defProp76(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues76 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp78.call(b2, prop))
      __defNormalProp76(a2, prop, b2[prop]);
  if (__getOwnPropSymbols78)
    for (var prop of __getOwnPropSymbols78(b2)) {
      if (__propIsEnum78.call(b2, prop))
        __defNormalProp76(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps37 = (a2, b2) => __defProps37(a2, __getOwnPropDescs37(b2));
var __objRest46 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp78.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols78)
    for (var prop of __getOwnPropSymbols78(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum78.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps32 = {
  radius: 0
};
var _BackgroundImage = (0, import_react141.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("BackgroundImage", defaultProps32, props), { src, radius: radius2, sx } = _a, others = __objRest46(_a, ["src", "radius", "sx"]);
  return import_react141.default.createElement(Box, __spreadProps37(__spreadValues76({}, others), {
    ref,
    sx: [
      (theme) => __spreadProps37(__spreadValues76({}, theme.fn.focusStyles()), {
        backgroundSize: "cover",
        backgroundPosition: "center",
        display: "block",
        width: "100%",
        border: 0,
        textDecoration: "none",
        color: theme.colorScheme === "dark" ? theme.colors.dark[0] : theme.black,
        backgroundImage: `url(${src})`,
        borderRadius: theme.fn.radius(radius2)
      }),
      ...packSx(sx)
    ]
  }));
});
_BackgroundImage.displayName = "@mantine/core/BackgroundImage";
var BackgroundImage = createPolymorphicComponent(_BackgroundImage);

// node_modules/@mantine/core/esm/Badge/Badge.js
var import_react142 = __toESM(require_react());

// node_modules/@mantine/core/esm/Badge/Badge.styles.js
var __defProp77 = Object.defineProperty;
var __defProps38 = Object.defineProperties;
var __getOwnPropDescs38 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols79 = Object.getOwnPropertySymbols;
var __hasOwnProp79 = Object.prototype.hasOwnProperty;
var __propIsEnum79 = Object.prototype.propertyIsEnumerable;
var __defNormalProp77 = (obj, key, value) => key in obj ? __defProp77(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues77 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp79.call(b2, prop))
      __defNormalProp77(a2, prop, b2[prop]);
  if (__getOwnPropSymbols79)
    for (var prop of __getOwnPropSymbols79(b2)) {
      if (__propIsEnum79.call(b2, prop))
        __defNormalProp77(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps38 = (a2, b2) => __defProps38(a2, __getOwnPropDescs38(b2));
var sizes6 = {
  xs: { fontSize: 9, height: 16 },
  sm: { fontSize: 10, height: 18 },
  md: { fontSize: 11, height: 20 },
  lg: { fontSize: 13, height: 26 },
  xl: { fontSize: 16, height: 32 }
};
var dotSizes = {
  xs: 4,
  sm: 4,
  md: 6,
  lg: 8,
  xl: 10
};
function getVariantStyles6({ theme, variant: variant2, color, size: size3, gradient: gradient2 }) {
  if (variant2 === "dot") {
    const dotSize = theme.fn.size({ size: size3, sizes: dotSizes });
    return {
      backgroundColor: "transparent",
      color: theme.colorScheme === "dark" ? theme.colors.dark[0] : theme.colors.gray[7],
      border: `1px solid ${theme.colorScheme === "dark" ? theme.colors.dark[3] : theme.colors.gray[3]}`,
      paddingLeft: theme.fn.size({ size: size3, sizes: theme.spacing }) / 1.5 - dotSize / 2,
      "&::before": {
        content: '""',
        display: "block",
        width: dotSize,
        height: dotSize,
        borderRadius: dotSize,
        backgroundColor: theme.fn.themeColor(color, theme.colorScheme === "dark" ? 4 : theme.fn.primaryShade("light"), true),
        marginRight: dotSize
      }
    };
  }
  const colors = theme.fn.variant({ color, variant: variant2, gradient: gradient2 });
  return {
    background: colors.background,
    color: colors.color,
    border: `${variant2 === "gradient" ? 0 : 1}px solid ${colors.border}`
  };
}
var useStyles26 = createStyles((theme, { color, size: size3, radius: radius2, gradient: gradient2, fullWidth, variant: variant2 }) => {
  const { fontSize, height } = size3 in sizes6 ? sizes6[size3] : sizes6.md;
  return {
    leftSection: {
      marginRight: `calc(${theme.spacing.xs}px / 2)`
    },
    rightSection: {
      marginLeft: `calc(${theme.spacing.xs}px / 2)`
    },
    inner: {
      whiteSpace: "nowrap",
      overflow: "hidden",
      textOverflow: "ellipsis"
    },
    root: __spreadValues77(__spreadProps38(__spreadValues77(__spreadValues77({}, theme.fn.focusStyles()), theme.fn.fontStyles()), {
      fontSize,
      height,
      WebkitTapHighlightColor: "transparent",
      lineHeight: `${height - 2}px`,
      textDecoration: "none",
      padding: `0 ${theme.fn.size({ size: size3, sizes: theme.spacing }) / 1.5}px`,
      boxSizing: "border-box",
      display: fullWidth ? "flex" : "inline-flex",
      alignItems: "center",
      justifyContent: "center",
      width: fullWidth ? "100%" : "auto",
      textTransform: "uppercase",
      borderRadius: theme.fn.radius(radius2),
      fontWeight: 700,
      letterSpacing: 0.25,
      cursor: "inherit",
      textOverflow: "ellipsis",
      overflow: "hidden"
    }), getVariantStyles6({ theme, variant: variant2, color, size: size3, gradient: gradient2 }))
  };
});
var Badge_styles_default = useStyles26;

// node_modules/@mantine/core/esm/Badge/Badge.js
var __defProp78 = Object.defineProperty;
var __getOwnPropSymbols80 = Object.getOwnPropertySymbols;
var __hasOwnProp80 = Object.prototype.hasOwnProperty;
var __propIsEnum80 = Object.prototype.propertyIsEnumerable;
var __defNormalProp78 = (obj, key, value) => key in obj ? __defProp78(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues78 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp80.call(b2, prop))
      __defNormalProp78(a2, prop, b2[prop]);
  if (__getOwnPropSymbols80)
    for (var prop of __getOwnPropSymbols80(b2)) {
      if (__propIsEnum80.call(b2, prop))
        __defNormalProp78(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest47 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp80.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols80)
    for (var prop of __getOwnPropSymbols80(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum80.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps33 = {
  variant: "light",
  size: "md",
  radius: "xl"
};
var _Badge = (0, import_react142.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("Badge", defaultProps33, props), {
    className,
    color,
    variant: variant2,
    fullWidth,
    children,
    size: size3,
    leftSection,
    rightSection,
    radius: radius2,
    gradient: gradient2,
    classNames,
    styles: styles2,
    unstyled
  } = _a, others = __objRest47(_a, [
    "className",
    "color",
    "variant",
    "fullWidth",
    "children",
    "size",
    "leftSection",
    "rightSection",
    "radius",
    "gradient",
    "classNames",
    "styles",
    "unstyled"
  ]);
  const { classes, cx } = Badge_styles_default({
    size: size3,
    fullWidth,
    color,
    radius: radius2,
    variant: variant2,
    gradient: gradient2
  }, { classNames, styles: styles2, name: "Badge", unstyled });
  return import_react142.default.createElement(Box, __spreadValues78({
    className: cx(classes.root, className),
    ref
  }, others), leftSection && import_react142.default.createElement("span", {
    className: classes.leftSection
  }, leftSection), import_react142.default.createElement("span", {
    className: classes.inner
  }, children), rightSection && import_react142.default.createElement("span", {
    className: classes.rightSection
  }, rightSection));
});
_Badge.displayName = "@mantine/core/Badge";
var Badge = createPolymorphicComponent(_Badge);

// node_modules/@mantine/core/esm/Blockquote/Blockquote.js
var import_react144 = __toESM(require_react());

// node_modules/@mantine/core/esm/Blockquote/QuoteIcon.js
var import_react143 = __toESM(require_react());
var __defProp79 = Object.defineProperty;
var __getOwnPropSymbols81 = Object.getOwnPropertySymbols;
var __hasOwnProp81 = Object.prototype.hasOwnProperty;
var __propIsEnum81 = Object.prototype.propertyIsEnumerable;
var __defNormalProp79 = (obj, key, value) => key in obj ? __defProp79(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues79 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp81.call(b2, prop))
      __defNormalProp79(a2, prop, b2[prop]);
  if (__getOwnPropSymbols81)
    for (var prop of __getOwnPropSymbols81(b2)) {
      if (__propIsEnum81.call(b2, prop))
        __defNormalProp79(a2, prop, b2[prop]);
    }
  return a2;
};
function QuoteIcon(props) {
  return import_react143.default.createElement("svg", __spreadValues79({
    width: "20",
    height: "20",
    viewBox: "0 0 409.294 409.294",
    fill: "currentColor",
    xmlns: "http://www.w3.org/2000/svg"
  }, props), import_react143.default.createElement("path", {
    d: "M0 204.647v175.412h175.412V204.647H58.471c0-64.48 52.461-116.941 116.941-116.941V29.235C78.684 29.235 0 107.919 0 204.647zM409.294 87.706V29.235c-96.728 0-175.412 78.684-175.412 175.412v175.412h175.412V204.647H292.353c0-64.48 52.461-116.941 116.941-116.941z"
  }));
}

// node_modules/@mantine/core/esm/Blockquote/Blockquote.styles.js
var __defProp80 = Object.defineProperty;
var __defProps39 = Object.defineProperties;
var __getOwnPropDescs39 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols82 = Object.getOwnPropertySymbols;
var __hasOwnProp82 = Object.prototype.hasOwnProperty;
var __propIsEnum82 = Object.prototype.propertyIsEnumerable;
var __defNormalProp80 = (obj, key, value) => key in obj ? __defProp80(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues80 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp82.call(b2, prop))
      __defNormalProp80(a2, prop, b2[prop]);
  if (__getOwnPropSymbols82)
    for (var prop of __getOwnPropSymbols82(b2)) {
      if (__propIsEnum82.call(b2, prop))
        __defNormalProp80(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps39 = (a2, b2) => __defProps39(a2, __getOwnPropDescs39(b2));
var useStyles27 = createStyles((theme, { color }) => ({
  root: __spreadProps39(__spreadValues80({}, theme.fn.fontStyles()), {
    fontSize: theme.fontSizes.lg,
    lineHeight: theme.lineHeight,
    color: theme.colorScheme === "dark" ? theme.colors.dark[0] : theme.black,
    margin: 0,
    borderTopRightRadius: theme.radius.sm,
    borderBottomRightRadius: theme.radius.sm,
    padding: `${theme.spacing.md}px ${theme.spacing.lg}px`
  }),
  inner: {
    display: "flex"
  },
  body: {
    flex: 1,
    overflow: "hidden",
    textOverflow: "ellipsis"
  },
  icon: {
    color: theme.fn.variant({ variant: "filled", color }).background,
    marginRight: theme.spacing.lg,
    marginTop: 2,
    width: 22
  },
  cite: {
    display: "block",
    fontSize: theme.fontSizes.sm,
    marginTop: theme.spacing.xs,
    color: theme.colorScheme === "dark" ? theme.colors.dark[2] : theme.colors.gray[6],
    overflow: "hidden",
    textOverflow: "ellipsis"
  }
}));
var Blockquote_styles_default = useStyles27;

// node_modules/@mantine/core/esm/Blockquote/Blockquote.js
var __defProp81 = Object.defineProperty;
var __getOwnPropSymbols83 = Object.getOwnPropertySymbols;
var __hasOwnProp83 = Object.prototype.hasOwnProperty;
var __propIsEnum83 = Object.prototype.propertyIsEnumerable;
var __defNormalProp81 = (obj, key, value) => key in obj ? __defProp81(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues81 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp83.call(b2, prop))
      __defNormalProp81(a2, prop, b2[prop]);
  if (__getOwnPropSymbols83)
    for (var prop of __getOwnPropSymbols83(b2)) {
      if (__propIsEnum83.call(b2, prop))
        __defNormalProp81(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest48 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp83.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols83)
    for (var prop of __getOwnPropSymbols83(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum83.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps34 = {
  color: "gray",
  icon: import_react144.default.createElement(QuoteIcon, null)
};
var Blockquote = (0, import_react144.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("Blockquote", defaultProps34, props), { className, color, icon, cite, children, classNames, styles: styles2, unstyled } = _a, others = __objRest48(_a, ["className", "color", "icon", "cite", "children", "classNames", "styles", "unstyled"]);
  const { classes, cx } = Blockquote_styles_default({ color }, { classNames, styles: styles2, unstyled, name: "Blockquote" });
  return import_react144.default.createElement(Box, __spreadValues81({
    component: "blockquote",
    className: cx(classes.root, className),
    ref
  }, others), import_react144.default.createElement("div", {
    className: classes.inner
  }, icon && import_react144.default.createElement("div", {
    className: classes.icon
  }, icon), import_react144.default.createElement("div", {
    className: classes.body
  }, children, cite && import_react144.default.createElement("cite", {
    className: classes.cite
  }, cite))));
});
Blockquote.displayName = "@mantine/core/Blockquote";

// node_modules/@mantine/core/esm/Breadcrumbs/Breadcrumbs.js
var import_react145 = __toESM(require_react());

// node_modules/@mantine/core/esm/Breadcrumbs/Breadcrumbs.styles.js
var useStyles28 = createStyles((theme) => ({
  root: {
    display: "flex",
    alignItems: "center"
  },
  breadcrumb: {
    lineHeight: 1,
    whiteSpace: "nowrap",
    WebkitTapHighlightColor: "transparent"
  },
  separator: {
    marginLeft: theme.spacing.xs,
    marginRight: theme.spacing.xs,
    color: theme.colorScheme === "dark" ? theme.colors.dark[2] : theme.colors.gray[7],
    lineHeight: 1,
    display: "flex",
    alignItems: "center",
    justifyContent: "center"
  }
}));
var Breadcrumbs_styles_default = useStyles28;

// node_modules/@mantine/core/esm/Breadcrumbs/Breadcrumbs.js
var __defProp82 = Object.defineProperty;
var __getOwnPropSymbols84 = Object.getOwnPropertySymbols;
var __hasOwnProp84 = Object.prototype.hasOwnProperty;
var __propIsEnum84 = Object.prototype.propertyIsEnumerable;
var __defNormalProp82 = (obj, key, value) => key in obj ? __defProp82(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues82 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp84.call(b2, prop))
      __defNormalProp82(a2, prop, b2[prop]);
  if (__getOwnPropSymbols84)
    for (var prop of __getOwnPropSymbols84(b2)) {
      if (__propIsEnum84.call(b2, prop))
        __defNormalProp82(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest49 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp84.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols84)
    for (var prop of __getOwnPropSymbols84(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum84.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps35 = {
  separator: "/"
};
var Breadcrumbs = (0, import_react145.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("Breadcrumbs", defaultProps35, props), { className, children, separator, classNames, styles: styles2, unstyled } = _a, others = __objRest49(_a, ["className", "children", "separator", "classNames", "styles", "unstyled"]);
  const { classes, cx } = Breadcrumbs_styles_default(null, { classNames, styles: styles2, unstyled, name: "Breadcrumbs" });
  const items = import_react145.default.Children.toArray(children).reduce((acc, child, index6, array) => {
    var _a2;
    const item = isElement(child) ? import_react145.default.cloneElement(child, {
      className: cx(classes.breadcrumb, (_a2 = child.props) == null ? void 0 : _a2.className),
      key: index6
    }) : import_react145.default.createElement("div", {
      className: classes.breadcrumb,
      key: index6
    }, child);
    acc.push(item);
    if (index6 !== array.length - 1) {
      acc.push(import_react145.default.createElement(Text, {
        size: "sm",
        className: classes.separator,
        key: `separator-${index6}`
      }, separator));
    }
    return acc;
  }, []);
  return import_react145.default.createElement(Box, __spreadValues82({
    className: cx(classes.root, className),
    ref
  }, others), items);
});
Breadcrumbs.displayName = "@mantine/core/Breadcrumbs";

// node_modules/@mantine/core/esm/Burger/Burger.js
var import_react146 = __toESM(require_react());

// node_modules/@mantine/core/esm/Burger/Burger.styles.js
var sizes7 = {
  xs: 12,
  sm: 18,
  md: 24,
  lg: 34,
  xl: 42
};
var useStyles29 = createStyles((theme, { size: size3, color, transitionDuration }) => {
  const sizeValue = theme.fn.size({ size: size3, sizes: sizes7 });
  const _color = color || (theme.colorScheme === "dark" ? theme.white : theme.black);
  return {
    root: {
      borderRadius: theme.radius.sm,
      width: `calc(${sizeValue}px + ${theme.spacing.xs}px)`,
      height: `calc(${sizeValue}px + ${theme.spacing.xs}px)`,
      padding: `calc(${theme.spacing.xs}px / 2)`,
      cursor: "pointer"
    },
    burger: {
      position: "relative",
      userSelect: "none",
      boxSizing: "border-box",
      "&, &::before, &::after": {
        display: "block",
        width: sizeValue,
        height: Math.ceil(sizeValue / 12),
        backgroundColor: _color,
        outline: "1px solid transparent",
        transitionProperty: "background-color, transform",
        transitionDuration: `${transitionDuration}ms`,
        "@media (prefers-reduced-motion)": {
          transitionDuration: theme.respectReducedMotion ? "0ms" : void 0
        }
      },
      "&::before, &::after": {
        position: "absolute",
        content: '""',
        left: 0
      },
      "&::before": {
        top: sizeValue / 3 * -1
      },
      "&::after": {
        top: sizeValue / 3
      },
      "&[data-opened]": {
        backgroundColor: "transparent",
        "&::before": {
          transform: `translateY(${sizeValue / 3}px) rotate(45deg)`
        },
        "&::after": {
          transform: `translateY(-${sizeValue / 3}px) rotate(-45deg)`
        }
      }
    }
  };
});
var Burger_styles_default = useStyles29;

// node_modules/@mantine/core/esm/Burger/Burger.js
var __defProp83 = Object.defineProperty;
var __getOwnPropSymbols85 = Object.getOwnPropertySymbols;
var __hasOwnProp85 = Object.prototype.hasOwnProperty;
var __propIsEnum85 = Object.prototype.propertyIsEnumerable;
var __defNormalProp83 = (obj, key, value) => key in obj ? __defProp83(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues83 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp85.call(b2, prop))
      __defNormalProp83(a2, prop, b2[prop]);
  if (__getOwnPropSymbols85)
    for (var prop of __getOwnPropSymbols85(b2)) {
      if (__propIsEnum85.call(b2, prop))
        __defNormalProp83(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest50 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp85.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols85)
    for (var prop of __getOwnPropSymbols85(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum85.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps36 = {
  size: "md",
  transitionDuration: 300
};
var Burger = (0, import_react146.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("Burger", defaultProps36, props), { className, opened, color, size: size3, classNames, styles: styles2, transitionDuration } = _a, others = __objRest50(_a, ["className", "opened", "color", "size", "classNames", "styles", "transitionDuration"]);
  const { classes, cx } = Burger_styles_default({ color, size: size3, transitionDuration }, { classNames, styles: styles2, name: "Burger" });
  return import_react146.default.createElement(UnstyledButton, __spreadValues83({
    className: cx(classes.root, className),
    ref
  }, others), import_react146.default.createElement("div", {
    "data-opened": opened || void 0,
    className: classes.burger
  }));
});
Burger.displayName = "@mantine/core/Burger";

// node_modules/@mantine/core/esm/Button/Button.js
var import_react148 = __toESM(require_react());

// node_modules/@mantine/core/esm/Button/ButtonGroup/ButtonGroup.js
var import_react147 = __toESM(require_react());

// node_modules/@mantine/core/esm/Button/ButtonGroup/ButtonGroup.styles.js
var useStyles30 = createStyles((_theme, { orientation, buttonBorderWidth }) => ({
  root: {
    display: "flex",
    flexDirection: orientation === "vertical" ? "column" : "row",
    "& [data-button]": {
      "&:first-of-type": {
        borderBottomRightRadius: 0,
        [orientation === "vertical" ? "borderBottomLeftRadius" : "borderTopRightRadius"]: 0,
        [orientation === "vertical" ? "borderBottomWidth" : "borderRightWidth"]: buttonBorderWidth / 2
      },
      "&:last-of-type": {
        borderTopLeftRadius: 0,
        [orientation === "vertical" ? "borderTopRightRadius" : "borderBottomLeftRadius"]: 0,
        [orientation === "vertical" ? "borderTopWidth" : "borderLeftWidth"]: buttonBorderWidth / 2
      },
      "&:not(:first-of-type):not(:last-of-type)": {
        borderRadius: 0,
        [orientation === "vertical" ? "borderTopWidth" : "borderLeftWidth"]: buttonBorderWidth / 2,
        [orientation === "vertical" ? "borderBottomWidth" : "borderRightWidth"]: buttonBorderWidth / 2
      },
      "& + [data-button]": {
        [orientation === "vertical" ? "marginTop" : "marginLeft"]: -buttonBorderWidth,
        "@media (min-resolution: 192dpi)": {
          [orientation === "vertical" ? "marginTop" : "marginLeft"]: 0
        }
      }
    }
  }
}));
var ButtonGroup_styles_default = useStyles30;

// node_modules/@mantine/core/esm/Button/ButtonGroup/ButtonGroup.js
var __defProp84 = Object.defineProperty;
var __getOwnPropSymbols86 = Object.getOwnPropertySymbols;
var __hasOwnProp86 = Object.prototype.hasOwnProperty;
var __propIsEnum86 = Object.prototype.propertyIsEnumerable;
var __defNormalProp84 = (obj, key, value) => key in obj ? __defProp84(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues84 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp86.call(b2, prop))
      __defNormalProp84(a2, prop, b2[prop]);
  if (__getOwnPropSymbols86)
    for (var prop of __getOwnPropSymbols86(b2)) {
      if (__propIsEnum86.call(b2, prop))
        __defNormalProp84(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest51 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp86.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols86)
    for (var prop of __getOwnPropSymbols86(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum86.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps37 = {
  orientation: "horizontal",
  buttonBorderWidth: 1
};
var ButtonGroup = (0, import_react147.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("ButtonGroup", defaultProps37, props), { className, orientation, buttonBorderWidth, unstyled } = _a, others = __objRest51(_a, ["className", "orientation", "buttonBorderWidth", "unstyled"]);
  const { classes, cx } = ButtonGroup_styles_default({ orientation, buttonBorderWidth }, { name: "ButtonGroup", unstyled });
  return import_react147.default.createElement(Box, __spreadValues84({
    className: cx(classes.root, className),
    ref
  }, others));
});
ButtonGroup.displayName = "@mantine/core/ButtonGroup";

// node_modules/@mantine/core/esm/Button/Button.styles.js
var __defProp85 = Object.defineProperty;
var __defProps40 = Object.defineProperties;
var __getOwnPropDescs40 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols87 = Object.getOwnPropertySymbols;
var __hasOwnProp87 = Object.prototype.hasOwnProperty;
var __propIsEnum87 = Object.prototype.propertyIsEnumerable;
var __defNormalProp85 = (obj, key, value) => key in obj ? __defProp85(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues85 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp87.call(b2, prop))
      __defNormalProp85(a2, prop, b2[prop]);
  if (__getOwnPropSymbols87)
    for (var prop of __getOwnPropSymbols87(b2)) {
      if (__propIsEnum87.call(b2, prop))
        __defNormalProp85(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps40 = (a2, b2) => __defProps40(a2, __getOwnPropDescs40(b2));
var sizes8 = {
  xs: { height: sizes4.xs, paddingLeft: 14, paddingRight: 14 },
  sm: { height: sizes4.sm, paddingLeft: 18, paddingRight: 18 },
  md: { height: sizes4.md, paddingLeft: 22, paddingRight: 22 },
  lg: { height: sizes4.lg, paddingLeft: 26, paddingRight: 26 },
  xl: { height: sizes4.xl, paddingLeft: 32, paddingRight: 32 },
  "compact-xs": { height: 22, paddingLeft: 7, paddingRight: 7 },
  "compact-sm": { height: 26, paddingLeft: 8, paddingRight: 8 },
  "compact-md": { height: 30, paddingLeft: 10, paddingRight: 10 },
  "compact-lg": { height: 34, paddingLeft: 12, paddingRight: 12 },
  "compact-xl": { height: 40, paddingLeft: 14, paddingRight: 14 }
};
function getSizeStyles({ compact, size: size3, withLeftIcon, withRightIcon }) {
  if (compact) {
    return sizes8[`compact-${size3}`];
  }
  const _sizes = sizes8[size3];
  return __spreadProps40(__spreadValues85({}, _sizes), {
    paddingLeft: withLeftIcon ? _sizes.paddingLeft / 1.5 : _sizes.paddingLeft,
    paddingRight: withRightIcon ? _sizes.paddingRight / 1.5 : _sizes.paddingRight
  });
}
var getWidthStyles = (fullWidth) => ({
  display: fullWidth ? "block" : "inline-block",
  width: fullWidth ? "100%" : "auto"
});
function getVariantStyles7({ variant: variant2, theme, color, gradient: gradient2 }) {
  const colors = theme.fn.variant({ color, variant: variant2, gradient: gradient2 });
  if (variant2 === "gradient") {
    return {
      border: 0,
      backgroundImage: colors.background,
      color: colors.color,
      "&:hover": theme.fn.hover({
        backgroundSize: "200%"
      })
    };
  }
  return __spreadValues85({
    border: `1px solid ${colors.border}`,
    backgroundColor: colors.background,
    color: colors.color
  }, theme.fn.hover({
    backgroundColor: colors.hover
  }));
}
var useStyles31 = createStyles((theme, {
  color,
  size: size3,
  radius: radius2,
  fullWidth,
  compact,
  gradient: gradient2,
  variant: variant2,
  withLeftIcon,
  withRightIcon
}) => ({
  root: __spreadProps40(__spreadValues85(__spreadProps40(__spreadValues85(__spreadValues85(__spreadValues85(__spreadValues85({}, getSizeStyles({ compact, size: size3, withLeftIcon, withRightIcon })), theme.fn.fontStyles()), theme.fn.focusStyles()), getWidthStyles(fullWidth)), {
    borderRadius: theme.fn.radius(radius2),
    fontWeight: 600,
    position: "relative",
    lineHeight: 1,
    fontSize: theme.fn.size({ size: size3, sizes: theme.fontSizes }),
    userSelect: "none",
    cursor: "pointer"
  }), getVariantStyles7({ variant: variant2, theme, color, gradient: gradient2 })), {
    "&:active": theme.activeStyles,
    "&:disabled, &[data-disabled]": {
      borderColor: "transparent",
      backgroundColor: theme.colorScheme === "dark" ? theme.colors.dark[4] : theme.colors.gray[2],
      color: theme.colorScheme === "dark" ? theme.colors.dark[6] : theme.colors.gray[5],
      cursor: "not-allowed",
      backgroundImage: "none",
      pointerEvents: "none",
      "&:active": {
        transform: "none"
      }
    },
    "&[data-loading]": {
      pointerEvents: "none",
      "&::before": {
        content: '""',
        position: "absolute",
        top: -1,
        left: -1,
        right: -1,
        bottom: -1,
        backgroundColor: theme.colorScheme === "dark" ? theme.fn.rgba(theme.colors.dark[7], 0.5) : "rgba(255, 255, 255, .5)",
        borderRadius: theme.fn.radius(radius2),
        cursor: "not-allowed"
      }
    }
  }),
  icon: {
    display: "flex",
    alignItems: "center"
  },
  leftIcon: {
    marginRight: 10
  },
  rightIcon: {
    marginLeft: 10
  },
  centerLoader: {
    position: "absolute",
    left: "50%",
    transform: "translateX(-50%)",
    opacity: 0.5
  },
  inner: {
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    height: "100%",
    overflow: "visible"
  },
  label: {
    whiteSpace: "nowrap",
    height: "100%",
    overflow: "hidden",
    display: "flex",
    alignItems: "center"
  }
}));
var Button_styles_default = useStyles31;

// node_modules/@mantine/core/esm/Button/Button.js
var __defProp86 = Object.defineProperty;
var __getOwnPropSymbols88 = Object.getOwnPropertySymbols;
var __hasOwnProp88 = Object.prototype.hasOwnProperty;
var __propIsEnum88 = Object.prototype.propertyIsEnumerable;
var __defNormalProp86 = (obj, key, value) => key in obj ? __defProp86(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues86 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp88.call(b2, prop))
      __defNormalProp86(a2, prop, b2[prop]);
  if (__getOwnPropSymbols88)
    for (var prop of __getOwnPropSymbols88(b2)) {
      if (__propIsEnum88.call(b2, prop))
        __defNormalProp86(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest52 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp88.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols88)
    for (var prop of __getOwnPropSymbols88(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum88.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps38 = {
  size: "sm",
  type: "button",
  variant: "filled",
  loaderPosition: "left"
};
var _Button = (0, import_react148.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("Button", defaultProps38, props), {
    className,
    size: size3,
    color,
    type,
    disabled,
    children,
    leftIcon,
    rightIcon,
    fullWidth,
    variant: variant2,
    radius: radius2,
    uppercase,
    compact,
    loading,
    loaderPosition,
    loaderProps,
    gradient: gradient2,
    classNames,
    styles: styles2,
    unstyled
  } = _a, others = __objRest52(_a, [
    "className",
    "size",
    "color",
    "type",
    "disabled",
    "children",
    "leftIcon",
    "rightIcon",
    "fullWidth",
    "variant",
    "radius",
    "uppercase",
    "compact",
    "loading",
    "loaderPosition",
    "loaderProps",
    "gradient",
    "classNames",
    "styles",
    "unstyled"
  ]);
  const { classes, cx, theme } = Button_styles_default({
    radius: radius2,
    color,
    size: size3,
    fullWidth,
    compact,
    gradient: gradient2,
    variant: variant2,
    withLeftIcon: !!leftIcon,
    withRightIcon: !!rightIcon
  }, { name: "Button", unstyled, classNames, styles: styles2 });
  const colors = theme.fn.variant({ color, variant: variant2 });
  const loader = import_react148.default.createElement(Loader, __spreadValues86({
    color: colors.color,
    size: theme.fn.size({ size: size3, sizes: sizes8 }).height / 2
  }, loaderProps));
  return import_react148.default.createElement(UnstyledButton, __spreadValues86({
    className: cx(classes.root, className),
    type,
    disabled,
    "data-button": true,
    "data-disabled": disabled || void 0,
    "data-loading": loading || void 0,
    ref,
    unstyled
  }, others), import_react148.default.createElement("div", {
    className: classes.inner
  }, (leftIcon || loading && loaderPosition === "left") && import_react148.default.createElement("span", {
    className: cx(classes.icon, classes.leftIcon)
  }, loading && loaderPosition === "left" ? loader : leftIcon), loading && loaderPosition === "center" && import_react148.default.createElement("span", {
    className: classes.centerLoader
  }, loader), import_react148.default.createElement("span", {
    className: classes.label,
    style: { textTransform: uppercase ? "uppercase" : void 0 }
  }, children), (rightIcon || loading && loaderPosition === "right") && import_react148.default.createElement("span", {
    className: cx(classes.icon, classes.rightIcon)
  }, loading && loaderPosition === "right" ? loader : rightIcon)));
});
_Button.displayName = "@mantine/core/Button";
_Button.Group = ButtonGroup;
var Button = createPolymorphicComponent(_Button);

// node_modules/@mantine/core/esm/Card/Card.js
var import_react152 = __toESM(require_react());

// node_modules/@mantine/core/esm/Paper/Paper.js
var import_react149 = __toESM(require_react());

// node_modules/@mantine/core/esm/Paper/Paper.styles.js
var useStyles32 = createStyles((theme, { radius: radius2, shadow, withBorder }) => ({
  root: {
    outline: 0,
    WebkitTapHighlightColor: "transparent",
    display: "block",
    textDecoration: "none",
    color: theme.colorScheme === "dark" ? theme.colors.dark[0] : theme.black,
    backgroundColor: theme.colorScheme === "dark" ? theme.colors.dark[7] : theme.white,
    boxSizing: "border-box",
    borderRadius: theme.fn.radius(radius2),
    boxShadow: theme.shadows[shadow] || shadow || "none",
    border: withBorder ? `1px solid ${theme.colorScheme === "dark" ? theme.colors.dark[4] : theme.colors.gray[3]}` : void 0
  }
}));
var Paper_styles_default = useStyles32;

// node_modules/@mantine/core/esm/Paper/Paper.js
var __defProp87 = Object.defineProperty;
var __getOwnPropSymbols89 = Object.getOwnPropertySymbols;
var __hasOwnProp89 = Object.prototype.hasOwnProperty;
var __propIsEnum89 = Object.prototype.propertyIsEnumerable;
var __defNormalProp87 = (obj, key, value) => key in obj ? __defProp87(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues87 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp89.call(b2, prop))
      __defNormalProp87(a2, prop, b2[prop]);
  if (__getOwnPropSymbols89)
    for (var prop of __getOwnPropSymbols89(b2)) {
      if (__propIsEnum89.call(b2, prop))
        __defNormalProp87(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest53 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp89.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols89)
    for (var prop of __getOwnPropSymbols89(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum89.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps39 = {};
var _Paper = (0, import_react149.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("Paper", defaultProps39, props), { className, children, radius: radius2, withBorder, shadow, unstyled } = _a, others = __objRest53(_a, ["className", "children", "radius", "withBorder", "shadow", "unstyled"]);
  const { classes, cx } = Paper_styles_default({ radius: radius2, shadow, withBorder }, { name: "Paper", unstyled });
  return import_react149.default.createElement(Box, __spreadValues87({
    className: cx(classes.root, className),
    ref
  }, others), children);
});
_Paper.displayName = "@mantine/core/Paper";
var Paper = createPolymorphicComponent(_Paper);

// node_modules/@mantine/core/esm/Card/CardSection/CardSection.js
var import_react151 = __toESM(require_react());

// node_modules/@mantine/core/esm/Card/Card.context.js
var import_react150 = __toESM(require_react());
var CardContext = (0, import_react150.createContext)({ padding: 0 });
var CardProvider = CardContext.Provider;
var useCardPadding = () => (0, import_react150.useContext)(CardContext).padding;

// node_modules/@mantine/core/esm/Card/CardSection/CardSection.styles.js
var useStyles33 = createStyles((theme, { padding: padding2, withBorder, inheritPadding }) => {
  const spacing = theme.fn.size({ size: padding2, sizes: theme.spacing });
  const offset3 = -1 * spacing;
  const borderColor = theme.colorScheme === "dark" ? theme.colors.dark[4] : theme.colors.gray[3];
  return {
    cardSection: {
      display: "block",
      marginLeft: offset3,
      marginRight: offset3,
      paddingLeft: inheritPadding ? spacing : void 0,
      paddingRight: inheritPadding ? spacing : void 0,
      borderTop: withBorder && `1px solid ${borderColor}`,
      borderBottom: withBorder && `1px solid ${borderColor}`,
      "& + &": {
        borderTop: 0
      },
      "&[data-first]": {
        marginTop: offset3,
        borderTop: 0,
        borderBottom: withBorder && `1px solid ${borderColor}`
      },
      "&[data-last]": {
        marginBottom: offset3,
        borderBottom: 0
      }
    }
  };
});
var CardSection_styles_default = useStyles33;

// node_modules/@mantine/core/esm/Card/CardSection/CardSection.js
var __defProp88 = Object.defineProperty;
var __getOwnPropSymbols90 = Object.getOwnPropertySymbols;
var __hasOwnProp90 = Object.prototype.hasOwnProperty;
var __propIsEnum90 = Object.prototype.propertyIsEnumerable;
var __defNormalProp88 = (obj, key, value) => key in obj ? __defProp88(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues88 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp90.call(b2, prop))
      __defNormalProp88(a2, prop, b2[prop]);
  if (__getOwnPropSymbols90)
    for (var prop of __getOwnPropSymbols90(b2)) {
      if (__propIsEnum90.call(b2, prop))
        __defNormalProp88(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest54 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp90.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols90)
    for (var prop of __getOwnPropSymbols90(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum90.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps40 = {
  withBorder: false,
  inheritPadding: false
};
var _CardSection = (0, import_react151.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("CardSection", defaultProps40, props), { className, withBorder, inheritPadding, unstyled } = _a, others = __objRest54(_a, ["className", "withBorder", "inheritPadding", "unstyled"]);
  const { classes, cx } = CardSection_styles_default({ padding: useCardPadding(), withBorder, inheritPadding }, { name: "Card", unstyled });
  return import_react151.default.createElement(Box, __spreadValues88({
    className: cx(classes.cardSection, className),
    ref
  }, others));
});
_CardSection.displayName = "@mantine/core/CardSection";
var CardSection = createPolymorphicComponent(_CardSection);

// node_modules/@mantine/core/esm/Card/Card.styles.js
var useStyles34 = createStyles((theme) => ({
  root: {
    position: "relative",
    overflow: "hidden",
    backgroundColor: theme.colorScheme === "dark" ? theme.colors.dark[6] : theme.white
  }
}));
var Card_styles_default = useStyles34;

// node_modules/@mantine/core/esm/Card/Card.js
var __defProp89 = Object.defineProperty;
var __getOwnPropSymbols91 = Object.getOwnPropertySymbols;
var __hasOwnProp91 = Object.prototype.hasOwnProperty;
var __propIsEnum91 = Object.prototype.propertyIsEnumerable;
var __defNormalProp89 = (obj, key, value) => key in obj ? __defProp89(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues89 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp91.call(b2, prop))
      __defNormalProp89(a2, prop, b2[prop]);
  if (__getOwnPropSymbols91)
    for (var prop of __getOwnPropSymbols91(b2)) {
      if (__propIsEnum91.call(b2, prop))
        __defNormalProp89(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest55 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp91.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols91)
    for (var prop of __getOwnPropSymbols91(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum91.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps41 = {
  p: "md"
};
var _Card = (0, import_react152.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("Card", defaultProps41, props), { className, p, radius: radius2, children, unstyled } = _a, others = __objRest55(_a, ["className", "p", "radius", "children", "unstyled"]);
  const { classes, cx } = Card_styles_default(null, { name: "Card", unstyled });
  const _children = import_react152.Children.toArray(children);
  const content = _children.map((child, index6) => {
    if (typeof child === "object" && child && "type" in child && child.type === CardSection) {
      return (0, import_react152.cloneElement)(child, {
        padding: p,
        "data-first": index6 === 0 || void 0,
        "data-last": index6 === _children.length - 1 || void 0
      });
    }
    return child;
  });
  return import_react152.default.createElement(CardProvider, {
    value: { padding: p }
  }, import_react152.default.createElement(Paper, __spreadValues89({
    className: cx(classes.root, className),
    radius: radius2,
    p,
    ref
  }, others), content));
});
_Card.Section = CardSection;
_Card.displayName = "@mantine/core/Card";
var Card = createPolymorphicComponent(_Card);

// node_modules/@mantine/core/esm/Center/Center.js
var import_react153 = __toESM(require_react());
var __defProp90 = Object.defineProperty;
var __getOwnPropSymbols92 = Object.getOwnPropertySymbols;
var __hasOwnProp92 = Object.prototype.hasOwnProperty;
var __propIsEnum92 = Object.prototype.propertyIsEnumerable;
var __defNormalProp90 = (obj, key, value) => key in obj ? __defProp90(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues90 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp92.call(b2, prop))
      __defNormalProp90(a2, prop, b2[prop]);
  if (__getOwnPropSymbols92)
    for (var prop of __getOwnPropSymbols92(b2)) {
      if (__propIsEnum92.call(b2, prop))
        __defNormalProp90(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest56 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp92.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols92)
    for (var prop of __getOwnPropSymbols92(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum92.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var _Center = (0, import_react153.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("Center", {}, props), { inline: inline2, sx } = _a, others = __objRest56(_a, ["inline", "sx"]);
  return import_react153.default.createElement(Box, __spreadValues90({
    ref,
    sx: [
      {
        display: inline2 ? "inline-flex" : "flex",
        alignItems: "center",
        justifyContent: "center"
      },
      ...packSx(sx)
    ]
  }, others));
});
_Center.displayName = "@mantine/core/Center";
var Center = createPolymorphicComponent(_Center);

// node_modules/@mantine/core/esm/Checkbox/Checkbox.js
var import_react162 = __toESM(require_react());

// node_modules/@mantine/core/esm/Checkbox/CheckboxGroup.context.js
var import_react154 = __toESM(require_react());
var CheckboxGroupContext = (0, import_react154.createContext)(null);
var CheckboxGroupProvider = CheckboxGroupContext.Provider;
var useCheckboxGroupContext = () => (0, import_react154.useContext)(CheckboxGroupContext);

// node_modules/@mantine/core/esm/Checkbox/CheckboxGroup/CheckboxGroup.js
var import_react159 = __toESM(require_react());

// node_modules/@mantine/core/esm/Checkbox/CheckboxGroup/InputsGroup.js
var import_react158 = __toESM(require_react());

// node_modules/@mantine/core/esm/Group/Group.js
var import_react156 = __toESM(require_react());

// node_modules/@mantine/core/esm/Group/filter-falsy-children/filter-falsy-children.js
var import_react155 = __toESM(require_react());
function filterFalsyChildren(children) {
  return import_react155.Children.toArray(children).filter(Boolean);
}

// node_modules/@mantine/core/esm/Group/Group.styles.js
var GROUP_POSITIONS = {
  left: "flex-start",
  center: "center",
  right: "flex-end",
  apart: "space-between"
};
var useStyles35 = createStyles((theme, { spacing, position: position2, noWrap, grow, align, count: count2 }) => ({
  root: {
    boxSizing: "border-box",
    display: "flex",
    flexDirection: "row",
    alignItems: align || "center",
    flexWrap: noWrap ? "nowrap" : "wrap",
    justifyContent: GROUP_POSITIONS[position2],
    gap: theme.fn.size({ size: spacing, sizes: theme.spacing }),
    "& > *": {
      boxSizing: "border-box",
      maxWidth: grow ? `calc(${100 / count2}% - ${theme.fn.size({ size: spacing, sizes: theme.spacing }) - theme.fn.size({ size: spacing, sizes: theme.spacing }) / count2}px)` : void 0,
      flexGrow: grow ? 1 : 0
    }
  }
}));
var Group_styles_default = useStyles35;

// node_modules/@mantine/core/esm/Group/Group.js
var __defProp91 = Object.defineProperty;
var __getOwnPropSymbols93 = Object.getOwnPropertySymbols;
var __hasOwnProp93 = Object.prototype.hasOwnProperty;
var __propIsEnum93 = Object.prototype.propertyIsEnumerable;
var __defNormalProp91 = (obj, key, value) => key in obj ? __defProp91(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues91 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp93.call(b2, prop))
      __defNormalProp91(a2, prop, b2[prop]);
  if (__getOwnPropSymbols93)
    for (var prop of __getOwnPropSymbols93(b2)) {
      if (__propIsEnum93.call(b2, prop))
        __defNormalProp91(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest57 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp93.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols93)
    for (var prop of __getOwnPropSymbols93(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum93.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps42 = {
  position: "left",
  spacing: "md"
};
var Group = (0, import_react156.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("Group", defaultProps42, props), { className, position: position2, align, children, noWrap, grow, spacing, unstyled } = _a, others = __objRest57(_a, ["className", "position", "align", "children", "noWrap", "grow", "spacing", "unstyled"]);
  const filteredChildren = filterFalsyChildren(children);
  const { classes, cx } = Group_styles_default({
    align,
    grow,
    noWrap,
    spacing,
    position: position2,
    count: filteredChildren.length
  }, { unstyled, name: "Group" });
  return import_react156.default.createElement(Box, __spreadValues91({
    className: cx(classes.root, className),
    ref
  }, others), filteredChildren);
});
Group.displayName = "@mantine/core/Group";

// node_modules/@mantine/core/esm/Stack/Stack.js
var import_react157 = __toESM(require_react());

// node_modules/@mantine/core/esm/Stack/Stack.styles.js
var useStyles36 = createStyles((theme, { spacing, align, justify }) => ({
  root: {
    display: "flex",
    flexDirection: "column",
    alignItems: align,
    justifyContent: justify,
    gap: theme.fn.size({ size: spacing, sizes: theme.spacing })
  }
}));
var Stack_styles_default = useStyles36;

// node_modules/@mantine/core/esm/Stack/Stack.js
var __defProp92 = Object.defineProperty;
var __getOwnPropSymbols94 = Object.getOwnPropertySymbols;
var __hasOwnProp94 = Object.prototype.hasOwnProperty;
var __propIsEnum94 = Object.prototype.propertyIsEnumerable;
var __defNormalProp92 = (obj, key, value) => key in obj ? __defProp92(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues92 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp94.call(b2, prop))
      __defNormalProp92(a2, prop, b2[prop]);
  if (__getOwnPropSymbols94)
    for (var prop of __getOwnPropSymbols94(b2)) {
      if (__propIsEnum94.call(b2, prop))
        __defNormalProp92(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest58 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp94.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols94)
    for (var prop of __getOwnPropSymbols94(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum94.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps43 = {
  spacing: "md",
  align: "stretch",
  justify: "flex-start"
};
var Stack = (0, import_react157.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("Stack", defaultProps43, props), { spacing, className, align, justify, unstyled } = _a, others = __objRest58(_a, ["spacing", "className", "align", "justify", "unstyled"]);
  const { classes, cx } = Stack_styles_default({ spacing, align, justify }, { name: "Stack", unstyled });
  return import_react157.default.createElement(Box, __spreadValues92({
    className: cx(classes.root, className),
    ref
  }, others));
});
Stack.displayName = "@mantine/core/Stack";

// node_modules/@mantine/core/esm/Checkbox/CheckboxGroup/InputsGroup.js
function InputsGroup({
  spacing,
  offset: offset3,
  orientation,
  children,
  role,
  unstyled
}) {
  if (orientation === "horizontal") {
    return import_react158.default.createElement(Group, {
      pt: offset3,
      spacing,
      role,
      unstyled
    }, children);
  }
  return import_react158.default.createElement(Stack, {
    pt: offset3,
    spacing,
    role,
    unstyled
  }, children);
}

// node_modules/@mantine/core/esm/Checkbox/CheckboxGroup/CheckboxGroup.js
var __defProp93 = Object.defineProperty;
var __getOwnPropSymbols95 = Object.getOwnPropertySymbols;
var __hasOwnProp95 = Object.prototype.hasOwnProperty;
var __propIsEnum95 = Object.prototype.propertyIsEnumerable;
var __defNormalProp93 = (obj, key, value) => key in obj ? __defProp93(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues93 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp95.call(b2, prop))
      __defNormalProp93(a2, prop, b2[prop]);
  if (__getOwnPropSymbols95)
    for (var prop of __getOwnPropSymbols95(b2)) {
      if (__propIsEnum95.call(b2, prop))
        __defNormalProp93(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest59 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp95.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols95)
    for (var prop of __getOwnPropSymbols95(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum95.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps44 = {
  orientation: "horizontal",
  spacing: "lg",
  size: "sm",
  offset: "xs"
};
var CheckboxGroup = (0, import_react159.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("CheckboxGroup", defaultProps44, props), {
    children,
    value,
    defaultValue,
    onChange,
    orientation,
    spacing,
    size: size3,
    wrapperProps,
    offset: offset3
  } = _a, others = __objRest59(_a, [
    "children",
    "value",
    "defaultValue",
    "onChange",
    "orientation",
    "spacing",
    "size",
    "wrapperProps",
    "offset"
  ]);
  const [_value, setValue] = useUncontrolled({
    value,
    defaultValue,
    finalValue: [],
    onChange
  });
  const handleChange = (event) => {
    const itemValue = event.currentTarget.value;
    setValue(_value.includes(itemValue) ? _value.filter((item) => item !== itemValue) : [..._value, itemValue]);
  };
  return import_react159.default.createElement(CheckboxGroupProvider, {
    value: { value: _value, onChange: handleChange, size: size3 }
  }, import_react159.default.createElement(Input.Wrapper, __spreadValues93(__spreadValues93({
    labelElement: "div",
    size: size3,
    __staticSelector: "CheckboxGroup",
    ref
  }, wrapperProps), others), import_react159.default.createElement(InputsGroup, {
    spacing,
    orientation,
    offset: offset3
  }, children)));
});
CheckboxGroup.displayName = "@mantine/core/CheckboxGroup";

// node_modules/@mantine/core/esm/Checkbox/CheckboxIcon.js
var import_react160 = __toESM(require_react());
var __defProp94 = Object.defineProperty;
var __getOwnPropSymbols96 = Object.getOwnPropertySymbols;
var __hasOwnProp96 = Object.prototype.hasOwnProperty;
var __propIsEnum96 = Object.prototype.propertyIsEnumerable;
var __defNormalProp94 = (obj, key, value) => key in obj ? __defProp94(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues94 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp96.call(b2, prop))
      __defNormalProp94(a2, prop, b2[prop]);
  if (__getOwnPropSymbols96)
    for (var prop of __getOwnPropSymbols96(b2)) {
      if (__propIsEnum96.call(b2, prop))
        __defNormalProp94(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest60 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp96.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols96)
    for (var prop of __getOwnPropSymbols96(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum96.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
function CheckIcon(props) {
  return import_react160.default.createElement("svg", __spreadValues94({
    viewBox: "0 0 10 7",
    fill: "none",
    xmlns: "http://www.w3.org/2000/svg"
  }, props), import_react160.default.createElement("path", {
    d: "M4 4.586L1.707 2.293A1 1 0 1 0 .293 3.707l3 3a.997.997 0 0 0 1.414 0l5-5A1 1 0 1 0 8.293.293L4 4.586z",
    fill: "currentColor",
    fillRule: "evenodd",
    clipRule: "evenodd"
  }));
}
function CheckboxIcon(_a) {
  var _b = _a, { indeterminate } = _b, others = __objRest60(_b, ["indeterminate"]);
  if (indeterminate) {
    return import_react160.default.createElement("svg", __spreadValues94({
      xmlns: "http://www.w3.org/2000/svg",
      fill: "none",
      viewBox: "0 0 32 6"
    }, others), import_react160.default.createElement("rect", {
      width: "32",
      height: "6",
      fill: "currentColor",
      rx: "3"
    }));
  }
  return import_react160.default.createElement(CheckIcon, __spreadValues94({}, others));
}

// node_modules/@mantine/core/esm/Checkbox/Checkbox.styles.js
var __defProp95 = Object.defineProperty;
var __defProps41 = Object.defineProperties;
var __getOwnPropDescs41 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols97 = Object.getOwnPropertySymbols;
var __hasOwnProp97 = Object.prototype.hasOwnProperty;
var __propIsEnum97 = Object.prototype.propertyIsEnumerable;
var __defNormalProp95 = (obj, key, value) => key in obj ? __defProp95(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues95 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp97.call(b2, prop))
      __defNormalProp95(a2, prop, b2[prop]);
  if (__getOwnPropSymbols97)
    for (var prop of __getOwnPropSymbols97(b2)) {
      if (__propIsEnum97.call(b2, prop))
        __defNormalProp95(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps41 = (a2, b2) => __defProps41(a2, __getOwnPropDescs41(b2));
var sizes9 = {
  xs: 16,
  sm: 20,
  md: 24,
  lg: 30,
  xl: 36
};
var iconSizes2 = {
  xs: 8,
  sm: 10,
  md: 14,
  lg: 16,
  xl: 20
};
var useStyles37 = createStyles((theme, {
  size: size3,
  radius: radius2,
  color,
  transitionDuration,
  labelPosition,
  error,
  indeterminate
}, getRef2) => {
  const _size = theme.fn.size({ size: size3, sizes: sizes9 });
  const colors = theme.fn.variant({ variant: "filled", color });
  const errorColor = theme.fn.variant({ variant: "filled", color: "red" }).background;
  return {
    icon: {
      ref: getRef2("icon"),
      color: indeterminate ? "inherit" : theme.white,
      transform: indeterminate ? "none" : "translateY(5px) scale(0.5)",
      opacity: indeterminate ? 1 : 0,
      transitionProperty: "opacity, transform",
      transitionTimingFunction: "ease",
      transitionDuration: `${transitionDuration}ms`,
      pointerEvents: "none",
      width: theme.fn.size({ size: size3, sizes: iconSizes2 }),
      position: "absolute",
      zIndex: 1,
      top: 0,
      bottom: 0,
      left: 0,
      right: 0,
      margin: "auto",
      "@media (prefers-reduced-motion)": {
        transitionDuration: theme.respectReducedMotion ? "0ms" : void 0
      }
    },
    inner: {
      position: "relative",
      width: _size,
      height: _size,
      order: labelPosition === "left" ? 2 : 1
    },
    input: __spreadProps41(__spreadValues95({}, theme.fn.focusStyles()), {
      appearance: "none",
      backgroundColor: theme.colorScheme === "dark" ? theme.colors.dark[6] : theme.white,
      border: `1px solid ${error ? errorColor : theme.colorScheme === "dark" ? theme.colors.dark[4] : theme.colors.gray[4]}`,
      width: _size,
      height: _size,
      borderRadius: theme.fn.radius(radius2),
      padding: 0,
      display: "block",
      margin: 0,
      transition: `border-color ${transitionDuration}ms ease, background-color ${transitionDuration}ms ease`,
      cursor: theme.cursorType,
      "&:checked": {
        backgroundColor: colors.background,
        borderColor: colors.background,
        [`& + .${getRef2("icon")}`]: {
          opacity: 1,
          color: theme.white,
          transform: "translateY(0) scale(1)"
        }
      },
      "&:disabled": {
        backgroundColor: theme.colorScheme === "dark" ? theme.colors.dark[4] : theme.colors.gray[2],
        borderColor: theme.colorScheme === "dark" ? theme.colors.dark[6] : theme.colors.gray[3],
        cursor: "not-allowed",
        [`& + .${getRef2("icon")}`]: {
          color: theme.colorScheme === "dark" ? theme.colors.dark[6] : theme.colors.gray[5]
        }
      }
    })
  };
});
var Checkbox_styles_default = useStyles37;

// node_modules/@mantine/core/esm/InlineInput/InlineInput.js
var import_react161 = __toESM(require_react());

// node_modules/@mantine/core/esm/InlineInput/InlineInput.styles.js
var __defProp96 = Object.defineProperty;
var __defProps42 = Object.defineProperties;
var __getOwnPropDescs42 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols98 = Object.getOwnPropertySymbols;
var __hasOwnProp98 = Object.prototype.hasOwnProperty;
var __propIsEnum98 = Object.prototype.propertyIsEnumerable;
var __defNormalProp96 = (obj, key, value) => key in obj ? __defProp96(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues96 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp98.call(b2, prop))
      __defNormalProp96(a2, prop, b2[prop]);
  if (__getOwnPropSymbols98)
    for (var prop of __getOwnPropSymbols98(b2)) {
      if (__propIsEnum98.call(b2, prop))
        __defNormalProp96(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps42 = (a2, b2) => __defProps42(a2, __getOwnPropDescs42(b2));
var sizes10 = {
  xs: 16,
  sm: 20,
  md: 24,
  lg: 30,
  xl: 36
};
var useStyles38 = createStyles((theme, { labelPosition, size: size3 }) => ({
  root: {},
  body: {
    display: "flex"
  },
  labelWrapper: __spreadProps42(__spreadValues96({}, theme.fn.fontStyles()), {
    display: "inline-flex",
    flexDirection: "column",
    WebkitTapHighlightColor: "transparent",
    fontSize: theme.fn.size({ size: size3, sizes: theme.fontSizes }),
    lineHeight: `${theme.fn.size({ size: size3, sizes: sizes10 })}px`,
    color: theme.colorScheme === "dark" ? theme.colors.dark[0] : theme.black,
    cursor: theme.cursorType,
    order: labelPosition === "left" ? 1 : 2
  }),
  description: {
    marginTop: `calc(${theme.spacing.xs}px / 2)`,
    [labelPosition === "left" ? "paddingRight" : "paddingLeft"]: theme.spacing.sm
  },
  error: {
    marginTop: `calc(${theme.spacing.xs}px / 2)`,
    [labelPosition === "left" ? "paddingRight" : "paddingLeft"]: theme.spacing.sm
  },
  label: {
    cursor: theme.cursorType,
    [labelPosition === "left" ? "paddingRight" : "paddingLeft"]: theme.spacing.sm,
    "&[data-disabled]": {
      color: theme.colorScheme === "dark" ? theme.colors.dark[3] : theme.colors.gray[5]
    }
  }
}));
var InlineInput_styles_default = useStyles38;

// node_modules/@mantine/core/esm/InlineInput/InlineInput.js
var __defProp97 = Object.defineProperty;
var __getOwnPropSymbols99 = Object.getOwnPropertySymbols;
var __hasOwnProp99 = Object.prototype.hasOwnProperty;
var __propIsEnum99 = Object.prototype.propertyIsEnumerable;
var __defNormalProp97 = (obj, key, value) => key in obj ? __defProp97(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues97 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp99.call(b2, prop))
      __defNormalProp97(a2, prop, b2[prop]);
  if (__getOwnPropSymbols99)
    for (var prop of __getOwnPropSymbols99(b2)) {
      if (__propIsEnum99.call(b2, prop))
        __defNormalProp97(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest61 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp99.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols99)
    for (var prop of __getOwnPropSymbols99(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum99.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
function InlineInput(_a) {
  var _b = _a, {
    __staticSelector,
    className,
    classNames,
    styles: styles2,
    unstyled,
    children,
    label,
    description,
    id,
    disabled,
    error,
    size: size3,
    labelPosition
  } = _b, others = __objRest61(_b, [
    "__staticSelector",
    "className",
    "classNames",
    "styles",
    "unstyled",
    "children",
    "label",
    "description",
    "id",
    "disabled",
    "error",
    "size",
    "labelPosition"
  ]);
  const { classes, cx } = InlineInput_styles_default({ size: size3, labelPosition }, { name: __staticSelector, styles: styles2, classNames, unstyled });
  return import_react161.default.createElement(Box, __spreadValues97({
    className: cx(classes.root, className)
  }, others), import_react161.default.createElement("div", {
    className: cx(classes.body)
  }, children, import_react161.default.createElement("div", {
    className: classes.labelWrapper
  }, label && import_react161.default.createElement("label", {
    className: classes.label,
    "data-disabled": disabled || void 0,
    htmlFor: id
  }, label), description && import_react161.default.createElement(Input.Description, {
    className: classes.description
  }, description), error && error !== "boolean" && import_react161.default.createElement(Input.Error, {
    className: classes.error
  }, error))));
}
InlineInput.displayName = "@mantine/core/InlineInput";

// node_modules/@mantine/core/esm/Checkbox/Checkbox.js
var __defProp98 = Object.defineProperty;
var __getOwnPropSymbols100 = Object.getOwnPropertySymbols;
var __hasOwnProp100 = Object.prototype.hasOwnProperty;
var __propIsEnum100 = Object.prototype.propertyIsEnumerable;
var __defNormalProp98 = (obj, key, value) => key in obj ? __defProp98(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues98 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp100.call(b2, prop))
      __defNormalProp98(a2, prop, b2[prop]);
  if (__getOwnPropSymbols100)
    for (var prop of __getOwnPropSymbols100(b2)) {
      if (__propIsEnum100.call(b2, prop))
        __defNormalProp98(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest62 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp100.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols100)
    for (var prop of __getOwnPropSymbols100(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum100.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps45 = {
  size: "sm",
  transitionDuration: 100,
  icon: CheckboxIcon,
  labelPosition: "right"
};
var Checkbox = (0, import_react162.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("Checkbox", defaultProps45, props), {
    className,
    style,
    sx,
    checked,
    disabled,
    color,
    label,
    indeterminate,
    id,
    size: size3,
    radius: radius2,
    wrapperProps,
    children,
    classNames,
    styles: styles2,
    transitionDuration,
    icon: Icon,
    unstyled,
    labelPosition,
    description,
    error
  } = _a, others = __objRest62(_a, [
    "className",
    "style",
    "sx",
    "checked",
    "disabled",
    "color",
    "label",
    "indeterminate",
    "id",
    "size",
    "radius",
    "wrapperProps",
    "children",
    "classNames",
    "styles",
    "transitionDuration",
    "icon",
    "unstyled",
    "labelPosition",
    "description",
    "error"
  ]);
  const ctx = useCheckboxGroupContext();
  const uuid = useId(id);
  const { systemStyles, rest } = extractSystemStyles(others);
  const { classes } = Checkbox_styles_default({
    size: (ctx == null ? void 0 : ctx.size) || size3,
    radius: radius2,
    color,
    transitionDuration,
    labelPosition,
    error: !!error,
    indeterminate
  }, { name: "Checkbox", classNames, styles: styles2, unstyled });
  const contextProps = ctx ? {
    checked: ctx.value.includes(rest.value),
    onChange: ctx.onChange
  } : {};
  return import_react162.default.createElement(InlineInput, __spreadValues98(__spreadValues98({
    className,
    sx,
    style,
    id: uuid,
    size: (ctx == null ? void 0 : ctx.size) || size3,
    labelPosition,
    label,
    description,
    error,
    disabled,
    __staticSelector: "Checkbox",
    classNames,
    styles: styles2,
    unstyled,
    "data-checked": contextProps.checked || void 0
  }, systemStyles), wrapperProps), import_react162.default.createElement("div", {
    className: classes.inner
  }, import_react162.default.createElement("input", __spreadValues98(__spreadValues98({
    id: uuid,
    ref,
    type: "checkbox",
    className: classes.input,
    checked,
    disabled
  }, rest), contextProps)), import_react162.default.createElement(Icon, {
    indeterminate,
    className: classes.icon
  })));
});
Checkbox.displayName = "@mantine/core/Checkbox";
Checkbox.Group = CheckboxGroup;

// node_modules/@mantine/core/esm/Chip/Chip.js
var import_react165 = __toESM(require_react());

// node_modules/@mantine/core/esm/Chip/ChipGroup/ChipGroup.js
var import_react164 = __toESM(require_react());

// node_modules/@mantine/core/esm/Chip/ChipGroup.context.js
var import_react163 = __toESM(require_react());
var ChipGroupContext = (0, import_react163.createContext)(null);
var ChipGroupProvider = ChipGroupContext.Provider;
var useChipGroup = () => (0, import_react163.useContext)(ChipGroupContext);

// node_modules/@mantine/core/esm/Chip/ChipGroup/ChipGroup.js
var __defProp99 = Object.defineProperty;
var __getOwnPropSymbols101 = Object.getOwnPropertySymbols;
var __hasOwnProp101 = Object.prototype.hasOwnProperty;
var __propIsEnum101 = Object.prototype.propertyIsEnumerable;
var __defNormalProp99 = (obj, key, value) => key in obj ? __defProp99(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues99 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp101.call(b2, prop))
      __defNormalProp99(a2, prop, b2[prop]);
  if (__getOwnPropSymbols101)
    for (var prop of __getOwnPropSymbols101(b2)) {
      if (__propIsEnum101.call(b2, prop))
        __defNormalProp99(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest63 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp101.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols101)
    for (var prop of __getOwnPropSymbols101(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum101.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps46 = {
  spacing: "xs"
};
function ChipGroup(props) {
  const _a = useComponentDefaultProps("ChipGroup", defaultProps46, props), { value, defaultValue, onChange, spacing, multiple, children, unstyled } = _a, others = __objRest63(_a, ["value", "defaultValue", "onChange", "spacing", "multiple", "children", "unstyled"]);
  const [_value, setValue] = useUncontrolled({
    value,
    defaultValue,
    finalValue: multiple ? [] : null,
    onChange
  });
  const isChipSelected = (val) => Array.isArray(_value) ? _value.includes(val) : val === _value;
  const handleChange = (event) => {
    const val = event.currentTarget.value;
    if (Array.isArray(_value)) {
      setValue(_value.includes(val) ? _value.filter((v) => v !== val) : [..._value, val]);
    } else {
      setValue(val);
    }
  };
  return import_react164.default.createElement(ChipGroupProvider, {
    value: { isChipSelected, onChange: handleChange, multiple }
  }, import_react164.default.createElement(Group, __spreadValues99({
    spacing,
    unstyled
  }, others), children));
}
ChipGroup.displayName = "@mantine/core/ChipGroup";

// node_modules/@mantine/core/esm/Chip/Chip.styles.js
var __defProp100 = Object.defineProperty;
var __defProps43 = Object.defineProperties;
var __getOwnPropDescs43 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols102 = Object.getOwnPropertySymbols;
var __hasOwnProp102 = Object.prototype.hasOwnProperty;
var __propIsEnum102 = Object.prototype.propertyIsEnumerable;
var __defNormalProp100 = (obj, key, value) => key in obj ? __defProp100(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues100 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp102.call(b2, prop))
      __defNormalProp100(a2, prop, b2[prop]);
  if (__getOwnPropSymbols102)
    for (var prop of __getOwnPropSymbols102(b2)) {
      if (__propIsEnum102.call(b2, prop))
        __defNormalProp100(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps43 = (a2, b2) => __defProps43(a2, __getOwnPropDescs43(b2));
var sizes11 = {
  xs: 24,
  sm: 28,
  md: 32,
  lg: 36,
  xl: 40
};
var iconSizes3 = {
  xs: 10,
  sm: 12,
  md: 14,
  lg: 16,
  xl: 18
};
var padding = {
  xs: 16,
  sm: 20,
  md: 24,
  lg: 28,
  xl: 32
};
var checkedPadding = {
  xs: 7.5,
  sm: 10,
  md: 11.5,
  lg: 13,
  xl: 15
};
var useStyles39 = createStyles((theme, { radius: radius2, size: size3, color }, getRef2) => ({
  root: {},
  label: __spreadProps43(__spreadValues100({
    ref: getRef2("label")
  }, theme.fn.fontStyles()), {
    boxSizing: "border-box",
    color: theme.colorScheme === "dark" ? theme.colors.dark[0] : theme.black,
    display: "inline-block",
    alignItems: "center",
    userSelect: "none",
    border: "1px solid transparent",
    borderRadius: theme.fn.radius(radius2),
    height: theme.fn.size({ size: size3, sizes: sizes11 }),
    fontSize: theme.fn.size({ size: size3, sizes: theme.fontSizes }),
    lineHeight: `${theme.fn.size({ size: size3, sizes: sizes11 }) - 2}px`,
    paddingLeft: theme.fn.size({ size: size3, sizes: padding }),
    paddingRight: theme.fn.size({ size: size3, sizes: padding }),
    cursor: "pointer",
    whiteSpace: "nowrap",
    transition: "background-color 100ms ease",
    WebkitTapHighlightColor: "transparent",
    '&[data-variant="filled"]': __spreadValues100({
      backgroundColor: theme.colorScheme === "dark" ? theme.colors.dark[4] : theme.colors.gray[1]
    }, theme.fn.hover({
      backgroundColor: theme.colorScheme === "dark" ? theme.colors.dark[5] : theme.colors.gray[0]
    })),
    '&[data-variant="outline"]': __spreadValues100({
      backgroundColor: theme.colorScheme === "dark" ? theme.colors.dark[6] : theme.white,
      borderColor: theme.colorScheme === "dark" ? theme.colors.dark[4] : theme.colors.gray[4]
    }, theme.fn.hover({
      backgroundColor: theme.colorScheme === "dark" ? theme.colors.dark[5] : theme.colors.gray[0]
    })),
    "&[data-disabled]": __spreadProps43(__spreadValues100({
      backgroundColor: `${theme.colorScheme === "dark" ? theme.colors.dark[5] : theme.colors.gray[1]} !important`,
      borderColor: `${theme.colorScheme === "dark" ? theme.colors.dark[5] : theme.colors.gray[1]} !important`,
      color: theme.colorScheme === "dark" ? theme.colors.dark[3] : theme.colors.gray[5],
      cursor: "not-allowed"
    }, theme.fn.hover({
      backgroundColor: theme.colorScheme === "dark" ? theme.colors.dark[5] : theme.colors.gray[1]
    })), {
      [`& .${getRef2("iconWrapper")}`]: {
        color: theme.colorScheme === "dark" ? theme.colors.dark[3] : theme.colors.gray[5]
      }
    }),
    "&[data-checked]": {
      paddingLeft: theme.fn.size({ size: size3, sizes: checkedPadding }),
      paddingRight: theme.fn.size({ size: size3, sizes: checkedPadding }),
      '&[data-variant="outline"]': {
        border: `1px solid ${theme.fn.variant({ variant: "filled", color }).background}`
      },
      '&[data-variant="filled"]': {
        "&, &:hover": {
          backgroundColor: theme.fn.variant({ variant: "light", color }).background
        }
      }
    }
  }),
  iconWrapper: {
    ref: getRef2("iconWrapper"),
    color: theme.fn.variant({ variant: "filled", color }).background,
    width: theme.fn.size({ size: size3, sizes: iconSizes3 }) + theme.fn.size({ size: size3, sizes: theme.spacing }) / 1.5,
    maxWidth: theme.fn.size({ size: size3, sizes: iconSizes3 }) + theme.fn.size({ size: size3, sizes: theme.spacing }) / 1.5,
    height: theme.fn.size({ size: size3, sizes: iconSizes3 }),
    display: "inline-block",
    verticalAlign: "middle",
    overflow: "hidden"
  },
  checkIcon: {
    width: theme.fn.size({ size: size3, sizes: iconSizes3 }),
    height: theme.fn.size({ size: size3, sizes: iconSizes3 }) / 1.1,
    display: "block"
  },
  input: {
    width: 0,
    height: 0,
    padding: 0,
    opacity: 0,
    margin: 0,
    "&:focus": {
      outline: "none",
      [`& + .${getRef2("label")}`]: __spreadValues100({}, theme.focusRing === "always" || theme.focusRing === "auto" ? theme.focusRingStyles.styles(theme) : theme.focusRingStyles.resetStyles(theme)),
      "&:focus:not(:focus-visible)": {
        [`& + .${getRef2("label")}`]: __spreadValues100({}, theme.focusRing === "auto" || theme.focusRing === "never" ? theme.focusRingStyles.resetStyles(theme) : null)
      }
    }
  }
}));
var Chip_styles_default = useStyles39;

// node_modules/@mantine/core/esm/Chip/Chip.js
var __defProp101 = Object.defineProperty;
var __getOwnPropSymbols103 = Object.getOwnPropertySymbols;
var __hasOwnProp103 = Object.prototype.hasOwnProperty;
var __propIsEnum103 = Object.prototype.propertyIsEnumerable;
var __defNormalProp101 = (obj, key, value) => key in obj ? __defProp101(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues101 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp103.call(b2, prop))
      __defNormalProp101(a2, prop, b2[prop]);
  if (__getOwnPropSymbols103)
    for (var prop of __getOwnPropSymbols103(b2)) {
      if (__propIsEnum103.call(b2, prop))
        __defNormalProp101(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest64 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp103.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols103)
    for (var prop of __getOwnPropSymbols103(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum103.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps47 = {
  type: "checkbox",
  size: "sm",
  radius: "xl",
  variant: "outline"
};
var Chip = (0, import_react165.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("Chip", defaultProps47, props), {
    radius: radius2,
    type,
    size: size3,
    variant: variant2,
    disabled,
    id,
    color,
    children,
    className,
    classNames,
    style,
    styles: styles2,
    checked,
    defaultChecked,
    onChange,
    sx,
    wrapperProps,
    value,
    unstyled
  } = _a, others = __objRest64(_a, [
    "radius",
    "type",
    "size",
    "variant",
    "disabled",
    "id",
    "color",
    "children",
    "className",
    "classNames",
    "style",
    "styles",
    "checked",
    "defaultChecked",
    "onChange",
    "sx",
    "wrapperProps",
    "value",
    "unstyled"
  ]);
  const ctx = useChipGroup();
  const uuid = useId(id);
  const { systemStyles, rest } = extractSystemStyles(others);
  const { classes, cx } = Chip_styles_default({ radius: radius2, size: size3, color }, { classNames, styles: styles2, unstyled, name: "Chip" });
  const [_value, setValue] = useUncontrolled({
    value: checked,
    defaultValue: defaultChecked,
    finalValue: false,
    onChange
  });
  const contextProps = ctx ? {
    checked: ctx.isChipSelected(value),
    onChange: ctx.onChange,
    type: ctx.multiple ? "checkbox" : "radio"
  } : {};
  const _checked = contextProps.checked || _value;
  return import_react165.default.createElement(Box, __spreadValues101(__spreadValues101({
    className: cx(classes.root, className),
    style,
    sx
  }, systemStyles), wrapperProps), import_react165.default.createElement("input", __spreadValues101(__spreadValues101({
    type,
    className: classes.input,
    checked: _checked,
    onChange: (event) => setValue(event.currentTarget.checked),
    id: uuid,
    disabled,
    ref,
    value
  }, contextProps), rest)), import_react165.default.createElement("label", {
    htmlFor: uuid,
    "data-checked": _checked || void 0,
    "data-disabled": disabled || void 0,
    "data-variant": variant2,
    className: classes.label
  }, _checked && import_react165.default.createElement("span", {
    className: classes.iconWrapper
  }, import_react165.default.createElement(CheckIcon, {
    className: classes.checkIcon
  })), children));
});
Chip.displayName = "@mantine/core/Chip";
Chip.Group = ChipGroup;

// node_modules/@mantine/core/esm/Code/Code.js
var import_react166 = __toESM(require_react());

// node_modules/@mantine/core/esm/Code/Code.styles.js
var __defProp102 = Object.defineProperty;
var __defProps44 = Object.defineProperties;
var __getOwnPropDescs44 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols104 = Object.getOwnPropertySymbols;
var __hasOwnProp104 = Object.prototype.hasOwnProperty;
var __propIsEnum104 = Object.prototype.propertyIsEnumerable;
var __defNormalProp102 = (obj, key, value) => key in obj ? __defProp102(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues102 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp104.call(b2, prop))
      __defNormalProp102(a2, prop, b2[prop]);
  if (__getOwnPropSymbols104)
    for (var prop of __getOwnPropSymbols104(b2)) {
      if (__propIsEnum104.call(b2, prop))
        __defNormalProp102(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps44 = (a2, b2) => __defProps44(a2, __getOwnPropDescs44(b2));
var useStyles40 = createStyles((theme, { color: _color }) => {
  const color = _color || (theme.colorScheme === "dark" ? "dark" : "gray");
  const colors = theme.fn.variant({ color, variant: "light" });
  return {
    root: __spreadProps44(__spreadValues102({}, theme.fn.fontStyles()), {
      lineHeight: theme.lineHeight,
      padding: `2px calc(${theme.spacing.xs}px / 2)`,
      borderRadius: theme.radius.sm,
      color: theme.colorScheme === "dark" ? color === "dark" ? theme.colors.dark[0] : theme.white : theme.colors.dark[7],
      backgroundColor: theme.colorScheme === "dark" && color === "dark" ? theme.colors.dark[5] : colors.background,
      fontFamily: theme.fontFamilyMonospace,
      fontSize: theme.fontSizes.xs
    }),
    block: {
      padding: theme.spacing.xs,
      margin: 0,
      overflowX: "auto"
    }
  };
});
var Code_styles_default = useStyles40;

// node_modules/@mantine/core/esm/Code/Code.js
var __defProp103 = Object.defineProperty;
var __getOwnPropSymbols105 = Object.getOwnPropertySymbols;
var __hasOwnProp105 = Object.prototype.hasOwnProperty;
var __propIsEnum105 = Object.prototype.propertyIsEnumerable;
var __defNormalProp103 = (obj, key, value) => key in obj ? __defProp103(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues103 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp105.call(b2, prop))
      __defNormalProp103(a2, prop, b2[prop]);
  if (__getOwnPropSymbols105)
    for (var prop of __getOwnPropSymbols105(b2)) {
      if (__propIsEnum105.call(b2, prop))
        __defNormalProp103(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest65 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp105.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols105)
    for (var prop of __getOwnPropSymbols105(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum105.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var Code = (0, import_react166.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("Code", {}, props), { className, children, block, color, unstyled } = _a, others = __objRest65(_a, ["className", "children", "block", "color", "unstyled"]);
  const { classes, cx } = Code_styles_default({ color }, { name: "Code", unstyled });
  if (block) {
    return import_react166.default.createElement(Box, __spreadValues103({
      component: "pre",
      dir: "ltr",
      className: cx(classes.root, classes.block, className),
      ref
    }, others), children);
  }
  return import_react166.default.createElement(Box, __spreadValues103({
    component: "code",
    className: cx(classes.root, className),
    ref,
    dir: "ltr"
  }, others), children);
});
Code.displayName = "@mantine/core/Code";

// node_modules/@mantine/core/esm/ColorInput/ColorInput.js
var import_react176 = __toESM(require_react());

// node_modules/@mantine/core/esm/ColorPicker/ColorPicker.js
var import_react174 = __toESM(require_react());

// node_modules/@mantine/core/esm/ColorSwatch/ColorSwatch.js
var import_react167 = __toESM(require_react());

// node_modules/@mantine/core/esm/ColorSwatch/ColorSwatch.styles.js
var __defProp104 = Object.defineProperty;
var __defProps45 = Object.defineProperties;
var __getOwnPropDescs45 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols106 = Object.getOwnPropertySymbols;
var __hasOwnProp106 = Object.prototype.hasOwnProperty;
var __propIsEnum106 = Object.prototype.propertyIsEnumerable;
var __defNormalProp104 = (obj, key, value) => key in obj ? __defProp104(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues104 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp106.call(b2, prop))
      __defNormalProp104(a2, prop, b2[prop]);
  if (__getOwnPropSymbols106)
    for (var prop of __getOwnPropSymbols106(b2)) {
      if (__propIsEnum106.call(b2, prop))
        __defNormalProp104(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps45 = (a2, b2) => __defProps45(a2, __getOwnPropDescs45(b2));
var useStyles41 = createStyles((theme, { size: size3, radius: radius2 }) => {
  const overlayColor = theme.colorScheme === "dark" ? theme.colors.dark[4] : theme.colors.gray[3];
  return {
    root: __spreadProps45(__spreadValues104({}, theme.fn.focusStyles()), {
      width: size3,
      height: size3,
      WebkitTapHighlightColor: "transparent",
      border: 0,
      borderRadius: theme.fn.size({ size: radius2, sizes: theme.radius }),
      appearance: "none",
      WebkitAppearance: "none",
      padding: 0,
      position: "relative",
      overflow: "hidden"
    }),
    overlay: {
      position: "absolute",
      borderRadius: theme.fn.size({ size: radius2, sizes: theme.radius }),
      top: 0,
      left: 0,
      right: 0,
      bottom: 0
    },
    children: {
      display: "inline-flex",
      justifyContent: "center",
      alignItems: "center"
    },
    shadowOverlay: {
      boxShadow: "rgba(0, 0, 0, .1) 0px 0px 0px 1px inset, rgb(0, 0, 0, .15) 0px 0px 4px inset",
      zIndex: 1
    },
    alphaOverlay: {
      backgroundImage: `linear-gradient(45deg, ${overlayColor} 25%, transparent 25%), linear-gradient(-45deg, ${overlayColor} 25%, transparent 25%), linear-gradient(45deg, transparent 75%, ${overlayColor} 75%), linear-gradient(-45deg, ${theme.colorScheme === "dark" ? theme.colors.dark[7] : theme.white} 75%, ${overlayColor} 75%)`,
      backgroundSize: "8px 8px",
      backgroundPosition: "0 0, 0 4px, 4px -4px, -4px 0px"
    }
  };
});
var ColorSwatch_styles_default = useStyles41;

// node_modules/@mantine/core/esm/ColorSwatch/ColorSwatch.js
var __defProp105 = Object.defineProperty;
var __getOwnPropSymbols107 = Object.getOwnPropertySymbols;
var __hasOwnProp107 = Object.prototype.hasOwnProperty;
var __propIsEnum107 = Object.prototype.propertyIsEnumerable;
var __defNormalProp105 = (obj, key, value) => key in obj ? __defProp105(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues105 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp107.call(b2, prop))
      __defNormalProp105(a2, prop, b2[prop]);
  if (__getOwnPropSymbols107)
    for (var prop of __getOwnPropSymbols107(b2)) {
      if (__propIsEnum107.call(b2, prop))
        __defNormalProp105(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest66 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp107.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols107)
    for (var prop of __getOwnPropSymbols107(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum107.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps48 = {
  size: 25,
  radius: 25,
  withShadow: true
};
var _ColorSwatch = (0, import_react167.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("ColorSwatch", defaultProps48, props), {
    color,
    size: size3,
    radius: radius2,
    className,
    children,
    classNames,
    styles: styles2,
    unstyled,
    withShadow
  } = _a, others = __objRest66(_a, [
    "color",
    "size",
    "radius",
    "className",
    "children",
    "classNames",
    "styles",
    "unstyled",
    "withShadow"
  ]);
  const { classes, cx } = ColorSwatch_styles_default({ radius: radius2, size: size3 }, { classNames, styles: styles2, unstyled, name: "ColorSwatch" });
  return import_react167.default.createElement(Box, __spreadValues105({
    className: cx(classes.root, className),
    ref
  }, others), import_react167.default.createElement("div", {
    className: cx(classes.alphaOverlay, classes.overlay)
  }), withShadow && import_react167.default.createElement("div", {
    className: cx(classes.shadowOverlay, classes.overlay)
  }), import_react167.default.createElement("div", {
    className: classes.overlay,
    style: { backgroundColor: color }
  }), import_react167.default.createElement("div", {
    className: cx(classes.children, classes.overlay)
  }, children));
});
_ColorSwatch.displayName = "@mantine/core/ColorSwatch";
var ColorSwatch = createPolymorphicComponent(_ColorSwatch);

// node_modules/@mantine/core/esm/ColorPicker/HueSlider/HueSlider.js
var import_react170 = __toESM(require_react());

// node_modules/@mantine/core/esm/ColorPicker/ColorSlider/ColorSlider.js
var import_react169 = __toESM(require_react());

// node_modules/@mantine/core/esm/ColorPicker/Thumb/Thumb.js
var import_react168 = __toESM(require_react());

// node_modules/@mantine/core/esm/ColorPicker/Thumb/Thumb.styles.js
var THUMB_SIZES = {
  xs: 8,
  sm: 12,
  md: 16,
  lg: 20,
  xl: 22
};
var useStyles42 = createStyles((theme, { size: size3 }) => {
  const _size = theme.fn.size({ size: size3, sizes: THUMB_SIZES });
  return {
    thumb: {
      overflow: "hidden",
      boxSizing: "border-box",
      position: "absolute",
      boxShadow: "0 0 1px rgba(0, 0, 0, .6)",
      border: `2px solid ${theme.white}`,
      backgroundColor: "transparent",
      width: _size,
      height: _size,
      borderRadius: _size
    }
  };
});
var Thumb_styles_default = useStyles42;

// node_modules/@mantine/core/esm/ColorPicker/Thumb/Thumb.js
var __defProp106 = Object.defineProperty;
var __getOwnPropSymbols108 = Object.getOwnPropertySymbols;
var __hasOwnProp108 = Object.prototype.hasOwnProperty;
var __propIsEnum108 = Object.prototype.propertyIsEnumerable;
var __defNormalProp106 = (obj, key, value) => key in obj ? __defProp106(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues106 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp108.call(b2, prop))
      __defNormalProp106(a2, prop, b2[prop]);
  if (__getOwnPropSymbols108)
    for (var prop of __getOwnPropSymbols108(b2)) {
      if (__propIsEnum108.call(b2, prop))
        __defNormalProp106(a2, prop, b2[prop]);
    }
  return a2;
};
function Thumb({
  position: position2,
  className,
  styles: styles2,
  classNames,
  style,
  size: size3,
  __staticSelector,
  unstyled
}) {
  const { classes, cx } = Thumb_styles_default({ size: size3 }, { classNames, styles: styles2, name: __staticSelector, unstyled });
  return import_react168.default.createElement("div", {
    className: cx(classes.thumb, className),
    style: __spreadValues106({
      left: `calc(${position2.x * 100}% - ${THUMB_SIZES[size3] / 2}px)`,
      top: `calc(${position2.y * 100}% - ${THUMB_SIZES[size3] / 2}px)`
    }, style)
  });
}
Thumb.displayName = "@mantine/core/Thumb";

// node_modules/@mantine/core/esm/ColorPicker/ColorSlider/ColorSlider.styles.js
var __defProp107 = Object.defineProperty;
var __getOwnPropSymbols109 = Object.getOwnPropertySymbols;
var __hasOwnProp109 = Object.prototype.hasOwnProperty;
var __propIsEnum109 = Object.prototype.propertyIsEnumerable;
var __defNormalProp107 = (obj, key, value) => key in obj ? __defProp107(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues107 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp109.call(b2, prop))
      __defNormalProp107(a2, prop, b2[prop]);
  if (__getOwnPropSymbols109)
    for (var prop of __getOwnPropSymbols109(b2)) {
      if (__propIsEnum109.call(b2, prop))
        __defNormalProp107(a2, prop, b2[prop]);
    }
  return a2;
};
var useStyles43 = createStyles((theme, { size: size3 }, getRef2) => ({
  sliderThumb: {
    ref: getRef2("sliderThumb")
  },
  slider: {
    position: "relative",
    height: theme.fn.size({ size: size3, sizes: THUMB_SIZES }) + 2,
    boxSizing: "border-box",
    marginLeft: theme.fn.size({ size: size3, sizes: THUMB_SIZES }) / 2,
    marginRight: theme.fn.size({ size: size3, sizes: THUMB_SIZES }) / 2,
    outline: 0,
    [`&:focus .${getRef2("sliderThumb")}`]: __spreadValues107({}, theme.focusRing === "always" || theme.focusRing === "auto" ? theme.focusRingStyles.styles(theme) : theme.focusRingStyles.resetStyles(theme)),
    [`&:focus:not(:focus-visible) .${getRef2("sliderThumb")}`]: __spreadValues107({}, theme.focusRing === "auto" || theme.focusRing === "never" ? theme.focusRingStyles.resetStyles(theme) : null)
  },
  sliderOverlay: {
    position: "absolute",
    boxSizing: "border-box",
    top: 0,
    bottom: 0,
    left: -theme.fn.size({ size: size3, sizes: THUMB_SIZES }) / 2 - 1,
    right: -theme.fn.size({ size: size3, sizes: THUMB_SIZES }) / 2 - 1,
    borderRadius: 1e3
  }
}));
var ColorSlider_styles_default = useStyles43;

// node_modules/@mantine/core/esm/ColorPicker/ColorSlider/ColorSlider.js
var __defProp108 = Object.defineProperty;
var __defProps46 = Object.defineProperties;
var __getOwnPropDescs46 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols110 = Object.getOwnPropertySymbols;
var __hasOwnProp110 = Object.prototype.hasOwnProperty;
var __propIsEnum110 = Object.prototype.propertyIsEnumerable;
var __defNormalProp108 = (obj, key, value) => key in obj ? __defProp108(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues108 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp110.call(b2, prop))
      __defNormalProp108(a2, prop, b2[prop]);
  if (__getOwnPropSymbols110)
    for (var prop of __getOwnPropSymbols110(b2)) {
      if (__propIsEnum110.call(b2, prop))
        __defNormalProp108(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps46 = (a2, b2) => __defProps46(a2, __getOwnPropDescs46(b2));
var __objRest67 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp110.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols110)
    for (var prop of __getOwnPropSymbols110(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum110.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var ColorSlider = (0, import_react169.forwardRef)((_a, ref) => {
  var _b = _a, {
    value,
    onChange,
    onChangeEnd,
    maxValue,
    round: round4,
    size: size3 = "md",
    thumbColor = "transparent",
    __staticSelector = "ColorSlider",
    focusable: focusable2 = true,
    overlays,
    classNames,
    styles: styles2,
    className,
    unstyled
  } = _b, others = __objRest67(_b, [
    "value",
    "onChange",
    "onChangeEnd",
    "maxValue",
    "round",
    "size",
    "thumbColor",
    "__staticSelector",
    "focusable",
    "overlays",
    "classNames",
    "styles",
    "className",
    "unstyled"
  ]);
  const { classes, cx } = ColorSlider_styles_default({ size: size3 }, { classNames, styles: styles2, name: __staticSelector, unstyled });
  const [position2, setPosition] = (0, import_react169.useState)({ y: 0, x: value / maxValue });
  const positionRef = (0, import_react169.useRef)(position2);
  const getChangeValue2 = (val) => round4 ? Math.round(val * maxValue) : val * maxValue;
  const { ref: sliderRef } = useMove(({ x, y }) => {
    positionRef.current = { x, y };
    onChange(getChangeValue2(x));
  }, {
    onScrubEnd: () => {
      const { x } = positionRef.current;
      onChangeEnd(getChangeValue2(x));
    }
  });
  useDidUpdate(() => {
    setPosition({ y: 0, x: value / maxValue });
  }, [value]);
  const handleArrow = (event, pos) => {
    event.preventDefault();
    const _position = clampUseMovePosition(pos);
    onChange(getChangeValue2(_position.x));
    onChangeEnd(getChangeValue2(_position.x));
  };
  const handleKeyDown = (event) => {
    switch (event.key) {
      case "ArrowRight": {
        handleArrow(event, { x: position2.x + 0.05, y: position2.y });
        break;
      }
      case "ArrowLeft": {
        handleArrow(event, { x: position2.x - 0.05, y: position2.y });
        break;
      }
    }
  };
  const layers = overlays.map((overlay, index6) => import_react169.default.createElement("div", {
    className: classes.sliderOverlay,
    style: overlay,
    key: index6
  }));
  return import_react169.default.createElement(Box, __spreadProps46(__spreadValues108({}, others), {
    ref: useMergedRef(sliderRef, ref),
    className: cx(classes.slider, className),
    role: "slider",
    "aria-valuenow": value,
    "aria-valuemax": maxValue,
    "aria-valuemin": 0,
    tabIndex: focusable2 ? 0 : -1,
    onKeyDown: handleKeyDown
  }), layers, import_react169.default.createElement(Thumb, {
    __staticSelector,
    classNames,
    styles: styles2,
    position: position2,
    style: { top: 1, backgroundColor: thumbColor },
    className: classes.sliderThumb,
    size: size3
  }));
});
ColorSlider.displayName = "@mantine/core/ColorSlider";

// node_modules/@mantine/core/esm/ColorPicker/HueSlider/HueSlider.js
var __defProp109 = Object.defineProperty;
var __defProps47 = Object.defineProperties;
var __getOwnPropDescs47 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols111 = Object.getOwnPropertySymbols;
var __hasOwnProp111 = Object.prototype.hasOwnProperty;
var __propIsEnum111 = Object.prototype.propertyIsEnumerable;
var __defNormalProp109 = (obj, key, value) => key in obj ? __defProp109(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues109 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp111.call(b2, prop))
      __defNormalProp109(a2, prop, b2[prop]);
  if (__getOwnPropSymbols111)
    for (var prop of __getOwnPropSymbols111(b2)) {
      if (__propIsEnum111.call(b2, prop))
        __defNormalProp109(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps47 = (a2, b2) => __defProps47(a2, __getOwnPropDescs47(b2));
var __objRest68 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp111.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols111)
    for (var prop of __getOwnPropSymbols111(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum111.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var HueSlider = (0, import_react170.forwardRef)((_a, ref) => {
  var _b = _a, { value, onChange, onChangeEnd } = _b, others = __objRest68(_b, ["value", "onChange", "onChangeEnd"]);
  return import_react170.default.createElement(ColorSlider, __spreadProps47(__spreadValues109({}, others), {
    ref,
    value,
    onChange,
    onChangeEnd,
    maxValue: 360,
    thumbColor: `hsl(${value}, 100%, 50%)`,
    round: true,
    overlays: [
      {
        backgroundImage: "linear-gradient(to right,hsl(0,100%,50%),hsl(60,100%,50%),hsl(120,100%,50%),hsl(170,100%,50%),hsl(240,100%,50%),hsl(300,100%,50%),hsl(360,100%,50%))"
      },
      {
        boxShadow: "rgba(0, 0, 0, .1) 0px 0px 0px 1px inset, rgb(0, 0, 0, .15) 0px 0px 4px inset"
      }
    ]
  }));
});
HueSlider.displayName = "@mantine/core/HueSlider";

// node_modules/@mantine/core/esm/ColorPicker/AlphaSlider/AlphaSlider.js
var import_react171 = __toESM(require_react());

// node_modules/@mantine/core/esm/ColorPicker/converters/parsers.js
var __defProp110 = Object.defineProperty;
var __defProps48 = Object.defineProperties;
var __getOwnPropDescs48 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols112 = Object.getOwnPropertySymbols;
var __hasOwnProp112 = Object.prototype.hasOwnProperty;
var __propIsEnum112 = Object.prototype.propertyIsEnumerable;
var __defNormalProp110 = (obj, key, value) => key in obj ? __defProp110(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues110 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp112.call(b2, prop))
      __defNormalProp110(a2, prop, b2[prop]);
  if (__getOwnPropSymbols112)
    for (var prop of __getOwnPropSymbols112(b2)) {
      if (__propIsEnum112.call(b2, prop))
        __defNormalProp110(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps48 = (a2, b2) => __defProps48(a2, __getOwnPropDescs48(b2));
function round2(number, digits = 0, base = 10 ** digits) {
  return Math.round(base * number) / base;
}
function hslaToHsva({ h, s, l, a: a2 }) {
  const ss = s * ((l < 50 ? l : 100 - l) / 100);
  return {
    h,
    s: ss > 0 ? 2 * ss / (l + ss) * 100 : 0,
    v: l + ss,
    a: a2
  };
}
var angleUnits = {
  grad: 360 / 400,
  turn: 360,
  rad: 360 / (Math.PI * 2)
};
function parseHue(value, unit = "deg") {
  return Number(value) * (angleUnits[unit] || 1);
}
var HSL_REGEXP = /hsla?\(?\s*(-?\d*\.?\d+)(deg|rad|grad|turn)?[,\s]+(-?\d*\.?\d+)%?[,\s]+(-?\d*\.?\d+)%?,?\s*[/\s]*(-?\d*\.?\d+)?(%)?\s*\)?/i;
function parseHsla(color) {
  const match2 = HSL_REGEXP.exec(color);
  if (!match2) {
    return { h: 0, s: 0, v: 0, a: 1 };
  }
  return hslaToHsva({
    h: parseHue(match2[1], match2[2]),
    s: Number(match2[3]),
    l: Number(match2[4]),
    a: match2[5] === void 0 ? 1 : Number(match2[5]) / (match2[6] ? 100 : 1)
  });
}
function rgbaToHsva({ r: r2, g, b: b2, a: a2 }) {
  const max3 = Math.max(r2, g, b2);
  const delta = max3 - Math.min(r2, g, b2);
  const hh = delta ? max3 === r2 ? (g - b2) / delta : max3 === g ? 2 + (b2 - r2) / delta : 4 + (r2 - g) / delta : 0;
  return {
    h: round2(60 * (hh < 0 ? hh + 6 : hh)),
    s: round2(max3 ? delta / max3 * 100 : 0),
    v: round2(max3 / 255 * 100),
    a: a2
  };
}
function parseHex(color) {
  const hex = color[0] === "#" ? color.slice(1) : color;
  if (hex.length === 3) {
    return rgbaToHsva({
      r: parseInt(hex[0] + hex[0], 16),
      g: parseInt(hex[1] + hex[1], 16),
      b: parseInt(hex[2] + hex[2], 16),
      a: 1
    });
  }
  return rgbaToHsva({
    r: parseInt(hex.slice(0, 2), 16),
    g: parseInt(hex.slice(2, 4), 16),
    b: parseInt(hex.slice(4, 6), 16),
    a: 1
  });
}
function parseHexa(color) {
  const hex = color[0] === "#" ? color.slice(1) : color;
  const roundA = (a22) => Math.round(parseInt(a22, 16) / 255 * 100) / 100;
  if (hex.length === 4) {
    const withoutOpacity2 = hex.slice(0, 3);
    const a22 = roundA(hex[3] + hex[3]);
    const hsvaColor2 = __spreadProps48(__spreadValues110({}, parseHex(withoutOpacity2)), { a: a22 });
    return hsvaColor2;
  }
  const withoutOpacity = hex.slice(0, 6);
  const a2 = roundA(hex.slice(6, 8));
  const hsvaColor = __spreadProps48(__spreadValues110({}, parseHex(withoutOpacity)), { a: a2 });
  return hsvaColor;
}
var RGB_REGEXP = /rgba?\(?\s*(-?\d*\.?\d+)(%)?[,\s]+(-?\d*\.?\d+)(%)?[,\s]+(-?\d*\.?\d+)(%)?,?\s*[/\s]*(-?\d*\.?\d+)?(%)?\s*\)?/i;
function parseRgba(color) {
  const match2 = RGB_REGEXP.exec(color);
  if (!match2) {
    return { h: 0, s: 0, v: 0, a: 1 };
  }
  return rgbaToHsva({
    r: Number(match2[1]) / (match2[2] ? 100 / 255 : 1),
    g: Number(match2[3]) / (match2[4] ? 100 / 255 : 1),
    b: Number(match2[5]) / (match2[6] ? 100 / 255 : 1),
    a: match2[7] === void 0 ? 1 : Number(match2[7]) / (match2[8] ? 100 : 1)
  });
}
var VALIDATION_REGEXP = {
  hex: /^#?([0-9A-F]{3}){1,2}$/i,
  hexa: /^#?([0-9A-F]{4}){1,2}$/i,
  rgb: /^rgb\((\d+),\s*(\d+),\s*(\d+)(?:,\s*(\d+(?:\.\d+)?))?\)$/i,
  rgba: /^rgba\((\d+),\s*(\d+),\s*(\d+)(?:,\s*(\d+(?:\.\d+)?))?\)$/i,
  hsl: /hsl\(\s*(\d+)\s*,\s*(\d+(?:\.\d+)?%)\s*,\s*(\d+(?:\.\d+)?%)\)/i,
  hsla: /^hsla\((\d+),\s*([\d.]+)%,\s*([\d.]+)%,\s*(\d*(?:\.\d+)?)\)$/i
};
var CONVERTERS = {
  hex: parseHex,
  hexa: parseHexa,
  rgb: parseRgba,
  rgba: parseRgba,
  hsl: parseHsla,
  hsla: parseHsla
};
function isColorValid(color) {
  for (const [, regexp] of Object.entries(VALIDATION_REGEXP)) {
    if (regexp.test(color)) {
      return true;
    }
  }
  return false;
}
function parseColor(color) {
  if (typeof color !== "string") {
    return { h: 0, s: 0, v: 0, a: 1 };
  }
  if (color === "transparent") {
    return { h: 0, s: 0, v: 0, a: 0 };
  }
  const trimmed = color.trim();
  for (const [rule, regexp] of Object.entries(VALIDATION_REGEXP)) {
    if (regexp.test(trimmed)) {
      return CONVERTERS[rule](trimmed);
    }
  }
  return { h: 0, s: 0, v: 0, a: 1 };
}

// node_modules/@mantine/core/esm/ColorPicker/AlphaSlider/AlphaSlider.js
var __defProp111 = Object.defineProperty;
var __defProps49 = Object.defineProperties;
var __getOwnPropDescs49 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols113 = Object.getOwnPropertySymbols;
var __hasOwnProp113 = Object.prototype.hasOwnProperty;
var __propIsEnum113 = Object.prototype.propertyIsEnumerable;
var __defNormalProp111 = (obj, key, value) => key in obj ? __defProp111(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues111 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp113.call(b2, prop))
      __defNormalProp111(a2, prop, b2[prop]);
  if (__getOwnPropSymbols113)
    for (var prop of __getOwnPropSymbols113(b2)) {
      if (__propIsEnum113.call(b2, prop))
        __defNormalProp111(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps49 = (a2, b2) => __defProps49(a2, __getOwnPropDescs49(b2));
var __objRest69 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp113.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols113)
    for (var prop of __getOwnPropSymbols113(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum113.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var AlphaSlider = (0, import_react171.forwardRef)((_a, ref) => {
  var _b = _a, { value, onChange, onChangeEnd, color } = _b, others = __objRest69(_b, ["value", "onChange", "onChangeEnd", "color"]);
  const theme = useMantineTheme();
  const _color = theme.colorScheme === "dark" ? theme.colors.dark[4] : theme.colors.gray[3];
  return import_react171.default.createElement(ColorSlider, __spreadProps49(__spreadValues111({}, others), {
    ref,
    value,
    onChange: (val) => onChange(round2(val, 2)),
    onChangeEnd: (val) => onChangeEnd(round2(val, 2)),
    maxValue: 1,
    round: false,
    overlays: [
      {
        backgroundImage: `linear-gradient(45deg, ${_color} 25%, transparent 25%), linear-gradient(-45deg, ${_color} 25%, transparent 25%), linear-gradient(45deg, transparent 75%, ${_color} 75%), linear-gradient(-45deg, ${theme.colorScheme === "dark" ? theme.colors.dark[7] : theme.white} 75%, ${_color} 75%)`,
        backgroundSize: "8px 8px",
        backgroundPosition: "0 0, 0 4px, 4px -4px, -4px 0px"
      },
      {
        backgroundImage: `linear-gradient(90deg, transparent, ${color})`
      },
      {
        boxShadow: "rgba(0, 0, 0, .1) 0px 0px 0px 1px inset, rgb(0, 0, 0, .15) 0px 0px 4px inset"
      }
    ]
  }));
});
AlphaSlider.displayName = "@mantine/core/AlphaSlider";

// node_modules/@mantine/core/esm/ColorPicker/Saturation/Saturation.js
var import_react172 = __toESM(require_react());

// node_modules/@mantine/core/esm/ColorPicker/Saturation/Saturation.styles.js
var __defProp112 = Object.defineProperty;
var __getOwnPropSymbols114 = Object.getOwnPropertySymbols;
var __hasOwnProp114 = Object.prototype.hasOwnProperty;
var __propIsEnum114 = Object.prototype.propertyIsEnumerable;
var __defNormalProp112 = (obj, key, value) => key in obj ? __defProp112(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues112 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp114.call(b2, prop))
      __defNormalProp112(a2, prop, b2[prop]);
  if (__getOwnPropSymbols114)
    for (var prop of __getOwnPropSymbols114(b2)) {
      if (__propIsEnum114.call(b2, prop))
        __defNormalProp112(a2, prop, b2[prop]);
    }
  return a2;
};
var SATURATION_HEIGHTS = {
  xs: 100,
  sm: 110,
  md: 120,
  lg: 140,
  xl: 160
};
var useStyles44 = createStyles((theme, { size: size3 }, getRef2) => ({
  saturationThumb: {
    ref: getRef2("saturationThumb")
  },
  saturation: {
    boxSizing: "border-box",
    position: "relative",
    height: theme.fn.size({ size: size3, sizes: SATURATION_HEIGHTS }),
    borderRadius: theme.radius.sm,
    margin: theme.fn.size({ size: size3, sizes: THUMB_SIZES }) / 2,
    WebkitTapHighlightColor: "transparent",
    [`&:focus .${getRef2("saturationThumb")}`]: __spreadValues112({}, theme.focusRing === "always" || theme.focusRing === "auto" ? theme.focusRingStyles.styles(theme) : theme.focusRingStyles.resetStyles(theme)),
    [`&:focus:not(:focus-visible) .${getRef2("saturationThumb")}`]: __spreadValues112({}, theme.focusRing === "auto" || theme.focusRing === "never" ? theme.focusRingStyles.resetStyles(theme) : null)
  },
  saturationOverlay: __spreadValues112({
    boxSizing: "border-box",
    borderRadius: theme.radius.sm
  }, theme.fn.cover(-theme.fn.size({ size: size3, sizes: THUMB_SIZES }) / 2 - 1))
}));
var Saturation_styles_default = useStyles44;

// node_modules/@mantine/core/esm/ColorPicker/converters/converters.js
function hsvaToRgbaObject({ h, s, v, a: a2 }) {
  const _h = h / 360 * 6;
  const _s = s / 100;
  const _v = v / 100;
  const hh = Math.floor(_h);
  const l = _v * (1 - _s);
  const c = _v * (1 - (_h - hh) * _s);
  const d2 = _v * (1 - (1 - _h + hh) * _s);
  const module = hh % 6;
  return {
    r: round2([_v, c, l, l, d2, _v][module] * 255),
    g: round2([d2, _v, _v, c, l, l][module] * 255),
    b: round2([l, l, d2, _v, _v, c][module] * 255),
    a: round2(a2, 2)
  };
}
function hsvaToRgba(color, includeAlpha) {
  const { r: r2, g, b: b2, a: a2 } = hsvaToRgbaObject(color);
  if (!includeAlpha) {
    return `rgb(${r2}, ${g}, ${b2})`;
  }
  return `rgba(${r2}, ${g}, ${b2}, ${round2(a2, 2)})`;
}
function hsvaToHsl({ h, s, v, a: a2 }, includeAlpha) {
  const hh = (200 - s) * v / 100;
  const result = {
    h: Math.round(h),
    s: Math.round(hh > 0 && hh < 200 ? s * v / 100 / (hh <= 100 ? hh : 200 - hh) * 100 : 0),
    l: Math.round(hh / 2)
  };
  if (!includeAlpha) {
    return `hsl(${result.h}, ${result.s}%, ${result.l}%)`;
  }
  return `hsla(${result.h}, ${result.s}%, ${result.l}%, ${round2(a2, 2)})`;
}
function formatHexPart(number) {
  const hex = number.toString(16);
  return hex.length < 2 ? `0${hex}` : hex;
}
function hsvaToHex(color) {
  const { r: r2, g, b: b2 } = hsvaToRgbaObject(color);
  return `#${formatHexPart(r2)}${formatHexPart(g)}${formatHexPart(b2)}`;
}
function hsvaToHexa(color) {
  const a2 = Math.round(color.a * 255);
  return `${hsvaToHex(color)}${formatHexPart(a2)}`;
}
var CONVERTERS2 = {
  hex: hsvaToHex,
  hexa: (color) => hsvaToHexa(color),
  rgb: (color) => hsvaToRgba(color, false),
  rgba: (color) => hsvaToRgba(color, true),
  hsl: (color) => hsvaToHsl(color, false),
  hsla: (color) => hsvaToHsl(color, true)
};
function convertHsvaTo(format, color) {
  if (!color) {
    return "#000000";
  }
  if (!(format in CONVERTERS2)) {
    return CONVERTERS2.hex(color);
  }
  return CONVERTERS2[format](color);
}

// node_modules/@mantine/core/esm/ColorPicker/Saturation/Saturation.js
function Saturation({
  value,
  onChange,
  onChangeEnd,
  focusable: focusable2 = true,
  __staticSelector = "saturation",
  size: size3,
  color,
  saturationLabel,
  classNames,
  styles: styles2,
  unstyled
}) {
  const { classes } = Saturation_styles_default({ size: size3 }, { classNames, styles: styles2, name: __staticSelector, unstyled });
  const [position2, setPosition] = (0, import_react172.useState)({ x: value.s / 100, y: 1 - value.v / 100 });
  const positionRef = (0, import_react172.useRef)(position2);
  const { ref } = useMove(({ x, y }) => {
    positionRef.current = { x, y };
    onChange({ s: Math.round(x * 100), v: Math.round((1 - y) * 100) });
  }, {
    onScrubEnd: () => {
      const { x, y } = positionRef.current;
      onChangeEnd({ s: Math.round(x * 100), v: Math.round((1 - y) * 100) });
    }
  });
  (0, import_react172.useEffect)(() => {
    setPosition({ x: value.s / 100, y: 1 - value.v / 100 });
  }, [value.s, value.v]);
  const handleArrow = (event, pos) => {
    event.preventDefault();
    const _position = clampUseMovePosition(pos);
    onChange({ s: Math.round(_position.x * 100), v: Math.round((1 - _position.y) * 100) });
    onChangeEnd({ s: Math.round(_position.x * 100), v: Math.round((1 - _position.y) * 100) });
  };
  const handleKeyDown = (event) => {
    switch (event.key) {
      case "ArrowUp": {
        handleArrow(event, { y: position2.y - 0.05, x: position2.x });
        break;
      }
      case "ArrowDown": {
        handleArrow(event, { y: position2.y + 0.05, x: position2.x });
        break;
      }
      case "ArrowRight": {
        handleArrow(event, { x: position2.x + 0.05, y: position2.y });
        break;
      }
      case "ArrowLeft": {
        handleArrow(event, { x: position2.x - 0.05, y: position2.y });
        break;
      }
    }
  };
  return import_react172.default.createElement("div", {
    className: classes.saturation,
    ref,
    role: "slider",
    "aria-label": saturationLabel,
    "aria-valuenow": position2.x,
    "aria-valuetext": convertHsvaTo("rgba", value),
    tabIndex: focusable2 ? 0 : -1,
    onKeyDown: handleKeyDown
  }, import_react172.default.createElement("div", {
    className: classes.saturationOverlay,
    style: { backgroundColor: `hsl(${value.h}, 100%, 50%)` }
  }), import_react172.default.createElement("div", {
    className: classes.saturationOverlay,
    style: { backgroundImage: "linear-gradient(90deg, #fff, transparent)" }
  }), import_react172.default.createElement("div", {
    className: classes.saturationOverlay,
    style: { backgroundImage: "linear-gradient(0deg, #000, transparent)" }
  }), import_react172.default.createElement(Thumb, {
    __staticSelector,
    classNames,
    styles: styles2,
    position: position2,
    className: classes.saturationThumb,
    style: { backgroundColor: color },
    size: size3
  }));
}
Saturation.displayName = "@mantine/core/Saturation";

// node_modules/@mantine/core/esm/ColorPicker/Swatches/Swatches.js
var import_react173 = __toESM(require_react());

// node_modules/@mantine/core/esm/ColorPicker/Swatches/Swatches.styles.js
var useStyles45 = createStyles((_theme, { swatchesPerRow }) => ({
  swatch: {
    width: `calc(${100 / swatchesPerRow}% - 4px)`,
    height: 0,
    paddingBottom: `calc(${100 / swatchesPerRow}% - 4px)`,
    margin: 2,
    boxSizing: "content-box"
  },
  swatches: {
    boxSizing: "border-box",
    marginLeft: -2,
    marginRight: -2,
    display: "flex",
    flexWrap: "wrap"
  }
}));
var Swatches_styles_default = useStyles45;

// node_modules/@mantine/core/esm/ColorPicker/Swatches/Swatches.js
var __defProp113 = Object.defineProperty;
var __getOwnPropSymbols115 = Object.getOwnPropertySymbols;
var __hasOwnProp115 = Object.prototype.hasOwnProperty;
var __propIsEnum115 = Object.prototype.propertyIsEnumerable;
var __defNormalProp113 = (obj, key, value) => key in obj ? __defProp113(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues113 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp115.call(b2, prop))
      __defNormalProp113(a2, prop, b2[prop]);
  if (__getOwnPropSymbols115)
    for (var prop of __getOwnPropSymbols115(b2)) {
      if (__propIsEnum115.call(b2, prop))
        __defNormalProp113(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest70 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp115.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols115)
    for (var prop of __getOwnPropSymbols115(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum115.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
function Swatches(_a) {
  var _b = _a, {
    data,
    swatchesPerRow = 10,
    focusable: focusable2 = true,
    classNames,
    styles: styles2,
    __staticSelector = "color-picker",
    unstyled,
    setValue,
    onChangeEnd
  } = _b, others = __objRest70(_b, [
    "data",
    "swatchesPerRow",
    "focusable",
    "classNames",
    "styles",
    "__staticSelector",
    "unstyled",
    "setValue",
    "onChangeEnd"
  ]);
  const { classes } = Swatches_styles_default({ swatchesPerRow }, { classNames, styles: styles2, name: __staticSelector, unstyled });
  const colors = data.map((color, index6) => import_react173.default.createElement(ColorSwatch, {
    className: classes.swatch,
    component: "button",
    type: "button",
    color,
    key: index6,
    radius: "sm",
    onClick: () => {
      setValue(color);
      onChangeEnd == null ? void 0 : onChangeEnd(color);
    },
    style: { cursor: "pointer" },
    "aria-label": color,
    tabIndex: focusable2 ? 0 : -1
  }));
  return import_react173.default.createElement("div", __spreadValues113({
    className: classes.swatches
  }, others), colors);
}
Swatches.displayName = "@mantine/core/Swatches";

// node_modules/@mantine/core/esm/ColorPicker/ColorPicker.styles.js
var sizes12 = {
  xs: 180,
  sm: 200,
  md: 240,
  lg: 280,
  xl: 320
};
var useStyles46 = createStyles((theme, { size: size3, fullWidth }) => ({
  preview: {},
  wrapper: {
    boxSizing: "border-box",
    width: fullWidth ? "100%" : theme.fn.size({ size: size3, sizes: sizes12 }),
    padding: 1
  },
  body: {
    display: "flex",
    boxSizing: "border-box",
    paddingTop: theme.fn.size({ size: size3, sizes: theme.spacing }) / 2
  },
  sliders: {
    flex: 1,
    boxSizing: "border-box",
    "&:not(:only-child)": {
      marginRight: theme.spacing.xs
    }
  },
  slider: {
    boxSizing: "border-box",
    "& + &": {
      marginTop: 5
    }
  },
  swatch: {
    cursor: "pointer"
  }
}));
var ColorPicker_styles_default = useStyles46;

// node_modules/@mantine/core/esm/ColorPicker/ColorPicker.js
var __defProp114 = Object.defineProperty;
var __defProps50 = Object.defineProperties;
var __getOwnPropDescs50 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols116 = Object.getOwnPropertySymbols;
var __hasOwnProp116 = Object.prototype.hasOwnProperty;
var __propIsEnum116 = Object.prototype.propertyIsEnumerable;
var __defNormalProp114 = (obj, key, value) => key in obj ? __defProp114(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues114 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp116.call(b2, prop))
      __defNormalProp114(a2, prop, b2[prop]);
  if (__getOwnPropSymbols116)
    for (var prop of __getOwnPropSymbols116(b2)) {
      if (__propIsEnum116.call(b2, prop))
        __defNormalProp114(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps50 = (a2, b2) => __defProps50(a2, __getOwnPropDescs50(b2));
var __objRest71 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp116.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols116)
    for (var prop of __getOwnPropSymbols116(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum116.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var SWATCH_SIZES = {
  xs: 26,
  sm: 34,
  md: 42,
  lg: 50,
  xl: 54
};
var defaultProps49 = {
  swatchesPerRow: 10,
  size: "sm",
  withPicker: true,
  focusable: true,
  __staticSelector: "ColorPicker"
};
var ColorPicker = (0, import_react174.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("ColorPicker", defaultProps49, props), {
    value,
    defaultValue,
    onChange,
    onChangeEnd,
    format,
    swatches,
    swatchesPerRow,
    size: size3,
    withPicker,
    fullWidth,
    focusable: focusable2,
    __staticSelector,
    saturationLabel,
    hueLabel,
    alphaLabel,
    className,
    styles: styles2,
    classNames,
    unstyled,
    onColorSwatchClick
  } = _a, others = __objRest71(_a, [
    "value",
    "defaultValue",
    "onChange",
    "onChangeEnd",
    "format",
    "swatches",
    "swatchesPerRow",
    "size",
    "withPicker",
    "fullWidth",
    "focusable",
    "__staticSelector",
    "saturationLabel",
    "hueLabel",
    "alphaLabel",
    "className",
    "styles",
    "classNames",
    "unstyled",
    "onColorSwatchClick"
  ]);
  const { classes, cx, theme } = ColorPicker_styles_default({ size: size3, fullWidth }, { classNames, styles: styles2, name: __staticSelector, unstyled });
  const formatRef = (0, import_react174.useRef)(format);
  const valueRef = (0, import_react174.useRef)(null);
  const updateRef3 = (0, import_react174.useRef)(true);
  const withAlpha = format === "hexa" || format === "rgba" || format === "hsla";
  const [_value, setValue] = useUncontrolled({
    value,
    defaultValue,
    finalValue: "#FFFFFF",
    onChange
  });
  const [parsed, setParsed] = (0, import_react174.useState)(parseColor(_value));
  const handleChange = (color) => {
    updateRef3.current = false;
    setParsed((current) => {
      const next2 = __spreadValues114(__spreadValues114({}, current), color);
      valueRef.current = convertHsvaTo(formatRef.current, next2);
      return next2;
    });
    setValue(valueRef.current);
    setTimeout(() => {
      updateRef3.current = true;
    }, 0);
  };
  useDidUpdate(() => {
    if (isColorValid(value) && updateRef3.current) {
      setParsed(parseColor(value));
      updateRef3.current = true;
    }
  }, [value]);
  useDidUpdate(() => {
    formatRef.current = format;
    setValue(convertHsvaTo(format, parsed));
  }, [format]);
  return import_react174.default.createElement(Box, __spreadValues114({
    className: cx(classes.wrapper, className),
    ref
  }, others), withPicker && import_react174.default.createElement(import_react174.default.Fragment, null, import_react174.default.createElement(Saturation, {
    value: parsed,
    onChange: handleChange,
    onChangeEnd: ({ s, v }) => onChangeEnd == null ? void 0 : onChangeEnd(convertHsvaTo(formatRef.current, __spreadProps50(__spreadValues114({}, parsed), { s, v }))),
    color: _value,
    styles: styles2,
    classNames,
    size: size3,
    focusable: focusable2,
    saturationLabel,
    __staticSelector
  }), import_react174.default.createElement("div", {
    className: classes.body
  }, import_react174.default.createElement("div", {
    className: classes.sliders
  }, import_react174.default.createElement(HueSlider, {
    value: parsed.h,
    onChange: (h) => handleChange({ h }),
    onChangeEnd: (h) => onChangeEnd == null ? void 0 : onChangeEnd(convertHsvaTo(formatRef.current, __spreadProps50(__spreadValues114({}, parsed), { h }))),
    size: size3,
    styles: styles2,
    classNames,
    focusable: focusable2,
    "aria-label": hueLabel,
    __staticSelector
  }), withAlpha && import_react174.default.createElement(AlphaSlider, {
    value: parsed.a,
    onChange: (a2) => handleChange({ a: a2 }),
    onChangeEnd: (a2) => {
      onChangeEnd == null ? void 0 : onChangeEnd(convertHsvaTo(formatRef.current, __spreadProps50(__spreadValues114({}, parsed), { a: a2 })));
    },
    size: size3,
    color: convertHsvaTo("hex", parsed),
    style: { marginTop: 6 },
    styles: styles2,
    classNames,
    focusable: focusable2,
    "aria-label": alphaLabel,
    __staticSelector
  })), withAlpha && import_react174.default.createElement(ColorSwatch, {
    color: _value,
    radius: "sm",
    size: theme.fn.size({ size: size3, sizes: SWATCH_SIZES }),
    className: classes.preview
  }))), Array.isArray(swatches) && import_react174.default.createElement(Swatches, {
    data: swatches,
    style: { marginTop: 5 },
    swatchesPerRow,
    focusable: focusable2,
    classNames,
    styles: styles2,
    __staticSelector,
    setValue,
    onChangeEnd: (color) => {
      const convertedColor = convertHsvaTo(format, parseColor(color));
      onColorSwatchClick == null ? void 0 : onColorSwatchClick(convertedColor);
      onChangeEnd == null ? void 0 : onChangeEnd(convertedColor);
    }
  }));
});
ColorPicker.displayName = "@mantine/core/ColorPicker";

// node_modules/@mantine/core/esm/ColorInput/EyeDropperIcon.js
var import_react175 = __toESM(require_react());
var __defProp115 = Object.defineProperty;
var __getOwnPropSymbols117 = Object.getOwnPropertySymbols;
var __hasOwnProp117 = Object.prototype.hasOwnProperty;
var __propIsEnum117 = Object.prototype.propertyIsEnumerable;
var __defNormalProp115 = (obj, key, value) => key in obj ? __defProp115(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues115 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp117.call(b2, prop))
      __defNormalProp115(a2, prop, b2[prop]);
  if (__getOwnPropSymbols117)
    for (var prop of __getOwnPropSymbols117(b2)) {
      if (__propIsEnum117.call(b2, prop))
        __defNormalProp115(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest72 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp117.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols117)
    for (var prop of __getOwnPropSymbols117(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum117.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
function EyeDropperIcon(_a) {
  var _b = _a, {
    size: size3
  } = _b, others = __objRest72(_b, [
    "size"
  ]);
  return import_react175.default.createElement("svg", __spreadValues115({
    xmlns: "http://www.w3.org/2000/svg",
    className: "icon icon-tabler icon-tabler-color-picker",
    width: size3,
    height: size3,
    viewBox: "0 0 24 24",
    strokeWidth: "1.5",
    stroke: "currentColor",
    fill: "none",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, others), import_react175.default.createElement("path", {
    stroke: "none",
    d: "M0 0h24v24H0z",
    fill: "none"
  }), import_react175.default.createElement("path", {
    d: "M11 7l6 6"
  }), import_react175.default.createElement("path", {
    d: "M4 16l11.7 -11.7a1 1 0 0 1 1.4 0l2.6 2.6a1 1 0 0 1 0 1.4l-11.7 11.7h-4v-4z"
  }));
}

// node_modules/@mantine/core/esm/ColorInput/ColorInput.js
var __defProp116 = Object.defineProperty;
var __defProps51 = Object.defineProperties;
var __getOwnPropDescs51 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols118 = Object.getOwnPropertySymbols;
var __hasOwnProp118 = Object.prototype.hasOwnProperty;
var __propIsEnum118 = Object.prototype.propertyIsEnumerable;
var __defNormalProp116 = (obj, key, value) => key in obj ? __defProp116(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues116 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp118.call(b2, prop))
      __defNormalProp116(a2, prop, b2[prop]);
  if (__getOwnPropSymbols118)
    for (var prop of __getOwnPropSymbols118(b2)) {
      if (__propIsEnum118.call(b2, prop))
        __defNormalProp116(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps51 = (a2, b2) => __defProps51(a2, __getOwnPropDescs51(b2));
var __objRest73 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp118.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols118)
    for (var prop of __getOwnPropSymbols118(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum118.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var SWATCH_SIZES2 = {
  xs: 16,
  sm: 18,
  md: 22,
  lg: 28,
  xl: 36
};
var EYE_DROPPER_SIZES = {
  xs: 14,
  sm: 16,
  md: 18,
  lg: 20,
  xl: 22
};
var RIGHT_SECTION_WIDTH = {
  xs: 28,
  sm: 32,
  md: 38,
  lg: 44,
  xl: 58
};
var defaultProps50 = {
  size: "sm",
  format: "hex",
  fixOnBlur: true,
  withPreview: true,
  swatchesPerRow: 10,
  withPicker: true,
  transition: "pop-top-left",
  dropdownZIndex: getDefaultZIndex("popover"),
  transitionDuration: 0,
  withinPortal: true,
  shadow: "md",
  withEyeDropper: true
};
var ColorInput = (0, import_react176.forwardRef)((props, ref) => {
  const _a = useInputProps("ColorInput", defaultProps50, props), {
    wrapperProps,
    inputProps,
    format,
    onChange,
    onChangeEnd,
    onFocus,
    onBlur,
    onClick,
    value,
    defaultValue,
    disallowInput,
    fixOnBlur,
    withPreview,
    swatchesPerRow,
    withPicker,
    icon,
    transition,
    dropdownZIndex,
    transitionDuration,
    transitionTimingFunction,
    withinPortal,
    swatches,
    shadow,
    classNames,
    styles: styles2,
    unstyled,
    readOnly,
    withEyeDropper,
    eyeDropperIcon,
    rightSection,
    rightSectionWidth,
    closeOnColorSwatchClick,
    disabled
  } = _a, others = __objRest73(_a, [
    "wrapperProps",
    "inputProps",
    "format",
    "onChange",
    "onChangeEnd",
    "onFocus",
    "onBlur",
    "onClick",
    "value",
    "defaultValue",
    "disallowInput",
    "fixOnBlur",
    "withPreview",
    "swatchesPerRow",
    "withPicker",
    "icon",
    "transition",
    "dropdownZIndex",
    "transitionDuration",
    "transitionTimingFunction",
    "withinPortal",
    "swatches",
    "shadow",
    "classNames",
    "styles",
    "unstyled",
    "readOnly",
    "withEyeDropper",
    "eyeDropperIcon",
    "rightSection",
    "rightSectionWidth",
    "closeOnColorSwatchClick",
    "disabled"
  ]);
  const theme = useMantineTheme();
  const [dropdownOpened, setDropdownOpened] = (0, import_react176.useState)(false);
  const [lastValidValue, setLastValidValue] = (0, import_react176.useState)("");
  const [_value, setValue] = useUncontrolled({
    value,
    defaultValue,
    finalValue: "",
    onChange
  });
  const { supported: eyeDropperSupported, open: openEyeDropper } = useEyeDropper();
  const eyeDropper = import_react176.default.createElement(ActionIcon, {
    sx: { color: theme.colorScheme === "dark" ? theme.colors.dark[0] : theme.black },
    size: inputProps.size,
    onClick: () => openEyeDropper().then(({ sRGBHex }) => setValue(convertHsvaTo(format, parseColor(sRGBHex)))).catch(noop)
  }, eyeDropperIcon || import_react176.default.createElement(EyeDropperIcon, {
    size: theme.fn.size({ size: inputProps.size, sizes: EYE_DROPPER_SIZES })
  }));
  const handleInputFocus = (event) => {
    onFocus == null ? void 0 : onFocus(event);
    setDropdownOpened(true);
  };
  const handleInputBlur = (event) => {
    onBlur == null ? void 0 : onBlur(event);
    setDropdownOpened(false);
    fixOnBlur && setValue(lastValidValue);
  };
  const handleInputClick = (event) => {
    onClick == null ? void 0 : onClick(event);
    setDropdownOpened(true);
  };
  (0, import_react176.useEffect)(() => {
    if (isColorValid(_value) || _value.trim() === "") {
      setLastValidValue(_value);
    }
  }, [_value]);
  useDidUpdate(() => {
    if (isColorValid(_value)) {
      setValue(convertHsvaTo(format, parseColor(_value)));
    }
  }, [format]);
  return import_react176.default.createElement(Input.Wrapper, __spreadProps51(__spreadValues116({}, wrapperProps), {
    __staticSelector: "ColorInput"
  }), import_react176.default.createElement(Popover, {
    __staticSelector: "ColorInput",
    position: "bottom-start",
    offset: 5,
    zIndex: dropdownZIndex,
    withinPortal,
    transitionDuration,
    transition,
    opened: dropdownOpened,
    shadow,
    classNames,
    styles: styles2,
    unstyled,
    disabled: readOnly || withPicker === false && (!Array.isArray(swatches) || swatches.length === 0)
  }, import_react176.default.createElement(Popover.Target, null, import_react176.default.createElement("div", null, import_react176.default.createElement(Input, __spreadProps51(__spreadValues116(__spreadValues116({
    autoComplete: "nope"
  }, others), inputProps), {
    disabled,
    ref,
    __staticSelector: "ColorInput",
    onFocus: handleInputFocus,
    onBlur: handleInputBlur,
    onClick: handleInputClick,
    spellCheck: false,
    value: _value,
    onChange: (event) => {
      const inputValue = event.currentTarget.value;
      setValue(inputValue);
      if (isColorValid(inputValue)) {
        onChangeEnd == null ? void 0 : onChangeEnd(convertHsvaTo(format, parseColor(inputValue)));
      }
    },
    icon: icon || (withPreview ? import_react176.default.createElement(ColorSwatch, {
      color: isColorValid(_value) ? _value : "#fff",
      size: theme.fn.size({ size: inputProps.size, sizes: SWATCH_SIZES2 })
    }) : null),
    readOnly: disallowInput || readOnly,
    sx: { cursor: disallowInput ? "pointer" : void 0 },
    unstyled,
    classNames,
    styles: styles2,
    rightSection: rightSection || (withEyeDropper && !disabled && !readOnly && eyeDropperSupported ? eyeDropper : null),
    rightSectionWidth: rightSectionWidth != null ? rightSectionWidth : theme.fn.size({ size: inputProps.size, sizes: RIGHT_SECTION_WIDTH })
  })))), import_react176.default.createElement(Popover.Dropdown, {
    onMouseDown: (event) => event.preventDefault(),
    p: inputProps.size
  }, import_react176.default.createElement(ColorPicker, {
    __staticSelector: "ColorInput",
    value: _value,
    onChange: setValue,
    onChangeEnd,
    format,
    swatches,
    swatchesPerRow,
    withPicker,
    size: inputProps.size,
    focusable: false,
    unstyled,
    styles: styles2,
    classNames,
    onColorSwatchClick: () => closeOnColorSwatchClick && setDropdownOpened(false)
  }))));
});
ColorInput.displayName = "@mantine/core/ColorInput";

// node_modules/@mantine/core/esm/Container/Container.js
var import_react177 = __toESM(require_react());

// node_modules/@mantine/core/esm/Container/Container.styles.js
var useStyles47 = createStyles((theme, { fluid, size: size3, sizes: sizes24 }) => ({
  root: {
    paddingLeft: theme.spacing.md,
    paddingRight: theme.spacing.md,
    maxWidth: fluid ? "100%" : theme.fn.size({ size: size3, sizes: sizes24 }),
    marginLeft: "auto",
    marginRight: "auto"
  }
}));
var Container_styles_default = useStyles47;

// node_modules/@mantine/core/esm/Container/Container.js
var __defProp117 = Object.defineProperty;
var __getOwnPropSymbols119 = Object.getOwnPropertySymbols;
var __hasOwnProp119 = Object.prototype.hasOwnProperty;
var __propIsEnum119 = Object.prototype.propertyIsEnumerable;
var __defNormalProp117 = (obj, key, value) => key in obj ? __defProp117(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues117 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp119.call(b2, prop))
      __defNormalProp117(a2, prop, b2[prop]);
  if (__getOwnPropSymbols119)
    for (var prop of __getOwnPropSymbols119(b2)) {
      if (__propIsEnum119.call(b2, prop))
        __defNormalProp117(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest74 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp119.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols119)
    for (var prop of __getOwnPropSymbols119(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum119.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps51 = {
  sizes: {
    xs: 540,
    sm: 720,
    md: 960,
    lg: 1140,
    xl: 1320
  }
};
var Container = (0, import_react177.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("Container", defaultProps51, props), { className, fluid, size: size3, unstyled, sizes: sizes24 } = _a, others = __objRest74(_a, ["className", "fluid", "size", "unstyled", "sizes"]);
  const { classes, cx } = Container_styles_default({ fluid, size: size3, sizes: sizes24 }, { unstyled, name: "Container" });
  return import_react177.default.createElement(Box, __spreadValues117({
    className: cx(classes.root, className),
    ref
  }, others));
});
Container.displayName = "@mantine/core/Container";

// node_modules/@mantine/core/esm/CopyButton/CopyButton.js
var import_react178 = __toESM(require_react());
var __defProp118 = Object.defineProperty;
var __getOwnPropSymbols120 = Object.getOwnPropertySymbols;
var __hasOwnProp120 = Object.prototype.hasOwnProperty;
var __propIsEnum120 = Object.prototype.propertyIsEnumerable;
var __defNormalProp118 = (obj, key, value) => key in obj ? __defProp118(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues118 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp120.call(b2, prop))
      __defNormalProp118(a2, prop, b2[prop]);
  if (__getOwnPropSymbols120)
    for (var prop of __getOwnPropSymbols120(b2)) {
      if (__propIsEnum120.call(b2, prop))
        __defNormalProp118(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest75 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp120.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols120)
    for (var prop of __getOwnPropSymbols120(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum120.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps52 = {
  timeout: 1e3
};
function CopyButton(props) {
  const _a = useComponentDefaultProps("CopyButton", defaultProps52, props), { children, timeout, value } = _a, others = __objRest75(_a, ["children", "timeout", "value"]);
  const clipboard = useClipboard({ timeout });
  const copy2 = () => clipboard.copy(value);
  return import_react178.default.createElement(import_react178.default.Fragment, null, children(__spreadValues118({ copy: copy2, copied: clipboard.copied }, others)));
}
CopyButton.displayName = "@mantine/core/CopyButton";

// node_modules/@mantine/core/esm/Dialog/Dialog.js
var import_react179 = __toESM(require_react());

// node_modules/@mantine/core/esm/Dialog/Dialog.styles.js
var __defProp119 = Object.defineProperty;
var __defProps52 = Object.defineProperties;
var __getOwnPropDescs52 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols121 = Object.getOwnPropertySymbols;
var __hasOwnProp121 = Object.prototype.hasOwnProperty;
var __propIsEnum121 = Object.prototype.propertyIsEnumerable;
var __defNormalProp119 = (obj, key, value) => key in obj ? __defProp119(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues119 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp121.call(b2, prop))
      __defNormalProp119(a2, prop, b2[prop]);
  if (__getOwnPropSymbols121)
    for (var prop of __getOwnPropSymbols121(b2)) {
      if (__propIsEnum121.call(b2, prop))
        __defNormalProp119(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps52 = (a2, b2) => __defProps52(a2, __getOwnPropDescs52(b2));
var sizes13 = {
  xs: 160,
  sm: 200,
  md: 340,
  lg: 400,
  xl: 500
};
var useStyles48 = createStyles((theme, { size: size3 }) => ({
  root: __spreadProps52(__spreadValues119({}, theme.fn.fontStyles()), {
    color: theme.colorScheme === "dark" ? theme.colors.dark[0] : theme.black,
    position: "relative",
    width: theme.fn.size({ size: size3, sizes: sizes13 }),
    maxWidth: "100%",
    minHeight: 50
  }),
  closeButton: {
    position: "absolute",
    top: `calc(${theme.spacing.md}px / 2)`,
    right: `calc(${theme.spacing.md}px / 2)`
  }
}));
var Dialog_styles_default = useStyles48;

// node_modules/@mantine/core/esm/Dialog/Dialog.js
var __defProp120 = Object.defineProperty;
var __getOwnPropSymbols122 = Object.getOwnPropertySymbols;
var __hasOwnProp122 = Object.prototype.hasOwnProperty;
var __propIsEnum122 = Object.prototype.propertyIsEnumerable;
var __defNormalProp120 = (obj, key, value) => key in obj ? __defProp120(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues120 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp122.call(b2, prop))
      __defNormalProp120(a2, prop, b2[prop]);
  if (__getOwnPropSymbols122)
    for (var prop of __getOwnPropSymbols122(b2)) {
      if (__propIsEnum122.call(b2, prop))
        __defNormalProp120(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest76 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp122.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols122)
    for (var prop of __getOwnPropSymbols122(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum122.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps53 = {
  shadow: "md",
  p: "md",
  withBorder: true,
  size: "md",
  transition: "pop-top-right",
  transitionDuration: 200
};
function DialogBody(props) {
  const _a = useComponentDefaultProps("Dialog", defaultProps53, props), {
    withCloseButton,
    onClose,
    position: position2,
    shadow,
    children,
    className,
    style,
    classNames,
    styles: styles2,
    opened,
    withBorder,
    size: size3,
    transition,
    transitionDuration,
    transitionTimingFunction,
    unstyled
  } = _a, others = __objRest76(_a, [
    "withCloseButton",
    "onClose",
    "position",
    "shadow",
    "children",
    "className",
    "style",
    "classNames",
    "styles",
    "opened",
    "withBorder",
    "size",
    "transition",
    "transitionDuration",
    "transitionTimingFunction",
    "unstyled"
  ]);
  const { classes, cx } = Dialog_styles_default({ size: size3 }, { classNames, styles: styles2, unstyled, name: "Dialog" });
  return import_react179.default.createElement(Transition, {
    mounted: opened,
    transition,
    duration: transitionDuration,
    timingFunction: transitionTimingFunction
  }, (transitionStyles) => import_react179.default.createElement(Paper, __spreadValues120({
    className: cx(classes.root, className),
    style: __spreadValues120(__spreadValues120({}, style), transitionStyles),
    shadow,
    withBorder,
    unstyled
  }, others), withCloseButton && import_react179.default.createElement(CloseButton, {
    onClick: onClose,
    className: classes.closeButton
  }), children));
}
var Dialog = (0, import_react179.forwardRef)((_a, ref) => {
  var _b = _a, { zIndex = getDefaultZIndex("modal") } = _b, props = __objRest76(_b, ["zIndex"]);
  const theme = useMantineTheme();
  return import_react179.default.createElement(Affix, {
    zIndex,
    position: props.position || { bottom: theme.spacing.xl, right: theme.spacing.xl },
    ref
  }, import_react179.default.createElement(DialogBody, __spreadValues120({}, props)));
});
Dialog.displayName = "@mantine/core/Dialog";

// node_modules/@mantine/core/esm/Drawer/Drawer.js
var import_react182 = __toESM(require_react());

// node_modules/@mantine/core/esm/Drawer/Drawer.styles.js
var __defProp121 = Object.defineProperty;
var __defProps53 = Object.defineProperties;
var __getOwnPropDescs53 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols123 = Object.getOwnPropertySymbols;
var __hasOwnProp123 = Object.prototype.hasOwnProperty;
var __propIsEnum123 = Object.prototype.propertyIsEnumerable;
var __defNormalProp121 = (obj, key, value) => key in obj ? __defProp121(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues121 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp123.call(b2, prop))
      __defNormalProp121(a2, prop, b2[prop]);
  if (__getOwnPropSymbols123)
    for (var prop of __getOwnPropSymbols123(b2)) {
      if (__propIsEnum123.call(b2, prop))
        __defNormalProp121(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps53 = (a2, b2) => __defProps53(a2, __getOwnPropDescs53(b2));
var sizes14 = {
  xs: 180,
  sm: 240,
  md: 320,
  lg: 360,
  xl: 500,
  full: "100%"
};
function getPositionStyles2({
  position: position2,
  size: size3,
  theme
}) {
  switch (position2) {
    case "top":
      return { top: 0, left: 0, right: 0, height: theme.fn.size({ size: size3, sizes: sizes14 }) };
    case "bottom":
      return { bottom: 0, left: 0, right: 0, height: theme.fn.size({ size: size3, sizes: sizes14 }) };
    case "right":
      return { bottom: 0, top: 0, right: 0, width: theme.fn.size({ size: size3, sizes: sizes14 }) };
    case "left":
      return { bottom: 0, top: 0, left: 0, width: theme.fn.size({ size: size3, sizes: sizes14 }) };
    default:
      return null;
  }
}
var useStyles49 = createStyles((theme, { position: position2, size: size3, zIndex, withOverlay }) => ({
  closeButton: {},
  overlay: {},
  body: {},
  root: {
    position: "fixed",
    zIndex,
    top: 0,
    left: 0,
    right: 0,
    bottom: 0,
    pointerEvents: withOverlay === false ? "none" : void 0
  },
  drawer: __spreadProps53(__spreadValues121({}, getPositionStyles2({ position: position2, size: size3, theme })), {
    maxWidth: "100%",
    maxHeight: "100vh",
    position: "fixed",
    outline: 0,
    zIndex: 1,
    pointerEvents: withOverlay === false ? "auto" : void 0
  }),
  title: {
    marginRight: theme.spacing.md,
    textOverflow: "ellipsis",
    display: "block",
    wordBreak: "break-word"
  },
  header: {
    display: "flex",
    alignItems: "center",
    justifyContent: "space-between",
    marginBottom: theme.spacing.md
  }
}));
var Drawer_styles_default = useStyles49;

// node_modules/@mantine/core/esm/Transition/GroupedTransition.js
var import_react180 = __toESM(require_react());
function GroupedTransition({
  transitions: transitions3,
  duration = 250,
  exitDuration = duration,
  mounted,
  children,
  timingFunction,
  onExit,
  onEntered,
  onEnter,
  onExited
}) {
  const { transitionDuration, transitionStatus, transitionTimingFunction } = useTransition({
    mounted,
    duration,
    exitDuration,
    timingFunction,
    onExit,
    onEntered,
    onEnter,
    onExited
  });
  if (transitionDuration === 0) {
    return mounted ? import_react180.default.createElement(import_react180.default.Fragment, null, children({})) : null;
  }
  if (transitionStatus === "exited") {
    return null;
  }
  const transitionsStyles = Object.keys(transitions3).reduce((acc, transition) => {
    acc[transition] = getTransitionStyles({
      duration: transitions3[transition].duration,
      transition: transitions3[transition].transition,
      timingFunction: transitions3[transition].timingFunction || transitionTimingFunction,
      state: transitionStatus
    });
    return acc;
  }, {});
  return import_react180.default.createElement(import_react180.default.Fragment, null, children(transitionsStyles));
}
GroupedTransition.displayName = "@mantine/core/GroupedTransition";

// node_modules/@mantine/core/esm/Overlay/Overlay.js
var import_react181 = __toESM(require_react());

// node_modules/@mantine/core/esm/Overlay/Overlay.styles.js
var useStyles50 = createStyles((theme, { zIndex }) => ({
  root: {
    position: "absolute",
    top: 0,
    bottom: 0,
    left: 0,
    right: 0,
    zIndex
  }
}));
var Overlay_styles_default = useStyles50;

// node_modules/@mantine/core/esm/Overlay/Overlay.js
var __defProp122 = Object.defineProperty;
var __defProps54 = Object.defineProperties;
var __getOwnPropDescs54 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols124 = Object.getOwnPropertySymbols;
var __hasOwnProp124 = Object.prototype.hasOwnProperty;
var __propIsEnum124 = Object.prototype.propertyIsEnumerable;
var __defNormalProp122 = (obj, key, value) => key in obj ? __defProp122(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues122 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp124.call(b2, prop))
      __defNormalProp122(a2, prop, b2[prop]);
  if (__getOwnPropSymbols124)
    for (var prop of __getOwnPropSymbols124(b2)) {
      if (__propIsEnum124.call(b2, prop))
        __defNormalProp122(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps54 = (a2, b2) => __defProps54(a2, __getOwnPropDescs54(b2));
var __objRest77 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp124.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols124)
    for (var prop of __getOwnPropSymbols124(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum124.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps54 = {
  opacity: 0.6,
  color: "#fff",
  zIndex: getDefaultZIndex("modal"),
  radius: 0,
  blur: 0
};
var _Overlay = (0, import_react181.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("Overlay", defaultProps54, props), { opacity, blur, color, gradient: gradient2, zIndex, radius: radius2, sx, unstyled, className } = _a, others = __objRest77(_a, ["opacity", "blur", "color", "gradient", "zIndex", "radius", "sx", "unstyled", "className"]);
  const { classes, cx } = Overlay_styles_default({ zIndex }, { name: "Overlay", unstyled });
  const background = gradient2 ? { backgroundImage: gradient2 } : { backgroundColor: color };
  const innerOverlay = (otherProps) => import_react181.default.createElement(Box, __spreadValues122({
    ref,
    className: cx(classes.root, className),
    sx: [
      (theme) => __spreadProps54(__spreadValues122({}, background), {
        opacity,
        borderRadius: theme.fn.size({ size: radius2, sizes: theme.radius })
      }),
      ...packSx(sx)
    ]
  }, otherProps));
  if (blur) {
    return import_react181.default.createElement(Box, __spreadValues122({
      className: cx(classes.root, className),
      sx: [{ backdropFilter: `blur(${blur}px)` }, ...packSx(sx)]
    }, others), innerOverlay());
  }
  return innerOverlay(others);
});
_Overlay.displayName = "@mantine/core/Overlay";
var Overlay = createPolymorphicComponent(_Overlay);

// node_modules/@mantine/core/esm/Drawer/Drawer.js
var __defProp123 = Object.defineProperty;
var __getOwnPropSymbols125 = Object.getOwnPropertySymbols;
var __hasOwnProp125 = Object.prototype.hasOwnProperty;
var __propIsEnum125 = Object.prototype.propertyIsEnumerable;
var __defNormalProp123 = (obj, key, value) => key in obj ? __defProp123(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues123 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp125.call(b2, prop))
      __defNormalProp123(a2, prop, b2[prop]);
  if (__getOwnPropSymbols125)
    for (var prop of __getOwnPropSymbols125(b2)) {
      if (__propIsEnum125.call(b2, prop))
        __defNormalProp123(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest78 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp125.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols125)
    for (var prop of __getOwnPropSymbols125(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum125.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var transitions2 = {
  top: "slide-down",
  bottom: "slide-up",
  left: "slide-right",
  right: "slide-left"
};
var rtlTransitions = {
  top: "slide-down",
  bottom: "slide-up",
  right: "slide-right",
  left: "slide-left"
};
var defaultProps55 = {
  position: "left",
  size: "md",
  transitionDuration: 250,
  transitionTimingFunction: "ease",
  zIndex: getDefaultZIndex("modal"),
  shadow: "md",
  padding: 0,
  lockScroll: true,
  closeOnClickOutside: true,
  closeOnEscape: true,
  trapFocus: true,
  withOverlay: true,
  withCloseButton: true,
  withinPortal: true,
  withFocusReturn: true,
  overlayBlur: 0
};
function Drawer(props) {
  const _a = useComponentDefaultProps("Drawer", defaultProps55, props), {
    className,
    opened,
    onClose,
    position: position2,
    size: size3,
    trapFocus,
    lockScroll,
    closeOnClickOutside,
    closeOnEscape: closeOnEscape2,
    transition,
    transitionDuration,
    transitionTimingFunction,
    zIndex,
    overlayColor,
    overlayOpacity,
    children,
    withOverlay,
    shadow,
    id,
    padding: padding2,
    title,
    withCloseButton,
    closeButtonLabel,
    classNames,
    styles: styles2,
    target,
    withinPortal,
    overlayBlur,
    unstyled,
    withFocusReturn
  } = _a, others = __objRest78(_a, [
    "className",
    "opened",
    "onClose",
    "position",
    "size",
    "trapFocus",
    "lockScroll",
    "closeOnClickOutside",
    "closeOnEscape",
    "transition",
    "transitionDuration",
    "transitionTimingFunction",
    "zIndex",
    "overlayColor",
    "overlayOpacity",
    "children",
    "withOverlay",
    "shadow",
    "id",
    "padding",
    "title",
    "withCloseButton",
    "closeButtonLabel",
    "classNames",
    "styles",
    "target",
    "withinPortal",
    "overlayBlur",
    "unstyled",
    "withFocusReturn"
  ]);
  const baseId = useId(id);
  const titleId = `${baseId}-title`;
  const bodyId = `${baseId}-body`;
  const { classes, cx, theme } = Drawer_styles_default({ size: size3, position: position2, zIndex, withOverlay }, { classNames, styles: styles2, unstyled, name: "Drawer" });
  const focusTrapRef = useFocusTrap(trapFocus && opened);
  const [, _lockScroll] = useScrollLock();
  const drawerTransition = transition || (theme.dir === "rtl" ? rtlTransitions : transitions2)[position2];
  const _overlayOpacity = typeof overlayOpacity === "number" ? overlayOpacity : theme.colorScheme === "dark" ? 0.85 : 0.75;
  const _closeOnEscape = (event) => {
    if (event.key === "Escape" && closeOnEscape2) {
      onClose();
    }
  };
  (0, import_react182.useEffect)(() => {
    if (!trapFocus) {
      window.addEventListener("keydown", _closeOnEscape);
      return () => window.removeEventListener("keydown", _closeOnEscape);
    }
    return void 0;
  }, [trapFocus]);
  useFocusReturn({ opened, shouldReturnFocus: trapFocus && withFocusReturn });
  return import_react182.default.createElement(OptionalPortal, {
    withinPortal,
    target
  }, import_react182.default.createElement(GroupedTransition, {
    onExited: () => _lockScroll(false),
    onEntered: () => _lockScroll(lockScroll && true),
    mounted: opened,
    duration: transitionDuration,
    timingFunction: transitionTimingFunction,
    transitions: {
      overlay: { duration: transitionDuration / 2, transition: "fade", timingFunction: "ease" },
      drawer: {
        duration: transitionDuration,
        transition: drawerTransition,
        timingFunction: transitionTimingFunction
      }
    }
  }, (transitionStyles) => import_react182.default.createElement(Box, __spreadValues123({
    className: cx(classes.root, className),
    role: "dialog",
    "aria-modal": true,
    "aria-labelledby": titleId,
    "aria-describedby": bodyId
  }, others), import_react182.default.createElement(Paper, {
    className: cx(classes.drawer, className),
    ref: focusTrapRef,
    style: transitionStyles.drawer,
    radius: 0,
    tabIndex: -1,
    onKeyDownCapture: (event) => {
      var _a2;
      const shouldTrigger = ((_a2 = event.target) == null ? void 0 : _a2.getAttribute("data-mantine-stop-propagation")) !== "true";
      shouldTrigger && event.key === "Escape" && closeOnEscape2 && onClose();
    },
    shadow,
    p: padding2,
    unstyled
  }, (title || withCloseButton) && import_react182.default.createElement("div", {
    className: classes.header
  }, import_react182.default.createElement(Text, {
    id: titleId,
    className: classes.title,
    unstyled
  }, title), withCloseButton && import_react182.default.createElement(CloseButton, {
    iconSize: 16,
    onClick: onClose,
    "aria-label": closeButtonLabel,
    className: classes.closeButton,
    unstyled
  })), import_react182.default.createElement("div", {
    id: bodyId,
    className: classes.body
  }, children)), withOverlay && import_react182.default.createElement("div", {
    style: transitionStyles.overlay
  }, import_react182.default.createElement(Overlay, {
    unstyled,
    blur: overlayBlur,
    onMouseDown: () => closeOnClickOutside && onClose(),
    className: classes.overlay,
    opacity: _overlayOpacity,
    zIndex: 0,
    color: overlayColor || (theme.colorScheme === "dark" ? theme.colors.dark[9] : theme.black)
  })))));
}
Drawer.displayName = "@mantine/core/Drawer";

// node_modules/@mantine/core/esm/FileButton/FileButton.js
var import_react183 = __toESM(require_react());
var __defProp124 = Object.defineProperty;
var __getOwnPropSymbols126 = Object.getOwnPropertySymbols;
var __hasOwnProp126 = Object.prototype.hasOwnProperty;
var __propIsEnum126 = Object.prototype.propertyIsEnumerable;
var __defNormalProp124 = (obj, key, value) => key in obj ? __defProp124(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues124 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp126.call(b2, prop))
      __defNormalProp124(a2, prop, b2[prop]);
  if (__getOwnPropSymbols126)
    for (var prop of __getOwnPropSymbols126(b2)) {
      if (__propIsEnum126.call(b2, prop))
        __defNormalProp124(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest79 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp126.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols126)
    for (var prop of __getOwnPropSymbols126(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum126.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps56 = {
  multiple: false
};
var FileButton = (0, import_react183.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("FileButton", defaultProps56, props), {
    onChange,
    children,
    multiple,
    accept,
    name,
    form,
    resetRef,
    disabled,
    capture,
    inputProps
  } = _a, others = __objRest79(_a, [
    "onChange",
    "children",
    "multiple",
    "accept",
    "name",
    "form",
    "resetRef",
    "disabled",
    "capture",
    "inputProps"
  ]);
  const inputRef = (0, import_react183.useRef)();
  const onClick = () => {
    !disabled && inputRef.current.click();
  };
  const handleChange = (event) => {
    if (multiple) {
      onChange(Array.from(event.currentTarget.files));
    } else {
      onChange(event.currentTarget.files[0] || null);
    }
  };
  const reset = () => {
    inputRef.current.value = "";
  };
  assignRef(resetRef, reset);
  return import_react183.default.createElement(import_react183.default.Fragment, null, children(__spreadValues124({ onClick }, others)), import_react183.default.createElement("input", __spreadValues124({
    style: { display: "none" },
    type: "file",
    accept,
    multiple,
    onChange: handleChange,
    ref: useMergedRef(ref, inputRef),
    name,
    form,
    capture
  }, inputProps)));
});
FileButton.displayName = "@mantine/core/FileButton";

// node_modules/@mantine/core/esm/FileInput/FileInput.js
var import_react184 = __toESM(require_react());

// node_modules/@mantine/core/esm/FileInput/FileInput.styles.js
var useStyles51 = createStyles(() => ({
  placeholder: {},
  input: {
    cursor: "pointer"
  }
}));
var FileInput_styles_default = useStyles51;

// node_modules/@mantine/core/esm/FileInput/FileInput.js
var __defProp125 = Object.defineProperty;
var __defProps55 = Object.defineProperties;
var __getOwnPropDescs55 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols127 = Object.getOwnPropertySymbols;
var __hasOwnProp127 = Object.prototype.hasOwnProperty;
var __propIsEnum127 = Object.prototype.propertyIsEnumerable;
var __defNormalProp125 = (obj, key, value) => key in obj ? __defProp125(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues125 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp127.call(b2, prop))
      __defNormalProp125(a2, prop, b2[prop]);
  if (__getOwnPropSymbols127)
    for (var prop of __getOwnPropSymbols127(b2)) {
      if (__propIsEnum127.call(b2, prop))
        __defNormalProp125(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps55 = (a2, b2) => __defProps55(a2, __getOwnPropDescs55(b2));
var __objRest80 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp127.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols127)
    for (var prop of __getOwnPropSymbols127(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum127.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var DefaultValue = ({ value }) => import_react184.default.createElement(Text, {
  sx: { overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }
}, Array.isArray(value) ? value.map((file) => file.name).join(", ") : value == null ? void 0 : value.name);
var defaultProps57 = {
  size: "sm",
  valueComponent: DefaultValue,
  clearButtonTabIndex: 0
};
var RIGHT_SECTION_WIDTH2 = {
  xs: 24,
  sm: 30,
  md: 34,
  lg: 40,
  xl: 44
};
var _FileInput = (0, import_react184.forwardRef)((props, ref) => {
  const _a = useInputProps("FileInput", defaultProps57, props), {
    inputProps,
    wrapperProps,
    placeholder,
    value,
    defaultValue,
    onChange,
    multiple,
    accept,
    name,
    form,
    classNames,
    styles: styles2,
    unstyled,
    valueComponent: ValueComponent,
    rightSection,
    rightSectionWidth,
    clearable,
    clearButtonLabel,
    clearButtonTabIndex,
    readOnly,
    capture,
    fileInputProps
  } = _a, others = __objRest80(_a, [
    "inputProps",
    "wrapperProps",
    "placeholder",
    "value",
    "defaultValue",
    "onChange",
    "multiple",
    "accept",
    "name",
    "form",
    "classNames",
    "styles",
    "unstyled",
    "valueComponent",
    "rightSection",
    "rightSectionWidth",
    "clearable",
    "clearButtonLabel",
    "clearButtonTabIndex",
    "readOnly",
    "capture",
    "fileInputProps"
  ]);
  const resetRef = (0, import_react184.useRef)();
  const { classes, theme, cx } = FileInput_styles_default(null, {
    name: "FileInput",
    classNames,
    styles: styles2,
    unstyled
  });
  const [_value, setValue] = useUncontrolled({
    value,
    defaultValue,
    onChange,
    finalValue: multiple ? [] : null
  });
  const hasValue = Array.isArray(_value) ? _value.length !== 0 : _value !== null;
  const _rightSection = rightSection || (clearable && hasValue && !readOnly ? import_react184.default.createElement(CloseButton, {
    variant: "transparent",
    "aria-label": clearButtonLabel,
    onClick: () => setValue(multiple ? [] : null),
    size: inputProps.size,
    tabIndex: clearButtonTabIndex,
    unstyled
  }) : null);
  (0, import_react184.useEffect)(() => {
    if (Array.isArray(_value) && _value.length === 0 || _value === null) {
      resetRef.current();
    }
  }, [_value]);
  return import_react184.default.createElement(Input.Wrapper, __spreadProps55(__spreadValues125({}, wrapperProps), {
    __staticSelector: "FileInput"
  }), import_react184.default.createElement(FileButton, {
    onChange: setValue,
    multiple,
    accept,
    name,
    form,
    resetRef,
    disabled: readOnly,
    capture,
    inputProps: fileInputProps
  }, (fileButtonProps) => import_react184.default.createElement(Input, __spreadProps55(__spreadValues125(__spreadValues125(__spreadValues125({
    multiline: true
  }, fileButtonProps), inputProps), others), {
    component: "button",
    type: "button",
    ref,
    __staticSelector: "FileInput",
    rightSection: _rightSection,
    rightSectionWidth: rightSectionWidth || theme.fn.size({ size: inputProps.size, sizes: RIGHT_SECTION_WIDTH2 }),
    classNames: __spreadProps55(__spreadValues125({}, classNames), { input: cx(classes.input, classNames == null ? void 0 : classNames.input) })
  }), !hasValue ? import_react184.default.createElement(Input.Placeholder, {
    className: classes.placeholder
  }, placeholder) : import_react184.default.createElement(ValueComponent, {
    value: _value
  }))));
});
_FileInput.displayName = "@mantine/core/FileInput";
var FileInput = _FileInput;

// node_modules/@mantine/core/esm/Flex/Flex.js
var import_react185 = __toESM(require_react());

// node_modules/@mantine/core/esm/Flex/flex-props.js
var FLEX_SYSTEM_PROPS = {
  gap: { type: "spacing", property: "gap" },
  rowGap: { type: "spacing", property: "rowGap" },
  columnGap: { type: "spacing", property: "columnGap" },
  align: { type: "default", property: "alignItems" },
  justify: { type: "default", property: "justifyContent" },
  wrap: { type: "default", property: "flexWrap" },
  direction: { type: "default", property: "flexDirection" }
};

// node_modules/@mantine/core/esm/Flex/Flex.js
var __defProp126 = Object.defineProperty;
var __defProps56 = Object.defineProperties;
var __getOwnPropDescs56 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols128 = Object.getOwnPropertySymbols;
var __hasOwnProp128 = Object.prototype.hasOwnProperty;
var __propIsEnum128 = Object.prototype.propertyIsEnumerable;
var __defNormalProp126 = (obj, key, value) => key in obj ? __defProp126(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues126 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp128.call(b2, prop))
      __defNormalProp126(a2, prop, b2[prop]);
  if (__getOwnPropSymbols128)
    for (var prop of __getOwnPropSymbols128(b2)) {
      if (__propIsEnum128.call(b2, prop))
        __defNormalProp126(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps56 = (a2, b2) => __defProps56(a2, __getOwnPropDescs56(b2));
var __objRest81 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp128.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols128)
    for (var prop of __getOwnPropSymbols128(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum128.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps58 = {};
var Flex = (0, import_react185.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("Flex", defaultProps58, props), { gap, rowGap, columnGap, align, justify, wrap, direction, sx } = _a, others = __objRest81(_a, ["gap", "rowGap", "columnGap", "align", "justify", "wrap", "direction", "sx"]);
  return import_react185.default.createElement(Box, __spreadProps56(__spreadValues126({}, others), {
    sx: [
      { display: "flex" },
      (theme) => getSystemStyles({ gap, rowGap, columnGap, align, justify, wrap, direction }, theme, FLEX_SYSTEM_PROPS),
      ...packSx(sx)
    ],
    ref
  }));
});
Flex.displayName = "@mantine/core/Flex";

// node_modules/@mantine/core/esm/Grid/Grid.js
var import_react187 = __toESM(require_react());

// node_modules/@mantine/core/esm/Grid/Col/Col.js
var import_react186 = __toESM(require_react());

// node_modules/@mantine/core/esm/Grid/Grid.context.js
var [GridProvider, useGridContext] = createSafeContext("Grid component was not found in tree");

// node_modules/@mantine/core/esm/Grid/Col/Col.styles.js
var __defProp127 = Object.defineProperty;
var __getOwnPropSymbols129 = Object.getOwnPropertySymbols;
var __hasOwnProp129 = Object.prototype.hasOwnProperty;
var __propIsEnum129 = Object.prototype.propertyIsEnumerable;
var __defNormalProp127 = (obj, key, value) => key in obj ? __defProp127(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues127 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp129.call(b2, prop))
      __defNormalProp127(a2, prop, b2[prop]);
  if (__getOwnPropSymbols129)
    for (var prop of __getOwnPropSymbols129(b2)) {
      if (__propIsEnum129.call(b2, prop))
        __defNormalProp127(a2, prop, b2[prop]);
    }
  return a2;
};
var getColumnFlexBasis = (colSpan, columns) => {
  if (colSpan === "content") {
    return "auto";
  }
  if (colSpan === "auto") {
    return "0px";
  }
  return colSpan ? `${100 / (columns / colSpan)}%` : void 0;
};
var getColumnMaxWidth = (colSpan, columns, grow) => {
  if (grow || colSpan === "auto" || colSpan === "content") {
    return "unset";
  }
  return getColumnFlexBasis(colSpan, columns);
};
var getColumnFlexGrow = (colSpan, grow) => {
  if (!colSpan) {
    return void 0;
  }
  return colSpan === "auto" || grow ? 1 : 0;
};
var getColumnOffset = (offset3, columns) => offset3 === 0 ? 0 : offset3 ? `${100 / (columns / offset3)}%` : void 0;
var getGutterSize = (gutter, theme) => typeof gutter !== "undefined" ? theme.fn.size({ size: gutter, sizes: theme.spacing }) / 2 : void 0;
function getBreakpointsStyles({
  sizes: sizes24,
  offsets,
  orders,
  theme,
  columns,
  gutters,
  grow
}) {
  return MANTINE_SIZES.reduce((acc, size3) => {
    acc[`@media (min-width: ${theme.breakpoints[size3]}px)`] = {
      order: orders[size3],
      flexBasis: getColumnFlexBasis(sizes24[size3], columns),
      padding: getGutterSize(gutters[size3], theme),
      flexShrink: 0,
      width: sizes24[size3] === "content" ? "auto" : void 0,
      maxWidth: getColumnMaxWidth(sizes24[size3], columns, grow),
      marginLeft: getColumnOffset(offsets[size3], columns),
      flexGrow: getColumnFlexGrow(sizes24[size3], grow)
    };
    return acc;
  }, {});
}
var useStyles52 = createStyles((theme, {
  gutter,
  gutterXs,
  gutterSm,
  gutterMd,
  gutterLg,
  gutterXl,
  grow,
  offset: offset3,
  offsetXs,
  offsetSm,
  offsetMd,
  offsetLg,
  offsetXl,
  columns,
  span,
  xs,
  sm,
  md,
  lg,
  xl,
  order: order2,
  orderXs,
  orderSm,
  orderMd,
  orderLg,
  orderXl
}) => ({
  col: __spreadValues127({
    boxSizing: "border-box",
    flexGrow: getColumnFlexGrow(span, grow),
    order: order2,
    padding: getGutterSize(gutter, theme),
    marginLeft: getColumnOffset(offset3, columns),
    flexBasis: getColumnFlexBasis(span, columns),
    flexShrink: 0,
    width: span === "content" ? "auto" : void 0,
    maxWidth: getColumnMaxWidth(span, columns, grow)
  }, getBreakpointsStyles({
    sizes: { xs, sm, md, lg, xl },
    offsets: { xs: offsetXs, sm: offsetSm, md: offsetMd, lg: offsetLg, xl: offsetXl },
    orders: { xs: orderXs, sm: orderSm, md: orderMd, lg: orderLg, xl: orderXl },
    gutters: { xs: gutterXs, sm: gutterSm, md: gutterMd, lg: gutterLg, xl: gutterXl },
    theme,
    columns,
    grow
  }))
}));
var Col_styles_default = useStyles52;

// node_modules/@mantine/core/esm/Grid/Col/Col.js
var __defProp128 = Object.defineProperty;
var __getOwnPropSymbols130 = Object.getOwnPropertySymbols;
var __hasOwnProp130 = Object.prototype.hasOwnProperty;
var __propIsEnum130 = Object.prototype.propertyIsEnumerable;
var __defNormalProp128 = (obj, key, value) => key in obj ? __defProp128(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues128 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp130.call(b2, prop))
      __defNormalProp128(a2, prop, b2[prop]);
  if (__getOwnPropSymbols130)
    for (var prop of __getOwnPropSymbols130(b2)) {
      if (__propIsEnum130.call(b2, prop))
        __defNormalProp128(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest82 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp130.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols130)
    for (var prop of __getOwnPropSymbols130(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum130.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps59 = {};
function isValidSpan(span) {
  if (span === "auto" || span === "content") {
    return true;
  }
  return typeof span === "number" && span > 0 && span % 1 === 0;
}
var Col = (0, import_react186.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("GridCol", defaultProps59, props), {
    children,
    span,
    offset: offset3,
    offsetXs,
    offsetSm,
    offsetMd,
    offsetLg,
    offsetXl,
    xs,
    sm,
    md,
    lg,
    xl,
    order: order2,
    orderXs,
    orderSm,
    orderMd,
    orderLg,
    orderXl,
    className,
    id,
    unstyled
  } = _a, others = __objRest82(_a, [
    "children",
    "span",
    "offset",
    "offsetXs",
    "offsetSm",
    "offsetMd",
    "offsetLg",
    "offsetXl",
    "xs",
    "sm",
    "md",
    "lg",
    "xl",
    "order",
    "orderXs",
    "orderSm",
    "orderMd",
    "orderLg",
    "orderXl",
    "className",
    "id",
    "unstyled"
  ]);
  const ctx = useGridContext();
  const colSpan = span || ctx.columns;
  const { classes, cx } = Col_styles_default({
    gutter: ctx.gutter,
    gutterXs: ctx.gutterXs,
    gutterSm: ctx.gutterSm,
    gutterMd: ctx.gutterMd,
    gutterLg: ctx.gutterLg,
    gutterXl: ctx.gutterXl,
    offset: offset3,
    offsetXs,
    offsetSm,
    offsetMd,
    offsetLg,
    offsetXl,
    xs,
    sm,
    md,
    lg,
    xl,
    order: order2,
    orderXs,
    orderSm,
    orderMd,
    orderLg,
    orderXl,
    grow: ctx.grow,
    columns: ctx.columns,
    span: colSpan
  }, { unstyled, name: "Grid" });
  if (!isValidSpan(colSpan) || colSpan > ctx.columns) {
    return null;
  }
  return import_react186.default.createElement(Box, __spreadValues128({
    className: cx(classes.col, className),
    ref
  }, others), children);
});
Col.displayName = "@mantine/core/Col";

// node_modules/@mantine/core/esm/Grid/Grid.styles.js
var __defProp129 = Object.defineProperty;
var __getOwnPropSymbols131 = Object.getOwnPropertySymbols;
var __hasOwnProp131 = Object.prototype.hasOwnProperty;
var __propIsEnum131 = Object.prototype.propertyIsEnumerable;
var __defNormalProp129 = (obj, key, value) => key in obj ? __defProp129(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues129 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp131.call(b2, prop))
      __defNormalProp129(a2, prop, b2[prop]);
  if (__getOwnPropSymbols131)
    for (var prop of __getOwnPropSymbols131(b2)) {
      if (__propIsEnum131.call(b2, prop))
        __defNormalProp129(a2, prop, b2[prop]);
    }
  return a2;
};
function getGutterStyles(gutters, theme) {
  return MANTINE_SIZES.reduce((acc, size3) => {
    if (typeof gutters[size3] !== "undefined") {
      acc[`@media (min-width: ${theme.breakpoints[size3]}px)`] = {
        margin: -theme.fn.size({ size: gutters[size3], sizes: theme.spacing }) / 2
      };
    }
    return acc;
  }, {});
}
var useStyles53 = createStyles((theme, { justify, align, gutter, gutterXs, gutterSm, gutterMd, gutterLg, gutterXl }) => ({
  root: __spreadValues129({
    margin: -theme.fn.size({ size: gutter, sizes: theme.spacing }) / 2,
    display: "flex",
    flexWrap: "wrap",
    justifyContent: justify,
    alignItems: align
  }, getGutterStyles({ xs: gutterXs, sm: gutterSm, md: gutterMd, lg: gutterLg, xl: gutterXl }, theme))
}));
var Grid_styles_default = useStyles53;

// node_modules/@mantine/core/esm/Grid/Grid.js
var __defProp130 = Object.defineProperty;
var __getOwnPropSymbols132 = Object.getOwnPropertySymbols;
var __hasOwnProp132 = Object.prototype.hasOwnProperty;
var __propIsEnum132 = Object.prototype.propertyIsEnumerable;
var __defNormalProp130 = (obj, key, value) => key in obj ? __defProp130(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues130 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp132.call(b2, prop))
      __defNormalProp130(a2, prop, b2[prop]);
  if (__getOwnPropSymbols132)
    for (var prop of __getOwnPropSymbols132(b2)) {
      if (__propIsEnum132.call(b2, prop))
        __defNormalProp130(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest83 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp132.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols132)
    for (var prop of __getOwnPropSymbols132(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum132.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps60 = {
  gutter: "md",
  justify: "flex-start",
  align: "stretch",
  columns: 12
};
var Grid = (0, import_react187.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("Grid", defaultProps60, props), {
    gutter,
    gutterXs,
    gutterSm,
    gutterMd,
    gutterLg,
    gutterXl,
    children,
    grow,
    justify,
    align,
    columns,
    className,
    id,
    unstyled
  } = _a, others = __objRest83(_a, [
    "gutter",
    "gutterXs",
    "gutterSm",
    "gutterMd",
    "gutterLg",
    "gutterXl",
    "children",
    "grow",
    "justify",
    "align",
    "columns",
    "className",
    "id",
    "unstyled"
  ]);
  const { classes, cx } = Grid_styles_default({ gutter, justify, align, gutterXs, gutterSm, gutterMd, gutterLg, gutterXl }, { unstyled, name: "Grid" });
  return import_react187.default.createElement(GridProvider, {
    value: { gutter, gutterXs, gutterSm, gutterMd, gutterLg, gutterXl, grow, columns }
  }, import_react187.default.createElement(Box, __spreadValues130({
    className: cx(classes.root, className),
    ref
  }, others), children));
});
Grid.Col = Col;
Grid.displayName = "@mantine/core/Grid";

// node_modules/@mantine/core/esm/Highlight/Highlight.js
var import_react189 = __toESM(require_react());

// node_modules/@mantine/core/esm/Mark/Mark.js
var import_react188 = __toESM(require_react());

// node_modules/@mantine/core/esm/Mark/Mark.styles.js
var useStyles54 = createStyles((theme, { color }) => ({
  root: {
    backgroundColor: theme.fn.themeColor(color, theme.colorScheme === "dark" ? 5 : 2),
    color: theme.colorScheme === "dark" ? theme.colors.dark[9] : "inherit"
  }
}));
var Mark_styles_default = useStyles54;

// node_modules/@mantine/core/esm/Mark/Mark.js
var __defProp131 = Object.defineProperty;
var __getOwnPropSymbols133 = Object.getOwnPropertySymbols;
var __hasOwnProp133 = Object.prototype.hasOwnProperty;
var __propIsEnum133 = Object.prototype.propertyIsEnumerable;
var __defNormalProp131 = (obj, key, value) => key in obj ? __defProp131(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues131 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp133.call(b2, prop))
      __defNormalProp131(a2, prop, b2[prop]);
  if (__getOwnPropSymbols133)
    for (var prop of __getOwnPropSymbols133(b2)) {
      if (__propIsEnum133.call(b2, prop))
        __defNormalProp131(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest84 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp133.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols133)
    for (var prop of __getOwnPropSymbols133(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum133.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps61 = {
  color: "yellow"
};
var Mark = (0, import_react188.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("Mark", defaultProps61, props), { color, className, unstyled } = _a, others = __objRest84(_a, ["color", "className", "unstyled"]);
  const { classes, cx } = Mark_styles_default({ color }, { unstyled, name: "Mark" });
  return import_react188.default.createElement(Box, __spreadValues131({
    component: "mark",
    ref,
    className: cx(classes.root, className)
  }, others));
});
Mark.displayName = "@mantine/core/Mark";

// node_modules/@mantine/core/esm/Highlight/highlighter/highlighter.js
function escapeRegex(value) {
  return value.replace(/[-[\]{}()*+?.,\\^$|#]/g, "\\$&");
}
function highlighter(value, _highlight) {
  if (_highlight == null) {
    return [{ chunk: value, highlighted: false }];
  }
  const highlight = Array.isArray(_highlight) ? _highlight.map(escapeRegex) : escapeRegex(_highlight);
  const shouldHighlight = Array.isArray(highlight) ? highlight.filter((part) => part.trim().length > 0).length > 0 : highlight.trim() !== "";
  if (!shouldHighlight) {
    return [{ chunk: value, highlighted: false }];
  }
  const matcher = typeof highlight === "string" ? highlight.trim() : highlight.filter((part) => part.trim().length !== 0).map((part) => part.trim()).join("|");
  const re = new RegExp(`(${matcher})`, "gi");
  const chunks = value.split(re).map((part) => ({ chunk: part, highlighted: re.test(part) })).filter(({ chunk }) => chunk);
  return chunks;
}

// node_modules/@mantine/core/esm/Highlight/Highlight.js
var __defProp132 = Object.defineProperty;
var __getOwnPropSymbols134 = Object.getOwnPropertySymbols;
var __hasOwnProp134 = Object.prototype.hasOwnProperty;
var __propIsEnum134 = Object.prototype.propertyIsEnumerable;
var __defNormalProp132 = (obj, key, value) => key in obj ? __defProp132(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues132 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp134.call(b2, prop))
      __defNormalProp132(a2, prop, b2[prop]);
  if (__getOwnPropSymbols134)
    for (var prop of __getOwnPropSymbols134(b2)) {
      if (__propIsEnum134.call(b2, prop))
        __defNormalProp132(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest85 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp134.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols134)
    for (var prop of __getOwnPropSymbols134(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum134.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps62 = {
  highlightColor: "yellow"
};
var _Highlight = (0, import_react189.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("Highlight", defaultProps62, props), { children, highlight, highlightColor, highlightStyles, unstyled } = _a, others = __objRest85(_a, ["children", "highlight", "highlightColor", "highlightStyles", "unstyled"]);
  const highlightChunks = highlighter(children, highlight);
  return import_react189.default.createElement(Text, __spreadValues132({
    unstyled,
    ref
  }, others), highlightChunks.map(({ chunk, highlighted }, i) => highlighted ? import_react189.default.createElement(Mark, {
    unstyled,
    key: i,
    color: highlightColor,
    sx: highlightStyles
  }, chunk) : import_react189.default.createElement("span", {
    key: i
  }, chunk)));
});
_Highlight.displayName = "@mantine/core/Highlight";
var Highlight = createPolymorphicComponent(_Highlight);

// node_modules/@mantine/core/esm/HoverCard/HoverCard.js
var import_react193 = __toESM(require_react());

// node_modules/@mantine/core/esm/HoverCard/HoverCard.errors.js
var HOVER_CARD_ERRORS = {
  context: "HoverCard component was not found in the tree",
  children: "HoverCard.Target component children should be an element or a component that accepts ref, fragments, strings, numbers and other primitive values are not supported"
};

// node_modules/@mantine/core/esm/HoverCard/HoverCard.context.js
var [HoverCardContextProvider, useHoverCardContext] = createSafeContext(HOVER_CARD_ERRORS.context);

// node_modules/@mantine/core/esm/HoverCard/HoverCardDropdown/HoverCardDropdown.js
var import_react190 = __toESM(require_react());
var __defProp133 = Object.defineProperty;
var __getOwnPropSymbols135 = Object.getOwnPropertySymbols;
var __hasOwnProp135 = Object.prototype.hasOwnProperty;
var __propIsEnum135 = Object.prototype.propertyIsEnumerable;
var __defNormalProp133 = (obj, key, value) => key in obj ? __defProp133(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues133 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp135.call(b2, prop))
      __defNormalProp133(a2, prop, b2[prop]);
  if (__getOwnPropSymbols135)
    for (var prop of __getOwnPropSymbols135(b2)) {
      if (__propIsEnum135.call(b2, prop))
        __defNormalProp133(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest86 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp135.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols135)
    for (var prop of __getOwnPropSymbols135(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum135.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps63 = {};
function HoverCardDropdown(props) {
  const _a = useComponentDefaultProps("HoverCardDropdown", defaultProps63, props), { children, onMouseEnter, onMouseLeave } = _a, others = __objRest86(_a, ["children", "onMouseEnter", "onMouseLeave"]);
  const ctx = useHoverCardContext();
  const handleMouseEnter = createEventHandler(onMouseEnter, ctx.openDropdown);
  const handleMouseLeave = createEventHandler(onMouseLeave, ctx.closeDropdown);
  return import_react190.default.createElement(Popover.Dropdown, __spreadValues133({
    onMouseEnter: handleMouseEnter,
    onMouseLeave: handleMouseLeave
  }, others), children);
}
HoverCardDropdown.displayName = "@mantine/core/HoverCardDropdown";

// node_modules/@mantine/core/esm/HoverCard/HoverCardTarget/HoverCardTarget.js
var import_react191 = __toESM(require_react());
var __defProp134 = Object.defineProperty;
var __getOwnPropSymbols136 = Object.getOwnPropertySymbols;
var __hasOwnProp136 = Object.prototype.hasOwnProperty;
var __propIsEnum136 = Object.prototype.propertyIsEnumerable;
var __defNormalProp134 = (obj, key, value) => key in obj ? __defProp134(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues134 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp136.call(b2, prop))
      __defNormalProp134(a2, prop, b2[prop]);
  if (__getOwnPropSymbols136)
    for (var prop of __getOwnPropSymbols136(b2)) {
      if (__propIsEnum136.call(b2, prop))
        __defNormalProp134(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest87 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp136.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols136)
    for (var prop of __getOwnPropSymbols136(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum136.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps64 = {
  refProp: "ref"
};
var HoverCardTarget = (0, import_react191.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("HoverCardTarget", defaultProps64, props), { children, refProp } = _a, others = __objRest87(_a, ["children", "refProp"]);
  if (!isElement(children)) {
    throw new Error(HOVER_CARD_ERRORS.children);
  }
  const ctx = useHoverCardContext();
  const onMouseEnter = createEventHandler(children.props.onMouseEnter, ctx.openDropdown);
  const onMouseLeave = createEventHandler(children.props.onMouseLeave, ctx.closeDropdown);
  return import_react191.default.createElement(Popover.Target, __spreadValues134({
    refProp,
    ref
  }, others), (0, import_react191.cloneElement)(children, { onMouseEnter, onMouseLeave }));
});
HoverCardTarget.displayName = "@mantine/core/HoverCardTarget";

// node_modules/@mantine/core/esm/Floating/use-delayed-hover.js
var import_react192 = __toESM(require_react());
function useDelayedHover({ open, close, openDelay, closeDelay }) {
  const openTimeout = (0, import_react192.useRef)(-1);
  const closeTimeout = (0, import_react192.useRef)(-1);
  const clearTimeouts = () => {
    window.clearTimeout(openTimeout.current);
    window.clearTimeout(closeTimeout.current);
  };
  const openDropdown = () => {
    clearTimeouts();
    if (openDelay === 0) {
      open();
    } else {
      openTimeout.current = window.setTimeout(open, openDelay);
    }
  };
  const closeDropdown = () => {
    clearTimeouts();
    if (closeDelay === 0) {
      close();
    } else {
      closeTimeout.current = window.setTimeout(close, closeDelay);
    }
  };
  (0, import_react192.useEffect)(() => clearTimeouts, []);
  return { openDropdown, closeDropdown };
}

// node_modules/@mantine/core/esm/HoverCard/HoverCard.js
var __defProp135 = Object.defineProperty;
var __getOwnPropSymbols137 = Object.getOwnPropertySymbols;
var __hasOwnProp137 = Object.prototype.hasOwnProperty;
var __propIsEnum137 = Object.prototype.propertyIsEnumerable;
var __defNormalProp135 = (obj, key, value) => key in obj ? __defProp135(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues135 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp137.call(b2, prop))
      __defNormalProp135(a2, prop, b2[prop]);
  if (__getOwnPropSymbols137)
    for (var prop of __getOwnPropSymbols137(b2)) {
      if (__propIsEnum137.call(b2, prop))
        __defNormalProp135(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest88 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp137.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols137)
    for (var prop of __getOwnPropSymbols137(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum137.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps65 = {
  openDelay: 0,
  closeDelay: 150,
  initiallyOpened: false
};
function HoverCard(props) {
  const _a = useComponentDefaultProps("HoverCard", defaultProps65, props), { children, onOpen, onClose, openDelay, closeDelay, initiallyOpened } = _a, others = __objRest88(_a, ["children", "onOpen", "onClose", "openDelay", "closeDelay", "initiallyOpened"]);
  const [opened, { open, close }] = useDisclosure(initiallyOpened, { onClose, onOpen });
  const { openDropdown, closeDropdown } = useDelayedHover({ open, close, openDelay, closeDelay });
  return import_react193.default.createElement(HoverCardContextProvider, {
    value: { openDropdown, closeDropdown }
  }, import_react193.default.createElement(Popover, __spreadValues135({
    opened,
    __staticSelector: "HoverCard"
  }, others), children));
}
HoverCard.displayName = "@mantine/core/HoverCard";
HoverCard.Target = HoverCardTarget;
HoverCard.Dropdown = HoverCardDropdown;

// node_modules/@mantine/core/esm/Image/Image.js
var import_react195 = __toESM(require_react());

// node_modules/@mantine/core/esm/Image/ImageIcon.js
var import_react194 = __toESM(require_react());
var __defProp136 = Object.defineProperty;
var __getOwnPropSymbols138 = Object.getOwnPropertySymbols;
var __hasOwnProp138 = Object.prototype.hasOwnProperty;
var __propIsEnum138 = Object.prototype.propertyIsEnumerable;
var __defNormalProp136 = (obj, key, value) => key in obj ? __defProp136(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues136 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp138.call(b2, prop))
      __defNormalProp136(a2, prop, b2[prop]);
  if (__getOwnPropSymbols138)
    for (var prop of __getOwnPropSymbols138(b2)) {
      if (__propIsEnum138.call(b2, prop))
        __defNormalProp136(a2, prop, b2[prop]);
    }
  return a2;
};
function ImageIcon(props) {
  return import_react194.default.createElement("svg", __spreadValues136({
    width: "15",
    height: "15",
    viewBox: "0 0 15 15",
    fill: "none",
    xmlns: "http://www.w3.org/2000/svg"
  }, props), import_react194.default.createElement("path", {
    d: "M2.5 1H12.5C13.3284 1 14 1.67157 14 2.5V12.5C14 13.3284 13.3284 14 12.5 14H2.5C1.67157 14 1 13.3284 1 12.5V2.5C1 1.67157 1.67157 1 2.5 1ZM2.5 2C2.22386 2 2 2.22386 2 2.5V8.3636L3.6818 6.6818C3.76809 6.59551 3.88572 6.54797 4.00774 6.55007C4.12975 6.55216 4.24568 6.60372 4.32895 6.69293L7.87355 10.4901L10.6818 7.6818C10.8575 7.50607 11.1425 7.50607 11.3182 7.6818L13 9.3636V2.5C13 2.22386 12.7761 2 12.5 2H2.5ZM2 12.5V9.6364L3.98887 7.64753L7.5311 11.4421L8.94113 13H2.5C2.22386 13 2 12.7761 2 12.5ZM12.5 13H10.155L8.48336 11.153L11 8.6364L13 10.6364V12.5C13 12.7761 12.7761 13 12.5 13ZM6.64922 5.5C6.64922 5.03013 7.03013 4.64922 7.5 4.64922C7.96987 4.64922 8.35078 5.03013 8.35078 5.5C8.35078 5.96987 7.96987 6.35078 7.5 6.35078C7.03013 6.35078 6.64922 5.96987 6.64922 5.5ZM7.5 3.74922C6.53307 3.74922 5.74922 4.53307 5.74922 5.5C5.74922 6.46693 6.53307 7.25078 7.5 7.25078C8.46693 7.25078 9.25078 6.46693 9.25078 5.5C9.25078 4.53307 8.46693 3.74922 7.5 3.74922Z",
    fill: "currentColor",
    fillRule: "evenodd",
    clipRule: "evenodd"
  }));
}

// node_modules/@mantine/core/esm/Image/Image.styles.js
var __defProp137 = Object.defineProperty;
var __defProps57 = Object.defineProperties;
var __getOwnPropDescs57 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols139 = Object.getOwnPropertySymbols;
var __hasOwnProp139 = Object.prototype.hasOwnProperty;
var __propIsEnum139 = Object.prototype.propertyIsEnumerable;
var __defNormalProp137 = (obj, key, value) => key in obj ? __defProp137(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues137 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp139.call(b2, prop))
      __defNormalProp137(a2, prop, b2[prop]);
  if (__getOwnPropSymbols139)
    for (var prop of __getOwnPropSymbols139(b2)) {
      if (__propIsEnum139.call(b2, prop))
        __defNormalProp137(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps57 = (a2, b2) => __defProps57(a2, __getOwnPropDescs57(b2));
var useStyles55 = createStyles((theme, { radius: radius2 }) => ({
  root: {},
  imageWrapper: {
    position: "relative"
  },
  figure: {
    margin: 0
  },
  image: __spreadProps57(__spreadValues137({}, theme.fn.fontStyles()), {
    display: "block",
    width: "100%",
    height: "100%",
    border: 0,
    borderRadius: theme.fn.size({ size: radius2, sizes: theme.radius })
  }),
  caption: {
    color: theme.colorScheme === "dark" ? theme.colors.dark[2] : theme.colors.gray[7],
    marginTop: theme.spacing.xs
  },
  placeholder: __spreadProps57(__spreadValues137({}, theme.fn.cover()), {
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    color: theme.colorScheme === "dark" ? theme.colors.dark[2] : theme.colors.gray[6],
    backgroundColor: theme.colorScheme === "dark" ? theme.colors.dark[8] : theme.colors.gray[0],
    borderRadius: theme.fn.size({ size: radius2, sizes: theme.radius })
  })
}));
var Image_styles_default = useStyles55;

// node_modules/@mantine/core/esm/Image/Image.js
var __defProp138 = Object.defineProperty;
var __getOwnPropSymbols140 = Object.getOwnPropertySymbols;
var __hasOwnProp140 = Object.prototype.hasOwnProperty;
var __propIsEnum140 = Object.prototype.propertyIsEnumerable;
var __defNormalProp138 = (obj, key, value) => key in obj ? __defProp138(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues138 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp140.call(b2, prop))
      __defNormalProp138(a2, prop, b2[prop]);
  if (__getOwnPropSymbols140)
    for (var prop of __getOwnPropSymbols140(b2)) {
      if (__propIsEnum140.call(b2, prop))
        __defNormalProp138(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest89 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp140.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols140)
    for (var prop of __getOwnPropSymbols140(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum140.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps66 = {
  fit: "cover",
  width: "100%",
  height: "auto",
  radius: 0
};
var Image = (0, import_react195.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("Image", defaultProps66, props), {
    className,
    alt,
    src,
    fit,
    width,
    height,
    radius: radius2,
    imageProps,
    withPlaceholder,
    placeholder,
    imageRef,
    classNames,
    styles: styles2,
    caption,
    unstyled,
    style
  } = _a, others = __objRest89(_a, [
    "className",
    "alt",
    "src",
    "fit",
    "width",
    "height",
    "radius",
    "imageProps",
    "withPlaceholder",
    "placeholder",
    "imageRef",
    "classNames",
    "styles",
    "caption",
    "unstyled",
    "style"
  ]);
  const { classes, cx } = Image_styles_default({ radius: radius2 }, { classNames, styles: styles2, unstyled, name: "Image" });
  const [error, setError] = (0, import_react195.useState)(!src);
  const isPlaceholder = withPlaceholder && error;
  useDidUpdate(() => {
    setError(!src);
  }, [src]);
  return import_react195.default.createElement(Box, __spreadValues138({
    className: cx(classes.root, className),
    ref,
    style: __spreadValues138({ width }, style)
  }, others), import_react195.default.createElement("figure", {
    className: classes.figure
  }, import_react195.default.createElement("div", {
    className: classes.imageWrapper
  }, import_react195.default.createElement("img", __spreadValues138({
    className: classes.image,
    src,
    alt,
    style: { objectFit: fit, width, height },
    ref: imageRef,
    onError: (event) => {
      setError(true);
      typeof (imageProps == null ? void 0 : imageProps.onError) === "function" && imageProps.onError(event);
    }
  }, imageProps)), isPlaceholder && import_react195.default.createElement("div", {
    className: classes.placeholder,
    title: alt
  }, placeholder || import_react195.default.createElement("div", null, import_react195.default.createElement(ImageIcon, {
    style: { width: 40, height: 40 }
  })))), !!caption && import_react195.default.createElement(Text, {
    component: "figcaption",
    size: "sm",
    align: "center",
    className: classes.caption
  }, caption)));
});
Image.displayName = "@mantine/core/Image";

// node_modules/@mantine/core/esm/Indicator/Indicator.js
var import_react198 = __toESM(require_react());

// node_modules/@mantine/core/esm/Indicator/Indicator.styles.js
var __defProp139 = Object.defineProperty;
var __defProps58 = Object.defineProperties;
var __getOwnPropDescs58 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols141 = Object.getOwnPropertySymbols;
var __hasOwnProp141 = Object.prototype.hasOwnProperty;
var __propIsEnum141 = Object.prototype.propertyIsEnumerable;
var __defNormalProp139 = (obj, key, value) => key in obj ? __defProp139(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues139 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp141.call(b2, prop))
      __defNormalProp139(a2, prop, b2[prop]);
  if (__getOwnPropSymbols141)
    for (var prop of __getOwnPropSymbols141(b2)) {
      if (__propIsEnum141.call(b2, prop))
        __defNormalProp139(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps58 = (a2, b2) => __defProps58(a2, __getOwnPropDescs58(b2));
var processingAnimation = (color) => keyframes({
  from: {
    boxShadow: `0 0 0.5px 0 ${color}`,
    opacity: 0.6
  },
  to: {
    boxShadow: `0 0 0.5px 4.4px ${color}`,
    opacity: 0
  }
});
function getPositionStyles3(_position, offset3 = 0) {
  const styles2 = {};
  const [position2, placement] = _position.split("-");
  let translateX = "";
  let translateY = "";
  if (position2 === "top") {
    styles2.top = offset3;
    translateY = "-50%";
  }
  if (position2 === "middle") {
    styles2.top = "50%";
    translateY = "-50%";
  }
  if (position2 === "bottom") {
    styles2.bottom = offset3;
    translateY = "50%";
  }
  if (placement === "start") {
    styles2.left = offset3;
    translateX = "-50%";
  }
  if (placement === "center") {
    styles2.left = "50%";
    translateX = "-50%";
  }
  if (placement === "end") {
    styles2.right = offset3;
    translateX = "50%";
  }
  styles2.transform = `translate(${translateX}, ${translateY})`;
  return styles2;
}
var useStyles56 = createStyles((theme, {
  radius: radius2,
  size: size3,
  color,
  position: position2,
  offset: offset3,
  inline: inline2,
  withBorder,
  withLabel,
  zIndex
}) => {
  const { background } = theme.fn.variant({
    variant: "filled",
    primaryFallback: false,
    color: color || theme.primaryColor
  });
  return {
    root: {
      position: "relative",
      display: inline2 ? "inline-block" : "block"
    },
    indicator: __spreadProps58(__spreadValues139({}, getPositionStyles3(position2, offset3)), {
      zIndex,
      position: "absolute",
      [withLabel ? "minWidth" : "width"]: size3,
      height: size3,
      display: "flex",
      justifyContent: "center",
      alignItems: "center",
      fontSize: theme.fontSizes.xs,
      paddingLeft: withLabel ? `calc(${theme.spacing.xs}px / 2)` : 0,
      paddingRight: withLabel ? `calc(${theme.spacing.xs}px / 2)` : 0,
      borderRadius: theme.fn.size({ size: radius2, sizes: theme.radius }),
      backgroundColor: theme.fn.variant({
        variant: "filled",
        primaryFallback: false,
        color: color || theme.primaryColor
      }).background,
      border: withBorder ? `2px solid ${theme.colorScheme === "dark" ? theme.colors.dark[7] : theme.white}` : void 0,
      color: theme.white,
      whiteSpace: "nowrap"
    }),
    processing: {
      animation: `${processingAnimation(background)} 1000ms linear infinite`
    },
    common: __spreadProps58(__spreadValues139({}, getPositionStyles3(position2, offset3)), {
      position: "absolute",
      [withLabel ? "minWidth" : "width"]: size3,
      height: size3,
      borderRadius: theme.fn.size({ size: radius2, sizes: theme.radius })
    })
  };
});
var Indicator_styles_default = useStyles56;

// node_modules/@mantine/core/esm/Indicator/Machine/Machine.js
var import_react197 = __toESM(require_react());

// node_modules/@mantine/core/esm/Indicator/Machine/MachineNumber.js
var import_react196 = __toESM(require_react());

// node_modules/@mantine/core/esm/Indicator/Machine/MachineNumber.styles.js
var currentScrollDownKeyframes = keyframes({
  from: {
    transform: "translateY(-60%)",
    opacity: 0
  },
  to: {
    transform: "translateY(0%)",
    opacity: 1
  }
});
var currentScrollUpKeyframes = keyframes({
  from: {
    transform: "translateY(60%)",
    opacity: 0
  },
  to: {
    transform: "translateY(0%)",
    opacity: 1
  }
});
var oldNumberScrollUpKeyframes = keyframes({
  from: {
    transform: "translateY(0%)",
    opacity: 1
  },
  to: {
    transform: "translateY(-60%)",
    opacity: 0
  }
});
var oldNumberScrollDownKeyframes = keyframes({
  from: {
    transform: "translateY(0%)",
    opacity: 1
  },
  to: {
    transform: "translateY(60%)",
    opacity: 0
  }
});
var useStyles57 = createStyles(() => ({
  baseNumber: {
    height: 18,
    width: "0.6em",
    maxWidth: "0.6em",
    position: "relative",
    display: "inline-block"
  },
  oldNumberTop: {
    transform: "translateY(-100%);"
  },
  oldNumberBottom: {
    transform: "translateY(100%);"
  },
  oldNumber: {
    display: "inline-block",
    opacity: 0,
    position: "absolute",
    left: 0,
    right: 0
  },
  currentNumberTop: {
    transform: "translateY(0%);"
  },
  currentNumber: {
    display: "inline-block",
    opacity: 1,
    position: "absolute",
    top: 0,
    bottom: 0,
    left: 0,
    right: 0
  },
  currentNumberScrollDown: {
    animation: `${currentScrollDownKeyframes} .2s cubic-bezier(0,0,.2, 1)`,
    animationIterationCount: 1
  },
  currentNumberScrollUp: {
    animation: `${currentScrollUpKeyframes} .2s cubic-bezier(0,0,.2, 1)`,
    animationIterationCount: 1
  },
  oldNumberScrollUp: {
    animation: `${oldNumberScrollUpKeyframes} .2s cubic-bezier(0,0,.2, 1)`,
    animationIterationCount: 1
  },
  oldNumberScrollDown: {
    animation: `${oldNumberScrollDownKeyframes} .2s cubic-bezier(0,0,.2, 1)`,
    animationIterationCount: 1
  }
}));
var MachineNumber_styles_default = useStyles57;

// node_modules/@mantine/core/esm/Indicator/Machine/MachineNumber.js
var MachineNumber = (0, import_react196.forwardRef)((props, ref) => {
  const [oldNumber, setOldNumber] = (0, import_react196.useState)(props.value);
  const [newNumber, setNewNumber] = (0, import_react196.useState)(props.value);
  const [scrollAnimationDirection, setScrollAnimationDirection] = (0, import_react196.useState)("up");
  const [isActive, setIsActive] = (0, import_react196.useState)(false);
  const prevValueRef = usePrevious(props.value);
  const scrollByDir = (dir) => {
    setIsActive(true);
    setScrollAnimationDirection(dir);
    setTimeout(() => {
      setIsActive(false);
    }, 180);
  };
  const scroll = () => {
    const { newOriginalNumber, oldOriginalNumber } = props;
    if (newOriginalNumber == null || oldOriginalNumber == null) {
      return;
    }
    if (newOriginalNumber > oldOriginalNumber) {
      scrollByDir("up");
    } else if (newOriginalNumber < oldOriginalNumber) {
      scrollByDir("down");
    }
  };
  (0, import_react196.useEffect)(() => {
    setOldNumber(prevValueRef);
    setNewNumber(props.value);
    scroll();
  }, [props.value, prevValueRef]);
  const { classes, cx } = MachineNumber_styles_default(null, { name: "MachineNumber" });
  const newNumberScrollAnimationClass = (0, import_react196.useMemo)(() => isActive ? scrollAnimationDirection === "up" ? classes.currentNumberScrollUp : classes.currentNumberScrollDown : null, [isActive, scrollAnimationDirection]);
  const oldNumberScrollAnimationClass = (0, import_react196.useMemo)(() => isActive ? scrollAnimationDirection === "up" ? classes.oldNumberScrollUp : classes.oldNumberScrollDown : null, [isActive, scrollAnimationDirection]);
  return import_react196.default.createElement("span", {
    ref,
    className: classes.baseNumber
  }, oldNumber && import_react196.default.createElement("span", {
    className: cx(classes.oldNumber, classes.currentNumberTop, oldNumberScrollAnimationClass)
  }, oldNumber) || null, import_react196.default.createElement("span", null, import_react196.default.createElement("span", {
    className: cx(classes.currentNumber, newNumberScrollAnimationClass)
  }, newNumber)), oldNumber && import_react196.default.createElement("span", {
    className: cx(classes.oldNumber, classes.oldNumberBottom, oldNumberScrollAnimationClass)
  }, oldNumber) || null);
});

// node_modules/@mantine/core/esm/Indicator/Machine/Machine.styles.js
var useStyles58 = createStyles(() => ({
  base: {
    display: "flex",
    alignItems: "center",
    overflow: "hidden"
  }
}));
var Machine_styles_default = useStyles58;

// node_modules/@mantine/core/esm/Indicator/Machine/Machine.js
var Machine = (0, import_react197.forwardRef)(({ value = 0, max: max3 }, ref) => {
  const [oldValue, setOldValue] = (0, import_react197.useState)();
  const [newValue, setNewValue] = (0, import_react197.useState)();
  const prevValueRef = usePrevious(value);
  (0, import_react197.useEffect)(() => {
    if (typeof value === "string") {
      setOldValue(void 0);
      setNewValue(void 0);
    } else if (typeof prevValueRef === "string") {
      setOldValue(void 0);
      setNewValue(value);
    } else {
      setOldValue(prevValueRef);
      setNewValue(value);
    }
  }, [value, prevValueRef]);
  const numbers = (0, import_react197.useMemo)(() => {
    if (typeof value === "string") {
      return [];
    }
    if (value < 1) {
      return [0];
    }
    const result = [];
    let currentValue = value;
    if (typeof max3 === "number") {
      currentValue = Math.min(max3, currentValue);
    }
    while (currentValue >= 1) {
      result.push(currentValue % 10);
      currentValue /= 10;
      currentValue = Math.floor(currentValue);
    }
    result.reverse();
    return result;
  }, [value, max3]);
  const { classes } = Machine_styles_default(null, { name: "machine" });
  return typeof value === "string" ? import_react197.default.createElement("span", {
    ref
  }, value) : import_react197.default.createElement("span", {
    ref,
    className: classes.base
  }, numbers.map((number, i) => import_react197.default.createElement(MachineNumber, {
    key: numbers.length - i - 1,
    value: number,
    oldOriginalNumber: oldValue,
    newOriginalNumber: newValue
  })), typeof max3 === "number" && value > max3 && import_react197.default.createElement("span", null, "+"));
});

// node_modules/@mantine/core/esm/Indicator/Indicator.js
var __defProp140 = Object.defineProperty;
var __getOwnPropSymbols142 = Object.getOwnPropertySymbols;
var __hasOwnProp142 = Object.prototype.hasOwnProperty;
var __propIsEnum142 = Object.prototype.propertyIsEnumerable;
var __defNormalProp140 = (obj, key, value) => key in obj ? __defProp140(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues140 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp142.call(b2, prop))
      __defNormalProp140(a2, prop, b2[prop]);
  if (__getOwnPropSymbols142)
    for (var prop of __getOwnPropSymbols142(b2)) {
      if (__propIsEnum142.call(b2, prop))
        __defNormalProp140(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest90 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp142.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols142)
    for (var prop of __getOwnPropSymbols142(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum142.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps67 = {
  position: "top-end",
  offset: 0,
  inline: false,
  withBorder: false,
  disabled: false,
  showZero: true,
  processing: false,
  dot: true,
  size: 10,
  overflowCount: 99,
  radius: 1e3,
  zIndex: getDefaultZIndex("app")
};
var Indicator = (0, import_react198.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("Indicator", defaultProps67, props), {
    children,
    position: position2,
    offset: offset3,
    size: size3,
    radius: radius2,
    inline: inline2,
    withBorder,
    className,
    color,
    dot,
    styles: styles2,
    label,
    overflowCount,
    showZero,
    classNames,
    disabled,
    zIndex,
    unstyled,
    processing
  } = _a, others = __objRest90(_a, [
    "children",
    "position",
    "offset",
    "size",
    "radius",
    "inline",
    "withBorder",
    "className",
    "color",
    "dot",
    "styles",
    "label",
    "overflowCount",
    "showZero",
    "classNames",
    "disabled",
    "zIndex",
    "unstyled",
    "processing"
  ]);
  const { classes, cx } = Indicator_styles_default({ position: position2, offset: offset3, size: size3, radius: radius2, inline: inline2, color, withBorder, zIndex, withLabel: !!label }, { name: "Indicator", classNames, styles: styles2, unstyled });
  const renderLabel = (0, import_react198.useMemo)(() => {
    if (typeof label === "number") {
      return import_react198.default.createElement(Machine, {
        value: label,
        max: overflowCount
      });
    }
    return label;
  }, [label, overflowCount]);
  const isShowIndicator = (0, import_react198.useMemo)(() => !disabled && (dot || label != null && !(label <= 0 && !showZero)), [disabled, label, showZero]);
  return import_react198.default.createElement(Box, __spreadValues140({
    ref,
    className: cx(classes.root, className)
  }, others), isShowIndicator && import_react198.default.createElement(import_react198.default.Fragment, null, import_react198.default.createElement("div", {
    className: cx(classes.indicator, classes.common)
  }, renderLabel), processing && import_react198.default.createElement("div", {
    className: cx(classes.processing, classes.common)
  })), children);
});
Indicator.displayName = "@mantine/core/Indicator";

// node_modules/@mantine/core/esm/InputBase/InputBase.js
var import_react199 = __toESM(require_react());
var __defProp141 = Object.defineProperty;
var __defProps59 = Object.defineProperties;
var __getOwnPropDescs59 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols143 = Object.getOwnPropertySymbols;
var __hasOwnProp143 = Object.prototype.hasOwnProperty;
var __propIsEnum143 = Object.prototype.propertyIsEnumerable;
var __defNormalProp141 = (obj, key, value) => key in obj ? __defProp141(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues141 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp143.call(b2, prop))
      __defNormalProp141(a2, prop, b2[prop]);
  if (__getOwnPropSymbols143)
    for (var prop of __getOwnPropSymbols143(b2)) {
      if (__propIsEnum143.call(b2, prop))
        __defNormalProp141(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps59 = (a2, b2) => __defProps59(a2, __getOwnPropDescs59(b2));
var __objRest91 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp143.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols143)
    for (var prop of __getOwnPropSymbols143(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum143.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps68 = {
  size: "sm",
  __staticSelector: "InputBase"
};
var _InputBase = (0, import_react199.forwardRef)((props, ref) => {
  const _a = useInputProps("InputBase", defaultProps68, props), { inputProps, wrapperProps } = _a, others = __objRest91(_a, ["inputProps", "wrapperProps"]);
  return import_react199.default.createElement(Input.Wrapper, __spreadValues141({}, wrapperProps), import_react199.default.createElement(Input, __spreadProps59(__spreadValues141(__spreadValues141({}, inputProps), others), {
    ref
  })));
});
_InputBase.displayName = "@mantine/core/InputBase";
var InputBase = createPolymorphicComponent(_InputBase);

// node_modules/@mantine/core/esm/JsonInput/validate-json/validate-json.js
function validateJson(value) {
  if (typeof value === "string" && value.trim().length === 0) {
    return true;
  }
  try {
    JSON.parse(value);
    return true;
  } catch (e) {
    return false;
  }
}

// node_modules/@mantine/core/esm/JsonInput/JsonInput.js
var import_react204 = __toESM(require_react());

// node_modules/@mantine/core/esm/JsonInput/JsonInput.styles.js
var useStyles59 = createStyles((theme, { size: size3 }) => ({
  input: {
    fontFamily: theme.fontFamilyMonospace,
    fontSize: theme.fn.size({ size: size3, sizes: theme.fontSizes }) - 2
  }
}));
var JsonInput_styles_default = useStyles59;

// node_modules/@mantine/core/esm/Textarea/Textarea.js
var import_react203 = __toESM(require_react());

// node_modules/@babel/runtime/helpers/esm/objectWithoutPropertiesLoose.js
function _objectWithoutPropertiesLoose(source, excluded) {
  if (source == null)
    return {};
  var target = {};
  var sourceKeys = Object.keys(source);
  var key, i;
  for (i = 0; i < sourceKeys.length; i++) {
    key = sourceKeys[i];
    if (excluded.indexOf(key) >= 0)
      continue;
    target[key] = source[key];
  }
  return target;
}

// node_modules/react-textarea-autosize/dist/react-textarea-autosize.browser.esm.js
var import_react202 = __toESM(require_react());

// node_modules/use-latest/dist/use-latest.esm.js
var React121 = __toESM(require_react());

// node_modules/use-isomorphic-layout-effect/dist/use-isomorphic-layout-effect.browser.esm.js
var import_react200 = __toESM(require_react());
var index3 = import_react200.useLayoutEffect;
var use_isomorphic_layout_effect_browser_esm_default = index3;

// node_modules/use-latest/dist/use-latest.esm.js
var useLatest = function useLatest2(value) {
  var ref = React121.useRef(value);
  use_isomorphic_layout_effect_browser_esm_default(function() {
    ref.current = value;
  });
  return ref;
};

// node_modules/use-composed-ref/dist/use-composed-ref.esm.js
var import_react201 = __toESM(require_react());
var updateRef = function updateRef2(ref, value) {
  if (typeof ref === "function") {
    ref(value);
    return;
  }
  ref.current = value;
};
var useComposedRef = function useComposedRef2(libRef, userRef) {
  var prevUserRef = (0, import_react201.useRef)();
  return (0, import_react201.useCallback)(function(instance) {
    libRef.current = instance;
    if (prevUserRef.current) {
      updateRef(prevUserRef.current, null);
    }
    prevUserRef.current = userRef;
    if (!userRef) {
      return;
    }
    updateRef(userRef, instance);
  }, [userRef]);
};
var use_composed_ref_esm_default = useComposedRef;

// node_modules/react-textarea-autosize/dist/react-textarea-autosize.browser.esm.js
var HIDDEN_TEXTAREA_STYLE = {
  "min-height": "0",
  "max-height": "none",
  height: "0",
  visibility: "hidden",
  overflow: "hidden",
  position: "absolute",
  "z-index": "-1000",
  top: "0",
  right: "0"
};
var forceHiddenStyles = function forceHiddenStyles2(node2) {
  Object.keys(HIDDEN_TEXTAREA_STYLE).forEach(function(key) {
    node2.style.setProperty(key, HIDDEN_TEXTAREA_STYLE[key], "important");
  });
};
var hiddenTextarea = null;
var getHeight = function getHeight2(node2, sizingData) {
  var height = node2.scrollHeight;
  if (sizingData.sizingStyle.boxSizing === "border-box") {
    return height + sizingData.borderSize;
  }
  return height - sizingData.paddingSize;
};
function calculateNodeHeight(sizingData, value, minRows, maxRows) {
  if (minRows === void 0) {
    minRows = 1;
  }
  if (maxRows === void 0) {
    maxRows = Infinity;
  }
  if (!hiddenTextarea) {
    hiddenTextarea = document.createElement("textarea");
    hiddenTextarea.setAttribute("tabindex", "-1");
    hiddenTextarea.setAttribute("aria-hidden", "true");
    forceHiddenStyles(hiddenTextarea);
  }
  if (hiddenTextarea.parentNode === null) {
    document.body.appendChild(hiddenTextarea);
  }
  var paddingSize = sizingData.paddingSize, borderSize = sizingData.borderSize, sizingStyle = sizingData.sizingStyle;
  var boxSizing = sizingStyle.boxSizing;
  Object.keys(sizingStyle).forEach(function(_key) {
    var key = _key;
    hiddenTextarea.style[key] = sizingStyle[key];
  });
  forceHiddenStyles(hiddenTextarea);
  hiddenTextarea.value = value;
  var height = getHeight(hiddenTextarea, sizingData);
  hiddenTextarea.value = "x";
  var rowHeight = hiddenTextarea.scrollHeight - paddingSize;
  var minHeight = rowHeight * minRows;
  if (boxSizing === "border-box") {
    minHeight = minHeight + paddingSize + borderSize;
  }
  height = Math.max(minHeight, height);
  var maxHeight = rowHeight * maxRows;
  if (boxSizing === "border-box") {
    maxHeight = maxHeight + paddingSize + borderSize;
  }
  height = Math.min(maxHeight, height);
  return [height, rowHeight];
}
var noop2 = function noop3() {
};
var pick = function pick2(props, obj) {
  return props.reduce(function(acc, prop) {
    acc[prop] = obj[prop];
    return acc;
  }, {});
};
var SIZING_STYLE = [
  "borderBottomWidth",
  "borderLeftWidth",
  "borderRightWidth",
  "borderTopWidth",
  "boxSizing",
  "fontFamily",
  "fontSize",
  "fontStyle",
  "fontWeight",
  "letterSpacing",
  "lineHeight",
  "paddingBottom",
  "paddingLeft",
  "paddingRight",
  "paddingTop",
  // non-standard
  "tabSize",
  "textIndent",
  // non-standard
  "textRendering",
  "textTransform",
  "width",
  "wordBreak"
];
var isIE = !!document.documentElement.currentStyle;
var getSizingData = function getSizingData2(node2) {
  var style = window.getComputedStyle(node2);
  if (style === null) {
    return null;
  }
  var sizingStyle = pick(SIZING_STYLE, style);
  var boxSizing = sizingStyle.boxSizing;
  if (boxSizing === "") {
    return null;
  }
  if (isIE && boxSizing === "border-box") {
    sizingStyle.width = parseFloat(sizingStyle.width) + parseFloat(sizingStyle.borderRightWidth) + parseFloat(sizingStyle.borderLeftWidth) + parseFloat(sizingStyle.paddingRight) + parseFloat(sizingStyle.paddingLeft) + "px";
  }
  var paddingSize = parseFloat(sizingStyle.paddingBottom) + parseFloat(sizingStyle.paddingTop);
  var borderSize = parseFloat(sizingStyle.borderBottomWidth) + parseFloat(sizingStyle.borderTopWidth);
  return {
    sizingStyle,
    paddingSize,
    borderSize
  };
};
var useWindowResizeListener = function useWindowResizeListener2(listener) {
  var latestListener = useLatest(listener);
  (0, import_react202.useLayoutEffect)(function() {
    var handler = function handler2(event) {
      latestListener.current(event);
    };
    window.addEventListener("resize", handler);
    return function() {
      window.removeEventListener("resize", handler);
    };
  }, []);
};
var TextareaAutosize = function TextareaAutosize2(_ref, userRef) {
  var cacheMeasurements = _ref.cacheMeasurements, maxRows = _ref.maxRows, minRows = _ref.minRows, _ref$onChange = _ref.onChange, onChange = _ref$onChange === void 0 ? noop2 : _ref$onChange, _ref$onHeightChange = _ref.onHeightChange, onHeightChange = _ref$onHeightChange === void 0 ? noop2 : _ref$onHeightChange, props = _objectWithoutPropertiesLoose(_ref, ["cacheMeasurements", "maxRows", "minRows", "onChange", "onHeightChange"]);
  if (props.style) {
    if ("maxHeight" in props.style) {
      throw new Error("Using `style.maxHeight` for <TextareaAutosize/> is not supported. Please use `maxRows`.");
    }
    if ("minHeight" in props.style) {
      throw new Error("Using `style.minHeight` for <TextareaAutosize/> is not supported. Please use `minRows`.");
    }
  }
  var isControlled = props.value !== void 0;
  var libRef = (0, import_react202.useRef)(null);
  var ref = use_composed_ref_esm_default(libRef, userRef);
  var heightRef = (0, import_react202.useRef)(0);
  var measurementsCacheRef = (0, import_react202.useRef)();
  var resizeTextarea = function resizeTextarea2() {
    var node2 = libRef.current;
    var nodeSizingData = cacheMeasurements && measurementsCacheRef.current ? measurementsCacheRef.current : getSizingData(node2);
    if (!nodeSizingData) {
      return;
    }
    measurementsCacheRef.current = nodeSizingData;
    var _calculateNodeHeight = calculateNodeHeight(nodeSizingData, node2.value || node2.placeholder || "x", minRows, maxRows), height = _calculateNodeHeight[0], rowHeight = _calculateNodeHeight[1];
    if (heightRef.current !== height) {
      heightRef.current = height;
      node2.style.setProperty("height", height + "px", "important");
      onHeightChange(height, {
        rowHeight
      });
    }
  };
  var handleChange = function handleChange2(event) {
    if (!isControlled) {
      resizeTextarea();
    }
    onChange(event);
  };
  {
    (0, import_react202.useLayoutEffect)(resizeTextarea);
    useWindowResizeListener(resizeTextarea);
  }
  return (0, import_react202.createElement)("textarea", _extends({}, props, {
    onChange: handleChange,
    ref
  }));
};
var index4 = (0, import_react202.forwardRef)(TextareaAutosize);
var react_textarea_autosize_browser_esm_default = index4;

// node_modules/@mantine/core/esm/Textarea/Textarea.styles.js
var useStyles60 = createStyles((theme) => ({
  input: {
    paddingTop: theme.spacing.xs,
    paddingBottom: theme.spacing.xs
  }
}));
var Textarea_styles_default = useStyles60;

// node_modules/@mantine/core/esm/Textarea/Textarea.js
var __defProp142 = Object.defineProperty;
var __defProps60 = Object.defineProperties;
var __getOwnPropDescs60 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols144 = Object.getOwnPropertySymbols;
var __hasOwnProp144 = Object.prototype.hasOwnProperty;
var __propIsEnum144 = Object.prototype.propertyIsEnumerable;
var __defNormalProp142 = (obj, key, value) => key in obj ? __defProp142(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues142 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp144.call(b2, prop))
      __defNormalProp142(a2, prop, b2[prop]);
  if (__getOwnPropSymbols144)
    for (var prop of __getOwnPropSymbols144(b2)) {
      if (__propIsEnum144.call(b2, prop))
        __defNormalProp142(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps60 = (a2, b2) => __defProps60(a2, __getOwnPropDescs60(b2));
var __objRest92 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp144.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols144)
    for (var prop of __getOwnPropSymbols144(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum144.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps69 = {
  autosize: false,
  size: "sm",
  __staticSelector: "Textarea"
};
var Textarea = (0, import_react203.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("Textarea", defaultProps69, props), {
    autosize,
    maxRows,
    minRows,
    label,
    error,
    description,
    id,
    className,
    required,
    style,
    wrapperProps,
    classNames,
    styles: styles2,
    size: size3,
    __staticSelector,
    sx,
    errorProps,
    descriptionProps,
    labelProps,
    inputWrapperOrder,
    inputContainer,
    unstyled,
    withAsterisk
  } = _a, others = __objRest92(_a, [
    "autosize",
    "maxRows",
    "minRows",
    "label",
    "error",
    "description",
    "id",
    "className",
    "required",
    "style",
    "wrapperProps",
    "classNames",
    "styles",
    "size",
    "__staticSelector",
    "sx",
    "errorProps",
    "descriptionProps",
    "labelProps",
    "inputWrapperOrder",
    "inputContainer",
    "unstyled",
    "withAsterisk"
  ]);
  const uuid = useId(id);
  const { classes, cx } = Textarea_styles_default();
  const { systemStyles, rest } = extractSystemStyles(others);
  const sharedProps = __spreadValues142({
    required,
    ref,
    invalid: !!error,
    id: uuid,
    classNames: __spreadProps60(__spreadValues142({}, classNames), { input: cx(classes.input, classNames == null ? void 0 : classNames.input) }),
    styles: styles2,
    __staticSelector,
    size: size3,
    multiline: true,
    unstyled
  }, rest);
  return import_react203.default.createElement(Input.Wrapper, __spreadValues142(__spreadValues142({
    label,
    error,
    id: uuid,
    description,
    required,
    style,
    className,
    classNames,
    styles: styles2,
    size: size3,
    __staticSelector,
    sx,
    errorProps,
    labelProps,
    descriptionProps,
    inputContainer,
    inputWrapperOrder,
    unstyled,
    withAsterisk
  }, systemStyles), wrapperProps), autosize ? import_react203.default.createElement(Input, __spreadProps60(__spreadValues142({}, sharedProps), {
    component: react_textarea_autosize_browser_esm_default,
    maxRows,
    minRows
  })) : import_react203.default.createElement(Input, __spreadProps60(__spreadValues142({}, sharedProps), {
    component: "textarea",
    rows: minRows
  })));
});
Textarea.displayName = "@mantine/core/Textarea";

// node_modules/@mantine/core/esm/JsonInput/JsonInput.js
var __defProp143 = Object.defineProperty;
var __defProps61 = Object.defineProperties;
var __getOwnPropDescs61 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols145 = Object.getOwnPropertySymbols;
var __hasOwnProp145 = Object.prototype.hasOwnProperty;
var __propIsEnum145 = Object.prototype.propertyIsEnumerable;
var __defNormalProp143 = (obj, key, value) => key in obj ? __defProp143(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues143 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp145.call(b2, prop))
      __defNormalProp143(a2, prop, b2[prop]);
  if (__getOwnPropSymbols145)
    for (var prop of __getOwnPropSymbols145(b2)) {
      if (__propIsEnum145.call(b2, prop))
        __defNormalProp143(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps61 = (a2, b2) => __defProps61(a2, __getOwnPropDescs61(b2));
var __objRest93 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp145.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols145)
    for (var prop of __getOwnPropSymbols145(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum145.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps70 = {
  formatOnBlur: false,
  size: "sm"
};
var JsonInput = (0, import_react204.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("JsonInput", defaultProps70, props), {
    value,
    defaultValue,
    onChange,
    onFocus,
    onBlur,
    error,
    formatOnBlur,
    size: size3,
    validationError,
    classNames,
    unstyled,
    readOnly
  } = _a, others = __objRest93(_a, [
    "value",
    "defaultValue",
    "onChange",
    "onFocus",
    "onBlur",
    "error",
    "formatOnBlur",
    "size",
    "validationError",
    "classNames",
    "unstyled",
    "readOnly"
  ]);
  const { classes, cx } = JsonInput_styles_default({ size: size3 }, { name: "JsonInput", unstyled });
  const [_value, setValue] = useUncontrolled({
    value,
    defaultValue,
    finalValue: "",
    onChange
  });
  const [valid, setValid] = (0, import_react204.useState)(validateJson(_value));
  const handleFocus = (event) => {
    typeof onFocus === "function" && onFocus(event);
    setValid(true);
  };
  const handleBlur = (event) => {
    typeof onBlur === "function" && onBlur(event);
    const isValid = validateJson(event.currentTarget.value);
    isValid && event.currentTarget.value.trim() !== "" && setValue(JSON.stringify(JSON.parse(event.currentTarget.value), null, 2));
    setValid(isValid);
  };
  return import_react204.default.createElement(Textarea, __spreadValues143({
    value: _value,
    onChange: (event) => setValue(event.currentTarget.value),
    onFocus: handleFocus,
    onBlur: handleBlur,
    error: valid ? error : validationError || true,
    __staticSelector: "JsonInput",
    classNames: __spreadProps61(__spreadValues143({}, classNames), { input: cx(classes.input, classNames == null ? void 0 : classNames.input) }),
    autoComplete: "nope",
    ref,
    unstyled,
    readOnly
  }, others));
});
JsonInput.displayName = "@mantine/core/JsonInput";

// node_modules/@mantine/core/esm/Kbd/Kbd.js
var import_react205 = __toESM(require_react());

// node_modules/@mantine/core/esm/Kbd/Kbd.styles.js
var useStyles61 = createStyles((theme) => ({
  root: {
    lineHeight: theme.lineHeight,
    fontFamily: theme.fontFamilyMonospace,
    fontSize: theme.fontSizes.xs,
    fontWeight: 700,
    backgroundColor: theme.colorScheme === "dark" ? theme.colors.dark[5] : theme.colors.gray[0],
    color: theme.colorScheme === "dark" ? theme.colors.dark[0] : theme.colors.gray[7],
    padding: `3px calc(${theme.spacing.xs}px / 2)`,
    borderRadius: theme.radius.sm,
    border: `1px solid ${theme.colorScheme === "dark" ? theme.colors.dark[4] : theme.colors.gray[3]}`,
    borderBottom: `3px solid ${theme.colorScheme === "dark" ? theme.colors.dark[4] : theme.colors.gray[3]}`
  }
}));
var Kbd_styles_default = useStyles61;

// node_modules/@mantine/core/esm/Kbd/Kbd.js
var __defProp144 = Object.defineProperty;
var __getOwnPropSymbols146 = Object.getOwnPropertySymbols;
var __hasOwnProp146 = Object.prototype.hasOwnProperty;
var __propIsEnum146 = Object.prototype.propertyIsEnumerable;
var __defNormalProp144 = (obj, key, value) => key in obj ? __defProp144(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues144 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp146.call(b2, prop))
      __defNormalProp144(a2, prop, b2[prop]);
  if (__getOwnPropSymbols146)
    for (var prop of __getOwnPropSymbols146(b2)) {
      if (__propIsEnum146.call(b2, prop))
        __defNormalProp144(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest94 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp146.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols146)
    for (var prop of __getOwnPropSymbols146(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum146.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var Kbd = (0, import_react205.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("Kbd", {}, props), { className, children, unstyled } = _a, others = __objRest94(_a, ["className", "children", "unstyled"]);
  const { classes, cx } = Kbd_styles_default(null, { name: "Kbd", unstyled });
  return import_react205.default.createElement(Box, __spreadValues144({
    component: "kbd",
    className: cx(classes.root, className),
    ref
  }, others), children);
});
Kbd.displayName = "@mantine/core/Kbd";

// node_modules/@mantine/core/esm/List/List.js
var import_react207 = __toESM(require_react());

// node_modules/@mantine/core/esm/List/ListItem/ListItem.js
var import_react206 = __toESM(require_react());

// node_modules/@mantine/core/esm/List/List.context.js
var [ListProvider, useListContext] = createSafeContext("List component was not found in tree");

// node_modules/@mantine/core/esm/List/ListItem/ListItem.styles.js
var useStyles62 = createStyles((theme, { spacing, center }, getRef2) => ({
  itemWrapper: {
    ref: getRef2("itemWrapper"),
    display: "inline-flex",
    flexDirection: "column",
    whiteSpace: "normal"
  },
  item: {
    whiteSpace: "nowrap",
    lineHeight: center ? 1 : theme.lineHeight,
    "&:not(:first-of-type)": {
      marginTop: theme.fn.size({ size: spacing, sizes: theme.spacing })
    }
  },
  withIcon: {
    listStyle: "none",
    [`& .${getRef2("itemWrapper")}`]: {
      display: "inline-flex",
      alignItems: center ? "center" : "flex-start",
      flexDirection: "row"
    }
  },
  itemIcon: {
    display: "inline-block",
    verticalAlign: "middle",
    marginRight: theme.spacing.sm
  }
}));
var ListItem_styles_default = useStyles62;

// node_modules/@mantine/core/esm/List/ListItem/ListItem.js
var __defProp145 = Object.defineProperty;
var __getOwnPropSymbols147 = Object.getOwnPropertySymbols;
var __hasOwnProp147 = Object.prototype.hasOwnProperty;
var __propIsEnum147 = Object.prototype.propertyIsEnumerable;
var __defNormalProp145 = (obj, key, value) => key in obj ? __defProp145(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues145 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp147.call(b2, prop))
      __defNormalProp145(a2, prop, b2[prop]);
  if (__getOwnPropSymbols147)
    for (var prop of __getOwnPropSymbols147(b2)) {
      if (__propIsEnum147.call(b2, prop))
        __defNormalProp145(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest95 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp147.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols147)
    for (var prop of __getOwnPropSymbols147(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum147.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps71 = {};
var ListItem = (0, import_react206.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("ListItem", defaultProps71, props), { className, children, icon } = _a, others = __objRest95(_a, ["className", "children", "icon"]);
  const {
    icon: ctxIcon,
    spacing,
    center,
    listStyleType,
    size: size3,
    withPadding,
    classNames,
    styles: styles2,
    unstyled
  } = useListContext();
  const _icon = icon || ctxIcon;
  const { classes, cx } = ListItem_styles_default({ withPadding, size: size3, listStyleType, center, spacing }, { classNames, styles: styles2, unstyled, name: "List" });
  return import_react206.default.createElement(Box, __spreadValues145({
    component: "li",
    className: cx(classes.item, { [classes.withIcon]: _icon }, className),
    ref
  }, others), import_react206.default.createElement("div", {
    className: classes.itemWrapper
  }, _icon && import_react206.default.createElement("span", {
    className: classes.itemIcon
  }, _icon), import_react206.default.createElement("span", null, children)));
});
ListItem.displayName = "@mantine/core/ListItem";

// node_modules/@mantine/core/esm/List/List.styles.js
var __defProp146 = Object.defineProperty;
var __defProps62 = Object.defineProperties;
var __getOwnPropDescs62 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols148 = Object.getOwnPropertySymbols;
var __hasOwnProp148 = Object.prototype.hasOwnProperty;
var __propIsEnum148 = Object.prototype.propertyIsEnumerable;
var __defNormalProp146 = (obj, key, value) => key in obj ? __defProp146(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues146 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp148.call(b2, prop))
      __defNormalProp146(a2, prop, b2[prop]);
  if (__getOwnPropSymbols148)
    for (var prop of __getOwnPropSymbols148(b2)) {
      if (__propIsEnum148.call(b2, prop))
        __defNormalProp146(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps62 = (a2, b2) => __defProps62(a2, __getOwnPropDescs62(b2));
var useStyles63 = createStyles((theme, { withPadding, size: size3, listStyleType }) => ({
  root: __spreadProps62(__spreadValues146({}, theme.fn.fontStyles()), {
    listStyleType,
    color: theme.colorScheme === "dark" ? theme.colors.dark[0] : theme.black,
    fontSize: theme.fn.size({ size: size3, sizes: theme.fontSizes }),
    lineHeight: theme.lineHeight,
    margin: 0,
    paddingLeft: withPadding ? theme.spacing.xl : 0,
    listStylePosition: "inside"
  })
}));
var List_styles_default = useStyles63;

// node_modules/@mantine/core/esm/List/List.js
var __defProp147 = Object.defineProperty;
var __getOwnPropSymbols149 = Object.getOwnPropertySymbols;
var __hasOwnProp149 = Object.prototype.hasOwnProperty;
var __propIsEnum149 = Object.prototype.propertyIsEnumerable;
var __defNormalProp147 = (obj, key, value) => key in obj ? __defProp147(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues147 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp149.call(b2, prop))
      __defNormalProp147(a2, prop, b2[prop]);
  if (__getOwnPropSymbols149)
    for (var prop of __getOwnPropSymbols149(b2)) {
      if (__propIsEnum149.call(b2, prop))
        __defNormalProp147(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest96 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp149.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols149)
    for (var prop of __getOwnPropSymbols149(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum149.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps72 = {
  type: "unordered",
  size: "md",
  spacing: 0
};
var List = (0, import_react207.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("List", defaultProps72, props), {
    children,
    type,
    size: size3,
    listStyleType,
    withPadding,
    center,
    spacing,
    icon,
    className,
    styles: styles2,
    classNames,
    unstyled
  } = _a, others = __objRest96(_a, [
    "children",
    "type",
    "size",
    "listStyleType",
    "withPadding",
    "center",
    "spacing",
    "icon",
    "className",
    "styles",
    "classNames",
    "unstyled"
  ]);
  const { classes, cx } = List_styles_default({ withPadding, size: size3, listStyleType, center, spacing }, { classNames, styles: styles2, name: "List", unstyled });
  return import_react207.default.createElement(ListProvider, {
    value: {
      spacing,
      center,
      icon,
      listStyleType,
      size: size3,
      withPadding,
      classNames,
      styles: styles2,
      unstyled
    }
  }, import_react207.default.createElement(Box, __spreadValues147({
    component: type === "unordered" ? "ul" : "ol",
    className: cx(classes.root, className),
    ref
  }, others), children));
});
List.Item = ListItem;
List.displayName = "@mantine/core/List";

// node_modules/@mantine/core/esm/LoadingOverlay/LoadingOverlay.js
var import_react208 = __toESM(require_react());

// node_modules/@mantine/core/esm/LoadingOverlay/LoadingOverlay.styles.js
var useStyles64 = createStyles({
  root: {
    position: "absolute",
    top: 0,
    left: 0,
    right: 0,
    bottom: 0,
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    overflow: "hidden"
  }
});
var LoadingOverlay_styles_default = useStyles64;

// node_modules/@mantine/core/esm/LoadingOverlay/LoadingOverlay.js
var __defProp148 = Object.defineProperty;
var __defProps63 = Object.defineProperties;
var __getOwnPropDescs63 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols150 = Object.getOwnPropertySymbols;
var __hasOwnProp150 = Object.prototype.hasOwnProperty;
var __propIsEnum150 = Object.prototype.propertyIsEnumerable;
var __defNormalProp148 = (obj, key, value) => key in obj ? __defProp148(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues148 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp150.call(b2, prop))
      __defNormalProp148(a2, prop, b2[prop]);
  if (__getOwnPropSymbols150)
    for (var prop of __getOwnPropSymbols150(b2)) {
      if (__propIsEnum150.call(b2, prop))
        __defNormalProp148(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps63 = (a2, b2) => __defProps63(a2, __getOwnPropDescs63(b2));
var __objRest97 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp150.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols150)
    for (var prop of __getOwnPropSymbols150(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum150.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps73 = {
  overlayOpacity: 0.75,
  transitionDuration: 0,
  zIndex: getDefaultZIndex("overlay")
};
var LoadingOverlay = (0, import_react208.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("LoadingOverlay", defaultProps73, props), {
    className,
    visible: visible2,
    loaderProps,
    overlayOpacity,
    overlayColor,
    transitionDuration,
    exitTransitionDuration,
    zIndex,
    style,
    loader,
    radius: radius2,
    overlayBlur,
    unstyled
  } = _a, others = __objRest97(_a, [
    "className",
    "visible",
    "loaderProps",
    "overlayOpacity",
    "overlayColor",
    "transitionDuration",
    "exitTransitionDuration",
    "zIndex",
    "style",
    "loader",
    "radius",
    "overlayBlur",
    "unstyled"
  ]);
  const { classes, cx, theme } = LoadingOverlay_styles_default(null, { name: "LoadingOverlay", unstyled });
  const _zIndex = `calc(${zIndex} + 1)`;
  return import_react208.default.createElement(Transition, {
    duration: transitionDuration,
    exitDuration: exitTransitionDuration,
    mounted: visible2,
    transition: "fade"
  }, (transitionStyles) => import_react208.default.createElement(Box, __spreadValues148({
    className: cx(classes.root, className),
    style: __spreadProps63(__spreadValues148(__spreadValues148({}, transitionStyles), style), { zIndex }),
    ref
  }, others), loader ? import_react208.default.createElement("div", {
    style: { zIndex: _zIndex }
  }, loader) : import_react208.default.createElement(Loader, __spreadValues148({
    style: { zIndex: _zIndex }
  }, loaderProps)), import_react208.default.createElement(Overlay, {
    opacity: overlayOpacity,
    zIndex,
    radius: radius2,
    blur: overlayBlur,
    unstyled,
    color: overlayColor || (theme.colorScheme === "dark" ? theme.colors.dark[5] : theme.white)
  })));
});
LoadingOverlay.displayName = "@mantine/core/LoadingOverlay";

// node_modules/@mantine/core/esm/MediaQuery/MediaQuery.js
var import_react209 = __toESM(require_react());

// node_modules/@mantine/core/esm/MediaQuery/MediaQuery.styles.js
var useStyles65 = createStyles((theme, { smallerThan: smallerThan2, largerThan: largerThan2, query, styles: styles2 }) => {
  const media = {};
  const minWidth = theme.fn.size({ size: largerThan2, sizes: theme.breakpoints });
  const maxWidth = theme.fn.size({ size: smallerThan2, sizes: theme.breakpoints });
  const _styles = typeof styles2 === "function" ? styles2(theme) : styles2;
  if (largerThan2 !== void 0 && smallerThan2 !== void 0) {
    media[`@media (min-width: ${minWidth}px) and (max-width: ${maxWidth - 1}px)`] = _styles;
  } else {
    if (largerThan2 !== void 0) {
      media[`@media (min-width: ${theme.fn.size({ size: largerThan2, sizes: theme.breakpoints })}px)`] = _styles;
    }
    if (smallerThan2 !== void 0) {
      media[`@media (max-width: ${theme.fn.size({ size: smallerThan2, sizes: theme.breakpoints }) - 1}px)`] = _styles;
    }
  }
  if (query) {
    media[`@media ${query}`] = _styles;
  }
  return { media };
});
var MediaQuery_styles_default = useStyles65;

// node_modules/@mantine/core/esm/MediaQuery/MediaQuery.js
function MediaQuery(props) {
  var _a;
  const { children, smallerThan: smallerThan2, largerThan: largerThan2, query, styles: styles2, className } = useComponentDefaultProps("MediaQuery", {}, props);
  const { classes, cx } = MediaQuery_styles_default({ smallerThan: smallerThan2, largerThan: largerThan2, query, styles: styles2 }, { name: "MediaQuery" });
  const child = import_react209.Children.only(children);
  if (typeof child === "object" && child !== null && "props" in child) {
    return import_react209.default.cloneElement(child, {
      className: cx(classes.media, (_a = child.props) == null ? void 0 : _a.className, className)
    });
  }
  return child;
}
MediaQuery.displayName = "@mantine/core/MediaQuery";

// node_modules/@mantine/core/esm/Menu/Menu.js
var import_react215 = __toESM(require_react());

// node_modules/@mantine/core/esm/Menu/MenuDivider/MenuDivider.js
var import_react210 = __toESM(require_react());

// node_modules/@mantine/core/esm/Menu/Menu.errors.js
var MENU_ERRORS = {
  context: "Menu component was not found in the tree",
  children: "Menu.Target component children should be an element or a component that accepts ref, fragments, strings, numbers and other primitive values are not supported"
};

// node_modules/@mantine/core/esm/Menu/Menu.context.js
var [MenuContextProvider, useMenuContext] = createSafeContext(MENU_ERRORS.context);

// node_modules/@mantine/core/esm/Menu/MenuDivider/MenuDivider.styles.js
var useStyles66 = createStyles((theme) => ({
  divider: {
    margin: `calc(${theme.spacing.xs}px / 2) -5px`,
    borderTop: `1px solid ${theme.colorScheme === "dark" ? theme.colors.dark[4] : theme.colors.gray[2]}`
  }
}));
var MenuDivider_styles_default = useStyles66;

// node_modules/@mantine/core/esm/Menu/MenuDivider/MenuDivider.js
var __defProp149 = Object.defineProperty;
var __getOwnPropSymbols151 = Object.getOwnPropertySymbols;
var __hasOwnProp151 = Object.prototype.hasOwnProperty;
var __propIsEnum151 = Object.prototype.propertyIsEnumerable;
var __defNormalProp149 = (obj, key, value) => key in obj ? __defProp149(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues149 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp151.call(b2, prop))
      __defNormalProp149(a2, prop, b2[prop]);
  if (__getOwnPropSymbols151)
    for (var prop of __getOwnPropSymbols151(b2)) {
      if (__propIsEnum151.call(b2, prop))
        __defNormalProp149(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest98 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp151.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols151)
    for (var prop of __getOwnPropSymbols151(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum151.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps74 = {};
var MenuDivider = (0, import_react210.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("MenuDivider", defaultProps74, props), { children, className } = _a, others = __objRest98(_a, ["children", "className"]);
  const { classNames, styles: styles2, unstyled } = useMenuContext();
  const { classes, cx } = MenuDivider_styles_default(null, { name: "Menu", classNames, styles: styles2, unstyled });
  return import_react210.default.createElement(Box, __spreadValues149({
    className: cx(classes.divider, className),
    ref
  }, others));
});
MenuDivider.displayName = "@mantine/core/MenuDivider";

// node_modules/@mantine/core/esm/Menu/MenuDropdown/MenuDropdown.js
var import_react211 = __toESM(require_react());
var __defProp150 = Object.defineProperty;
var __getOwnPropSymbols152 = Object.getOwnPropertySymbols;
var __hasOwnProp152 = Object.prototype.hasOwnProperty;
var __propIsEnum152 = Object.prototype.propertyIsEnumerable;
var __defNormalProp150 = (obj, key, value) => key in obj ? __defProp150(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues150 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp152.call(b2, prop))
      __defNormalProp150(a2, prop, b2[prop]);
  if (__getOwnPropSymbols152)
    for (var prop of __getOwnPropSymbols152(b2)) {
      if (__propIsEnum152.call(b2, prop))
        __defNormalProp150(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest99 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp152.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols152)
    for (var prop of __getOwnPropSymbols152(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum152.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps75 = {};
function MenuDropdown(props) {
  const _a = useComponentDefaultProps("MenuDropdown", defaultProps75, props), { children, onMouseEnter, onMouseLeave } = _a, others = __objRest99(_a, ["children", "onMouseEnter", "onMouseLeave"]);
  const wrapperRef = (0, import_react211.useRef)();
  const ctx = useMenuContext();
  const handleKeyDown = (event) => {
    if (event.key === "ArrowUp" || event.key === "ArrowDown") {
      event.preventDefault();
      wrapperRef.current.querySelectorAll("[data-menu-item]")[0].focus();
    }
  };
  const handleMouseEnter = createEventHandler(onMouseEnter, () => ctx.trigger === "hover" && ctx.openDropdown());
  const handleMouseLeave = createEventHandler(onMouseLeave, () => ctx.trigger === "hover" && ctx.closeDropdown());
  return import_react211.default.createElement(Popover.Dropdown, __spreadValues150({
    onMouseEnter: handleMouseEnter,
    onMouseLeave: handleMouseLeave,
    role: "menu",
    "aria-orientation": "vertical"
  }, others), import_react211.default.createElement("div", {
    tabIndex: -1,
    "data-menu-dropdown": true,
    "data-autofocus": true,
    onKeyDown: handleKeyDown,
    ref: wrapperRef,
    style: { outline: 0 }
  }, children));
}
MenuDropdown.displayName = "@mantine/core/MenuDropdown";

// node_modules/@mantine/core/esm/Menu/MenuItem/MenuItem.js
var import_react212 = __toESM(require_react());

// node_modules/@mantine/core/esm/Menu/MenuItem/MenuItem.styles.js
var __defProp151 = Object.defineProperty;
var __defProps64 = Object.defineProperties;
var __getOwnPropDescs64 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols153 = Object.getOwnPropertySymbols;
var __hasOwnProp153 = Object.prototype.hasOwnProperty;
var __propIsEnum153 = Object.prototype.propertyIsEnumerable;
var __defNormalProp151 = (obj, key, value) => key in obj ? __defProp151(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues151 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp153.call(b2, prop))
      __defNormalProp151(a2, prop, b2[prop]);
  if (__getOwnPropSymbols153)
    for (var prop of __getOwnPropSymbols153(b2)) {
      if (__propIsEnum153.call(b2, prop))
        __defNormalProp151(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps64 = (a2, b2) => __defProps64(a2, __getOwnPropDescs64(b2));
var useStyles67 = createStyles((theme, { color, radius: radius2 }) => ({
  item: __spreadProps64(__spreadValues151({}, theme.fn.fontStyles()), {
    WebkitTapHighlightColor: "transparent",
    fontSize: theme.fontSizes.sm,
    border: 0,
    backgroundColor: "transparent",
    outline: 0,
    width: "100%",
    textAlign: "left",
    textDecoration: "none",
    boxSizing: "border-box",
    padding: `${theme.spacing.xs}px ${theme.spacing.sm}px`,
    cursor: "pointer",
    borderRadius: theme.fn.radius(radius2),
    color: color ? theme.fn.variant({ variant: "filled", primaryFallback: false, color }).background : theme.colorScheme === "dark" ? theme.colors.dark[0] : theme.black,
    display: "flex",
    alignItems: "center",
    "&:disabled": {
      color: theme.colorScheme === "dark" ? theme.colors.dark[3] : theme.colors.gray[5],
      pointerEvents: "none",
      userSelect: "none"
    },
    "&[data-hovered]": {
      backgroundColor: color ? theme.fn.variant({ variant: "light", color }).background : theme.colorScheme === "dark" ? theme.fn.rgba(theme.colors.dark[3], 0.35) : theme.colors.gray[1]
    }
  }),
  itemLabel: {
    flex: 1
  },
  itemIcon: {
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    marginRight: theme.spacing.xs
  },
  itemRightSection: {}
}));
var MenuItem_styles_default = useStyles67;

// node_modules/@mantine/core/esm/Menu/MenuItem/MenuItem.js
var __defProp152 = Object.defineProperty;
var __defProps65 = Object.defineProperties;
var __getOwnPropDescs65 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols154 = Object.getOwnPropertySymbols;
var __hasOwnProp154 = Object.prototype.hasOwnProperty;
var __propIsEnum154 = Object.prototype.propertyIsEnumerable;
var __defNormalProp152 = (obj, key, value) => key in obj ? __defProp152(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues152 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp154.call(b2, prop))
      __defNormalProp152(a2, prop, b2[prop]);
  if (__getOwnPropSymbols154)
    for (var prop of __getOwnPropSymbols154(b2)) {
      if (__propIsEnum154.call(b2, prop))
        __defNormalProp152(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps65 = (a2, b2) => __defProps65(a2, __getOwnPropDescs65(b2));
var __objRest100 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp154.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols154)
    for (var prop of __getOwnPropSymbols154(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum154.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps76 = {};
var _MenuItem = (0, import_react212.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("MenuItem", defaultProps76, props), { children, className, color, closeMenuOnClick, icon, rightSection } = _a, others = __objRest100(_a, ["children", "className", "color", "closeMenuOnClick", "icon", "rightSection"]);
  const ctx = useMenuContext();
  const { classes, cx, theme } = MenuItem_styles_default({ radius: ctx.radius, color }, { name: "Menu", classNames: ctx.classNames, styles: ctx.styles, unstyled: ctx.unstyled });
  const itemRef = (0, import_react212.useRef)();
  const itemIndex = ctx.getItemIndex(itemRef.current);
  const _others = others;
  const handleMouseLeave = createEventHandler(_others.onMouseLeave, () => ctx.setHovered(-1));
  const handleMouseEnter = createEventHandler(_others.onMouseEnter, () => ctx.setHovered(ctx.getItemIndex(itemRef.current)));
  const handleClick = createEventHandler(_others.onClick, () => {
    if (typeof closeMenuOnClick === "boolean") {
      closeMenuOnClick && ctx.closeDropdownImmediately();
    } else {
      ctx.closeOnItemClick && ctx.closeDropdownImmediately();
    }
  });
  const handleFocus = createEventHandler(_others.onFocus, () => ctx.setHovered(ctx.getItemIndex(itemRef.current)));
  return import_react212.default.createElement(Box, __spreadProps65(__spreadValues152({
    component: "button",
    type: "button"
  }, others), {
    tabIndex: -1,
    onFocus: handleFocus,
    className: cx(classes.item, className),
    ref: useMergedRef(itemRef, ref),
    role: "menuitem",
    "data-menu-item": true,
    "data-hovered": ctx.hovered === itemIndex ? true : void 0,
    onMouseEnter: handleMouseEnter,
    onMouseLeave: handleMouseLeave,
    onClick: handleClick,
    onKeyDown: createScopedKeydownHandler({
      siblingSelector: "[data-menu-item]",
      parentSelector: "[data-menu-dropdown]",
      activateOnFocus: false,
      loop: ctx.loop,
      dir: theme.dir,
      orientation: "vertical",
      onKeyDown: _others.onKeydown
    })
  }), icon && import_react212.default.createElement("div", {
    className: classes.itemIcon
  }, icon), children && import_react212.default.createElement("div", {
    className: classes.itemLabel
  }, children), rightSection && import_react212.default.createElement("div", {
    className: classes.itemRightSection
  }, rightSection));
});
_MenuItem.displayName = "@mantine/core/MenuItem";
var MenuItem = createPolymorphicComponent(_MenuItem);

// node_modules/@mantine/core/esm/Menu/MenuLabel/MenuLabel.js
var import_react213 = __toESM(require_react());

// node_modules/@mantine/core/esm/Menu/MenuLabel/MenuLabel.styles.js
var useStyles68 = createStyles((theme) => ({
  label: {
    color: theme.colorScheme === "dark" ? theme.colors.dark[2] : theme.colors.gray[6],
    fontWeight: 500,
    fontSize: theme.fontSizes.xs,
    padding: `calc(${theme.spacing.xs}px / 2) ${theme.spacing.sm}px`,
    cursor: "default"
  }
}));
var MenuLabel_styles_default = useStyles68;

// node_modules/@mantine/core/esm/Menu/MenuLabel/MenuLabel.js
var __defProp153 = Object.defineProperty;
var __getOwnPropSymbols155 = Object.getOwnPropertySymbols;
var __hasOwnProp155 = Object.prototype.hasOwnProperty;
var __propIsEnum155 = Object.prototype.propertyIsEnumerable;
var __defNormalProp153 = (obj, key, value) => key in obj ? __defProp153(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues153 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp155.call(b2, prop))
      __defNormalProp153(a2, prop, b2[prop]);
  if (__getOwnPropSymbols155)
    for (var prop of __getOwnPropSymbols155(b2)) {
      if (__propIsEnum155.call(b2, prop))
        __defNormalProp153(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest101 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp155.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols155)
    for (var prop of __getOwnPropSymbols155(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum155.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps77 = {};
var MenuLabel = (0, import_react213.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("MenuLabel", defaultProps77, props), { children, className } = _a, others = __objRest101(_a, ["children", "className"]);
  const { classNames, styles: styles2, unstyled } = useMenuContext();
  const { classes, cx } = MenuLabel_styles_default(null, { name: "Menu", classNames, styles: styles2, unstyled });
  return import_react213.default.createElement(Text, __spreadValues153({
    className: cx(classes.label, className),
    ref
  }, others), children);
});
MenuLabel.displayName = "@mantine/core/MenuLabel";

// node_modules/@mantine/core/esm/Menu/MenuTarget/MenuTarget.js
var import_react214 = __toESM(require_react());
var __defProp154 = Object.defineProperty;
var __getOwnPropSymbols156 = Object.getOwnPropertySymbols;
var __hasOwnProp156 = Object.prototype.hasOwnProperty;
var __propIsEnum156 = Object.prototype.propertyIsEnumerable;
var __defNormalProp154 = (obj, key, value) => key in obj ? __defProp154(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues154 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp156.call(b2, prop))
      __defNormalProp154(a2, prop, b2[prop]);
  if (__getOwnPropSymbols156)
    for (var prop of __getOwnPropSymbols156(b2)) {
      if (__propIsEnum156.call(b2, prop))
        __defNormalProp154(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest102 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp156.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols156)
    for (var prop of __getOwnPropSymbols156(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum156.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps78 = {
  refProp: "ref"
};
var MenuTarget = (0, import_react214.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("MenuTarget", defaultProps78, props), { children, refProp } = _a, others = __objRest102(_a, ["children", "refProp"]);
  if (!isElement(children)) {
    throw new Error(MENU_ERRORS.children);
  }
  const ctx = useMenuContext();
  const onClick = createEventHandler(children.props.onClick, () => ctx.trigger === "click" && ctx.toggleDropdown());
  const onMouseEnter = createEventHandler(children.props.onMouseEnter, () => ctx.trigger === "hover" && ctx.openDropdown());
  const onMouseLeave = createEventHandler(children.props.onMouseLeave, () => ctx.trigger === "hover" && ctx.closeDropdown());
  return import_react214.default.createElement(Popover.Target, __spreadValues154({
    refProp,
    popupType: "menu",
    ref
  }, others), (0, import_react214.cloneElement)(children, {
    onClick,
    onMouseEnter,
    onMouseLeave,
    "data-expanded": ctx.opened ? true : void 0
  }));
});
MenuTarget.displayName = "@mantine/core/MenuTarget";

// node_modules/@mantine/core/esm/Menu/Menu.styles.js
var useStyles69 = createStyles({
  dropdown: { padding: 4 }
});
var Menu_styles_default = useStyles69;

// node_modules/@mantine/core/esm/Menu/Menu.js
var __defProp155 = Object.defineProperty;
var __defProps66 = Object.defineProperties;
var __getOwnPropDescs66 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols157 = Object.getOwnPropertySymbols;
var __hasOwnProp157 = Object.prototype.hasOwnProperty;
var __propIsEnum157 = Object.prototype.propertyIsEnumerable;
var __defNormalProp155 = (obj, key, value) => key in obj ? __defProp155(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues155 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp157.call(b2, prop))
      __defNormalProp155(a2, prop, b2[prop]);
  if (__getOwnPropSymbols157)
    for (var prop of __getOwnPropSymbols157(b2)) {
      if (__propIsEnum157.call(b2, prop))
        __defNormalProp155(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps66 = (a2, b2) => __defProps66(a2, __getOwnPropDescs66(b2));
var __objRest103 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp157.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols157)
    for (var prop of __getOwnPropSymbols157(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum157.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps79 = {
  closeOnItemClick: true,
  loop: true,
  trigger: "click",
  openDelay: 0,
  closeDelay: 100
};
function Menu(props) {
  const _a = useComponentDefaultProps("Menu", defaultProps79, props), {
    children,
    onOpen,
    onClose,
    opened,
    defaultOpened,
    onChange,
    closeOnItemClick,
    loop,
    closeOnEscape: closeOnEscape2,
    trigger,
    openDelay,
    closeDelay,
    classNames,
    styles: styles2,
    unstyled,
    radius: radius2
  } = _a, others = __objRest103(_a, [
    "children",
    "onOpen",
    "onClose",
    "opened",
    "defaultOpened",
    "onChange",
    "closeOnItemClick",
    "loop",
    "closeOnEscape",
    "trigger",
    "openDelay",
    "closeDelay",
    "classNames",
    "styles",
    "unstyled",
    "radius"
  ]);
  const { classes, cx } = Menu_styles_default();
  const [hovered, { setHovered, resetHovered }] = useHovered();
  const [_opened, setOpened] = useUncontrolled({
    value: opened,
    defaultValue: defaultOpened,
    finalValue: false,
    onChange
  });
  const close = () => {
    setOpened(false);
    _opened && (onClose == null ? void 0 : onClose());
  };
  const open = () => {
    setOpened(true);
    !_opened && (onOpen == null ? void 0 : onOpen());
  };
  const toggleDropdown = () => _opened ? close() : open();
  const { openDropdown, closeDropdown } = useDelayedHover({ open, close, closeDelay, openDelay });
  const getItemIndex = (node2) => getContextItemIndex("[data-menu-item]", "[data-menu-dropdown]", node2);
  useDidUpdate(() => {
    resetHovered();
  }, [_opened]);
  return import_react215.default.createElement(MenuContextProvider, {
    value: {
      opened: _opened,
      toggleDropdown,
      getItemIndex,
      hovered,
      setHovered,
      closeOnItemClick,
      closeDropdown: trigger === "click" ? close : closeDropdown,
      openDropdown: trigger === "click" ? open : openDropdown,
      closeDropdownImmediately: close,
      loop,
      trigger,
      radius: radius2,
      classNames,
      styles: styles2,
      unstyled
    }
  }, import_react215.default.createElement(Popover, __spreadProps66(__spreadValues155({}, others), {
    radius: radius2,
    opened: _opened,
    onChange: setOpened,
    defaultOpened,
    trapFocus: trigger === "click",
    closeOnEscape: closeOnEscape2 && trigger === "click",
    __staticSelector: "Menu",
    classNames: __spreadProps66(__spreadValues155({}, classNames), { dropdown: cx(classes.dropdown, classNames == null ? void 0 : classNames.dropdown) }),
    styles: styles2,
    unstyled,
    onClose: close,
    onOpen: open
  }), children));
}
Menu.displayName = "@mantine/core/Menu";
Menu.Item = MenuItem;
Menu.Label = MenuLabel;
Menu.Dropdown = MenuDropdown;
Menu.Target = MenuTarget;
Menu.Divider = MenuDivider;

// node_modules/@mantine/core/esm/Modal/Modal.js
var import_react216 = __toESM(require_react());

// node_modules/@mantine/core/esm/Modal/Modal.styles.js
var __defProp156 = Object.defineProperty;
var __getOwnPropSymbols158 = Object.getOwnPropertySymbols;
var __hasOwnProp158 = Object.prototype.hasOwnProperty;
var __propIsEnum158 = Object.prototype.propertyIsEnumerable;
var __defNormalProp156 = (obj, key, value) => key in obj ? __defProp156(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues156 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp158.call(b2, prop))
      __defNormalProp156(a2, prop, b2[prop]);
  if (__getOwnPropSymbols158)
    for (var prop of __getOwnPropSymbols158(b2)) {
      if (__propIsEnum158.call(b2, prop))
        __defNormalProp156(a2, prop, b2[prop]);
    }
  return a2;
};
var sizes15 = {
  xs: 320,
  sm: 380,
  md: 440,
  lg: 620,
  xl: 780
};
function getFullScreenStyles(fullScreen) {
  if (!fullScreen) {
    return {};
  }
  return {
    position: "absolute",
    top: 0,
    left: 0,
    right: 0,
    bottom: 0,
    maxHeight: "100vh",
    overflowY: "auto"
  };
}
var useStyles70 = createStyles((theme, { overflow, size: size3, centered, zIndex, fullScreen }) => ({
  close: {},
  overlay: {
    display: fullScreen ? "none" : void 0
  },
  root: {
    position: "fixed",
    zIndex,
    top: 0,
    left: 0,
    right: 0,
    bottom: 0
  },
  inner: {
    position: "absolute",
    top: 0,
    left: 0,
    right: 0,
    bottom: 0,
    overflowY: "auto",
    padding: fullScreen ? 0 : `${theme.spacing.xl * 2}px ${theme.spacing.md}px`,
    display: "flex",
    justifyContent: "center",
    alignItems: centered ? "center" : "flex-start"
  },
  title: {
    marginRight: theme.spacing.md,
    textOverflow: "ellipsis",
    display: "block",
    wordBreak: "break-word"
  },
  modal: __spreadValues156({
    position: "relative",
    width: fullScreen ? "100vw" : theme.fn.size({ sizes: sizes15, size: size3 }),
    borderRadius: fullScreen ? 0 : void 0,
    outline: 0,
    backgroundColor: theme.colorScheme === "dark" ? theme.colors.dark[7] : theme.white,
    marginTop: centered ? "auto" : void 0,
    marginBottom: centered ? "auto" : void 0,
    zIndex: 1
  }, getFullScreenStyles(fullScreen)),
  header: {
    display: "flex",
    alignItems: "center",
    justifyContent: "space-between",
    marginBottom: theme.spacing.md,
    marginRight: -9
  },
  body: {
    maxHeight: overflow === "inside" ? "calc(100vh - 185px)" : null,
    overflowY: overflow === "inside" ? "auto" : null,
    wordBreak: "break-word"
  }
}));
var Modal_styles_default = useStyles70;

// node_modules/@mantine/core/esm/Modal/Modal.js
var __defProp157 = Object.defineProperty;
var __getOwnPropSymbols159 = Object.getOwnPropertySymbols;
var __hasOwnProp159 = Object.prototype.hasOwnProperty;
var __propIsEnum159 = Object.prototype.propertyIsEnumerable;
var __defNormalProp157 = (obj, key, value) => key in obj ? __defProp157(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues157 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp159.call(b2, prop))
      __defNormalProp157(a2, prop, b2[prop]);
  if (__getOwnPropSymbols159)
    for (var prop of __getOwnPropSymbols159(b2)) {
      if (__propIsEnum159.call(b2, prop))
        __defNormalProp157(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest104 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp159.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols159)
    for (var prop of __getOwnPropSymbols159(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum159.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps80 = {
  size: "md",
  transitionDuration: 250,
  overflow: "outside",
  padding: "lg",
  shadow: "lg",
  closeOnClickOutside: true,
  closeOnEscape: true,
  trapFocus: true,
  withCloseButton: true,
  withinPortal: true,
  lockScroll: true,
  withFocusReturn: true,
  overlayBlur: 0,
  zIndex: getDefaultZIndex("modal"),
  exitTransitionDuration: 0
};
function Modal(props) {
  const _a = useComponentDefaultProps("Modal", defaultProps80, props), {
    className,
    opened,
    title,
    onClose,
    children,
    withCloseButton,
    overlayOpacity,
    size: size3,
    transitionDuration,
    exitTransitionDuration,
    closeButtonLabel,
    overlayColor,
    overflow,
    transition,
    padding: padding2,
    shadow,
    radius: radius2,
    id,
    classNames,
    styles: styles2,
    closeOnClickOutside,
    trapFocus,
    closeOnEscape: closeOnEscape2,
    centered,
    target,
    withinPortal,
    zIndex,
    overlayBlur,
    transitionTimingFunction,
    fullScreen,
    unstyled,
    lockScroll: shouldLockScroll,
    withFocusReturn
  } = _a, others = __objRest104(_a, [
    "className",
    "opened",
    "title",
    "onClose",
    "children",
    "withCloseButton",
    "overlayOpacity",
    "size",
    "transitionDuration",
    "exitTransitionDuration",
    "closeButtonLabel",
    "overlayColor",
    "overflow",
    "transition",
    "padding",
    "shadow",
    "radius",
    "id",
    "classNames",
    "styles",
    "closeOnClickOutside",
    "trapFocus",
    "closeOnEscape",
    "centered",
    "target",
    "withinPortal",
    "zIndex",
    "overlayBlur",
    "transitionTimingFunction",
    "fullScreen",
    "unstyled",
    "lockScroll",
    "withFocusReturn"
  ]);
  const baseId = useId(id);
  const titleId = `${baseId}-title`;
  const bodyId = `${baseId}-body`;
  const { classes, cx, theme } = Modal_styles_default({ size: size3, overflow, centered, zIndex, fullScreen }, { unstyled, classNames, styles: styles2, name: "Modal" });
  const focusTrapRef = useFocusTrap(trapFocus && opened);
  const overlayRef = (0, import_react216.useRef)(null);
  const mergedRef = useMergedRef(focusTrapRef, overlayRef);
  const _overlayOpacity = typeof overlayOpacity === "number" ? overlayOpacity : theme.colorScheme === "dark" ? 0.85 : 0.75;
  useScrollLock(shouldLockScroll && opened);
  const closeOnEscapePress = (event) => {
    if (!trapFocus && event.key === "Escape" && closeOnEscape2) {
      onClose();
    }
  };
  (0, import_react216.useEffect)(() => {
    if (!trapFocus) {
      window.addEventListener("keydown", closeOnEscapePress);
      return () => window.removeEventListener("keydown", closeOnEscapePress);
    }
    return void 0;
  }, [trapFocus]);
  useFocusReturn({ opened, shouldReturnFocus: trapFocus && withFocusReturn });
  const clickTarget = (0, import_react216.useRef)(null);
  useWindowEvent("mousedown", (e) => {
    clickTarget.current = e.target;
  });
  const handleOutsideClick = () => {
    if (clickTarget.current === overlayRef.current) {
      closeOnClickOutside && onClose();
    }
  };
  return import_react216.default.createElement(OptionalPortal, {
    withinPortal,
    target
  }, import_react216.default.createElement(GroupedTransition, {
    mounted: opened,
    duration: transitionDuration,
    exitDuration: exitTransitionDuration,
    timingFunction: transitionTimingFunction,
    transitions: {
      modal: {
        duration: transitionDuration,
        transition: transition || (fullScreen ? "fade" : "pop")
      },
      overlay: {
        duration: transitionDuration / 2,
        transition: "fade",
        timingFunction: "ease"
      }
    }
  }, (transitionStyles) => import_react216.default.createElement(import_react216.default.Fragment, null, import_react216.default.createElement(Box, __spreadValues157({
    id: baseId,
    className: cx(classes.root, className)
  }, others), import_react216.default.createElement("div", {
    style: transitionStyles.overlay
  }, import_react216.default.createElement(Overlay, {
    className: classes.overlay,
    sx: { position: "fixed" },
    zIndex: 0,
    blur: overlayBlur,
    color: overlayColor || (theme.colorScheme === "dark" ? theme.colors.dark[9] : theme.black),
    opacity: _overlayOpacity,
    unstyled
  })), import_react216.default.createElement("div", {
    role: "presentation",
    className: classes.inner,
    onClick: handleOutsideClick,
    onKeyDown: (event) => {
      var _a2;
      const shouldTrigger = ((_a2 = event.target) == null ? void 0 : _a2.getAttribute("data-mantine-stop-propagation")) !== "true";
      shouldTrigger && event.key === "Escape" && closeOnEscape2 && onClose();
    },
    ref: mergedRef
  }, import_react216.default.createElement(Paper, {
    className: classes.modal,
    shadow,
    p: padding2,
    radius: radius2,
    role: "dialog",
    "aria-labelledby": titleId,
    "aria-describedby": bodyId,
    "aria-modal": true,
    tabIndex: -1,
    style: transitionStyles.modal,
    unstyled,
    onClick: (event) => event.stopPropagation()
  }, (title || withCloseButton) && import_react216.default.createElement("div", {
    className: classes.header
  }, import_react216.default.createElement(Text, {
    id: titleId,
    className: classes.title
  }, title), withCloseButton && import_react216.default.createElement(CloseButton, {
    iconSize: 16,
    onClick: onClose,
    "aria-label": closeButtonLabel,
    className: classes.close
  })), import_react216.default.createElement("div", {
    id: bodyId,
    className: classes.body
  }, children)))))));
}
Modal.displayName = "@mantine/core/Modal";

// node_modules/@mantine/core/esm/MultiSelect/MultiSelect.js
var import_react221 = __toESM(require_react());

// node_modules/@mantine/core/esm/MultiSelect/DefaultValue/DefaultValue.js
var import_react217 = __toESM(require_react());

// node_modules/@mantine/core/esm/MultiSelect/DefaultValue/DefaultValue.styles.js
var sizes16 = {
  xs: 16,
  sm: 22,
  md: 26,
  lg: 30,
  xl: 36
};
var fontSizes = {
  xs: 10,
  sm: 12,
  md: 14,
  lg: 16,
  xl: 18
};
var useStyles71 = createStyles((theme, { size: size3, disabled, radius: radius2, readOnly, variant: variant2 }) => ({
  defaultValue: {
    display: "flex",
    alignItems: "center",
    backgroundColor: disabled ? theme.colorScheme === "dark" ? theme.colors.dark[5] : theme.colors.gray[3] : theme.colorScheme === "dark" ? theme.colors.dark[7] : variant2 === "filled" ? theme.white : theme.colors.gray[1],
    color: disabled ? theme.colorScheme === "dark" ? theme.colors.dark[1] : theme.colors.gray[7] : theme.colorScheme === "dark" ? theme.colors.dark[0] : theme.colors.gray[7],
    height: theme.fn.size({ size: size3, sizes: sizes16 }),
    paddingLeft: theme.fn.size({ size: size3, sizes: theme.spacing }),
    paddingRight: disabled || readOnly ? theme.fn.size({ size: size3, sizes: theme.spacing }) : 0,
    fontWeight: 500,
    fontSize: theme.fn.size({ size: size3, sizes: fontSizes }),
    borderRadius: theme.fn.size({ size: radius2, sizes: theme.radius }),
    cursor: disabled ? "not-allowed" : "default",
    userSelect: "none",
    maxWidth: "calc(100% - 20px)"
  },
  defaultValueRemove: {
    color: theme.colorScheme === "dark" ? theme.colors.dark[0] : theme.colors.gray[7],
    marginLeft: theme.fn.size({ size: size3, sizes: theme.spacing }) / 6
  },
  defaultValueLabel: {
    display: "block",
    overflow: "hidden",
    textOverflow: "ellipsis",
    whiteSpace: "nowrap"
  }
}));
var DefaultValue_styles_default = useStyles71;

// node_modules/@mantine/core/esm/MultiSelect/DefaultValue/DefaultValue.js
var __defProp158 = Object.defineProperty;
var __getOwnPropSymbols160 = Object.getOwnPropertySymbols;
var __hasOwnProp160 = Object.prototype.hasOwnProperty;
var __propIsEnum160 = Object.prototype.propertyIsEnumerable;
var __defNormalProp158 = (obj, key, value) => key in obj ? __defProp158(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues158 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp160.call(b2, prop))
      __defNormalProp158(a2, prop, b2[prop]);
  if (__getOwnPropSymbols160)
    for (var prop of __getOwnPropSymbols160(b2)) {
      if (__propIsEnum160.call(b2, prop))
        __defNormalProp158(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest105 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp160.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols160)
    for (var prop of __getOwnPropSymbols160(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum160.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var buttonSizes = {
  xs: 16,
  sm: 22,
  md: 24,
  lg: 26,
  xl: 30
};
function DefaultValue2(_a) {
  var _b = _a, {
    label,
    classNames,
    styles: styles2,
    className,
    onRemove,
    disabled,
    readOnly,
    size: size3,
    radius: radius2 = "sm",
    variant: variant2
  } = _b, others = __objRest105(_b, [
    "label",
    "classNames",
    "styles",
    "className",
    "onRemove",
    "disabled",
    "readOnly",
    "size",
    "radius",
    "variant"
  ]);
  const { classes, cx } = DefaultValue_styles_default({ size: size3, disabled, readOnly, radius: radius2, variant: variant2 }, { classNames, styles: styles2, name: "MultiSelect" });
  return import_react217.default.createElement("div", __spreadValues158({
    className: cx(classes.defaultValue, className)
  }, others), import_react217.default.createElement("span", {
    className: classes.defaultValueLabel
  }, label), !disabled && !readOnly && import_react217.default.createElement(CloseButton, {
    "aria-hidden": true,
    onMouseDown: onRemove,
    size: buttonSizes[size3],
    radius: 2,
    color: "blue",
    variant: "transparent",
    iconSize: buttonSizes[size3] / 2,
    className: classes.defaultValueRemove,
    tabIndex: -1
  }));
}
DefaultValue2.displayName = "@mantine/core/MultiSelect/DefaultValue";

// node_modules/@mantine/core/esm/MultiSelect/filter-data/filter-data.js
function filterData2({
  data,
  searchable,
  limit,
  searchValue,
  filter,
  value,
  disableSelectedItemFiltering
}) {
  if (!searchable && value.length === 0) {
    return data;
  }
  if (!searchable) {
    const result2 = [];
    for (let i = 0; i < data.length; i += 1) {
      if (!!disableSelectedItemFiltering || !value.some((val) => val === data[i].value && !data[i].disabled)) {
        result2.push(data[i]);
      }
    }
    return result2;
  }
  const result = [];
  for (let i = 0; i < data.length; i += 1) {
    if (filter(searchValue, value.some((val) => val === data[i].value && !data[i].disabled), data[i])) {
      result.push(data[i]);
    }
    if (result.length >= limit) {
      break;
    }
  }
  return result;
}

// node_modules/@mantine/core/esm/Select/SelectRightSection/get-select-right-section-props.js
var import_react220 = __toESM(require_react());

// node_modules/@mantine/core/esm/Select/SelectRightSection/SelectRightSection.js
var import_react219 = __toESM(require_react());

// node_modules/@mantine/core/esm/Select/SelectRightSection/ChevronIcon.js
var import_react218 = __toESM(require_react());
var __defProp159 = Object.defineProperty;
var __getOwnPropSymbols161 = Object.getOwnPropertySymbols;
var __hasOwnProp161 = Object.prototype.hasOwnProperty;
var __propIsEnum161 = Object.prototype.propertyIsEnumerable;
var __defNormalProp159 = (obj, key, value) => key in obj ? __defProp159(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues159 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp161.call(b2, prop))
      __defNormalProp159(a2, prop, b2[prop]);
  if (__getOwnPropSymbols161)
    for (var prop of __getOwnPropSymbols161(b2)) {
      if (__propIsEnum161.call(b2, prop))
        __defNormalProp159(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest106 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp161.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols161)
    for (var prop of __getOwnPropSymbols161(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum161.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var iconSizes4 = {
  xs: 14,
  sm: 18,
  md: 20,
  lg: 24,
  xl: 28
};
function ChevronIcon2(_a) {
  var _b = _a, { size: size3, error, style } = _b, others = __objRest106(_b, ["size", "error", "style"]);
  const theme = useMantineTheme();
  const _size = theme.fn.size({ size: size3, sizes: iconSizes4 });
  return import_react218.default.createElement("svg", __spreadValues159({
    width: _size,
    height: _size,
    viewBox: "0 0 15 15",
    fill: "none",
    xmlns: "http://www.w3.org/2000/svg",
    style: __spreadValues159({ color: error ? theme.colors.red[6] : theme.colors.gray[6] }, style),
    "data-chevron": true
  }, others), import_react218.default.createElement("path", {
    d: "M4.93179 5.43179C4.75605 5.60753 4.75605 5.89245 4.93179 6.06819C5.10753 6.24392 5.39245 6.24392 5.56819 6.06819L7.49999 4.13638L9.43179 6.06819C9.60753 6.24392 9.89245 6.24392 10.0682 6.06819C10.2439 5.89245 10.2439 5.60753 10.0682 5.43179L7.81819 3.18179C7.73379 3.0974 7.61933 3.04999 7.49999 3.04999C7.38064 3.04999 7.26618 3.0974 7.18179 3.18179L4.93179 5.43179ZM10.0682 9.56819C10.2439 9.39245 10.2439 9.10753 10.0682 8.93179C9.89245 8.75606 9.60753 8.75606 9.43179 8.93179L7.49999 10.8636L5.56819 8.93179C5.39245 8.75606 5.10753 8.75606 4.93179 8.93179C4.75605 9.10753 4.75605 9.39245 4.93179 9.56819L7.18179 11.8182C7.35753 11.9939 7.64245 11.9939 7.81819 11.8182L10.0682 9.56819Z",
    fill: "currentColor",
    fillRule: "evenodd",
    clipRule: "evenodd"
  }));
}

// node_modules/@mantine/core/esm/Select/SelectRightSection/SelectRightSection.js
function SelectRightSection({
  shouldClear,
  clearButtonLabel,
  onClear,
  size: size3,
  error,
  clearButtonTabIndex
}) {
  return shouldClear ? import_react219.default.createElement(CloseButton, {
    variant: "transparent",
    "aria-label": clearButtonLabel,
    onClick: onClear,
    size: size3,
    tabIndex: clearButtonTabIndex,
    onMouseDown: (event) => event.preventDefault()
  }) : import_react219.default.createElement(ChevronIcon2, {
    error,
    size: size3
  });
}
SelectRightSection.displayName = "@mantine/core/SelectRightSection";

// node_modules/@mantine/core/esm/Select/SelectRightSection/get-select-right-section-props.js
var __defProp160 = Object.defineProperty;
var __defProps67 = Object.defineProperties;
var __getOwnPropDescs67 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols162 = Object.getOwnPropertySymbols;
var __hasOwnProp162 = Object.prototype.hasOwnProperty;
var __propIsEnum162 = Object.prototype.propertyIsEnumerable;
var __defNormalProp160 = (obj, key, value) => key in obj ? __defProp160(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues160 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp162.call(b2, prop))
      __defNormalProp160(a2, prop, b2[prop]);
  if (__getOwnPropSymbols162)
    for (var prop of __getOwnPropSymbols162(b2)) {
      if (__propIsEnum162.call(b2, prop))
        __defNormalProp160(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps67 = (a2, b2) => __defProps67(a2, __getOwnPropDescs67(b2));
var __objRest107 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp162.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols162)
    for (var prop of __getOwnPropSymbols162(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum162.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var RIGHT_SECTION_WIDTH3 = {
  xs: 24,
  sm: 30,
  md: 34,
  lg: 44,
  xl: 54
};
function getSelectRightSectionProps(_a) {
  var _b = _a, {
    styles: styles2,
    rightSection,
    rightSectionWidth,
    theme
  } = _b, props = __objRest107(_b, [
    "styles",
    "rightSection",
    "rightSectionWidth",
    "theme"
  ]);
  if (rightSection) {
    return { rightSection, rightSectionWidth, styles: styles2 };
  }
  const _styles = typeof styles2 === "function" ? styles2(theme) : styles2;
  return {
    rightSectionWidth: theme.fn.size({ size: props.size, sizes: RIGHT_SECTION_WIDTH3 }),
    rightSection: !props.readOnly && !(props.disabled && props.shouldClear) && import_react220.default.createElement(SelectRightSection, __spreadValues160({}, props)),
    styles: __spreadProps67(__spreadValues160({}, _styles), {
      rightSection: __spreadProps67(__spreadValues160({}, _styles == null ? void 0 : _styles.rightSection), {
        pointerEvents: props.shouldClear ? void 0 : "none"
      })
    })
  };
}

// node_modules/@mantine/core/esm/MultiSelect/MultiSelect.styles.js
var __defProp161 = Object.defineProperty;
var __defProps68 = Object.defineProperties;
var __getOwnPropDescs68 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols163 = Object.getOwnPropertySymbols;
var __hasOwnProp163 = Object.prototype.hasOwnProperty;
var __propIsEnum163 = Object.prototype.propertyIsEnumerable;
var __defNormalProp161 = (obj, key, value) => key in obj ? __defProp161(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues161 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp163.call(b2, prop))
      __defNormalProp161(a2, prop, b2[prop]);
  if (__getOwnPropSymbols163)
    for (var prop of __getOwnPropSymbols163(b2)) {
      if (__propIsEnum163.call(b2, prop))
        __defNormalProp161(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps68 = (a2, b2) => __defProps68(a2, __getOwnPropDescs68(b2));
var useStyles72 = createStyles((theme, { size: size3, invalid }) => ({
  wrapper: {
    position: "relative"
  },
  values: {
    minHeight: theme.fn.size({ size: size3, sizes: sizes4 }) - 2,
    display: "flex",
    alignItems: "center",
    flexWrap: "wrap",
    marginLeft: `calc(${-theme.spacing.xs}px / 2)`,
    boxSizing: "border-box"
  },
  value: {
    margin: `calc(${theme.spacing.xs}px / 2 - 2px) calc(${theme.spacing.xs}px / 2)`
  },
  searchInput: __spreadProps68(__spreadValues161({}, theme.fn.fontStyles()), {
    flex: 1,
    minWidth: 60,
    backgroundColor: "transparent",
    border: 0,
    outline: 0,
    fontSize: theme.fn.size({ size: size3, sizes: theme.fontSizes }),
    padding: 0,
    marginLeft: `calc(${theme.spacing.xs}px / 2)`,
    appearance: "none",
    color: "inherit",
    lineHeight: `${theme.fn.size({ size: size3, sizes: sizes4 }) - 2}px`,
    "&::placeholder": {
      opacity: 1,
      color: invalid ? theme.colors.red[theme.colorScheme === "dark" ? 6 : 7] : theme.colorScheme === "dark" ? theme.colors.dark[3] : theme.colors.gray[5]
    },
    "&:disabled": {
      cursor: "not-allowed"
    }
  }),
  searchInputEmpty: {
    width: "100%"
  },
  searchInputInputHidden: {
    width: 0,
    height: 0,
    margin: 0,
    overflow: "hidden"
  },
  searchInputPointer: {
    cursor: "pointer",
    "&:disabled": {
      cursor: "not-allowed"
    }
  },
  input: {
    cursor: "pointer",
    "&:disabled": {
      cursor: "not-allowed"
    }
  }
}));
var MultiSelect_styles_default = useStyles72;

// node_modules/@mantine/core/esm/MultiSelect/MultiSelect.js
var __defProp162 = Object.defineProperty;
var __defProps69 = Object.defineProperties;
var __getOwnPropDescs69 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols164 = Object.getOwnPropertySymbols;
var __hasOwnProp164 = Object.prototype.hasOwnProperty;
var __propIsEnum164 = Object.prototype.propertyIsEnumerable;
var __defNormalProp162 = (obj, key, value) => key in obj ? __defProp162(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues162 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp164.call(b2, prop))
      __defNormalProp162(a2, prop, b2[prop]);
  if (__getOwnPropSymbols164)
    for (var prop of __getOwnPropSymbols164(b2)) {
      if (__propIsEnum164.call(b2, prop))
        __defNormalProp162(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps69 = (a2, b2) => __defProps69(a2, __getOwnPropDescs69(b2));
var __objRest108 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp164.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols164)
    for (var prop of __getOwnPropSymbols164(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum164.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
function defaultFilter2(value, selected, item) {
  if (selected) {
    return false;
  }
  return item.label.toLowerCase().trim().includes(value.toLowerCase().trim());
}
function defaultShouldCreate(query, data) {
  return !!query && !data.some((item) => item.value.toLowerCase() === query.toLowerCase());
}
function filterValue(value, data) {
  if (!Array.isArray(value)) {
    return void 0;
  }
  if (data.length === 0) {
    return [];
  }
  const flatData = data.map((item) => {
    if (typeof item === "object") {
      return item.value;
    }
    return item;
  });
  return value.filter((val) => flatData.includes(val));
}
var defaultProps81 = {
  size: "sm",
  valueComponent: DefaultValue2,
  itemComponent: DefaultItem,
  transition: "pop-top-left",
  transitionDuration: 0,
  maxDropdownHeight: 220,
  shadow: "sm",
  searchable: false,
  filter: defaultFilter2,
  limit: Infinity,
  clearSearchOnChange: true,
  clearable: false,
  clearSearchOnBlur: false,
  disabled: false,
  initiallyOpened: false,
  creatable: false,
  shouldCreate: defaultShouldCreate,
  switchDirectionOnFlip: false,
  zIndex: getDefaultZIndex("popover"),
  selectOnBlur: false,
  clearButtonTabIndex: 0,
  positionDependencies: [],
  dropdownPosition: "flip"
};
var MultiSelect = (0, import_react221.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("MultiSelect", defaultProps81, props), {
    className,
    style,
    required,
    label,
    description,
    size: size3,
    error,
    classNames,
    styles: styles2,
    wrapperProps,
    value,
    defaultValue,
    data,
    onChange,
    valueComponent: Value,
    itemComponent,
    id,
    transition,
    transitionDuration,
    transitionTimingFunction,
    maxDropdownHeight,
    shadow,
    nothingFound,
    onFocus,
    onBlur,
    searchable,
    placeholder,
    filter,
    limit,
    clearSearchOnChange,
    clearable,
    clearSearchOnBlur,
    clearButtonLabel,
    variant: variant2,
    onSearchChange,
    searchValue,
    disabled,
    initiallyOpened,
    radius: radius2,
    icon,
    rightSection,
    rightSectionWidth,
    creatable,
    getCreateLabel,
    shouldCreate,
    onCreate: onCreate2,
    sx,
    dropdownComponent,
    onDropdownClose,
    onDropdownOpen,
    maxSelectedValues,
    withinPortal,
    switchDirectionOnFlip,
    zIndex,
    selectOnBlur,
    name,
    dropdownPosition,
    errorProps,
    labelProps,
    descriptionProps,
    clearButtonTabIndex,
    form,
    positionDependencies,
    onKeyDown,
    unstyled,
    inputContainer,
    inputWrapperOrder,
    readOnly,
    withAsterisk,
    hoverOnSearchChange,
    disableSelectedItemFiltering
  } = _a, others = __objRest108(_a, [
    "className",
    "style",
    "required",
    "label",
    "description",
    "size",
    "error",
    "classNames",
    "styles",
    "wrapperProps",
    "value",
    "defaultValue",
    "data",
    "onChange",
    "valueComponent",
    "itemComponent",
    "id",
    "transition",
    "transitionDuration",
    "transitionTimingFunction",
    "maxDropdownHeight",
    "shadow",
    "nothingFound",
    "onFocus",
    "onBlur",
    "searchable",
    "placeholder",
    "filter",
    "limit",
    "clearSearchOnChange",
    "clearable",
    "clearSearchOnBlur",
    "clearButtonLabel",
    "variant",
    "onSearchChange",
    "searchValue",
    "disabled",
    "initiallyOpened",
    "radius",
    "icon",
    "rightSection",
    "rightSectionWidth",
    "creatable",
    "getCreateLabel",
    "shouldCreate",
    "onCreate",
    "sx",
    "dropdownComponent",
    "onDropdownClose",
    "onDropdownOpen",
    "maxSelectedValues",
    "withinPortal",
    "switchDirectionOnFlip",
    "zIndex",
    "selectOnBlur",
    "name",
    "dropdownPosition",
    "errorProps",
    "labelProps",
    "descriptionProps",
    "clearButtonTabIndex",
    "form",
    "positionDependencies",
    "onKeyDown",
    "unstyled",
    "inputContainer",
    "inputWrapperOrder",
    "readOnly",
    "withAsterisk",
    "hoverOnSearchChange",
    "disableSelectedItemFiltering"
  ]);
  const { classes, cx, theme } = MultiSelect_styles_default({ size: size3, invalid: !!error }, { classNames, styles: styles2, unstyled, name: "MultiSelect" });
  const { systemStyles, rest } = extractSystemStyles(others);
  const inputRef = (0, import_react221.useRef)();
  const itemsRefs = (0, import_react221.useRef)({});
  const uuid = useId(id);
  const [dropdownOpened, setDropdownOpened] = (0, import_react221.useState)(initiallyOpened);
  const [hovered, setHovered] = (0, import_react221.useState)(-1);
  const [direction, setDirection] = (0, import_react221.useState)("column");
  const [_searchValue, handleSearchChange] = useUncontrolled({
    value: searchValue,
    defaultValue: "",
    finalValue: void 0,
    onChange: onSearchChange
  });
  const [IMEOpen, setIMEOpen] = (0, import_react221.useState)(false);
  const { scrollIntoView, targetRef, scrollableRef } = useScrollIntoView({
    duration: 0,
    offset: 5,
    cancelable: false,
    isList: true
  });
  const isCreatable = creatable && typeof getCreateLabel === "function";
  let createLabel = null;
  const formattedData = data.map((item) => typeof item === "string" ? { label: item, value: item } : item);
  const sortedData = groupOptions({ data: formattedData });
  const [_value, setValue] = useUncontrolled({
    value: filterValue(value, data),
    defaultValue: filterValue(defaultValue, data),
    finalValue: [],
    onChange
  });
  const valuesOverflow = (0, import_react221.useRef)(!!maxSelectedValues && maxSelectedValues < _value.length);
  const handleValueRemove = (_val) => {
    if (!readOnly) {
      const newValue = _value.filter((val) => val !== _val);
      setValue(newValue);
      if (!!maxSelectedValues && newValue.length < maxSelectedValues) {
        valuesOverflow.current = false;
      }
    }
  };
  const handleInputChange = (event) => {
    handleSearchChange(event.currentTarget.value);
    !disabled && !valuesOverflow.current && searchable && setDropdownOpened(true);
  };
  const handleInputFocus = (event) => {
    typeof onFocus === "function" && onFocus(event);
    !disabled && !valuesOverflow.current && searchable && setDropdownOpened(true);
  };
  const filteredData = filterData2({
    data: sortedData,
    searchable,
    searchValue: _searchValue,
    limit,
    filter,
    value: _value,
    disableSelectedItemFiltering
  });
  const getNextIndex2 = (index6, nextItem, compareFn) => {
    let i = index6;
    while (compareFn(i)) {
      i = nextItem(i);
      if (!filteredData[i].disabled)
        return i;
    }
    return index6;
  };
  useDidUpdate(() => {
    if (hoverOnSearchChange && _searchValue) {
      setHovered(0);
    } else {
      setHovered(-1);
    }
  }, [_searchValue, hoverOnSearchChange]);
  useDidUpdate(() => {
    if (!disabled && _value.length > data.length) {
      setDropdownOpened(false);
    }
    if (!!maxSelectedValues && _value.length < maxSelectedValues) {
      valuesOverflow.current = false;
    }
    if (!!maxSelectedValues && _value.length >= maxSelectedValues) {
      valuesOverflow.current = true;
      setDropdownOpened(false);
    }
  }, [_value]);
  const handleItemSelect = (item) => {
    if (!readOnly) {
      clearSearchOnChange && handleSearchChange("");
      if (_value.includes(item.value)) {
        handleValueRemove(item.value);
      } else {
        if (item.creatable && typeof onCreate2 === "function") {
          const createdItem = onCreate2(item.value);
          if (typeof createdItem !== "undefined" && createdItem !== null) {
            if (typeof createdItem === "string") {
              setValue([..._value, createdItem]);
            } else {
              setValue([..._value, createdItem.value]);
            }
          }
        } else {
          setValue([..._value, item.value]);
        }
        if (_value.length === maxSelectedValues - 1) {
          valuesOverflow.current = true;
          setDropdownOpened(false);
        }
        if (hovered === filteredData.length - 1) {
          setHovered(filteredData.length - 2);
        }
        if (filteredData.length === 1) {
          setDropdownOpened(false);
        }
      }
    }
  };
  const handleInputBlur = (event) => {
    typeof onBlur === "function" && onBlur(event);
    if (selectOnBlur && filteredData[hovered] && dropdownOpened) {
      handleItemSelect(filteredData[hovered]);
    }
    clearSearchOnBlur && handleSearchChange("");
    setDropdownOpened(false);
  };
  const handleInputKeydown = (event) => {
    if (IMEOpen) {
      return;
    }
    onKeyDown == null ? void 0 : onKeyDown(event);
    if (readOnly) {
      return;
    }
    if (event.key !== "Backspace" && !!maxSelectedValues && valuesOverflow.current) {
      return;
    }
    const isColumn = direction === "column";
    const handleNext = () => {
      setHovered((current) => {
        var _a2;
        const nextIndex = getNextIndex2(current, (index6) => index6 + 1, (index6) => index6 < filteredData.length - 1);
        if (dropdownOpened) {
          targetRef.current = itemsRefs.current[(_a2 = filteredData[nextIndex]) == null ? void 0 : _a2.value];
          scrollIntoView({
            alignment: isColumn ? "end" : "start"
          });
        }
        return nextIndex;
      });
    };
    const handlePrevious = () => {
      setHovered((current) => {
        var _a2;
        const nextIndex = getNextIndex2(current, (index6) => index6 - 1, (index6) => index6 > 0);
        if (dropdownOpened) {
          targetRef.current = itemsRefs.current[(_a2 = filteredData[nextIndex]) == null ? void 0 : _a2.value];
          scrollIntoView({
            alignment: isColumn ? "start" : "end"
          });
        }
        return nextIndex;
      });
    };
    switch (event.key) {
      case "ArrowUp": {
        event.preventDefault();
        setDropdownOpened(true);
        isColumn ? handlePrevious() : handleNext();
        break;
      }
      case "ArrowDown": {
        event.preventDefault();
        setDropdownOpened(true);
        isColumn ? handleNext() : handlePrevious();
        break;
      }
      case "Enter": {
        event.preventDefault();
        if (filteredData[hovered] && dropdownOpened) {
          handleItemSelect(filteredData[hovered]);
        } else {
          setDropdownOpened(true);
        }
        break;
      }
      case " ": {
        if (!searchable) {
          event.preventDefault();
          if (filteredData[hovered] && dropdownOpened) {
            handleItemSelect(filteredData[hovered]);
          } else {
            setDropdownOpened(true);
          }
        }
        break;
      }
      case "Backspace": {
        if (_value.length > 0 && _searchValue.length === 0) {
          setValue(_value.slice(0, -1));
          setDropdownOpened(true);
          if (maxSelectedValues) {
            valuesOverflow.current = false;
          }
        }
        break;
      }
      case "Home": {
        if (!searchable) {
          event.preventDefault();
          if (!dropdownOpened) {
            setDropdownOpened(true);
          }
          const firstItemIndex = filteredData.findIndex((item) => !item.disabled);
          setHovered(firstItemIndex);
          scrollIntoView({
            alignment: isColumn ? "end" : "start"
          });
        }
        break;
      }
      case "End": {
        if (!searchable) {
          event.preventDefault();
          if (!dropdownOpened) {
            setDropdownOpened(true);
          }
          const lastItemIndex = filteredData.map((item) => !!item.disabled).lastIndexOf(false);
          setHovered(lastItemIndex);
          scrollIntoView({
            alignment: isColumn ? "end" : "start"
          });
        }
        break;
      }
      case "Escape": {
        setDropdownOpened(false);
      }
    }
  };
  const selectedItems = _value.map((val) => {
    let selectedItem = sortedData.find((item) => item.value === val && !item.disabled);
    if (!selectedItem && isCreatable) {
      selectedItem = {
        value: val,
        label: val
      };
    }
    return selectedItem;
  }).filter((val) => !!val).map((item) => import_react221.default.createElement(Value, __spreadProps69(__spreadValues162({}, item), {
    variant: variant2,
    disabled,
    className: classes.value,
    readOnly,
    onRemove: (event) => {
      event.preventDefault();
      event.stopPropagation();
      handleValueRemove(item.value);
    },
    key: item.value,
    size: size3,
    styles: styles2,
    classNames,
    radius: radius2
  })));
  const isItemSelected = (itemValue) => _value.includes(itemValue);
  const handleClear = () => {
    var _a2;
    handleSearchChange("");
    setValue([]);
    (_a2 = inputRef.current) == null ? void 0 : _a2.focus();
    if (maxSelectedValues) {
      valuesOverflow.current = false;
    }
  };
  if (isCreatable && shouldCreate(_searchValue, sortedData)) {
    createLabel = getCreateLabel(_searchValue);
    filteredData.push({ label: _searchValue, value: _searchValue, creatable: true });
  }
  const shouldRenderDropdown = !readOnly && (filteredData.length > 0 ? dropdownOpened : dropdownOpened && !!nothingFound);
  useDidUpdate(() => {
    const handler = shouldRenderDropdown ? onDropdownOpen : onDropdownClose;
    typeof handler === "function" && handler();
  }, [shouldRenderDropdown]);
  return import_react221.default.createElement(Input.Wrapper, __spreadValues162(__spreadValues162({
    required,
    id: uuid,
    label,
    error,
    description,
    size: size3,
    className,
    style,
    classNames,
    styles: styles2,
    __staticSelector: "MultiSelect",
    sx,
    errorProps,
    descriptionProps,
    labelProps,
    inputContainer,
    inputWrapperOrder,
    unstyled,
    withAsterisk
  }, systemStyles), wrapperProps), import_react221.default.createElement(SelectPopover, {
    opened: shouldRenderDropdown,
    transition,
    transitionDuration,
    shadow: "sm",
    withinPortal,
    __staticSelector: "MultiSelect",
    onDirectionChange: setDirection,
    switchDirectionOnFlip,
    zIndex,
    dropdownPosition,
    positionDependencies: [...positionDependencies, _searchValue],
    classNames,
    styles: styles2,
    unstyled
  }, import_react221.default.createElement(SelectPopover.Target, null, import_react221.default.createElement("div", {
    className: classes.wrapper,
    role: "combobox",
    "aria-haspopup": "listbox",
    "aria-owns": dropdownOpened && shouldRenderDropdown ? `${uuid}-items` : null,
    "aria-controls": uuid,
    "aria-expanded": dropdownOpened,
    onMouseLeave: () => setHovered(-1),
    tabIndex: -1
  }, import_react221.default.createElement("input", {
    type: "hidden",
    name,
    value: _value.join(","),
    form,
    disabled
  }), import_react221.default.createElement(Input, __spreadValues162({
    __staticSelector: "MultiSelect",
    style: { overflow: "hidden" },
    component: "div",
    multiline: true,
    size: size3,
    variant: variant2,
    disabled,
    invalid: !!error,
    required,
    radius: radius2,
    icon,
    unstyled,
    onMouseDown: (event) => {
      var _a2;
      event.preventDefault();
      !disabled && !valuesOverflow.current && setDropdownOpened(!dropdownOpened);
      (_a2 = inputRef.current) == null ? void 0 : _a2.focus();
    },
    classNames: __spreadProps69(__spreadValues162({}, classNames), {
      input: cx({ [classes.input]: !searchable }, classNames == null ? void 0 : classNames.input)
    })
  }, getSelectRightSectionProps({
    theme,
    rightSection,
    rightSectionWidth,
    styles: styles2,
    size: size3,
    shouldClear: clearable && _value.length > 0,
    clearButtonLabel,
    onClear: handleClear,
    error,
    disabled,
    clearButtonTabIndex,
    readOnly
  })), import_react221.default.createElement("div", {
    className: classes.values
  }, selectedItems, import_react221.default.createElement("input", __spreadValues162({
    ref: useMergedRef(ref, inputRef),
    type: "search",
    id: uuid,
    className: cx(classes.searchInput, {
      [classes.searchInputPointer]: !searchable,
      [classes.searchInputInputHidden]: !dropdownOpened && _value.length > 0 || !searchable && _value.length > 0,
      [classes.searchInputEmpty]: _value.length === 0
    }),
    onKeyDown: handleInputKeydown,
    value: _searchValue,
    onChange: handleInputChange,
    onFocus: handleInputFocus,
    onBlur: handleInputBlur,
    readOnly: !searchable || valuesOverflow.current || readOnly,
    placeholder: _value.length === 0 ? placeholder : void 0,
    disabled,
    "data-mantine-stop-propagation": dropdownOpened,
    autoComplete: "off",
    onCompositionStart: () => setIMEOpen(true),
    onCompositionEnd: () => setIMEOpen(false)
  }, rest)))))), import_react221.default.createElement(SelectPopover.Dropdown, {
    component: dropdownComponent || SelectScrollArea,
    maxHeight: maxDropdownHeight,
    direction,
    id: uuid,
    innerRef: scrollableRef,
    __staticSelector: "MultiSelect",
    classNames,
    styles: styles2
  }, import_react221.default.createElement(SelectItems, {
    data: filteredData,
    hovered,
    classNames,
    styles: styles2,
    uuid,
    __staticSelector: "MultiSelect",
    onItemHover: setHovered,
    onItemSelect: handleItemSelect,
    itemsRefs,
    itemComponent,
    size: size3,
    nothingFound,
    isItemSelected,
    creatable: creatable && !!createLabel,
    createLabel,
    unstyled
  }))));
});
MultiSelect.displayName = "@mantine/core/MultiSelect";

// node_modules/@mantine/core/esm/NativeSelect/NativeSelect.js
var import_react222 = __toESM(require_react());
var __defProp163 = Object.defineProperty;
var __defProps70 = Object.defineProperties;
var __getOwnPropDescs70 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols165 = Object.getOwnPropertySymbols;
var __hasOwnProp165 = Object.prototype.hasOwnProperty;
var __propIsEnum165 = Object.prototype.propertyIsEnumerable;
var __defNormalProp163 = (obj, key, value) => key in obj ? __defProp163(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues163 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp165.call(b2, prop))
      __defNormalProp163(a2, prop, b2[prop]);
  if (__getOwnPropSymbols165)
    for (var prop of __getOwnPropSymbols165(b2)) {
      if (__propIsEnum165.call(b2, prop))
        __defNormalProp163(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps70 = (a2, b2) => __defProps70(a2, __getOwnPropDescs70(b2));
var __objRest109 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp165.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols165)
    for (var prop of __getOwnPropSymbols165(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum165.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps82 = {
  size: "sm"
};
var NativeSelect = (0, import_react222.forwardRef)((props, ref) => {
  const _a = useInputProps("NativeSelect", defaultProps82, props), {
    inputProps,
    wrapperProps,
    data,
    onChange,
    value,
    classNames,
    styles: styles2,
    rightSection,
    rightSectionWidth
  } = _a, others = __objRest109(_a, [
    "inputProps",
    "wrapperProps",
    "data",
    "onChange",
    "value",
    "classNames",
    "styles",
    "rightSection",
    "rightSectionWidth"
  ]);
  const theme = useMantineTheme();
  const formattedData = data.map((item) => typeof item === "string" ? { label: item, value: item } : item);
  const options = formattedData.map((item) => import_react222.default.createElement("option", {
    key: item.value,
    value: item.value,
    disabled: item.disabled
  }, item.label));
  return import_react222.default.createElement(Input.Wrapper, __spreadProps70(__spreadValues163({}, wrapperProps), {
    __staticSelector: "NativeSelect"
  }), import_react222.default.createElement(Input, __spreadValues163(__spreadProps70(__spreadValues163(__spreadValues163({}, inputProps), others), {
    onChange,
    component: "select",
    ref,
    value: value === null ? "" : value,
    __staticSelector: "NativeSelect",
    pointer: theme.cursorType === "pointer"
  }), getSelectRightSectionProps({
    theme,
    rightSection,
    rightSectionWidth,
    styles: styles2,
    shouldClear: false,
    size: inputProps.size,
    error: wrapperProps.error,
    readOnly: false
  })), options));
});
NativeSelect.displayName = "@mantine/core/NativeSelect";

// node_modules/@mantine/core/esm/NavLink/NavLink.js
var import_react223 = __toESM(require_react());

// node_modules/@mantine/core/esm/NavLink/NavLink.styles.js
var __defProp164 = Object.defineProperty;
var __defProps71 = Object.defineProperties;
var __getOwnPropDescs71 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols166 = Object.getOwnPropertySymbols;
var __hasOwnProp166 = Object.prototype.hasOwnProperty;
var __propIsEnum166 = Object.prototype.propertyIsEnumerable;
var __defNormalProp164 = (obj, key, value) => key in obj ? __defProp164(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues164 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp166.call(b2, prop))
      __defNormalProp164(a2, prop, b2[prop]);
  if (__getOwnPropSymbols166)
    for (var prop of __getOwnPropSymbols166(b2)) {
      if (__propIsEnum166.call(b2, prop))
        __defNormalProp164(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps71 = (a2, b2) => __defProps71(a2, __getOwnPropDescs71(b2));
var useStyles73 = createStyles((theme, { color, variant: variant2, noWrap, childrenOffset, alignIcon }) => {
  const colors = theme.fn.variant({ variant: variant2, color });
  return {
    root: __spreadProps71(__spreadValues164({
      display: "flex",
      alignItems: "center",
      width: "100%",
      padding: `8px ${theme.spacing.sm}px`,
      userSelect: "none"
    }, theme.fn.hover({
      backgroundColor: theme.colorScheme === "dark" ? theme.colors.dark[6] : theme.colors.gray[0]
    })), {
      "&[data-active]": __spreadValues164({
        backgroundColor: colors.background,
        color: colors.color
      }, theme.fn.hover({ backgroundColor: colors.hover })),
      "&[data-disabled]": {
        opacity: 0.4,
        pointerEvents: "none"
      }
    }),
    icon: {
      marginRight: theme.spacing.sm,
      display: "flex",
      justifyContent: "center",
      alignItems: "center",
      alignSelf: alignIcon === "center" ? "center" : "flex-start",
      paddingTop: alignIcon === "center" ? void 0 : 4
    },
    rightSection: {
      marginLeft: theme.spacing.sm,
      display: "flex",
      justifyContent: "center",
      alignItems: "center",
      transition: `transform 150ms ${theme.transitionTimingFunction}`,
      "&[data-rotate]": {
        transform: "rotate(90deg)"
      }
    },
    body: {
      flex: 1,
      overflow: "hidden",
      textOverflow: "ellipsis",
      whiteSpace: noWrap ? "nowrap" : void 0
    },
    label: {},
    description: {
      display: "block",
      "&[data-active]": {
        color: "inherit"
      }
    },
    children: {
      paddingLeft: theme.fn.size({ size: childrenOffset, sizes: theme.spacing })
    }
  };
});
var NavLink_styles_default = useStyles73;

// node_modules/@mantine/core/esm/NavLink/NavLink.js
var __defProp165 = Object.defineProperty;
var __getOwnPropSymbols167 = Object.getOwnPropertySymbols;
var __hasOwnProp167 = Object.prototype.hasOwnProperty;
var __propIsEnum167 = Object.prototype.propertyIsEnumerable;
var __defNormalProp165 = (obj, key, value) => key in obj ? __defProp165(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues165 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp167.call(b2, prop))
      __defNormalProp165(a2, prop, b2[prop]);
  if (__getOwnPropSymbols167)
    for (var prop of __getOwnPropSymbols167(b2)) {
      if (__propIsEnum167.call(b2, prop))
        __defNormalProp165(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest110 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp167.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols167)
    for (var prop of __getOwnPropSymbols167(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum167.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps83 = {
  variant: "light",
  childrenOffset: "lg"
};
var _NavLink = (0, import_react223.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("NavLink", defaultProps83, props), {
    label,
    description,
    icon,
    rightSection,
    className,
    classNames,
    styles: styles2,
    unstyled,
    active,
    color,
    variant: variant2,
    noWrap,
    children,
    opened,
    defaultOpened,
    onChange,
    disableRightSectionRotation,
    childrenOffset,
    disabled,
    onClick
  } = _a, others = __objRest110(_a, [
    "label",
    "description",
    "icon",
    "rightSection",
    "className",
    "classNames",
    "styles",
    "unstyled",
    "active",
    "color",
    "variant",
    "noWrap",
    "children",
    "opened",
    "defaultOpened",
    "onChange",
    "disableRightSectionRotation",
    "childrenOffset",
    "disabled",
    "onClick"
  ]);
  const { classes, cx } = NavLink_styles_default({ color, variant: variant2, noWrap, childrenOffset, alignIcon: description ? "top" : "center" }, { name: "NavLink", classNames, styles: styles2, unstyled });
  const [_opened, setOpened] = useUncontrolled({
    value: opened,
    defaultValue: defaultOpened,
    finalValue: false,
    onChange
  });
  const withChildren = !!children;
  const handleClick = (event) => {
    if (withChildren) {
      event.preventDefault();
      onClick == null ? void 0 : onClick(event);
      setOpened(!_opened);
    } else {
      onClick == null ? void 0 : onClick(event);
    }
  };
  return import_react223.default.createElement(import_react223.default.Fragment, null, import_react223.default.createElement(UnstyledButton, __spreadValues165({
    ref,
    className: cx(classes.root, className),
    "data-active": active || void 0,
    onClick: handleClick,
    unstyled,
    "data-expanded": _opened || void 0,
    "data-disabled": disabled || void 0,
    disabled
  }, others), icon && import_react223.default.createElement("span", {
    className: classes.icon
  }, icon), import_react223.default.createElement("span", {
    className: classes.body
  }, import_react223.default.createElement(Text, {
    component: "span",
    size: "sm",
    className: classes.label
  }, label), import_react223.default.createElement(Text, {
    component: "span",
    color: "dimmed",
    size: "xs",
    "data-active": active || void 0,
    className: classes.description
  }, description)), (withChildren || rightSection) && import_react223.default.createElement("span", {
    className: classes.rightSection,
    "data-rotate": _opened && !disableRightSectionRotation || void 0
  }, withChildren ? rightSection || import_react223.default.createElement(ChevronIcon, {
    width: 14,
    height: 14,
    style: { transform: "rotate(-90deg)" }
  }) : rightSection)), import_react223.default.createElement(Collapse, {
    in: _opened
  }, import_react223.default.createElement("div", {
    className: classes.children
  }, children)));
});
_NavLink.displayName = "@mantine/core/NavLink";
var NavLink = createPolymorphicComponent(_NavLink);

// node_modules/@mantine/core/esm/Notification/Notification.js
var import_react224 = __toESM(require_react());

// node_modules/@mantine/core/esm/Notification/Notification.styles.js
var useStyles74 = createStyles((theme, { color, radius: radius2, withTitle }, getRef2) => {
  const _radius = theme.fn.radius(radius2);
  const topBottom = Math.min(Math.max(_radius / 1.2, 4), 30);
  const colors = theme.fn.variant({ variant: "filled", color });
  return {
    closeButton: theme.fn.hover({
      backgroundColor: theme.colorScheme === "dark" ? theme.colors.dark[8] : theme.colors.gray[0]
    }),
    icon: {
      ref: getRef2("icon"),
      boxSizing: "border-box",
      marginRight: theme.spacing.md,
      width: 28,
      height: 28,
      borderRadius: 28,
      display: "flex",
      flex: "none",
      alignItems: "center",
      justifyContent: "center",
      color: theme.white
    },
    withIcon: {
      paddingLeft: theme.spacing.xs,
      "&::before": {
        display: "none"
      }
    },
    root: {
      boxSizing: "border-box",
      position: "relative",
      display: "flex",
      alignItems: "center",
      overflow: "hidden",
      paddingLeft: 22,
      paddingRight: 5,
      paddingTop: theme.spacing.xs,
      paddingBottom: theme.spacing.xs,
      borderRadius: _radius,
      backgroundColor: theme.colorScheme === "dark" ? theme.colors.dark[6] : theme.white,
      boxShadow: theme.shadows.lg,
      border: `1px solid ${theme.colorScheme === "dark" ? theme.colors.dark[6] : theme.colors.gray[2]}`,
      "&::before": {
        content: '""',
        display: "block",
        position: "absolute",
        width: 6,
        top: topBottom,
        bottom: topBottom,
        left: 4,
        borderRadius: _radius,
        backgroundColor: colors.background
      },
      [`& .${getRef2("icon")}`]: {
        backgroundColor: colors.background,
        color: theme.white
      }
    },
    body: {
      flex: 1,
      overflow: "hidden",
      marginRight: 10
    },
    loader: {
      marginRight: theme.spacing.md
    },
    title: {
      lineHeight: 1.4,
      marginBottom: 2,
      overflow: "hidden",
      textOverflow: "ellipsis",
      color: theme.colorScheme === "dark" ? theme.white : theme.colors.gray[9]
    },
    description: {
      color: withTitle ? theme.colorScheme === "dark" ? theme.colors.dark[2] : theme.colors.gray[6] : theme.colorScheme === "dark" ? theme.colors.dark[0] : theme.black,
      lineHeight: 1.4,
      overflow: "hidden",
      textOverflow: "ellipsis"
    }
  };
});
var Notification_styles_default = useStyles74;

// node_modules/@mantine/core/esm/Notification/Notification.js
var __defProp166 = Object.defineProperty;
var __defProps72 = Object.defineProperties;
var __getOwnPropDescs72 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols168 = Object.getOwnPropertySymbols;
var __hasOwnProp168 = Object.prototype.hasOwnProperty;
var __propIsEnum168 = Object.prototype.propertyIsEnumerable;
var __defNormalProp166 = (obj, key, value) => key in obj ? __defProp166(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues166 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp168.call(b2, prop))
      __defNormalProp166(a2, prop, b2[prop]);
  if (__getOwnPropSymbols168)
    for (var prop of __getOwnPropSymbols168(b2)) {
      if (__propIsEnum168.call(b2, prop))
        __defNormalProp166(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps72 = (a2, b2) => __defProps72(a2, __getOwnPropDescs72(b2));
var __objRest111 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp168.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols168)
    for (var prop of __getOwnPropSymbols168(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum168.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var Notification = (0, import_react224.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("Notification", {}, props), {
    className,
    color,
    radius: radius2,
    loading,
    disallowClose,
    title,
    icon,
    children,
    onClose,
    closeButtonProps,
    classNames,
    styles: styles2,
    unstyled
  } = _a, others = __objRest111(_a, [
    "className",
    "color",
    "radius",
    "loading",
    "disallowClose",
    "title",
    "icon",
    "children",
    "onClose",
    "closeButtonProps",
    "classNames",
    "styles",
    "unstyled"
  ]);
  const { classes, cx } = Notification_styles_default({ color, radius: radius2, withTitle: !!title }, { classNames, styles: styles2, unstyled, name: "Notification" });
  const withIcon = icon || loading;
  return import_react224.default.createElement(Box, __spreadValues166({
    className: cx(classes.root, { [classes.withIcon]: withIcon }, className),
    role: "alert",
    ref
  }, others), icon && !loading && import_react224.default.createElement("div", {
    className: classes.icon
  }, icon), loading && import_react224.default.createElement(Loader, {
    size: 28,
    color,
    className: classes.loader
  }), import_react224.default.createElement("div", {
    className: classes.body
  }, title && import_react224.default.createElement(Text, {
    className: classes.title,
    size: "sm",
    weight: 500
  }, title), import_react224.default.createElement(Text, {
    color: "dimmed",
    className: classes.description,
    size: "sm"
  }, children)), !disallowClose && import_react224.default.createElement(CloseButton, __spreadProps72(__spreadValues166({
    iconSize: 16,
    color: "gray"
  }, closeButtonProps), {
    onClick: onClose,
    className: classes.closeButton
  })));
});
Notification.displayName = "@mantine/core/Notification";

// node_modules/@mantine/core/esm/NumberInput/NumberInput.js
var import_react227 = __toESM(require_react());

// node_modules/@mantine/core/esm/NumberInput/get-input-mode/get-input-mode.js
var getInputMode = (step, precision, os) => {
  if (Number.isInteger(step) && step >= 0 && precision === 0)
    return "numeric";
  if (!Number.isInteger(step) && step >= 0 && precision !== 0)
    return "decimal";
  if (Number.isInteger(step) && step < 0 && precision === 0) {
    if (os === "ios")
      return "text";
    return "decimal";
  }
  if (!Number.isInteger(step) && step < 0 && precision !== 0) {
    if (os === "ios")
      return "text";
    return "decimal";
  }
  return "numeric";
};

// node_modules/@mantine/core/esm/NumberInput/Chevron.js
var import_react225 = __toESM(require_react());
function Chevron({ direction, size: size3 }) {
  return import_react225.default.createElement("svg", {
    style: { transform: direction === "up" ? "rotate(180deg)" : void 0 },
    width: size3,
    height: size3,
    viewBox: "0 0 15 15",
    fill: "none",
    xmlns: "http://www.w3.org/2000/svg"
  }, import_react225.default.createElement("path", {
    d: "M3.13523 6.15803C3.3241 5.95657 3.64052 5.94637 3.84197 6.13523L7.5 9.56464L11.158 6.13523C11.3595 5.94637 11.6759 5.95657 11.8648 6.15803C12.0536 6.35949 12.0434 6.67591 11.842 6.86477L7.84197 10.6148C7.64964 10.7951 7.35036 10.7951 7.15803 10.6148L3.15803 6.86477C2.95657 6.67591 2.94637 6.35949 3.13523 6.15803Z",
    fill: "currentColor",
    fillRule: "evenodd",
    clipRule: "evenodd"
  }));
}

// node_modules/@mantine/core/esm/NumberInput/NumberInput.styles.js
var CONTROL_SIZES = {
  xs: 20,
  sm: 24,
  md: 30,
  lg: 34,
  xl: 36
};
var useStyles75 = createStyles((theme, { radius: radius2, size: size3 }) => ({
  rightSection: {
    display: "flex",
    flexDirection: "column",
    height: "calc(100% - 2px)",
    margin: 1,
    marginRight: 1,
    overflow: "hidden",
    borderTopRightRadius: theme.fn.radius(radius2),
    borderBottomRightRadius: theme.fn.radius(radius2)
  },
  control: {
    margin: 0,
    position: "relative",
    flex: "0 0 50%",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    boxSizing: "border-box",
    width: theme.fn.size({ size: size3, sizes: CONTROL_SIZES }),
    padding: 0,
    WebkitTapHighlightColor: "transparent",
    borderBottom: `1px solid ${theme.colorScheme === "dark" ? theme.colors.dark[4] : theme.colors.gray[4]}`,
    borderLeft: `1px solid ${theme.colorScheme === "dark" ? theme.colors.dark[4] : theme.colors.gray[4]}`,
    borderTop: 0,
    borderRight: 0,
    backgroundColor: "transparent",
    marginRight: 1,
    color: theme.colorScheme === "dark" ? theme.colors.dark[0] : theme.black,
    "&:not(:disabled):hover": {
      backgroundColor: theme.colorScheme === "dark" ? theme.colors.dark[5] : theme.colors.gray[0]
    },
    "&:disabled": {
      color: theme.colorScheme === "dark" ? theme.colors.dark[3] : theme.colors.gray[4]
    }
  },
  controlUp: {},
  controlDown: {
    borderBottom: 0
  }
}));
var NumberInput_styles_default = useStyles75;

// node_modules/@mantine/core/esm/TextInput/TextInput.js
var import_react226 = __toESM(require_react());
var __defProp167 = Object.defineProperty;
var __defProps73 = Object.defineProperties;
var __getOwnPropDescs73 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols169 = Object.getOwnPropertySymbols;
var __hasOwnProp169 = Object.prototype.hasOwnProperty;
var __propIsEnum169 = Object.prototype.propertyIsEnumerable;
var __defNormalProp167 = (obj, key, value) => key in obj ? __defProp167(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues167 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp169.call(b2, prop))
      __defNormalProp167(a2, prop, b2[prop]);
  if (__getOwnPropSymbols169)
    for (var prop of __getOwnPropSymbols169(b2)) {
      if (__propIsEnum169.call(b2, prop))
        __defNormalProp167(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps73 = (a2, b2) => __defProps73(a2, __getOwnPropDescs73(b2));
var __objRest112 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp169.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols169)
    for (var prop of __getOwnPropSymbols169(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum169.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps84 = {
  type: "text",
  size: "sm",
  __staticSelector: "TextInput"
};
var TextInput = (0, import_react226.forwardRef)((props, ref) => {
  const _a = useInputProps("TextInput", defaultProps84, props), { inputProps, wrapperProps } = _a, others = __objRest112(_a, ["inputProps", "wrapperProps"]);
  return import_react226.default.createElement(Input.Wrapper, __spreadValues167({}, wrapperProps), import_react226.default.createElement(Input, __spreadProps73(__spreadValues167(__spreadValues167({}, inputProps), others), {
    ref
  })));
});
TextInput.displayName = "@mantine/core/TextInput";

// node_modules/@mantine/core/esm/NumberInput/NumberInput.js
var __defProp168 = Object.defineProperty;
var __defProps74 = Object.defineProperties;
var __getOwnPropDescs74 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols170 = Object.getOwnPropertySymbols;
var __hasOwnProp170 = Object.prototype.hasOwnProperty;
var __propIsEnum170 = Object.prototype.propertyIsEnumerable;
var __defNormalProp168 = (obj, key, value) => key in obj ? __defProp168(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues168 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp170.call(b2, prop))
      __defNormalProp168(a2, prop, b2[prop]);
  if (__getOwnPropSymbols170)
    for (var prop of __getOwnPropSymbols170(b2)) {
      if (__propIsEnum170.call(b2, prop))
        __defNormalProp168(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps74 = (a2, b2) => __defProps74(a2, __getOwnPropDescs74(b2));
var __objRest113 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp170.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols170)
    for (var prop of __getOwnPropSymbols170(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum170.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultFormatter = (value) => value || "";
var defaultParser = (num) => {
  if (num === "-") {
    return num;
  }
  let tempNum = num;
  if (tempNum[0] === ".") {
    tempNum = `0${num}`;
  }
  const parsedNum = parseFloat(tempNum);
  if (Number.isNaN(parsedNum)) {
    return void 0;
  }
  return num;
};
var CHEVRON_SIZES = {
  xs: 10,
  sm: 14,
  md: 16,
  lg: 18,
  xl: 20
};
var defaultProps85 = {
  step: 1,
  hideControls: false,
  size: "sm",
  precision: 0,
  noClampOnBlur: false,
  removeTrailingZeros: false,
  formatter: defaultFormatter,
  parser: defaultParser,
  type: "text"
};
var NumberInput = (0, import_react227.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("NumberInput", defaultProps85, props), {
    readOnly,
    disabled,
    value,
    onChange,
    decimalSeparator,
    min: min3,
    max: max3,
    startValue,
    step,
    stepHoldInterval,
    stepHoldDelay,
    onBlur,
    onFocus,
    onKeyDown,
    onKeyUp,
    hideControls,
    radius: radius2,
    variant: variant2,
    precision,
    removeTrailingZeros,
    defaultValue,
    noClampOnBlur,
    handlersRef,
    classNames,
    styles: styles2,
    size: size3,
    rightSection,
    rightSectionWidth,
    formatter,
    parser,
    inputMode,
    unstyled,
    type
  } = _a, others = __objRest113(_a, [
    "readOnly",
    "disabled",
    "value",
    "onChange",
    "decimalSeparator",
    "min",
    "max",
    "startValue",
    "step",
    "stepHoldInterval",
    "stepHoldDelay",
    "onBlur",
    "onFocus",
    "onKeyDown",
    "onKeyUp",
    "hideControls",
    "radius",
    "variant",
    "precision",
    "removeTrailingZeros",
    "defaultValue",
    "noClampOnBlur",
    "handlersRef",
    "classNames",
    "styles",
    "size",
    "rightSection",
    "rightSectionWidth",
    "formatter",
    "parser",
    "inputMode",
    "unstyled",
    "type"
  ]);
  const { classes, cx, theme } = NumberInput_styles_default({ radius: radius2, size: size3 }, { classNames, styles: styles2, unstyled, name: "NumberInput" });
  const parsePrecision = (val) => {
    if (val === void 0)
      return void 0;
    let result = val.toFixed(precision);
    if (removeTrailingZeros && precision > 0) {
      result = result.replace(new RegExp(`[0]{0,${precision}}$`), "");
      if (result.endsWith(".") || result.endsWith(decimalSeparator)) {
        result = result.slice(0, -1);
      }
    }
    return result;
  };
  const [focused, setFocused] = (0, import_react227.useState)(false);
  const [_value, setValue] = (0, import_react227.useState)(typeof value === "number" ? value : typeof defaultValue === "number" ? defaultValue : void 0);
  const finalValue = typeof value === "number" ? value : _value;
  const [tempValue, setTempValue] = (0, import_react227.useState)(typeof finalValue === "number" ? parsePrecision(finalValue) : "");
  const inputRef = (0, import_react227.useRef)();
  const handleValueChange = (val) => {
    if (val !== _value && !Number.isNaN(val)) {
      typeof onChange === "function" && onChange(val);
      setValue(val);
    }
  };
  const formatNum = (val = "") => {
    let parsedStr = typeof val === "number" ? String(val) : val;
    if (decimalSeparator) {
      parsedStr = parsedStr.replace(/\./g, decimalSeparator);
    }
    return formatter(parsedStr);
  };
  const parseNum = (val) => {
    let num = val;
    if (decimalSeparator) {
      num = num.replace(new RegExp(`\\${decimalSeparator}`, "g"), ".");
    }
    return parser(num);
  };
  const _min = typeof min3 === "number" ? min3 : -Infinity;
  const _max = typeof max3 === "number" ? max3 : Infinity;
  const incrementRef = (0, import_react227.useRef)();
  incrementRef.current = () => {
    var _a2, _b, _c;
    if (_value === void 0) {
      handleValueChange((_a2 = startValue != null ? startValue : min3) != null ? _a2 : 0);
      setTempValue((_c = (_b = parsePrecision(startValue)) != null ? _b : parsePrecision(min3)) != null ? _c : "0");
    } else {
      const result = parsePrecision(clamp(_value + step, _min, _max));
      handleValueChange(parseFloat(result));
      setTempValue(result);
    }
  };
  const decrementRef = (0, import_react227.useRef)();
  decrementRef.current = () => {
    var _a2, _b, _c;
    if (_value === void 0) {
      handleValueChange((_a2 = startValue != null ? startValue : min3) != null ? _a2 : 0);
      setTempValue((_c = (_b = parsePrecision(startValue)) != null ? _b : parsePrecision(min3)) != null ? _c : "0");
    } else {
      const result = parsePrecision(clamp(_value - step, _min, _max));
      handleValueChange(parseFloat(result));
      setTempValue(result);
    }
  };
  assignRef(handlersRef, { increment: incrementRef.current, decrement: decrementRef.current });
  (0, import_react227.useEffect)(() => {
    if (typeof value === "number" && !focused) {
      setValue(value);
      setTempValue(parsePrecision(value));
    }
    if (defaultValue === void 0 && value === void 0 && !focused) {
      setValue(value);
      setTempValue("");
    }
  }, [value, precision]);
  const shouldUseStepInterval = stepHoldDelay !== void 0 && stepHoldInterval !== void 0;
  const onStepTimeoutRef = (0, import_react227.useRef)(null);
  const stepCountRef = (0, import_react227.useRef)(0);
  const onStepDone = () => {
    if (onStepTimeoutRef.current) {
      window.clearTimeout(onStepTimeoutRef.current);
    }
    onStepTimeoutRef.current = null;
    stepCountRef.current = 0;
  };
  const onStepHandleChange = (isIncrement) => {
    if (isIncrement) {
      incrementRef.current();
    } else {
      decrementRef.current();
    }
    stepCountRef.current += 1;
  };
  const onStepLoop = (isIncrement) => {
    onStepHandleChange(isIncrement);
    if (shouldUseStepInterval) {
      const interval = typeof stepHoldInterval === "number" ? stepHoldInterval : stepHoldInterval(stepCountRef.current);
      onStepTimeoutRef.current = window.setTimeout(() => onStepLoop(isIncrement), interval);
    }
  };
  const onStep = (event, isIncrement) => {
    event.preventDefault();
    inputRef.current.focus();
    onStepHandleChange(isIncrement);
    if (shouldUseStepInterval) {
      onStepTimeoutRef.current = window.setTimeout(() => onStepLoop(isIncrement), stepHoldDelay);
    }
  };
  (0, import_react227.useEffect)(() => {
    onStepDone();
    return onStepDone;
  }, []);
  const controls = import_react227.default.createElement("div", {
    className: classes.rightSection
  }, import_react227.default.createElement("button", {
    type: "button",
    tabIndex: -1,
    "aria-hidden": true,
    disabled: finalValue >= max3,
    className: cx(classes.control, classes.controlUp),
    onPointerDown: (event) => {
      onStep(event, true);
    },
    onPointerUp: onStepDone,
    onPointerLeave: onStepDone
  }, import_react227.default.createElement(Chevron, {
    size: theme.fn.size({ size: size3, sizes: CHEVRON_SIZES }),
    direction: "up"
  })), import_react227.default.createElement("button", {
    type: "button",
    tabIndex: -1,
    "aria-hidden": true,
    disabled: finalValue <= min3,
    className: cx(classes.control, classes.controlDown),
    onPointerDown: (event) => {
      onStep(event, false);
    },
    onPointerUp: onStepDone,
    onPointerLeave: onStepDone
  }, import_react227.default.createElement(Chevron, {
    size: theme.fn.size({ size: size3, sizes: CHEVRON_SIZES }),
    direction: "down"
  })));
  const handleChange = (event) => {
    const evt = event.nativeEvent;
    if (evt.isComposing) {
      return;
    }
    const val = event.target.value;
    const parsed = parseNum(val);
    setTempValue(parsed);
    if (val === "" || val === "-") {
      handleValueChange(void 0);
    } else {
      val.trim() !== "" && !Number.isNaN(parsed) && handleValueChange(parseFloat(parsed));
    }
  };
  const handleBlur = (event) => {
    var _a2;
    if (event.target.value === "") {
      setTempValue("");
      handleValueChange(void 0);
    } else {
      let newNumber = event.target.value;
      if (newNumber[0] === `${decimalSeparator}` || newNumber[0] === ".") {
        newNumber = `0${newNumber}`;
      }
      const parsedVal = parseNum(newNumber);
      const val = clamp(parseFloat(parsedVal), _min, _max);
      if (!Number.isNaN(val)) {
        if (!noClampOnBlur) {
          setTempValue(parsePrecision(val));
          handleValueChange(parseFloat(parsePrecision(val)));
        }
      } else {
        setTempValue((_a2 = parsePrecision(finalValue)) != null ? _a2 : "");
      }
    }
    setFocused(false);
    typeof onBlur === "function" && onBlur(event);
  };
  const handleFocus = (event) => {
    setFocused(true);
    typeof onFocus === "function" && onFocus(event);
  };
  const handleKeyDown = (event) => {
    typeof onKeyDown === "function" && onKeyDown(event);
    if (event.repeat && shouldUseStepInterval && (event.key === "ArrowUp" || event.key === "ArrowDown")) {
      event.preventDefault();
      return;
    }
    if (!readOnly) {
      if (event.key === "ArrowUp") {
        onStep(event, true);
      } else if (event.key === "ArrowDown") {
        onStep(event, false);
      }
    }
  };
  const handleKeyUp = (event) => {
    typeof onKeyUp === "function" && onKeyUp(event);
    if (event.key === "ArrowUp" || event.key === "ArrowDown") {
      onStepDone();
    }
  };
  return import_react227.default.createElement(TextInput, __spreadProps74(__spreadValues168({}, others), {
    type,
    variant: variant2,
    value: formatNum(tempValue),
    disabled,
    readOnly,
    ref: useMergedRef(inputRef, ref),
    onChange: handleChange,
    onBlur: handleBlur,
    onFocus: handleFocus,
    onKeyDown: handleKeyDown,
    onKeyUp: handleKeyUp,
    rightSection: rightSection || (disabled || readOnly || hideControls || variant2 === "unstyled" ? null : controls),
    rightSectionWidth: rightSectionWidth || theme.fn.size({ size: size3, sizes: CONTROL_SIZES }) + 1,
    radius: radius2,
    max: max3,
    min: min3,
    step,
    size: size3,
    styles: styles2,
    classNames,
    inputMode: inputMode || getInputMode(step, precision, useOs()),
    __staticSelector: "NumberInput",
    unstyled
  }));
});
NumberInput.displayName = "@mantine/core/NumberInput";

// node_modules/@mantine/core/esm/Pagination/Pagination.js
var import_react234 = __toESM(require_react());

// node_modules/@mantine/core/esm/Pagination/DefaultItem/DefaultItem.js
var import_react233 = __toESM(require_react());

// node_modules/@mantine/core/esm/Pagination/icons/DotsIcon.js
var import_react228 = __toESM(require_react());
var __defProp169 = Object.defineProperty;
var __getOwnPropSymbols171 = Object.getOwnPropertySymbols;
var __hasOwnProp171 = Object.prototype.hasOwnProperty;
var __propIsEnum171 = Object.prototype.propertyIsEnumerable;
var __defNormalProp169 = (obj, key, value) => key in obj ? __defProp169(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues169 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp171.call(b2, prop))
      __defNormalProp169(a2, prop, b2[prop]);
  if (__getOwnPropSymbols171)
    for (var prop of __getOwnPropSymbols171(b2)) {
      if (__propIsEnum171.call(b2, prop))
        __defNormalProp169(a2, prop, b2[prop]);
    }
  return a2;
};
function DotsIcon(props) {
  return import_react228.default.createElement("svg", __spreadValues169({
    width: 16,
    height: 16,
    viewBox: "0 0 16 16",
    fill: "none",
    xmlns: "http://www.w3.org/2000/svg"
  }, props), import_react228.default.createElement("path", {
    d: "M2 8c0-.733.6-1.333 1.333-1.333.734 0 1.334.6 1.334 1.333s-.6 1.333-1.334 1.333C2.6 9.333 2 8.733 2 8zm9.333 0c0-.733.6-1.333 1.334-1.333C13.4 6.667 14 7.267 14 8s-.6 1.333-1.333 1.333c-.734 0-1.334-.6-1.334-1.333zM6.667 8c0-.733.6-1.333 1.333-1.333s1.333.6 1.333 1.333S8.733 9.333 8 9.333 6.667 8.733 6.667 8z",
    fill: "currentColor"
  }));
}

// node_modules/@mantine/core/esm/Pagination/icons/NextIcon.js
var import_react229 = __toESM(require_react());
var __defProp170 = Object.defineProperty;
var __getOwnPropSymbols172 = Object.getOwnPropertySymbols;
var __hasOwnProp172 = Object.prototype.hasOwnProperty;
var __propIsEnum172 = Object.prototype.propertyIsEnumerable;
var __defNormalProp170 = (obj, key, value) => key in obj ? __defProp170(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues170 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp172.call(b2, prop))
      __defNormalProp170(a2, prop, b2[prop]);
  if (__getOwnPropSymbols172)
    for (var prop of __getOwnPropSymbols172(b2)) {
      if (__propIsEnum172.call(b2, prop))
        __defNormalProp170(a2, prop, b2[prop]);
    }
  return a2;
};
function NextIcon(props) {
  return import_react229.default.createElement("svg", __spreadValues170({
    width: 16,
    height: 16,
    viewBox: "0 0 16 16",
    fill: "none",
    xmlns: "http://www.w3.org/2000/svg"
  }, props), import_react229.default.createElement("path", {
    d: "M8.781 8l-3.3-3.3.943-.943L10.667 8l-4.243 4.243-.943-.943 3.3-3.3z",
    fill: "currentColor"
  }));
}

// node_modules/@mantine/core/esm/Pagination/icons/PrevIcon.js
var import_react230 = __toESM(require_react());
var __defProp171 = Object.defineProperty;
var __getOwnPropSymbols173 = Object.getOwnPropertySymbols;
var __hasOwnProp173 = Object.prototype.hasOwnProperty;
var __propIsEnum173 = Object.prototype.propertyIsEnumerable;
var __defNormalProp171 = (obj, key, value) => key in obj ? __defProp171(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues171 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp173.call(b2, prop))
      __defNormalProp171(a2, prop, b2[prop]);
  if (__getOwnPropSymbols173)
    for (var prop of __getOwnPropSymbols173(b2)) {
      if (__propIsEnum173.call(b2, prop))
        __defNormalProp171(a2, prop, b2[prop]);
    }
  return a2;
};
function PrevIcon(props) {
  return import_react230.default.createElement("svg", __spreadValues171({
    width: 16,
    height: 16,
    viewBox: "0 0 16 16",
    fill: "none",
    xmlns: "http://www.w3.org/2000/svg"
  }, props), import_react230.default.createElement("path", {
    d: "M7.219 8l3.3 3.3-.943.943L5.333 8l4.243-4.243.943.943-3.3 3.3z",
    fill: "currentColor"
  }));
}

// node_modules/@mantine/core/esm/Pagination/icons/FirstIcon.js
var import_react231 = __toESM(require_react());
var __defProp172 = Object.defineProperty;
var __getOwnPropSymbols174 = Object.getOwnPropertySymbols;
var __hasOwnProp174 = Object.prototype.hasOwnProperty;
var __propIsEnum174 = Object.prototype.propertyIsEnumerable;
var __defNormalProp172 = (obj, key, value) => key in obj ? __defProp172(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues172 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp174.call(b2, prop))
      __defNormalProp172(a2, prop, b2[prop]);
  if (__getOwnPropSymbols174)
    for (var prop of __getOwnPropSymbols174(b2)) {
      if (__propIsEnum174.call(b2, prop))
        __defNormalProp172(a2, prop, b2[prop]);
    }
  return a2;
};
function FirstIcon(props) {
  return import_react231.default.createElement("svg", __spreadValues172({
    width: 16,
    height: 16,
    viewBox: "0 0 16 16",
    fill: "none",
    xmlns: "http://www.w3.org/2000/svg"
  }, props), import_react231.default.createElement("path", {
    d: "M6.85355 3.85355C7.04882 3.65829 7.04882 3.34171 6.85355 3.14645C6.65829 2.95118 6.34171 2.95118 6.14645 3.14645L2.14645 7.14645C1.95118 7.34171 1.95118 7.65829 2.14645 7.85355L6.14645 11.8536C6.34171 12.0488 6.65829 12.0488 6.85355 11.8536C7.04882 11.6583 7.04882 11.3417 6.85355 11.1464L3.20711 7.5L6.85355 3.85355ZM12.8536 3.85355C13.0488 3.65829 13.0488 3.34171 12.8536 3.14645C12.6583 2.95118 12.3417 2.95118 12.1464 3.14645L8.14645 7.14645C7.95118 7.34171 7.95118 7.65829 8.14645 7.85355L12.1464 11.8536C12.3417 12.0488 12.6583 12.0488 12.8536 11.8536C13.0488 11.6583 13.0488 11.3417 12.8536 11.1464L9.20711 7.5L12.8536 3.85355Z",
    fill: "currentColor"
  }));
}

// node_modules/@mantine/core/esm/Pagination/icons/LastIcon.js
var import_react232 = __toESM(require_react());
var __defProp173 = Object.defineProperty;
var __getOwnPropSymbols175 = Object.getOwnPropertySymbols;
var __hasOwnProp175 = Object.prototype.hasOwnProperty;
var __propIsEnum175 = Object.prototype.propertyIsEnumerable;
var __defNormalProp173 = (obj, key, value) => key in obj ? __defProp173(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues173 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp175.call(b2, prop))
      __defNormalProp173(a2, prop, b2[prop]);
  if (__getOwnPropSymbols175)
    for (var prop of __getOwnPropSymbols175(b2)) {
      if (__propIsEnum175.call(b2, prop))
        __defNormalProp173(a2, prop, b2[prop]);
    }
  return a2;
};
function LastIcon(props) {
  return import_react232.default.createElement("svg", __spreadValues173({
    width: 16,
    height: 16,
    viewBox: "0 0 16 16",
    fill: "none",
    xmlns: "http://www.w3.org/2000/svg"
  }, props), import_react232.default.createElement("path", {
    d: "M2.14645 11.1464C1.95118 11.3417 1.95118 11.6583 2.14645 11.8536C2.34171 12.0488 2.65829 12.0488 2.85355 11.8536L6.85355 7.85355C7.04882 7.65829 7.04882 7.34171 6.85355 7.14645L2.85355 3.14645C2.65829 2.95118 2.34171 2.95118 2.14645 3.14645C1.95118 3.34171 1.95118 3.65829 2.14645 3.85355L5.79289 7.5L2.14645 11.1464ZM8.14645 11.1464C7.95118 11.3417 7.95118 11.6583 8.14645 11.8536C8.34171 12.0488 8.65829 12.0488 8.85355 11.8536L12.8536 7.85355C13.0488 7.65829 13.0488 7.34171 12.8536 7.14645L8.85355 3.14645C8.65829 2.95118 8.34171 2.95118 8.14645 3.14645C7.95118 3.34171 7.95118 3.65829 8.14645 3.85355L11.7929 7.5L8.14645 11.1464Z",
    fill: "currentColor"
  }));
}

// node_modules/@mantine/core/esm/Pagination/DefaultItem/DefaultItem.js
var __defProp174 = Object.defineProperty;
var __getOwnPropSymbols176 = Object.getOwnPropertySymbols;
var __hasOwnProp176 = Object.prototype.hasOwnProperty;
var __propIsEnum176 = Object.prototype.propertyIsEnumerable;
var __defNormalProp174 = (obj, key, value) => key in obj ? __defProp174(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues174 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp176.call(b2, prop))
      __defNormalProp174(a2, prop, b2[prop]);
  if (__getOwnPropSymbols176)
    for (var prop of __getOwnPropSymbols176(b2)) {
      if (__propIsEnum176.call(b2, prop))
        __defNormalProp174(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest114 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp176.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols176)
    for (var prop of __getOwnPropSymbols176(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum176.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var icons = {
  dots: DotsIcon,
  next: NextIcon,
  prev: PrevIcon,
  first: FirstIcon,
  last: LastIcon
};
var rtlIcons = {
  dots: DotsIcon,
  prev: NextIcon,
  next: PrevIcon,
  last: FirstIcon,
  first: LastIcon
};
function DefaultItem2(_a) {
  var _b = _a, { page, active, onClick } = _b, others = __objRest114(_b, ["page", "active", "onClick"]);
  const theme = useMantineTheme();
  const Item = (theme.dir === "rtl" ? rtlIcons : icons)[page];
  const children = Item ? import_react233.default.createElement(Item, null) : page;
  return import_react233.default.createElement("button", __spreadValues174({
    type: "button",
    onClick
  }, others), children);
}
DefaultItem2.displayName = "@mantine/core/Pagination/DefaultItem";

// node_modules/@mantine/core/esm/Pagination/Pagination.styles.js
var __defProp175 = Object.defineProperty;
var __defProps75 = Object.defineProperties;
var __getOwnPropDescs75 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols177 = Object.getOwnPropertySymbols;
var __hasOwnProp177 = Object.prototype.hasOwnProperty;
var __propIsEnum177 = Object.prototype.propertyIsEnumerable;
var __defNormalProp175 = (obj, key, value) => key in obj ? __defProp175(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues175 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp177.call(b2, prop))
      __defNormalProp175(a2, prop, b2[prop]);
  if (__getOwnPropSymbols177)
    for (var prop of __getOwnPropSymbols177(b2)) {
      if (__propIsEnum177.call(b2, prop))
        __defNormalProp175(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps75 = (a2, b2) => __defProps75(a2, __getOwnPropDescs75(b2));
var sizes17 = {
  xs: 22,
  sm: 26,
  md: 32,
  lg: 38,
  xl: 44
};
var useStyles76 = createStyles((theme, { size: size3, radius: radius2, color }) => {
  const colors = theme.fn.variant({ color, variant: "filled" });
  return {
    item: __spreadProps75(__spreadValues175(__spreadValues175({}, theme.fn.fontStyles()), theme.fn.focusStyles()), {
      cursor: "pointer",
      userSelect: "none",
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      border: `1px solid ${theme.colorScheme === "dark" ? theme.colors.dark[4] : theme.colors.gray[4]}`,
      color: theme.colorScheme === "dark" ? theme.colors.dark[0] : theme.black,
      height: theme.fn.size({ size: size3, sizes: sizes17 }),
      minWidth: theme.fn.size({ size: size3, sizes: sizes17 }),
      padding: `0 ${theme.fn.size({ size: size3, sizes: theme.spacing }) / 2}px`,
      fontSize: theme.fn.size({ size: size3, sizes: theme.fontSizes }),
      borderRadius: theme.fn.radius(radius2),
      lineHeight: 1,
      backgroundColor: theme.colorScheme === "dark" ? theme.colors.dark[6] : theme.white,
      "&:active:not(:disabled):not([data-dots])": theme.activeStyles,
      "&:disabled": {
        opacity: 0.6,
        cursor: "not-allowed",
        color: theme.colorScheme === "dark" ? theme.colors.dark[3] : theme.colors.gray[5]
      },
      "&[data-dots]": {
        cursor: "default",
        borderColor: "transparent",
        backgroundColor: "transparent"
      },
      "&[data-active]": {
        borderColor: "transparent",
        color: colors.color,
        backgroundColor: colors.background
      }
    })
  };
});
var Pagination_styles_default = useStyles76;

// node_modules/@mantine/core/esm/Pagination/Pagination.js
var __defProp176 = Object.defineProperty;
var __getOwnPropSymbols178 = Object.getOwnPropertySymbols;
var __hasOwnProp178 = Object.prototype.hasOwnProperty;
var __propIsEnum178 = Object.prototype.propertyIsEnumerable;
var __defNormalProp176 = (obj, key, value) => key in obj ? __defProp176(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues176 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp178.call(b2, prop))
      __defNormalProp176(a2, prop, b2[prop]);
  if (__getOwnPropSymbols178)
    for (var prop of __getOwnPropSymbols178(b2)) {
      if (__propIsEnum178.call(b2, prop))
        __defNormalProp176(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest115 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp178.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols178)
    for (var prop of __getOwnPropSymbols178(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum178.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps86 = {
  itemComponent: DefaultItem2,
  initialPage: 1,
  siblings: 1,
  boundaries: 1,
  size: "md",
  radius: "sm",
  withEdges: false,
  withControls: true
};
var Pagination = (0, import_react234.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("Pagination", defaultProps86, props), {
    itemComponent: Item,
    classNames,
    styles: styles2,
    page,
    initialPage,
    color,
    total,
    siblings,
    boundaries,
    size: size3,
    radius: radius2,
    onChange,
    getItemAriaLabel,
    spacing,
    withEdges,
    withControls,
    sx,
    unstyled,
    disabled
  } = _a, others = __objRest115(_a, [
    "itemComponent",
    "classNames",
    "styles",
    "page",
    "initialPage",
    "color",
    "total",
    "siblings",
    "boundaries",
    "size",
    "radius",
    "onChange",
    "getItemAriaLabel",
    "spacing",
    "withEdges",
    "withControls",
    "sx",
    "unstyled",
    "disabled"
  ]);
  const { classes, theme } = Pagination_styles_default({ color, size: size3, radius: radius2 }, { classNames, styles: styles2, unstyled, name: "Pagination" });
  const { range: range2, setPage, next: next2, previous, active, first, last } = usePagination({
    page,
    siblings,
    total,
    onChange,
    initialPage,
    boundaries
  });
  if (total <= 0) {
    return null;
  }
  const items = range2.map((pageNumber, index6) => import_react234.default.createElement(Item, {
    key: index6,
    page: pageNumber,
    active: pageNumber === active,
    "aria-current": pageNumber === active ? "page" : void 0,
    "aria-label": typeof getItemAriaLabel === "function" ? getItemAriaLabel(pageNumber) : null,
    tabIndex: pageNumber === "dots" ? -1 : 0,
    "data-dots": pageNumber === "dots" || void 0,
    "data-active": pageNumber === active || void 0,
    className: classes.item,
    onClick: pageNumber !== "dots" ? () => setPage(pageNumber) : void 0,
    disabled
  }));
  return import_react234.default.createElement(Group, __spreadValues176({
    role: "navigation",
    spacing: spacing != null ? spacing : theme.fn.size({ size: size3, sizes: theme.spacing }) / 2,
    ref,
    sx,
    unstyled
  }, others), withEdges && import_react234.default.createElement(Item, {
    page: "first",
    onClick: first,
    "aria-label": getItemAriaLabel ? getItemAriaLabel("first") : void 0,
    "aria-disabled": active === 1 || disabled,
    className: classes.item,
    disabled: active === 1 || disabled
  }), withControls && import_react234.default.createElement(Item, {
    page: "prev",
    onClick: previous,
    "aria-label": getItemAriaLabel ? getItemAriaLabel("prev") : void 0,
    "aria-disabled": active === 1 || disabled,
    className: classes.item,
    disabled: active === 1 || disabled
  }), items, withControls && import_react234.default.createElement(Item, {
    page: "next",
    onClick: next2,
    "aria-label": getItemAriaLabel ? getItemAriaLabel("next") : void 0,
    "aria-disabled": active === total || disabled,
    className: classes.item,
    disabled: active === total || disabled
  }), withEdges && import_react234.default.createElement(Item, {
    page: "last",
    onClick: last,
    "aria-label": getItemAriaLabel ? getItemAriaLabel("last") : void 0,
    "aria-disabled": active === total || disabled,
    className: classes.item,
    disabled: active === total || disabled
  }));
});
Pagination.displayName = "@mantine/core/Pagination";

// node_modules/@mantine/core/esm/PasswordInput/PasswordInput.js
var import_react236 = __toESM(require_react());

// node_modules/@mantine/core/esm/PasswordInput/PasswordToggleIcon.js
var import_react235 = __toESM(require_react());
var PasswordToggleIcon = ({
  reveal,
  size: size3 = 15
}) => import_react235.default.createElement("svg", {
  width: size3,
  height: size3,
  viewBox: "0 0 15 15",
  fill: "none",
  xmlns: "http://www.w3.org/2000/svg"
}, import_react235.default.createElement("path", {
  d: reveal ? "M13.3536 2.35355C13.5488 2.15829 13.5488 1.84171 13.3536 1.64645C13.1583 1.45118 12.8417 1.45118 12.6464 1.64645L10.6828 3.61012C9.70652 3.21671 8.63759 3 7.5 3C4.30786 3 1.65639 4.70638 0.0760002 7.23501C-0.0253338 7.39715 -0.0253334 7.60288 0.0760014 7.76501C0.902945 9.08812 2.02314 10.1861 3.36061 10.9323L1.64645 12.6464C1.45118 12.8417 1.45118 13.1583 1.64645 13.3536C1.84171 13.5488 2.15829 13.5488 2.35355 13.3536L4.31723 11.3899C5.29348 11.7833 6.36241 12 7.5 12C10.6921 12 13.3436 10.2936 14.924 7.76501C15.0253 7.60288 15.0253 7.39715 14.924 7.23501C14.0971 5.9119 12.9769 4.81391 11.6394 4.06771L13.3536 2.35355ZM9.90428 4.38861C9.15332 4.1361 8.34759 4 7.5 4C4.80285 4 2.52952 5.37816 1.09622 7.50001C1.87284 8.6497 2.89609 9.58106 4.09974 10.1931L9.90428 4.38861ZM5.09572 10.6114L10.9003 4.80685C12.1039 5.41894 13.1272 6.35031 13.9038 7.50001C12.4705 9.62183 10.1971 11 7.5 11C6.65241 11 5.84668 10.8639 5.09572 10.6114Z" : "M7.5 11C4.80285 11 2.52952 9.62184 1.09622 7.50001C2.52952 5.37816 4.80285 4 7.5 4C10.1971 4 12.4705 5.37816 13.9038 7.50001C12.4705 9.62183 10.1971 11 7.5 11ZM7.5 3C4.30786 3 1.65639 4.70638 0.0760002 7.23501C-0.0253338 7.39715 -0.0253334 7.60288 0.0760014 7.76501C1.65639 10.2936 4.30786 12 7.5 12C10.6921 12 13.3436 10.2936 14.924 7.76501C15.0253 7.60288 15.0253 7.39715 14.924 7.23501C13.3436 4.70638 10.6921 3 7.5 3ZM7.5 9.5C8.60457 9.5 9.5 8.60457 9.5 7.5C9.5 6.39543 8.60457 5.5 7.5 5.5C6.39543 5.5 5.5 6.39543 5.5 7.5C5.5 8.60457 6.39543 9.5 7.5 9.5Z",
  fill: "currentColor",
  fillRule: "evenodd",
  clipRule: "evenodd"
}));

// node_modules/@mantine/core/esm/PasswordInput/PasswordInput.styles.js
var __defProp177 = Object.defineProperty;
var __defProps76 = Object.defineProperties;
var __getOwnPropDescs76 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols179 = Object.getOwnPropertySymbols;
var __hasOwnProp179 = Object.prototype.hasOwnProperty;
var __propIsEnum179 = Object.prototype.propertyIsEnumerable;
var __defNormalProp177 = (obj, key, value) => key in obj ? __defProp177(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues177 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp179.call(b2, prop))
      __defNormalProp177(a2, prop, b2[prop]);
  if (__getOwnPropSymbols179)
    for (var prop of __getOwnPropSymbols179(b2)) {
      if (__propIsEnum179.call(b2, prop))
        __defNormalProp177(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps76 = (a2, b2) => __defProps76(a2, __getOwnPropDescs76(b2));
var useStyles77 = createStyles((theme, { size: size3, rightSectionWidth }) => ({
  visibilityToggle: {},
  input: {
    position: "relative",
    overflow: "hidden"
  },
  innerInput: __spreadProps76(__spreadValues177({}, theme.fn.fontStyles()), {
    backgroundColor: "transparent",
    border: "1px solid transparent",
    borderLeftWidth: 0,
    borderRightWidth: 0,
    boxSizing: "border-box",
    position: "absolute",
    display: "block",
    width: `calc(100% - ${rightSectionWidth}px)`,
    paddingLeft: theme.fn.size({ size: size3, sizes: sizes4 }) / 3,
    fontSize: theme.fn.size({ size: size3, sizes: theme.fontSizes }),
    height: theme.fn.size({ size: size3, sizes: sizes4 }) - 2,
    lineHeight: `${theme.fn.size({ size: size3, sizes: sizes4 }) - 4}px`,
    color: theme.colorScheme === "dark" ? theme.colors.dark[0] : theme.black,
    top: 0,
    bottom: 0,
    left: 0,
    right: 0,
    "&::-ms-reveal, &::-ms-clear": {
      display: "none"
    },
    "&:focus": {
      outline: 0
    },
    "&:disabled": {
      cursor: "not-allowed"
    },
    "&::placeholder": __spreadProps76(__spreadValues177({}, theme.fn.placeholderStyles()), {
      opacity: 1
    })
  }),
  invalid: {
    color: theme.fn.variant({ variant: "filled", color: "red" }).background,
    "&::placeholder": {
      opacity: 1,
      color: theme.fn.variant({ variant: "filled", color: "red" }).background
    }
  },
  withIcon: {
    paddingLeft: `${theme.fn.size({ size: size3, sizes: sizes4 })}px !important`
  }
}));
var PasswordInput_styles_default = useStyles77;

// node_modules/@mantine/core/esm/PasswordInput/PasswordInput.js
var __defProp178 = Object.defineProperty;
var __defProps77 = Object.defineProperties;
var __getOwnPropDescs77 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols180 = Object.getOwnPropertySymbols;
var __hasOwnProp180 = Object.prototype.hasOwnProperty;
var __propIsEnum180 = Object.prototype.propertyIsEnumerable;
var __defNormalProp178 = (obj, key, value) => key in obj ? __defProp178(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues178 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp180.call(b2, prop))
      __defNormalProp178(a2, prop, b2[prop]);
  if (__getOwnPropSymbols180)
    for (var prop of __getOwnPropSymbols180(b2)) {
      if (__propIsEnum180.call(b2, prop))
        __defNormalProp178(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps77 = (a2, b2) => __defProps77(a2, __getOwnPropDescs77(b2));
var __objRest116 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp180.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols180)
    for (var prop of __getOwnPropSymbols180(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum180.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var buttonSizes2 = {
  xs: 22,
  sm: 26,
  md: 28,
  lg: 32,
  xl: 40
};
var iconSizes5 = {
  xs: 12,
  sm: 15,
  md: 17,
  lg: 19,
  xl: 21
};
var rightSectionSizes = {
  xs: 28,
  sm: 32,
  md: 34,
  lg: 44,
  xl: 54
};
var defaultProps87 = {
  size: "sm",
  toggleTabIndex: -1,
  visibilityToggleIcon: PasswordToggleIcon,
  __staticSelector: "PasswordInput"
};
var PasswordInput = (0, import_react236.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("PasswordInput", defaultProps87, props), {
    radius: radius2,
    disabled,
    size: size3,
    toggleTabIndex,
    className,
    id,
    label,
    error,
    required,
    style,
    icon,
    description,
    wrapperProps,
    classNames,
    styles: styles2,
    variant: variant2,
    visibilityToggleIcon: VisibilityToggleIcon,
    __staticSelector,
    rightSection: _rightSection,
    rightSectionWidth: _rightSectionWidth,
    rightSectionProps: _rightSectionProps,
    sx,
    labelProps,
    descriptionProps,
    errorProps,
    unstyled,
    visibilityToggleLabel,
    withAsterisk,
    inputContainer,
    iconWidth,
    inputWrapperOrder,
    visible: visible2,
    defaultVisible,
    onVisibilityChange
  } = _a, others = __objRest116(_a, [
    "radius",
    "disabled",
    "size",
    "toggleTabIndex",
    "className",
    "id",
    "label",
    "error",
    "required",
    "style",
    "icon",
    "description",
    "wrapperProps",
    "classNames",
    "styles",
    "variant",
    "visibilityToggleIcon",
    "__staticSelector",
    "rightSection",
    "rightSectionWidth",
    "rightSectionProps",
    "sx",
    "labelProps",
    "descriptionProps",
    "errorProps",
    "unstyled",
    "visibilityToggleLabel",
    "withAsterisk",
    "inputContainer",
    "iconWidth",
    "inputWrapperOrder",
    "visible",
    "defaultVisible",
    "onVisibilityChange"
  ]);
  const theme = useMantineTheme();
  const rightSectionWidth = theme.fn.size({ size: size3, sizes: rightSectionSizes });
  const { classes, cx } = PasswordInput_styles_default({ size: size3, rightSectionWidth }, { name: "PasswordInput", classNames, styles: styles2, unstyled });
  const uuid = useId(id);
  const { systemStyles, rest } = extractSystemStyles(others);
  const [_visible, setVisibility] = useUncontrolled({
    value: visible2,
    defaultValue: defaultVisible,
    finalValue: false,
    onChange: onVisibilityChange
  });
  const toggleVisibility = () => setVisibility(!_visible);
  const rightSection = import_react236.default.createElement(ActionIcon, {
    className: classes.visibilityToggle,
    tabIndex: toggleTabIndex,
    radius: radius2,
    size: theme.fn.size({ size: size3, sizes: buttonSizes2 }),
    "aria-hidden": !visibilityToggleLabel,
    "aria-label": visibilityToggleLabel,
    unstyled,
    onMouseDown: (event) => {
      event.preventDefault();
      toggleVisibility();
    },
    onKeyDown: (event) => {
      if (event.key === " ") {
        event.preventDefault();
        toggleVisibility();
      }
    }
  }, import_react236.default.createElement(VisibilityToggleIcon, {
    reveal: _visible,
    size: theme.fn.size({ size: size3, sizes: iconSizes5 })
  }));
  return import_react236.default.createElement(Input.Wrapper, __spreadValues178(__spreadValues178({
    required,
    id: uuid,
    label,
    error,
    description,
    size: size3,
    className,
    style,
    classNames,
    styles: styles2,
    __staticSelector,
    sx,
    errorProps,
    descriptionProps,
    labelProps,
    unstyled,
    withAsterisk,
    inputWrapperOrder,
    inputContainer
  }, systemStyles), wrapperProps), import_react236.default.createElement(Input, {
    component: "div",
    invalid: !!error,
    icon,
    size: size3,
    classNames: __spreadProps77(__spreadValues178({}, classNames), { input: classes.input }),
    styles: styles2,
    radius: radius2,
    disabled,
    __staticSelector,
    rightSectionWidth,
    rightSection: !disabled && rightSection,
    variant: variant2,
    unstyled,
    iconWidth
  }, import_react236.default.createElement("input", __spreadValues178({
    type: _visible ? "text" : "password",
    required,
    className: cx(classes.innerInput, {
      [classes.withIcon]: icon,
      [classes.invalid]: !!error
    }),
    disabled,
    id: uuid,
    ref
  }, rest))));
});
PasswordInput.displayName = "@mantine/core/PasswordInput";

// node_modules/@mantine/core/esm/Progress/Progress.js
var import_react246 = __toESM(require_react());

// node_modules/@mantine/core/esm/Progress/Progress.styles.js
var sizes18 = {
  xs: 3,
  sm: 5,
  md: 8,
  lg: 12,
  xl: 16
};
var stripesAnimation = keyframes({
  from: { backgroundPosition: "0 0" },
  to: { backgroundPosition: "40px 0" }
});
var useStyles78 = createStyles((theme, { color, radius: radius2, size: size3, striped, animate }) => ({
  root: {
    position: "relative",
    height: theme.fn.size({ size: size3, sizes: sizes18 }),
    backgroundColor: theme.colorScheme === "dark" ? theme.colors.dark[4] : theme.colors.gray[2],
    borderRadius: theme.fn.size({ size: radius2, sizes: theme.radius }),
    overflow: "hidden"
  },
  bar: {
    position: "absolute",
    top: 0,
    bottom: 0,
    left: 0,
    height: "100%",
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    backgroundColor: theme.fn.variant({
      variant: "filled",
      primaryFallback: false,
      color: color || theme.primaryColor
    }).background,
    transition: "width 100ms linear",
    animation: animate ? `${stripesAnimation} 1000ms linear infinite` : "none",
    backgroundSize: "20px 20px",
    backgroundImage: striped ? "linear-gradient(45deg, rgba(255, 255, 255, 0.15) 25%, transparent 25%, transparent 50%, rgba(255, 255, 255, 0.15) 50%, rgba(255, 255, 255, 0.15) 75%, transparent 75%, transparent)" : "none",
    "&:last-of-type": {
      borderTopRightRadius: theme.fn.size({ size: radius2, sizes: theme.radius }),
      borderBottomRightRadius: theme.fn.size({ size: radius2, sizes: theme.radius })
    },
    "&:first-of-type": {
      borderTopLeftRadius: theme.fn.size({ size: radius2, sizes: theme.radius }),
      borderBottomLeftRadius: theme.fn.size({ size: radius2, sizes: theme.radius })
    },
    "@media (prefers-reduced-motion)": {
      transitionDuration: theme.respectReducedMotion ? "0ms" : void 0
    }
  },
  label: {
    color: theme.white,
    fontSize: theme.fn.size({ size: size3, sizes: sizes18 }) * 0.65,
    fontWeight: 700,
    userSelect: "none",
    overflow: "hidden",
    whiteSpace: "nowrap"
  }
}));
var Progress_styles_default = useStyles78;

// node_modules/@mantine/core/esm/Tooltip/Tooltip.js
var import_react245 = __toESM(require_react());

// node_modules/@mantine/core/esm/Tooltip/TooltipGroup/TooltipGroup.js
var import_react238 = __toESM(require_react());

// node_modules/@mantine/core/esm/Tooltip/TooltipGroup/TooltipGroup.context.js
var import_react237 = __toESM(require_react());
var TooltipGroupContext = (0, import_react237.createContext)(false);
var TooltipGroupProvider = TooltipGroupContext.Provider;
var useTooltipGroupContext = () => (0, import_react237.useContext)(TooltipGroupContext);

// node_modules/@mantine/core/esm/Tooltip/TooltipGroup/TooltipGroup.js
function TooltipGroup({ children, openDelay = 0, closeDelay = 0 }) {
  return import_react238.default.createElement(TooltipGroupProvider, {
    value: true
  }, import_react238.default.createElement(FloatingDelayGroup, {
    delay: { open: openDelay, close: closeDelay }
  }, children));
}
TooltipGroup.displayName = "@mantine/core/TooltipGroup";

// node_modules/@mantine/core/esm/Tooltip/TooltipFloating/TooltipFloating.js
var import_react242 = __toESM(require_react());

// node_modules/@mantine/core/esm/Tooltip/Tooltip.styles.js
var __defProp179 = Object.defineProperty;
var __defProps78 = Object.defineProperties;
var __getOwnPropDescs78 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols181 = Object.getOwnPropertySymbols;
var __hasOwnProp181 = Object.prototype.hasOwnProperty;
var __propIsEnum181 = Object.prototype.propertyIsEnumerable;
var __defNormalProp179 = (obj, key, value) => key in obj ? __defProp179(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues179 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp181.call(b2, prop))
      __defNormalProp179(a2, prop, b2[prop]);
  if (__getOwnPropSymbols181)
    for (var prop of __getOwnPropSymbols181(b2)) {
      if (__propIsEnum181.call(b2, prop))
        __defNormalProp179(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps78 = (a2, b2) => __defProps78(a2, __getOwnPropDescs78(b2));
function getColors(theme, color) {
  if (!color) {
    return {
      backgroundColor: theme.colorScheme === "dark" ? theme.colors.dark[6] : theme.colors.gray[9],
      color: theme.white
    };
  }
  const colors = theme.fn.variant({ variant: "filled", color, primaryFallback: false });
  return {
    backgroundColor: colors.background,
    color: colors.color
  };
}
var useStyles79 = createStyles((theme, { color, radius: radius2, width, multiline }) => ({
  tooltip: __spreadProps78(__spreadValues179(__spreadValues179({}, theme.fn.fontStyles()), getColors(theme, color)), {
    lineHeight: theme.lineHeight,
    fontSize: theme.fontSizes.sm,
    borderRadius: theme.fn.radius(radius2),
    padding: `calc(${theme.spacing.xs}px / 2) ${theme.spacing.xs}px`,
    position: "absolute",
    whiteSpace: multiline ? "unset" : "nowrap",
    pointerEvents: "none",
    width
  }),
  arrow: {
    backgroundColor: "inherit",
    border: 0,
    zIndex: 1
  }
}));
var Tooltip_styles_default = useStyles79;

// node_modules/@mantine/core/esm/Tooltip/Tooltip.errors.js
var TOOLTIP_ERRORS = {
  children: "Tooltip component children should be an element or a component that accepts ref, fragments, strings, numbers and other primitive values are not supported"
};

// node_modules/@mantine/core/esm/Tooltip/TooltipFloating/use-floating-tooltip.js
var import_react240 = __toESM(require_react());
function useFloatingTooltip({
  offset: offset3,
  position: position2
}) {
  const [opened, setOpened] = (0, import_react240.useState)(false);
  const boundaryRef = (0, import_react240.useRef)();
  const { x, y, reference: reference2, floating, refs, update, placement } = useFloating2({
    placement: position2,
    middleware: [
      shift({
        crossAxis: true,
        padding: 5,
        rootBoundary: "document"
      })
    ]
  });
  const horizontalOffset = placement.includes("right") ? offset3 : position2.includes("left") ? offset3 * -1 : 0;
  const verticalOffset = placement.includes("bottom") ? offset3 : position2.includes("top") ? offset3 * -1 : 0;
  const handleMouseMove = (0, import_react240.useCallback)(({ clientX, clientY }) => {
    reference2({
      getBoundingClientRect() {
        return {
          width: 0,
          height: 0,
          x: clientX,
          y: clientY,
          left: clientX + horizontalOffset,
          top: clientY + verticalOffset,
          right: clientX,
          bottom: clientY
        };
      }
    });
  }, [reference2]);
  (0, import_react240.useEffect)(() => {
    if (refs.floating.current) {
      const boundary = boundaryRef.current;
      boundary.addEventListener("mousemove", handleMouseMove);
      const parents = getOverflowAncestors(refs.floating.current);
      parents.forEach((parent) => {
        parent.addEventListener("scroll", update);
      });
      return () => {
        boundary.removeEventListener("mousemove", handleMouseMove);
        parents.forEach((parent) => {
          parent.removeEventListener("scroll", update);
        });
      };
    }
    return void 0;
  }, [reference2, refs.floating.current, update, handleMouseMove, opened]);
  return { handleMouseMove, x, y, opened, setOpened, boundaryRef, floating };
}

// node_modules/@mantine/core/esm/Tooltip/TooltipFloating/TooltipFloating.js
var __defProp180 = Object.defineProperty;
var __defProps79 = Object.defineProperties;
var __getOwnPropDescs79 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols182 = Object.getOwnPropertySymbols;
var __hasOwnProp182 = Object.prototype.hasOwnProperty;
var __propIsEnum182 = Object.prototype.propertyIsEnumerable;
var __defNormalProp180 = (obj, key, value) => key in obj ? __defProp180(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues180 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp182.call(b2, prop))
      __defNormalProp180(a2, prop, b2[prop]);
  if (__getOwnPropSymbols182)
    for (var prop of __getOwnPropSymbols182(b2)) {
      if (__propIsEnum182.call(b2, prop))
        __defNormalProp180(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps79 = (a2, b2) => __defProps79(a2, __getOwnPropDescs79(b2));
var __objRest117 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp182.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols182)
    for (var prop of __getOwnPropSymbols182(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum182.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps88 = {
  refProp: "ref",
  withinPortal: true,
  offset: 10,
  position: "right",
  zIndex: getDefaultZIndex("popover")
};
function TooltipFloating(props) {
  var _b;
  const _a = useComponentDefaultProps("TooltipFloating", defaultProps88, props), {
    children,
    refProp,
    withinPortal,
    style,
    className,
    classNames,
    styles: styles2,
    unstyled,
    radius: radius2,
    color,
    label,
    offset: offset3,
    position: position2,
    multiline,
    width,
    zIndex,
    disabled
  } = _a, others = __objRest117(_a, [
    "children",
    "refProp",
    "withinPortal",
    "style",
    "className",
    "classNames",
    "styles",
    "unstyled",
    "radius",
    "color",
    "label",
    "offset",
    "position",
    "multiline",
    "width",
    "zIndex",
    "disabled"
  ]);
  const { handleMouseMove, x, y, opened, boundaryRef, floating, setOpened } = useFloatingTooltip({
    offset: offset3,
    position: position2
  });
  const { classes, cx } = Tooltip_styles_default({ radius: radius2, color, multiline, width }, { name: "TooltipFloating", classNames, styles: styles2, unstyled });
  if (!isElement(children)) {
    throw new Error(TOOLTIP_ERRORS.children);
  }
  const targetRef = useMergedRef(boundaryRef, children.ref);
  const onMouseEnter = (event) => {
    var _a2, _b2;
    (_b2 = (_a2 = children.props).onMouseEnter) == null ? void 0 : _b2.call(_a2, event);
    handleMouseMove(event);
    setOpened(true);
  };
  const onMouseLeave = (event) => {
    var _a2, _b2;
    (_b2 = (_a2 = children.props).onMouseLeave) == null ? void 0 : _b2.call(_a2, event);
    setOpened(false);
  };
  return import_react242.default.createElement(import_react242.default.Fragment, null, import_react242.default.createElement(OptionalPortal, {
    withinPortal
  }, import_react242.default.createElement(Box, __spreadProps79(__spreadValues180({}, others), {
    ref: floating,
    className: cx(classes.tooltip, className),
    style: __spreadProps79(__spreadValues180({}, style), {
      zIndex,
      display: !disabled && opened ? "block" : "none",
      top: y != null ? y : "",
      left: (_b = Math.round(x)) != null ? _b : ""
    })
  }), label)), (0, import_react242.cloneElement)(children, __spreadProps79(__spreadValues180({}, children.props), {
    [refProp]: targetRef,
    onMouseEnter,
    onMouseLeave
  })));
}
TooltipFloating.displayName = "@mantine/core/TooltipFloating";

// node_modules/@mantine/core/esm/Tooltip/use-tooltip.js
var import_react243 = __toESM(require_react());
function useTooltip(settings) {
  const [uncontrolledOpened, setUncontrolledOpened] = (0, import_react243.useState)(false);
  const controlled = typeof settings.opened === "boolean";
  const opened = controlled ? settings.opened : uncontrolledOpened;
  const withinGroup = useTooltipGroupContext();
  const uid = useId();
  const { delay: groupDelay, currentId, setCurrentId } = useDelayGroupContext();
  const onChange = (0, import_react243.useCallback)((_opened) => {
    setUncontrolledOpened(_opened);
    if (_opened) {
      setCurrentId(uid);
    }
  }, [setCurrentId, uid]);
  const {
    x,
    y,
    reference: reference2,
    floating,
    context,
    refs,
    update,
    placement,
    middlewareData: { arrow: { x: arrowX, y: arrowY } = {} }
  } = useFloating2({
    placement: settings.position,
    open: opened,
    onOpenChange: onChange,
    middleware: [
      offset(settings.offset),
      shift({ padding: 8 }),
      flip(),
      arrow2({ element: settings.arrowRef, padding: settings.arrowOffset }),
      ...settings.inline ? [inline()] : []
    ]
  });
  const { getReferenceProps, getFloatingProps } = useInteractions([
    useHover2(context, {
      enabled: settings.events.hover,
      delay: withinGroup ? groupDelay : { open: settings.openDelay, close: settings.closeDelay },
      mouseOnly: !settings.events.touch
    }),
    useFocus(context, { enabled: settings.events.focus, keyboardOnly: true }),
    useRole(context, { role: "tooltip" }),
    useDismiss(context, { enabled: typeof settings.opened === void 0 }),
    useDelayGroup(context, { id: uid })
  ]);
  useFloatingAutoUpdate({
    opened,
    positionDependencies: settings.positionDependencies,
    floating: { refs, update }
  });
  useDidUpdate(() => {
    var _a;
    (_a = settings.onPositionChange) == null ? void 0 : _a.call(settings, placement);
  }, [placement]);
  const isGroupPhase = opened && currentId && currentId !== uid;
  return {
    x,
    y,
    arrowX,
    arrowY,
    reference: reference2,
    floating,
    getFloatingProps,
    getReferenceProps,
    isGroupPhase,
    opened,
    placement
  };
}

// node_modules/@mantine/core/esm/Tooltip/Tooltip.js
var __defProp181 = Object.defineProperty;
var __defProps80 = Object.defineProperties;
var __getOwnPropDescs80 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols183 = Object.getOwnPropertySymbols;
var __hasOwnProp183 = Object.prototype.hasOwnProperty;
var __propIsEnum183 = Object.prototype.propertyIsEnumerable;
var __defNormalProp181 = (obj, key, value) => key in obj ? __defProp181(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues181 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp183.call(b2, prop))
      __defNormalProp181(a2, prop, b2[prop]);
  if (__getOwnPropSymbols183)
    for (var prop of __getOwnPropSymbols183(b2)) {
      if (__propIsEnum183.call(b2, prop))
        __defNormalProp181(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps80 = (a2, b2) => __defProps80(a2, __getOwnPropDescs80(b2));
var __objRest118 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp183.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols183)
    for (var prop of __getOwnPropSymbols183(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum183.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps89 = {
  position: "top",
  refProp: "ref",
  withinPortal: false,
  inline: false,
  arrowSize: 4,
  arrowOffset: 5,
  arrowRadius: 0,
  arrowPosition: "side",
  offset: 5,
  transition: "fade",
  transitionDuration: 100,
  width: "auto",
  events: { hover: true, focus: false, touch: false },
  zIndex: getDefaultZIndex("popover"),
  positionDependencies: []
};
var _Tooltip = (0, import_react245.forwardRef)((props, ref) => {
  const arrowRef = (0, import_react245.useRef)(null);
  const _a = useComponentDefaultProps("Tooltip", defaultProps89, props), {
    children,
    position: position2,
    refProp,
    label,
    openDelay,
    closeDelay,
    onPositionChange,
    opened,
    withinPortal,
    radius: radius2,
    color,
    classNames,
    styles: styles2,
    unstyled,
    style,
    className,
    withArrow,
    arrowSize,
    arrowOffset,
    arrowRadius,
    arrowPosition,
    offset: offset3,
    transition,
    transitionDuration,
    multiline,
    width,
    events,
    zIndex,
    disabled,
    positionDependencies,
    onClick,
    onMouseEnter,
    onMouseLeave,
    inline: inline2
  } = _a, others = __objRest118(_a, [
    "children",
    "position",
    "refProp",
    "label",
    "openDelay",
    "closeDelay",
    "onPositionChange",
    "opened",
    "withinPortal",
    "radius",
    "color",
    "classNames",
    "styles",
    "unstyled",
    "style",
    "className",
    "withArrow",
    "arrowSize",
    "arrowOffset",
    "arrowRadius",
    "arrowPosition",
    "offset",
    "transition",
    "transitionDuration",
    "multiline",
    "width",
    "events",
    "zIndex",
    "disabled",
    "positionDependencies",
    "onClick",
    "onMouseEnter",
    "onMouseLeave",
    "inline"
  ]);
  const { classes, cx, theme } = Tooltip_styles_default({ radius: radius2, color, width, multiline }, { name: "Tooltip", classNames, styles: styles2, unstyled });
  const tooltip = useTooltip({
    position: getFloatingPosition(theme.dir, position2),
    closeDelay,
    openDelay,
    onPositionChange,
    opened,
    events,
    arrowRef,
    arrowOffset,
    offset: offset3 + (withArrow ? arrowSize / 2 : 0),
    positionDependencies: [...positionDependencies, children],
    inline: inline2
  });
  if (!isElement(children)) {
    throw new Error(TOOLTIP_ERRORS.children);
  }
  const targetRef = useMergedRef(tooltip.reference, children.ref, ref);
  return import_react245.default.createElement(import_react245.default.Fragment, null, import_react245.default.createElement(OptionalPortal, {
    withinPortal
  }, import_react245.default.createElement(Transition, {
    mounted: !disabled && tooltip.opened,
    transition,
    duration: tooltip.isGroupPhase ? 10 : transitionDuration
  }, (transitionStyles) => {
    var _a2, _b;
    return import_react245.default.createElement(Box, __spreadValues181(__spreadValues181({}, others), tooltip.getFloatingProps({
      ref: tooltip.floating,
      className: classes.tooltip,
      style: __spreadProps80(__spreadValues181(__spreadValues181({}, style), transitionStyles), {
        zIndex,
        top: (_a2 = tooltip.y) != null ? _a2 : 0,
        left: (_b = tooltip.x) != null ? _b : 0
      })
    })), label, import_react245.default.createElement(FloatingArrow, {
      ref: arrowRef,
      arrowX: tooltip.arrowX,
      arrowY: tooltip.arrowY,
      visible: withArrow,
      withBorder: false,
      position: tooltip.placement,
      arrowSize,
      arrowOffset,
      arrowRadius,
      arrowPosition,
      className: classes.arrow
    }));
  })), (0, import_react245.cloneElement)(children, tooltip.getReferenceProps(__spreadValues181({
    onClick,
    onMouseEnter,
    onMouseLeave,
    onMouseMove: props.onMouseMove,
    onPointerDown: props.onPointerDown,
    onPointerEnter: props.onPointerEnter,
    [refProp]: targetRef,
    className: cx(className, children.props.className)
  }, children.props))));
});
_Tooltip.Group = TooltipGroup;
_Tooltip.Floating = TooltipFloating;
_Tooltip.displayName = "@mantine/core/Tooltip";
var Tooltip = _Tooltip;

// node_modules/@mantine/core/esm/Progress/Progress.js
var __defProp182 = Object.defineProperty;
var __defProps81 = Object.defineProperties;
var __getOwnPropDescs81 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols184 = Object.getOwnPropertySymbols;
var __hasOwnProp184 = Object.prototype.hasOwnProperty;
var __propIsEnum184 = Object.prototype.propertyIsEnumerable;
var __defNormalProp182 = (obj, key, value) => key in obj ? __defProp182(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues182 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp184.call(b2, prop))
      __defNormalProp182(a2, prop, b2[prop]);
  if (__getOwnPropSymbols184)
    for (var prop of __getOwnPropSymbols184(b2)) {
      if (__propIsEnum184.call(b2, prop))
        __defNormalProp182(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps81 = (a2, b2) => __defProps81(a2, __getOwnPropDescs81(b2));
var __objRest119 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp184.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols184)
    for (var prop of __getOwnPropSymbols184(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum184.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
function getCumulativeSections(sections) {
  return sections.reduce((acc, section) => {
    acc.sections.push(__spreadProps81(__spreadValues182({}, section), { accumulated: acc.accumulated }));
    acc.accumulated += section.value;
    return acc;
  }, { accumulated: 0, sections: [] }).sections;
}
var defaultProps90 = {
  size: "md",
  radius: "sm",
  striped: false,
  animate: false,
  label: ""
};
var Progress = (0, import_react246.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("Progress", defaultProps90, props), {
    className,
    value,
    color,
    size: size3,
    radius: radius2,
    striped,
    animate,
    label,
    "aria-label": ariaLabel,
    classNames,
    styles: styles2,
    sections,
    unstyled
  } = _a, others = __objRest119(_a, [
    "className",
    "value",
    "color",
    "size",
    "radius",
    "striped",
    "animate",
    "label",
    "aria-label",
    "classNames",
    "styles",
    "sections",
    "unstyled"
  ]);
  const { classes, cx, theme } = Progress_styles_default({ color, size: size3, radius: radius2, striped: striped || animate, animate }, { classNames, styles: styles2, unstyled, name: "Progress" });
  const segments = Array.isArray(sections) ? getCumulativeSections(sections).map((_b, index6) => {
    var _c = _b, {
      tooltip,
      accumulated,
      value: sectionValue,
      label: sectionLabel,
      color: sectionColor
    } = _c, sectionProps = __objRest119(_c, [
      "tooltip",
      "accumulated",
      "value",
      "label",
      "color"
    ]);
    return import_react246.default.createElement(Tooltip.Floating, {
      label: tooltip,
      disabled: !tooltip,
      key: index6
    }, import_react246.default.createElement(Box, __spreadProps81(__spreadValues182({}, sectionProps), {
      className: cx(classes.bar, sectionProps.className),
      sx: {
        width: `${sectionValue}%`,
        left: `${accumulated}%`,
        backgroundColor: theme.fn.variant({
          variant: "filled",
          primaryFallback: false,
          color: sectionColor || theme.primaryColor
        }).background
      }
    }), sectionLabel && import_react246.default.createElement(Text, {
      className: classes.label
    }, sectionLabel)));
  }) : null;
  return import_react246.default.createElement(Box, __spreadValues182({
    className: cx(classes.root, className),
    ref
  }, others), segments || import_react246.default.createElement("div", {
    role: "progressbar",
    "aria-valuemax": 100,
    "aria-valuemin": 0,
    "aria-valuenow": value,
    "aria-label": ariaLabel,
    className: classes.bar,
    style: { width: `${value}%` }
  }, label ? import_react246.default.createElement(Text, {
    className: classes.label
  }, label) : ""));
});
Progress.displayName = "@mantine/core/Progress";

// node_modules/@mantine/core/esm/Radio/Radio.js
var import_react250 = __toESM(require_react());

// node_modules/@mantine/core/esm/Radio/RadioIcon.js
var import_react247 = __toESM(require_react());
var __defProp183 = Object.defineProperty;
var __getOwnPropSymbols185 = Object.getOwnPropertySymbols;
var __hasOwnProp185 = Object.prototype.hasOwnProperty;
var __propIsEnum185 = Object.prototype.propertyIsEnumerable;
var __defNormalProp183 = (obj, key, value) => key in obj ? __defProp183(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues183 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp185.call(b2, prop))
      __defNormalProp183(a2, prop, b2[prop]);
  if (__getOwnPropSymbols185)
    for (var prop of __getOwnPropSymbols185(b2)) {
      if (__propIsEnum185.call(b2, prop))
        __defNormalProp183(a2, prop, b2[prop]);
    }
  return a2;
};
function RadioIcon(props) {
  return import_react247.default.createElement("svg", __spreadValues183({
    xmlns: "http://www.w3.org/2000/svg",
    fill: "none",
    viewBox: "0 0 5 5"
  }, props), import_react247.default.createElement("path", {
    fill: "currentColor",
    d: "M0 2.5a2.5 2.5 0 115 0 2.5 2.5 0 01-5 0z"
  }));
}

// node_modules/@mantine/core/esm/Radio/RadioGroup.context.js
var import_react248 = __toESM(require_react());
var RadioGroupContext = (0, import_react248.createContext)(null);
var RadioGroupProvider = RadioGroupContext.Provider;
var useRadioGroupContext = () => (0, import_react248.useContext)(RadioGroupContext);

// node_modules/@mantine/core/esm/Radio/RadioGroup/RadioGroup.js
var import_react249 = __toESM(require_react());
var __defProp184 = Object.defineProperty;
var __getOwnPropSymbols186 = Object.getOwnPropertySymbols;
var __hasOwnProp186 = Object.prototype.hasOwnProperty;
var __propIsEnum186 = Object.prototype.propertyIsEnumerable;
var __defNormalProp184 = (obj, key, value) => key in obj ? __defProp184(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues184 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp186.call(b2, prop))
      __defNormalProp184(a2, prop, b2[prop]);
  if (__getOwnPropSymbols186)
    for (var prop of __getOwnPropSymbols186(b2)) {
      if (__propIsEnum186.call(b2, prop))
        __defNormalProp184(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest120 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp186.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols186)
    for (var prop of __getOwnPropSymbols186(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum186.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps91 = {
  orientation: "horizontal",
  spacing: "lg",
  offset: "xs",
  size: "sm"
};
var RadioGroup = (0, import_react249.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("RadioGroup", defaultProps91, props), {
    children,
    value,
    defaultValue,
    onChange,
    orientation,
    spacing,
    size: size3,
    wrapperProps,
    unstyled,
    offset: offset3,
    name
  } = _a, others = __objRest120(_a, [
    "children",
    "value",
    "defaultValue",
    "onChange",
    "orientation",
    "spacing",
    "size",
    "wrapperProps",
    "unstyled",
    "offset",
    "name"
  ]);
  const _name = useId(name);
  const [_value, setValue] = useUncontrolled({
    value,
    defaultValue,
    finalValue: "",
    onChange
  });
  const handleChange = (event) => setValue(event.currentTarget.value);
  return import_react249.default.createElement(RadioGroupProvider, {
    value: { value: _value, onChange: handleChange, size: size3, name: _name }
  }, import_react249.default.createElement(Input.Wrapper, __spreadValues184(__spreadValues184({
    labelElement: "div",
    size: size3,
    __staticSelector: "RadioGroup",
    ref,
    unstyled
  }, wrapperProps), others), import_react249.default.createElement(InputsGroup, {
    spacing,
    orientation,
    unstyled,
    role: "radiogroup",
    offset: offset3
  }, children)));
});
RadioGroup.displayName = "@mantine/core/RadioGroup";

// node_modules/@mantine/core/esm/Radio/Radio.styles.js
var __defProp185 = Object.defineProperty;
var __defProps82 = Object.defineProperties;
var __getOwnPropDescs82 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols187 = Object.getOwnPropertySymbols;
var __hasOwnProp187 = Object.prototype.hasOwnProperty;
var __propIsEnum187 = Object.prototype.propertyIsEnumerable;
var __defNormalProp185 = (obj, key, value) => key in obj ? __defProp185(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues185 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp187.call(b2, prop))
      __defNormalProp185(a2, prop, b2[prop]);
  if (__getOwnPropSymbols187)
    for (var prop of __getOwnPropSymbols187(b2)) {
      if (__propIsEnum187.call(b2, prop))
        __defNormalProp185(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps82 = (a2, b2) => __defProps82(a2, __getOwnPropDescs82(b2));
var sizes19 = {
  xs: 16,
  sm: 20,
  md: 24,
  lg: 30,
  xl: 36
};
var iconSizes6 = {
  xs: 6,
  sm: 8,
  md: 10,
  lg: 14,
  xl: 16
};
var useStyles80 = createStyles((theme, { size: size3, color, transitionDuration, labelPosition, error }, getRef2) => {
  const colors = theme.fn.variant({ variant: "filled", color });
  const errorColor = theme.fn.variant({ variant: "filled", color: "red" }).background;
  return {
    inner: {
      order: labelPosition === "left" ? 2 : 1,
      position: "relative",
      alignSelf: "flex-start"
    },
    icon: {
      ref: getRef2("icon"),
      color: theme.white,
      opacity: 0,
      transform: "scale(0.75) translateY(2px)",
      transition: `opacity ${transitionDuration}ms ${theme.transitionTimingFunction}`,
      pointerEvents: "none",
      width: theme.fn.size({ sizes: iconSizes6, size: size3 }),
      height: theme.fn.size({ sizes: iconSizes6, size: size3 }),
      position: "absolute",
      top: `calc(50% - ${theme.fn.size({ sizes: iconSizes6, size: size3 }) / 2}px)`,
      left: `calc(50% - ${theme.fn.size({ sizes: iconSizes6, size: size3 }) / 2}px)`
    },
    radio: __spreadProps82(__spreadValues185({}, theme.fn.focusStyles()), {
      backgroundColor: theme.colorScheme === "dark" ? theme.colors.dark[6] : theme.white,
      border: `1px solid ${error ? errorColor : theme.colorScheme === "dark" ? theme.colors.dark[4] : theme.colors.gray[4]}`,
      position: "relative",
      appearance: "none",
      width: theme.fn.size({ sizes: sizes19, size: size3 }),
      height: theme.fn.size({ sizes: sizes19, size: size3 }),
      borderRadius: theme.fn.size({ sizes: sizes19, size: size3 }),
      margin: 0,
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      transitionProperty: "background-color, border-color",
      transitionTimingFunction: theme.transitionTimingFunction,
      transitionDuration: `${transitionDuration}ms`,
      cursor: theme.cursorType,
      "&:checked": {
        background: colors.background,
        borderColor: colors.background,
        [`& + .${getRef2("icon")}`]: {
          opacity: 1,
          transform: "scale(1)"
        }
      },
      "&:disabled": {
        borderColor: theme.colorScheme === "dark" ? theme.colors.dark[5] : theme.colors.gray[4],
        backgroundColor: theme.colorScheme === "dark" ? theme.colors.dark[5] : theme.colors.gray[1],
        [`& + .${getRef2("icon")}`]: {
          color: theme.colorScheme === "dark" ? theme.colors.dark[6] : theme.colors.gray[4]
        }
      }
    })
  };
});
var Radio_styles_default = useStyles80;

// node_modules/@mantine/core/esm/Radio/Radio.js
var __defProp186 = Object.defineProperty;
var __getOwnPropSymbols188 = Object.getOwnPropertySymbols;
var __hasOwnProp188 = Object.prototype.hasOwnProperty;
var __propIsEnum188 = Object.prototype.propertyIsEnumerable;
var __defNormalProp186 = (obj, key, value) => key in obj ? __defProp186(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues186 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp188.call(b2, prop))
      __defNormalProp186(a2, prop, b2[prop]);
  if (__getOwnPropSymbols188)
    for (var prop of __getOwnPropSymbols188(b2)) {
      if (__propIsEnum188.call(b2, prop))
        __defNormalProp186(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest121 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp188.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols188)
    for (var prop of __getOwnPropSymbols188(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum188.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps92 = {
  icon: RadioIcon,
  transitionDuration: 100,
  size: "sm",
  labelPosition: "right"
};
var Radio = (0, import_react250.forwardRef)((props, ref) => {
  var _b, _c;
  const _a = useComponentDefaultProps("Radio", defaultProps92, props), {
    className,
    style,
    id,
    label,
    size: size3,
    title,
    disabled,
    color,
    classNames,
    styles: styles2,
    sx,
    icon: Icon,
    transitionDuration,
    wrapperProps,
    unstyled,
    labelPosition,
    description,
    error
  } = _a, others = __objRest121(_a, [
    "className",
    "style",
    "id",
    "label",
    "size",
    "title",
    "disabled",
    "color",
    "classNames",
    "styles",
    "sx",
    "icon",
    "transitionDuration",
    "wrapperProps",
    "unstyled",
    "labelPosition",
    "description",
    "error"
  ]);
  const ctx = useRadioGroupContext();
  const contextSize = (_b = ctx == null ? void 0 : ctx.size) != null ? _b : size3;
  const componentSize = props.size ? size3 : contextSize;
  const { classes } = Radio_styles_default({ color, size: componentSize, transitionDuration, labelPosition, error: !!error }, { classNames, styles: styles2, unstyled, name: "Radio" });
  const { systemStyles, rest } = extractSystemStyles(others);
  const uuid = useId(id);
  const contextProps = ctx ? {
    checked: ctx.value === rest.value,
    name: (_c = rest.name) != null ? _c : ctx.name,
    onChange: ctx.onChange
  } : {};
  return import_react250.default.createElement(InlineInput, __spreadValues186(__spreadValues186({
    className,
    sx,
    style,
    id: uuid,
    size: componentSize,
    labelPosition,
    label,
    description,
    error,
    disabled,
    __staticSelector: "Radio",
    classNames,
    styles: styles2,
    unstyled,
    "data-checked": contextProps.checked || void 0
  }, systemStyles), wrapperProps), import_react250.default.createElement("div", {
    className: classes.inner
  }, import_react250.default.createElement("input", __spreadValues186(__spreadValues186({
    ref,
    className: classes.radio,
    type: "radio",
    id: uuid,
    disabled
  }, rest), contextProps)), import_react250.default.createElement(Icon, {
    className: classes.icon,
    "aria-hidden": true
  })));
});
Radio.displayName = "@mantine/core/Radio";
Radio.Group = RadioGroup;

// node_modules/@mantine/core/esm/Rating/Rating.js
var import_react254 = __toESM(require_react());

// node_modules/@mantine/core/esm/Rating/RatingItem/RatingItem.js
var import_react253 = __toESM(require_react());

// node_modules/@mantine/core/esm/Rating/StarSymbol/StarSymbol.js
var import_react252 = __toESM(require_react());

// node_modules/@mantine/core/esm/Rating/StarSymbol/StarIcon.js
var import_react251 = __toESM(require_react());
var __defProp187 = Object.defineProperty;
var __getOwnPropSymbols189 = Object.getOwnPropertySymbols;
var __hasOwnProp189 = Object.prototype.hasOwnProperty;
var __propIsEnum189 = Object.prototype.propertyIsEnumerable;
var __defNormalProp187 = (obj, key, value) => key in obj ? __defProp187(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues187 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp189.call(b2, prop))
      __defNormalProp187(a2, prop, b2[prop]);
  if (__getOwnPropSymbols189)
    for (var prop of __getOwnPropSymbols189(b2)) {
      if (__propIsEnum189.call(b2, prop))
        __defNormalProp187(a2, prop, b2[prop]);
    }
  return a2;
};
function StarIcon(props) {
  return import_react251.default.createElement("svg", __spreadValues187({
    viewBox: "0 0 24 24",
    strokeLinecap: "round",
    strokeLinejoin: "round",
    fill: "none",
    xmlns: "http://www.w3.org/2000/svg"
  }, props), import_react251.default.createElement("path", {
    d: "M12 17.75l-6.172 3.245l1.179 -6.873l-5 -4.867l6.9 -1l3.086 -6.253l3.086 6.253l6.9 1l-5 4.867l1.179 6.873z"
  }));
}
StarIcon.displayName = "@mantine/core/StarIcon";

// node_modules/@mantine/core/esm/Rating/StarSymbol/StarSymbol.styles.js
var sizes20 = {
  xs: 14,
  sm: 18,
  md: 20,
  lg: 28,
  xl: 32
};
var useStyles81 = createStyles((theme, { size: size3, type, color }) => {
  const emptyColor = theme.colorScheme === "light" ? theme.colors.gray[3] : theme.colors.gray[8];
  const fullColor = theme.fn.variant({ variant: "filled", color }).background;
  return {
    icon: {
      stroke: type === "empty" ? emptyColor : fullColor,
      fill: type === "empty" ? emptyColor : fullColor,
      width: theme.fn.size({ size: size3, sizes: sizes20 }),
      height: theme.fn.size({ size: size3, sizes: sizes20 })
    }
  };
});
var StarSymbol_styles_default = useStyles81;

// node_modules/@mantine/core/esm/Rating/StarSymbol/StarSymbol.js
function StarSymbol({ size: size3, type, color }) {
  const { classes } = StarSymbol_styles_default({ size: size3, type, color });
  return import_react252.default.createElement(StarIcon, {
    className: classes.icon
  });
}
StarSymbol.displayName = "@mantine/core/StarSymbol";

// node_modules/@mantine/core/esm/Rating/RatingItem/RatingItem.styles.js
var useStyles82 = createStyles((theme, _params, getRef2) => ({
  input: {
    height: 0,
    width: 0,
    position: "absolute",
    overflow: "hidden",
    whiteSpace: "nowrap",
    opacity: 0,
    "&:focus": {
      [`& + .${getRef2("label")}`]: {
        outline: theme.focusRing === "always" || theme.focusRing === "auto" ? `1px solid ${theme.colors[theme.primaryColor][theme.colorScheme === "dark" ? 7 : 5]}` : void 0
      },
      "&:focus:not(:focus-visible)": {
        [`& + .${getRef2("label")}`]: {
          outline: theme.focusRing === "auto" || theme.focusRing === "never" ? "none" : void 0
        }
      }
    }
  },
  label: {
    ref: getRef2("label"),
    display: "block",
    boxSizing: "border-box",
    marginInline: 1,
    top: 0,
    left: 0,
    cursor: "pointer",
    position: "absolute",
    zIndex: 0,
    "&[data-read-only]": {
      cursor: "default"
    },
    "&:last-of-type": {
      position: "relative",
      zIndex: 0
    }
  },
  symbolBody: {
    display: "grid",
    placeContent: "center",
    placeItems: "center"
  }
}));
var RatingItem_styles_default = useStyles82;

// node_modules/@mantine/core/esm/Rating/RatingItem/RatingItem.js
var __defProp188 = Object.defineProperty;
var __getOwnPropSymbols190 = Object.getOwnPropertySymbols;
var __hasOwnProp190 = Object.prototype.hasOwnProperty;
var __propIsEnum190 = Object.prototype.propertyIsEnumerable;
var __defNormalProp188 = (obj, key, value) => key in obj ? __defProp188(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues188 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp190.call(b2, prop))
      __defNormalProp188(a2, prop, b2[prop]);
  if (__getOwnPropSymbols190)
    for (var prop of __getOwnPropSymbols190(b2)) {
      if (__propIsEnum190.call(b2, prop))
        __defNormalProp188(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest122 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp190.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols190)
    for (var prop of __getOwnPropSymbols190(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum190.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
function RatingItem(_a) {
  var _b = _a, {
    size: size3,
    getSymbolLabel,
    emptyIcon,
    fullIcon,
    full,
    active,
    value,
    readOnly,
    fractionValue,
    classNames,
    styles: styles2,
    unstyled,
    color,
    id
  } = _b, others = __objRest122(_b, [
    "size",
    "getSymbolLabel",
    "emptyIcon",
    "fullIcon",
    "full",
    "active",
    "value",
    "readOnly",
    "fractionValue",
    "classNames",
    "styles",
    "unstyled",
    "color",
    "id"
  ]);
  const { classes } = RatingItem_styles_default(null, { name: "Rating", classNames, styles: styles2, unstyled });
  const _fullIcon = typeof fullIcon === "function" ? fullIcon(value) : fullIcon;
  const _emptyIcon = typeof emptyIcon === "function" ? emptyIcon(value) : emptyIcon;
  return import_react253.default.createElement(import_react253.default.Fragment, null, !readOnly && import_react253.default.createElement("input", __spreadValues188({
    className: classes.input,
    id,
    type: "radio",
    "data-active": active,
    "aria-label": getSymbolLabel(value),
    value
  }, others)), import_react253.default.createElement(Box, {
    component: readOnly ? "div" : "label",
    className: classes.label,
    "data-read-only": readOnly || void 0,
    htmlFor: id,
    sx: fractionValue === 1 ? void 0 : { zIndex: active ? 2 : 0 }
  }, import_react253.default.createElement(Box, {
    className: classes.symbolBody,
    sx: fractionValue === 1 ? void 0 : { clipPath: `inset(0 ${active ? 100 - fractionValue * 100 : 100}% 0 0)` }
  }, full ? _fullIcon || import_react253.default.createElement(StarSymbol, {
    color,
    size: size3,
    type: "full"
  }) : _emptyIcon || import_react253.default.createElement(StarSymbol, {
    color,
    size: size3,
    type: "empty"
  }))));
}
RatingItem.displayName = "@mantine/core/RatingItem";

// node_modules/@mantine/core/esm/Rating/Rating.styles.js
var useStyles83 = createStyles(() => ({
  root: {
    display: "flex",
    width: "max-content"
  },
  symbolGroup: {
    position: "relative",
    transition: "transform 100ms ease",
    '&[data-active="true"]': {
      zIndex: 1,
      transform: "scale(1.2)"
    }
  }
}));
var Rating_styles_default = useStyles83;

// node_modules/@mantine/core/esm/Rating/Rating.js
var __defProp189 = Object.defineProperty;
var __getOwnPropSymbols191 = Object.getOwnPropertySymbols;
var __hasOwnProp191 = Object.prototype.hasOwnProperty;
var __propIsEnum191 = Object.prototype.propertyIsEnumerable;
var __defNormalProp189 = (obj, key, value) => key in obj ? __defProp189(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues189 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp191.call(b2, prop))
      __defNormalProp189(a2, prop, b2[prop]);
  if (__getOwnPropSymbols191)
    for (var prop of __getOwnPropSymbols191(b2)) {
      if (__propIsEnum191.call(b2, prop))
        __defNormalProp189(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest123 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp191.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols191)
    for (var prop of __getOwnPropSymbols191(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum191.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
function roundValueTo(value, to2) {
  var _a;
  const rounded = Math.round(value / to2) * to2;
  const precision = ((_a = `${to2}`.split(".")[1]) == null ? void 0 : _a.length) || 0;
  return Number(rounded.toFixed(precision));
}
var defaultProps93 = {
  size: "sm",
  getSymbolLabel: (value) => `${value}`,
  count: 5,
  fractions: 1,
  color: "yellow"
};
var Rating = (0, import_react254.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("Rating", defaultProps93, props), {
    defaultValue,
    value,
    emptySymbol,
    fullSymbol,
    size: size3,
    count: count2,
    fractions,
    onChange,
    onHover,
    getSymbolLabel,
    name,
    readOnly,
    className,
    classNames,
    styles: styles2,
    unstyled,
    onMouseEnter,
    onMouseMove,
    onMouseLeave,
    highlightSelectedOnly,
    color,
    id
  } = _a, others = __objRest123(_a, [
    "defaultValue",
    "value",
    "emptySymbol",
    "fullSymbol",
    "size",
    "count",
    "fractions",
    "onChange",
    "onHover",
    "getSymbolLabel",
    "name",
    "readOnly",
    "className",
    "classNames",
    "styles",
    "unstyled",
    "onMouseEnter",
    "onMouseMove",
    "onMouseLeave",
    "highlightSelectedOnly",
    "color",
    "id"
  ]);
  const { classes, cx, theme } = Rating_styles_default(null, { name: "Rating", classNames, styles: styles2, unstyled });
  const _name = useId(name);
  const _id = useId(id);
  const rootRef = (0, import_react254.useRef)(null);
  const [_value, setValue] = useUncontrolled({
    value,
    defaultValue,
    finalValue: 0,
    onChange
  });
  const [hovered, setHovered] = (0, import_react254.useState)(-1);
  const [isOutside, setOutside] = (0, import_react254.useState)(true);
  const _fractions = Math.floor(fractions);
  const _count = Math.floor(count2);
  const decimalUnit = 1 / _fractions;
  const stableValueRounded = roundValueTo(_value, decimalUnit);
  const finalValue = hovered !== -1 ? hovered : stableValueRounded;
  const handleMouseEnter = (event) => {
    onMouseEnter == null ? void 0 : onMouseEnter(event);
    !readOnly && setOutside(false);
  };
  const handleMouseMove = (event) => {
    onMouseMove == null ? void 0 : onMouseMove(event);
    if (readOnly) {
      return;
    }
    const { left: left2, right: right2, width } = rootRef.current.getBoundingClientRect();
    const symbolWidth = width / _count;
    const hoverPosition = theme.dir === "rtl" ? right2 - event.clientX : event.clientX - left2;
    const hoverValue = hoverPosition / symbolWidth;
    const rounded = clamp(roundValueTo(hoverValue + decimalUnit / 2, decimalUnit), decimalUnit, _count);
    setHovered(rounded);
    rounded !== hovered && (onHover == null ? void 0 : onHover(rounded));
  };
  const handleMouseLeave = (event) => {
    onMouseLeave == null ? void 0 : onMouseLeave(event);
    if (readOnly) {
      return;
    }
    setHovered(-1);
    setOutside(true);
    hovered !== -1 && (onHover == null ? void 0 : onHover(-1));
  };
  const handleItemBlur = () => isOutside && setHovered(-1);
  const handleChange = (event) => {
    const resultedValue = parseFloat(event.target.value);
    setValue(resultedValue);
  };
  const items = Array(_count).fill(0).map((_, index6) => {
    const integerValue = index6 + 1;
    const fractionItems = Array.from(new Array(index6 === 0 ? _fractions + 1 : _fractions));
    const isGroupActive = !readOnly && Math.ceil(hovered) === integerValue;
    return import_react254.default.createElement("div", {
      key: integerValue,
      "data-active": isGroupActive,
      className: classes.symbolGroup
    }, fractionItems.map((__, fractionIndex) => {
      const fractionValue = decimalUnit * (index6 === 0 ? fractionIndex : fractionIndex + 1);
      const symbolValue = roundValueTo(integerValue - 1 + fractionValue, decimalUnit);
      return import_react254.default.createElement(RatingItem, {
        key: `${integerValue}-${symbolValue}`,
        size: size3,
        getSymbolLabel,
        emptyIcon: emptySymbol,
        fullIcon: fullSymbol,
        full: highlightSelectedOnly ? symbolValue === finalValue : symbolValue <= finalValue,
        active: symbolValue === finalValue,
        checked: symbolValue === stableValueRounded,
        readOnly,
        fractionValue,
        value: symbolValue,
        name: _name,
        onChange: handleChange,
        onBlur: handleItemBlur,
        classNames,
        styles: styles2,
        unstyled,
        color,
        id: `${_id}-${index6}-${fractionIndex}`
      });
    }));
  });
  return import_react254.default.createElement(Box, __spreadValues189({
    ref: useMergedRef(rootRef, ref),
    className: cx(classes.root, className),
    onMouseMove: handleMouseMove,
    onMouseEnter: handleMouseEnter,
    onMouseLeave: handleMouseLeave
  }, others), items);
});
Rating.displayName = "@mantine/core/Rating";

// node_modules/@mantine/core/esm/RingProgress/RingProgress.js
var import_react256 = __toESM(require_react());

// node_modules/@mantine/core/esm/RingProgress/Curve/Curve.js
var import_react255 = __toESM(require_react());

// node_modules/@mantine/core/esm/RingProgress/Curve/get-curve-props.js
function getCurveProps({ size: size3, thickness, sum, value, root, offset: offset3 }) {
  const radius2 = (size3 * 0.9 - thickness * 2) / 2;
  const deg = Math.PI * radius2 * 2 / 100;
  const strokeDasharray = root ? `${(100 - sum) * deg}, ${sum * deg}` : `${value * deg}, ${(100 - value) * deg}`;
  return {
    strokeWidth: thickness,
    cx: size3 / 2,
    cy: size3 / 2,
    r: radius2,
    transform: root ? `scale(1, -1) translate(0, -${size3})` : null,
    strokeDasharray,
    strokeDashoffset: root ? 0 : offset3
  };
}

// node_modules/@mantine/core/esm/RingProgress/Curve/Curve.js
var __defProp190 = Object.defineProperty;
var __defProps83 = Object.defineProperties;
var __getOwnPropDescs83 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols192 = Object.getOwnPropertySymbols;
var __hasOwnProp192 = Object.prototype.hasOwnProperty;
var __propIsEnum192 = Object.prototype.propertyIsEnumerable;
var __defNormalProp190 = (obj, key, value) => key in obj ? __defProp190(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues190 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp192.call(b2, prop))
      __defNormalProp190(a2, prop, b2[prop]);
  if (__getOwnPropSymbols192)
    for (var prop of __getOwnPropSymbols192(b2)) {
      if (__propIsEnum192.call(b2, prop))
        __defNormalProp190(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps83 = (a2, b2) => __defProps83(a2, __getOwnPropDescs83(b2));
var __objRest124 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp192.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols192)
    for (var prop of __getOwnPropSymbols192(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum192.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
function Curve(_a) {
  var _b = _a, {
    size: size3,
    value,
    offset: offset3,
    sum,
    thickness,
    root,
    color,
    lineRoundCaps,
    tooltip
  } = _b, others = __objRest124(_b, [
    "size",
    "value",
    "offset",
    "sum",
    "thickness",
    "root",
    "color",
    "lineRoundCaps",
    "tooltip"
  ]);
  const theme = useMantineTheme();
  const stroke = theme.fn.themeColor(color || (theme.colorScheme === "dark" ? "dark" : "gray"), color ? theme.fn.primaryShade() : theme.colorScheme === "dark" ? 4 : 1, false);
  return import_react255.default.createElement(Tooltip.Floating, {
    disabled: !tooltip,
    label: tooltip
  }, import_react255.default.createElement("circle", __spreadValues190(__spreadProps83(__spreadValues190({}, others), {
    fill: "none",
    strokeLinecap: lineRoundCaps ? "round" : "butt",
    stroke
  }), getCurveProps({ sum, size: size3, thickness, value, offset: offset3, root }))));
}
Curve.displayName = "@mantine/core/Curve";

// node_modules/@mantine/core/esm/RingProgress/get-curves/get-curves.js
var __defProp191 = Object.defineProperty;
var __defProps84 = Object.defineProperties;
var __getOwnPropDescs84 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols193 = Object.getOwnPropertySymbols;
var __hasOwnProp193 = Object.prototype.hasOwnProperty;
var __propIsEnum193 = Object.prototype.propertyIsEnumerable;
var __defNormalProp191 = (obj, key, value) => key in obj ? __defProp191(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues191 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp193.call(b2, prop))
      __defNormalProp191(a2, prop, b2[prop]);
  if (__getOwnPropSymbols193)
    for (var prop of __getOwnPropSymbols193(b2)) {
      if (__propIsEnum193.call(b2, prop))
        __defNormalProp191(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps84 = (a2, b2) => __defProps84(a2, __getOwnPropDescs84(b2));
function getCurves({
  size: size3,
  thickness,
  sections,
  renderRoundedLineCaps,
  rootColor
}) {
  const sum = sections.reduce((acc, current) => acc + current.value, 0);
  const accumulated = Math.PI * ((size3 * 0.9 - thickness * 2) / 2) * 2;
  let offset3 = accumulated;
  const curves = [];
  const curvesInOrder = [];
  for (let i = 0; i < sections.length; i += 1) {
    curves.push({ sum, offset: offset3, data: sections[i], root: false });
    offset3 -= sections[i].value / 100 * accumulated;
  }
  curves.push({ sum, offset: offset3, data: { color: rootColor }, root: true });
  curvesInOrder.push(__spreadProps84(__spreadValues191({}, curves[curves.length - 1]), { lineRoundCaps: false }));
  if (curves.length > 2) {
    curvesInOrder.push(__spreadProps84(__spreadValues191({}, curves[0]), { lineRoundCaps: renderRoundedLineCaps }));
    curvesInOrder.push(__spreadProps84(__spreadValues191({}, curves[curves.length - 2]), { lineRoundCaps: renderRoundedLineCaps }));
    for (let i = 1; i <= curves.length - 3; i += 1) {
      curvesInOrder.push(__spreadProps84(__spreadValues191({}, curves[i]), { lineRoundCaps: false }));
    }
  } else {
    curvesInOrder.push(__spreadProps84(__spreadValues191({}, curves[0]), { lineRoundCaps: renderRoundedLineCaps }));
  }
  return curvesInOrder;
}

// node_modules/@mantine/core/esm/RingProgress/RingProgress.styles.js
var useStyles84 = createStyles({
  root: {
    position: "relative"
  },
  label: {
    position: "absolute",
    top: "50%",
    transform: "translateY(-50%)"
  }
});
var RingProgress_styles_default = useStyles84;

// node_modules/@mantine/core/esm/RingProgress/RingProgress.js
var __defProp192 = Object.defineProperty;
var __defProps85 = Object.defineProperties;
var __getOwnPropDescs85 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols194 = Object.getOwnPropertySymbols;
var __hasOwnProp194 = Object.prototype.hasOwnProperty;
var __propIsEnum194 = Object.prototype.propertyIsEnumerable;
var __defNormalProp192 = (obj, key, value) => key in obj ? __defProp192(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues192 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp194.call(b2, prop))
      __defNormalProp192(a2, prop, b2[prop]);
  if (__getOwnPropSymbols194)
    for (var prop of __getOwnPropSymbols194(b2)) {
      if (__propIsEnum194.call(b2, prop))
        __defNormalProp192(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps85 = (a2, b2) => __defProps85(a2, __getOwnPropDescs85(b2));
var __objRest125 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp194.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols194)
    for (var prop of __getOwnPropSymbols194(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum194.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps94 = {
  size: 120,
  thickness: 12
};
var RingProgress = (0, import_react256.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("RingProgress", defaultProps94, props), {
    className,
    style,
    label,
    sections,
    size: size3,
    thickness,
    classNames,
    styles: styles2,
    roundCaps,
    rootColor,
    unstyled
  } = _a, others = __objRest125(_a, [
    "className",
    "style",
    "label",
    "sections",
    "size",
    "thickness",
    "classNames",
    "styles",
    "roundCaps",
    "rootColor",
    "unstyled"
  ]);
  const { classes, cx } = RingProgress_styles_default(null, { classNames, styles: styles2, unstyled, name: "RingProgress" });
  const curves = getCurves({
    size: size3,
    thickness,
    sections,
    renderRoundedLineCaps: roundCaps,
    rootColor
  }).map(({ data, sum, root, lineRoundCaps, offset: offset3 }, index6) => import_react256.default.createElement(Curve, __spreadProps85(__spreadValues192({}, data), {
    key: index6,
    size: size3,
    thickness,
    sum,
    offset: offset3,
    color: data == null ? void 0 : data.color,
    root,
    lineRoundCaps
  })));
  return import_react256.default.createElement(Box, __spreadValues192({
    style: __spreadValues192({ width: size3, height: size3 }, style),
    className: cx(classes.root, className),
    ref
  }, others), import_react256.default.createElement("svg", {
    width: size3,
    height: size3,
    style: { transform: "rotate(-90deg)" }
  }, curves), label && import_react256.default.createElement("div", {
    className: classes.label,
    style: { right: thickness * 2, left: thickness * 2 }
  }, label));
});
RingProgress.displayName = "@mantine/core/RingProgress";

// node_modules/@mantine/core/esm/SegmentedControl/SegmentedControl.js
var import_react257 = __toESM(require_react());

// node_modules/@mantine/core/esm/SegmentedControl/SegmentedControl.styles.js
var __defProp193 = Object.defineProperty;
var __defProps86 = Object.defineProperties;
var __getOwnPropDescs86 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols195 = Object.getOwnPropertySymbols;
var __hasOwnProp195 = Object.prototype.hasOwnProperty;
var __propIsEnum195 = Object.prototype.propertyIsEnumerable;
var __defNormalProp193 = (obj, key, value) => key in obj ? __defProp193(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues193 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp195.call(b2, prop))
      __defNormalProp193(a2, prop, b2[prop]);
  if (__getOwnPropSymbols195)
    for (var prop of __getOwnPropSymbols195(b2)) {
      if (__propIsEnum195.call(b2, prop))
        __defNormalProp193(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps86 = (a2, b2) => __defProps86(a2, __getOwnPropDescs86(b2));
var WRAPPER_PADDING = 4;
var sizes21 = {
  xs: "3px 6px",
  sm: "5px 10px",
  md: "7px 14px",
  lg: "9px 16px",
  xl: "12px 20px"
};
var useStyles85 = createStyles((theme, {
  fullWidth,
  color,
  radius: radius2,
  shouldAnimate,
  transitionDuration,
  transitionTimingFunction,
  size: size3,
  orientation
}, getRef2) => {
  const vertical = orientation === "vertical";
  const colors = theme.fn.variant({ variant: "filled", color });
  return {
    label: __spreadProps86(__spreadValues193(__spreadValues193({
      ref: getRef2("label")
    }, theme.fn.focusStyles()), theme.fn.fontStyles()), {
      WebkitTapHighlightColor: "transparent",
      borderRadius: theme.fn.radius(radius2),
      fontWeight: 500,
      fontSize: size3 in theme.fontSizes ? theme.fontSizes[size3] : theme.fontSizes.sm,
      cursor: "pointer",
      display: "block",
      textAlign: "center",
      padding: sizes21[size3 in sizes21 ? size3 : "sm"],
      whiteSpace: "nowrap",
      overflow: "hidden",
      textOverflow: "ellipsis",
      userSelect: "none",
      color: theme.colorScheme === "dark" ? theme.colors.dark[1] : theme.colors.gray[7],
      transition: `color ${shouldAnimate ? 0 : transitionDuration}ms ${transitionTimingFunction || theme.transitionTimingFunction}`,
      "&:hover": {
        color: theme.colorScheme === "dark" ? theme.colors.dark[0] : theme.black
      }
    }),
    control: {
      ref: getRef2("control"),
      position: "relative",
      boxSizing: "border-box",
      flex: 1,
      zIndex: 2,
      transition: `border-left-color ${shouldAnimate ? 0 : transitionDuration}ms ${transitionTimingFunction || theme.transitionTimingFunction}`,
      "&:not(:first-of-type)": {
        borderStyle: "solid",
        borderWidth: vertical ? "1px 0 0 0" : "0 0 0 1px",
        borderColor: theme.colorScheme === "dark" ? theme.colors.dark[4] : theme.colors.gray[3]
      }
    },
    input: {
      height: 0,
      width: 0,
      position: "absolute",
      overflow: "hidden",
      whiteSpace: "nowrap",
      opacity: 0,
      "&:focus": {
        outline: "none",
        [`& + .${getRef2("label")}`]: __spreadValues193({}, theme.focusRing === "always" || theme.focusRing === "auto" ? theme.focusRingStyles.styles(theme) : theme.focusRingStyles.resetStyles(theme)),
        "&:focus:not(:focus-visible)": {
          [`& + .${getRef2("label")}`]: __spreadValues193({}, theme.focusRing === "auto" || theme.focusRing === "never" ? theme.focusRingStyles.resetStyles(theme) : null)
        }
      }
    },
    root: {
      position: "relative",
      display: fullWidth || vertical ? "flex" : "inline-flex",
      width: vertical && !fullWidth ? "max-content" : "auto",
      flexDirection: vertical ? "column" : "row",
      backgroundColor: theme.colorScheme === "dark" ? theme.colors.dark[8] : theme.colors.gray[1],
      borderRadius: theme.fn.radius(radius2),
      overflow: "hidden",
      padding: WRAPPER_PADDING
    },
    controlActive: {
      borderLeftColor: "transparent !important",
      borderTopColor: "transparent !important",
      [`& + .${getRef2("control")}`]: {
        borderLeftColor: "transparent !important",
        borderTopColor: "transparent !important"
      },
      borderRadius: theme.fn.radius(radius2),
      boxShadow: shouldAnimate ? color || theme.colorScheme === "dark" ? "none" : theme.shadows.xs : void 0,
      backgroundColor: shouldAnimate ? color ? colors.background : theme.colorScheme === "dark" ? theme.colors.dark[5] : theme.white : void 0
    },
    labelActive: {
      "&, &:hover": {
        color: color || theme.colorScheme === "dark" ? theme.white : theme.black
      }
    },
    disabled: {
      "&, &:hover": {
        color: theme.colorScheme === "dark" ? theme.colors.dark[3] : theme.colors.gray[5],
        cursor: "not-allowed"
      }
    },
    active: {
      boxSizing: "border-box",
      borderRadius: theme.fn.radius(radius2),
      position: "absolute",
      zIndex: 1,
      boxShadow: color || theme.colorScheme === "dark" ? "none" : theme.shadows.xs,
      transition: `transform ${shouldAnimate ? 0 : transitionDuration}ms ${theme.transitionTimingFunction}, width ${shouldAnimate ? 0 : transitionDuration / 2}ms ${transitionTimingFunction || theme.transitionTimingFunction}`,
      backgroundColor: color ? colors.background : theme.colorScheme === "dark" ? theme.colors.dark[5] : theme.white
    }
  };
});
var SegmentedControl_styles_default = useStyles85;

// node_modules/@mantine/core/esm/SegmentedControl/SegmentedControl.js
var __defProp194 = Object.defineProperty;
var __getOwnPropSymbols196 = Object.getOwnPropertySymbols;
var __hasOwnProp196 = Object.prototype.hasOwnProperty;
var __propIsEnum196 = Object.prototype.propertyIsEnumerable;
var __defNormalProp194 = (obj, key, value) => key in obj ? __defProp194(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues194 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp196.call(b2, prop))
      __defNormalProp194(a2, prop, b2[prop]);
  if (__getOwnPropSymbols196)
    for (var prop of __getOwnPropSymbols196(b2)) {
      if (__propIsEnum196.call(b2, prop))
        __defNormalProp194(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest126 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp196.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols196)
    for (var prop of __getOwnPropSymbols196(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum196.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps95 = {
  disabled: false,
  size: "sm",
  transitionDuration: 200
};
var SegmentedControl = (0, import_react257.forwardRef)((props, ref) => {
  var _b, _c, _d, _e2;
  const _a = useComponentDefaultProps("SegmentedControl", defaultProps95, props), {
    className,
    disabled,
    data: _data,
    name,
    value,
    onChange,
    color,
    fullWidth,
    radius: radius2,
    size: size3,
    transitionDuration,
    transitionTimingFunction,
    classNames,
    styles: styles2,
    defaultValue,
    orientation,
    unstyled
  } = _a, others = __objRest126(_a, [
    "className",
    "disabled",
    "data",
    "name",
    "value",
    "onChange",
    "color",
    "fullWidth",
    "radius",
    "size",
    "transitionDuration",
    "transitionTimingFunction",
    "classNames",
    "styles",
    "defaultValue",
    "orientation",
    "unstyled"
  ]);
  const theme = useMantineTheme();
  const shouldReduceMotion = useReducedMotion();
  const reduceMotion = theme.respectReducedMotion ? shouldReduceMotion : false;
  const data = _data.map((item) => typeof item === "string" ? { label: item, value: item } : item);
  const mounted = (0, import_react257.useRef)();
  const [shouldAnimate, setShouldAnimate] = (0, import_react257.useState)(false);
  const [_value, handleValueChange] = useUncontrolled({
    value,
    defaultValue,
    finalValue: Array.isArray(data) ? (_e2 = (_d = (_b = data.find((item) => !item.disabled)) == null ? void 0 : _b.value) != null ? _d : (_c = data[0]) == null ? void 0 : _c.value) != null ? _e2 : null : null,
    onChange
  });
  const { classes, cx } = SegmentedControl_styles_default({
    size: size3,
    fullWidth,
    color,
    radius: radius2,
    shouldAnimate: reduceMotion || !shouldAnimate,
    transitionDuration,
    transitionTimingFunction,
    orientation
  }, { classNames, styles: styles2, unstyled, name: "SegmentedControl" });
  const [activePosition, setActivePosition] = (0, import_react257.useState)({
    width: 0,
    height: 0,
    translate: [0, 0]
  });
  const uuid = useId(name);
  const refs = (0, import_react257.useRef)({});
  const [observerRef, containerRect] = useResizeObserver();
  useIsomorphicEffect(() => {
    if (!mounted.current) {
      mounted.current = true;
      setShouldAnimate(false);
    } else {
      setShouldAnimate(true);
    }
  });
  (0, import_react257.useEffect)(() => {
    if (_value in refs.current && observerRef.current) {
      const element = refs.current[_value];
      const elementRect = element.getBoundingClientRect();
      const scaledValue = element.offsetWidth / elementRect.width;
      const width = elementRect.width * scaledValue || 0;
      const height = elementRect.height * scaledValue || 0;
      const offsetRight = containerRect.width - element.parentElement.offsetLeft + WRAPPER_PADDING - width;
      const offsetLeft = element.parentElement.offsetLeft - WRAPPER_PADDING;
      setActivePosition({
        width,
        height,
        translate: [
          theme.dir === "rtl" ? offsetRight : offsetLeft,
          element.parentElement.offsetTop - WRAPPER_PADDING
        ]
      });
    }
  }, [_value, containerRect]);
  const controls = data.map((item) => import_react257.default.createElement("div", {
    className: cx(classes.control, { [classes.controlActive]: _value === item.value }),
    key: item.value
  }, import_react257.default.createElement("input", {
    className: classes.input,
    disabled: disabled || item.disabled,
    type: "radio",
    name: uuid,
    value: item.value,
    id: `${uuid}-${item.value}`,
    checked: _value === item.value,
    onChange: () => handleValueChange(item.value)
  }), import_react257.default.createElement("label", {
    className: cx(classes.label, {
      [classes.labelActive]: _value === item.value,
      [classes.disabled]: disabled || item.disabled
    }),
    htmlFor: `${uuid}-${item.value}`,
    ref: (node2) => {
      refs.current[item.value] = node2;
    }
  }, item.label)));
  const mergedRef = useMergedRef(observerRef, ref);
  if (data.length === 0) {
    return null;
  }
  return import_react257.default.createElement(Box, __spreadValues194({
    className: cx(classes.root, className),
    ref: mergedRef
  }, others), typeof _value === "string" && shouldAnimate && import_react257.default.createElement(Box, {
    component: "span",
    className: classes.active,
    sx: {
      width: activePosition.width,
      height: activePosition.height,
      transform: `translate(${activePosition.translate[0]}px, ${activePosition.translate[1]}px )`
    }
  }), controls);
});
SegmentedControl.displayName = "@mantine/core/SegmentedControl";

// node_modules/@mantine/core/esm/Select/Select.js
var import_react258 = __toESM(require_react());

// node_modules/@mantine/core/esm/Select/filter-data/filter-data.js
function filterData3({
  data,
  searchable,
  limit,
  searchValue,
  filter,
  value,
  filterDataOnExactSearchMatch
}) {
  if (!searchable) {
    return data;
  }
  const selected = value != null ? data.find((item) => item.value === value) || null : null;
  if (selected && !filterDataOnExactSearchMatch && (selected == null ? void 0 : selected.label) === searchValue) {
    if (limit) {
      if (limit >= data.length) {
        return data;
      }
      const firstIndex = data.indexOf(selected);
      const lastIndex = firstIndex + limit;
      const firstIndexOffset = lastIndex - data.length;
      if (firstIndexOffset > 0) {
        return data.slice(firstIndex - firstIndexOffset);
      }
      return data.slice(firstIndex, lastIndex);
    }
    return data;
  }
  const result = [];
  for (let i = 0; i < data.length; i += 1) {
    if (filter(searchValue, data[i])) {
      result.push(data[i]);
    }
    if (result.length >= limit) {
      break;
    }
  }
  return result;
}

// node_modules/@mantine/core/esm/Select/Select.styles.js
var useStyles86 = createStyles(() => ({
  input: {
    "&:not(:disabled)": {
      cursor: "pointer",
      "&::selection": {
        backgroundColor: "transparent"
      }
    }
  }
}));
var Select_styles_default = useStyles86;

// node_modules/@mantine/core/esm/Select/Select.js
var __defProp195 = Object.defineProperty;
var __defProps87 = Object.defineProperties;
var __getOwnPropDescs87 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols197 = Object.getOwnPropertySymbols;
var __hasOwnProp197 = Object.prototype.hasOwnProperty;
var __propIsEnum197 = Object.prototype.propertyIsEnumerable;
var __defNormalProp195 = (obj, key, value) => key in obj ? __defProp195(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues195 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp197.call(b2, prop))
      __defNormalProp195(a2, prop, b2[prop]);
  if (__getOwnPropSymbols197)
    for (var prop of __getOwnPropSymbols197(b2)) {
      if (__propIsEnum197.call(b2, prop))
        __defNormalProp195(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps87 = (a2, b2) => __defProps87(a2, __getOwnPropDescs87(b2));
var __objRest127 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp197.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols197)
    for (var prop of __getOwnPropSymbols197(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum197.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
function defaultFilter3(value, item) {
  return item.label.toLowerCase().trim().includes(value.toLowerCase().trim());
}
function defaultShouldCreate2(query, data) {
  return !!query && !data.some((item) => item.label.toLowerCase() === query.toLowerCase());
}
var defaultProps96 = {
  required: false,
  size: "sm",
  shadow: "sm",
  itemComponent: DefaultItem,
  transition: "fade",
  transitionDuration: 0,
  initiallyOpened: false,
  filter: defaultFilter3,
  maxDropdownHeight: 220,
  searchable: false,
  clearable: false,
  limit: Infinity,
  disabled: false,
  creatable: false,
  shouldCreate: defaultShouldCreate2,
  selectOnBlur: false,
  switchDirectionOnFlip: false,
  filterDataOnExactSearchMatch: false,
  zIndex: getDefaultZIndex("popover"),
  clearButtonTabIndex: 0,
  positionDependencies: [],
  dropdownPosition: "flip"
};
var Select = (0, import_react258.forwardRef)((props, ref) => {
  const _a = useInputProps("Select", defaultProps96, props), {
    inputProps,
    wrapperProps,
    shadow,
    data,
    value,
    defaultValue,
    onChange,
    itemComponent,
    onKeyDown,
    onBlur,
    onFocus,
    transition,
    transitionDuration,
    initiallyOpened,
    transitionTimingFunction,
    unstyled,
    classNames,
    styles: styles2,
    filter,
    maxDropdownHeight,
    searchable,
    clearable,
    nothingFound,
    clearButtonLabel,
    limit,
    disabled,
    onSearchChange,
    searchValue,
    rightSection,
    rightSectionWidth,
    creatable,
    getCreateLabel,
    shouldCreate,
    selectOnBlur,
    onCreate: onCreate2,
    dropdownComponent,
    onDropdownClose,
    onDropdownOpen,
    withinPortal,
    switchDirectionOnFlip,
    zIndex,
    name,
    dropdownPosition,
    allowDeselect,
    placeholder,
    filterDataOnExactSearchMatch,
    clearButtonTabIndex,
    form,
    positionDependencies,
    readOnly,
    hoverOnSearchChange
  } = _a, others = __objRest127(_a, [
    "inputProps",
    "wrapperProps",
    "shadow",
    "data",
    "value",
    "defaultValue",
    "onChange",
    "itemComponent",
    "onKeyDown",
    "onBlur",
    "onFocus",
    "transition",
    "transitionDuration",
    "initiallyOpened",
    "transitionTimingFunction",
    "unstyled",
    "classNames",
    "styles",
    "filter",
    "maxDropdownHeight",
    "searchable",
    "clearable",
    "nothingFound",
    "clearButtonLabel",
    "limit",
    "disabled",
    "onSearchChange",
    "searchValue",
    "rightSection",
    "rightSectionWidth",
    "creatable",
    "getCreateLabel",
    "shouldCreate",
    "selectOnBlur",
    "onCreate",
    "dropdownComponent",
    "onDropdownClose",
    "onDropdownOpen",
    "withinPortal",
    "switchDirectionOnFlip",
    "zIndex",
    "name",
    "dropdownPosition",
    "allowDeselect",
    "placeholder",
    "filterDataOnExactSearchMatch",
    "clearButtonTabIndex",
    "form",
    "positionDependencies",
    "readOnly",
    "hoverOnSearchChange"
  ]);
  const { classes, cx, theme } = Select_styles_default();
  const [dropdownOpened, _setDropdownOpened] = (0, import_react258.useState)(initiallyOpened);
  const [hovered, setHovered] = (0, import_react258.useState)(-1);
  const inputRef = (0, import_react258.useRef)();
  const itemsRefs = (0, import_react258.useRef)({});
  const [direction, setDirection] = (0, import_react258.useState)("column");
  const isColumn = direction === "column";
  const { scrollIntoView, targetRef, scrollableRef } = useScrollIntoView({
    duration: 0,
    offset: 5,
    cancelable: false,
    isList: true
  });
  const isDeselectable = allowDeselect === void 0 ? clearable : allowDeselect;
  const setDropdownOpened = (opened) => {
    if (dropdownOpened !== opened) {
      _setDropdownOpened(opened);
      const handler = opened ? onDropdownOpen : onDropdownClose;
      typeof handler === "function" && handler();
    }
  };
  const isCreatable = creatable && typeof getCreateLabel === "function";
  let createLabel = null;
  const formattedData = data.map((item) => typeof item === "string" ? { label: item, value: item } : item);
  const sortedData = groupOptions({ data: formattedData });
  const [_value, handleChange, controlled] = useUncontrolled({
    value,
    defaultValue,
    finalValue: null,
    onChange
  });
  const selectedValue = sortedData.find((item) => item.value === _value);
  const [inputValue, setInputValue] = useUncontrolled({
    value: searchValue,
    defaultValue: (selectedValue == null ? void 0 : selectedValue.label) || "",
    finalValue: void 0,
    onChange: onSearchChange
  });
  const handleSearchChange = (val) => {
    setInputValue(val);
    if (searchable && typeof onSearchChange === "function") {
      onSearchChange(val);
    }
  };
  const handleClear = () => {
    var _a2;
    if (!readOnly) {
      handleChange(null);
      if (!controlled) {
        handleSearchChange("");
      }
      (_a2 = inputRef.current) == null ? void 0 : _a2.focus();
    }
  };
  (0, import_react258.useEffect)(() => {
    const newSelectedValue = sortedData.find((item) => item.value === _value);
    if (newSelectedValue) {
      handleSearchChange(newSelectedValue.label);
    } else if (!isCreatable || !_value) {
      handleSearchChange("");
    }
  }, [_value]);
  (0, import_react258.useEffect)(() => {
    if (selectedValue && (!searchable || !dropdownOpened)) {
      handleSearchChange(selectedValue.label);
    }
  }, [selectedValue == null ? void 0 : selectedValue.label]);
  const handleItemSelect = (item) => {
    if (!readOnly) {
      if (isDeselectable && (selectedValue == null ? void 0 : selectedValue.value) === item.value) {
        handleChange(null);
        setDropdownOpened(false);
      } else {
        if (item.creatable && typeof onCreate2 === "function") {
          const createdItem = onCreate2(item.value);
          if (typeof createdItem !== "undefined" && createdItem !== null) {
            if (typeof createdItem === "string") {
              handleChange(createdItem);
            } else {
              handleChange(createdItem.value);
            }
          }
        } else {
          handleChange(item.value);
        }
        if (!controlled) {
          handleSearchChange(item.label);
        }
        setHovered(-1);
        setDropdownOpened(false);
        inputRef.current.focus();
      }
    }
  };
  const filteredData = filterData3({
    data: sortedData,
    searchable,
    limit,
    searchValue: inputValue,
    filter,
    filterDataOnExactSearchMatch,
    value: _value
  });
  if (isCreatable && shouldCreate(inputValue, filteredData)) {
    createLabel = getCreateLabel(inputValue);
    filteredData.push({ label: inputValue, value: inputValue, creatable: true });
  }
  const getNextIndex2 = (index6, nextItem, compareFn) => {
    let i = index6;
    while (compareFn(i)) {
      i = nextItem(i);
      if (!filteredData[i].disabled)
        return i;
    }
    return index6;
  };
  useDidUpdate(() => {
    if (hoverOnSearchChange && inputValue) {
      setHovered(0);
    } else {
      setHovered(-1);
    }
  }, [inputValue, hoverOnSearchChange]);
  const selectedItemIndex = _value ? filteredData.findIndex((el) => el.value === _value) : 0;
  const shouldShowDropdown = !readOnly && (filteredData.length > 0 ? dropdownOpened : dropdownOpened && !!nothingFound);
  const handlePrevious = () => {
    setHovered((current) => {
      var _a2;
      const nextIndex = getNextIndex2(current, (index6) => index6 - 1, (index6) => index6 > 0);
      targetRef.current = itemsRefs.current[(_a2 = filteredData[nextIndex]) == null ? void 0 : _a2.value];
      shouldShowDropdown && scrollIntoView({ alignment: isColumn ? "start" : "end" });
      return nextIndex;
    });
  };
  const handleNext = () => {
    setHovered((current) => {
      var _a2;
      const nextIndex = getNextIndex2(current, (index6) => index6 + 1, (index6) => index6 < filteredData.length - 1);
      targetRef.current = itemsRefs.current[(_a2 = filteredData[nextIndex]) == null ? void 0 : _a2.value];
      shouldShowDropdown && scrollIntoView({ alignment: isColumn ? "end" : "start" });
      return nextIndex;
    });
  };
  const scrollSelectedItemIntoView = () => window.setTimeout(() => {
    var _a2;
    targetRef.current = itemsRefs.current[(_a2 = filteredData[selectedItemIndex]) == null ? void 0 : _a2.value];
    scrollIntoView({ alignment: isColumn ? "end" : "start" });
  }, 0);
  useDidUpdate(() => {
    if (shouldShowDropdown)
      scrollSelectedItemIntoView();
  }, [shouldShowDropdown]);
  const handleInputKeydown = (event) => {
    typeof onKeyDown === "function" && onKeyDown(event);
    switch (event.key) {
      case "ArrowUp": {
        event.preventDefault();
        if (!dropdownOpened) {
          setHovered(selectedItemIndex);
          setDropdownOpened(true);
          scrollSelectedItemIntoView();
        } else {
          isColumn ? handlePrevious() : handleNext();
        }
        break;
      }
      case "ArrowDown": {
        event.preventDefault();
        if (!dropdownOpened) {
          setHovered(selectedItemIndex);
          setDropdownOpened(true);
          scrollSelectedItemIntoView();
        } else {
          isColumn ? handleNext() : handlePrevious();
        }
        break;
      }
      case "Home": {
        if (!searchable) {
          event.preventDefault();
          if (!dropdownOpened) {
            setDropdownOpened(true);
          }
          const firstItemIndex = filteredData.findIndex((item) => !item.disabled);
          setHovered(firstItemIndex);
          shouldShowDropdown && scrollIntoView({ alignment: isColumn ? "end" : "start" });
        }
        break;
      }
      case "End": {
        if (!searchable) {
          event.preventDefault();
          if (!dropdownOpened) {
            setDropdownOpened(true);
          }
          const lastItemIndex = filteredData.map((item) => !!item.disabled).lastIndexOf(false);
          setHovered(lastItemIndex);
          shouldShowDropdown && scrollIntoView({ alignment: isColumn ? "end" : "start" });
        }
        break;
      }
      case "Escape": {
        event.preventDefault();
        setDropdownOpened(false);
        setHovered(-1);
        break;
      }
      case " ": {
        if (!searchable) {
          event.preventDefault();
          if (filteredData[hovered] && dropdownOpened) {
            handleItemSelect(filteredData[hovered]);
          } else {
            setDropdownOpened(true);
            setHovered(selectedItemIndex);
            scrollSelectedItemIntoView();
          }
        }
        break;
      }
      case "Enter": {
        if (!searchable) {
          event.preventDefault();
        }
        if (filteredData[hovered] && dropdownOpened) {
          event.preventDefault();
          handleItemSelect(filteredData[hovered]);
        }
      }
    }
  };
  const handleInputBlur = (event) => {
    typeof onBlur === "function" && onBlur(event);
    const selected = sortedData.find((item) => item.value === _value);
    if (selectOnBlur && filteredData[hovered] && dropdownOpened) {
      handleItemSelect(filteredData[hovered]);
    }
    handleSearchChange((selected == null ? void 0 : selected.label) || "");
    setDropdownOpened(false);
  };
  const handleInputFocus = (event) => {
    typeof onFocus === "function" && onFocus(event);
    if (searchable) {
      setDropdownOpened(true);
    }
  };
  const handleInputChange = (event) => {
    if (!readOnly) {
      handleSearchChange(event.currentTarget.value);
      if (clearable && event.currentTarget.value === "") {
        handleChange(null);
      }
      setHovered(-1);
      setDropdownOpened(true);
    }
  };
  const handleInputClick = () => {
    if (!readOnly) {
      setDropdownOpened(!dropdownOpened);
      if (_value && !dropdownOpened) {
        setHovered(selectedItemIndex);
      }
    }
  };
  return import_react258.default.createElement(Input.Wrapper, __spreadProps87(__spreadValues195({}, wrapperProps), {
    __staticSelector: "Select"
  }), import_react258.default.createElement(SelectPopover, {
    opened: shouldShowDropdown,
    transition,
    transitionDuration,
    shadow: "sm",
    withinPortal,
    __staticSelector: "Select",
    onDirectionChange: setDirection,
    switchDirectionOnFlip,
    zIndex,
    dropdownPosition,
    positionDependencies: [...positionDependencies, inputValue],
    classNames,
    styles: styles2,
    unstyled
  }, import_react258.default.createElement(SelectPopover.Target, null, import_react258.default.createElement("div", {
    role: "combobox",
    "aria-haspopup": "listbox",
    "aria-owns": shouldShowDropdown ? `${inputProps.id}-items` : null,
    "aria-controls": inputProps.id,
    "aria-expanded": shouldShowDropdown,
    onMouseLeave: () => setHovered(-1),
    tabIndex: -1
  }, import_react258.default.createElement("input", {
    type: "hidden",
    name,
    value: _value || "",
    form,
    disabled
  }), import_react258.default.createElement(Input, __spreadValues195(__spreadProps87(__spreadValues195(__spreadValues195({
    autoComplete: "off",
    type: "search"
  }, inputProps), others), {
    ref: useMergedRef(ref, inputRef),
    onKeyDown: handleInputKeydown,
    __staticSelector: "Select",
    value: inputValue,
    placeholder,
    onChange: handleInputChange,
    "aria-autocomplete": "list",
    "aria-controls": shouldShowDropdown ? `${inputProps.id}-items` : null,
    "aria-activedescendant": hovered >= 0 ? `${inputProps.id}-${hovered}` : null,
    onMouseDown: handleInputClick,
    onBlur: handleInputBlur,
    onFocus: handleInputFocus,
    readOnly: !searchable || readOnly,
    disabled,
    "data-mantine-stop-propagation": shouldShowDropdown,
    name: null,
    classNames: __spreadProps87(__spreadValues195({}, classNames), {
      input: cx({ [classes.input]: !searchable }, classNames == null ? void 0 : classNames.input)
    })
  }), getSelectRightSectionProps({
    theme,
    rightSection,
    rightSectionWidth,
    styles: styles2,
    size: inputProps.size,
    shouldClear: clearable && !!selectedValue,
    clearButtonLabel,
    onClear: handleClear,
    error: wrapperProps.error,
    clearButtonTabIndex,
    disabled,
    readOnly
  }))))), import_react258.default.createElement(SelectPopover.Dropdown, {
    component: dropdownComponent || SelectScrollArea,
    maxHeight: maxDropdownHeight,
    direction,
    id: inputProps.id,
    innerRef: scrollableRef,
    __staticSelector: "Select",
    classNames,
    styles: styles2
  }, import_react258.default.createElement(SelectItems, {
    data: filteredData,
    hovered,
    classNames,
    styles: styles2,
    isItemSelected: (val) => val === _value,
    uuid: inputProps.id,
    __staticSelector: "Select",
    onItemHover: setHovered,
    onItemSelect: handleItemSelect,
    itemsRefs,
    itemComponent,
    size: inputProps.size,
    nothingFound,
    creatable: isCreatable && !!createLabel,
    createLabel,
    "aria-label": wrapperProps.label,
    unstyled
  }))));
});
Select.displayName = "@mantine/core/Select";

// node_modules/@mantine/core/esm/SimpleGrid/SimpleGrid.js
var import_react259 = __toESM(require_react());

// node_modules/@mantine/core/esm/SimpleGrid/get-sorted-breakpoints/get-sorted-breakpoints.js
function getSortedBreakpoints2(theme, breakpoints) {
  if (breakpoints.length === 0) {
    return breakpoints;
  }
  const property = "maxWidth" in breakpoints[0] ? "maxWidth" : "minWidth";
  const sorted = [...breakpoints].sort((a2, b2) => theme.fn.size({ size: b2[property], sizes: theme.breakpoints }) - theme.fn.size({ size: a2[property], sizes: theme.breakpoints }));
  return property === "minWidth" ? sorted.reverse() : sorted;
}

// node_modules/@mantine/core/esm/SimpleGrid/SimpleGrid.styles.js
var __defProp196 = Object.defineProperty;
var __getOwnPropSymbols198 = Object.getOwnPropertySymbols;
var __hasOwnProp198 = Object.prototype.hasOwnProperty;
var __propIsEnum198 = Object.prototype.propertyIsEnumerable;
var __defNormalProp196 = (obj, key, value) => key in obj ? __defProp196(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues196 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp198.call(b2, prop))
      __defNormalProp196(a2, prop, b2[prop]);
  if (__getOwnPropSymbols198)
    for (var prop of __getOwnPropSymbols198(b2)) {
      if (__propIsEnum198.call(b2, prop))
        __defNormalProp196(a2, prop, b2[prop]);
    }
  return a2;
};
var useStyles87 = createStyles((theme, { spacing, breakpoints, cols, verticalSpacing }) => {
  const hasVerticalSpacing = verticalSpacing != null;
  const gridBreakpoints = getSortedBreakpoints2(theme, breakpoints).reduce((acc, breakpoint) => {
    var _a, _b;
    const property = "maxWidth" in breakpoint ? "max-width" : "min-width";
    const breakpointSize = theme.fn.size({
      size: property === "max-width" ? breakpoint.maxWidth : breakpoint.minWidth,
      sizes: theme.breakpoints
    });
    acc[`@media (${property}: ${breakpointSize - (property === "max-width" ? 1 : 0)}px)`] = {
      gridTemplateColumns: `repeat(${breakpoint.cols}, minmax(0, 1fr))`,
      gap: `${theme.fn.size({
        size: (_a = breakpoint.verticalSpacing) != null ? _a : hasVerticalSpacing ? verticalSpacing : spacing,
        sizes: theme.spacing
      })}px ${theme.fn.size({
        size: (_b = breakpoint.spacing) != null ? _b : spacing,
        sizes: theme.spacing
      })}px`
    };
    return acc;
  }, {});
  return {
    root: __spreadValues196({
      boxSizing: "border-box",
      display: "grid",
      gridTemplateColumns: `repeat(${cols}, minmax(0, 1fr))`,
      gap: `${theme.fn.size({
        size: hasVerticalSpacing ? verticalSpacing : spacing,
        sizes: theme.spacing
      })}px ${theme.fn.size({
        size: spacing,
        sizes: theme.spacing
      })}px`
    }, gridBreakpoints)
  };
});
var SimpleGrid_styles_default = useStyles87;

// node_modules/@mantine/core/esm/SimpleGrid/SimpleGrid.js
var __defProp197 = Object.defineProperty;
var __getOwnPropSymbols199 = Object.getOwnPropertySymbols;
var __hasOwnProp199 = Object.prototype.hasOwnProperty;
var __propIsEnum199 = Object.prototype.propertyIsEnumerable;
var __defNormalProp197 = (obj, key, value) => key in obj ? __defProp197(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues197 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp199.call(b2, prop))
      __defNormalProp197(a2, prop, b2[prop]);
  if (__getOwnPropSymbols199)
    for (var prop of __getOwnPropSymbols199(b2)) {
      if (__propIsEnum199.call(b2, prop))
        __defNormalProp197(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest128 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp199.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols199)
    for (var prop of __getOwnPropSymbols199(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum199.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps97 = {
  breakpoints: [],
  cols: 1,
  spacing: "md"
};
var SimpleGrid = (0, import_react259.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("SimpleGrid", defaultProps97, props), { className, breakpoints, cols, spacing, verticalSpacing, children, unstyled } = _a, others = __objRest128(_a, ["className", "breakpoints", "cols", "spacing", "verticalSpacing", "children", "unstyled"]);
  const { classes, cx } = SimpleGrid_styles_default({ breakpoints, cols, spacing, verticalSpacing }, { unstyled, name: "SimpleGrid" });
  return import_react259.default.createElement(Box, __spreadValues197({
    className: cx(classes.root, className),
    ref
  }, others), children);
});
SimpleGrid.displayName = "@mantine/core/SimpleGrid";

// node_modules/@mantine/core/esm/Skeleton/Skeleton.js
var import_react260 = __toESM(require_react());

// node_modules/@mantine/core/esm/Skeleton/Skeleton.styles.js
var fade = keyframes({
  "from, to": { opacity: 0.4 },
  "50%": { opacity: 1 }
});
var useStyles88 = createStyles((theme, { height, width, radius: radius2, circle, animate }) => ({
  root: {
    height,
    width: circle ? height : width,
    borderRadius: circle ? height : theme.fn.radius(radius2),
    position: "relative",
    WebkitTransform: "translateZ(0)"
  },
  visible: {
    overflow: "hidden",
    "&::before": {
      content: '""',
      position: "absolute",
      background: theme.colorScheme === "dark" ? theme.colors.dark[7] : theme.white,
      top: 0,
      bottom: 0,
      left: 0,
      right: 0,
      zIndex: 10
    },
    "&::after": {
      content: '""',
      position: "absolute",
      background: theme.colorScheme === "dark" ? theme.colors.dark[4] : theme.colors.gray[3],
      top: 0,
      bottom: 0,
      left: 0,
      right: 0,
      animation: animate ? `${fade} 1500ms linear infinite` : "none",
      zIndex: 11
    }
  }
}));
var Skeleton_styles_default = useStyles88;

// node_modules/@mantine/core/esm/Skeleton/Skeleton.js
var __defProp198 = Object.defineProperty;
var __getOwnPropSymbols200 = Object.getOwnPropertySymbols;
var __hasOwnProp200 = Object.prototype.hasOwnProperty;
var __propIsEnum200 = Object.prototype.propertyIsEnumerable;
var __defNormalProp198 = (obj, key, value) => key in obj ? __defProp198(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues198 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp200.call(b2, prop))
      __defNormalProp198(a2, prop, b2[prop]);
  if (__getOwnPropSymbols200)
    for (var prop of __getOwnPropSymbols200(b2)) {
      if (__propIsEnum200.call(b2, prop))
        __defNormalProp198(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest129 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp200.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols200)
    for (var prop of __getOwnPropSymbols200(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum200.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps98 = {
  height: "auto",
  width: "100%",
  visible: true,
  animate: true
};
var Skeleton = (0, import_react260.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("Skeleton", defaultProps98, props), { height, width, visible: visible2, animate, className, circle, radius: radius2, unstyled } = _a, others = __objRest129(_a, ["height", "width", "visible", "animate", "className", "circle", "radius", "unstyled"]);
  const { classes, cx } = Skeleton_styles_default({ height, width, circle, radius: radius2, animate }, { unstyled, name: "Skeleton" });
  return import_react260.default.createElement(Box, __spreadValues198({
    className: cx(classes.root, { [classes.visible]: visible2 }, className),
    ref
  }, others));
});
Skeleton.displayName = "@mantine/core/Skeleton";

// node_modules/@mantine/core/esm/Slider/Slider/Slider.js
var import_react265 = __toESM(require_react());

// node_modules/@mantine/core/esm/Slider/utils/get-position/get-position.js
function getPosition({ value, min: min3, max: max3 }) {
  const position2 = (value - min3) / (max3 - min3) * 100;
  return Math.min(Math.max(position2, 0), 100);
}

// node_modules/@mantine/core/esm/Slider/utils/get-change-value/get-change-value.js
function getChangeValue({
  value,
  containerWidth,
  min: min3,
  max: max3,
  step,
  precision
}) {
  const left2 = !containerWidth ? value : Math.min(Math.max(value, 0), containerWidth) / containerWidth;
  const dx = left2 * (max3 - min3);
  const nextValue = (dx !== 0 ? Math.round(dx / step) * step : 0) + min3;
  const nextValueWithinStep = Math.max(nextValue, min3);
  if (precision !== void 0) {
    return Number(nextValueWithinStep.toFixed(precision));
  }
  return nextValueWithinStep;
}

// node_modules/@mantine/core/esm/Slider/Thumb/Thumb.js
var import_react261 = __toESM(require_react());

// node_modules/@mantine/core/esm/Slider/SliderRoot/SliderRoot.styles.js
var __defProp199 = Object.defineProperty;
var __defProps88 = Object.defineProperties;
var __getOwnPropDescs88 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols201 = Object.getOwnPropertySymbols;
var __hasOwnProp201 = Object.prototype.hasOwnProperty;
var __propIsEnum201 = Object.prototype.propertyIsEnumerable;
var __defNormalProp199 = (obj, key, value) => key in obj ? __defProp199(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues199 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp201.call(b2, prop))
      __defNormalProp199(a2, prop, b2[prop]);
  if (__getOwnPropSymbols201)
    for (var prop of __getOwnPropSymbols201(b2)) {
      if (__propIsEnum201.call(b2, prop))
        __defNormalProp199(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps88 = (a2, b2) => __defProps88(a2, __getOwnPropDescs88(b2));
var sizes22 = {
  xs: 4,
  sm: 6,
  md: 8,
  lg: 10,
  xl: 12
};
var useStyles89 = createStyles((theme, { size: size3, disabled }) => ({
  root: __spreadProps88(__spreadValues199({}, theme.fn.fontStyles()), {
    WebkitTapHighlightColor: "transparent",
    outline: 0,
    height: theme.fn.size({ sizes: sizes22, size: size3 }) * 2,
    display: "flex",
    alignItems: "center",
    cursor: disabled ? "not-allowed" : "pointer",
    touchAction: "none"
  })
}));
var SliderRoot_styles_default = useStyles89;

// node_modules/@mantine/core/esm/Slider/Thumb/Thumb.styles.js
var __defProp200 = Object.defineProperty;
var __defProps89 = Object.defineProperties;
var __getOwnPropDescs89 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols202 = Object.getOwnPropertySymbols;
var __hasOwnProp202 = Object.prototype.hasOwnProperty;
var __propIsEnum202 = Object.prototype.propertyIsEnumerable;
var __defNormalProp200 = (obj, key, value) => key in obj ? __defProp200(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues200 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp202.call(b2, prop))
      __defNormalProp200(a2, prop, b2[prop]);
  if (__getOwnPropSymbols202)
    for (var prop of __getOwnPropSymbols202(b2)) {
      if (__propIsEnum202.call(b2, prop))
        __defNormalProp200(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps89 = (a2, b2) => __defProps89(a2, __getOwnPropDescs89(b2));
var useStyles90 = createStyles((theme, { color, size: size3, disabled, thumbSize }) => ({
  label: {
    position: "absolute",
    top: -36,
    backgroundColor: theme.colorScheme === "dark" ? theme.colors.dark[4] : theme.colors.gray[9],
    fontSize: theme.fontSizes.xs,
    color: theme.white,
    padding: `calc(${theme.spacing.xs}px / 2)`,
    borderRadius: theme.radius.sm,
    whiteSpace: "nowrap",
    pointerEvents: "none",
    userSelect: "none",
    touchAction: "none"
  },
  thumb: __spreadProps89(__spreadValues200({}, theme.fn.focusStyles()), {
    boxSizing: "border-box",
    position: "absolute",
    display: disabled ? "none" : "flex",
    height: thumbSize || theme.fn.size({ sizes: sizes22, size: size3 }) * 2,
    width: thumbSize || theme.fn.size({ sizes: sizes22, size: size3 }) * 2,
    backgroundColor: theme.colorScheme === "dark" ? theme.fn.themeColor(color, theme.fn.primaryShade()) : theme.white,
    border: `4px solid ${theme.colorScheme === "dark" ? theme.white : theme.fn.themeColor(color, theme.fn.primaryShade())}`,
    color: theme.colorScheme === "dark" ? theme.white : theme.fn.themeColor(color, theme.fn.primaryShade()),
    transform: "translate(-50%, -50%)",
    top: "50%",
    cursor: "pointer",
    borderRadius: 1e3,
    alignItems: "center",
    justifyContent: "center",
    transitionDuration: "100ms",
    transitionProperty: "box-shadow, transform",
    transitionTimingFunction: theme.transitionTimingFunction,
    zIndex: 3,
    userSelect: "none",
    touchAction: "none"
  }),
  dragging: {
    transform: "translate(-50%, -50%) scale(1.05)",
    boxShadow: theme.shadows.sm
  }
}));
var Thumb_styles_default2 = useStyles90;

// node_modules/@mantine/core/esm/Slider/Thumb/Thumb.js
var Thumb2 = (0, import_react261.forwardRef)(({
  max: max3,
  min: min3,
  value,
  position: position2,
  label,
  dragging,
  onMouseDown,
  color,
  classNames,
  styles: styles2,
  size: size3,
  labelTransition,
  labelTransitionDuration,
  labelTransitionTimingFunction,
  labelAlwaysOn,
  thumbLabel,
  onFocus,
  onBlur,
  showLabelOnHover,
  children = null,
  disabled,
  unstyled,
  thumbSize
}, ref) => {
  const { classes, cx, theme } = Thumb_styles_default2({ color, size: size3, disabled, thumbSize }, { classNames, styles: styles2, unstyled, name: "Slider" });
  const [focused, setFocused] = (0, import_react261.useState)(false);
  const isVisible = labelAlwaysOn || dragging || focused || showLabelOnHover;
  return import_react261.default.createElement(Box, {
    tabIndex: 0,
    role: "slider",
    "aria-label": thumbLabel,
    "aria-valuemax": max3,
    "aria-valuemin": min3,
    "aria-valuenow": value,
    ref,
    className: cx(classes.thumb, { [classes.dragging]: dragging }),
    onFocus: () => {
      setFocused(true);
      typeof onFocus === "function" && onFocus();
    },
    onBlur: () => {
      setFocused(false);
      typeof onBlur === "function" && onBlur();
    },
    onTouchStart: onMouseDown,
    onMouseDown,
    onClick: (event) => event.stopPropagation(),
    style: { [theme.dir === "rtl" ? "right" : "left"]: `${position2}%` }
  }, children, import_react261.default.createElement(Transition, {
    mounted: label != null && isVisible,
    duration: labelTransitionDuration,
    transition: labelTransition,
    timingFunction: labelTransitionTimingFunction || theme.transitionTimingFunction
  }, (transitionStyles) => import_react261.default.createElement("div", {
    style: transitionStyles,
    className: classes.label
  }, label)));
});
Thumb2.displayName = "@mantine/core/SliderThumb";

// node_modules/@mantine/core/esm/Slider/Track/Track.js
var import_react263 = __toESM(require_react());

// node_modules/@mantine/core/esm/Slider/Marks/Marks.js
var import_react262 = __toESM(require_react());

// node_modules/@mantine/core/esm/Slider/Marks/is-mark-filled.js
function isMarkFilled({ mark, offset: offset3, value, inverted = false }) {
  return inverted ? typeof offset3 === "number" ? mark.value <= offset3 || mark.value >= value : mark.value >= value : typeof offset3 === "number" ? mark.value >= offset3 && mark.value <= value : mark.value <= value;
}

// node_modules/@mantine/core/esm/Slider/Marks/Marks.styles.js
var useStyles91 = createStyles((theme, { size: size3, color, disabled }) => ({
  markWrapper: {
    position: "absolute",
    top: 0,
    zIndex: 2
  },
  mark: {
    boxSizing: "border-box",
    border: `${theme.fn.size({ size: size3, sizes: sizes22 }) >= 8 ? "2px" : "1px"} solid ${theme.colorScheme === "dark" ? theme.colors.dark[4] : theme.colors.gray[2]}`,
    height: theme.fn.size({ sizes: sizes22, size: size3 }),
    width: theme.fn.size({ sizes: sizes22, size: size3 }),
    borderRadius: 1e3,
    transform: `translateX(-${theme.fn.size({ sizes: sizes22, size: size3 }) / 2}px)`,
    backgroundColor: theme.white
  },
  markFilled: {
    borderColor: disabled ? theme.colorScheme === "dark" ? theme.colors.dark[3] : theme.colors.gray[4] : theme.fn.variant({ variant: "filled", color }).background
  },
  markLabel: {
    transform: "translate(-50%, 0)",
    fontSize: theme.fontSizes.sm,
    color: theme.colorScheme === "dark" ? theme.colors.dark[2] : theme.colors.gray[6],
    marginTop: `calc(${theme.spacing.xs}px / 2)`,
    whiteSpace: "nowrap"
  }
}));
var Marks_styles_default = useStyles91;

// node_modules/@mantine/core/esm/Slider/Marks/Marks.js
function Marks({
  marks,
  color,
  size: size3,
  min: min3,
  max: max3,
  value,
  classNames,
  styles: styles2,
  offset: offset3,
  onChange,
  disabled,
  unstyled,
  inverted
}) {
  const { classes, cx } = Marks_styles_default({ size: size3, color, disabled }, { classNames, styles: styles2, unstyled, name: "Slider" });
  const items = marks.map((mark, index6) => import_react262.default.createElement(Box, {
    className: classes.markWrapper,
    sx: { left: `${getPosition({ value: mark.value, min: min3, max: max3 })}%` },
    key: index6
  }, import_react262.default.createElement("div", {
    className: cx(classes.mark, {
      [classes.markFilled]: isMarkFilled({ mark, value, offset: offset3, inverted })
    })
  }), mark.label && import_react262.default.createElement("div", {
    className: classes.markLabel,
    onMouseDown: (event) => {
      event.stopPropagation();
      onChange(mark.value);
    },
    onTouchStart: (event) => {
      event.stopPropagation();
      onChange(mark.value);
    }
  }, mark.label)));
  return import_react262.default.createElement("div", null, items);
}
Marks.displayName = "@mantine/core/SliderMarks";

// node_modules/@mantine/core/esm/Slider/Track/Track.styles.js
var useStyles92 = createStyles((theme, { radius: radius2, size: size3, color, disabled, inverted }) => ({
  track: {
    position: "relative",
    height: theme.fn.size({ sizes: sizes22, size: size3 }),
    width: "100%",
    marginRight: theme.fn.size({ size: size3, sizes: sizes22 }),
    marginLeft: theme.fn.size({ size: size3, sizes: sizes22 }),
    "&::before": {
      content: '""',
      position: "absolute",
      top: 0,
      bottom: 0,
      borderRadius: theme.fn.size({ size: radius2, sizes: theme.radius }),
      right: -theme.fn.size({ size: size3, sizes: sizes22 }),
      left: -theme.fn.size({ size: size3, sizes: sizes22 }),
      backgroundColor: inverted ? disabled ? theme.colorScheme === "dark" ? theme.colors.dark[3] : theme.colors.gray[4] : theme.fn.variant({ variant: "filled", color }).background : theme.colorScheme === "dark" ? theme.colors.dark[4] : theme.colors.gray[2],
      zIndex: 0
    }
  },
  bar: {
    position: "absolute",
    zIndex: 1,
    top: 0,
    bottom: 0,
    backgroundColor: inverted ? theme.colorScheme === "dark" ? theme.colors.dark[4] : theme.colors.gray[2] : disabled ? theme.colorScheme === "dark" ? theme.colors.dark[3] : theme.colors.gray[4] : theme.fn.variant({ variant: "filled", color }).background,
    borderRadius: theme.fn.size({ size: radius2, sizes: theme.radius })
  }
}));
var Track_styles_default = useStyles92;

// node_modules/@mantine/core/esm/Slider/Track/Track.js
var __defProp201 = Object.defineProperty;
var __defProps90 = Object.defineProperties;
var __getOwnPropDescs90 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols203 = Object.getOwnPropertySymbols;
var __hasOwnProp203 = Object.prototype.hasOwnProperty;
var __propIsEnum203 = Object.prototype.propertyIsEnumerable;
var __defNormalProp201 = (obj, key, value) => key in obj ? __defProp201(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues201 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp203.call(b2, prop))
      __defNormalProp201(a2, prop, b2[prop]);
  if (__getOwnPropSymbols203)
    for (var prop of __getOwnPropSymbols203(b2)) {
      if (__propIsEnum203.call(b2, prop))
        __defNormalProp201(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps90 = (a2, b2) => __defProps90(a2, __getOwnPropDescs90(b2));
var __objRest130 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp203.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols203)
    for (var prop of __getOwnPropSymbols203(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum203.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
function Track(_a) {
  var _b = _a, {
    filled,
    size: size3,
    color,
    classNames,
    styles: styles2,
    radius: radius2,
    children,
    offset: offset3,
    onMouseLeave,
    onMouseEnter,
    disabled,
    marksOffset,
    unstyled,
    inverted
  } = _b, others = __objRest130(_b, [
    "filled",
    "size",
    "color",
    "classNames",
    "styles",
    "radius",
    "children",
    "offset",
    "onMouseLeave",
    "onMouseEnter",
    "disabled",
    "marksOffset",
    "unstyled",
    "inverted"
  ]);
  const { classes } = Track_styles_default({ color, size: size3, radius: radius2, disabled, inverted }, { classNames, styles: styles2, unstyled, name: "Slider" });
  return import_react263.default.createElement("div", {
    className: classes.track,
    onMouseLeave,
    onMouseEnter
  }, import_react263.default.createElement(Box, {
    className: classes.bar,
    sx: (theme) => ({
      left: `calc(${offset3}% - ${theme.fn.size({ size: size3, sizes: sizes22 })}px)`,
      width: `calc(${filled}% + ${theme.fn.size({ size: size3, sizes: sizes22 })}px)`
    })
  }), children, import_react263.default.createElement(Marks, __spreadProps90(__spreadValues201({}, others), {
    size: size3,
    color,
    offset: marksOffset,
    classNames,
    styles: styles2,
    disabled,
    unstyled,
    inverted
  })));
}
Track.displayName = "@mantine/core/SliderTrack";

// node_modules/@mantine/core/esm/Slider/SliderRoot/SliderRoot.js
var import_react264 = __toESM(require_react());
var __defProp202 = Object.defineProperty;
var __defProps91 = Object.defineProperties;
var __getOwnPropDescs91 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols204 = Object.getOwnPropertySymbols;
var __hasOwnProp204 = Object.prototype.hasOwnProperty;
var __propIsEnum204 = Object.prototype.propertyIsEnumerable;
var __defNormalProp202 = (obj, key, value) => key in obj ? __defProp202(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues202 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp204.call(b2, prop))
      __defNormalProp202(a2, prop, b2[prop]);
  if (__getOwnPropSymbols204)
    for (var prop of __getOwnPropSymbols204(b2)) {
      if (__propIsEnum204.call(b2, prop))
        __defNormalProp202(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps91 = (a2, b2) => __defProps91(a2, __getOwnPropDescs91(b2));
var __objRest131 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp204.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols204)
    for (var prop of __getOwnPropSymbols204(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum204.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var SliderRoot = (0, import_react264.forwardRef)((_a, ref) => {
  var _b = _a, { className, size: size3, classNames, styles: styles2, disabled, unstyled } = _b, others = __objRest131(_b, ["className", "size", "classNames", "styles", "disabled", "unstyled"]);
  const { classes, cx } = SliderRoot_styles_default({ size: size3, disabled }, { classNames, styles: styles2, unstyled, name: "Slider" });
  return import_react264.default.createElement(Box, __spreadProps91(__spreadValues202({}, others), {
    tabIndex: -1,
    className: cx(classes.root, className),
    ref
  }));
});
SliderRoot.displayName = "@mantine/core/SliderRoot";

// node_modules/@mantine/core/esm/Slider/Slider/Slider.js
var __defProp203 = Object.defineProperty;
var __defProps92 = Object.defineProperties;
var __getOwnPropDescs92 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols205 = Object.getOwnPropertySymbols;
var __hasOwnProp205 = Object.prototype.hasOwnProperty;
var __propIsEnum205 = Object.prototype.propertyIsEnumerable;
var __defNormalProp203 = (obj, key, value) => key in obj ? __defProp203(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues203 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp205.call(b2, prop))
      __defNormalProp203(a2, prop, b2[prop]);
  if (__getOwnPropSymbols205)
    for (var prop of __getOwnPropSymbols205(b2)) {
      if (__propIsEnum205.call(b2, prop))
        __defNormalProp203(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps92 = (a2, b2) => __defProps92(a2, __getOwnPropDescs92(b2));
var __objRest132 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp205.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols205)
    for (var prop of __getOwnPropSymbols205(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum205.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps99 = {
  size: "md",
  radius: "xl",
  min: 0,
  max: 100,
  step: 1,
  marks: [],
  label: (f) => f,
  labelTransition: "skew-down",
  labelTransitionDuration: 0,
  labelAlwaysOn: false,
  thumbLabel: "",
  showLabelOnHover: true,
  disabled: false,
  scale: (v) => v
};
var Slider = (0, import_react265.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("Slider", defaultProps99, props), {
    classNames,
    styles: styles2,
    color,
    value,
    onChange,
    onChangeEnd,
    size: size3,
    radius: radius2,
    min: min3,
    max: max3,
    step,
    precision,
    defaultValue,
    name,
    marks,
    label,
    labelTransition,
    labelTransitionDuration,
    labelTransitionTimingFunction,
    labelAlwaysOn,
    thumbLabel,
    showLabelOnHover,
    thumbChildren,
    disabled,
    unstyled,
    thumbSize,
    scale,
    inverted
  } = _a, others = __objRest132(_a, [
    "classNames",
    "styles",
    "color",
    "value",
    "onChange",
    "onChangeEnd",
    "size",
    "radius",
    "min",
    "max",
    "step",
    "precision",
    "defaultValue",
    "name",
    "marks",
    "label",
    "labelTransition",
    "labelTransitionDuration",
    "labelTransitionTimingFunction",
    "labelAlwaysOn",
    "thumbLabel",
    "showLabelOnHover",
    "thumbChildren",
    "disabled",
    "unstyled",
    "thumbSize",
    "scale",
    "inverted"
  ]);
  const theme = useMantineTheme();
  const [hovered, setHovered] = (0, import_react265.useState)(false);
  const [_value, setValue] = useUncontrolled({
    value: typeof value === "number" ? clamp(value, min3, max3) : value,
    defaultValue: typeof defaultValue === "number" ? clamp(defaultValue, min3, max3) : defaultValue,
    finalValue: clamp(0, min3, max3),
    onChange
  });
  const valueRef = (0, import_react265.useRef)(_value);
  const thumb = (0, import_react265.useRef)();
  const position2 = getPosition({ value: _value, min: min3, max: max3 });
  const scaledValue = scale(_value);
  const _label = typeof label === "function" ? label(scaledValue) : label;
  const handleChange = (0, import_react265.useCallback)(({ x }) => {
    if (!disabled) {
      const nextValue = getChangeValue({ value: x, min: min3, max: max3, step, precision });
      setValue(nextValue);
      valueRef.current = nextValue;
    }
  }, [disabled, min3, max3, step, precision]);
  const { ref: container, active } = useMove(handleChange, { onScrubEnd: () => onChangeEnd == null ? void 0 : onChangeEnd(valueRef.current) }, theme.dir);
  const handleThumbMouseDown = (event) => {
    event.stopPropagation();
  };
  const handleTrackKeydownCapture = (event) => {
    if (!disabled) {
      switch (event.key) {
        case "ArrowUp": {
          event.preventDefault();
          thumb.current.focus();
          const nextValue = Math.min(Math.max(_value + step, min3), max3);
          onChangeEnd == null ? void 0 : onChangeEnd(nextValue);
          setValue(nextValue);
          break;
        }
        case "ArrowRight": {
          event.preventDefault();
          thumb.current.focus();
          const nextValue = Math.min(Math.max(theme.dir === "rtl" ? _value - step : _value + step, min3), max3);
          onChangeEnd == null ? void 0 : onChangeEnd(nextValue);
          setValue(nextValue);
          break;
        }
        case "ArrowDown": {
          event.preventDefault();
          thumb.current.focus();
          const nextValue = Math.min(Math.max(_value - step, min3), max3);
          onChangeEnd == null ? void 0 : onChangeEnd(nextValue);
          setValue(nextValue);
          break;
        }
        case "ArrowLeft": {
          event.preventDefault();
          thumb.current.focus();
          const nextValue = Math.min(Math.max(theme.dir === "rtl" ? _value + step : _value - step, min3), max3);
          onChangeEnd == null ? void 0 : onChangeEnd(nextValue);
          setValue(nextValue);
          break;
        }
        case "Home": {
          event.preventDefault();
          thumb.current.focus();
          onChangeEnd == null ? void 0 : onChangeEnd(min3);
          setValue(min3);
          break;
        }
        case "End": {
          event.preventDefault();
          thumb.current.focus();
          onChangeEnd == null ? void 0 : onChangeEnd(max3);
          setValue(max3);
          break;
        }
      }
    }
  };
  return import_react265.default.createElement(SliderRoot, __spreadProps92(__spreadValues203({}, others), {
    size: size3,
    ref: useMergedRef(container, ref),
    onKeyDownCapture: handleTrackKeydownCapture,
    onMouseDownCapture: () => {
      var _a2;
      return (_a2 = container.current) == null ? void 0 : _a2.focus();
    },
    classNames,
    styles: styles2,
    disabled,
    unstyled
  }), import_react265.default.createElement(Track, {
    inverted,
    offset: 0,
    filled: position2,
    marks,
    size: size3,
    radius: radius2,
    color,
    min: min3,
    max: max3,
    value: scaledValue,
    onChange: setValue,
    onMouseEnter: showLabelOnHover ? () => setHovered(true) : void 0,
    onMouseLeave: showLabelOnHover ? () => setHovered(false) : void 0,
    classNames,
    styles: styles2,
    disabled,
    unstyled
  }, import_react265.default.createElement(Thumb2, {
    max: max3,
    min: min3,
    value: scaledValue,
    position: position2,
    dragging: active,
    color,
    size: size3,
    label: _label,
    ref: thumb,
    onMouseDown: handleThumbMouseDown,
    labelTransition,
    labelTransitionDuration,
    labelTransitionTimingFunction,
    labelAlwaysOn,
    classNames,
    styles: styles2,
    thumbLabel,
    showLabelOnHover: showLabelOnHover && hovered,
    disabled,
    unstyled,
    thumbSize
  }, thumbChildren)), import_react265.default.createElement("input", {
    type: "hidden",
    name,
    value: scaledValue
  }));
});
Slider.displayName = "@mantine/core/Slider";

// node_modules/@mantine/core/esm/Slider/RangeSlider/RangeSlider.js
var import_react266 = __toESM(require_react());

// node_modules/@mantine/core/esm/Slider/utils/get-client-position/get-client-position.js
function getClientPosition(event) {
  if ("TouchEvent" in window && event instanceof window.TouchEvent) {
    const touch = event.touches[0];
    return touch.clientX;
  }
  return event.clientX;
}

// node_modules/@mantine/core/esm/Slider/RangeSlider/RangeSlider.js
var __defProp204 = Object.defineProperty;
var __defProps93 = Object.defineProperties;
var __getOwnPropDescs93 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols206 = Object.getOwnPropertySymbols;
var __hasOwnProp206 = Object.prototype.hasOwnProperty;
var __propIsEnum206 = Object.prototype.propertyIsEnumerable;
var __defNormalProp204 = (obj, key, value) => key in obj ? __defProp204(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues204 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp206.call(b2, prop))
      __defNormalProp204(a2, prop, b2[prop]);
  if (__getOwnPropSymbols206)
    for (var prop of __getOwnPropSymbols206(b2)) {
      if (__propIsEnum206.call(b2, prop))
        __defNormalProp204(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps93 = (a2, b2) => __defProps93(a2, __getOwnPropDescs93(b2));
var __objRest133 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp206.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols206)
    for (var prop of __getOwnPropSymbols206(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum206.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps100 = {
  size: "md",
  radius: "xl",
  min: 0,
  max: 100,
  minRange: 10,
  step: 1,
  marks: [],
  label: (f) => f,
  labelTransition: "skew-down",
  labelTransitionDuration: 0,
  labelAlwaysOn: false,
  thumbFromLabel: "",
  thumbToLabel: "",
  showLabelOnHover: true,
  disabled: false,
  scale: (v) => v
};
var RangeSlider = (0, import_react266.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("RangeSlider", defaultProps100, props), {
    classNames,
    styles: styles2,
    color,
    value,
    onChange,
    onChangeEnd,
    size: size3,
    radius: radius2,
    min: min3,
    max: max3,
    minRange,
    maxRange,
    step,
    precision,
    defaultValue,
    name,
    marks,
    label,
    labelTransition,
    labelTransitionDuration,
    labelTransitionTimingFunction,
    labelAlwaysOn,
    thumbFromLabel,
    thumbToLabel,
    showLabelOnHover,
    thumbChildren,
    disabled,
    unstyled,
    thumbSize,
    scale,
    inverted
  } = _a, others = __objRest133(_a, [
    "classNames",
    "styles",
    "color",
    "value",
    "onChange",
    "onChangeEnd",
    "size",
    "radius",
    "min",
    "max",
    "minRange",
    "maxRange",
    "step",
    "precision",
    "defaultValue",
    "name",
    "marks",
    "label",
    "labelTransition",
    "labelTransitionDuration",
    "labelTransitionTimingFunction",
    "labelAlwaysOn",
    "thumbFromLabel",
    "thumbToLabel",
    "showLabelOnHover",
    "thumbChildren",
    "disabled",
    "unstyled",
    "thumbSize",
    "scale",
    "inverted"
  ]);
  const theme = useMantineTheme();
  const [focused, setFocused] = (0, import_react266.useState)(-1);
  const [hovered, setHovered] = (0, import_react266.useState)(false);
  const [_value, setValue] = useUncontrolled({
    value,
    defaultValue,
    finalValue: [min3, max3],
    onChange
  });
  const valueRef = (0, import_react266.useRef)(_value);
  const thumbs = (0, import_react266.useRef)([]);
  const thumbIndex = (0, import_react266.useRef)(void 0);
  const positions = [
    getPosition({ value: _value[0], min: min3, max: max3 }),
    getPosition({ value: _value[1], min: min3, max: max3 })
  ];
  const _setValue = (val) => {
    setValue(val);
    valueRef.current = val;
  };
  (0, import_react266.useEffect)(() => {
    if (Array.isArray(value)) {
      valueRef.current = value;
    }
  }, Array.isArray(value) ? [value[0], value[1]] : [null, null]);
  const setRangedValue = (val, index6, triggerChangeEnd) => {
    const clone = [...valueRef.current];
    clone[index6] = val;
    if (index6 === 0) {
      if (val > clone[1] - (minRange - 1e-9)) {
        clone[1] = Math.min(val + minRange, max3);
      }
      if (val > (max3 - (minRange - 1e-9) || min3)) {
        clone[index6] = valueRef.current[index6];
      }
      if (clone[1] - val > maxRange) {
        clone[1] = val + maxRange;
      }
    }
    if (index6 === 1) {
      if (val < clone[0] + minRange) {
        clone[0] = Math.max(val - minRange, min3);
      }
      if (val < clone[0] + minRange) {
        clone[index6] = valueRef.current[index6];
      }
      if (val - clone[0] > maxRange) {
        clone[0] = val - maxRange;
      }
    }
    _setValue(clone);
    if (triggerChangeEnd) {
      onChangeEnd == null ? void 0 : onChangeEnd(valueRef.current);
    }
  };
  const handleChange = (val) => {
    if (!disabled) {
      const nextValue = getChangeValue({ value: val, min: min3, max: max3, step, precision });
      setRangedValue(nextValue, thumbIndex.current, false);
    }
  };
  const { ref: container, active } = useMove(({ x }) => handleChange(x), { onScrubEnd: () => onChangeEnd == null ? void 0 : onChangeEnd(valueRef.current) }, theme.dir);
  function handleThumbMouseDown(event, index6) {
    event.stopPropagation();
    thumbIndex.current = index6;
  }
  const handleTrackMouseDownCapture = (event) => {
    container.current.focus();
    const rect = container.current.getBoundingClientRect();
    const changePosition = getClientPosition(event.nativeEvent);
    const changeValue = getChangeValue({
      value: changePosition - rect.left,
      max: max3,
      min: min3,
      step,
      containerWidth: rect.width
    });
    const nearestHandle = Math.abs(_value[0] - changeValue) > Math.abs(_value[1] - changeValue) ? 1 : 0;
    const _nearestHandle = theme.dir === "ltr" ? nearestHandle : nearestHandle === 1 ? 0 : 1;
    thumbIndex.current = _nearestHandle;
  };
  const getFocusedThumbIndex = () => {
    if (focused !== 1 && focused !== 0) {
      setFocused(0);
      return 0;
    }
    return focused;
  };
  const handleTrackKeydownCapture = (event) => {
    if (!disabled) {
      switch (event.key) {
        case "ArrowUp": {
          event.preventDefault();
          const focusedIndex = getFocusedThumbIndex();
          thumbs.current[focusedIndex].focus();
          setRangedValue(Math.min(Math.max(valueRef.current[focusedIndex] + step, min3), max3), focusedIndex, true);
          break;
        }
        case "ArrowRight": {
          event.preventDefault();
          const focusedIndex = getFocusedThumbIndex();
          thumbs.current[focusedIndex].focus();
          setRangedValue(Math.min(Math.max(theme.dir === "rtl" ? valueRef.current[focusedIndex] - step : valueRef.current[focusedIndex] + step, min3), max3), focusedIndex, true);
          break;
        }
        case "ArrowDown": {
          event.preventDefault();
          const focusedIndex = getFocusedThumbIndex();
          thumbs.current[focusedIndex].focus();
          setRangedValue(Math.min(Math.max(valueRef.current[focusedIndex] - step, min3), max3), focusedIndex, true);
          break;
        }
        case "ArrowLeft": {
          event.preventDefault();
          const focusedIndex = getFocusedThumbIndex();
          thumbs.current[focusedIndex].focus();
          setRangedValue(Math.min(Math.max(theme.dir === "rtl" ? valueRef.current[focusedIndex] + step : valueRef.current[focusedIndex] - step, min3), max3), focusedIndex, true);
          break;
        }
      }
    }
  };
  const sharedThumbProps = {
    max: max3,
    min: min3,
    color,
    size: size3,
    labelTransition,
    labelTransitionDuration,
    labelTransitionTimingFunction,
    labelAlwaysOn,
    onBlur: () => setFocused(-1),
    classNames,
    styles: styles2
  };
  const hasArrayThumbChildren = Array.isArray(thumbChildren);
  return import_react266.default.createElement(SliderRoot, __spreadProps93(__spreadValues204({}, others), {
    size: size3,
    ref: useMergedRef(container, ref),
    onTouchStartCapture: handleTrackMouseDownCapture,
    onTouchEndCapture: () => {
      thumbIndex.current = -1;
    },
    onMouseDownCapture: handleTrackMouseDownCapture,
    onMouseUpCapture: () => {
      thumbIndex.current = -1;
    },
    onKeyDownCapture: handleTrackKeydownCapture,
    styles: styles2,
    classNames,
    disabled,
    unstyled
  }), import_react266.default.createElement(Track, {
    offset: positions[0],
    marksOffset: _value[0],
    filled: positions[1] - positions[0],
    marks,
    inverted,
    size: size3,
    radius: radius2,
    color,
    min: min3,
    max: max3,
    value: _value[1],
    styles: styles2,
    classNames,
    onMouseEnter: showLabelOnHover ? () => setHovered(true) : void 0,
    onMouseLeave: showLabelOnHover ? () => setHovered(false) : void 0,
    onChange: (val) => {
      const nearestValue = Math.abs(_value[0] - val) > Math.abs(_value[1] - val) ? 1 : 0;
      const clone = [..._value];
      clone[nearestValue] = val;
      _setValue(clone);
    },
    disabled,
    unstyled
  }, import_react266.default.createElement(Thumb2, __spreadProps93(__spreadValues204({}, sharedThumbProps), {
    value: scale(_value[0]),
    position: positions[0],
    dragging: active,
    label: typeof label === "function" ? label(scale(_value[0])) : label,
    ref: (node2) => {
      thumbs.current[0] = node2;
    },
    thumbLabel: thumbFromLabel,
    onMouseDown: (event) => handleThumbMouseDown(event, 0),
    onFocus: () => setFocused(0),
    showLabelOnHover: showLabelOnHover && hovered,
    disabled,
    unstyled,
    thumbSize
  }), hasArrayThumbChildren ? thumbChildren[0] : thumbChildren), import_react266.default.createElement(Thumb2, __spreadProps93(__spreadValues204({}, sharedThumbProps), {
    thumbLabel: thumbToLabel,
    value: scale(_value[1]),
    position: positions[1],
    dragging: active,
    label: typeof label === "function" ? label(scale(_value[1])) : label,
    ref: (node2) => {
      thumbs.current[1] = node2;
    },
    onMouseDown: (event) => handleThumbMouseDown(event, 1),
    onFocus: () => setFocused(1),
    showLabelOnHover: showLabelOnHover && hovered,
    disabled,
    unstyled,
    thumbSize
  }), hasArrayThumbChildren ? thumbChildren[1] : thumbChildren)), import_react266.default.createElement("input", {
    type: "hidden",
    name: `${name}_from`,
    value: _value[0]
  }), import_react266.default.createElement("input", {
    type: "hidden",
    name: `${name}_to`,
    value: _value[1]
  }));
});
RangeSlider.displayName = "@mantine/core/RangeSlider";

// node_modules/@mantine/core/esm/Space/Space.js
var import_react267 = __toESM(require_react());
var __defProp205 = Object.defineProperty;
var __getOwnPropSymbols207 = Object.getOwnPropertySymbols;
var __hasOwnProp207 = Object.prototype.hasOwnProperty;
var __propIsEnum207 = Object.prototype.propertyIsEnumerable;
var __defNormalProp205 = (obj, key, value) => key in obj ? __defProp205(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues205 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp207.call(b2, prop))
      __defNormalProp205(a2, prop, b2[prop]);
  if (__getOwnPropSymbols207)
    for (var prop of __getOwnPropSymbols207(b2)) {
      if (__propIsEnum207.call(b2, prop))
        __defNormalProp205(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest134 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp207.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols207)
    for (var prop of __getOwnPropSymbols207(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum207.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps101 = {
  w: 0,
  h: 0
};
var Space = (0, import_react267.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("Space", defaultProps101, props), { w, h } = _a, others = __objRest134(_a, ["w", "h"]);
  return import_react267.default.createElement(Box, __spreadValues205({
    ref,
    w,
    miw: w,
    h,
    mih: h
  }, others));
});
Space.displayName = "@mantine/core/Space";

// node_modules/@mantine/core/esm/Spoiler/Spoiler.js
var import_react268 = __toESM(require_react());

// node_modules/@mantine/core/esm/Spoiler/Spoiler.styles.js
var useStyles93 = createStyles((theme, { transitionDuration }) => ({
  control: {},
  root: {
    position: "relative"
  },
  content: {
    display: "flex",
    flexDirection: "column",
    overflow: "hidden",
    transitionProperty: "max-height",
    transitionTimingFunction: theme.transitionTimingFunction,
    transitionDuration: `${transitionDuration}ms`,
    "@media (prefers-reduced-motion)": {
      transitionDuration: theme.respectReducedMotion ? "0ms" : void 0
    }
  }
}));
var Spoiler_styles_default = useStyles93;

// node_modules/@mantine/core/esm/Spoiler/Spoiler.js
var __defProp206 = Object.defineProperty;
var __getOwnPropSymbols208 = Object.getOwnPropertySymbols;
var __hasOwnProp208 = Object.prototype.hasOwnProperty;
var __propIsEnum208 = Object.prototype.propertyIsEnumerable;
var __defNormalProp206 = (obj, key, value) => key in obj ? __defProp206(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues206 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp208.call(b2, prop))
      __defNormalProp206(a2, prop, b2[prop]);
  if (__getOwnPropSymbols208)
    for (var prop of __getOwnPropSymbols208(b2)) {
      if (__propIsEnum208.call(b2, prop))
        __defNormalProp206(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest135 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp208.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols208)
    for (var prop of __getOwnPropSymbols208(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum208.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps102 = {
  maxHeight: 100,
  transitionDuration: 200,
  initialState: false
};
var Spoiler = (0, import_react268.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("Spoiler", defaultProps102, props), {
    className,
    children,
    maxHeight,
    hideLabel,
    showLabel,
    transitionDuration,
    controlRef,
    initialState,
    classNames,
    styles: styles2,
    unstyled
  } = _a, others = __objRest135(_a, [
    "className",
    "children",
    "maxHeight",
    "hideLabel",
    "showLabel",
    "transitionDuration",
    "controlRef",
    "initialState",
    "classNames",
    "styles",
    "unstyled"
  ]);
  const { classes, cx } = Spoiler_styles_default({ transitionDuration }, { classNames, styles: styles2, unstyled, name: "Spoiler" });
  const [show, setShowState] = (0, import_react268.useState)(initialState);
  const [spoiler, setSpoilerState] = (0, import_react268.useState)(initialState);
  const { ref: contentRef, height } = useElementSize();
  const spoilerMoreContent = show ? hideLabel : showLabel;
  (0, import_react268.useEffect)(() => {
    setSpoilerState(maxHeight < height);
  }, [height, maxHeight, children]);
  return import_react268.default.createElement(Box, __spreadValues206({
    className: cx(classes.root, className),
    ref
  }, others), import_react268.default.createElement("div", {
    className: classes.content,
    style: {
      maxHeight: !show ? maxHeight : height || void 0
    }
  }, import_react268.default.createElement("div", {
    ref: contentRef
  }, children)), spoiler && import_react268.default.createElement(Anchor, {
    component: "button",
    ref: controlRef,
    onClick: () => setShowState((opened) => !opened),
    className: classes.control
  }, spoilerMoreContent));
});
Spoiler.displayName = "@mantine/core/Spoiler";

// node_modules/@mantine/core/esm/Stepper/Step/Step.js
var import_react269 = __toESM(require_react());

// node_modules/@mantine/core/esm/Stepper/Step/Step.styles.js
var __defProp207 = Object.defineProperty;
var __defProps94 = Object.defineProperties;
var __getOwnPropDescs94 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols209 = Object.getOwnPropertySymbols;
var __hasOwnProp209 = Object.prototype.hasOwnProperty;
var __propIsEnum209 = Object.prototype.propertyIsEnumerable;
var __defNormalProp207 = (obj, key, value) => key in obj ? __defProp207(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues207 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp209.call(b2, prop))
      __defNormalProp207(a2, prop, b2[prop]);
  if (__getOwnPropSymbols209)
    for (var prop of __getOwnPropSymbols209(b2)) {
      if (__propIsEnum209.call(b2, prop))
        __defNormalProp207(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps94 = (a2, b2) => __defProps94(a2, __getOwnPropDescs94(b2));
var iconSizes7 = {
  xs: 34,
  sm: 36,
  md: 42,
  lg: 48,
  xl: 52
};
var useStyles94 = createStyles((theme, { color, iconSize, size: size3, radius: radius2, allowStepClick, iconPosition, orientation }, getRef2) => {
  const _iconSize = iconSize || theme.fn.size({ size: size3, sizes: iconSizes7 });
  const iconMargin = size3 === "xl" || size3 === "lg" ? theme.spacing.md : theme.spacing.sm;
  const _radius = theme.fn.size({ size: radius2, sizes: theme.radius });
  const colors = theme.fn.variant({
    variant: "filled",
    color: color || theme.primaryColor,
    primaryFallback: false
  });
  const separatorDistanceFromIcon = theme.spacing.xs / 2;
  const verticalOrientationStyles = {
    step: {
      justifyContent: "flex-start",
      minHeight: `calc(${_iconSize}px + ${theme.spacing.xl}px + ${separatorDistanceFromIcon}px)`,
      marginTop: `${separatorDistanceFromIcon}px`,
      overflow: "hidden",
      "&:first-of-type": {
        marginTop: 0
      },
      [`&:last-of-type .${getRef2("verticalSeparator")}`]: {
        display: "none"
      }
    }
  };
  return {
    stepLoader: {},
    step: __spreadValues207({
      display: "flex",
      flexDirection: iconPosition === "left" ? "row" : "row-reverse",
      cursor: allowStepClick ? "pointer" : "default"
    }, orientation === "vertical" ? verticalOrientationStyles.step : {
      alignItems: "center"
    }),
    stepWrapper: {
      position: "relative"
    },
    verticalSeparator: {
      top: `${_iconSize + separatorDistanceFromIcon}px`,
      left: `${_iconSize / 2}px`,
      height: "100vh",
      position: "absolute",
      borderLeft: `2px solid ${theme.colorScheme === "dark" ? theme.colors.dark[5] : theme.colors.gray[1]}`,
      ref: getRef2("verticalSeparator")
    },
    verticalSeparatorActive: {
      borderColor: theme.fn.variant({ variant: "filled", color }).background
    },
    stepIcon: {
      boxSizing: "border-box",
      height: _iconSize,
      width: _iconSize,
      minWidth: _iconSize,
      borderRadius: _radius,
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      backgroundColor: theme.colorScheme === "dark" ? theme.colors.dark[5] : theme.colors.gray[1],
      border: `2px solid ${theme.colorScheme === "dark" ? theme.colors.dark[5] : theme.colors.gray[1]}`,
      transition: "background-color 150ms ease, border-color 150ms ease",
      position: "relative",
      fontWeight: 700,
      color: theme.colorScheme === "dark" ? theme.colors.dark[1] : theme.colors.gray[7],
      fontSize: theme.fn.size({ size: size3, sizes: theme.fontSizes }),
      "&[data-progress]": {
        borderColor: colors.background
      },
      "&[data-completed]": {
        backgroundColor: colors.background,
        borderColor: colors.background,
        color: theme.white
      }
    },
    stepCompletedIcon: __spreadProps94(__spreadValues207({}, theme.fn.cover()), {
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      color: theme.white
    }),
    stepBody: __spreadValues207({
      display: "flex",
      flexDirection: "column",
      marginLeft: iconPosition === "left" ? iconMargin : void 0,
      marginRight: iconPosition === "right" ? iconMargin : void 0
    }, orientation === "vertical" ? {
      marginTop: _iconSize > theme.fn.size({ size: size3, sizes: theme.fontSizes }) * 4 ? _iconSize / 4 : _iconSize / 12
    } : null),
    stepLabel: {
      textAlign: iconPosition,
      fontWeight: 500,
      fontSize: theme.fn.size({ size: size3, sizes: theme.fontSizes }),
      lineHeight: 1
    },
    stepDescription: {
      textAlign: iconPosition,
      marginTop: theme.fn.size({ size: size3, sizes: theme.spacing }) / 3,
      marginBottom: theme.fn.size({ size: size3, sizes: theme.spacing }) / 3,
      fontSize: theme.fn.size({ size: size3, sizes: theme.fontSizes }) - 2,
      lineHeight: 1
    }
  };
});
var Step_styles_default = useStyles94;

// node_modules/@mantine/core/esm/Stepper/Step/Step.js
var __defProp208 = Object.defineProperty;
var __getOwnPropSymbols210 = Object.getOwnPropertySymbols;
var __hasOwnProp210 = Object.prototype.hasOwnProperty;
var __propIsEnum210 = Object.prototype.propertyIsEnumerable;
var __defNormalProp208 = (obj, key, value) => key in obj ? __defProp208(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues208 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp210.call(b2, prop))
      __defNormalProp208(a2, prop, b2[prop]);
  if (__getOwnPropSymbols210)
    for (var prop of __getOwnPropSymbols210(b2)) {
      if (__propIsEnum210.call(b2, prop))
        __defNormalProp208(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest136 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp210.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols210)
    for (var prop of __getOwnPropSymbols210(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum210.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultIconSizes = {
  xs: 16,
  sm: 18,
  md: 20,
  lg: 22,
  xl: 24
};
var defaultProps103 = {
  withIcon: true,
  size: "md",
  radius: "xl",
  allowStepClick: true,
  iconPosition: "left",
  __staticSelector: "Step"
};
var getStepFragment = (Fragment5, step) => {
  if (typeof Fragment5 === "function") {
    return import_react269.default.createElement(Fragment5, {
      step
    });
  }
  return Fragment5;
};
var Step = (0, import_react269.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("StepperStep", defaultProps103, props), {
    className,
    step,
    state,
    color,
    icon,
    completedIcon,
    progressIcon,
    label,
    description,
    withIcon,
    iconSize,
    size: size3,
    radius: radius2,
    loading,
    allowStepClick,
    allowStepSelect,
    iconPosition,
    __staticSelector,
    classNames,
    styles: styles2,
    unstyled,
    orientation
  } = _a, others = __objRest136(_a, [
    "className",
    "step",
    "state",
    "color",
    "icon",
    "completedIcon",
    "progressIcon",
    "label",
    "description",
    "withIcon",
    "iconSize",
    "size",
    "radius",
    "loading",
    "allowStepClick",
    "allowStepSelect",
    "iconPosition",
    "__staticSelector",
    "classNames",
    "styles",
    "unstyled",
    "orientation"
  ]);
  const { classes, cx, theme } = Step_styles_default({ color, iconSize, size: size3, radius: radius2, allowStepClick, iconPosition, orientation }, { name: __staticSelector, classNames, styles: styles2, unstyled });
  const _iconSize = theme.fn.size({ size: size3, sizes: defaultIconSizes });
  const _icon = state === "stepCompleted" ? null : state === "stepProgress" ? progressIcon : icon;
  const dataAttributes = {
    "data-progress": state === "stepProgress" || void 0,
    "data-completed": state === "stepCompleted" || void 0
  };
  return import_react269.default.createElement(UnstyledButton, __spreadValues208(__spreadValues208({
    className: cx(classes.step, className),
    tabIndex: allowStepClick ? 0 : -1,
    ref
  }, dataAttributes), others), withIcon && import_react269.default.createElement("div", {
    className: classes.stepWrapper
  }, import_react269.default.createElement("div", __spreadValues208({
    className: classes.stepIcon
  }, dataAttributes), import_react269.default.createElement(Transition, {
    mounted: state === "stepCompleted",
    transition: "pop",
    duration: 200
  }, (transitionStyles) => import_react269.default.createElement("div", {
    className: classes.stepCompletedIcon,
    style: transitionStyles
  }, loading ? import_react269.default.createElement(Loader, {
    color: "#fff",
    size: _iconSize,
    className: classes.stepLoader
  }) : getStepFragment(completedIcon, step) || import_react269.default.createElement(CheckboxIcon, {
    indeterminate: false,
    width: _iconSize,
    height: _iconSize
  }))), state !== "stepCompleted" ? loading ? import_react269.default.createElement(Loader, {
    size: _iconSize,
    color
  }) : getStepFragment(_icon || icon, step) : null), orientation === "vertical" && import_react269.default.createElement("div", {
    className: cx(classes.verticalSeparator, {
      [classes.verticalSeparatorActive]: state === "stepCompleted"
    })
  })), (label || description) && import_react269.default.createElement("div", {
    className: classes.stepBody
  }, label && import_react269.default.createElement(Text, {
    className: classes.stepLabel
  }, getStepFragment(label, step)), description && import_react269.default.createElement(Text, {
    className: classes.stepDescription,
    color: "dimmed"
  }, getStepFragment(description, step))));
});
Step.displayName = "@mantine/core/Step";

// node_modules/@mantine/core/esm/Stepper/Stepper.js
var import_react270 = __toESM(require_react());

// node_modules/@mantine/core/esm/Stepper/StepCompleted/StepCompleted.js
function StepCompleted(props) {
  return null;
}
StepCompleted.displayName = "@mantine/core/StepCompleted";

// node_modules/@mantine/core/esm/Stepper/Stepper.styles.js
var __defProp209 = Object.defineProperty;
var __defProps95 = Object.defineProperties;
var __getOwnPropDescs95 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols211 = Object.getOwnPropertySymbols;
var __hasOwnProp211 = Object.prototype.hasOwnProperty;
var __propIsEnum211 = Object.prototype.propertyIsEnumerable;
var __defNormalProp209 = (obj, key, value) => key in obj ? __defProp209(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues209 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp211.call(b2, prop))
      __defNormalProp209(a2, prop, b2[prop]);
  if (__getOwnPropSymbols211)
    for (var prop of __getOwnPropSymbols211(b2)) {
      if (__propIsEnum211.call(b2, prop))
        __defNormalProp209(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps95 = (a2, b2) => __defProps95(a2, __getOwnPropDescs95(b2));
var useStyles95 = createStyles((theme, {
  contentPadding,
  color,
  orientation,
  iconPosition,
  iconSize,
  size: size3,
  breakpoint
}) => {
  const shouldBeResponsive = typeof breakpoint !== "undefined";
  const breakpointValue = theme.fn.size({ size: breakpoint, sizes: theme.breakpoints });
  const separatorOffset = typeof iconSize !== "undefined" ? iconSize / 2 - 1 : theme.fn.size({ size: size3, sizes: iconSizes7 }) / 2 - 1;
  const verticalOrientationStyles = {
    steps: {
      flexDirection: "column",
      alignItems: iconPosition === "left" ? "flex-start" : "flex-end"
    },
    separator: {
      width: 2,
      minHeight: theme.spacing.xl,
      marginLeft: iconPosition === "left" ? separatorOffset : 0,
      marginRight: iconPosition === "right" ? separatorOffset : 0,
      marginTop: `calc(${theme.spacing.xs}px / 2)`,
      marginBottom: `calc(${theme.spacing.xs}px - 2px)`
    }
  };
  const responsiveStyles = {
    steps: {
      [`@media (max-width: ${breakpointValue - 1}px)`]: verticalOrientationStyles.steps
    },
    separator: {
      [`@media (max-width: ${breakpointValue - 1}px)`]: verticalOrientationStyles.separator
    }
  };
  return {
    root: {},
    steps: __spreadValues209(__spreadValues209({
      display: "flex",
      boxSizing: "border-box",
      alignItems: "center"
    }, orientation === "vertical" ? verticalOrientationStyles.steps : null), shouldBeResponsive ? responsiveStyles.steps : null),
    separator: __spreadValues209(__spreadValues209({
      boxSizing: "border-box",
      transition: "background-color 150ms ease",
      flex: 1,
      height: 2,
      backgroundColor: theme.colorScheme === "dark" ? theme.colors.dark[4] : theme.colors.gray[2],
      marginLeft: theme.spacing.md,
      marginRight: theme.spacing.md
    }, orientation === "vertical" ? verticalOrientationStyles.separator : null), shouldBeResponsive ? responsiveStyles.separator : null),
    separatorActive: {
      backgroundColor: theme.fn.variant({
        variant: "filled",
        color: color || theme.primaryColor,
        primaryFallback: false
      }).background
    },
    content: __spreadProps95(__spreadValues209({}, theme.fn.fontStyles()), {
      paddingTop: theme.fn.size({ size: contentPadding, sizes: theme.spacing })
    })
  };
});
var Stepper_styles_default = useStyles95;

// node_modules/@mantine/core/esm/Stepper/Stepper.js
var __defProp210 = Object.defineProperty;
var __getOwnPropSymbols212 = Object.getOwnPropertySymbols;
var __hasOwnProp212 = Object.prototype.hasOwnProperty;
var __propIsEnum212 = Object.prototype.propertyIsEnumerable;
var __defNormalProp210 = (obj, key, value) => key in obj ? __defProp210(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues210 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp212.call(b2, prop))
      __defNormalProp210(a2, prop, b2[prop]);
  if (__getOwnPropSymbols212)
    for (var prop of __getOwnPropSymbols212(b2)) {
      if (__propIsEnum212.call(b2, prop))
        __defNormalProp210(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest137 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp212.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols212)
    for (var prop of __getOwnPropSymbols212(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum212.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps104 = {
  contentPadding: "md",
  size: "md",
  radius: "xl",
  orientation: "horizontal",
  iconPosition: "left",
  allowNextStepsSelect: true
};
var Stepper = (0, import_react270.forwardRef)((props, ref) => {
  var _b, _c, _d;
  const _a = useComponentDefaultProps("Stepper", defaultProps104, props), {
    className,
    children,
    onStepClick,
    active,
    icon,
    completedIcon,
    progressIcon,
    color,
    iconSize,
    contentPadding,
    size: size3,
    radius: radius2,
    orientation,
    breakpoint,
    iconPosition,
    allowNextStepsSelect,
    classNames,
    styles: styles2,
    unstyled
  } = _a, others = __objRest137(_a, [
    "className",
    "children",
    "onStepClick",
    "active",
    "icon",
    "completedIcon",
    "progressIcon",
    "color",
    "iconSize",
    "contentPadding",
    "size",
    "radius",
    "orientation",
    "breakpoint",
    "iconPosition",
    "allowNextStepsSelect",
    "classNames",
    "styles",
    "unstyled"
  ]);
  const { classes, cx } = Stepper_styles_default({ contentPadding, color, orientation, iconPosition, size: size3, iconSize, breakpoint }, { classNames, styles: styles2, unstyled, name: "Stepper" });
  const convertedChildren = import_react270.Children.toArray(children);
  const _children = convertedChildren.filter((child) => child.type !== StepCompleted);
  const completedStep = convertedChildren.find((item) => item.type === StepCompleted);
  const items = _children.reduce((acc, item, index6) => {
    const state = active === index6 ? "stepProgress" : active > index6 ? "stepCompleted" : "stepInactive";
    const shouldAllowSelect = () => {
      if (typeof onStepClick !== "function") {
        return false;
      }
      if (typeof item.props.allowStepSelect === "boolean") {
        return item.props.allowStepSelect;
      }
      return state === "stepCompleted" || allowNextStepsSelect;
    };
    const isStepSelectionEnabled = shouldAllowSelect();
    acc.push((0, import_react270.cloneElement)(item, {
      __staticSelector: "Stepper",
      icon: item.props.icon || icon || index6 + 1,
      key: index6,
      step: index6,
      state,
      onClick: () => isStepSelectionEnabled && onStepClick(index6),
      allowStepClick: isStepSelectionEnabled,
      completedIcon: item.props.completedIcon || completedIcon,
      progressIcon: item.props.progressIcon || progressIcon,
      color: item.props.color || color,
      iconSize,
      size: size3,
      radius: radius2,
      classNames,
      styles: styles2,
      iconPosition: item.props.iconPosition || iconPosition,
      orientation,
      unstyled
    }));
    if (orientation === "horizontal" && index6 !== _children.length - 1) {
      acc.push(import_react270.default.createElement("div", {
        className: cx(classes.separator, { [classes.separatorActive]: index6 < active }),
        key: `separator-${index6}`
      }));
    }
    return acc;
  }, []);
  const stepContent = (_c = (_b = _children[active]) == null ? void 0 : _b.props) == null ? void 0 : _c.children;
  const completedContent = (_d = completedStep == null ? void 0 : completedStep.props) == null ? void 0 : _d.children;
  const content = active > _children.length - 1 ? completedContent : stepContent;
  return import_react270.default.createElement(Box, __spreadValues210({
    className: cx(classes.root, className),
    ref
  }, others), import_react270.default.createElement("div", {
    className: classes.steps
  }, items), content && import_react270.default.createElement("div", {
    className: classes.content
  }, content));
});
Stepper.Step = Step;
Stepper.Completed = StepCompleted;
Stepper.displayName = "@mantine/core/Stepper";

// node_modules/@mantine/core/esm/Switch/Switch.js
var import_react273 = __toESM(require_react());

// node_modules/@mantine/core/esm/Switch/SwitchGroup/SwitchGroup.js
var import_react272 = __toESM(require_react());

// node_modules/@mantine/core/esm/Switch/SwitchGroup.context.js
var import_react271 = __toESM(require_react());
var SwitchGroupContext = (0, import_react271.createContext)(null);
var SwitchGroupProvider = SwitchGroupContext.Provider;
var useSwitchGroupContext = () => (0, import_react271.useContext)(SwitchGroupContext);

// node_modules/@mantine/core/esm/Switch/SwitchGroup/SwitchGroup.js
var __defProp211 = Object.defineProperty;
var __getOwnPropSymbols213 = Object.getOwnPropertySymbols;
var __hasOwnProp213 = Object.prototype.hasOwnProperty;
var __propIsEnum213 = Object.prototype.propertyIsEnumerable;
var __defNormalProp211 = (obj, key, value) => key in obj ? __defProp211(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues211 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp213.call(b2, prop))
      __defNormalProp211(a2, prop, b2[prop]);
  if (__getOwnPropSymbols213)
    for (var prop of __getOwnPropSymbols213(b2)) {
      if (__propIsEnum213.call(b2, prop))
        __defNormalProp211(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest138 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp213.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols213)
    for (var prop of __getOwnPropSymbols213(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum213.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps105 = {
  orientation: "horizontal",
  spacing: "lg",
  size: "sm",
  offset: "xs"
};
var SwitchGroup = (0, import_react272.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("SwitchGroup", defaultProps105, props), {
    children,
    value,
    defaultValue,
    onChange,
    orientation,
    spacing,
    size: size3,
    wrapperProps,
    offset: offset3
  } = _a, others = __objRest138(_a, [
    "children",
    "value",
    "defaultValue",
    "onChange",
    "orientation",
    "spacing",
    "size",
    "wrapperProps",
    "offset"
  ]);
  const [_value, setValue] = useUncontrolled({
    value,
    defaultValue,
    finalValue: [],
    onChange
  });
  const handleChange = (event) => {
    const itemValue = event.currentTarget.value;
    setValue(_value.includes(itemValue) ? _value.filter((item) => item !== itemValue) : [..._value, itemValue]);
  };
  return import_react272.default.createElement(SwitchGroupProvider, {
    value: { value: _value, onChange: handleChange, size: size3 }
  }, import_react272.default.createElement(Input.Wrapper, __spreadValues211(__spreadValues211({
    labelElement: "div",
    size: size3,
    __staticSelector: "SwitchGroup",
    ref
  }, wrapperProps), others), import_react272.default.createElement(InputsGroup, {
    spacing,
    orientation,
    offset: offset3
  }, children)));
});
SwitchGroup.displayName = "@mantine/core/SwitchGroup";

// node_modules/@mantine/core/esm/Switch/Switch.styles.js
var __defProp212 = Object.defineProperty;
var __defProps96 = Object.defineProperties;
var __getOwnPropDescs96 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols214 = Object.getOwnPropertySymbols;
var __hasOwnProp214 = Object.prototype.hasOwnProperty;
var __propIsEnum214 = Object.prototype.propertyIsEnumerable;
var __defNormalProp212 = (obj, key, value) => key in obj ? __defProp212(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues212 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp214.call(b2, prop))
      __defNormalProp212(a2, prop, b2[prop]);
  if (__getOwnPropSymbols214)
    for (var prop of __getOwnPropSymbols214(b2)) {
      if (__propIsEnum214.call(b2, prop))
        __defNormalProp212(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps96 = (a2, b2) => __defProps96(a2, __getOwnPropDescs96(b2));
var switchHeight = {
  xs: 16,
  sm: 20,
  md: 24,
  lg: 30,
  xl: 36
};
var switchWidth = {
  xs: 32,
  sm: 38,
  md: 46,
  lg: 56,
  xl: 72
};
var handleSizes = {
  xs: 12,
  sm: 14,
  md: 18,
  lg: 22,
  xl: 28
};
var labelFontSizes = {
  xs: 5,
  sm: 6,
  md: 7,
  lg: 9,
  xl: 11
};
var trackLabelPaddings = {
  xs: 4,
  sm: 5,
  md: 6,
  lg: 8,
  xl: 10
};
var useStyles96 = createStyles((theme, { size: size3, radius: radius2, color, labelPosition, error }) => {
  const handleSize = theme.fn.size({ size: size3, sizes: handleSizes });
  const borderRadius = theme.fn.size({ size: radius2, sizes: theme.radius });
  const colors = theme.fn.variant({ variant: "filled", color });
  const trackWidth = theme.fn.size({ size: size3, sizes: switchWidth });
  const trackPadding = size3 === "xs" ? 1 : 2;
  const errorColor = theme.fn.variant({ variant: "filled", color: "red" }).background;
  return {
    input: {
      clip: "rect(1px, 1px, 1px, 1px)",
      height: 0,
      width: 0,
      overflow: "hidden",
      whiteSpace: "nowrap",
      padding: 0,
      WebkitClipPath: "inset(50%)",
      clipPath: "inset(50%)",
      position: "absolute"
    },
    track: __spreadProps96(__spreadValues212({}, theme.fn.focusStyles("input:focus + &")), {
      cursor: theme.cursorType,
      overflow: "hidden",
      WebkitTapHighlightColor: "transparent",
      position: "relative",
      borderRadius,
      backgroundColor: theme.colorScheme === "dark" ? theme.colors.dark[6] : theme.colors.gray[2],
      border: `1px solid ${error ? errorColor : theme.colorScheme === "dark" ? theme.colors.dark[4] : theme.colors.gray[3]}`,
      height: theme.fn.size({ size: size3, sizes: switchHeight }),
      minWidth: trackWidth,
      margin: 0,
      transitionProperty: "background-color, border-color",
      transitionTimingFunction: theme.transitionTimingFunction,
      transitionDuration: "150ms",
      boxSizing: "border-box",
      appearance: "none",
      display: "flex",
      alignItems: "center",
      fontSize: theme.fn.size({ size: size3, sizes: labelFontSizes }),
      fontWeight: 600,
      order: labelPosition === "left" ? 2 : 1,
      userSelect: "none",
      MozUserSelect: "none",
      WebkitUserSelect: "none",
      MsUserSelect: "none",
      zIndex: 0,
      lineHeight: 0,
      color: theme.colorScheme === "dark" ? theme.colors.dark[1] : theme.colors.gray[6],
      transition: `color 150ms ${theme.transitionTimingFunction}`,
      "input:checked + &": {
        backgroundColor: colors.background,
        borderColor: colors.background,
        color: theme.white,
        transition: `color 150ms ${theme.transitionTimingFunction}`
      },
      "input:disabled + &": {
        backgroundColor: theme.colorScheme === "dark" ? theme.colors.dark[4] : theme.colors.gray[2],
        borderColor: theme.colorScheme === "dark" ? theme.colors.dark[4] : theme.colors.gray[2],
        cursor: "not-allowed"
      }
    }),
    thumb: {
      position: "absolute",
      zIndex: 1,
      borderRadius,
      boxSizing: "border-box",
      display: "flex",
      backgroundColor: theme.white,
      height: handleSize,
      width: handleSize,
      border: `1px solid ${theme.colorScheme === "dark" ? theme.white : theme.colors.gray[3]}`,
      left: `${trackPadding}px`,
      transition: `left 150ms ${theme.transitionTimingFunction}`,
      "& > *": {
        margin: "auto"
      },
      "@media (prefers-reduced-motion)": {
        transitionDuration: theme.respectReducedMotion ? "0ms" : ""
      },
      "input:checked + * > &": {
        left: `calc(100% - ${handleSize}px - ${trackPadding}px)`,
        borderColor: theme.white
      },
      "input:disabled + * > &": {
        borderColor: theme.colorScheme === "dark" ? theme.colors.dark[4] : theme.colors.gray[2],
        backgroundColor: theme.colorScheme === "dark" ? theme.colors.dark[3] : theme.colors.gray[0]
      }
    },
    trackLabel: {
      height: "100%",
      display: "grid",
      placeContent: "center",
      minWidth: trackWidth - handleSize,
      paddingInline: theme.fn.size({ size: size3, sizes: trackLabelPaddings }),
      margin: `0 0 0 ${handleSize + trackPadding}px`,
      transition: `margin 150ms ${theme.transitionTimingFunction}`,
      "input:checked + * > &": {
        margin: `0 ${handleSize + trackPadding}px 0 0`
      }
    }
  };
});
var Switch_styles_default = useStyles96;

// node_modules/@mantine/core/esm/Switch/Switch.js
var __defProp213 = Object.defineProperty;
var __defProps97 = Object.defineProperties;
var __getOwnPropDescs97 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols215 = Object.getOwnPropertySymbols;
var __hasOwnProp215 = Object.prototype.hasOwnProperty;
var __propIsEnum215 = Object.prototype.propertyIsEnumerable;
var __defNormalProp213 = (obj, key, value) => key in obj ? __defProp213(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues213 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp215.call(b2, prop))
      __defNormalProp213(a2, prop, b2[prop]);
  if (__getOwnPropSymbols215)
    for (var prop of __getOwnPropSymbols215(b2)) {
      if (__propIsEnum215.call(b2, prop))
        __defNormalProp213(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps97 = (a2, b2) => __defProps97(a2, __getOwnPropDescs97(b2));
var __objRest139 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp215.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols215)
    for (var prop of __getOwnPropSymbols215(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum215.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps106 = {
  offLabel: "",
  onLabel: "",
  size: "sm",
  radius: "xl",
  error: false
};
var Switch = (0, import_react273.forwardRef)((props, ref) => {
  var _b;
  const _a = useComponentDefaultProps("Switch", defaultProps106, props), {
    className,
    color,
    label,
    offLabel,
    onLabel,
    id,
    style,
    size: size3,
    radius: radius2,
    wrapperProps,
    children,
    unstyled,
    styles: styles2,
    classNames,
    thumbIcon,
    sx,
    checked,
    defaultChecked,
    onChange,
    labelPosition,
    description,
    error,
    disabled
  } = _a, others = __objRest139(_a, [
    "className",
    "color",
    "label",
    "offLabel",
    "onLabel",
    "id",
    "style",
    "size",
    "radius",
    "wrapperProps",
    "children",
    "unstyled",
    "styles",
    "classNames",
    "thumbIcon",
    "sx",
    "checked",
    "defaultChecked",
    "onChange",
    "labelPosition",
    "description",
    "error",
    "disabled"
  ]);
  const ctx = useSwitchGroupContext();
  const { classes } = Switch_styles_default({ size: (ctx == null ? void 0 : ctx.size) || size3, color, radius: radius2, labelPosition, error: !!error }, { unstyled, styles: styles2, classNames, name: "Switch" });
  const { systemStyles, rest } = extractSystemStyles(others);
  const uuid = useId(id);
  const contextProps = ctx ? {
    checked: ctx.value.includes(rest.value),
    onChange: ctx.onChange
  } : {};
  const [_checked, handleChange] = useUncontrolled({
    value: (_b = contextProps.checked) != null ? _b : checked,
    defaultValue: defaultChecked,
    finalValue: false
  });
  return import_react273.default.createElement(InlineInput, __spreadValues213(__spreadValues213({
    className,
    sx,
    style,
    id: uuid,
    size: (ctx == null ? void 0 : ctx.size) || size3,
    labelPosition,
    label,
    description,
    error,
    disabled,
    __staticSelector: "Switch",
    classNames,
    styles: styles2,
    unstyled,
    "data-checked": contextProps.checked || void 0
  }, systemStyles), wrapperProps), import_react273.default.createElement("input", __spreadProps97(__spreadValues213({}, rest), {
    disabled,
    checked: _checked,
    onChange: (event) => {
      ctx ? contextProps.onChange(event) : onChange == null ? void 0 : onChange(event);
      handleChange(event.currentTarget.checked);
    },
    id: uuid,
    ref,
    type: "checkbox",
    className: classes.input
  })), import_react273.default.createElement("label", {
    htmlFor: uuid,
    className: classes.track
  }, import_react273.default.createElement("div", {
    className: classes.thumb
  }, thumbIcon), import_react273.default.createElement("div", {
    className: classes.trackLabel
  }, _checked ? onLabel : offLabel)));
});
Switch.displayName = "@mantine/core/Switch";
Switch.Group = SwitchGroup;

// node_modules/@mantine/core/esm/Table/Table.js
var import_react274 = __toESM(require_react());

// node_modules/@mantine/core/esm/Table/Table.styles.js
var __defProp214 = Object.defineProperty;
var __defProps98 = Object.defineProperties;
var __getOwnPropDescs98 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols216 = Object.getOwnPropertySymbols;
var __hasOwnProp216 = Object.prototype.hasOwnProperty;
var __propIsEnum216 = Object.prototype.propertyIsEnumerable;
var __defNormalProp214 = (obj, key, value) => key in obj ? __defProp214(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues214 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp216.call(b2, prop))
      __defNormalProp214(a2, prop, b2[prop]);
  if (__getOwnPropSymbols216)
    for (var prop of __getOwnPropSymbols216(b2)) {
      if (__propIsEnum216.call(b2, prop))
        __defNormalProp214(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps98 = (a2, b2) => __defProps98(a2, __getOwnPropDescs98(b2));
var useStyles97 = createStyles((theme, {
  captionSide,
  horizontalSpacing,
  verticalSpacing,
  fontSize,
  withBorder,
  withColumnBorders
}) => {
  const border = `1px solid ${theme.colorScheme === "dark" ? theme.colors.dark[4] : theme.colors.gray[3]}`;
  return {
    root: __spreadProps98(__spreadValues214({}, theme.fn.fontStyles()), {
      width: "100%",
      borderCollapse: "collapse",
      captionSide,
      color: theme.colorScheme === "dark" ? theme.colors.dark[0] : theme.black,
      lineHeight: theme.lineHeight,
      border: withBorder ? border : "",
      "& caption": {
        marginTop: captionSide === "top" ? 0 : theme.spacing.xs,
        marginBottom: captionSide === "bottom" ? 0 : theme.spacing.xs,
        fontSize: theme.fontSizes.sm,
        color: theme.colorScheme === "dark" ? theme.colors.dark[2] : theme.colors.gray[6]
      },
      "& thead tr th, & tfoot tr th, & tbody tr th": {
        textAlign: "left",
        fontWeight: "bold",
        color: theme.colorScheme === "dark" ? theme.colors.dark[0] : theme.colors.gray[7],
        fontSize: theme.fn.size({ size: fontSize, sizes: theme.fontSizes }),
        padding: `${theme.fn.size({
          size: verticalSpacing,
          sizes: theme.spacing
        })}px ${theme.fn.size({ size: horizontalSpacing, sizes: theme.spacing })}px`
      },
      "& thead tr th": {
        borderBottom: border
      },
      "& tfoot tr th, & tbody tr th": {
        borderTop: border
      },
      "& tbody tr td": {
        padding: `${theme.fn.size({
          size: verticalSpacing,
          sizes: theme.spacing
        })}px ${theme.fn.size({ size: horizontalSpacing, sizes: theme.spacing })}px`,
        borderTop: border,
        fontSize: theme.fn.size({ size: fontSize, sizes: theme.fontSizes })
      },
      "& tbody tr:first-of-type td, & tbody tr:first-of-type th": {
        borderTop: "none"
      },
      "& thead th, & tbody td": {
        borderRight: withColumnBorders ? border : "none",
        "&:last-of-type": {
          borderRight: "none",
          borderLeft: withColumnBorders ? border : "none"
        }
      },
      "& tbody tr th": {
        borderRight: withColumnBorders ? border : "none"
      },
      "&[data-striped] tbody tr:nth-of-type(odd)": {
        backgroundColor: theme.colorScheme === "dark" ? theme.colors.dark[6] : theme.colors.gray[0]
      },
      "&[data-hover] tbody tr": theme.fn.hover({
        backgroundColor: theme.colorScheme === "dark" ? theme.colors.dark[5] : theme.colors.gray[1]
      })
    })
  };
});
var Table_styles_default = useStyles97;

// node_modules/@mantine/core/esm/Table/Table.js
var __defProp215 = Object.defineProperty;
var __defProps99 = Object.defineProperties;
var __getOwnPropDescs99 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols217 = Object.getOwnPropertySymbols;
var __hasOwnProp217 = Object.prototype.hasOwnProperty;
var __propIsEnum217 = Object.prototype.propertyIsEnumerable;
var __defNormalProp215 = (obj, key, value) => key in obj ? __defProp215(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues215 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp217.call(b2, prop))
      __defNormalProp215(a2, prop, b2[prop]);
  if (__getOwnPropSymbols217)
    for (var prop of __getOwnPropSymbols217(b2)) {
      if (__propIsEnum217.call(b2, prop))
        __defNormalProp215(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps99 = (a2, b2) => __defProps99(a2, __getOwnPropDescs99(b2));
var __objRest140 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp217.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols217)
    for (var prop of __getOwnPropSymbols217(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum217.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps107 = {
  striped: false,
  highlightOnHover: false,
  captionSide: "top",
  horizontalSpacing: "xs",
  fontSize: "sm",
  verticalSpacing: 7,
  withBorder: false,
  withColumnBorders: false
};
var Table = (0, import_react274.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("Table", defaultProps107, props), {
    className,
    children,
    striped,
    highlightOnHover,
    captionSide,
    horizontalSpacing,
    verticalSpacing,
    fontSize,
    unstyled,
    withBorder,
    withColumnBorders
  } = _a, others = __objRest140(_a, [
    "className",
    "children",
    "striped",
    "highlightOnHover",
    "captionSide",
    "horizontalSpacing",
    "verticalSpacing",
    "fontSize",
    "unstyled",
    "withBorder",
    "withColumnBorders"
  ]);
  const { classes, cx } = Table_styles_default({ captionSide, verticalSpacing, horizontalSpacing, fontSize, withBorder, withColumnBorders }, { unstyled, name: "Table" });
  return import_react274.default.createElement(Box, __spreadProps99(__spreadValues215({}, others), {
    component: "table",
    ref,
    className: cx(classes.root, className),
    "data-striped": striped || void 0,
    "data-hover": highlightOnHover || void 0
  }), children);
});
Table.displayName = "@mantine/core/Table";

// node_modules/@mantine/core/esm/Tabs/Tabs.js
var import_react279 = __toESM(require_react());

// node_modules/@mantine/core/esm/Tabs/TabsList/TabsList.js
var import_react275 = __toESM(require_react());

// node_modules/@mantine/core/esm/Tabs/Tabs.errors.js
var TABS_ERRORS = {
  context: "Tabs component was not found in the tree",
  value: "Tabs.Tab or Tabs.Panel component was rendered with invalid value or without value"
};

// node_modules/@mantine/core/esm/Tabs/Tabs.context.js
var [TabsContextProvider, useTabsContext] = createSafeContext(TABS_ERRORS.context);

// node_modules/@mantine/core/esm/Tabs/TabsList/TabsList.styles.js
var __defProp216 = Object.defineProperty;
var __getOwnPropSymbols218 = Object.getOwnPropertySymbols;
var __hasOwnProp218 = Object.prototype.hasOwnProperty;
var __propIsEnum218 = Object.prototype.propertyIsEnumerable;
var __defNormalProp216 = (obj, key, value) => key in obj ? __defProp216(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues216 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp218.call(b2, prop))
      __defNormalProp216(a2, prop, b2[prop]);
  if (__getOwnPropSymbols218)
    for (var prop of __getOwnPropSymbols218(b2)) {
      if (__propIsEnum218.call(b2, prop))
        __defNormalProp216(a2, prop, b2[prop]);
    }
  return a2;
};
function getVariantStyles8({ variant: variant2, orientation, inverted, placement }, theme) {
  const vertical = orientation === "vertical";
  if (variant2 === "default") {
    return {
      [vertical ? placement === "left" ? "borderRight" : "borderLeft" : inverted ? "borderTop" : "borderBottom"]: `2px solid ${theme.colorScheme === "dark" ? theme.colors.dark[4] : theme.colors.gray[3]}`
    };
  }
  if (variant2 === "outline") {
    return {
      [vertical ? placement === "left" ? "borderRight" : "borderLeft" : inverted ? "borderTop" : "borderBottom"]: `1px solid ${theme.colorScheme === "dark" ? theme.colors.dark[4] : theme.colors.gray[3]}`
    };
  }
  if (variant2 === "pills") {
    return {
      gap: `calc(${theme.spacing.sm}px / 2)`
    };
  }
  return {};
}
var useStyles98 = createStyles((theme, params) => {
  const vertical = params.orientation === "vertical";
  return {
    tabsList: __spreadValues216({
      display: "flex",
      flexWrap: "wrap",
      flexDirection: vertical ? "column" : "row",
      justifyContent: GROUP_POSITIONS[params.position],
      '& [role="tab"]': {
        flex: params.grow ? 1 : void 0
      }
    }, getVariantStyles8(params, theme))
  };
});
var TabsList_styles_default = useStyles98;

// node_modules/@mantine/core/esm/Tabs/TabsList/TabsList.js
var __defProp217 = Object.defineProperty;
var __defProps100 = Object.defineProperties;
var __getOwnPropDescs100 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols219 = Object.getOwnPropertySymbols;
var __hasOwnProp219 = Object.prototype.hasOwnProperty;
var __propIsEnum219 = Object.prototype.propertyIsEnumerable;
var __defNormalProp217 = (obj, key, value) => key in obj ? __defProp217(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues217 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp219.call(b2, prop))
      __defNormalProp217(a2, prop, b2[prop]);
  if (__getOwnPropSymbols219)
    for (var prop of __getOwnPropSymbols219(b2)) {
      if (__propIsEnum219.call(b2, prop))
        __defNormalProp217(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps100 = (a2, b2) => __defProps100(a2, __getOwnPropDescs100(b2));
var __objRest141 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp219.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols219)
    for (var prop of __getOwnPropSymbols219(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum219.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps108 = {
  grow: false,
  position: "left"
};
var TabsList = (0, import_react275.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("TabsList", defaultProps108, props), { children, className, grow, position: position2 } = _a, others = __objRest141(_a, ["children", "className", "grow", "position"]);
  const { orientation, variant: variant2, color, radius: radius2, inverted, placement, classNames, styles: styles2, unstyled } = useTabsContext();
  const { classes, cx } = TabsList_styles_default({ orientation, grow, variant: variant2, color, position: position2, radius: radius2, inverted, placement }, { name: "Tabs", unstyled, classNames, styles: styles2 });
  return import_react275.default.createElement(Box, __spreadProps100(__spreadValues217({}, others), {
    className: cx(classes.tabsList, className),
    ref,
    role: "tablist",
    "aria-orientation": orientation
  }), children);
});
TabsList.displayName = "@mantine/core/TabsList";

// node_modules/@mantine/core/esm/Tabs/TabsPanel/TabsPanel.js
var import_react276 = __toESM(require_react());

// node_modules/@mantine/core/esm/Tabs/TabsPanel/TabsPanel.styles.js
var useStyles99 = createStyles((_theme, { orientation }) => ({
  panel: {
    flex: orientation === "vertical" ? 1 : void 0
  }
}));
var TabsPanel_styles_default = useStyles99;

// node_modules/@mantine/core/esm/Tabs/TabsPanel/TabsPanel.js
var __defProp218 = Object.defineProperty;
var __defProps101 = Object.defineProperties;
var __getOwnPropDescs101 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols220 = Object.getOwnPropertySymbols;
var __hasOwnProp220 = Object.prototype.hasOwnProperty;
var __propIsEnum220 = Object.prototype.propertyIsEnumerable;
var __defNormalProp218 = (obj, key, value) => key in obj ? __defProp218(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues218 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp220.call(b2, prop))
      __defNormalProp218(a2, prop, b2[prop]);
  if (__getOwnPropSymbols220)
    for (var prop of __getOwnPropSymbols220(b2)) {
      if (__propIsEnum220.call(b2, prop))
        __defNormalProp218(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps101 = (a2, b2) => __defProps101(a2, __getOwnPropDescs101(b2));
var __objRest142 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp220.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols220)
    for (var prop of __getOwnPropSymbols220(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum220.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps109 = {};
var TabsPanel = (0, import_react276.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("TabsPanel", defaultProps109, props), { value, children, sx, className } = _a, others = __objRest142(_a, ["value", "children", "sx", "className"]);
  const ctx = useTabsContext();
  const { classes, cx } = TabsPanel_styles_default({
    orientation: ctx.orientation,
    variant: ctx.variant,
    color: ctx.color,
    radius: ctx.radius,
    inverted: ctx.inverted,
    placement: ctx.placement
  }, { name: "Tabs", unstyled: ctx.unstyled, classNames: ctx.classNames, styles: ctx.styles });
  const active = ctx.value === value;
  const content = ctx.keepMounted ? children : active ? children : null;
  return import_react276.default.createElement(Box, __spreadProps101(__spreadValues218({}, others), {
    ref,
    sx: [{ display: !active ? "none" : void 0 }, ...packSx(sx)],
    className: cx(classes.panel, className),
    role: "tabpanel",
    id: ctx.getPanelId(value),
    "aria-labelledby": ctx.getTabId(value)
  }), content);
});
TabsPanel.displayName = "@mantine/core/TabsPanel";

// node_modules/@mantine/core/esm/Tabs/Tab/Tab.js
var import_react277 = __toESM(require_react());

// node_modules/@mantine/core/esm/Tabs/Tab/Tab.styles.js
var __defProp219 = Object.defineProperty;
var __defProps102 = Object.defineProperties;
var __getOwnPropDescs102 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols221 = Object.getOwnPropertySymbols;
var __hasOwnProp221 = Object.prototype.hasOwnProperty;
var __propIsEnum221 = Object.prototype.propertyIsEnumerable;
var __defNormalProp219 = (obj, key, value) => key in obj ? __defProp219(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues219 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp221.call(b2, prop))
      __defNormalProp219(a2, prop, b2[prop]);
  if (__getOwnPropSymbols221)
    for (var prop of __getOwnPropSymbols221(b2)) {
      if (__propIsEnum221.call(b2, prop))
        __defNormalProp219(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps102 = (a2, b2) => __defProps102(a2, __getOwnPropDescs102(b2));
function getVariantStyles9(theme, { variant: variant2, orientation, color, radius: radius2, inverted, placement }) {
  const vertical = orientation === "vertical";
  const filledScheme = theme.fn.variant({ color, variant: "filled" });
  const radiusValue = theme.fn.radius(radius2);
  const borderRadius = orientation === "vertical" ? placement === "left" ? `${radiusValue}px 0 0 ${radiusValue}px` : ` 0 ${radiusValue}px ${radiusValue}px 0` : inverted ? `0 0 ${radiusValue}px ${radiusValue}px` : `${radiusValue}px ${radiusValue}px 0 0`;
  if (variant2 === "default") {
    return __spreadProps102(__spreadValues219({
      [vertical ? placement === "left" ? "borderRight" : "borderLeft" : inverted ? "borderTop" : "borderBottom"]: "2px solid transparent",
      [vertical ? placement === "left" ? "marginRight" : "marginLeft" : inverted ? "marginTop" : "marginBottom"]: -2,
      borderRadius
    }, theme.fn.hover({
      backgroundColor: theme.colorScheme === "dark" ? theme.colors.dark[6] : theme.colors.gray[0],
      borderColor: theme.colorScheme === "dark" ? theme.colors.dark[4] : theme.colors.gray[3]
    })), {
      "&[data-active]": __spreadValues219({
        borderColor: filledScheme.background,
        color: theme.colorScheme === "dark" ? theme.white : theme.black
      }, theme.fn.hover({ borderColor: filledScheme.background }))
    });
  }
  if (variant2 === "outline") {
    return {
      borderRadius,
      border: "1px solid transparent",
      [vertical ? placement === "left" ? "borderRight" : "borderLeft" : inverted ? "borderTop" : "borderBottom"]: "none",
      "&[data-active]": {
        borderColor: theme.colorScheme === "dark" ? theme.colors.dark[4] : theme.colors.gray[3],
        "&::before": {
          content: '""',
          backgroundColor: theme.colorScheme === "dark" ? theme.colors.dark[7] : theme.white,
          position: "absolute",
          bottom: vertical ? 0 : inverted ? "unset" : -1,
          top: vertical ? 0 : inverted ? -1 : "unset",
          [vertical ? "width" : "height"]: 1,
          right: vertical ? placement === "left" ? -1 : "unset" : 0,
          left: vertical ? placement === "left" ? "unset" : -1 : 0
        }
      }
    };
  }
  if (variant2 === "pills") {
    return __spreadProps102(__spreadValues219({
      borderRadius: theme.fn.radius(radius2)
    }, theme.fn.hover({
      backgroundColor: theme.colorScheme === "dark" ? theme.colors.dark[6] : theme.colors.gray[0]
    })), {
      "&[data-active]": __spreadValues219({
        backgroundColor: filledScheme.background,
        color: theme.white
      }, theme.fn.hover({ backgroundColor: filledScheme.background }))
    });
  }
  return {};
}
var useStyles100 = createStyles((theme, params) => ({
  tabLabel: {},
  tab: __spreadValues219({
    position: "relative",
    padding: `${theme.spacing.xs}px ${theme.spacing.md}px`,
    paddingLeft: params.withIcon ? theme.spacing.xs : void 0,
    paddingRight: params.withRightSection ? theme.spacing.xs : void 0,
    fontSize: theme.fontSizes.sm,
    whiteSpace: "nowrap",
    zIndex: 0,
    display: "flex",
    alignItems: "center",
    justifyContent: params.orientation === "horizontal" ? "center" : void 0,
    lineHeight: 1,
    "&:disabled": __spreadValues219({
      opacity: 0.5,
      cursor: "not-allowed"
    }, theme.fn.hover({ backgroundColor: "transparent" })),
    "&:focus": {
      zIndex: 1
    }
  }, getVariantStyles9(theme, params)),
  tabRightSection: {
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    "&:not(:only-child)": {
      marginLeft: 7
    }
  },
  tabIcon: {
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    "&:not(:only-child)": {
      marginRight: 7
    }
  }
}));
var Tab_styles_default = useStyles100;

// node_modules/@mantine/core/esm/Tabs/Tab/Tab.js
var __defProp220 = Object.defineProperty;
var __defProps103 = Object.defineProperties;
var __getOwnPropDescs103 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols222 = Object.getOwnPropertySymbols;
var __hasOwnProp222 = Object.prototype.hasOwnProperty;
var __propIsEnum222 = Object.prototype.propertyIsEnumerable;
var __defNormalProp220 = (obj, key, value) => key in obj ? __defProp220(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues220 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp222.call(b2, prop))
      __defNormalProp220(a2, prop, b2[prop]);
  if (__getOwnPropSymbols222)
    for (var prop of __getOwnPropSymbols222(b2)) {
      if (__propIsEnum222.call(b2, prop))
        __defNormalProp220(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps103 = (a2, b2) => __defProps103(a2, __getOwnPropDescs103(b2));
var __objRest143 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp222.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols222)
    for (var prop of __getOwnPropSymbols222(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum222.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps110 = {};
var Tab = (0, import_react277.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("TabsTab", defaultProps110, props), { value, children, onKeyDown, onClick, className, icon, rightSection, color } = _a, others = __objRest143(_a, ["value", "children", "onKeyDown", "onClick", "className", "icon", "rightSection", "color"]);
  const ctx = useTabsContext();
  const hasIcon = !!icon;
  const hasRightSection = !!rightSection;
  const { theme, classes, cx } = Tab_styles_default({
    withIcon: hasIcon || hasRightSection && !children,
    withRightSection: hasRightSection || hasIcon && !children,
    orientation: ctx.orientation,
    color: color || ctx.color,
    variant: ctx.variant,
    radius: ctx.radius,
    inverted: ctx.inverted,
    placement: ctx.placement
  }, { name: "Tabs", unstyled: ctx.unstyled, classNames: ctx.classNames, styles: ctx.styles });
  const isActive = value === ctx.value;
  const activateTab = (event) => {
    ctx.onTabChange(ctx.allowTabDeactivation ? value === ctx.value ? null : value : value);
    onClick == null ? void 0 : onClick(event);
  };
  return import_react277.default.createElement(UnstyledButton, __spreadProps103(__spreadValues220({}, others), {
    unstyled: ctx.unstyled,
    className: cx(classes.tab, className),
    "data-active": isActive || void 0,
    ref,
    type: "button",
    role: "tab",
    id: ctx.getTabId(value),
    "aria-selected": isActive,
    tabIndex: isActive || ctx.value === null ? 0 : -1,
    "aria-controls": ctx.getPanelId(value),
    onClick: activateTab,
    onKeyDown: createScopedKeydownHandler({
      siblingSelector: '[role="tab"]',
      parentSelector: '[role="tablist"]',
      activateOnFocus: ctx.activateTabWithKeyboard,
      loop: ctx.loop,
      dir: theme.dir,
      orientation: ctx.orientation,
      onKeyDown
    })
  }), icon && import_react277.default.createElement("div", {
    className: classes.tabIcon
  }, icon), children && import_react277.default.createElement("div", {
    className: classes.tabLabel
  }, children), rightSection && import_react277.default.createElement("div", {
    className: classes.tabRightSection
  }, rightSection));
});
Tab.displayName = "@mantine/core/Tab";

// node_modules/@mantine/core/esm/Tabs/TabsProvider.js
var import_react278 = __toESM(require_react());
function TabsProvider({
  defaultValue,
  value,
  onTabChange,
  orientation,
  children,
  loop,
  id,
  activateTabWithKeyboard,
  allowTabDeactivation,
  variant: variant2,
  color,
  radius: radius2,
  inverted,
  placement,
  keepMounted = true,
  classNames,
  styles: styles2,
  unstyled
}) {
  const uid = useId(id);
  const [_value, onChange] = useUncontrolled({
    value,
    defaultValue,
    finalValue: null,
    onChange: onTabChange
  });
  return import_react278.default.createElement(TabsContextProvider, {
    value: {
      placement,
      value: _value,
      orientation,
      id: uid,
      loop,
      activateTabWithKeyboard,
      getTabId: getSafeId(`${uid}-tab`, TABS_ERRORS.value),
      getPanelId: getSafeId(`${uid}-panel`, TABS_ERRORS.value),
      onTabChange: onChange,
      allowTabDeactivation,
      variant: variant2,
      color,
      radius: radius2,
      inverted,
      keepMounted,
      classNames,
      styles: styles2,
      unstyled
    }
  }, children);
}
TabsProvider.displayName = "@mantine/core/TabsProvider";

// node_modules/@mantine/core/esm/Tabs/Tabs.styles.js
var useStyles101 = createStyles((_theme, { orientation, placement }) => ({
  root: {
    display: orientation === "vertical" ? "flex" : void 0,
    flexDirection: placement === "right" ? "row-reverse" : "row"
  }
}));
var Tabs_styles_default = useStyles101;

// node_modules/@mantine/core/esm/Tabs/Tabs.js
var __defProp221 = Object.defineProperty;
var __defProps104 = Object.defineProperties;
var __getOwnPropDescs104 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols223 = Object.getOwnPropertySymbols;
var __hasOwnProp223 = Object.prototype.hasOwnProperty;
var __propIsEnum223 = Object.prototype.propertyIsEnumerable;
var __defNormalProp221 = (obj, key, value) => key in obj ? __defProp221(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues221 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp223.call(b2, prop))
      __defNormalProp221(a2, prop, b2[prop]);
  if (__getOwnPropSymbols223)
    for (var prop of __getOwnPropSymbols223(b2)) {
      if (__propIsEnum223.call(b2, prop))
        __defNormalProp221(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps104 = (a2, b2) => __defProps104(a2, __getOwnPropDescs104(b2));
var __objRest144 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp223.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols223)
    for (var prop of __getOwnPropSymbols223(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum223.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps111 = {
  orientation: "horizontal",
  loop: true,
  activateTabWithKeyboard: true,
  allowTabDeactivation: false,
  unstyled: false,
  inverted: false,
  variant: "default",
  placement: "left"
};
var Tabs = (0, import_react279.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("Tabs", defaultProps111, props), {
    defaultValue,
    value,
    orientation,
    loop,
    activateTabWithKeyboard,
    allowTabDeactivation,
    children,
    id,
    onTabChange,
    variant: variant2,
    color,
    className,
    unstyled,
    classNames,
    styles: styles2,
    radius: radius2,
    inverted,
    keepMounted,
    placement
  } = _a, others = __objRest144(_a, [
    "defaultValue",
    "value",
    "orientation",
    "loop",
    "activateTabWithKeyboard",
    "allowTabDeactivation",
    "children",
    "id",
    "onTabChange",
    "variant",
    "color",
    "className",
    "unstyled",
    "classNames",
    "styles",
    "radius",
    "inverted",
    "keepMounted",
    "placement"
  ]);
  const { classes, cx } = Tabs_styles_default({ orientation, color, variant: variant2, radius: radius2, inverted, placement }, { unstyled, name: "Tabs", classNames, styles: styles2 });
  return import_react279.default.createElement(TabsProvider, {
    activateTabWithKeyboard,
    defaultValue,
    orientation,
    onTabChange,
    value,
    id,
    loop,
    allowTabDeactivation,
    color,
    variant: variant2,
    radius: radius2,
    inverted,
    keepMounted,
    placement,
    classNames,
    styles: styles2,
    unstyled
  }, import_react279.default.createElement(Box, __spreadProps104(__spreadValues221({}, others), {
    className: cx(classes.root, className),
    id,
    ref
  }), children));
});
Tabs.List = TabsList;
Tabs.Tab = Tab;
Tabs.Panel = TabsPanel;
Tabs.displayName = "@mantine/core/Tabs";

// node_modules/@mantine/core/esm/ThemeIcon/ThemeIcon.js
var import_react280 = __toESM(require_react());

// node_modules/@mantine/core/esm/ThemeIcon/ThemeIcon.styles.js
var __defProp222 = Object.defineProperty;
var __defProps105 = Object.defineProperties;
var __getOwnPropDescs105 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols224 = Object.getOwnPropertySymbols;
var __hasOwnProp224 = Object.prototype.hasOwnProperty;
var __propIsEnum224 = Object.prototype.propertyIsEnumerable;
var __defNormalProp222 = (obj, key, value) => key in obj ? __defProp222(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues222 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp224.call(b2, prop))
      __defNormalProp222(a2, prop, b2[prop]);
  if (__getOwnPropSymbols224)
    for (var prop of __getOwnPropSymbols224(b2)) {
      if (__propIsEnum224.call(b2, prop))
        __defNormalProp222(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps105 = (a2, b2) => __defProps105(a2, __getOwnPropDescs105(b2));
var sizes23 = {
  xs: 16,
  sm: 20,
  md: 26,
  lg: 32,
  xl: 40
};
var useStyles102 = createStyles((theme, { color, size: size3, radius: radius2, gradient: gradient2, variant: variant2 }) => {
  const colors = theme.fn.variant({
    variant: variant2,
    color: color || theme.primaryColor,
    gradient: gradient2,
    primaryFallback: false
  });
  const iconSize = theme.fn.size({ size: size3, sizes: sizes23 });
  return {
    root: __spreadProps105(__spreadValues222({}, theme.fn.fontStyles()), {
      display: "inline-flex",
      alignItems: "center",
      justifyContent: "center",
      boxSizing: "border-box",
      width: iconSize,
      height: iconSize,
      minWidth: iconSize,
      minHeight: iconSize,
      borderRadius: theme.fn.radius(radius2),
      backgroundColor: colors.background,
      color: colors.color,
      backgroundImage: variant2 === "gradient" ? colors.background : void 0,
      border: `${variant2 === "gradient" ? 0 : 1}px solid ${colors.border}`
    })
  };
});
var ThemeIcon_styles_default = useStyles102;

// node_modules/@mantine/core/esm/ThemeIcon/ThemeIcon.js
var __defProp223 = Object.defineProperty;
var __getOwnPropSymbols225 = Object.getOwnPropertySymbols;
var __hasOwnProp225 = Object.prototype.hasOwnProperty;
var __propIsEnum225 = Object.prototype.propertyIsEnumerable;
var __defNormalProp223 = (obj, key, value) => key in obj ? __defProp223(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues223 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp225.call(b2, prop))
      __defNormalProp223(a2, prop, b2[prop]);
  if (__getOwnPropSymbols225)
    for (var prop of __getOwnPropSymbols225(b2)) {
      if (__propIsEnum225.call(b2, prop))
        __defNormalProp223(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest145 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp225.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols225)
    for (var prop of __getOwnPropSymbols225(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum225.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps112 = {
  size: "md",
  variant: "filled"
};
var ThemeIcon = (0, import_react280.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("ThemeIcon", defaultProps112, props), { className, size: size3, radius: radius2, variant: variant2, color, children, gradient: gradient2, unstyled } = _a, others = __objRest145(_a, ["className", "size", "radius", "variant", "color", "children", "gradient", "unstyled"]);
  const { classes, cx } = ThemeIcon_styles_default({ variant: variant2, radius: radius2, color, size: size3, gradient: gradient2 }, { name: "ThemeIcon", unstyled });
  return import_react280.default.createElement(Box, __spreadValues223({
    className: cx(classes.root, className),
    ref
  }, others), children);
});
ThemeIcon.displayName = "@mantine/core/ThemeIcon";

// node_modules/@mantine/core/esm/Timeline/Timeline.js
var import_react282 = __toESM(require_react());

// node_modules/@mantine/core/esm/Timeline/TimelineItem/TimelineItem.js
var import_react281 = __toESM(require_react());

// node_modules/@mantine/core/esm/Timeline/TimelineItem/TimelineItem.styles.js
var useStyles103 = createStyles((theme, { bulletSize, color, radius: radius2, align, lineVariant, lineWidth }) => {
  const colors = theme.fn.variant({ variant: "filled", color });
  return {
    itemBody: {},
    itemContent: {},
    itemBullet: {
      boxSizing: "border-box",
      width: bulletSize,
      height: bulletSize,
      borderRadius: theme.fn.size({ size: radius2, sizes: theme.radius }),
      border: `${lineWidth}px solid ${theme.colorScheme === "dark" ? theme.colors.dark[4] : theme.colors.gray[3]}`,
      backgroundColor: theme.colorScheme === "dark" ? theme.colors.dark[7] : theme.white,
      position: "absolute",
      top: 0,
      left: align === "left" ? -bulletSize / 2 - lineWidth / 2 : "auto",
      right: align === "right" ? -bulletSize / 2 - lineWidth / 2 : "auto",
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      color: theme.white,
      "&[data-with-child]": {
        borderWidth: 1,
        backgroundColor: theme.colorScheme === "dark" ? theme.colors.dark[4] : theme.colors.gray[3],
        color: theme.colorScheme === "dark" ? theme.colors.dark[0] : theme.black
      },
      "&[data-active]": {
        borderColor: colors.background,
        backgroundColor: theme.white,
        "&[data-with-child]": {
          backgroundColor: colors.background,
          color: theme.white
        }
      }
    },
    item: {
      position: "relative",
      boxSizing: "border-box",
      color: theme.colorScheme === "dark" ? theme.colors.dark[0] : theme.black,
      paddingLeft: align === "left" ? theme.spacing.xl : 0,
      paddingRight: align === "right" ? theme.spacing.xl : 0,
      textAlign: align,
      "&:not(:last-of-type)::before": {
        display: "block"
      },
      "&:not(:first-of-type)": {
        marginTop: theme.spacing.xl
      },
      "&::before": {
        boxSizing: "border-box",
        position: "absolute",
        top: 0,
        left: align === "left" ? -lineWidth : "auto",
        right: align === "right" ? -lineWidth : "auto",
        bottom: -theme.spacing.xl,
        borderLeft: `${lineWidth}px ${lineVariant} ${theme.colorScheme === "dark" ? theme.colors.dark[4] : theme.colors.gray[3]}`,
        content: '""',
        display: "none"
      },
      "&[data-line-active]": {
        "&::before": {
          borderLeftColor: colors.background
        }
      }
    },
    itemTitle: {
      fontWeight: 500,
      lineHeight: 1,
      marginBottom: `calc(${theme.spacing.xs}px / 2)`,
      textAlign: align
    }
  };
});
var TimelineItem_styles_default = useStyles103;

// node_modules/@mantine/core/esm/Timeline/TimelineItem/TimelineItem.js
var __defProp224 = Object.defineProperty;
var __getOwnPropSymbols226 = Object.getOwnPropertySymbols;
var __hasOwnProp226 = Object.prototype.hasOwnProperty;
var __propIsEnum226 = Object.prototype.propertyIsEnumerable;
var __defNormalProp224 = (obj, key, value) => key in obj ? __defProp224(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues224 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp226.call(b2, prop))
      __defNormalProp224(a2, prop, b2[prop]);
  if (__getOwnPropSymbols226)
    for (var prop of __getOwnPropSymbols226(b2)) {
      if (__propIsEnum226.call(b2, prop))
        __defNormalProp224(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest146 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp226.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols226)
    for (var prop of __getOwnPropSymbols226(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum226.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps113 = {
  bulletSize: 20,
  radius: "xl",
  lineWidth: 4,
  lineVariant: "solid"
};
var TimelineItem = (0, import_react281.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("TimelineItem", defaultProps113, props), {
    className,
    bullet,
    title,
    bulletSize,
    radius: radius2,
    lineWidth,
    active,
    lineActive,
    classNames,
    styles: styles2,
    children,
    color,
    align,
    lineVariant,
    unstyled
  } = _a, others = __objRest146(_a, [
    "className",
    "bullet",
    "title",
    "bulletSize",
    "radius",
    "lineWidth",
    "active",
    "lineActive",
    "classNames",
    "styles",
    "children",
    "color",
    "align",
    "lineVariant",
    "unstyled"
  ]);
  const { classes, cx } = TimelineItem_styles_default({ bulletSize, color, radius: radius2, align, lineVariant, lineWidth }, { classNames, styles: styles2, unstyled, name: "Timeline" });
  return import_react281.default.createElement(Box, __spreadValues224({
    className: cx(classes.item, className),
    "data-line-active": lineActive || void 0,
    "data-active": active || void 0,
    ref
  }, others), import_react281.default.createElement("div", {
    className: classes.itemBullet,
    "data-with-child": !!bullet || void 0,
    "data-active": active || void 0
  }, bullet), import_react281.default.createElement("div", {
    className: classes.itemBody
  }, title && import_react281.default.createElement(Text, {
    className: classes.itemTitle
  }, title), import_react281.default.createElement("div", {
    className: classes.itemContent
  }, children)));
});
TimelineItem.displayName = "@mantine/core/TimelineItem";

// node_modules/@mantine/core/esm/Timeline/Timeline.js
var __defProp225 = Object.defineProperty;
var __getOwnPropSymbols227 = Object.getOwnPropertySymbols;
var __hasOwnProp227 = Object.prototype.hasOwnProperty;
var __propIsEnum227 = Object.prototype.propertyIsEnumerable;
var __defNormalProp225 = (obj, key, value) => key in obj ? __defProp225(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues225 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp227.call(b2, prop))
      __defNormalProp225(a2, prop, b2[prop]);
  if (__getOwnPropSymbols227)
    for (var prop of __getOwnPropSymbols227(b2)) {
      if (__propIsEnum227.call(b2, prop))
        __defNormalProp225(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest147 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp227.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols227)
    for (var prop of __getOwnPropSymbols227(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum227.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps114 = {
  active: -1,
  radius: "xl",
  bulletSize: 20,
  align: "left",
  lineWidth: 4,
  reverseActive: false
};
var Timeline = (0, import_react282.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("Timeline", defaultProps114, props), {
    children,
    active,
    color,
    radius: radius2,
    bulletSize,
    align,
    lineWidth,
    classNames,
    styles: styles2,
    sx,
    reverseActive,
    unstyled
  } = _a, others = __objRest147(_a, [
    "children",
    "active",
    "color",
    "radius",
    "bulletSize",
    "align",
    "lineWidth",
    "classNames",
    "styles",
    "sx",
    "reverseActive",
    "unstyled"
  ]);
  const _children = import_react282.Children.toArray(children);
  const items = _children.map((item, index6) => import_react282.default.cloneElement(item, {
    classNames,
    styles: styles2,
    align,
    lineWidth,
    radius: item.props.radius || radius2,
    color: item.props.color || color,
    bulletSize: item.props.bulletSize || bulletSize,
    unstyled,
    active: item.props.active || (reverseActive ? active >= _children.length - index6 - 1 : active >= index6),
    lineActive: item.props.lineActive || (reverseActive ? active >= _children.length - index6 - 1 : active - 1 >= index6)
  }));
  const offset3 = align === "left" ? { paddingLeft: bulletSize / 2 + lineWidth / 2 } : { paddingRight: bulletSize / 2 + lineWidth / 2 };
  return import_react282.default.createElement(Box, __spreadValues225({
    ref,
    sx: [offset3, ...packSx(sx)]
  }, others), items);
});
Timeline.Item = TimelineItem;
Timeline.displayName = "@mantine/core/Timeline";

// node_modules/@mantine/core/esm/Title/Title.js
var import_react283 = __toESM(require_react());

// node_modules/@mantine/core/esm/Title/Title.styles.js
var __defProp226 = Object.defineProperty;
var __defProps106 = Object.defineProperties;
var __getOwnPropDescs106 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols228 = Object.getOwnPropertySymbols;
var __hasOwnProp228 = Object.prototype.hasOwnProperty;
var __propIsEnum228 = Object.prototype.propertyIsEnumerable;
var __defNormalProp226 = (obj, key, value) => key in obj ? __defProp226(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues226 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp228.call(b2, prop))
      __defNormalProp226(a2, prop, b2[prop]);
  if (__getOwnPropSymbols228)
    for (var prop of __getOwnPropSymbols228(b2)) {
      if (__propIsEnum228.call(b2, prop))
        __defNormalProp226(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps106 = (a2, b2) => __defProps106(a2, __getOwnPropDescs106(b2));
function getFontSize(size3, element, theme) {
  if (typeof size3 !== "undefined") {
    return size3 in theme.headings.sizes ? theme.headings.sizes[size3].fontSize : size3;
  }
  return theme.headings.sizes[element].fontSize;
}
function getLineHeight(size3, element, theme) {
  if (typeof size3 !== "undefined" && size3 in theme.headings.sizes) {
    return theme.headings.sizes[size3].lineHeight;
  }
  return theme.headings.sizes[element].lineHeight;
}
var useStyles104 = createStyles((theme, { element, weight, size: size3, inline: inline2 }) => ({
  root: __spreadProps106(__spreadValues226({}, theme.fn.fontStyles()), {
    fontFamily: theme.headings.fontFamily,
    fontWeight: weight || theme.headings.sizes[element].fontWeight || theme.headings.fontWeight,
    fontSize: getFontSize(size3, element, theme),
    lineHeight: inline2 ? 1 : getLineHeight(size3, element, theme),
    margin: 0
  })
}));
var Title_styles_default = useStyles104;

// node_modules/@mantine/core/esm/Title/Title.js
var __defProp227 = Object.defineProperty;
var __getOwnPropSymbols229 = Object.getOwnPropertySymbols;
var __hasOwnProp229 = Object.prototype.hasOwnProperty;
var __propIsEnum229 = Object.prototype.propertyIsEnumerable;
var __defNormalProp227 = (obj, key, value) => key in obj ? __defProp227(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues227 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp229.call(b2, prop))
      __defNormalProp227(a2, prop, b2[prop]);
  if (__getOwnPropSymbols229)
    for (var prop of __getOwnPropSymbols229(b2)) {
      if (__propIsEnum229.call(b2, prop))
        __defNormalProp227(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest148 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp229.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols229)
    for (var prop of __getOwnPropSymbols229(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum229.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var defaultProps115 = {
  order: 1
};
var Title = (0, import_react283.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("Title", defaultProps115, props), { className, order: order2, children, unstyled, size: size3, weight, inline: inline2 } = _a, others = __objRest148(_a, ["className", "order", "children", "unstyled", "size", "weight", "inline"]);
  const { classes, cx } = Title_styles_default({ element: `h${order2}`, weight, size: size3, inline: inline2 }, { name: "Title", unstyled });
  if (![1, 2, 3, 4, 5, 6].includes(order2)) {
    return null;
  }
  return import_react283.default.createElement(Text, __spreadValues227({
    component: `h${order2}`,
    ref,
    className: cx(classes.root, className)
  }, others), children);
});
Title.displayName = "@mantine/core/Title";

// node_modules/@mantine/core/esm/TransferList/TransferList.js
var import_react287 = __toESM(require_react());

// node_modules/@mantine/core/esm/TransferList/RenderList/RenderList.js
var import_react284 = __toESM(require_react());

// node_modules/@mantine/core/esm/TransferList/RenderList/RenderList.styles.js
var ITEM_PADDING = 7;
var useStyles105 = createStyles((theme, { reversed, native, radius: radius2 }) => ({
  transferList: {
    flex: 1,
    display: "flex",
    flexDirection: "column"
  },
  transferListItem: {
    display: "block",
    width: `calc(100% - ${ITEM_PADDING * 2}px)`,
    padding: ITEM_PADDING,
    marginLeft: `calc(${theme.spacing.sm}px - ${ITEM_PADDING}px)`,
    marginRight: `calc(${theme.spacing.sm}px - ${ITEM_PADDING}px)`,
    borderRadius: theme.fn.radius(radius2),
    "&:first-of-type": {
      marginTop: `calc(${theme.spacing.sm}px - ${ITEM_PADDING}px)`
    },
    "&:last-of-type": {
      marginBottom: `calc(${theme.spacing.sm}px - ${ITEM_PADDING}px)`
    }
  },
  transferListItemHovered: {
    backgroundColor: theme.colorScheme === "dark" ? theme.colors.dark[6] : theme.colors.gray[0]
  },
  transferListItems: {
    overflow: native ? "auto" : "hidden"
  },
  transferListHeader: {
    display: "flex",
    flexDirection: reversed ? "row-reverse" : "row"
  },
  transferListBody: {
    flex: 1,
    borderRadius: theme.fn.radius(radius2),
    backgroundColor: theme.colorScheme === "dark" ? theme.colors.dark[7] : theme.white,
    border: `1px solid ${theme.colorScheme === "dark" ? theme.colors.dark[4] : theme.colors.gray[4]}`
  },
  transferListTitle: {
    marginBottom: 5
  },
  transferListSearch: {
    borderTopWidth: 0,
    borderRightWidth: 0,
    borderLeftWidth: 0,
    borderTopLeftRadius: reversed ? 0 : `calc(${theme.fn.radius(radius2)}px - 1px)`,
    borderTopRightRadius: reversed ? `calc(${theme.fn.radius(radius2)}px - 1px)` : 0,
    display: "block",
    borderBottomColor: theme.colorScheme === "dark" ? theme.colors.dark[4] : theme.colors.gray[4]
  },
  transferListControl: {
    borderTop: 0,
    borderRightWidth: reversed ? void 0 : 0,
    borderLeftWidth: reversed ? 0 : void 0,
    borderColor: theme.colorScheme === "dark" ? theme.colors.dark[4] : theme.colors.gray[4],
    "&:last-of-type": {
      borderTopLeftRadius: reversed ? `calc(${theme.fn.radius(radius2)}px - 1px)` : 0,
      borderTopRightRadius: reversed ? 0 : `calc(${theme.fn.radius(radius2)}px - 1px)`
    },
    "&:disabled": {
      backgroundColor: theme.colorScheme === "dark" ? theme.colors.dark[6] : "transparent",
      borderColor: theme.colorScheme === "dark" ? theme.colors.dark[4] : theme.colors.gray[4]
    }
  },
  separator: {
    boxSizing: "border-box",
    textAlign: "left",
    width: "100%",
    padding: "7px 12px"
  },
  separatorLabel: {
    color: theme.colorScheme === "dark" ? theme.colors.dark[3] : theme.colors.gray[5]
  }
}));
var RenderList_styles_default = useStyles105;

// node_modules/@mantine/core/esm/TransferList/RenderList/RenderList.js
var icons2 = {
  Prev: PrevIcon,
  Next: NextIcon,
  First: FirstIcon,
  Last: LastIcon
};
var rtlIons = {
  Next: PrevIcon,
  Prev: NextIcon,
  Last: FirstIcon,
  First: LastIcon
};
function RenderList({
  className,
  data,
  onSelect,
  selection,
  itemComponent: ItemComponent,
  listComponent,
  transferIcon: TransferIcon,
  transferAllIcon: TransferAllIcon,
  transferAllMatchingFilter,
  searchPlaceholder,
  query,
  onSearch,
  filter,
  nothingFound,
  placeholder,
  title,
  showTransferAll,
  reversed,
  onMoveAll,
  onMove,
  height,
  radius: radius2,
  classNames,
  styles: styles2,
  limit,
  unstyled
}) {
  const { classes, cx, theme } = RenderList_styles_default({ reversed, native: listComponent !== SelectScrollArea, radius: radius2 }, { name: "TransferList", classNames, styles: styles2, unstyled });
  const unGroupedItems = [];
  const groupedItems = [];
  const [hovered, setHovered] = (0, import_react284.useState)(-1);
  const filteredData = data.filter((item) => filter(query, item)).slice(0, limit);
  const ListComponent = listComponent || "div";
  const Icons = theme.dir === "rtl" ? rtlIons : icons2;
  const itemsRefs = (0, import_react284.useRef)({});
  const sortedData = groupOptions({ data: filteredData });
  const { scrollIntoView, targetRef, scrollableRef } = useScrollIntoView({
    duration: 0,
    offset: 5,
    cancelable: false,
    isList: true
  });
  let groupName = null;
  sortedData.forEach((item, index6) => {
    const itemComponent = import_react284.default.createElement(UnstyledButton, {
      unstyled,
      tabIndex: -1,
      onClick: () => onSelect(item.value),
      key: item.value,
      onMouseEnter: () => setHovered(index6),
      className: cx(classes.transferListItem, {
        [classes.transferListItemHovered]: index6 === hovered
      }),
      ref: (node2) => {
        if (itemsRefs && itemsRefs.current) {
          itemsRefs.current[item.value] = node2;
        }
      }
    }, import_react284.default.createElement(ItemComponent, {
      data: item,
      selected: selection.includes(item.value),
      radius: radius2
    }));
    if (!item.group) {
      unGroupedItems.push(itemComponent);
    } else {
      if (groupName !== item.group) {
        groupName = item.group;
        groupedItems.push(import_react284.default.createElement("div", {
          className: classes.separator,
          key: groupName
        }, import_react284.default.createElement(Divider, {
          classNames: { label: classes.separatorLabel },
          label: groupName
        })));
      }
      groupedItems.push(itemComponent);
    }
  });
  if (groupedItems.length > 0 && unGroupedItems.length > 0) {
    unGroupedItems.unshift(import_react284.default.createElement("div", {
      className: classes.separator
    }, import_react284.default.createElement(Divider, {
      unstyled,
      classNames: { label: classes.separatorLabel }
    })));
  }
  const handleSearchKeydown = (event) => {
    switch (event.key) {
      case "Enter": {
        event.preventDefault();
        if (filteredData[hovered]) {
          onSelect(filteredData[hovered].value);
        }
        break;
      }
      case "ArrowDown": {
        event.preventDefault();
        setHovered((current) => {
          var _a;
          const nextIndex = current < filteredData.length - 1 ? current + 1 : current;
          targetRef.current = itemsRefs.current[(_a = filteredData[nextIndex]) == null ? void 0 : _a.value];
          scrollIntoView({
            alignment: "end"
          });
          return nextIndex;
        });
        break;
      }
      case "ArrowUp": {
        event.preventDefault();
        setHovered((current) => {
          var _a;
          const nextIndex = current > 0 ? current - 1 : current;
          targetRef.current = itemsRefs.current[(_a = filteredData[nextIndex]) == null ? void 0 : _a.value];
          scrollIntoView({
            alignment: "start"
          });
          return nextIndex;
        });
      }
    }
  };
  const transferIcon = reversed ? import_react284.default.createElement(Icons.Prev, null) : import_react284.default.createElement(Icons.Next, null);
  const transferAllIcon = reversed ? import_react284.default.createElement(Icons.First, null) : import_react284.default.createElement(Icons.Last, null);
  return import_react284.default.createElement("div", {
    className: cx(classes.transferList, className)
  }, title && import_react284.default.createElement(Text, {
    weight: 500,
    unstyled,
    className: classes.transferListTitle
  }, title), import_react284.default.createElement("div", {
    className: classes.transferListBody
  }, import_react284.default.createElement("div", {
    className: classes.transferListHeader
  }, import_react284.default.createElement(TextInput, {
    unstyled,
    value: query,
    onChange: (event) => {
      onSearch(event.currentTarget.value);
      setHovered(0);
    },
    onFocus: () => setHovered(0),
    onBlur: () => setHovered(-1),
    placeholder: searchPlaceholder,
    radius: 0,
    onKeyDown: handleSearchKeydown,
    sx: { flex: 1 },
    classNames: { input: classes.transferListSearch }
  }), import_react284.default.createElement(ActionIcon, {
    variant: "default",
    size: 36,
    radius: 0,
    className: classes.transferListControl,
    disabled: selection.length === 0,
    onClick: onMove,
    unstyled
  }, TransferIcon ? import_react284.default.createElement(TransferIcon, {
    reversed
  }) : transferIcon), showTransferAll && import_react284.default.createElement(ActionIcon, {
    variant: "default",
    size: 36,
    radius: 0,
    className: classes.transferListControl,
    disabled: transferAllMatchingFilter ? filteredData.length === 0 : data.length === 0,
    onClick: onMoveAll,
    unstyled
  }, TransferAllIcon ? import_react284.default.createElement(TransferAllIcon, {
    reversed
  }) : transferAllIcon)), import_react284.default.createElement(ListComponent, {
    ref: scrollableRef,
    onMouseLeave: () => setHovered(-1),
    className: classes.transferListItems,
    style: { height, position: "relative", overflowX: "hidden" }
  }, groupedItems.length > 0 || unGroupedItems.length > 0 ? import_react284.default.createElement(import_react284.default.Fragment, null, groupedItems, unGroupedItems) : import_react284.default.createElement(Text, {
    color: "dimmed",
    unstyled,
    size: "sm",
    align: "center",
    mt: "sm"
  }, !query && placeholder ? placeholder : nothingFound))));
}
RenderList.displayName = "@mantine/core/RenderList";

// node_modules/@mantine/core/esm/TransferList/DefaultItem/DefaultItem.js
var import_react285 = __toESM(require_react());
var DefaultItem3 = import_react285.default.memo(({ data, selected, radius: radius2 }) => import_react285.default.createElement(Checkbox, {
  checked: selected,
  onChange: () => {
  },
  label: data.label,
  tabIndex: -1,
  radius: radius2,
  sx: { pointerEvents: "none" }
}));

// node_modules/@mantine/core/esm/TransferList/use-selection-state/use-selection-state.js
var import_react286 = __toESM(require_react());
function useSelectionState(initialSelection = [[], []]) {
  const [selection, setSelection] = (0, import_react286.useState)(initialSelection);
  const handleSelect = (listIndex, value) => setSelection((currentSelection) => {
    const listSelection = currentSelection[listIndex];
    let result = listSelection;
    if (typeof value === "string") {
      if (listSelection.includes(value)) {
        result = listSelection.filter((item) => item !== value);
      } else {
        result = [...listSelection, value];
      }
    }
    const clone = [...currentSelection];
    clone[listIndex] = result;
    return clone;
  });
  const handleDeselect = (listIndex, values) => setSelection((currentSelection) => {
    const clone = [...currentSelection];
    clone[listIndex] = currentSelection[listIndex].filter((item) => !values.includes(item));
    return clone;
  });
  const handleDeselectAll = (listIndex) => setSelection((currentSelection) => {
    const clone = [...currentSelection];
    clone[listIndex] = [];
    return clone;
  });
  const handlers = {
    select: handleSelect,
    deselect: handleDeselect,
    deselectAll: handleDeselectAll
  };
  return [selection, handlers];
}

// node_modules/@mantine/core/esm/TransferList/TransferList.js
var __defProp228 = Object.defineProperty;
var __defProps107 = Object.defineProperties;
var __getOwnPropDescs107 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols230 = Object.getOwnPropertySymbols;
var __hasOwnProp230 = Object.prototype.hasOwnProperty;
var __propIsEnum230 = Object.prototype.propertyIsEnumerable;
var __defNormalProp228 = (obj, key, value) => key in obj ? __defProp228(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues228 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp230.call(b2, prop))
      __defNormalProp228(a2, prop, b2[prop]);
  if (__getOwnPropSymbols230)
    for (var prop of __getOwnPropSymbols230(b2)) {
      if (__propIsEnum230.call(b2, prop))
        __defNormalProp228(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps107 = (a2, b2) => __defProps107(a2, __getOwnPropDescs107(b2));
var __objRest149 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp230.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols230)
    for (var prop of __getOwnPropSymbols230(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum230.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
function defaultFilter4(query, item) {
  return item.label.toLowerCase().trim().includes(query.toLowerCase().trim());
}
var defaultProps116 = {
  itemComponent: DefaultItem3,
  filter: defaultFilter4,
  titles: [null, null],
  placeholder: [null, null],
  listHeight: 150,
  listComponent: SelectScrollArea,
  showTransferAll: true,
  limit: Infinity,
  transferAllMatchingFilter: false
};
var TransferList = (0, import_react287.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("TransferList", defaultProps116, props), {
    value,
    onChange,
    itemComponent,
    searchPlaceholder,
    searchValues,
    onSearch,
    filter,
    nothingFound,
    placeholder,
    titles,
    initialSelection,
    listHeight,
    listComponent,
    showTransferAll,
    breakpoint,
    radius: radius2,
    classNames,
    styles: styles2,
    limit,
    unstyled,
    transferIcon,
    transferAllIcon,
    transferAllMatchingFilter
  } = _a, others = __objRest149(_a, [
    "value",
    "onChange",
    "itemComponent",
    "searchPlaceholder",
    "searchValues",
    "onSearch",
    "filter",
    "nothingFound",
    "placeholder",
    "titles",
    "initialSelection",
    "listHeight",
    "listComponent",
    "showTransferAll",
    "breakpoint",
    "radius",
    "classNames",
    "styles",
    "limit",
    "unstyled",
    "transferIcon",
    "transferAllIcon",
    "transferAllMatchingFilter"
  ]);
  const [selection, handlers] = useSelectionState(initialSelection);
  const [search, handleSearch] = useUncontrolled({
    value: searchValues,
    defaultValue: ["", ""],
    finalValue: ["", ""],
    onChange: onSearch
  });
  const handleMoveAll = (listIndex) => {
    const items = Array(2);
    const moveToIndex = listIndex === 0 ? 1 : 0;
    if (transferAllMatchingFilter) {
      const query = search[listIndex];
      const shownItems = value[listIndex].filter((item) => filter(query, item)).slice(0, limit);
      const hiddenItems = value[listIndex].filter((item) => !filter(query, item));
      items[listIndex] = hiddenItems;
      items[moveToIndex] = [...value[moveToIndex], ...shownItems];
    } else {
      items[listIndex] = [];
      items[moveToIndex] = [...value[moveToIndex], ...value[listIndex]];
    }
    onChange(items);
    handlers.deselectAll(listIndex);
  };
  const handleMove = (listIndex) => {
    const moveToIndex = listIndex === 0 ? 1 : 0;
    const items = Array(2);
    const transferData = value[listIndex].reduce((acc, item) => {
      if (!selection[listIndex].includes(item.value)) {
        acc.filtered.push(item);
      } else {
        acc.current.push(item);
      }
      return acc;
    }, { filtered: [], current: [] });
    items[listIndex] = transferData.filtered;
    items[moveToIndex] = [...transferData.current, ...value[moveToIndex]];
    onChange(items);
    handlers.deselectAll(listIndex);
  };
  const breakpoints = breakpoint ? [{ maxWidth: breakpoint, cols: 1 }] : [];
  const sharedListProps = {
    itemComponent,
    listComponent,
    transferIcon,
    transferAllIcon,
    filter,
    height: listHeight,
    showTransferAll,
    classNames,
    styles: styles2,
    limit,
    radius: radius2
  };
  return import_react287.default.createElement(SimpleGrid, __spreadValues228({
    cols: 2,
    spacing: "xl",
    breakpoints,
    ref,
    unstyled
  }, others), import_react287.default.createElement(RenderList, __spreadProps107(__spreadValues228({}, sharedListProps), {
    data: value[0],
    selection: selection[0],
    onSelect: (val) => handlers.select(0, val),
    onMoveAll: () => handleMoveAll(0),
    onMove: () => handleMove(0),
    title: titles[0],
    placeholder: Array.isArray(placeholder) ? placeholder[0] : placeholder,
    searchPlaceholder: Array.isArray(searchPlaceholder) ? searchPlaceholder[0] : searchPlaceholder,
    nothingFound: Array.isArray(nothingFound) ? nothingFound[0] : nothingFound,
    query: search[0],
    onSearch: (query) => handleSearch([query, search[1]]),
    unstyled,
    transferAllMatchingFilter
  })), import_react287.default.createElement(RenderList, __spreadProps107(__spreadValues228({}, sharedListProps), {
    data: value[1],
    selection: selection[1],
    onSelect: (val) => handlers.select(1, val),
    onMoveAll: () => handleMoveAll(1),
    onMove: () => handleMove(1),
    title: titles[1],
    placeholder: Array.isArray(placeholder) ? placeholder[1] : placeholder,
    searchPlaceholder: Array.isArray(searchPlaceholder) ? searchPlaceholder[1] : searchPlaceholder,
    nothingFound: Array.isArray(nothingFound) ? nothingFound[1] : nothingFound,
    query: search[1],
    onSearch: (query) => handleSearch([search[0], query]),
    reversed: true,
    unstyled,
    transferAllMatchingFilter
  })));
});
TransferList.displayName = "@mantine/core/TransferList";

// node_modules/@mantine/core/esm/TypographyStylesProvider/TypographyStylesProvider.js
var import_react288 = __toESM(require_react());

// node_modules/@mantine/core/esm/TypographyStylesProvider/TypographyStylesProvider.styles.js
var __defProp229 = Object.defineProperty;
var __defProps108 = Object.defineProperties;
var __getOwnPropDescs108 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols231 = Object.getOwnPropertySymbols;
var __hasOwnProp231 = Object.prototype.hasOwnProperty;
var __propIsEnum231 = Object.prototype.propertyIsEnumerable;
var __defNormalProp229 = (obj, key, value) => key in obj ? __defProp229(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues229 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp231.call(b2, prop))
      __defNormalProp229(a2, prop, b2[prop]);
  if (__getOwnPropSymbols231)
    for (var prop of __getOwnPropSymbols231(b2)) {
      if (__propIsEnum231.call(b2, prop))
        __defNormalProp229(a2, prop, b2[prop]);
    }
  return a2;
};
var __spreadProps108 = (a2, b2) => __defProps108(a2, __getOwnPropDescs108(b2));
var useStyles106 = createStyles((theme) => {
  const headings = keys(theme.headings.sizes).reduce((acc, h) => {
    const values = theme.headings.sizes[h];
    acc[`& ${h}`] = __spreadProps108(__spreadValues229({
      fontFamily: theme.headings.fontFamily,
      fontWeight: values.fontWeight || theme.headings.fontWeight,
      marginTop: typeof values.lineHeight === "number" ? `calc(${theme.spacing.xl}px * ${values.lineHeight})` : theme.spacing.xl,
      marginBottom: theme.spacing.sm
    }, values), {
      "@media (max-width: 755px)": {
        fontSize: typeof values.fontSize === "number" && values.fontSize / 1.3
      }
    });
    return acc;
  }, {});
  return {
    root: __spreadProps108(__spreadValues229(__spreadProps108(__spreadValues229({}, theme.fn.fontStyles()), {
      color: theme.colorScheme === "dark" ? theme.colors.dark[0] : theme.black,
      lineHeight: theme.lineHeight,
      fontSize: theme.fontSizes.md,
      "@media (max-width: 755px)": {
        fontSize: theme.fontSizes.sm
      }
    }), headings), {
      "& .ql-align-center": {
        textAlign: "center"
      },
      "& .ql-align-right": {
        textAlign: "right"
      },
      "& .ql-align-left": {
        textAlign: "left"
      },
      "& img": {
        maxWidth: "100%",
        marginBottom: theme.spacing.xs
      },
      "& p": {
        marginTop: 0,
        marginBottom: theme.spacing.lg
      },
      "& mark": {
        backgroundColor: theme.fn.themeColor("yellow", theme.colorScheme === "dark" ? 5 : 2),
        color: theme.colorScheme === "dark" ? theme.colors.dark[9] : "inherit"
      },
      "& hr": {
        marginTop: theme.spacing.md,
        marginBottom: theme.spacing.sm,
        borderBottom: 0,
        borderLeft: 0,
        borderRight: 0,
        borderTop: `1px dashed ${theme.colors.gray[theme.colorScheme === "dark" ? 4 : 6]}`
      },
      "& a": __spreadProps108(__spreadValues229({}, theme.fn.focusStyles()), {
        color: theme.colors[theme.primaryColor][theme.colorScheme === "dark" ? 4 : 6],
        textDecoration: "none",
        "&:hover": {
          textDecoration: "underline"
        }
      }),
      "& pre": {
        padding: theme.spacing.xs,
        lineHeight: theme.lineHeight,
        margin: 0,
        marginTop: theme.spacing.md,
        marginBottom: theme.spacing.md,
        overflowX: "auto",
        fontFamily: theme.fontFamilyMonospace,
        fontSize: theme.fontSizes.sm,
        borderRadius: theme.radius.sm,
        backgroundColor: theme.colorScheme === "dark" ? theme.colors.dark[8] : theme.colors.gray[0],
        "& code": {
          backgroundColor: "transparent",
          padding: 0,
          borderRadius: 0,
          color: "inherit",
          border: 0
        }
      },
      "& code": {
        lineHeight: theme.lineHeight,
        padding: "1px 5px",
        borderRadius: theme.radius.sm,
        color: theme.colorScheme === "dark" ? theme.colors.dark[0] : theme.black,
        backgroundColor: theme.colorScheme === "dark" ? theme.colors.dark[9] : theme.colors.gray[0],
        fontFamily: theme.fontFamilyMonospace,
        fontSize: theme.fontSizes.xs,
        border: `1px solid ${theme.colorScheme === "dark" ? theme.colors.dark[9] : theme.colors.gray[3]}`
      },
      "& ul, & ol": {
        marginBottom: theme.spacing.md,
        paddingLeft: 38,
        "& li": {
          marginTop: theme.spacing.xs
        }
      },
      "& table": {
        width: "100%",
        borderCollapse: "collapse",
        captionSide: "bottom",
        marginBottom: theme.spacing.md,
        "& caption": {
          marginTop: theme.spacing.xs,
          fontSize: theme.fontSizes.sm,
          color: theme.colorScheme === "dark" ? theme.colors.dark[2] : theme.colors.gray[6]
        },
        "& th": {
          textAlign: "left",
          fontWeight: "bold",
          color: theme.colorScheme === "dark" ? theme.colors.dark[0] : theme.colors.gray[7],
          fontSize: 14,
          padding: "7px 10px"
        },
        "& thead th": {
          borderBottom: `1px solid ${theme.colorScheme === "dark" ? theme.colors.dark[4] : theme.colors.gray[3]}`
        },
        "& tfoot th": {
          borderTop: `1px solid ${theme.colorScheme === "dark" ? theme.colors.dark[4] : theme.colors.gray[3]}`
        },
        "& td": {
          padding: "7px 10px",
          borderBottom: `1px solid ${theme.colorScheme === "dark" ? theme.colors.dark[4] : theme.colors.gray[3]}`,
          fontSize: 14
        },
        "& tr:last-of-type td": {
          borderBottom: "none"
        }
      },
      "& blockquote": {
        fontSize: theme.fontSizes.lg,
        lineHeight: theme.lineHeight,
        margin: `${theme.spacing.md}px 0`,
        borderTopRightRadius: theme.radius.sm,
        borderBottomRightRadius: theme.radius.sm,
        padding: `${theme.spacing.md}px ${theme.spacing.lg}px`,
        color: theme.colorScheme === "dark" ? theme.colors.dark[0] : theme.black,
        borderLeft: `6px solid ${theme.colorScheme === "dark" ? theme.colors.dark[4] : theme.colors.gray[3]}`,
        "& cite": {
          display: "block",
          fontSize: theme.fontSizes.sm,
          marginTop: theme.spacing.xs,
          color: theme.colorScheme === "dark" ? theme.colors.dark[2] : theme.colors.gray[6],
          overflow: "hidden",
          textOverflow: "ellipsis"
        }
      }
    })
  };
});
var TypographyStylesProvider_styles_default = useStyles106;

// node_modules/@mantine/core/esm/TypographyStylesProvider/TypographyStylesProvider.js
var __defProp230 = Object.defineProperty;
var __getOwnPropSymbols232 = Object.getOwnPropertySymbols;
var __hasOwnProp232 = Object.prototype.hasOwnProperty;
var __propIsEnum232 = Object.prototype.propertyIsEnumerable;
var __defNormalProp230 = (obj, key, value) => key in obj ? __defProp230(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues230 = (a2, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp232.call(b2, prop))
      __defNormalProp230(a2, prop, b2[prop]);
  if (__getOwnPropSymbols232)
    for (var prop of __getOwnPropSymbols232(b2)) {
      if (__propIsEnum232.call(b2, prop))
        __defNormalProp230(a2, prop, b2[prop]);
    }
  return a2;
};
var __objRest150 = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp232.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols232)
    for (var prop of __getOwnPropSymbols232(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum232.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var TypographyStylesProvider = (0, import_react288.forwardRef)((props, ref) => {
  const _a = useComponentDefaultProps("TypographyStylesProvider", {}, props), { className, unstyled } = _a, others = __objRest150(_a, ["className", "unstyled"]);
  const { classes, cx } = TypographyStylesProvider_styles_default(null, { name: "TypographyStylesProvider", unstyled });
  return import_react288.default.createElement(Box, __spreadValues230({
    className: cx(classes.root, className),
    ref
  }, others));
});
TypographyStylesProvider.displayName = "@mantine/core/TypographyStylesProvider";

// node_modules/@popperjs/core/lib/enums.js
var top = "top";
var bottom = "bottom";
var right = "right";
var left = "left";
var auto = "auto";
var basePlacements = [top, bottom, right, left];
var start = "start";
var end = "end";
var clippingParents = "clippingParents";
var viewport = "viewport";
var popper = "popper";
var reference = "reference";
var variationPlacements = basePlacements.reduce(function(acc, placement) {
  return acc.concat([placement + "-" + start, placement + "-" + end]);
}, []);
var placements2 = [].concat(basePlacements, [auto]).reduce(function(acc, placement) {
  return acc.concat([placement, placement + "-" + start, placement + "-" + end]);
}, []);
var beforeRead = "beforeRead";
var read = "read";
var afterRead = "afterRead";
var beforeMain = "beforeMain";
var main = "main";
var afterMain = "afterMain";
var beforeWrite = "beforeWrite";
var write = "write";
var afterWrite = "afterWrite";
var modifierPhases = [beforeRead, read, afterRead, beforeMain, main, afterMain, beforeWrite, write, afterWrite];

// node_modules/@popperjs/core/lib/dom-utils/getNodeName.js
function getNodeName2(element) {
  return element ? (element.nodeName || "").toLowerCase() : null;
}

// node_modules/@popperjs/core/lib/dom-utils/getWindow.js
function getWindow3(node2) {
  if (node2 == null) {
    return window;
  }
  if (node2.toString() !== "[object Window]") {
    var ownerDocument = node2.ownerDocument;
    return ownerDocument ? ownerDocument.defaultView || window : window;
  }
  return node2;
}

// node_modules/@popperjs/core/lib/dom-utils/instanceOf.js
function isElement4(node2) {
  var OwnElement = getWindow3(node2).Element;
  return node2 instanceof OwnElement || node2 instanceof Element;
}
function isHTMLElement3(node2) {
  var OwnElement = getWindow3(node2).HTMLElement;
  return node2 instanceof OwnElement || node2 instanceof HTMLElement;
}
function isShadowRoot3(node2) {
  if (typeof ShadowRoot === "undefined") {
    return false;
  }
  var OwnElement = getWindow3(node2).ShadowRoot;
  return node2 instanceof OwnElement || node2 instanceof ShadowRoot;
}

// node_modules/@popperjs/core/lib/modifiers/applyStyles.js
function applyStyles(_ref) {
  var state = _ref.state;
  Object.keys(state.elements).forEach(function(name) {
    var style = state.styles[name] || {};
    var attributes = state.attributes[name] || {};
    var element = state.elements[name];
    if (!isHTMLElement3(element) || !getNodeName2(element)) {
      return;
    }
    Object.assign(element.style, style);
    Object.keys(attributes).forEach(function(name2) {
      var value = attributes[name2];
      if (value === false) {
        element.removeAttribute(name2);
      } else {
        element.setAttribute(name2, value === true ? "" : value);
      }
    });
  });
}
function effect(_ref2) {
  var state = _ref2.state;
  var initialStyles = {
    popper: {
      position: state.options.strategy,
      left: "0",
      top: "0",
      margin: "0"
    },
    arrow: {
      position: "absolute"
    },
    reference: {}
  };
  Object.assign(state.elements.popper.style, initialStyles.popper);
  state.styles = initialStyles;
  if (state.elements.arrow) {
    Object.assign(state.elements.arrow.style, initialStyles.arrow);
  }
  return function() {
    Object.keys(state.elements).forEach(function(name) {
      var element = state.elements[name];
      var attributes = state.attributes[name] || {};
      var styleProperties = Object.keys(state.styles.hasOwnProperty(name) ? state.styles[name] : initialStyles[name]);
      var style = styleProperties.reduce(function(style2, property) {
        style2[property] = "";
        return style2;
      }, {});
      if (!isHTMLElement3(element) || !getNodeName2(element)) {
        return;
      }
      Object.assign(element.style, style);
      Object.keys(attributes).forEach(function(attribute) {
        element.removeAttribute(attribute);
      });
    });
  };
}
var applyStyles_default = {
  name: "applyStyles",
  enabled: true,
  phase: "write",
  fn: applyStyles,
  effect,
  requires: ["computeStyles"]
};

// node_modules/@popperjs/core/lib/utils/getBasePlacement.js
function getBasePlacement(placement) {
  return placement.split("-")[0];
}

// node_modules/@popperjs/core/lib/utils/math.js
var max2 = Math.max;
var min2 = Math.min;
var round3 = Math.round;

// node_modules/@popperjs/core/lib/utils/userAgent.js
function getUAString() {
  var uaData = navigator.userAgentData;
  if (uaData != null && uaData.brands && Array.isArray(uaData.brands)) {
    return uaData.brands.map(function(item) {
      return item.brand + "/" + item.version;
    }).join(" ");
  }
  return navigator.userAgent;
}

// node_modules/@popperjs/core/lib/dom-utils/isLayoutViewport.js
function isLayoutViewport() {
  return !/^((?!chrome|android).)*safari/i.test(getUAString());
}

// node_modules/@popperjs/core/lib/dom-utils/getBoundingClientRect.js
function getBoundingClientRect2(element, includeScale, isFixedStrategy) {
  if (includeScale === void 0) {
    includeScale = false;
  }
  if (isFixedStrategy === void 0) {
    isFixedStrategy = false;
  }
  var clientRect = element.getBoundingClientRect();
  var scaleX = 1;
  var scaleY = 1;
  if (includeScale && isHTMLElement3(element)) {
    scaleX = element.offsetWidth > 0 ? round3(clientRect.width) / element.offsetWidth || 1 : 1;
    scaleY = element.offsetHeight > 0 ? round3(clientRect.height) / element.offsetHeight || 1 : 1;
  }
  var _ref = isElement4(element) ? getWindow3(element) : window, visualViewport = _ref.visualViewport;
  var addVisualOffsets = !isLayoutViewport() && isFixedStrategy;
  var x = (clientRect.left + (addVisualOffsets && visualViewport ? visualViewport.offsetLeft : 0)) / scaleX;
  var y = (clientRect.top + (addVisualOffsets && visualViewport ? visualViewport.offsetTop : 0)) / scaleY;
  var width = clientRect.width / scaleX;
  var height = clientRect.height / scaleY;
  return {
    width,
    height,
    top: y,
    right: x + width,
    bottom: y + height,
    left: x,
    x,
    y
  };
}

// node_modules/@popperjs/core/lib/dom-utils/getLayoutRect.js
function getLayoutRect(element) {
  var clientRect = getBoundingClientRect2(element);
  var width = element.offsetWidth;
  var height = element.offsetHeight;
  if (Math.abs(clientRect.width - width) <= 1) {
    width = clientRect.width;
  }
  if (Math.abs(clientRect.height - height) <= 1) {
    height = clientRect.height;
  }
  return {
    x: element.offsetLeft,
    y: element.offsetTop,
    width,
    height
  };
}

// node_modules/@popperjs/core/lib/dom-utils/contains.js
function contains2(parent, child) {
  var rootNode = child.getRootNode && child.getRootNode();
  if (parent.contains(child)) {
    return true;
  } else if (rootNode && isShadowRoot3(rootNode)) {
    var next2 = child;
    do {
      if (next2 && parent.isSameNode(next2)) {
        return true;
      }
      next2 = next2.parentNode || next2.host;
    } while (next2);
  }
  return false;
}

// node_modules/@popperjs/core/lib/dom-utils/getComputedStyle.js
function getComputedStyle3(element) {
  return getWindow3(element).getComputedStyle(element);
}

// node_modules/@popperjs/core/lib/dom-utils/isTableElement.js
function isTableElement2(element) {
  return ["table", "td", "th"].indexOf(getNodeName2(element)) >= 0;
}

// node_modules/@popperjs/core/lib/dom-utils/getDocumentElement.js
function getDocumentElement2(element) {
  return ((isElement4(element) ? element.ownerDocument : (
    // $FlowFixMe[prop-missing]
    element.document
  )) || window.document).documentElement;
}

// node_modules/@popperjs/core/lib/dom-utils/getParentNode.js
function getParentNode2(element) {
  if (getNodeName2(element) === "html") {
    return element;
  }
  return (
    // this is a quicker (but less type safe) way to save quite some bytes from the bundle
    // $FlowFixMe[incompatible-return]
    // $FlowFixMe[prop-missing]
    element.assignedSlot || // step into the shadow DOM of the parent of a slotted node
    element.parentNode || // DOM Element detected
    (isShadowRoot3(element) ? element.host : null) || // ShadowRoot detected
    // $FlowFixMe[incompatible-call]: HTMLElement is a Node
    getDocumentElement2(element)
  );
}

// node_modules/@popperjs/core/lib/dom-utils/getOffsetParent.js
function getTrueOffsetParent2(element) {
  if (!isHTMLElement3(element) || // https://github.com/popperjs/popper-core/issues/837
  getComputedStyle3(element).position === "fixed") {
    return null;
  }
  return element.offsetParent;
}
function getContainingBlock2(element) {
  var isFirefox = /firefox/i.test(getUAString());
  var isIE2 = /Trident/i.test(getUAString());
  if (isIE2 && isHTMLElement3(element)) {
    var elementCss = getComputedStyle3(element);
    if (elementCss.position === "fixed") {
      return null;
    }
  }
  var currentNode = getParentNode2(element);
  if (isShadowRoot3(currentNode)) {
    currentNode = currentNode.host;
  }
  while (isHTMLElement3(currentNode) && ["html", "body"].indexOf(getNodeName2(currentNode)) < 0) {
    var css2 = getComputedStyle3(currentNode);
    if (css2.transform !== "none" || css2.perspective !== "none" || css2.contain === "paint" || ["transform", "perspective"].indexOf(css2.willChange) !== -1 || isFirefox && css2.willChange === "filter" || isFirefox && css2.filter && css2.filter !== "none") {
      return currentNode;
    } else {
      currentNode = currentNode.parentNode;
    }
  }
  return null;
}
function getOffsetParent2(element) {
  var window2 = getWindow3(element);
  var offsetParent = getTrueOffsetParent2(element);
  while (offsetParent && isTableElement2(offsetParent) && getComputedStyle3(offsetParent).position === "static") {
    offsetParent = getTrueOffsetParent2(offsetParent);
  }
  if (offsetParent && (getNodeName2(offsetParent) === "html" || getNodeName2(offsetParent) === "body" && getComputedStyle3(offsetParent).position === "static")) {
    return window2;
  }
  return offsetParent || getContainingBlock2(element) || window2;
}

// node_modules/@popperjs/core/lib/utils/getMainAxisFromPlacement.js
function getMainAxisFromPlacement(placement) {
  return ["top", "bottom"].indexOf(placement) >= 0 ? "x" : "y";
}

// node_modules/@popperjs/core/lib/utils/within.js
function within(min3, value, max3) {
  return max2(min3, min2(value, max3));
}
function withinMaxClamp(min3, value, max3) {
  var v = within(min3, value, max3);
  return v > max3 ? max3 : v;
}

// node_modules/@popperjs/core/lib/utils/getFreshSideObject.js
function getFreshSideObject() {
  return {
    top: 0,
    right: 0,
    bottom: 0,
    left: 0
  };
}

// node_modules/@popperjs/core/lib/utils/mergePaddingObject.js
function mergePaddingObject(paddingObject) {
  return Object.assign({}, getFreshSideObject(), paddingObject);
}

// node_modules/@popperjs/core/lib/utils/expandToHashMap.js
function expandToHashMap(value, keys2) {
  return keys2.reduce(function(hashMap, key) {
    hashMap[key] = value;
    return hashMap;
  }, {});
}

// node_modules/@popperjs/core/lib/modifiers/arrow.js
var toPaddingObject = function toPaddingObject2(padding2, state) {
  padding2 = typeof padding2 === "function" ? padding2(Object.assign({}, state.rects, {
    placement: state.placement
  })) : padding2;
  return mergePaddingObject(typeof padding2 !== "number" ? padding2 : expandToHashMap(padding2, basePlacements));
};
function arrow3(_ref) {
  var _state$modifiersData$;
  var state = _ref.state, name = _ref.name, options = _ref.options;
  var arrowElement = state.elements.arrow;
  var popperOffsets2 = state.modifiersData.popperOffsets;
  var basePlacement = getBasePlacement(state.placement);
  var axis = getMainAxisFromPlacement(basePlacement);
  var isVertical = [left, right].indexOf(basePlacement) >= 0;
  var len = isVertical ? "height" : "width";
  if (!arrowElement || !popperOffsets2) {
    return;
  }
  var paddingObject = toPaddingObject(options.padding, state);
  var arrowRect = getLayoutRect(arrowElement);
  var minProp = axis === "y" ? top : left;
  var maxProp = axis === "y" ? bottom : right;
  var endDiff = state.rects.reference[len] + state.rects.reference[axis] - popperOffsets2[axis] - state.rects.popper[len];
  var startDiff = popperOffsets2[axis] - state.rects.reference[axis];
  var arrowOffsetParent = getOffsetParent2(arrowElement);
  var clientSize = arrowOffsetParent ? axis === "y" ? arrowOffsetParent.clientHeight || 0 : arrowOffsetParent.clientWidth || 0 : 0;
  var centerToReference = endDiff / 2 - startDiff / 2;
  var min3 = paddingObject[minProp];
  var max3 = clientSize - arrowRect[len] - paddingObject[maxProp];
  var center = clientSize / 2 - arrowRect[len] / 2 + centerToReference;
  var offset3 = within(min3, center, max3);
  var axisProp = axis;
  state.modifiersData[name] = (_state$modifiersData$ = {}, _state$modifiersData$[axisProp] = offset3, _state$modifiersData$.centerOffset = offset3 - center, _state$modifiersData$);
}
function effect2(_ref2) {
  var state = _ref2.state, options = _ref2.options;
  var _options$element = options.element, arrowElement = _options$element === void 0 ? "[data-popper-arrow]" : _options$element;
  if (arrowElement == null) {
    return;
  }
  if (typeof arrowElement === "string") {
    arrowElement = state.elements.popper.querySelector(arrowElement);
    if (!arrowElement) {
      return;
    }
  }
  if (!contains2(state.elements.popper, arrowElement)) {
    return;
  }
  state.elements.arrow = arrowElement;
}
var arrow_default = {
  name: "arrow",
  enabled: true,
  phase: "main",
  fn: arrow3,
  effect: effect2,
  requires: ["popperOffsets"],
  requiresIfExists: ["preventOverflow"]
};

// node_modules/@popperjs/core/lib/utils/getVariation.js
function getVariation(placement) {
  return placement.split("-")[1];
}

// node_modules/@popperjs/core/lib/modifiers/computeStyles.js
var unsetSides = {
  top: "auto",
  right: "auto",
  bottom: "auto",
  left: "auto"
};
function roundOffsetsByDPR(_ref, win) {
  var x = _ref.x, y = _ref.y;
  var dpr = win.devicePixelRatio || 1;
  return {
    x: round3(x * dpr) / dpr || 0,
    y: round3(y * dpr) / dpr || 0
  };
}
function mapToStyles(_ref2) {
  var _Object$assign2;
  var popper2 = _ref2.popper, popperRect = _ref2.popperRect, placement = _ref2.placement, variation = _ref2.variation, offsets = _ref2.offsets, position2 = _ref2.position, gpuAcceleration = _ref2.gpuAcceleration, adaptive = _ref2.adaptive, roundOffsets = _ref2.roundOffsets, isFixed = _ref2.isFixed;
  var _offsets$x = offsets.x, x = _offsets$x === void 0 ? 0 : _offsets$x, _offsets$y = offsets.y, y = _offsets$y === void 0 ? 0 : _offsets$y;
  var _ref3 = typeof roundOffsets === "function" ? roundOffsets({
    x,
    y
  }) : {
    x,
    y
  };
  x = _ref3.x;
  y = _ref3.y;
  var hasX = offsets.hasOwnProperty("x");
  var hasY = offsets.hasOwnProperty("y");
  var sideX = left;
  var sideY = top;
  var win = window;
  if (adaptive) {
    var offsetParent = getOffsetParent2(popper2);
    var heightProp = "clientHeight";
    var widthProp = "clientWidth";
    if (offsetParent === getWindow3(popper2)) {
      offsetParent = getDocumentElement2(popper2);
      if (getComputedStyle3(offsetParent).position !== "static" && position2 === "absolute") {
        heightProp = "scrollHeight";
        widthProp = "scrollWidth";
      }
    }
    offsetParent = offsetParent;
    if (placement === top || (placement === left || placement === right) && variation === end) {
      sideY = bottom;
      var offsetY = isFixed && offsetParent === win && win.visualViewport ? win.visualViewport.height : (
        // $FlowFixMe[prop-missing]
        offsetParent[heightProp]
      );
      y -= offsetY - popperRect.height;
      y *= gpuAcceleration ? 1 : -1;
    }
    if (placement === left || (placement === top || placement === bottom) && variation === end) {
      sideX = right;
      var offsetX = isFixed && offsetParent === win && win.visualViewport ? win.visualViewport.width : (
        // $FlowFixMe[prop-missing]
        offsetParent[widthProp]
      );
      x -= offsetX - popperRect.width;
      x *= gpuAcceleration ? 1 : -1;
    }
  }
  var commonStyles = Object.assign({
    position: position2
  }, adaptive && unsetSides);
  var _ref4 = roundOffsets === true ? roundOffsetsByDPR({
    x,
    y
  }, getWindow3(popper2)) : {
    x,
    y
  };
  x = _ref4.x;
  y = _ref4.y;
  if (gpuAcceleration) {
    var _Object$assign;
    return Object.assign({}, commonStyles, (_Object$assign = {}, _Object$assign[sideY] = hasY ? "0" : "", _Object$assign[sideX] = hasX ? "0" : "", _Object$assign.transform = (win.devicePixelRatio || 1) <= 1 ? "translate(" + x + "px, " + y + "px)" : "translate3d(" + x + "px, " + y + "px, 0)", _Object$assign));
  }
  return Object.assign({}, commonStyles, (_Object$assign2 = {}, _Object$assign2[sideY] = hasY ? y + "px" : "", _Object$assign2[sideX] = hasX ? x + "px" : "", _Object$assign2.transform = "", _Object$assign2));
}
function computeStyles(_ref5) {
  var state = _ref5.state, options = _ref5.options;
  var _options$gpuAccelerat = options.gpuAcceleration, gpuAcceleration = _options$gpuAccelerat === void 0 ? true : _options$gpuAccelerat, _options$adaptive = options.adaptive, adaptive = _options$adaptive === void 0 ? true : _options$adaptive, _options$roundOffsets = options.roundOffsets, roundOffsets = _options$roundOffsets === void 0 ? true : _options$roundOffsets;
  var commonStyles = {
    placement: getBasePlacement(state.placement),
    variation: getVariation(state.placement),
    popper: state.elements.popper,
    popperRect: state.rects.popper,
    gpuAcceleration,
    isFixed: state.options.strategy === "fixed"
  };
  if (state.modifiersData.popperOffsets != null) {
    state.styles.popper = Object.assign({}, state.styles.popper, mapToStyles(Object.assign({}, commonStyles, {
      offsets: state.modifiersData.popperOffsets,
      position: state.options.strategy,
      adaptive,
      roundOffsets
    })));
  }
  if (state.modifiersData.arrow != null) {
    state.styles.arrow = Object.assign({}, state.styles.arrow, mapToStyles(Object.assign({}, commonStyles, {
      offsets: state.modifiersData.arrow,
      position: "absolute",
      adaptive: false,
      roundOffsets
    })));
  }
  state.attributes.popper = Object.assign({}, state.attributes.popper, {
    "data-popper-placement": state.placement
  });
}
var computeStyles_default = {
  name: "computeStyles",
  enabled: true,
  phase: "beforeWrite",
  fn: computeStyles,
  data: {}
};

// node_modules/@popperjs/core/lib/modifiers/eventListeners.js
var passive = {
  passive: true
};
function effect3(_ref) {
  var state = _ref.state, instance = _ref.instance, options = _ref.options;
  var _options$scroll = options.scroll, scroll = _options$scroll === void 0 ? true : _options$scroll, _options$resize = options.resize, resize = _options$resize === void 0 ? true : _options$resize;
  var window2 = getWindow3(state.elements.popper);
  var scrollParents = [].concat(state.scrollParents.reference, state.scrollParents.popper);
  if (scroll) {
    scrollParents.forEach(function(scrollParent) {
      scrollParent.addEventListener("scroll", instance.update, passive);
    });
  }
  if (resize) {
    window2.addEventListener("resize", instance.update, passive);
  }
  return function() {
    if (scroll) {
      scrollParents.forEach(function(scrollParent) {
        scrollParent.removeEventListener("scroll", instance.update, passive);
      });
    }
    if (resize) {
      window2.removeEventListener("resize", instance.update, passive);
    }
  };
}
var eventListeners_default = {
  name: "eventListeners",
  enabled: true,
  phase: "write",
  fn: function fn() {
  },
  effect: effect3,
  data: {}
};

// node_modules/@popperjs/core/lib/utils/getOppositePlacement.js
var hash2 = {
  left: "right",
  right: "left",
  bottom: "top",
  top: "bottom"
};
function getOppositePlacement2(placement) {
  return placement.replace(/left|right|bottom|top/g, function(matched) {
    return hash2[matched];
  });
}

// node_modules/@popperjs/core/lib/utils/getOppositeVariationPlacement.js
var hash3 = {
  start: "end",
  end: "start"
};
function getOppositeVariationPlacement(placement) {
  return placement.replace(/start|end/g, function(matched) {
    return hash3[matched];
  });
}

// node_modules/@popperjs/core/lib/dom-utils/getWindowScroll.js
function getWindowScroll(node2) {
  var win = getWindow3(node2);
  var scrollLeft = win.pageXOffset;
  var scrollTop = win.pageYOffset;
  return {
    scrollLeft,
    scrollTop
  };
}

// node_modules/@popperjs/core/lib/dom-utils/getWindowScrollBarX.js
function getWindowScrollBarX2(element) {
  return getBoundingClientRect2(getDocumentElement2(element)).left + getWindowScroll(element).scrollLeft;
}

// node_modules/@popperjs/core/lib/dom-utils/getViewportRect.js
function getViewportRect2(element, strategy) {
  var win = getWindow3(element);
  var html = getDocumentElement2(element);
  var visualViewport = win.visualViewport;
  var width = html.clientWidth;
  var height = html.clientHeight;
  var x = 0;
  var y = 0;
  if (visualViewport) {
    width = visualViewport.width;
    height = visualViewport.height;
    var layoutViewport = isLayoutViewport();
    if (layoutViewport || !layoutViewport && strategy === "fixed") {
      x = visualViewport.offsetLeft;
      y = visualViewport.offsetTop;
    }
  }
  return {
    width,
    height,
    x: x + getWindowScrollBarX2(element),
    y
  };
}

// node_modules/@popperjs/core/lib/dom-utils/getDocumentRect.js
function getDocumentRect2(element) {
  var _element$ownerDocumen;
  var html = getDocumentElement2(element);
  var winScroll = getWindowScroll(element);
  var body = (_element$ownerDocumen = element.ownerDocument) == null ? void 0 : _element$ownerDocumen.body;
  var width = max2(html.scrollWidth, html.clientWidth, body ? body.scrollWidth : 0, body ? body.clientWidth : 0);
  var height = max2(html.scrollHeight, html.clientHeight, body ? body.scrollHeight : 0, body ? body.clientHeight : 0);
  var x = -winScroll.scrollLeft + getWindowScrollBarX2(element);
  var y = -winScroll.scrollTop;
  if (getComputedStyle3(body || html).direction === "rtl") {
    x += max2(html.clientWidth, body ? body.clientWidth : 0) - width;
  }
  return {
    width,
    height,
    x,
    y
  };
}

// node_modules/@popperjs/core/lib/dom-utils/isScrollParent.js
function isScrollParent(element) {
  var _getComputedStyle = getComputedStyle3(element), overflow = _getComputedStyle.overflow, overflowX = _getComputedStyle.overflowX, overflowY = _getComputedStyle.overflowY;
  return /auto|scroll|overlay|hidden/.test(overflow + overflowY + overflowX);
}

// node_modules/@popperjs/core/lib/dom-utils/getScrollParent.js
function getScrollParent(node2) {
  if (["html", "body", "#document"].indexOf(getNodeName2(node2)) >= 0) {
    return node2.ownerDocument.body;
  }
  if (isHTMLElement3(node2) && isScrollParent(node2)) {
    return node2;
  }
  return getScrollParent(getParentNode2(node2));
}

// node_modules/@popperjs/core/lib/dom-utils/listScrollParents.js
function listScrollParents(element, list) {
  var _element$ownerDocumen;
  if (list === void 0) {
    list = [];
  }
  var scrollParent = getScrollParent(element);
  var isBody = scrollParent === ((_element$ownerDocumen = element.ownerDocument) == null ? void 0 : _element$ownerDocumen.body);
  var win = getWindow3(scrollParent);
  var target = isBody ? [win].concat(win.visualViewport || [], isScrollParent(scrollParent) ? scrollParent : []) : scrollParent;
  var updatedList = list.concat(target);
  return isBody ? updatedList : (
    // $FlowFixMe[incompatible-call]: isBody tells us target will be an HTMLElement here
    updatedList.concat(listScrollParents(getParentNode2(target)))
  );
}

// node_modules/@popperjs/core/lib/utils/rectToClientRect.js
function rectToClientRect2(rect) {
  return Object.assign({}, rect, {
    left: rect.x,
    top: rect.y,
    right: rect.x + rect.width,
    bottom: rect.y + rect.height
  });
}

// node_modules/@popperjs/core/lib/dom-utils/getClippingRect.js
function getInnerBoundingClientRect2(element, strategy) {
  var rect = getBoundingClientRect2(element, false, strategy === "fixed");
  rect.top = rect.top + element.clientTop;
  rect.left = rect.left + element.clientLeft;
  rect.bottom = rect.top + element.clientHeight;
  rect.right = rect.left + element.clientWidth;
  rect.width = element.clientWidth;
  rect.height = element.clientHeight;
  rect.x = rect.left;
  rect.y = rect.top;
  return rect;
}
function getClientRectFromMixedType(element, clippingParent, strategy) {
  return clippingParent === viewport ? rectToClientRect2(getViewportRect2(element, strategy)) : isElement4(clippingParent) ? getInnerBoundingClientRect2(clippingParent, strategy) : rectToClientRect2(getDocumentRect2(getDocumentElement2(element)));
}
function getClippingParents(element) {
  var clippingParents2 = listScrollParents(getParentNode2(element));
  var canEscapeClipping = ["absolute", "fixed"].indexOf(getComputedStyle3(element).position) >= 0;
  var clipperElement = canEscapeClipping && isHTMLElement3(element) ? getOffsetParent2(element) : element;
  if (!isElement4(clipperElement)) {
    return [];
  }
  return clippingParents2.filter(function(clippingParent) {
    return isElement4(clippingParent) && contains2(clippingParent, clipperElement) && getNodeName2(clippingParent) !== "body";
  });
}
function getClippingRect2(element, boundary, rootBoundary, strategy) {
  var mainClippingParents = boundary === "clippingParents" ? getClippingParents(element) : [].concat(boundary);
  var clippingParents2 = [].concat(mainClippingParents, [rootBoundary]);
  var firstClippingParent = clippingParents2[0];
  var clippingRect = clippingParents2.reduce(function(accRect, clippingParent) {
    var rect = getClientRectFromMixedType(element, clippingParent, strategy);
    accRect.top = max2(rect.top, accRect.top);
    accRect.right = min2(rect.right, accRect.right);
    accRect.bottom = min2(rect.bottom, accRect.bottom);
    accRect.left = max2(rect.left, accRect.left);
    return accRect;
  }, getClientRectFromMixedType(element, firstClippingParent, strategy));
  clippingRect.width = clippingRect.right - clippingRect.left;
  clippingRect.height = clippingRect.bottom - clippingRect.top;
  clippingRect.x = clippingRect.left;
  clippingRect.y = clippingRect.top;
  return clippingRect;
}

// node_modules/@popperjs/core/lib/utils/computeOffsets.js
function computeOffsets(_ref) {
  var reference2 = _ref.reference, element = _ref.element, placement = _ref.placement;
  var basePlacement = placement ? getBasePlacement(placement) : null;
  var variation = placement ? getVariation(placement) : null;
  var commonX = reference2.x + reference2.width / 2 - element.width / 2;
  var commonY = reference2.y + reference2.height / 2 - element.height / 2;
  var offsets;
  switch (basePlacement) {
    case top:
      offsets = {
        x: commonX,
        y: reference2.y - element.height
      };
      break;
    case bottom:
      offsets = {
        x: commonX,
        y: reference2.y + reference2.height
      };
      break;
    case right:
      offsets = {
        x: reference2.x + reference2.width,
        y: commonY
      };
      break;
    case left:
      offsets = {
        x: reference2.x - element.width,
        y: commonY
      };
      break;
    default:
      offsets = {
        x: reference2.x,
        y: reference2.y
      };
  }
  var mainAxis = basePlacement ? getMainAxisFromPlacement(basePlacement) : null;
  if (mainAxis != null) {
    var len = mainAxis === "y" ? "height" : "width";
    switch (variation) {
      case start:
        offsets[mainAxis] = offsets[mainAxis] - (reference2[len] / 2 - element[len] / 2);
        break;
      case end:
        offsets[mainAxis] = offsets[mainAxis] + (reference2[len] / 2 - element[len] / 2);
        break;
      default:
    }
  }
  return offsets;
}

// node_modules/@popperjs/core/lib/utils/detectOverflow.js
function detectOverflow2(state, options) {
  if (options === void 0) {
    options = {};
  }
  var _options = options, _options$placement = _options.placement, placement = _options$placement === void 0 ? state.placement : _options$placement, _options$strategy = _options.strategy, strategy = _options$strategy === void 0 ? state.strategy : _options$strategy, _options$boundary = _options.boundary, boundary = _options$boundary === void 0 ? clippingParents : _options$boundary, _options$rootBoundary = _options.rootBoundary, rootBoundary = _options$rootBoundary === void 0 ? viewport : _options$rootBoundary, _options$elementConte = _options.elementContext, elementContext = _options$elementConte === void 0 ? popper : _options$elementConte, _options$altBoundary = _options.altBoundary, altBoundary = _options$altBoundary === void 0 ? false : _options$altBoundary, _options$padding = _options.padding, padding2 = _options$padding === void 0 ? 0 : _options$padding;
  var paddingObject = mergePaddingObject(typeof padding2 !== "number" ? padding2 : expandToHashMap(padding2, basePlacements));
  var altContext = elementContext === popper ? reference : popper;
  var popperRect = state.rects.popper;
  var element = state.elements[altBoundary ? altContext : elementContext];
  var clippingClientRect = getClippingRect2(isElement4(element) ? element : element.contextElement || getDocumentElement2(state.elements.popper), boundary, rootBoundary, strategy);
  var referenceClientRect = getBoundingClientRect2(state.elements.reference);
  var popperOffsets2 = computeOffsets({
    reference: referenceClientRect,
    element: popperRect,
    strategy: "absolute",
    placement
  });
  var popperClientRect = rectToClientRect2(Object.assign({}, popperRect, popperOffsets2));
  var elementClientRect = elementContext === popper ? popperClientRect : referenceClientRect;
  var overflowOffsets = {
    top: clippingClientRect.top - elementClientRect.top + paddingObject.top,
    bottom: elementClientRect.bottom - clippingClientRect.bottom + paddingObject.bottom,
    left: clippingClientRect.left - elementClientRect.left + paddingObject.left,
    right: elementClientRect.right - clippingClientRect.right + paddingObject.right
  };
  var offsetData = state.modifiersData.offset;
  if (elementContext === popper && offsetData) {
    var offset3 = offsetData[placement];
    Object.keys(overflowOffsets).forEach(function(key) {
      var multiply = [right, bottom].indexOf(key) >= 0 ? 1 : -1;
      var axis = [top, bottom].indexOf(key) >= 0 ? "y" : "x";
      overflowOffsets[key] += offset3[axis] * multiply;
    });
  }
  return overflowOffsets;
}

// node_modules/@popperjs/core/lib/utils/computeAutoPlacement.js
function computeAutoPlacement(state, options) {
  if (options === void 0) {
    options = {};
  }
  var _options = options, placement = _options.placement, boundary = _options.boundary, rootBoundary = _options.rootBoundary, padding2 = _options.padding, flipVariations = _options.flipVariations, _options$allowedAutoP = _options.allowedAutoPlacements, allowedAutoPlacements = _options$allowedAutoP === void 0 ? placements2 : _options$allowedAutoP;
  var variation = getVariation(placement);
  var placements3 = variation ? flipVariations ? variationPlacements : variationPlacements.filter(function(placement2) {
    return getVariation(placement2) === variation;
  }) : basePlacements;
  var allowedPlacements = placements3.filter(function(placement2) {
    return allowedAutoPlacements.indexOf(placement2) >= 0;
  });
  if (allowedPlacements.length === 0) {
    allowedPlacements = placements3;
  }
  var overflows = allowedPlacements.reduce(function(acc, placement2) {
    acc[placement2] = detectOverflow2(state, {
      placement: placement2,
      boundary,
      rootBoundary,
      padding: padding2
    })[getBasePlacement(placement2)];
    return acc;
  }, {});
  return Object.keys(overflows).sort(function(a2, b2) {
    return overflows[a2] - overflows[b2];
  });
}

// node_modules/@popperjs/core/lib/modifiers/flip.js
function getExpandedFallbackPlacements(placement) {
  if (getBasePlacement(placement) === auto) {
    return [];
  }
  var oppositePlacement = getOppositePlacement2(placement);
  return [getOppositeVariationPlacement(placement), oppositePlacement, getOppositeVariationPlacement(oppositePlacement)];
}
function flip2(_ref) {
  var state = _ref.state, options = _ref.options, name = _ref.name;
  if (state.modifiersData[name]._skip) {
    return;
  }
  var _options$mainAxis = options.mainAxis, checkMainAxis = _options$mainAxis === void 0 ? true : _options$mainAxis, _options$altAxis = options.altAxis, checkAltAxis = _options$altAxis === void 0 ? true : _options$altAxis, specifiedFallbackPlacements = options.fallbackPlacements, padding2 = options.padding, boundary = options.boundary, rootBoundary = options.rootBoundary, altBoundary = options.altBoundary, _options$flipVariatio = options.flipVariations, flipVariations = _options$flipVariatio === void 0 ? true : _options$flipVariatio, allowedAutoPlacements = options.allowedAutoPlacements;
  var preferredPlacement = state.options.placement;
  var basePlacement = getBasePlacement(preferredPlacement);
  var isBasePlacement = basePlacement === preferredPlacement;
  var fallbackPlacements = specifiedFallbackPlacements || (isBasePlacement || !flipVariations ? [getOppositePlacement2(preferredPlacement)] : getExpandedFallbackPlacements(preferredPlacement));
  var placements3 = [preferredPlacement].concat(fallbackPlacements).reduce(function(acc, placement2) {
    return acc.concat(getBasePlacement(placement2) === auto ? computeAutoPlacement(state, {
      placement: placement2,
      boundary,
      rootBoundary,
      padding: padding2,
      flipVariations,
      allowedAutoPlacements
    }) : placement2);
  }, []);
  var referenceRect = state.rects.reference;
  var popperRect = state.rects.popper;
  var checksMap = /* @__PURE__ */ new Map();
  var makeFallbackChecks = true;
  var firstFittingPlacement = placements3[0];
  for (var i = 0; i < placements3.length; i++) {
    var placement = placements3[i];
    var _basePlacement = getBasePlacement(placement);
    var isStartVariation = getVariation(placement) === start;
    var isVertical = [top, bottom].indexOf(_basePlacement) >= 0;
    var len = isVertical ? "width" : "height";
    var overflow = detectOverflow2(state, {
      placement,
      boundary,
      rootBoundary,
      altBoundary,
      padding: padding2
    });
    var mainVariationSide = isVertical ? isStartVariation ? right : left : isStartVariation ? bottom : top;
    if (referenceRect[len] > popperRect[len]) {
      mainVariationSide = getOppositePlacement2(mainVariationSide);
    }
    var altVariationSide = getOppositePlacement2(mainVariationSide);
    var checks = [];
    if (checkMainAxis) {
      checks.push(overflow[_basePlacement] <= 0);
    }
    if (checkAltAxis) {
      checks.push(overflow[mainVariationSide] <= 0, overflow[altVariationSide] <= 0);
    }
    if (checks.every(function(check) {
      return check;
    })) {
      firstFittingPlacement = placement;
      makeFallbackChecks = false;
      break;
    }
    checksMap.set(placement, checks);
  }
  if (makeFallbackChecks) {
    var numberOfChecks = flipVariations ? 3 : 1;
    var _loop = function _loop2(_i2) {
      var fittingPlacement = placements3.find(function(placement2) {
        var checks2 = checksMap.get(placement2);
        if (checks2) {
          return checks2.slice(0, _i2).every(function(check) {
            return check;
          });
        }
      });
      if (fittingPlacement) {
        firstFittingPlacement = fittingPlacement;
        return "break";
      }
    };
    for (var _i = numberOfChecks; _i > 0; _i--) {
      var _ret = _loop(_i);
      if (_ret === "break")
        break;
    }
  }
  if (state.placement !== firstFittingPlacement) {
    state.modifiersData[name]._skip = true;
    state.placement = firstFittingPlacement;
    state.reset = true;
  }
}
var flip_default = {
  name: "flip",
  enabled: true,
  phase: "main",
  fn: flip2,
  requiresIfExists: ["offset"],
  data: {
    _skip: false
  }
};

// node_modules/@popperjs/core/lib/modifiers/hide.js
function getSideOffsets(overflow, rect, preventedOffsets) {
  if (preventedOffsets === void 0) {
    preventedOffsets = {
      x: 0,
      y: 0
    };
  }
  return {
    top: overflow.top - rect.height - preventedOffsets.y,
    right: overflow.right - rect.width + preventedOffsets.x,
    bottom: overflow.bottom - rect.height + preventedOffsets.y,
    left: overflow.left - rect.width - preventedOffsets.x
  };
}
function isAnySideFullyClipped(overflow) {
  return [top, right, bottom, left].some(function(side) {
    return overflow[side] >= 0;
  });
}
function hide2(_ref) {
  var state = _ref.state, name = _ref.name;
  var referenceRect = state.rects.reference;
  var popperRect = state.rects.popper;
  var preventedOffsets = state.modifiersData.preventOverflow;
  var referenceOverflow = detectOverflow2(state, {
    elementContext: "reference"
  });
  var popperAltOverflow = detectOverflow2(state, {
    altBoundary: true
  });
  var referenceClippingOffsets = getSideOffsets(referenceOverflow, referenceRect);
  var popperEscapeOffsets = getSideOffsets(popperAltOverflow, popperRect, preventedOffsets);
  var isReferenceHidden = isAnySideFullyClipped(referenceClippingOffsets);
  var hasPopperEscaped = isAnySideFullyClipped(popperEscapeOffsets);
  state.modifiersData[name] = {
    referenceClippingOffsets,
    popperEscapeOffsets,
    isReferenceHidden,
    hasPopperEscaped
  };
  state.attributes.popper = Object.assign({}, state.attributes.popper, {
    "data-popper-reference-hidden": isReferenceHidden,
    "data-popper-escaped": hasPopperEscaped
  });
}
var hide_default = {
  name: "hide",
  enabled: true,
  phase: "main",
  requiresIfExists: ["preventOverflow"],
  fn: hide2
};

// node_modules/@popperjs/core/lib/modifiers/offset.js
function distanceAndSkiddingToXY(placement, rects, offset3) {
  var basePlacement = getBasePlacement(placement);
  var invertDistance = [left, top].indexOf(basePlacement) >= 0 ? -1 : 1;
  var _ref = typeof offset3 === "function" ? offset3(Object.assign({}, rects, {
    placement
  })) : offset3, skidding = _ref[0], distance = _ref[1];
  skidding = skidding || 0;
  distance = (distance || 0) * invertDistance;
  return [left, right].indexOf(basePlacement) >= 0 ? {
    x: distance,
    y: skidding
  } : {
    x: skidding,
    y: distance
  };
}
function offset2(_ref2) {
  var state = _ref2.state, options = _ref2.options, name = _ref2.name;
  var _options$offset = options.offset, offset3 = _options$offset === void 0 ? [0, 0] : _options$offset;
  var data = placements2.reduce(function(acc, placement) {
    acc[placement] = distanceAndSkiddingToXY(placement, state.rects, offset3);
    return acc;
  }, {});
  var _data$state$placement = data[state.placement], x = _data$state$placement.x, y = _data$state$placement.y;
  if (state.modifiersData.popperOffsets != null) {
    state.modifiersData.popperOffsets.x += x;
    state.modifiersData.popperOffsets.y += y;
  }
  state.modifiersData[name] = data;
}
var offset_default = {
  name: "offset",
  enabled: true,
  phase: "main",
  requires: ["popperOffsets"],
  fn: offset2
};

// node_modules/@popperjs/core/lib/modifiers/popperOffsets.js
function popperOffsets(_ref) {
  var state = _ref.state, name = _ref.name;
  state.modifiersData[name] = computeOffsets({
    reference: state.rects.reference,
    element: state.rects.popper,
    strategy: "absolute",
    placement: state.placement
  });
}
var popperOffsets_default = {
  name: "popperOffsets",
  enabled: true,
  phase: "read",
  fn: popperOffsets,
  data: {}
};

// node_modules/@popperjs/core/lib/utils/getAltAxis.js
function getAltAxis(axis) {
  return axis === "x" ? "y" : "x";
}

// node_modules/@popperjs/core/lib/modifiers/preventOverflow.js
function preventOverflow(_ref) {
  var state = _ref.state, options = _ref.options, name = _ref.name;
  var _options$mainAxis = options.mainAxis, checkMainAxis = _options$mainAxis === void 0 ? true : _options$mainAxis, _options$altAxis = options.altAxis, checkAltAxis = _options$altAxis === void 0 ? false : _options$altAxis, boundary = options.boundary, rootBoundary = options.rootBoundary, altBoundary = options.altBoundary, padding2 = options.padding, _options$tether = options.tether, tether = _options$tether === void 0 ? true : _options$tether, _options$tetherOffset = options.tetherOffset, tetherOffset = _options$tetherOffset === void 0 ? 0 : _options$tetherOffset;
  var overflow = detectOverflow2(state, {
    boundary,
    rootBoundary,
    padding: padding2,
    altBoundary
  });
  var basePlacement = getBasePlacement(state.placement);
  var variation = getVariation(state.placement);
  var isBasePlacement = !variation;
  var mainAxis = getMainAxisFromPlacement(basePlacement);
  var altAxis = getAltAxis(mainAxis);
  var popperOffsets2 = state.modifiersData.popperOffsets;
  var referenceRect = state.rects.reference;
  var popperRect = state.rects.popper;
  var tetherOffsetValue = typeof tetherOffset === "function" ? tetherOffset(Object.assign({}, state.rects, {
    placement: state.placement
  })) : tetherOffset;
  var normalizedTetherOffsetValue = typeof tetherOffsetValue === "number" ? {
    mainAxis: tetherOffsetValue,
    altAxis: tetherOffsetValue
  } : Object.assign({
    mainAxis: 0,
    altAxis: 0
  }, tetherOffsetValue);
  var offsetModifierState = state.modifiersData.offset ? state.modifiersData.offset[state.placement] : null;
  var data = {
    x: 0,
    y: 0
  };
  if (!popperOffsets2) {
    return;
  }
  if (checkMainAxis) {
    var _offsetModifierState$;
    var mainSide = mainAxis === "y" ? top : left;
    var altSide = mainAxis === "y" ? bottom : right;
    var len = mainAxis === "y" ? "height" : "width";
    var offset3 = popperOffsets2[mainAxis];
    var min3 = offset3 + overflow[mainSide];
    var max3 = offset3 - overflow[altSide];
    var additive = tether ? -popperRect[len] / 2 : 0;
    var minLen = variation === start ? referenceRect[len] : popperRect[len];
    var maxLen = variation === start ? -popperRect[len] : -referenceRect[len];
    var arrowElement = state.elements.arrow;
    var arrowRect = tether && arrowElement ? getLayoutRect(arrowElement) : {
      width: 0,
      height: 0
    };
    var arrowPaddingObject = state.modifiersData["arrow#persistent"] ? state.modifiersData["arrow#persistent"].padding : getFreshSideObject();
    var arrowPaddingMin = arrowPaddingObject[mainSide];
    var arrowPaddingMax = arrowPaddingObject[altSide];
    var arrowLen = within(0, referenceRect[len], arrowRect[len]);
    var minOffset = isBasePlacement ? referenceRect[len] / 2 - additive - arrowLen - arrowPaddingMin - normalizedTetherOffsetValue.mainAxis : minLen - arrowLen - arrowPaddingMin - normalizedTetherOffsetValue.mainAxis;
    var maxOffset = isBasePlacement ? -referenceRect[len] / 2 + additive + arrowLen + arrowPaddingMax + normalizedTetherOffsetValue.mainAxis : maxLen + arrowLen + arrowPaddingMax + normalizedTetherOffsetValue.mainAxis;
    var arrowOffsetParent = state.elements.arrow && getOffsetParent2(state.elements.arrow);
    var clientOffset = arrowOffsetParent ? mainAxis === "y" ? arrowOffsetParent.clientTop || 0 : arrowOffsetParent.clientLeft || 0 : 0;
    var offsetModifierValue = (_offsetModifierState$ = offsetModifierState == null ? void 0 : offsetModifierState[mainAxis]) != null ? _offsetModifierState$ : 0;
    var tetherMin = offset3 + minOffset - offsetModifierValue - clientOffset;
    var tetherMax = offset3 + maxOffset - offsetModifierValue;
    var preventedOffset = within(tether ? min2(min3, tetherMin) : min3, offset3, tether ? max2(max3, tetherMax) : max3);
    popperOffsets2[mainAxis] = preventedOffset;
    data[mainAxis] = preventedOffset - offset3;
  }
  if (checkAltAxis) {
    var _offsetModifierState$2;
    var _mainSide = mainAxis === "x" ? top : left;
    var _altSide = mainAxis === "x" ? bottom : right;
    var _offset = popperOffsets2[altAxis];
    var _len = altAxis === "y" ? "height" : "width";
    var _min = _offset + overflow[_mainSide];
    var _max = _offset - overflow[_altSide];
    var isOriginSide = [top, left].indexOf(basePlacement) !== -1;
    var _offsetModifierValue = (_offsetModifierState$2 = offsetModifierState == null ? void 0 : offsetModifierState[altAxis]) != null ? _offsetModifierState$2 : 0;
    var _tetherMin = isOriginSide ? _min : _offset - referenceRect[_len] - popperRect[_len] - _offsetModifierValue + normalizedTetherOffsetValue.altAxis;
    var _tetherMax = isOriginSide ? _offset + referenceRect[_len] + popperRect[_len] - _offsetModifierValue - normalizedTetherOffsetValue.altAxis : _max;
    var _preventedOffset = tether && isOriginSide ? withinMaxClamp(_tetherMin, _offset, _tetherMax) : within(tether ? _tetherMin : _min, _offset, tether ? _tetherMax : _max);
    popperOffsets2[altAxis] = _preventedOffset;
    data[altAxis] = _preventedOffset - _offset;
  }
  state.modifiersData[name] = data;
}
var preventOverflow_default = {
  name: "preventOverflow",
  enabled: true,
  phase: "main",
  fn: preventOverflow,
  requiresIfExists: ["offset"]
};

// node_modules/@popperjs/core/lib/dom-utils/getHTMLElementScroll.js
function getHTMLElementScroll(element) {
  return {
    scrollLeft: element.scrollLeft,
    scrollTop: element.scrollTop
  };
}

// node_modules/@popperjs/core/lib/dom-utils/getNodeScroll.js
function getNodeScroll2(node2) {
  if (node2 === getWindow3(node2) || !isHTMLElement3(node2)) {
    return getWindowScroll(node2);
  } else {
    return getHTMLElementScroll(node2);
  }
}

// node_modules/@popperjs/core/lib/dom-utils/getCompositeRect.js
function isElementScaled(element) {
  var rect = element.getBoundingClientRect();
  var scaleX = round3(rect.width) / element.offsetWidth || 1;
  var scaleY = round3(rect.height) / element.offsetHeight || 1;
  return scaleX !== 1 || scaleY !== 1;
}
function getCompositeRect(elementOrVirtualElement, offsetParent, isFixed) {
  if (isFixed === void 0) {
    isFixed = false;
  }
  var isOffsetParentAnElement = isHTMLElement3(offsetParent);
  var offsetParentIsScaled = isHTMLElement3(offsetParent) && isElementScaled(offsetParent);
  var documentElement = getDocumentElement2(offsetParent);
  var rect = getBoundingClientRect2(elementOrVirtualElement, offsetParentIsScaled, isFixed);
  var scroll = {
    scrollLeft: 0,
    scrollTop: 0
  };
  var offsets = {
    x: 0,
    y: 0
  };
  if (isOffsetParentAnElement || !isOffsetParentAnElement && !isFixed) {
    if (getNodeName2(offsetParent) !== "body" || // https://github.com/popperjs/popper-core/issues/1078
    isScrollParent(documentElement)) {
      scroll = getNodeScroll2(offsetParent);
    }
    if (isHTMLElement3(offsetParent)) {
      offsets = getBoundingClientRect2(offsetParent, true);
      offsets.x += offsetParent.clientLeft;
      offsets.y += offsetParent.clientTop;
    } else if (documentElement) {
      offsets.x = getWindowScrollBarX2(documentElement);
    }
  }
  return {
    x: rect.left + scroll.scrollLeft - offsets.x,
    y: rect.top + scroll.scrollTop - offsets.y,
    width: rect.width,
    height: rect.height
  };
}

// node_modules/@popperjs/core/lib/utils/orderModifiers.js
function order(modifiers) {
  var map = /* @__PURE__ */ new Map();
  var visited = /* @__PURE__ */ new Set();
  var result = [];
  modifiers.forEach(function(modifier) {
    map.set(modifier.name, modifier);
  });
  function sort(modifier) {
    visited.add(modifier.name);
    var requires = [].concat(modifier.requires || [], modifier.requiresIfExists || []);
    requires.forEach(function(dep) {
      if (!visited.has(dep)) {
        var depModifier = map.get(dep);
        if (depModifier) {
          sort(depModifier);
        }
      }
    });
    result.push(modifier);
  }
  modifiers.forEach(function(modifier) {
    if (!visited.has(modifier.name)) {
      sort(modifier);
    }
  });
  return result;
}
function orderModifiers(modifiers) {
  var orderedModifiers = order(modifiers);
  return modifierPhases.reduce(function(acc, phase) {
    return acc.concat(orderedModifiers.filter(function(modifier) {
      return modifier.phase === phase;
    }));
  }, []);
}

// node_modules/@popperjs/core/lib/utils/debounce.js
function debounce(fn5) {
  var pending;
  return function() {
    if (!pending) {
      pending = new Promise(function(resolve) {
        Promise.resolve().then(function() {
          pending = void 0;
          resolve(fn5());
        });
      });
    }
    return pending;
  };
}

// node_modules/@popperjs/core/lib/utils/mergeByName.js
function mergeByName(modifiers) {
  var merged = modifiers.reduce(function(merged2, current) {
    var existing = merged2[current.name];
    merged2[current.name] = existing ? Object.assign({}, existing, current, {
      options: Object.assign({}, existing.options, current.options),
      data: Object.assign({}, existing.data, current.data)
    }) : current;
    return merged2;
  }, {});
  return Object.keys(merged).map(function(key) {
    return merged[key];
  });
}

// node_modules/@popperjs/core/lib/createPopper.js
var DEFAULT_OPTIONS = {
  placement: "bottom",
  modifiers: [],
  strategy: "absolute"
};
function areValidElements() {
  for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
    args[_key] = arguments[_key];
  }
  return !args.some(function(element) {
    return !(element && typeof element.getBoundingClientRect === "function");
  });
}
function popperGenerator(generatorOptions) {
  if (generatorOptions === void 0) {
    generatorOptions = {};
  }
  var _generatorOptions = generatorOptions, _generatorOptions$def = _generatorOptions.defaultModifiers, defaultModifiers3 = _generatorOptions$def === void 0 ? [] : _generatorOptions$def, _generatorOptions$def2 = _generatorOptions.defaultOptions, defaultOptions = _generatorOptions$def2 === void 0 ? DEFAULT_OPTIONS : _generatorOptions$def2;
  return function createPopper4(reference2, popper2, options) {
    if (options === void 0) {
      options = defaultOptions;
    }
    var state = {
      placement: "bottom",
      orderedModifiers: [],
      options: Object.assign({}, DEFAULT_OPTIONS, defaultOptions),
      modifiersData: {},
      elements: {
        reference: reference2,
        popper: popper2
      },
      attributes: {},
      styles: {}
    };
    var effectCleanupFns = [];
    var isDestroyed = false;
    var instance = {
      state,
      setOptions: function setOptions(setOptionsAction) {
        var options2 = typeof setOptionsAction === "function" ? setOptionsAction(state.options) : setOptionsAction;
        cleanupModifierEffects();
        state.options = Object.assign({}, defaultOptions, state.options, options2);
        state.scrollParents = {
          reference: isElement4(reference2) ? listScrollParents(reference2) : reference2.contextElement ? listScrollParents(reference2.contextElement) : [],
          popper: listScrollParents(popper2)
        };
        var orderedModifiers = orderModifiers(mergeByName([].concat(defaultModifiers3, state.options.modifiers)));
        state.orderedModifiers = orderedModifiers.filter(function(m) {
          return m.enabled;
        });
        runModifierEffects();
        return instance.update();
      },
      // Sync update – it will always be executed, even if not necessary. This
      // is useful for low frequency updates where sync behavior simplifies the
      // logic.
      // For high frequency updates (e.g. `resize` and `scroll` events), always
      // prefer the async Popper#update method
      forceUpdate: function forceUpdate() {
        if (isDestroyed) {
          return;
        }
        var _state$elements = state.elements, reference3 = _state$elements.reference, popper3 = _state$elements.popper;
        if (!areValidElements(reference3, popper3)) {
          return;
        }
        state.rects = {
          reference: getCompositeRect(reference3, getOffsetParent2(popper3), state.options.strategy === "fixed"),
          popper: getLayoutRect(popper3)
        };
        state.reset = false;
        state.placement = state.options.placement;
        state.orderedModifiers.forEach(function(modifier) {
          return state.modifiersData[modifier.name] = Object.assign({}, modifier.data);
        });
        for (var index6 = 0; index6 < state.orderedModifiers.length; index6++) {
          if (state.reset === true) {
            state.reset = false;
            index6 = -1;
            continue;
          }
          var _state$orderedModifie = state.orderedModifiers[index6], fn5 = _state$orderedModifie.fn, _state$orderedModifie2 = _state$orderedModifie.options, _options = _state$orderedModifie2 === void 0 ? {} : _state$orderedModifie2, name = _state$orderedModifie.name;
          if (typeof fn5 === "function") {
            state = fn5({
              state,
              options: _options,
              name,
              instance
            }) || state;
          }
        }
      },
      // Async and optimistically optimized update – it will not be executed if
      // not necessary (debounced to run at most once-per-tick)
      update: debounce(function() {
        return new Promise(function(resolve) {
          instance.forceUpdate();
          resolve(state);
        });
      }),
      destroy: function destroy() {
        cleanupModifierEffects();
        isDestroyed = true;
      }
    };
    if (!areValidElements(reference2, popper2)) {
      return instance;
    }
    instance.setOptions(options).then(function(state2) {
      if (!isDestroyed && options.onFirstUpdate) {
        options.onFirstUpdate(state2);
      }
    });
    function runModifierEffects() {
      state.orderedModifiers.forEach(function(_ref) {
        var name = _ref.name, _ref$options = _ref.options, options2 = _ref$options === void 0 ? {} : _ref$options, effect5 = _ref.effect;
        if (typeof effect5 === "function") {
          var cleanupFn = effect5({
            state,
            name,
            instance,
            options: options2
          });
          var noopFn = function noopFn2() {
          };
          effectCleanupFns.push(cleanupFn || noopFn);
        }
      });
    }
    function cleanupModifierEffects() {
      effectCleanupFns.forEach(function(fn5) {
        return fn5();
      });
      effectCleanupFns = [];
    }
    return instance;
  };
}
var createPopper = popperGenerator();

// node_modules/@popperjs/core/lib/popper-lite.js
var defaultModifiers = [eventListeners_default, popperOffsets_default, computeStyles_default, applyStyles_default];
var createPopper2 = popperGenerator({
  defaultModifiers
});

// node_modules/@popperjs/core/lib/popper.js
var defaultModifiers2 = [eventListeners_default, popperOffsets_default, computeStyles_default, applyStyles_default, offset_default, flip_default, preventOverflow_default, arrow_default, hide_default];
var createPopper3 = popperGenerator({
  defaultModifiers: defaultModifiers2
});

// node_modules/tippy.js/dist/tippy.esm.js
var BOX_CLASS = "tippy-box";
var CONTENT_CLASS = "tippy-content";
var BACKDROP_CLASS = "tippy-backdrop";
var ARROW_CLASS = "tippy-arrow";
var SVG_ARROW_CLASS = "tippy-svg-arrow";
var TOUCH_OPTIONS = {
  passive: true,
  capture: true
};
var TIPPY_DEFAULT_APPEND_TO = function TIPPY_DEFAULT_APPEND_TO2() {
  return document.body;
};
function hasOwnProperty2(obj, key) {
  return {}.hasOwnProperty.call(obj, key);
}
function getValueAtIndexOrReturn(value, index6, defaultValue) {
  if (Array.isArray(value)) {
    var v = value[index6];
    return v == null ? Array.isArray(defaultValue) ? defaultValue[index6] : defaultValue : v;
  }
  return value;
}
function isType(value, type) {
  var str = {}.toString.call(value);
  return str.indexOf("[object") === 0 && str.indexOf(type + "]") > -1;
}
function invokeWithArgsOrReturn(value, args) {
  return typeof value === "function" ? value.apply(void 0, args) : value;
}
function debounce2(fn5, ms) {
  if (ms === 0) {
    return fn5;
  }
  var timeout;
  return function(arg) {
    clearTimeout(timeout);
    timeout = setTimeout(function() {
      fn5(arg);
    }, ms);
  };
}
function removeProperties(obj, keys2) {
  var clone = Object.assign({}, obj);
  keys2.forEach(function(key) {
    delete clone[key];
  });
  return clone;
}
function splitBySpaces(value) {
  return value.split(/\s+/).filter(Boolean);
}
function normalizeToArray(value) {
  return [].concat(value);
}
function pushIfUnique(arr, value) {
  if (arr.indexOf(value) === -1) {
    arr.push(value);
  }
}
function unique(arr) {
  return arr.filter(function(item, index6) {
    return arr.indexOf(item) === index6;
  });
}
function getBasePlacement2(placement) {
  return placement.split("-")[0];
}
function arrayFrom(value) {
  return [].slice.call(value);
}
function removeUndefinedProps(obj) {
  return Object.keys(obj).reduce(function(acc, key) {
    if (obj[key] !== void 0) {
      acc[key] = obj[key];
    }
    return acc;
  }, {});
}
function div() {
  return document.createElement("div");
}
function isElement5(value) {
  return ["Element", "Fragment"].some(function(type) {
    return isType(value, type);
  });
}
function isNodeList(value) {
  return isType(value, "NodeList");
}
function isMouseEvent(value) {
  return isType(value, "MouseEvent");
}
function isReferenceElement(value) {
  return !!(value && value._tippy && value._tippy.reference === value);
}
function getArrayOfElements(value) {
  if (isElement5(value)) {
    return [value];
  }
  if (isNodeList(value)) {
    return arrayFrom(value);
  }
  if (Array.isArray(value)) {
    return value;
  }
  return arrayFrom(document.querySelectorAll(value));
}
function setTransitionDuration(els, value) {
  els.forEach(function(el) {
    if (el) {
      el.style.transitionDuration = value + "ms";
    }
  });
}
function setVisibilityState(els, state) {
  els.forEach(function(el) {
    if (el) {
      el.setAttribute("data-state", state);
    }
  });
}
function getOwnerDocument(elementOrElements) {
  var _element$ownerDocumen;
  var _normalizeToArray = normalizeToArray(elementOrElements), element = _normalizeToArray[0];
  return element != null && (_element$ownerDocumen = element.ownerDocument) != null && _element$ownerDocumen.body ? element.ownerDocument : document;
}
function isCursorOutsideInteractiveBorder(popperTreeData, event) {
  var clientX = event.clientX, clientY = event.clientY;
  return popperTreeData.every(function(_ref) {
    var popperRect = _ref.popperRect, popperState = _ref.popperState, props = _ref.props;
    var interactiveBorder = props.interactiveBorder;
    var basePlacement = getBasePlacement2(popperState.placement);
    var offsetData = popperState.modifiersData.offset;
    if (!offsetData) {
      return true;
    }
    var topDistance = basePlacement === "bottom" ? offsetData.top.y : 0;
    var bottomDistance = basePlacement === "top" ? offsetData.bottom.y : 0;
    var leftDistance = basePlacement === "right" ? offsetData.left.x : 0;
    var rightDistance = basePlacement === "left" ? offsetData.right.x : 0;
    var exceedsTop = popperRect.top - clientY + topDistance > interactiveBorder;
    var exceedsBottom = clientY - popperRect.bottom - bottomDistance > interactiveBorder;
    var exceedsLeft = popperRect.left - clientX + leftDistance > interactiveBorder;
    var exceedsRight = clientX - popperRect.right - rightDistance > interactiveBorder;
    return exceedsTop || exceedsBottom || exceedsLeft || exceedsRight;
  });
}
function updateTransitionEndListener(box, action, listener) {
  var method = action + "EventListener";
  ["transitionend", "webkitTransitionEnd"].forEach(function(event) {
    box[method](event, listener);
  });
}
function actualContains(parent, child) {
  var target = child;
  while (target) {
    var _target$getRootNode;
    if (parent.contains(target)) {
      return true;
    }
    target = target.getRootNode == null ? void 0 : (_target$getRootNode = target.getRootNode()) == null ? void 0 : _target$getRootNode.host;
  }
  return false;
}
var currentInput = {
  isTouch: false
};
var lastMouseMoveTime = 0;
function onDocumentTouchStart() {
  if (currentInput.isTouch) {
    return;
  }
  currentInput.isTouch = true;
  if (window.performance) {
    document.addEventListener("mousemove", onDocumentMouseMove);
  }
}
function onDocumentMouseMove() {
  var now = performance.now();
  if (now - lastMouseMoveTime < 20) {
    currentInput.isTouch = false;
    document.removeEventListener("mousemove", onDocumentMouseMove);
  }
  lastMouseMoveTime = now;
}
function onWindowBlur() {
  var activeElement2 = document.activeElement;
  if (isReferenceElement(activeElement2)) {
    var instance = activeElement2._tippy;
    if (activeElement2.blur && !instance.state.isVisible) {
      activeElement2.blur();
    }
  }
}
function bindGlobalEventListeners() {
  document.addEventListener("touchstart", onDocumentTouchStart, TOUCH_OPTIONS);
  window.addEventListener("blur", onWindowBlur);
}
var isBrowser3 = typeof window !== "undefined" && typeof document !== "undefined";
var isIE11 = isBrowser3 ? (
  // @ts-ignore
  !!window.msCrypto
) : false;
function createMemoryLeakWarning(method) {
  var txt = method === "destroy" ? "n already-" : " ";
  return [method + "() was called on a" + txt + "destroyed instance. This is a no-op but", "indicates a potential memory leak."].join(" ");
}
function clean(value) {
  var spacesAndTabs = /[ \t]{2,}/g;
  var lineStartWithSpaces = /^[ \t]*/gm;
  return value.replace(spacesAndTabs, " ").replace(lineStartWithSpaces, "").trim();
}
function getDevMessage(message) {
  return clean("\n  %ctippy.js\n\n  %c" + clean(message) + "\n\n  %c👷‍ This is a development-only message. It will be removed in production.\n  ");
}
function getFormattedMessage(message) {
  return [
    getDevMessage(message),
    // title
    "color: #00C584; font-size: 1.3em; font-weight: bold;",
    // message
    "line-height: 1.5",
    // footer
    "color: #a6a095;"
  ];
}
var visitedMessages;
if (true) {
  resetVisitedMessages();
}
function resetVisitedMessages() {
  visitedMessages = /* @__PURE__ */ new Set();
}
function warnWhen(condition, message) {
  if (condition && !visitedMessages.has(message)) {
    var _console;
    visitedMessages.add(message);
    (_console = console).warn.apply(_console, getFormattedMessage(message));
  }
}
function errorWhen(condition, message) {
  if (condition && !visitedMessages.has(message)) {
    var _console2;
    visitedMessages.add(message);
    (_console2 = console).error.apply(_console2, getFormattedMessage(message));
  }
}
function validateTargets(targets) {
  var didPassFalsyValue = !targets;
  var didPassPlainObject = Object.prototype.toString.call(targets) === "[object Object]" && !targets.addEventListener;
  errorWhen(didPassFalsyValue, ["tippy() was passed", "`" + String(targets) + "`", "as its targets (first) argument. Valid types are: String, Element,", "Element[], or NodeList."].join(" "));
  errorWhen(didPassPlainObject, ["tippy() was passed a plain object which is not supported as an argument", "for virtual positioning. Use props.getReferenceClientRect instead."].join(" "));
}
var pluginProps = {
  animateFill: false,
  followCursor: false,
  inlinePositioning: false,
  sticky: false
};
var renderProps = {
  allowHTML: false,
  animation: "fade",
  arrow: true,
  content: "",
  inertia: false,
  maxWidth: 350,
  role: "tooltip",
  theme: "",
  zIndex: 9999
};
var defaultProps117 = Object.assign({
  appendTo: TIPPY_DEFAULT_APPEND_TO,
  aria: {
    content: "auto",
    expanded: "auto"
  },
  delay: 0,
  duration: [300, 250],
  getReferenceClientRect: null,
  hideOnClick: true,
  ignoreAttributes: false,
  interactive: false,
  interactiveBorder: 2,
  interactiveDebounce: 0,
  moveTransition: "",
  offset: [0, 10],
  onAfterUpdate: function onAfterUpdate() {
  },
  onBeforeUpdate: function onBeforeUpdate() {
  },
  onCreate: function onCreate() {
  },
  onDestroy: function onDestroy() {
  },
  onHidden: function onHidden() {
  },
  onHide: function onHide() {
  },
  onMount: function onMount() {
  },
  onShow: function onShow() {
  },
  onShown: function onShown() {
  },
  onTrigger: function onTrigger() {
  },
  onUntrigger: function onUntrigger() {
  },
  onClickOutside: function onClickOutside() {
  },
  placement: "top",
  plugins: [],
  popperOptions: {},
  render: null,
  showOnCreate: false,
  touch: true,
  trigger: "mouseenter focus",
  triggerTarget: null
}, pluginProps, renderProps);
var defaultKeys = Object.keys(defaultProps117);
var setDefaultProps = function setDefaultProps2(partialProps) {
  if (true) {
    validateProps(partialProps, []);
  }
  var keys2 = Object.keys(partialProps);
  keys2.forEach(function(key) {
    defaultProps117[key] = partialProps[key];
  });
};
function getExtendedPassedProps(passedProps) {
  var plugins = passedProps.plugins || [];
  var pluginProps2 = plugins.reduce(function(acc, plugin) {
    var name = plugin.name, defaultValue = plugin.defaultValue;
    if (name) {
      var _name;
      acc[name] = passedProps[name] !== void 0 ? passedProps[name] : (_name = defaultProps117[name]) != null ? _name : defaultValue;
    }
    return acc;
  }, {});
  return Object.assign({}, passedProps, pluginProps2);
}
function getDataAttributeProps(reference2, plugins) {
  var propKeys = plugins ? Object.keys(getExtendedPassedProps(Object.assign({}, defaultProps117, {
    plugins
  }))) : defaultKeys;
  var props = propKeys.reduce(function(acc, key) {
    var valueAsString = (reference2.getAttribute("data-tippy-" + key) || "").trim();
    if (!valueAsString) {
      return acc;
    }
    if (key === "content") {
      acc[key] = valueAsString;
    } else {
      try {
        acc[key] = JSON.parse(valueAsString);
      } catch (e) {
        acc[key] = valueAsString;
      }
    }
    return acc;
  }, {});
  return props;
}
function evaluateProps(reference2, props) {
  var out = Object.assign({}, props, {
    content: invokeWithArgsOrReturn(props.content, [reference2])
  }, props.ignoreAttributes ? {} : getDataAttributeProps(reference2, props.plugins));
  out.aria = Object.assign({}, defaultProps117.aria, out.aria);
  out.aria = {
    expanded: out.aria.expanded === "auto" ? props.interactive : out.aria.expanded,
    content: out.aria.content === "auto" ? props.interactive ? null : "describedby" : out.aria.content
  };
  return out;
}
function validateProps(partialProps, plugins) {
  if (partialProps === void 0) {
    partialProps = {};
  }
  if (plugins === void 0) {
    plugins = [];
  }
  var keys2 = Object.keys(partialProps);
  keys2.forEach(function(prop) {
    var nonPluginProps = removeProperties(defaultProps117, Object.keys(pluginProps));
    var didPassUnknownProp = !hasOwnProperty2(nonPluginProps, prop);
    if (didPassUnknownProp) {
      didPassUnknownProp = plugins.filter(function(plugin) {
        return plugin.name === prop;
      }).length === 0;
    }
    warnWhen(didPassUnknownProp, ["`" + prop + "`", "is not a valid prop. You may have spelled it incorrectly, or if it's", "a plugin, forgot to pass it in an array as props.plugins.", "\n\n", "All props: https://atomiks.github.io/tippyjs/v6/all-props/\n", "Plugins: https://atomiks.github.io/tippyjs/v6/plugins/"].join(" "));
  });
}
var innerHTML = function innerHTML2() {
  return "innerHTML";
};
function dangerouslySetInnerHTML(element, html) {
  element[innerHTML()] = html;
}
function createArrowElement(value) {
  var arrow4 = div();
  if (value === true) {
    arrow4.className = ARROW_CLASS;
  } else {
    arrow4.className = SVG_ARROW_CLASS;
    if (isElement5(value)) {
      arrow4.appendChild(value);
    } else {
      dangerouslySetInnerHTML(arrow4, value);
    }
  }
  return arrow4;
}
function setContent(content, props) {
  if (isElement5(props.content)) {
    dangerouslySetInnerHTML(content, "");
    content.appendChild(props.content);
  } else if (typeof props.content !== "function") {
    if (props.allowHTML) {
      dangerouslySetInnerHTML(content, props.content);
    } else {
      content.textContent = props.content;
    }
  }
}
function getChildren2(popper2) {
  var box = popper2.firstElementChild;
  var boxChildren = arrayFrom(box.children);
  return {
    box,
    content: boxChildren.find(function(node2) {
      return node2.classList.contains(CONTENT_CLASS);
    }),
    arrow: boxChildren.find(function(node2) {
      return node2.classList.contains(ARROW_CLASS) || node2.classList.contains(SVG_ARROW_CLASS);
    }),
    backdrop: boxChildren.find(function(node2) {
      return node2.classList.contains(BACKDROP_CLASS);
    })
  };
}
function render(instance) {
  var popper2 = div();
  var box = div();
  box.className = BOX_CLASS;
  box.setAttribute("data-state", "hidden");
  box.setAttribute("tabindex", "-1");
  var content = div();
  content.className = CONTENT_CLASS;
  content.setAttribute("data-state", "hidden");
  setContent(content, instance.props);
  popper2.appendChild(box);
  box.appendChild(content);
  onUpdate(instance.props, instance.props);
  function onUpdate(prevProps, nextProps) {
    var _getChildren = getChildren2(popper2), box2 = _getChildren.box, content2 = _getChildren.content, arrow4 = _getChildren.arrow;
    if (nextProps.theme) {
      box2.setAttribute("data-theme", nextProps.theme);
    } else {
      box2.removeAttribute("data-theme");
    }
    if (typeof nextProps.animation === "string") {
      box2.setAttribute("data-animation", nextProps.animation);
    } else {
      box2.removeAttribute("data-animation");
    }
    if (nextProps.inertia) {
      box2.setAttribute("data-inertia", "");
    } else {
      box2.removeAttribute("data-inertia");
    }
    box2.style.maxWidth = typeof nextProps.maxWidth === "number" ? nextProps.maxWidth + "px" : nextProps.maxWidth;
    if (nextProps.role) {
      box2.setAttribute("role", nextProps.role);
    } else {
      box2.removeAttribute("role");
    }
    if (prevProps.content !== nextProps.content || prevProps.allowHTML !== nextProps.allowHTML) {
      setContent(content2, instance.props);
    }
    if (nextProps.arrow) {
      if (!arrow4) {
        box2.appendChild(createArrowElement(nextProps.arrow));
      } else if (prevProps.arrow !== nextProps.arrow) {
        box2.removeChild(arrow4);
        box2.appendChild(createArrowElement(nextProps.arrow));
      }
    } else if (arrow4) {
      box2.removeChild(arrow4);
    }
  }
  return {
    popper: popper2,
    onUpdate
  };
}
render.$$tippy = true;
var idCounter = 1;
var mouseMoveListeners = [];
var mountedInstances = [];
function createTippy(reference2, passedProps) {
  var props = evaluateProps(reference2, Object.assign({}, defaultProps117, getExtendedPassedProps(removeUndefinedProps(passedProps))));
  var showTimeout;
  var hideTimeout;
  var scheduleHideAnimationFrame;
  var isVisibleFromClick = false;
  var didHideDueToDocumentMouseDown = false;
  var didTouchMove = false;
  var ignoreOnFirstUpdate = false;
  var lastTriggerEvent;
  var currentTransitionEndListener;
  var onFirstUpdate;
  var listeners = [];
  var debouncedOnMouseMove = debounce2(onMouseMove, props.interactiveDebounce);
  var currentTarget;
  var id = idCounter++;
  var popperInstance = null;
  var plugins = unique(props.plugins);
  var state = {
    // Is the instance currently enabled?
    isEnabled: true,
    // Is the tippy currently showing and not transitioning out?
    isVisible: false,
    // Has the instance been destroyed?
    isDestroyed: false,
    // Is the tippy currently mounted to the DOM?
    isMounted: false,
    // Has the tippy finished transitioning in?
    isShown: false
  };
  var instance = {
    // properties
    id,
    reference: reference2,
    popper: div(),
    popperInstance,
    props,
    state,
    plugins,
    // methods
    clearDelayTimeouts,
    setProps,
    setContent: setContent2,
    show,
    hide: hide3,
    hideWithInteractivity,
    enable,
    disable,
    unmount,
    destroy
  };
  if (!props.render) {
    if (true) {
      errorWhen(true, "render() function has not been supplied.");
    }
    return instance;
  }
  var _props$render = props.render(instance), popper2 = _props$render.popper, onUpdate = _props$render.onUpdate;
  popper2.setAttribute("data-tippy-root", "");
  popper2.id = "tippy-" + instance.id;
  instance.popper = popper2;
  reference2._tippy = instance;
  popper2._tippy = instance;
  var pluginsHooks = plugins.map(function(plugin) {
    return plugin.fn(instance);
  });
  var hasAriaExpanded = reference2.hasAttribute("aria-expanded");
  addListeners();
  handleAriaExpandedAttribute();
  handleStyles();
  invokeHook("onCreate", [instance]);
  if (props.showOnCreate) {
    scheduleShow();
  }
  popper2.addEventListener("mouseenter", function() {
    if (instance.props.interactive && instance.state.isVisible) {
      instance.clearDelayTimeouts();
    }
  });
  popper2.addEventListener("mouseleave", function() {
    if (instance.props.interactive && instance.props.trigger.indexOf("mouseenter") >= 0) {
      getDocument2().addEventListener("mousemove", debouncedOnMouseMove);
    }
  });
  return instance;
  function getNormalizedTouchSettings() {
    var touch = instance.props.touch;
    return Array.isArray(touch) ? touch : [touch, 0];
  }
  function getIsCustomTouchBehavior() {
    return getNormalizedTouchSettings()[0] === "hold";
  }
  function getIsDefaultRenderFn() {
    var _instance$props$rende;
    return !!((_instance$props$rende = instance.props.render) != null && _instance$props$rende.$$tippy);
  }
  function getCurrentTarget() {
    return currentTarget || reference2;
  }
  function getDocument2() {
    var parent = getCurrentTarget().parentNode;
    return parent ? getOwnerDocument(parent) : document;
  }
  function getDefaultTemplateChildren() {
    return getChildren2(popper2);
  }
  function getDelay2(isShow) {
    if (instance.state.isMounted && !instance.state.isVisible || currentInput.isTouch || lastTriggerEvent && lastTriggerEvent.type === "focus") {
      return 0;
    }
    return getValueAtIndexOrReturn(instance.props.delay, isShow ? 0 : 1, defaultProps117.delay);
  }
  function handleStyles(fromHide) {
    if (fromHide === void 0) {
      fromHide = false;
    }
    popper2.style.pointerEvents = instance.props.interactive && !fromHide ? "" : "none";
    popper2.style.zIndex = "" + instance.props.zIndex;
  }
  function invokeHook(hook, args, shouldInvokePropsHook) {
    if (shouldInvokePropsHook === void 0) {
      shouldInvokePropsHook = true;
    }
    pluginsHooks.forEach(function(pluginHooks) {
      if (pluginHooks[hook]) {
        pluginHooks[hook].apply(pluginHooks, args);
      }
    });
    if (shouldInvokePropsHook) {
      var _instance$props;
      (_instance$props = instance.props)[hook].apply(_instance$props, args);
    }
  }
  function handleAriaContentAttribute() {
    var aria = instance.props.aria;
    if (!aria.content) {
      return;
    }
    var attr = "aria-" + aria.content;
    var id2 = popper2.id;
    var nodes = normalizeToArray(instance.props.triggerTarget || reference2);
    nodes.forEach(function(node2) {
      var currentValue = node2.getAttribute(attr);
      if (instance.state.isVisible) {
        node2.setAttribute(attr, currentValue ? currentValue + " " + id2 : id2);
      } else {
        var nextValue = currentValue && currentValue.replace(id2, "").trim();
        if (nextValue) {
          node2.setAttribute(attr, nextValue);
        } else {
          node2.removeAttribute(attr);
        }
      }
    });
  }
  function handleAriaExpandedAttribute() {
    if (hasAriaExpanded || !instance.props.aria.expanded) {
      return;
    }
    var nodes = normalizeToArray(instance.props.triggerTarget || reference2);
    nodes.forEach(function(node2) {
      if (instance.props.interactive) {
        node2.setAttribute("aria-expanded", instance.state.isVisible && node2 === getCurrentTarget() ? "true" : "false");
      } else {
        node2.removeAttribute("aria-expanded");
      }
    });
  }
  function cleanupInteractiveMouseListeners() {
    getDocument2().removeEventListener("mousemove", debouncedOnMouseMove);
    mouseMoveListeners = mouseMoveListeners.filter(function(listener) {
      return listener !== debouncedOnMouseMove;
    });
  }
  function onDocumentPress(event) {
    if (currentInput.isTouch) {
      if (didTouchMove || event.type === "mousedown") {
        return;
      }
    }
    var actualTarget = event.composedPath && event.composedPath()[0] || event.target;
    if (instance.props.interactive && actualContains(popper2, actualTarget)) {
      return;
    }
    if (normalizeToArray(instance.props.triggerTarget || reference2).some(function(el) {
      return actualContains(el, actualTarget);
    })) {
      if (currentInput.isTouch) {
        return;
      }
      if (instance.state.isVisible && instance.props.trigger.indexOf("click") >= 0) {
        return;
      }
    } else {
      invokeHook("onClickOutside", [instance, event]);
    }
    if (instance.props.hideOnClick === true) {
      instance.clearDelayTimeouts();
      instance.hide();
      didHideDueToDocumentMouseDown = true;
      setTimeout(function() {
        didHideDueToDocumentMouseDown = false;
      });
      if (!instance.state.isMounted) {
        removeDocumentPress();
      }
    }
  }
  function onTouchMove() {
    didTouchMove = true;
  }
  function onTouchStart() {
    didTouchMove = false;
  }
  function addDocumentPress() {
    var doc = getDocument2();
    doc.addEventListener("mousedown", onDocumentPress, true);
    doc.addEventListener("touchend", onDocumentPress, TOUCH_OPTIONS);
    doc.addEventListener("touchstart", onTouchStart, TOUCH_OPTIONS);
    doc.addEventListener("touchmove", onTouchMove, TOUCH_OPTIONS);
  }
  function removeDocumentPress() {
    var doc = getDocument2();
    doc.removeEventListener("mousedown", onDocumentPress, true);
    doc.removeEventListener("touchend", onDocumentPress, TOUCH_OPTIONS);
    doc.removeEventListener("touchstart", onTouchStart, TOUCH_OPTIONS);
    doc.removeEventListener("touchmove", onTouchMove, TOUCH_OPTIONS);
  }
  function onTransitionedOut(duration, callback) {
    onTransitionEnd(duration, function() {
      if (!instance.state.isVisible && popper2.parentNode && popper2.parentNode.contains(popper2)) {
        callback();
      }
    });
  }
  function onTransitionedIn(duration, callback) {
    onTransitionEnd(duration, callback);
  }
  function onTransitionEnd(duration, callback) {
    var box = getDefaultTemplateChildren().box;
    function listener(event) {
      if (event.target === box) {
        updateTransitionEndListener(box, "remove", listener);
        callback();
      }
    }
    if (duration === 0) {
      return callback();
    }
    updateTransitionEndListener(box, "remove", currentTransitionEndListener);
    updateTransitionEndListener(box, "add", listener);
    currentTransitionEndListener = listener;
  }
  function on2(eventType, handler, options) {
    if (options === void 0) {
      options = false;
    }
    var nodes = normalizeToArray(instance.props.triggerTarget || reference2);
    nodes.forEach(function(node2) {
      node2.addEventListener(eventType, handler, options);
      listeners.push({
        node: node2,
        eventType,
        handler,
        options
      });
    });
  }
  function addListeners() {
    if (getIsCustomTouchBehavior()) {
      on2("touchstart", onTrigger2, {
        passive: true
      });
      on2("touchend", onMouseLeave, {
        passive: true
      });
    }
    splitBySpaces(instance.props.trigger).forEach(function(eventType) {
      if (eventType === "manual") {
        return;
      }
      on2(eventType, onTrigger2);
      switch (eventType) {
        case "mouseenter":
          on2("mouseleave", onMouseLeave);
          break;
        case "focus":
          on2(isIE11 ? "focusout" : "blur", onBlurOrFocusOut);
          break;
        case "focusin":
          on2("focusout", onBlurOrFocusOut);
          break;
      }
    });
  }
  function removeListeners() {
    listeners.forEach(function(_ref) {
      var node2 = _ref.node, eventType = _ref.eventType, handler = _ref.handler, options = _ref.options;
      node2.removeEventListener(eventType, handler, options);
    });
    listeners = [];
  }
  function onTrigger2(event) {
    var _lastTriggerEvent;
    var shouldScheduleClickHide = false;
    if (!instance.state.isEnabled || isEventListenerStopped(event) || didHideDueToDocumentMouseDown) {
      return;
    }
    var wasFocused = ((_lastTriggerEvent = lastTriggerEvent) == null ? void 0 : _lastTriggerEvent.type) === "focus";
    lastTriggerEvent = event;
    currentTarget = event.currentTarget;
    handleAriaExpandedAttribute();
    if (!instance.state.isVisible && isMouseEvent(event)) {
      mouseMoveListeners.forEach(function(listener) {
        return listener(event);
      });
    }
    if (event.type === "click" && (instance.props.trigger.indexOf("mouseenter") < 0 || isVisibleFromClick) && instance.props.hideOnClick !== false && instance.state.isVisible) {
      shouldScheduleClickHide = true;
    } else {
      scheduleShow(event);
    }
    if (event.type === "click") {
      isVisibleFromClick = !shouldScheduleClickHide;
    }
    if (shouldScheduleClickHide && !wasFocused) {
      scheduleHide(event);
    }
  }
  function onMouseMove(event) {
    var target = event.target;
    var isCursorOverReferenceOrPopper = getCurrentTarget().contains(target) || popper2.contains(target);
    if (event.type === "mousemove" && isCursorOverReferenceOrPopper) {
      return;
    }
    var popperTreeData = getNestedPopperTree().concat(popper2).map(function(popper3) {
      var _instance$popperInsta;
      var instance2 = popper3._tippy;
      var state2 = (_instance$popperInsta = instance2.popperInstance) == null ? void 0 : _instance$popperInsta.state;
      if (state2) {
        return {
          popperRect: popper3.getBoundingClientRect(),
          popperState: state2,
          props
        };
      }
      return null;
    }).filter(Boolean);
    if (isCursorOutsideInteractiveBorder(popperTreeData, event)) {
      cleanupInteractiveMouseListeners();
      scheduleHide(event);
    }
  }
  function onMouseLeave(event) {
    var shouldBail = isEventListenerStopped(event) || instance.props.trigger.indexOf("click") >= 0 && isVisibleFromClick;
    if (shouldBail) {
      return;
    }
    if (instance.props.interactive) {
      instance.hideWithInteractivity(event);
      return;
    }
    scheduleHide(event);
  }
  function onBlurOrFocusOut(event) {
    if (instance.props.trigger.indexOf("focusin") < 0 && event.target !== getCurrentTarget()) {
      return;
    }
    if (instance.props.interactive && event.relatedTarget && popper2.contains(event.relatedTarget)) {
      return;
    }
    scheduleHide(event);
  }
  function isEventListenerStopped(event) {
    return currentInput.isTouch ? getIsCustomTouchBehavior() !== event.type.indexOf("touch") >= 0 : false;
  }
  function createPopperInstance() {
    destroyPopperInstance();
    var _instance$props2 = instance.props, popperOptions = _instance$props2.popperOptions, placement = _instance$props2.placement, offset3 = _instance$props2.offset, getReferenceClientRect = _instance$props2.getReferenceClientRect, moveTransition = _instance$props2.moveTransition;
    var arrow4 = getIsDefaultRenderFn() ? getChildren2(popper2).arrow : null;
    var computedReference = getReferenceClientRect ? {
      getBoundingClientRect: getReferenceClientRect,
      contextElement: getReferenceClientRect.contextElement || getCurrentTarget()
    } : reference2;
    var tippyModifier = {
      name: "$$tippy",
      enabled: true,
      phase: "beforeWrite",
      requires: ["computeStyles"],
      fn: function fn5(_ref2) {
        var state2 = _ref2.state;
        if (getIsDefaultRenderFn()) {
          var _getDefaultTemplateCh = getDefaultTemplateChildren(), box = _getDefaultTemplateCh.box;
          ["placement", "reference-hidden", "escaped"].forEach(function(attr) {
            if (attr === "placement") {
              box.setAttribute("data-placement", state2.placement);
            } else {
              if (state2.attributes.popper["data-popper-" + attr]) {
                box.setAttribute("data-" + attr, "");
              } else {
                box.removeAttribute("data-" + attr);
              }
            }
          });
          state2.attributes.popper = {};
        }
      }
    };
    var modifiers = [{
      name: "offset",
      options: {
        offset: offset3
      }
    }, {
      name: "preventOverflow",
      options: {
        padding: {
          top: 2,
          bottom: 2,
          left: 5,
          right: 5
        }
      }
    }, {
      name: "flip",
      options: {
        padding: 5
      }
    }, {
      name: "computeStyles",
      options: {
        adaptive: !moveTransition
      }
    }, tippyModifier];
    if (getIsDefaultRenderFn() && arrow4) {
      modifiers.push({
        name: "arrow",
        options: {
          element: arrow4,
          padding: 3
        }
      });
    }
    modifiers.push.apply(modifiers, (popperOptions == null ? void 0 : popperOptions.modifiers) || []);
    instance.popperInstance = createPopper3(computedReference, popper2, Object.assign({}, popperOptions, {
      placement,
      onFirstUpdate,
      modifiers
    }));
  }
  function destroyPopperInstance() {
    if (instance.popperInstance) {
      instance.popperInstance.destroy();
      instance.popperInstance = null;
    }
  }
  function mount() {
    var appendTo = instance.props.appendTo;
    var parentNode;
    var node2 = getCurrentTarget();
    if (instance.props.interactive && appendTo === TIPPY_DEFAULT_APPEND_TO || appendTo === "parent") {
      parentNode = node2.parentNode;
    } else {
      parentNode = invokeWithArgsOrReturn(appendTo, [node2]);
    }
    if (!parentNode.contains(popper2)) {
      parentNode.appendChild(popper2);
    }
    instance.state.isMounted = true;
    createPopperInstance();
    if (true) {
      warnWhen(instance.props.interactive && appendTo === defaultProps117.appendTo && node2.nextElementSibling !== popper2, ["Interactive tippy element may not be accessible via keyboard", "navigation because it is not directly after the reference element", "in the DOM source order.", "\n\n", "Using a wrapper <div> or <span> tag around the reference element", "solves this by creating a new parentNode context.", "\n\n", "Specifying `appendTo: document.body` silences this warning, but it", "assumes you are using a focus management solution to handle", "keyboard navigation.", "\n\n", "See: https://atomiks.github.io/tippyjs/v6/accessibility/#interactivity"].join(" "));
    }
  }
  function getNestedPopperTree() {
    return arrayFrom(popper2.querySelectorAll("[data-tippy-root]"));
  }
  function scheduleShow(event) {
    instance.clearDelayTimeouts();
    if (event) {
      invokeHook("onTrigger", [instance, event]);
    }
    addDocumentPress();
    var delay = getDelay2(true);
    var _getNormalizedTouchSe = getNormalizedTouchSettings(), touchValue = _getNormalizedTouchSe[0], touchDelay = _getNormalizedTouchSe[1];
    if (currentInput.isTouch && touchValue === "hold" && touchDelay) {
      delay = touchDelay;
    }
    if (delay) {
      showTimeout = setTimeout(function() {
        instance.show();
      }, delay);
    } else {
      instance.show();
    }
  }
  function scheduleHide(event) {
    instance.clearDelayTimeouts();
    invokeHook("onUntrigger", [instance, event]);
    if (!instance.state.isVisible) {
      removeDocumentPress();
      return;
    }
    if (instance.props.trigger.indexOf("mouseenter") >= 0 && instance.props.trigger.indexOf("click") >= 0 && ["mouseleave", "mousemove"].indexOf(event.type) >= 0 && isVisibleFromClick) {
      return;
    }
    var delay = getDelay2(false);
    if (delay) {
      hideTimeout = setTimeout(function() {
        if (instance.state.isVisible) {
          instance.hide();
        }
      }, delay);
    } else {
      scheduleHideAnimationFrame = requestAnimationFrame(function() {
        instance.hide();
      });
    }
  }
  function enable() {
    instance.state.isEnabled = true;
  }
  function disable() {
    instance.hide();
    instance.state.isEnabled = false;
  }
  function clearDelayTimeouts() {
    clearTimeout(showTimeout);
    clearTimeout(hideTimeout);
    cancelAnimationFrame(scheduleHideAnimationFrame);
  }
  function setProps(partialProps) {
    if (true) {
      warnWhen(instance.state.isDestroyed, createMemoryLeakWarning("setProps"));
    }
    if (instance.state.isDestroyed) {
      return;
    }
    invokeHook("onBeforeUpdate", [instance, partialProps]);
    removeListeners();
    var prevProps = instance.props;
    var nextProps = evaluateProps(reference2, Object.assign({}, prevProps, removeUndefinedProps(partialProps), {
      ignoreAttributes: true
    }));
    instance.props = nextProps;
    addListeners();
    if (prevProps.interactiveDebounce !== nextProps.interactiveDebounce) {
      cleanupInteractiveMouseListeners();
      debouncedOnMouseMove = debounce2(onMouseMove, nextProps.interactiveDebounce);
    }
    if (prevProps.triggerTarget && !nextProps.triggerTarget) {
      normalizeToArray(prevProps.triggerTarget).forEach(function(node2) {
        node2.removeAttribute("aria-expanded");
      });
    } else if (nextProps.triggerTarget) {
      reference2.removeAttribute("aria-expanded");
    }
    handleAriaExpandedAttribute();
    handleStyles();
    if (onUpdate) {
      onUpdate(prevProps, nextProps);
    }
    if (instance.popperInstance) {
      createPopperInstance();
      getNestedPopperTree().forEach(function(nestedPopper) {
        requestAnimationFrame(nestedPopper._tippy.popperInstance.forceUpdate);
      });
    }
    invokeHook("onAfterUpdate", [instance, partialProps]);
  }
  function setContent2(content) {
    instance.setProps({
      content
    });
  }
  function show() {
    if (true) {
      warnWhen(instance.state.isDestroyed, createMemoryLeakWarning("show"));
    }
    var isAlreadyVisible = instance.state.isVisible;
    var isDestroyed = instance.state.isDestroyed;
    var isDisabled = !instance.state.isEnabled;
    var isTouchAndTouchDisabled = currentInput.isTouch && !instance.props.touch;
    var duration = getValueAtIndexOrReturn(instance.props.duration, 0, defaultProps117.duration);
    if (isAlreadyVisible || isDestroyed || isDisabled || isTouchAndTouchDisabled) {
      return;
    }
    if (getCurrentTarget().hasAttribute("disabled")) {
      return;
    }
    invokeHook("onShow", [instance], false);
    if (instance.props.onShow(instance) === false) {
      return;
    }
    instance.state.isVisible = true;
    if (getIsDefaultRenderFn()) {
      popper2.style.visibility = "visible";
    }
    handleStyles();
    addDocumentPress();
    if (!instance.state.isMounted) {
      popper2.style.transition = "none";
    }
    if (getIsDefaultRenderFn()) {
      var _getDefaultTemplateCh2 = getDefaultTemplateChildren(), box = _getDefaultTemplateCh2.box, content = _getDefaultTemplateCh2.content;
      setTransitionDuration([box, content], 0);
    }
    onFirstUpdate = function onFirstUpdate2() {
      var _instance$popperInsta2;
      if (!instance.state.isVisible || ignoreOnFirstUpdate) {
        return;
      }
      ignoreOnFirstUpdate = true;
      void popper2.offsetHeight;
      popper2.style.transition = instance.props.moveTransition;
      if (getIsDefaultRenderFn() && instance.props.animation) {
        var _getDefaultTemplateCh3 = getDefaultTemplateChildren(), _box = _getDefaultTemplateCh3.box, _content = _getDefaultTemplateCh3.content;
        setTransitionDuration([_box, _content], duration);
        setVisibilityState([_box, _content], "visible");
      }
      handleAriaContentAttribute();
      handleAriaExpandedAttribute();
      pushIfUnique(mountedInstances, instance);
      (_instance$popperInsta2 = instance.popperInstance) == null ? void 0 : _instance$popperInsta2.forceUpdate();
      invokeHook("onMount", [instance]);
      if (instance.props.animation && getIsDefaultRenderFn()) {
        onTransitionedIn(duration, function() {
          instance.state.isShown = true;
          invokeHook("onShown", [instance]);
        });
      }
    };
    mount();
  }
  function hide3() {
    if (true) {
      warnWhen(instance.state.isDestroyed, createMemoryLeakWarning("hide"));
    }
    var isAlreadyHidden = !instance.state.isVisible;
    var isDestroyed = instance.state.isDestroyed;
    var isDisabled = !instance.state.isEnabled;
    var duration = getValueAtIndexOrReturn(instance.props.duration, 1, defaultProps117.duration);
    if (isAlreadyHidden || isDestroyed || isDisabled) {
      return;
    }
    invokeHook("onHide", [instance], false);
    if (instance.props.onHide(instance) === false) {
      return;
    }
    instance.state.isVisible = false;
    instance.state.isShown = false;
    ignoreOnFirstUpdate = false;
    isVisibleFromClick = false;
    if (getIsDefaultRenderFn()) {
      popper2.style.visibility = "hidden";
    }
    cleanupInteractiveMouseListeners();
    removeDocumentPress();
    handleStyles(true);
    if (getIsDefaultRenderFn()) {
      var _getDefaultTemplateCh4 = getDefaultTemplateChildren(), box = _getDefaultTemplateCh4.box, content = _getDefaultTemplateCh4.content;
      if (instance.props.animation) {
        setTransitionDuration([box, content], duration);
        setVisibilityState([box, content], "hidden");
      }
    }
    handleAriaContentAttribute();
    handleAriaExpandedAttribute();
    if (instance.props.animation) {
      if (getIsDefaultRenderFn()) {
        onTransitionedOut(duration, instance.unmount);
      }
    } else {
      instance.unmount();
    }
  }
  function hideWithInteractivity(event) {
    if (true) {
      warnWhen(instance.state.isDestroyed, createMemoryLeakWarning("hideWithInteractivity"));
    }
    getDocument2().addEventListener("mousemove", debouncedOnMouseMove);
    pushIfUnique(mouseMoveListeners, debouncedOnMouseMove);
    debouncedOnMouseMove(event);
  }
  function unmount() {
    if (true) {
      warnWhen(instance.state.isDestroyed, createMemoryLeakWarning("unmount"));
    }
    if (instance.state.isVisible) {
      instance.hide();
    }
    if (!instance.state.isMounted) {
      return;
    }
    destroyPopperInstance();
    getNestedPopperTree().forEach(function(nestedPopper) {
      nestedPopper._tippy.unmount();
    });
    if (popper2.parentNode) {
      popper2.parentNode.removeChild(popper2);
    }
    mountedInstances = mountedInstances.filter(function(i) {
      return i !== instance;
    });
    instance.state.isMounted = false;
    invokeHook("onHidden", [instance]);
  }
  function destroy() {
    if (true) {
      warnWhen(instance.state.isDestroyed, createMemoryLeakWarning("destroy"));
    }
    if (instance.state.isDestroyed) {
      return;
    }
    instance.clearDelayTimeouts();
    instance.unmount();
    removeListeners();
    delete reference2._tippy;
    instance.state.isDestroyed = true;
    invokeHook("onDestroy", [instance]);
  }
}
function tippy(targets, optionalProps) {
  if (optionalProps === void 0) {
    optionalProps = {};
  }
  var plugins = defaultProps117.plugins.concat(optionalProps.plugins || []);
  if (true) {
    validateTargets(targets);
    validateProps(optionalProps, plugins);
  }
  bindGlobalEventListeners();
  var passedProps = Object.assign({}, optionalProps, {
    plugins
  });
  var elements = getArrayOfElements(targets);
  if (true) {
    var isSingleContentElement = isElement5(passedProps.content);
    var isMoreThanOneReferenceElement = elements.length > 1;
    warnWhen(isSingleContentElement && isMoreThanOneReferenceElement, ["tippy() was passed an Element as the `content` prop, but more than", "one tippy instance was created by this invocation. This means the", "content element will only be appended to the last tippy instance.", "\n\n", "Instead, pass the .innerHTML of the element, or use a function that", "returns a cloned version of the element instead.", "\n\n", "1) content: element.innerHTML\n", "2) content: () => element.cloneNode(true)"].join(" "));
  }
  var instances = elements.reduce(function(acc, reference2) {
    var instance = reference2 && createTippy(reference2, passedProps);
    if (instance) {
      acc.push(instance);
    }
    return acc;
  }, []);
  return isElement5(targets) ? instances[0] : instances;
}
tippy.defaultProps = defaultProps117;
tippy.setDefaultProps = setDefaultProps;
tippy.currentInput = currentInput;
var applyStylesModifier = Object.assign({}, applyStyles_default, {
  effect: function effect4(_ref) {
    var state = _ref.state;
    var initialStyles = {
      popper: {
        position: state.options.strategy,
        left: "0",
        top: "0",
        margin: "0"
      },
      arrow: {
        position: "absolute"
      },
      reference: {}
    };
    Object.assign(state.elements.popper.style, initialStyles.popper);
    state.styles = initialStyles;
    if (state.elements.arrow) {
      Object.assign(state.elements.arrow.style, initialStyles.arrow);
    }
  }
});
var createSingleton = function createSingleton2(tippyInstances, optionalProps) {
  var _optionalProps$popper;
  if (optionalProps === void 0) {
    optionalProps = {};
  }
  if (true) {
    errorWhen(!Array.isArray(tippyInstances), ["The first argument passed to createSingleton() must be an array of", "tippy instances. The passed value was", String(tippyInstances)].join(" "));
  }
  var individualInstances = tippyInstances;
  var references = [];
  var triggerTargets = [];
  var currentTarget;
  var overrides = optionalProps.overrides;
  var interceptSetPropsCleanups = [];
  var shownOnCreate = false;
  function setTriggerTargets() {
    triggerTargets = individualInstances.map(function(instance) {
      return normalizeToArray(instance.props.triggerTarget || instance.reference);
    }).reduce(function(acc, item) {
      return acc.concat(item);
    }, []);
  }
  function setReferences() {
    references = individualInstances.map(function(instance) {
      return instance.reference;
    });
  }
  function enableInstances(isEnabled) {
    individualInstances.forEach(function(instance) {
      if (isEnabled) {
        instance.enable();
      } else {
        instance.disable();
      }
    });
  }
  function interceptSetProps(singleton2) {
    return individualInstances.map(function(instance) {
      var originalSetProps2 = instance.setProps;
      instance.setProps = function(props) {
        originalSetProps2(props);
        if (instance.reference === currentTarget) {
          singleton2.setProps(props);
        }
      };
      return function() {
        instance.setProps = originalSetProps2;
      };
    });
  }
  function prepareInstance(singleton2, target) {
    var index6 = triggerTargets.indexOf(target);
    if (target === currentTarget) {
      return;
    }
    currentTarget = target;
    var overrideProps = (overrides || []).concat("content").reduce(function(acc, prop) {
      acc[prop] = individualInstances[index6].props[prop];
      return acc;
    }, {});
    singleton2.setProps(Object.assign({}, overrideProps, {
      getReferenceClientRect: typeof overrideProps.getReferenceClientRect === "function" ? overrideProps.getReferenceClientRect : function() {
        var _references$index;
        return (_references$index = references[index6]) == null ? void 0 : _references$index.getBoundingClientRect();
      }
    }));
  }
  enableInstances(false);
  setReferences();
  setTriggerTargets();
  var plugin = {
    fn: function fn5() {
      return {
        onDestroy: function onDestroy2() {
          enableInstances(true);
        },
        onHidden: function onHidden2() {
          currentTarget = null;
        },
        onClickOutside: function onClickOutside2(instance) {
          if (instance.props.showOnCreate && !shownOnCreate) {
            shownOnCreate = true;
            currentTarget = null;
          }
        },
        onShow: function onShow2(instance) {
          if (instance.props.showOnCreate && !shownOnCreate) {
            shownOnCreate = true;
            prepareInstance(instance, references[0]);
          }
        },
        onTrigger: function onTrigger2(instance, event) {
          prepareInstance(instance, event.currentTarget);
        }
      };
    }
  };
  var singleton = tippy(div(), Object.assign({}, removeProperties(optionalProps, ["overrides"]), {
    plugins: [plugin].concat(optionalProps.plugins || []),
    triggerTarget: triggerTargets,
    popperOptions: Object.assign({}, optionalProps.popperOptions, {
      modifiers: [].concat(((_optionalProps$popper = optionalProps.popperOptions) == null ? void 0 : _optionalProps$popper.modifiers) || [], [applyStylesModifier])
    })
  }));
  var originalShow = singleton.show;
  singleton.show = function(target) {
    originalShow();
    if (!currentTarget && target == null) {
      return prepareInstance(singleton, references[0]);
    }
    if (currentTarget && target == null) {
      return;
    }
    if (typeof target === "number") {
      return references[target] && prepareInstance(singleton, references[target]);
    }
    if (individualInstances.indexOf(target) >= 0) {
      var ref = target.reference;
      return prepareInstance(singleton, ref);
    }
    if (references.indexOf(target) >= 0) {
      return prepareInstance(singleton, target);
    }
  };
  singleton.showNext = function() {
    var first = references[0];
    if (!currentTarget) {
      return singleton.show(0);
    }
    var index6 = references.indexOf(currentTarget);
    singleton.show(references[index6 + 1] || first);
  };
  singleton.showPrevious = function() {
    var last = references[references.length - 1];
    if (!currentTarget) {
      return singleton.show(last);
    }
    var index6 = references.indexOf(currentTarget);
    var target = references[index6 - 1] || last;
    singleton.show(target);
  };
  var originalSetProps = singleton.setProps;
  singleton.setProps = function(props) {
    overrides = props.overrides || overrides;
    originalSetProps(props);
  };
  singleton.setInstances = function(nextInstances) {
    enableInstances(true);
    interceptSetPropsCleanups.forEach(function(fn5) {
      return fn5();
    });
    individualInstances = nextInstances;
    enableInstances(false);
    setReferences();
    setTriggerTargets();
    interceptSetPropsCleanups = interceptSetProps(singleton);
    singleton.setProps({
      triggerTarget: triggerTargets
    });
  };
  interceptSetPropsCleanups = interceptSetProps(singleton);
  return singleton;
};
var sticky = {
  name: "sticky",
  defaultValue: false,
  fn: function fn2(instance) {
    var reference2 = instance.reference, popper2 = instance.popper;
    function getReference() {
      return instance.popperInstance ? instance.popperInstance.state.elements.reference : reference2;
    }
    function shouldCheck(value) {
      return instance.props.sticky === true || instance.props.sticky === value;
    }
    var prevRefRect = null;
    var prevPopRect = null;
    function updatePosition() {
      var currentRefRect = shouldCheck("reference") ? getReference().getBoundingClientRect() : null;
      var currentPopRect = shouldCheck("popper") ? popper2.getBoundingClientRect() : null;
      if (currentRefRect && areRectsDifferent(prevRefRect, currentRefRect) || currentPopRect && areRectsDifferent(prevPopRect, currentPopRect)) {
        if (instance.popperInstance) {
          instance.popperInstance.update();
        }
      }
      prevRefRect = currentRefRect;
      prevPopRect = currentPopRect;
      if (instance.state.isMounted) {
        requestAnimationFrame(updatePosition);
      }
    }
    return {
      onMount: function onMount2() {
        if (instance.props.sticky) {
          updatePosition();
        }
      }
    };
  }
};
function areRectsDifferent(rectA, rectB) {
  if (rectA && rectB) {
    return rectA.top !== rectB.top || rectA.right !== rectB.right || rectA.bottom !== rectB.bottom || rectA.left !== rectB.left;
  }
  return true;
}
tippy.setDefaultProps({
  render
});
var tippy_esm_default = tippy;

// node_modules/@tiptap/extension-bubble-menu/dist/index.js
var BubbleMenuView = class {
  constructor({ editor, element, view, tippyOptions = {}, updateDelay = 250, shouldShow }) {
    this.preventHide = false;
    this.shouldShow = ({ view: view2, state, from: from2, to: to2 }) => {
      const { doc, selection } = state;
      const { empty } = selection;
      const isEmptyTextBlock = !doc.textBetween(from2, to2).length && isTextSelection(state.selection);
      const isChildOfMenu = this.element.contains(document.activeElement);
      const hasEditorFocus = view2.hasFocus() || isChildOfMenu;
      if (!hasEditorFocus || empty || isEmptyTextBlock || !this.editor.isEditable) {
        return false;
      }
      return true;
    };
    this.mousedownHandler = () => {
      this.preventHide = true;
    };
    this.dragstartHandler = () => {
      this.hide();
    };
    this.focusHandler = () => {
      setTimeout(() => this.update(this.editor.view));
    };
    this.blurHandler = ({ event }) => {
      var _a;
      if (this.preventHide) {
        this.preventHide = false;
        return;
      }
      if ((event === null || event === void 0 ? void 0 : event.relatedTarget) && ((_a = this.element.parentNode) === null || _a === void 0 ? void 0 : _a.contains(event.relatedTarget))) {
        return;
      }
      this.hide();
    };
    this.tippyBlurHandler = (event) => {
      this.blurHandler({ event });
    };
    this.handleDebouncedUpdate = (view2, oldState) => {
      const selectionChanged = !(oldState === null || oldState === void 0 ? void 0 : oldState.selection.eq(view2.state.selection));
      const docChanged = !(oldState === null || oldState === void 0 ? void 0 : oldState.doc.eq(view2.state.doc));
      if (!selectionChanged && !docChanged) {
        return;
      }
      if (this.updateDebounceTimer) {
        clearTimeout(this.updateDebounceTimer);
      }
      this.updateDebounceTimer = window.setTimeout(() => {
        this.updateHandler(view2, selectionChanged, docChanged, oldState);
      }, this.updateDelay);
    };
    this.updateHandler = (view2, selectionChanged, docChanged, oldState) => {
      var _a, _b, _c;
      const { state, composing } = view2;
      const { selection } = state;
      const isSame = !selectionChanged && !docChanged;
      if (composing || isSame) {
        return;
      }
      this.createTooltip();
      const { ranges } = selection;
      const from2 = Math.min(...ranges.map((range2) => range2.$from.pos));
      const to2 = Math.max(...ranges.map((range2) => range2.$to.pos));
      const shouldShow2 = (_a = this.shouldShow) === null || _a === void 0 ? void 0 : _a.call(this, {
        editor: this.editor,
        view: view2,
        state,
        oldState,
        from: from2,
        to: to2
      });
      if (!shouldShow2) {
        this.hide();
        return;
      }
      (_b = this.tippy) === null || _b === void 0 ? void 0 : _b.setProps({
        getReferenceClientRect: ((_c = this.tippyOptions) === null || _c === void 0 ? void 0 : _c.getReferenceClientRect) || (() => {
          if (isNodeSelection(state.selection)) {
            let node2 = view2.nodeDOM(from2);
            const nodeViewWrapper = node2.dataset.nodeViewWrapper ? node2 : node2.querySelector("[data-node-view-wrapper]");
            if (nodeViewWrapper) {
              node2 = nodeViewWrapper.firstChild;
            }
            if (node2) {
              return node2.getBoundingClientRect();
            }
          }
          return posToDOMRect(view2, from2, to2);
        })
      });
      this.show();
    };
    this.editor = editor;
    this.element = element;
    this.view = view;
    this.updateDelay = updateDelay;
    if (shouldShow) {
      this.shouldShow = shouldShow;
    }
    this.element.addEventListener("mousedown", this.mousedownHandler, { capture: true });
    this.view.dom.addEventListener("dragstart", this.dragstartHandler);
    this.editor.on("focus", this.focusHandler);
    this.editor.on("blur", this.blurHandler);
    this.tippyOptions = tippyOptions;
    this.element.remove();
    this.element.style.visibility = "visible";
  }
  createTooltip() {
    const { element: editorElement } = this.editor.options;
    const editorIsAttached = !!editorElement.parentElement;
    if (this.tippy || !editorIsAttached) {
      return;
    }
    this.tippy = tippy_esm_default(editorElement, {
      duration: 0,
      getReferenceClientRect: null,
      content: this.element,
      interactive: true,
      trigger: "manual",
      placement: "top",
      hideOnClick: "toggle",
      ...this.tippyOptions
    });
    if (this.tippy.popper.firstChild) {
      this.tippy.popper.firstChild.addEventListener("blur", this.tippyBlurHandler);
    }
  }
  update(view, oldState) {
    const { state } = view;
    const hasValidSelection = state.selection.$from.pos !== state.selection.$to.pos;
    if (this.updateDelay > 0 && hasValidSelection) {
      this.handleDebouncedUpdate(view, oldState);
      return;
    }
    const selectionChanged = !(oldState === null || oldState === void 0 ? void 0 : oldState.selection.eq(view.state.selection));
    const docChanged = !(oldState === null || oldState === void 0 ? void 0 : oldState.doc.eq(view.state.doc));
    this.updateHandler(view, selectionChanged, docChanged, oldState);
  }
  show() {
    var _a;
    (_a = this.tippy) === null || _a === void 0 ? void 0 : _a.show();
  }
  hide() {
    var _a;
    (_a = this.tippy) === null || _a === void 0 ? void 0 : _a.hide();
  }
  destroy() {
    var _a, _b;
    if ((_a = this.tippy) === null || _a === void 0 ? void 0 : _a.popper.firstChild) {
      this.tippy.popper.firstChild.removeEventListener("blur", this.tippyBlurHandler);
    }
    (_b = this.tippy) === null || _b === void 0 ? void 0 : _b.destroy();
    this.element.removeEventListener("mousedown", this.mousedownHandler, { capture: true });
    this.view.dom.removeEventListener("dragstart", this.dragstartHandler);
    this.editor.off("focus", this.focusHandler);
    this.editor.off("blur", this.blurHandler);
  }
};
var BubbleMenuPlugin = (options) => {
  return new Plugin({
    key: typeof options.pluginKey === "string" ? new PluginKey(options.pluginKey) : options.pluginKey,
    view: (view) => new BubbleMenuView({ view, ...options })
  });
};
var BubbleMenu = Extension.create({
  name: "bubbleMenu",
  addOptions() {
    return {
      element: null,
      tippyOptions: {},
      pluginKey: "bubbleMenu",
      updateDelay: void 0,
      shouldShow: null
    };
  },
  addProseMirrorPlugins() {
    if (!this.options.element) {
      return [];
    }
    return [
      BubbleMenuPlugin({
        pluginKey: this.options.pluginKey,
        editor: this.editor,
        element: this.options.element,
        tippyOptions: this.options.tippyOptions,
        updateDelay: this.options.updateDelay,
        shouldShow: this.options.shouldShow
      })
    ];
  }
});

// node_modules/@tiptap/react/dist/index.js
var import_react289 = __toESM(require_react());
var import_react_dom8 = __toESM(require_react_dom());

// node_modules/@tiptap/extension-floating-menu/dist/index.js
var FloatingMenuView = class {
  constructor({ editor, element, view, tippyOptions = {}, shouldShow }) {
    this.preventHide = false;
    this.shouldShow = ({ view: view2, state }) => {
      const { selection } = state;
      const { $anchor, empty } = selection;
      const isRootDepth = $anchor.depth === 1;
      const isEmptyTextBlock = $anchor.parent.isTextblock && !$anchor.parent.type.spec.code && !$anchor.parent.textContent;
      if (!view2.hasFocus() || !empty || !isRootDepth || !isEmptyTextBlock || !this.editor.isEditable) {
        return false;
      }
      return true;
    };
    this.mousedownHandler = () => {
      this.preventHide = true;
    };
    this.focusHandler = () => {
      setTimeout(() => this.update(this.editor.view));
    };
    this.blurHandler = ({ event }) => {
      var _a;
      if (this.preventHide) {
        this.preventHide = false;
        return;
      }
      if ((event === null || event === void 0 ? void 0 : event.relatedTarget) && ((_a = this.element.parentNode) === null || _a === void 0 ? void 0 : _a.contains(event.relatedTarget))) {
        return;
      }
      this.hide();
    };
    this.tippyBlurHandler = (event) => {
      this.blurHandler({ event });
    };
    this.editor = editor;
    this.element = element;
    this.view = view;
    if (shouldShow) {
      this.shouldShow = shouldShow;
    }
    this.element.addEventListener("mousedown", this.mousedownHandler, { capture: true });
    this.editor.on("focus", this.focusHandler);
    this.editor.on("blur", this.blurHandler);
    this.tippyOptions = tippyOptions;
    this.element.remove();
    this.element.style.visibility = "visible";
  }
  createTooltip() {
    const { element: editorElement } = this.editor.options;
    const editorIsAttached = !!editorElement.parentElement;
    if (this.tippy || !editorIsAttached) {
      return;
    }
    this.tippy = tippy_esm_default(editorElement, {
      duration: 0,
      getReferenceClientRect: null,
      content: this.element,
      interactive: true,
      trigger: "manual",
      placement: "right",
      hideOnClick: "toggle",
      ...this.tippyOptions
    });
    if (this.tippy.popper.firstChild) {
      this.tippy.popper.firstChild.addEventListener("blur", this.tippyBlurHandler);
    }
  }
  update(view, oldState) {
    var _a, _b, _c;
    const { state } = view;
    const { doc, selection } = state;
    const { from: from2, to: to2 } = selection;
    const isSame = oldState && oldState.doc.eq(doc) && oldState.selection.eq(selection);
    if (isSame) {
      return;
    }
    this.createTooltip();
    const shouldShow = (_a = this.shouldShow) === null || _a === void 0 ? void 0 : _a.call(this, {
      editor: this.editor,
      view,
      state,
      oldState
    });
    if (!shouldShow) {
      this.hide();
      return;
    }
    (_b = this.tippy) === null || _b === void 0 ? void 0 : _b.setProps({
      getReferenceClientRect: ((_c = this.tippyOptions) === null || _c === void 0 ? void 0 : _c.getReferenceClientRect) || (() => posToDOMRect(view, from2, to2))
    });
    this.show();
  }
  show() {
    var _a;
    (_a = this.tippy) === null || _a === void 0 ? void 0 : _a.show();
  }
  hide() {
    var _a;
    (_a = this.tippy) === null || _a === void 0 ? void 0 : _a.hide();
  }
  destroy() {
    var _a, _b;
    if ((_a = this.tippy) === null || _a === void 0 ? void 0 : _a.popper.firstChild) {
      this.tippy.popper.firstChild.removeEventListener("blur", this.tippyBlurHandler);
    }
    (_b = this.tippy) === null || _b === void 0 ? void 0 : _b.destroy();
    this.element.removeEventListener("mousedown", this.mousedownHandler, { capture: true });
    this.editor.off("focus", this.focusHandler);
    this.editor.off("blur", this.blurHandler);
  }
};
var FloatingMenuPlugin = (options) => {
  return new Plugin({
    key: typeof options.pluginKey === "string" ? new PluginKey(options.pluginKey) : options.pluginKey,
    view: (view) => new FloatingMenuView({ view, ...options })
  });
};
var FloatingMenu = Extension.create({
  name: "floatingMenu",
  addOptions() {
    return {
      element: null,
      tippyOptions: {},
      pluginKey: "floatingMenu",
      shouldShow: null
    };
  },
  addProseMirrorPlugins() {
    if (!this.options.element) {
      return [];
    }
    return [
      FloatingMenuPlugin({
        pluginKey: this.options.pluginKey,
        editor: this.editor,
        element: this.options.element,
        tippyOptions: this.options.tippyOptions,
        shouldShow: this.options.shouldShow
      })
    ];
  }
});

// node_modules/@tiptap/react/dist/index.js
var Portals = ({ renderers }) => {
  return import_react289.default.createElement(import_react289.default.Fragment, null, Object.entries(renderers).map(([key, renderer]) => {
    return import_react_dom8.default.createPortal(renderer.reactElement, renderer.element, key);
  }));
};
var PureEditorContent = class extends import_react289.default.Component {
  constructor(props) {
    super(props);
    this.editorContentRef = import_react289.default.createRef();
    this.initialized = false;
    this.state = {
      renderers: {}
    };
  }
  componentDidMount() {
    this.init();
  }
  componentDidUpdate() {
    this.init();
  }
  init() {
    const { editor } = this.props;
    if (editor && editor.options.element) {
      if (editor.contentComponent) {
        return;
      }
      const element = this.editorContentRef.current;
      element.append(...editor.options.element.childNodes);
      editor.setOptions({
        element
      });
      editor.contentComponent = this;
      editor.createNodeViews();
      this.initialized = true;
    }
  }
  maybeFlushSync(fn5) {
    if (this.initialized) {
      (0, import_react_dom8.flushSync)(fn5);
    } else {
      fn5();
    }
  }
  setRenderer(id, renderer) {
    this.maybeFlushSync(() => {
      this.setState(({ renderers }) => ({
        renderers: {
          ...renderers,
          [id]: renderer
        }
      }));
    });
  }
  removeRenderer(id) {
    this.maybeFlushSync(() => {
      this.setState(({ renderers }) => {
        const nextRenderers = { ...renderers };
        delete nextRenderers[id];
        return { renderers: nextRenderers };
      });
    });
  }
  componentWillUnmount() {
    const { editor } = this.props;
    if (!editor) {
      return;
    }
    this.initialized = false;
    if (!editor.isDestroyed) {
      editor.view.setProps({
        nodeViews: {}
      });
    }
    editor.contentComponent = null;
    if (!editor.options.element.firstChild) {
      return;
    }
    const newElement = document.createElement("div");
    newElement.append(...editor.options.element.childNodes);
    editor.setOptions({
      element: newElement
    });
  }
  render() {
    const { editor, ...rest } = this.props;
    return import_react289.default.createElement(
      import_react289.default.Fragment,
      null,
      import_react289.default.createElement("div", { ref: this.editorContentRef, ...rest }),
      import_react289.default.createElement(Portals, { renderers: this.state.renderers })
    );
  }
};
var EditorContentWithKey = (props) => {
  const key = import_react289.default.useMemo(() => {
    return Math.floor(Math.random() * 4294967295).toString();
  }, [props.editor]);
  return import_react289.default.createElement(PureEditorContent, { key, ...props });
};
var EditorContent = import_react289.default.memo(EditorContentWithKey);
var EditorContext = (0, import_react289.createContext)({
  editor: null
});
var EditorConsumer = EditorContext.Consumer;
var ReactNodeViewContext = (0, import_react289.createContext)({
  onDragStart: void 0
});
var useReactNodeView = () => (0, import_react289.useContext)(ReactNodeViewContext);
var NodeViewContent = (props) => {
  const Tag = props.as || "div";
  const { nodeViewContentRef } = useReactNodeView();
  return import_react289.default.createElement(Tag, { ...props, ref: nodeViewContentRef, "data-node-view-content": "", style: {
    whiteSpace: "pre-wrap",
    ...props.style
  } });
};
var NodeViewWrapper = import_react289.default.forwardRef((props, ref) => {
  const { onDragStart } = useReactNodeView();
  const Tag = props.as || "div";
  return import_react289.default.createElement(Tag, { ...props, ref, "data-node-view-wrapper": "", onDragStart, style: {
    whiteSpace: "normal",
    ...props.style
  } });
});
function isClassComponent(Component) {
  return !!(typeof Component === "function" && Component.prototype && Component.prototype.isReactComponent);
}
function isForwardRefComponent(Component) {
  var _a;
  return !!(typeof Component === "object" && ((_a = Component.$$typeof) === null || _a === void 0 ? void 0 : _a.toString()) === "Symbol(react.forward_ref)");
}
var ReactRenderer = class {
  constructor(component, { editor, props = {}, as = "div", className = "", attrs }) {
    this.ref = null;
    this.id = Math.floor(Math.random() * 4294967295).toString();
    this.component = component;
    this.editor = editor;
    this.props = props;
    this.element = document.createElement(as);
    this.element.classList.add("react-renderer");
    if (className) {
      this.element.classList.add(...className.split(" "));
    }
    if (attrs) {
      Object.keys(attrs).forEach((key) => {
        this.element.setAttribute(key, attrs[key]);
      });
    }
    this.render();
  }
  render() {
    var _a, _b;
    const Component = this.component;
    const props = this.props;
    if (isClassComponent(Component) || isForwardRefComponent(Component)) {
      props.ref = (ref) => {
        this.ref = ref;
      };
    }
    this.reactElement = import_react289.default.createElement(Component, { ...props });
    (_b = (_a = this.editor) === null || _a === void 0 ? void 0 : _a.contentComponent) === null || _b === void 0 ? void 0 : _b.setRenderer(this.id, this);
  }
  updateProps(props = {}) {
    this.props = {
      ...this.props,
      ...props
    };
    this.render();
  }
  destroy() {
    var _a, _b;
    (_b = (_a = this.editor) === null || _a === void 0 ? void 0 : _a.contentComponent) === null || _b === void 0 ? void 0 : _b.removeRenderer(this.id);
  }
};
var ReactNodeView = class extends NodeView {
  mount() {
    const props = {
      editor: this.editor,
      node: this.node,
      decorations: this.decorations,
      selected: false,
      extension: this.extension,
      getPos: () => this.getPos(),
      updateAttributes: (attributes = {}) => this.updateAttributes(attributes),
      deleteNode: () => this.deleteNode()
    };
    if (!this.component.displayName) {
      const capitalizeFirstChar = (string) => {
        return string.charAt(0).toUpperCase() + string.substring(1);
      };
      this.component.displayName = capitalizeFirstChar(this.extension.name);
    }
    const ReactNodeViewProvider = (componentProps) => {
      const Component = this.component;
      const onDragStart = this.onDragStart.bind(this);
      const nodeViewContentRef = (element) => {
        if (element && this.contentDOMElement && element.firstChild !== this.contentDOMElement) {
          element.appendChild(this.contentDOMElement);
        }
      };
      return import_react289.default.createElement(
        import_react289.default.Fragment,
        null,
        import_react289.default.createElement(
          ReactNodeViewContext.Provider,
          { value: { onDragStart, nodeViewContentRef } },
          import_react289.default.createElement(Component, { ...componentProps })
        )
      );
    };
    ReactNodeViewProvider.displayName = "ReactNodeView";
    this.contentDOMElement = this.node.isLeaf ? null : document.createElement(this.node.isInline ? "span" : "div");
    if (this.contentDOMElement) {
      this.contentDOMElement.style.whiteSpace = "inherit";
    }
    let as = this.node.isInline ? "span" : "div";
    if (this.options.as) {
      as = this.options.as;
    }
    const { className = "" } = this.options;
    this.handleSelectionUpdate = this.handleSelectionUpdate.bind(this);
    this.editor.on("selectionUpdate", this.handleSelectionUpdate);
    this.renderer = new ReactRenderer(ReactNodeViewProvider, {
      editor: this.editor,
      props,
      as,
      className: `node-${this.node.type.name} ${className}`.trim(),
      attrs: this.options.attrs
    });
  }
  get dom() {
    var _a;
    if (this.renderer.element.firstElementChild && !((_a = this.renderer.element.firstElementChild) === null || _a === void 0 ? void 0 : _a.hasAttribute("data-node-view-wrapper"))) {
      throw Error("Please use the NodeViewWrapper component for your node view.");
    }
    return this.renderer.element;
  }
  get contentDOM() {
    if (this.node.isLeaf) {
      return null;
    }
    return this.contentDOMElement;
  }
  handleSelectionUpdate() {
    const { from: from2, to: to2 } = this.editor.state.selection;
    if (from2 <= this.getPos() && to2 >= this.getPos() + this.node.nodeSize) {
      this.selectNode();
    } else {
      this.deselectNode();
    }
  }
  update(node2, decorations) {
    const updateProps = (props) => {
      this.renderer.updateProps(props);
    };
    if (node2.type !== this.node.type) {
      return false;
    }
    if (typeof this.options.update === "function") {
      const oldNode = this.node;
      const oldDecorations = this.decorations;
      this.node = node2;
      this.decorations = decorations;
      return this.options.update({
        oldNode,
        oldDecorations,
        newNode: node2,
        newDecorations: decorations,
        updateProps: () => updateProps({ node: node2, decorations })
      });
    }
    if (node2 === this.node && this.decorations === decorations) {
      return true;
    }
    this.node = node2;
    this.decorations = decorations;
    updateProps({ node: node2, decorations });
    return true;
  }
  selectNode() {
    this.renderer.updateProps({
      selected: true
    });
    this.renderer.element.classList.add("ProseMirror-selectednode");
  }
  deselectNode() {
    this.renderer.updateProps({
      selected: false
    });
    this.renderer.element.classList.remove("ProseMirror-selectednode");
  }
  destroy() {
    this.renderer.destroy();
    this.editor.off("selectionUpdate", this.handleSelectionUpdate);
    this.contentDOMElement = null;
  }
};
function ReactNodeViewRenderer(component, options) {
  return (props) => {
    if (!props.editor.contentComponent) {
      return {};
    }
    return new ReactNodeView(component, props, options);
  };
}

// node_modules/use-prefers-color-scheme/dist/index.esm.js
var n = __toESM(require_react());
var a = () => {
  let [c, i] = n.useState(() => {
    if (typeof window > "u")
      return "no-preference";
    let r2 = window.matchMedia("(prefers-color-scheme: dark)"), e = window.matchMedia("(prefers-color-scheme: light)");
    return r2.matches ? "dark" : e.matches ? "light" : "no-preference";
  });
  return n.useEffect(() => {
    if (typeof window.matchMedia != "function")
      return;
    let r2 = window.matchMedia("(prefers-color-scheme: dark)"), e = window.matchMedia("(prefers-color-scheme: light)");
    if (typeof e.addEventListener == "function") {
      let t = ({ matches: o }) => {
        o && i("dark");
      }, s = ({ matches: o }) => {
        o && i("light");
      };
      return r2.addEventListener("change", t), e.addEventListener("change", s), () => {
        r2.removeEventListener("change", t), e.removeEventListener("change", s);
      };
    }
    if (typeof e.addListener == "function") {
      let t = () => i(r2.matches ? "dark" : e.matches ? "light" : "no-preference");
      return r2.addListener(t), e.addListener(t), () => {
        r2.removeListener(t), e.removeListener(t);
      };
    }
  }, []), c;
};
var d = a;

// node_modules/@blocknote/react/dist/blocknote-react.js
var it = __toESM(require_lodash());
var import_lodash = __toESM(require_lodash());

// node_modules/@tippyjs/react/dist/tippy-react.esm.js
var import_react290 = __toESM(require_react());
var import_react_dom9 = __toESM(require_react_dom());
function _objectWithoutPropertiesLoose2(source, excluded) {
  if (source == null)
    return {};
  var target = {};
  var sourceKeys = Object.keys(source);
  var key, i;
  for (i = 0; i < sourceKeys.length; i++) {
    key = sourceKeys[i];
    if (excluded.indexOf(key) >= 0)
      continue;
    target[key] = source[key];
  }
  return target;
}
var isBrowser4 = typeof window !== "undefined" && typeof document !== "undefined";
function preserveRef(ref, node2) {
  if (ref) {
    if (typeof ref === "function") {
      ref(node2);
    }
    if ({}.hasOwnProperty.call(ref, "current")) {
      ref.current = node2;
    }
  }
}
function ssrSafeCreateDiv() {
  return isBrowser4 && document.createElement("div");
}
function toDataAttributes(attrs) {
  var dataAttrs = {
    "data-placement": attrs.placement
  };
  if (attrs.referenceHidden) {
    dataAttrs["data-reference-hidden"] = "";
  }
  if (attrs.escaped) {
    dataAttrs["data-escaped"] = "";
  }
  return dataAttrs;
}
function deepEqual2(x, y) {
  if (x === y) {
    return true;
  } else if (typeof x === "object" && x != null && typeof y === "object" && y != null) {
    if (Object.keys(x).length !== Object.keys(y).length) {
      return false;
    }
    for (var prop in x) {
      if (y.hasOwnProperty(prop)) {
        if (!deepEqual2(x[prop], y[prop])) {
          return false;
        }
      } else {
        return false;
      }
    }
    return true;
  } else {
    return false;
  }
}
function uniqueByShape(arr) {
  var output = [];
  arr.forEach(function(item) {
    if (!output.find(function(outputItem) {
      return deepEqual2(item, outputItem);
    })) {
      output.push(item);
    }
  });
  return output;
}
function deepPreserveProps(instanceProps, componentProps) {
  var _instanceProps$popper, _componentProps$poppe;
  return Object.assign({}, componentProps, {
    popperOptions: Object.assign({}, instanceProps.popperOptions, componentProps.popperOptions, {
      modifiers: uniqueByShape([].concat(((_instanceProps$popper = instanceProps.popperOptions) == null ? void 0 : _instanceProps$popper.modifiers) || [], ((_componentProps$poppe = componentProps.popperOptions) == null ? void 0 : _componentProps$poppe.modifiers) || []))
    })
  });
}
var useIsomorphicLayoutEffect = isBrowser4 ? import_react290.useLayoutEffect : import_react290.useEffect;
function useMutableBox(initialValue) {
  var ref = (0, import_react290.useRef)();
  if (!ref.current) {
    ref.current = typeof initialValue === "function" ? initialValue() : initialValue;
  }
  return ref.current;
}
function updateClassName(box, action, classNames) {
  classNames.split(/\s+/).forEach(function(name) {
    if (name) {
      box.classList[action](name);
    }
  });
}
var classNamePlugin = {
  name: "className",
  defaultValue: "",
  fn: function fn3(instance) {
    var box = instance.popper.firstElementChild;
    var isDefaultRenderFn = function isDefaultRenderFn2() {
      var _instance$props$rende;
      return !!((_instance$props$rende = instance.props.render) == null ? void 0 : _instance$props$rende.$$tippy);
    };
    function add() {
      if (instance.props.className && !isDefaultRenderFn()) {
        if (true) {
          console.warn(["@tippyjs/react: Cannot use `className` prop in conjunction with", "`render` prop. Place the className on the element you are", "rendering."].join(" "));
        }
        return;
      }
      updateClassName(box, "add", instance.props.className);
    }
    function remove() {
      if (isDefaultRenderFn()) {
        updateClassName(box, "remove", instance.props.className);
      }
    }
    return {
      onCreate: add,
      onBeforeUpdate: remove,
      onAfterUpdate: add
    };
  }
};
function TippyGenerator(tippy2) {
  function Tippy(_ref) {
    var children = _ref.children, content = _ref.content, visible2 = _ref.visible, singleton = _ref.singleton, render2 = _ref.render, reference2 = _ref.reference, _ref$disabled = _ref.disabled, disabled = _ref$disabled === void 0 ? false : _ref$disabled, _ref$ignoreAttributes = _ref.ignoreAttributes, ignoreAttributes = _ref$ignoreAttributes === void 0 ? true : _ref$ignoreAttributes, __source = _ref.__source, __self = _ref.__self, restOfNativeProps = _objectWithoutPropertiesLoose2(_ref, ["children", "content", "visible", "singleton", "render", "reference", "disabled", "ignoreAttributes", "__source", "__self"]);
    var isControlledMode = visible2 !== void 0;
    var isSingletonMode = singleton !== void 0;
    var _useState = (0, import_react290.useState)(false), mounted = _useState[0], setMounted = _useState[1];
    var _useState2 = (0, import_react290.useState)({}), attrs = _useState2[0], setAttrs = _useState2[1];
    var _useState3 = (0, import_react290.useState)(), singletonContent = _useState3[0], setSingletonContent = _useState3[1];
    var mutableBox = useMutableBox(function() {
      return {
        container: ssrSafeCreateDiv(),
        renders: 1
      };
    });
    var props = Object.assign({
      ignoreAttributes
    }, restOfNativeProps, {
      content: mutableBox.container
    });
    if (isControlledMode) {
      if (true) {
        ["trigger", "hideOnClick", "showOnCreate"].forEach(function(nativeStateProp) {
          if (props[nativeStateProp] !== void 0) {
            console.warn(["@tippyjs/react: Cannot specify `" + nativeStateProp + "` prop in", "controlled mode (`visible` prop)"].join(" "));
          }
        });
      }
      props.trigger = "manual";
      props.hideOnClick = false;
    }
    if (isSingletonMode) {
      disabled = true;
    }
    var computedProps = props;
    var plugins = props.plugins || [];
    if (render2) {
      computedProps = Object.assign({}, props, {
        plugins: isSingletonMode && singleton.data != null ? [].concat(plugins, [{
          fn: function fn5() {
            return {
              onTrigger: function onTrigger2(instance, event) {
                var node2 = singleton.data.children.find(function(_ref2) {
                  var instance2 = _ref2.instance;
                  return instance2.reference === event.currentTarget;
                });
                instance.state.$$activeSingletonInstance = node2.instance;
                setSingletonContent(node2.content);
              }
            };
          }
        }]) : plugins,
        render: function render3() {
          return {
            popper: mutableBox.container
          };
        }
      });
    }
    var deps = [reference2].concat(children ? [children.type] : []);
    useIsomorphicLayoutEffect(function() {
      var element = reference2;
      if (reference2 && reference2.hasOwnProperty("current")) {
        element = reference2.current;
      }
      var instance = tippy2(element || mutableBox.ref || ssrSafeCreateDiv(), Object.assign({}, computedProps, {
        plugins: [classNamePlugin].concat(props.plugins || [])
      }));
      mutableBox.instance = instance;
      if (disabled) {
        instance.disable();
      }
      if (visible2) {
        instance.show();
      }
      if (isSingletonMode) {
        singleton.hook({
          instance,
          content,
          props: computedProps,
          setSingletonContent
        });
      }
      setMounted(true);
      return function() {
        instance.destroy();
        singleton == null ? void 0 : singleton.cleanup(instance);
      };
    }, deps);
    useIsomorphicLayoutEffect(function() {
      var _instance$popperInsta;
      if (mutableBox.renders === 1) {
        mutableBox.renders++;
        return;
      }
      var instance = mutableBox.instance;
      instance.setProps(deepPreserveProps(instance.props, computedProps));
      (_instance$popperInsta = instance.popperInstance) == null ? void 0 : _instance$popperInsta.forceUpdate();
      if (disabled) {
        instance.disable();
      } else {
        instance.enable();
      }
      if (isControlledMode) {
        if (visible2) {
          instance.show();
        } else {
          instance.hide();
        }
      }
      if (isSingletonMode) {
        singleton.hook({
          instance,
          content,
          props: computedProps,
          setSingletonContent
        });
      }
    });
    useIsomorphicLayoutEffect(function() {
      var _instance$props$poppe;
      if (!render2) {
        return;
      }
      var instance = mutableBox.instance;
      instance.setProps({
        popperOptions: Object.assign({}, instance.props.popperOptions, {
          modifiers: [].concat((((_instance$props$poppe = instance.props.popperOptions) == null ? void 0 : _instance$props$poppe.modifiers) || []).filter(function(_ref3) {
            var name = _ref3.name;
            return name !== "$$tippyReact";
          }), [{
            name: "$$tippyReact",
            enabled: true,
            phase: "beforeWrite",
            requires: ["computeStyles"],
            fn: function fn5(_ref4) {
              var _state$modifiersData;
              var state = _ref4.state;
              var hideData = (_state$modifiersData = state.modifiersData) == null ? void 0 : _state$modifiersData.hide;
              if (attrs.placement !== state.placement || attrs.referenceHidden !== (hideData == null ? void 0 : hideData.isReferenceHidden) || attrs.escaped !== (hideData == null ? void 0 : hideData.hasPopperEscaped)) {
                setAttrs({
                  placement: state.placement,
                  referenceHidden: hideData == null ? void 0 : hideData.isReferenceHidden,
                  escaped: hideData == null ? void 0 : hideData.hasPopperEscaped
                });
              }
              state.attributes.popper = {};
            }
          }])
        })
      });
    }, [attrs.placement, attrs.referenceHidden, attrs.escaped].concat(deps));
    return import_react290.default.createElement(import_react290.default.Fragment, null, children ? (0, import_react290.cloneElement)(children, {
      ref: function ref(node2) {
        mutableBox.ref = node2;
        preserveRef(children.ref, node2);
      }
    }) : null, mounted && (0, import_react_dom9.createPortal)(render2 ? render2(toDataAttributes(attrs), singletonContent, mutableBox.instance) : content, mutableBox.container));
  }
  return Tippy;
}
function useSingletonGenerator(createSingleton3) {
  return function useSingleton2(_temp) {
    var _ref = _temp === void 0 ? {} : _temp, _ref$disabled = _ref.disabled, disabled = _ref$disabled === void 0 ? false : _ref$disabled, _ref$overrides = _ref.overrides, overrides = _ref$overrides === void 0 ? [] : _ref$overrides;
    var _useState = (0, import_react290.useState)(false), mounted = _useState[0], setMounted = _useState[1];
    var mutableBox = useMutableBox({
      children: [],
      renders: 1
    });
    useIsomorphicLayoutEffect(function() {
      if (!mounted) {
        setMounted(true);
        return;
      }
      var children = mutableBox.children, sourceData = mutableBox.sourceData;
      if (!sourceData) {
        if (true) {
          console.error(["@tippyjs/react: The `source` variable from `useSingleton()` has", "not been passed to a <Tippy /> component."].join(" "));
        }
        return;
      }
      var instance = createSingleton3(children.map(function(child) {
        return child.instance;
      }), Object.assign({}, sourceData.props, {
        popperOptions: sourceData.instance.props.popperOptions,
        overrides,
        plugins: [classNamePlugin].concat(sourceData.props.plugins || [])
      }));
      mutableBox.instance = instance;
      if (disabled) {
        instance.disable();
      }
      return function() {
        instance.destroy();
        mutableBox.children = children.filter(function(_ref2) {
          var instance2 = _ref2.instance;
          return !instance2.state.isDestroyed;
        });
      };
    }, [mounted]);
    useIsomorphicLayoutEffect(function() {
      if (!mounted) {
        return;
      }
      if (mutableBox.renders === 1) {
        mutableBox.renders++;
        return;
      }
      var children = mutableBox.children, instance = mutableBox.instance, sourceData = mutableBox.sourceData;
      if (!(instance && sourceData)) {
        return;
      }
      var _sourceData$props = sourceData.props, content = _sourceData$props.content, props = _objectWithoutPropertiesLoose2(_sourceData$props, ["content"]);
      instance.setProps(deepPreserveProps(instance.props, Object.assign({}, props, {
        overrides
      })));
      instance.setInstances(children.map(function(child) {
        return child.instance;
      }));
      if (disabled) {
        instance.disable();
      } else {
        instance.enable();
      }
    });
    return (0, import_react290.useMemo)(function() {
      var source = {
        data: mutableBox,
        hook: function hook(data) {
          mutableBox.sourceData = data;
          mutableBox.setSingletonContent = data.setSingletonContent;
        },
        cleanup: function cleanup() {
          mutableBox.sourceData = null;
        }
      };
      var target = {
        hook: function hook(data) {
          var _mutableBox$instance, _mutableBox$instance2;
          mutableBox.children = mutableBox.children.filter(function(_ref3) {
            var instance = _ref3.instance;
            return data.instance !== instance;
          });
          mutableBox.children.push(data);
          if (((_mutableBox$instance = mutableBox.instance) == null ? void 0 : _mutableBox$instance.state.isMounted) && ((_mutableBox$instance2 = mutableBox.instance) == null ? void 0 : _mutableBox$instance2.state.$$activeSingletonInstance) === data.instance) {
            mutableBox.setSingletonContent == null ? void 0 : mutableBox.setSingletonContent(data.content);
          }
          if (mutableBox.instance && !mutableBox.instance.state.isDestroyed) {
            mutableBox.instance.setInstances(mutableBox.children.map(function(child) {
              return child.instance;
            }));
          }
        },
        cleanup: function cleanup(instance) {
          mutableBox.children = mutableBox.children.filter(function(data) {
            return data.instance !== instance;
          });
          if (mutableBox.instance && !mutableBox.instance.state.isDestroyed) {
            mutableBox.instance.setInstances(mutableBox.children.map(function(child) {
              return child.instance;
            }));
          }
        }
      };
      return [source, target];
    }, []);
  };
}
var forwardRef128 = function(Tippy, defaultProps118) {
  return (0, import_react290.forwardRef)(function TippyWrapper(_ref, _ref2) {
    var children = _ref.children, props = _objectWithoutPropertiesLoose2(_ref, ["children"]);
    return (
      // If I spread them separately here, Babel adds the _extends ponyfill for
      // some reason
      import_react290.default.createElement(Tippy, Object.assign({}, defaultProps118, props), children ? (0, import_react290.cloneElement)(children, {
        ref: function ref(node2) {
          preserveRef(_ref2, node2);
          preserveRef(children.ref, node2);
        }
      }) : null)
    );
  });
};
var useSingleton = useSingletonGenerator(createSingleton);
var index5 = forwardRef128(TippyGenerator(tippy_esm_default));
var tippy_react_esm_default = index5;

// node_modules/@blocknote/react/dist/blocknote-react.js
var Me = { exports: {} };
var ne = {};
var lt;
function Ir() {
  return lt || (lt = 1, function() {
    var e = import_react291.default, n2 = Symbol.for("react.element"), o = Symbol.for("react.portal"), i = Symbol.for("react.fragment"), s = Symbol.for("react.strict_mode"), c = Symbol.for("react.profiler"), d2 = Symbol.for("react.provider"), g = Symbol.for("react.context"), a2 = Symbol.for("react.forward_ref"), y = Symbol.for("react.suspense"), x = Symbol.for("react.suspense_list"), p = Symbol.for("react.memo"), S2 = Symbol.for("react.lazy"), T = Symbol.for("react.offscreen"), D = Symbol.iterator, le = "@@iterator";
    function Bt(t) {
      if (t === null || typeof t != "object")
        return null;
      var l = D && t[D] || t[le];
      return typeof l == "function" ? l : null;
    }
    var G = e.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED;
    function H(t) {
      {
        for (var l = arguments.length, u = new Array(l > 1 ? l - 1 : 0), f = 1; f < l; f++)
          u[f - 1] = arguments[f];
        _t("error", t, u);
      }
    }
    function _t(t, l, u) {
      {
        var f = G.ReactDebugCurrentFrame, m = f.getStackAddendum();
        m !== "" && (l += "%s", u = u.concat([m]));
        var C = u.map(function(v) {
          return String(v);
        });
        C.unshift("Warning: " + l), Function.prototype.apply.call(console[t], console, C);
      }
    }
    var Dt = false, Pt = false, Ot = false, Ft = false, At = false, De;
    De = Symbol.for("react.module.reference");
    function Lt(t) {
      return !!(typeof t == "string" || typeof t == "function" || t === i || t === c || At || t === s || t === y || t === x || Ft || t === T || Dt || Pt || Ot || typeof t == "object" && t !== null && (t.$$typeof === S2 || t.$$typeof === p || t.$$typeof === d2 || t.$$typeof === g || t.$$typeof === a2 || // This needs to include all possible module reference object
      // types supported by any Flight configuration anywhere since
      // we don't know which Flight build this will end up being used
      // with.
      t.$$typeof === De || t.getModuleId !== void 0));
    }
    function Vt(t, l, u) {
      var f = t.displayName;
      if (f)
        return f;
      var m = l.displayName || l.name || "";
      return m !== "" ? u + "(" + m + ")" : u;
    }
    function Pe(t) {
      return t.displayName || "Context";
    }
    function L(t) {
      if (t == null)
        return null;
      if (typeof t.tag == "number" && H("Received an unexpected object in getComponentNameFromType(). This is likely a bug in React. Please file an issue."), typeof t == "function")
        return t.displayName || t.name || null;
      if (typeof t == "string")
        return t;
      switch (t) {
        case i:
          return "Fragment";
        case o:
          return "Portal";
        case c:
          return "Profiler";
        case s:
          return "StrictMode";
        case y:
          return "Suspense";
        case x:
          return "SuspenseList";
      }
      if (typeof t == "object")
        switch (t.$$typeof) {
          case g:
            var l = t;
            return Pe(l) + ".Consumer";
          case d2:
            var u = t;
            return Pe(u._context) + ".Provider";
          case a2:
            return Vt(t, t.render, "ForwardRef");
          case p:
            var f = t.displayName || null;
            return f !== null ? f : L(t.type) || "Memo";
          case S2: {
            var m = t, C = m._payload, v = m._init;
            try {
              return L(v(C));
            } catch {
              return null;
            }
          }
        }
      return null;
    }
    var K = Object.assign, ee = 0, Oe, Fe, Ae, Le, Ve, Ue, Ne;
    function $e() {
    }
    $e.__reactDisabledLog = true;
    function Ut() {
      {
        if (ee === 0) {
          Oe = console.log, Fe = console.info, Ae = console.warn, Le = console.error, Ve = console.group, Ue = console.groupCollapsed, Ne = console.groupEnd;
          var t = {
            configurable: true,
            enumerable: true,
            value: $e,
            writable: true
          };
          Object.defineProperties(console, {
            info: t,
            log: t,
            warn: t,
            error: t,
            group: t,
            groupCollapsed: t,
            groupEnd: t
          });
        }
        ee++;
      }
    }
    function Nt() {
      {
        if (ee--, ee === 0) {
          var t = {
            configurable: true,
            enumerable: true,
            writable: true
          };
          Object.defineProperties(console, {
            log: K({}, t, {
              value: Oe
            }),
            info: K({}, t, {
              value: Fe
            }),
            warn: K({}, t, {
              value: Ae
            }),
            error: K({}, t, {
              value: Le
            }),
            group: K({}, t, {
              value: Ve
            }),
            groupCollapsed: K({}, t, {
              value: Ue
            }),
            groupEnd: K({}, t, {
              value: Ne
            })
          });
        }
        ee < 0 && H("disabledDepth fell below zero. This is a bug in React. Please file an issue.");
      }
    }
    var me = G.ReactCurrentDispatcher, ke;
    function ce(t, l, u) {
      {
        if (ke === void 0)
          try {
            throw Error();
          } catch (m) {
            var f = m.stack.trim().match(/\n( *(at )?)/);
            ke = f && f[1] || "";
          }
        return `
` + ke + t;
      }
    }
    var ye = false, se;
    {
      var $t = typeof WeakMap == "function" ? WeakMap : Map;
      se = new $t();
    }
    function We(t, l) {
      if (!t || ye)
        return "";
      {
        var u = se.get(t);
        if (u !== void 0)
          return u;
      }
      var f;
      ye = true;
      var m = Error.prepareStackTrace;
      Error.prepareStackTrace = void 0;
      var C;
      C = me.current, me.current = null, Ut();
      try {
        if (l) {
          var v = function() {
            throw Error();
          };
          if (Object.defineProperty(v.prototype, "props", {
            set: function() {
              throw Error();
            }
          }), typeof Reflect == "object" && Reflect.construct) {
            try {
              Reflect.construct(v, []);
            } catch (V) {
              f = V;
            }
            Reflect.construct(t, [], v);
          } else {
            try {
              v.call();
            } catch (V) {
              f = V;
            }
            t.call(v.prototype);
          }
        } else {
          try {
            throw Error();
          } catch (V) {
            f = V;
          }
          t();
        }
      } catch (V) {
        if (V && f && typeof V.stack == "string") {
          for (var h = V.stack.split(`
`), I = f.stack.split(`
`), M = h.length - 1, E = I.length - 1; M >= 1 && E >= 0 && h[M] !== I[E]; )
            E--;
          for (; M >= 1 && E >= 0; M--, E--)
            if (h[M] !== I[E]) {
              if (M !== 1 || E !== 1)
                do
                  if (M--, E--, E < 0 || h[M] !== I[E]) {
                    var _ = `
` + h[M].replace(" at new ", " at ");
                    return t.displayName && _.includes("<anonymous>") && (_ = _.replace("<anonymous>", t.displayName)), typeof t == "function" && se.set(t, _), _;
                  }
                while (M >= 1 && E >= 0);
              break;
            }
        }
      } finally {
        ye = false, me.current = C, Nt(), Error.prepareStackTrace = m;
      }
      var X = t ? t.displayName || t.name : "", ot = X ? ce(X) : "";
      return typeof t == "function" && se.set(t, ot), ot;
    }
    function Wt(t, l, u) {
      return We(t, false);
    }
    function Yt(t) {
      var l = t.prototype;
      return !!(l && l.isReactComponent);
    }
    function de(t, l, u) {
      if (t == null)
        return "";
      if (typeof t == "function")
        return We(t, Yt(t));
      if (typeof t == "string")
        return ce(t);
      switch (t) {
        case y:
          return ce("Suspense");
        case x:
          return ce("SuspenseList");
      }
      if (typeof t == "object")
        switch (t.$$typeof) {
          case a2:
            return Wt(t.render);
          case p:
            return de(t.type, l, u);
          case S2: {
            var f = t, m = f._payload, C = f._init;
            try {
              return de(C(m), l, u);
            } catch {
            }
          }
        }
      return "";
    }
    var ue = Object.prototype.hasOwnProperty, Ye = {}, qe = G.ReactDebugCurrentFrame;
    function ge(t) {
      if (t) {
        var l = t._owner, u = de(t.type, t._source, l ? l.type : null);
        qe.setExtraStackFrame(u);
      } else
        qe.setExtraStackFrame(null);
    }
    function qt(t, l, u, f, m) {
      {
        var C = Function.call.bind(ue);
        for (var v in t)
          if (C(t, v)) {
            var h = void 0;
            try {
              if (typeof t[v] != "function") {
                var I = Error((f || "React class") + ": " + u + " type `" + v + "` is invalid; it must be a function, usually from the `prop-types` package, but received `" + typeof t[v] + "`.This often happens because of typos such as `PropTypes.function` instead of `PropTypes.func`.");
                throw I.name = "Invariant Violation", I;
              }
              h = t[v](l, v, f, u, null, "SECRET_DO_NOT_PASS_THIS_OR_YOU_WILL_BE_FIRED");
            } catch (M) {
              h = M;
            }
            h && !(h instanceof Error) && (ge(m), H("%s: type specification of %s `%s` is invalid; the type checker function must return `null` or an `Error` but returned a %s. You may have forgotten to pass an argument to the type checker creator (arrayOf, instanceOf, objectOf, oneOf, oneOfType, and shape all require an argument).", f || "React class", u, v, typeof h), ge(null)), h instanceof Error && !(h.message in Ye) && (Ye[h.message] = true, ge(m), H("Failed %s type: %s", u, h.message), ge(null));
          }
      }
    }
    var Kt = Array.isArray;
    function pe(t) {
      return Kt(t);
    }
    function Gt(t) {
      {
        var l = typeof Symbol == "function" && Symbol.toStringTag, u = l && t[Symbol.toStringTag] || t.constructor.name || "Object";
        return u;
      }
    }
    function Jt(t) {
      try {
        return Ke(t), false;
      } catch {
        return true;
      }
    }
    function Ke(t) {
      return "" + t;
    }
    function Ge(t) {
      if (Jt(t))
        return H("The provided key is an unsupported type %s. This value must be coerced to a string before before using it here.", Gt(t)), Ke(t);
    }
    var te = G.ReactCurrentOwner, Xt = {
      key: true,
      ref: true,
      __self: true,
      __source: true
    }, Je, Xe, Ce;
    Ce = {};
    function Zt(t) {
      if (ue.call(t, "ref")) {
        var l = Object.getOwnPropertyDescriptor(t, "ref").get;
        if (l && l.isReactWarning)
          return false;
      }
      return t.ref !== void 0;
    }
    function Qt(t) {
      if (ue.call(t, "key")) {
        var l = Object.getOwnPropertyDescriptor(t, "key").get;
        if (l && l.isReactWarning)
          return false;
      }
      return t.key !== void 0;
    }
    function er(t, l) {
      if (typeof t.ref == "string" && te.current && l && te.current.stateNode !== l) {
        var u = L(te.current.type);
        Ce[u] || (H('Component "%s" contains the string ref "%s". Support for string refs will be removed in a future major release. This case cannot be automatically converted to an arrow function. We ask you to manually fix this case by using useRef() or createRef() instead. Learn more about using refs safely here: https://reactjs.org/link/strict-mode-string-ref', L(te.current.type), t.ref), Ce[u] = true);
      }
    }
    function tr(t, l) {
      {
        var u = function() {
          Je || (Je = true, H("%s: `key` is not a prop. Trying to access it will result in `undefined` being returned. If you need to access the same value within the child component, you should pass it as a different prop. (https://reactjs.org/link/special-props)", l));
        };
        u.isReactWarning = true, Object.defineProperty(t, "key", {
          get: u,
          configurable: true
        });
      }
    }
    function rr(t, l) {
      {
        var u = function() {
          Xe || (Xe = true, H("%s: `ref` is not a prop. Trying to access it will result in `undefined` being returned. If you need to access the same value within the child component, you should pass it as a different prop. (https://reactjs.org/link/special-props)", l));
        };
        u.isReactWarning = true, Object.defineProperty(t, "ref", {
          get: u,
          configurable: true
        });
      }
    }
    var nr = function(t, l, u, f, m, C, v) {
      var h = {
        // This tag allows us to uniquely identify this as a React Element
        $$typeof: n2,
        // Built-in properties that belong on the element
        type: t,
        key: l,
        ref: u,
        props: v,
        // Record the component responsible for creating this element.
        _owner: C
      };
      return h._store = {}, Object.defineProperty(h._store, "validated", {
        configurable: false,
        enumerable: false,
        writable: true,
        value: false
      }), Object.defineProperty(h, "_self", {
        configurable: false,
        enumerable: false,
        writable: false,
        value: f
      }), Object.defineProperty(h, "_source", {
        configurable: false,
        enumerable: false,
        writable: false,
        value: m
      }), Object.freeze && (Object.freeze(h.props), Object.freeze(h)), h;
    };
    function or(t, l, u, f, m) {
      {
        var C, v = {}, h = null, I = null;
        u !== void 0 && (Ge(u), h = "" + u), Qt(l) && (Ge(l.key), h = "" + l.key), Zt(l) && (I = l.ref, er(l, m));
        for (C in l)
          ue.call(l, C) && !Xt.hasOwnProperty(C) && (v[C] = l[C]);
        if (t && t.defaultProps) {
          var M = t.defaultProps;
          for (C in M)
            v[C] === void 0 && (v[C] = M[C]);
        }
        if (h || I) {
          var E = typeof t == "function" ? t.displayName || t.name || "Unknown" : t;
          h && tr(v, E), I && rr(v, E);
        }
        return nr(t, h, I, m, f, te.current, v);
      }
    }
    var je = G.ReactCurrentOwner, Ze = G.ReactDebugCurrentFrame;
    function J(t) {
      if (t) {
        var l = t._owner, u = de(t.type, t._source, l ? l.type : null);
        Ze.setExtraStackFrame(u);
      } else
        Ze.setExtraStackFrame(null);
    }
    var Te;
    Te = false;
    function we(t) {
      return typeof t == "object" && t !== null && t.$$typeof === n2;
    }
    function Qe() {
      {
        if (je.current) {
          var t = L(je.current.type);
          if (t)
            return `

Check the render method of \`` + t + "`.";
        }
        return "";
      }
    }
    function ir(t) {
      {
        if (t !== void 0) {
          var l = t.fileName.replace(/^.*[\\\/]/, ""), u = t.lineNumber;
          return `

Check your code at ` + l + ":" + u + ".";
        }
        return "";
      }
    }
    var et = {};
    function ar(t) {
      {
        var l = Qe();
        if (!l) {
          var u = typeof t == "string" ? t : t.displayName || t.name;
          u && (l = `

Check the top-level render call using <` + u + ">.");
        }
        return l;
      }
    }
    function tt(t, l) {
      {
        if (!t._store || t._store.validated || t.key != null)
          return;
        t._store.validated = true;
        var u = ar(l);
        if (et[u])
          return;
        et[u] = true;
        var f = "";
        t && t._owner && t._owner !== je.current && (f = " It was passed a child from " + L(t._owner.type) + "."), J(t), H('Each child in a list should have a unique "key" prop.%s%s See https://reactjs.org/link/warning-keys for more information.', u, f), J(null);
      }
    }
    function rt(t, l) {
      {
        if (typeof t != "object")
          return;
        if (pe(t))
          for (var u = 0; u < t.length; u++) {
            var f = t[u];
            we(f) && tt(f, l);
          }
        else if (we(t))
          t._store && (t._store.validated = true);
        else if (t) {
          var m = Bt(t);
          if (typeof m == "function" && m !== t.entries)
            for (var C = m.call(t), v; !(v = C.next()).done; )
              we(v.value) && tt(v.value, l);
        }
      }
    }
    function lr(t) {
      {
        var l = t.type;
        if (l == null || typeof l == "string")
          return;
        var u;
        if (typeof l == "function")
          u = l.propTypes;
        else if (typeof l == "object" && (l.$$typeof === a2 || // Note: Memo only checks outer props here.
        // Inner props are checked in the reconciler.
        l.$$typeof === p))
          u = l.propTypes;
        else
          return;
        if (u) {
          var f = L(l);
          qt(u, t.props, "prop", f, t);
        } else if (l.PropTypes !== void 0 && !Te) {
          Te = true;
          var m = L(l);
          H("Component %s declared `PropTypes` instead of `propTypes`. Did you misspell the property assignment?", m || "Unknown");
        }
        typeof l.getDefaultProps == "function" && !l.getDefaultProps.isReactClassApproved && H("getDefaultProps is only used on classic React.createClass definitions. Use a static property named `defaultProps` instead.");
      }
    }
    function cr(t) {
      {
        for (var l = Object.keys(t.props), u = 0; u < l.length; u++) {
          var f = l[u];
          if (f !== "children" && f !== "key") {
            J(t), H("Invalid prop `%s` supplied to `React.Fragment`. React.Fragment can only have `key` and `children` props.", f), J(null);
            break;
          }
        }
        t.ref !== null && (J(t), H("Invalid attribute `ref` supplied to `React.Fragment`."), J(null));
      }
    }
    function nt(t, l, u, f, m, C) {
      {
        var v = Lt(t);
        if (!v) {
          var h = "";
          (t === void 0 || typeof t == "object" && t !== null && Object.keys(t).length === 0) && (h += " You likely forgot to export your component from the file it's defined in, or you might have mixed up default and named imports.");
          var I = ir(m);
          I ? h += I : h += Qe();
          var M;
          t === null ? M = "null" : pe(t) ? M = "array" : t !== void 0 && t.$$typeof === n2 ? (M = "<" + (L(t.type) || "Unknown") + " />", h = " Did you accidentally export a JSX literal instead of a component?") : M = typeof t, H("React.jsx: type is invalid -- expected a string (for built-in components) or a class/function (for composite components) but got: %s.%s", M, h);
        }
        var E = or(t, l, u, m, C);
        if (E == null)
          return E;
        if (v) {
          var _ = l.children;
          if (_ !== void 0)
            if (f)
              if (pe(_)) {
                for (var X = 0; X < _.length; X++)
                  rt(_[X], t);
                Object.freeze && Object.freeze(_);
              } else
                H("React.jsx: Static children should always be an array. You are likely explicitly calling React.jsxs or React.jsxDEV. Use the Babel transform instead.");
            else
              rt(_, t);
        }
        return t === i ? cr(E) : lr(E), E;
      }
    }
    function sr(t, l, u) {
      return nt(t, l, u, true);
    }
    function dr(t, l, u) {
      return nt(t, l, u, false);
    }
    var ur = dr, gr = sr;
    ne.Fragment = i, ne.jsx = ur, ne.jsxs = gr;
  }()), ne;
}
false ? Me.exports = Hr() : Me.exports = Ir();
var r = Me.exports;
var fe = (e) => {
  const n2 = `0 4px 12px ${e.colors.shadow}`, o = `1px solid ${e.colors.border}`, i = {
    default: e.colors.editor.text,
    gray: e.colors.highlightColors.gray.text,
    brown: e.colors.highlightColors.brown.text,
    red: e.colors.highlightColors.red.text,
    orange: e.colors.highlightColors.orange.text,
    yellow: e.colors.highlightColors.yellow.text,
    green: e.colors.highlightColors.green.text,
    blue: e.colors.highlightColors.blue.text,
    purple: e.colors.highlightColors.purple.text,
    pink: e.colors.highlightColors.pink.text
  }, s = {
    default: e.colors.editor.background,
    gray: e.colors.highlightColors.gray.background,
    brown: e.colors.highlightColors.brown.background,
    red: e.colors.highlightColors.red.background,
    orange: e.colors.highlightColors.orange.background,
    yellow: e.colors.highlightColors.yellow.background,
    green: e.colors.highlightColors.green.background,
    blue: e.colors.highlightColors.blue.background,
    purple: e.colors.highlightColors.purple.background,
    pink: e.colors.highlightColors.pink.background
  }, c = `${Math.max(e.borderRadius + 2, 1)}px`, d2 = `${e.borderRadius}px`, g = `${Math.max(e.borderRadius - 2, 1)}px`;
  return {
    activeStyles: {
      // Removes button press effect.
      transform: "none"
    },
    components: {
      // Slash Menu, Formatting Toolbar dropdown, color picker dropdown
      Menu: {
        styles: () => {
          var a2;
          return {
            dropdown: import_lodash.default.merge(
              {
                backgroundColor: e.colors.menu.background,
                border: o,
                borderRadius: d2,
                boxShadow: n2,
                color: e.colors.menu.text,
                padding: "2px",
                overflowY: "scroll",
                ".mantine-Menu-label": {
                  backgroundColor: e.colors.menu.background,
                  color: e.colors.menu.text
                },
                ".mantine-Menu-item": {
                  backgroundColor: e.colors.menu.background,
                  border: "none",
                  borderRadius: g,
                  color: e.colors.menu.text
                },
                ".mantine-Menu-item[data-hovered]": {
                  backgroundColor: e.colors.hovered.background,
                  border: "none",
                  color: e.colors.hovered.text
                }
              },
              ((a2 = e.componentStyles) == null ? void 0 : a2.call(e, e).Menu) || {}
            )
          };
        }
      },
      Tabs: {
        styles: () => ({
          root: {
            width: "100%",
            backgroundColor: e.colors.menu.background
          },
          tabsList: {
            borderColor: e.colors.hovered.background
          },
          tab: {
            color: e.colors.menu.text,
            borderColor: e.colors.hovered.background,
            "&:hover": {
              color: e.colors.hovered.text,
              backgroundColor: e.colors.hovered.background,
              borderColor: e.colors.hovered.background
            },
            "&[data-active], &[data-active]&:hover": {
              color: e.colors.menu.text,
              borderColor: e.colors.menu.text
            }
          },
          panel: {
            padding: "8px",
            ".mantine-UnstyledButton-root": {
              width: "60%",
              border: `solid ${e.colors.border} 1px`,
              borderRadius: "4px",
              height: "32px"
            },
            ".mantine-UnstyledButton-root:hover": {
              color: e.colors.hovered.text,
              backgroundColor: e.colors.hovered.background
            }
          }
        })
      },
      FileInput: {
        styles: () => ({
          input: {
            color: e.colors.menu.text,
            backgroundColor: e.colors.menu.background,
            display: "flex",
            flexDirection: "row",
            alignItems: "center",
            justifyContent: "center",
            border: "none",
            borderRadius: "4px",
            "&:hover": {
              backgroundColor: e.colors.hovered.background
            }
          },
          wrapper: {
            border: `solid ${e.colors.border} 1px`,
            borderRadius: "4px"
          },
          placeholder: {
            color: `${e.colors.menu.text} !important`,
            fontWeight: 600
          }
        })
      },
      TextInput: {
        styles: () => ({
          input: {
            color: e.colors.menu.text,
            backgroundColor: e.colors.menu.background,
            border: `solid ${e.colors.border} 1px`,
            borderRadius: "4px",
            height: "32px"
          }
        })
      },
      ColorIcon: {
        styles: () => {
          var a2;
          return {
            root: import_lodash.default.merge(
              {
                border: o,
                borderRadius: g
              },
              ((a2 = e.componentStyles) == null ? void 0 : a2.call(e, e).ColorIcon) || {}
            )
          };
        }
      },
      DragHandleMenu: {
        styles: () => {
          var a2;
          return {
            root: import_lodash.default.merge(
              {
                ".mantine-Menu-item": {
                  fontSize: "12px",
                  height: "30px"
                }
              },
              ((a2 = e.componentStyles) == null ? void 0 : a2.call(e, e).DragHandleMenu) || {}
            )
          };
        }
      },
      Editor: {
        styles: () => {
          var a2;
          return {
            root: import_lodash.default.merge(
              {
                ".ProseMirror": {
                  backgroundColor: e.colors.editor.background,
                  borderRadius: c,
                  color: e.colors.editor.text,
                  fontFamily: e.fontFamily
                },
                // Placeholders
                [`.${Bn.isEmpty} .${Bn.inlineContent}:before, .${Bn.isFilter} .${Bn.inlineContent}:before`]: {
                  color: e.colors.sideMenu
                },
                // Indent lines
                [`.${Bn.blockGroup}`]: {
                  [`.${Bn.blockGroup}`]: {
                    [`.${Bn.blockOuter}:not([data-prev-depth-changed])::before`]: {
                      borderLeft: `1px solid ${e.colors.sideMenu}`
                    }
                  }
                },
                // Highlight text colors
                ...Object.fromEntries(
                  Object.entries(i).map(([y, x]) => [
                    `[data-text-color="${y}"]`,
                    { color: x }
                  ])
                ),
                // Highlight background colors
                ...Object.fromEntries(
                  Object.entries(s).map(([y, x]) => [
                    `[data-background-color="${y}"]`,
                    { backgroundColor: x }
                  ])
                )
              },
              ((a2 = e.componentStyles) == null ? void 0 : a2.call(e, e).Editor) || {}
            )
          };
        }
      },
      Toolbar: {
        styles: () => {
          var a2;
          return {
            root: import_lodash.default.merge(
              {
                backgroundColor: e.colors.menu.background,
                boxShadow: n2,
                border: o,
                borderRadius: d2,
                flexWrap: "nowrap",
                gap: "2px",
                padding: "2px",
                width: "fit-content",
                // Button (including dropdown target)
                ".mantine-Button-root, .mantine-ActionIcon-root": {
                  backgroundColor: e.colors.menu.background,
                  border: "none",
                  borderRadius: g,
                  color: e.colors.menu.text
                },
                // Hovered button
                ".mantine-Button-root:hover, .mantine-ActionIcon-root:hover": {
                  backgroundColor: e.colors.hovered.background,
                  border: "none",
                  color: e.colors.hovered.text
                },
                // Selected button
                ".mantine-Button-root[data-selected], .mantine-ActionIcon-root[data-selected]": {
                  backgroundColor: e.colors.selected.background,
                  border: "none",
                  color: e.colors.selected.text
                },
                // Disabled button
                ".mantine-Button-root[data-disabled], .mantine-ActionIcon-root[data-disabled]": {
                  backgroundColor: e.colors.disabled.background,
                  border: "none",
                  color: e.colors.disabled.text
                },
                // Dropdown
                ".mantine-Menu-dropdown": {
                  // Dropdown item
                  ".mantine-Menu-item": {
                    fontSize: "12px",
                    height: "30px",
                    ".mantine-Menu-itemRightSection": {
                      paddingLeft: "5px"
                    }
                  },
                  ".mantine-Menu-item:hover": {
                    backgroundColor: e.colors.hovered.background
                  }
                }
              },
              ((a2 = e.componentStyles) == null ? void 0 : a2.call(e, e).Toolbar) || {}
            )
          };
        }
      },
      ToolbarInputDropdown: {
        styles: () => {
          var a2;
          return {
            root: import_lodash.default.merge(
              {
                backgroundColor: e.colors.menu.background,
                border: o,
                borderRadius: d2,
                boxShadow: n2,
                color: e.colors.menu.text,
                gap: "4px",
                minWidth: "145px",
                padding: "2px",
                // Row
                ".mantine-Group-root": {
                  flexWrap: "nowrap",
                  // Row input field
                  ".mantine-TextInput-root, .mantine-FileInput-root": {
                    width: "300px",
                    ".mantine-TextInput-wrapper:hover": {
                      backgroundColor: e.colors.hovered.background
                    },
                    ".mantine-TextInput-wrapper, .mantine-FileInput-wrapper": {
                      padding: 0,
                      borderRadius: "4px",
                      ".mantine-FileInput-icon": {
                        color: e.colors.menu.text
                      },
                      ".mantine-TextInput-input, .mantine-FileInput-input": {
                        border: "none",
                        fontSize: "12px",
                        ".mantine-FileInput-placeholder": {
                          color: e.colors.menu.text
                        }
                      },
                      ".mantine-FileInput-input:hover": {
                        backgroundColor: e.colors.hovered.background
                      }
                    }
                  }
                }
              },
              ((a2 = e.componentStyles) == null ? void 0 : a2.call(e, e).EditHyperlinkMenu) || {}
            )
          };
        }
      },
      Tooltip: {
        styles: () => {
          var a2;
          return {
            root: import_lodash.default.merge(
              {
                backgroundColor: e.colors.tooltip.background,
                border: o,
                borderRadius: d2,
                boxShadow: n2,
                color: e.colors.tooltip.text,
                padding: "4px 10px",
                textAlign: "center",
                "div ~ div": {
                  color: e.colors.tooltip.text
                }
              },
              ((a2 = e.componentStyles) == null ? void 0 : a2.call(e, e).Tooltip) || {}
            )
          };
        }
      },
      SlashMenu: {
        styles: () => {
          var a2;
          return {
            root: import_lodash.default.merge(
              {
                position: "relative",
                ".mantine-Menu-item": {
                  // Icon
                  ".mantine-Menu-itemIcon": {
                    backgroundColor: e.colors.tooltip.background,
                    borderRadius: g,
                    color: e.colors.tooltip.text,
                    padding: "8px"
                  },
                  // Text
                  ".mantine-Menu-itemLabel": {
                    paddingRight: "16px",
                    ".mantine-Stack-root": {
                      gap: "0"
                    }
                  },
                  // Badge (keyboard shortcut)
                  ".mantine-Menu-itemRightSection": {
                    ".mantine-Badge-root": {
                      backgroundColor: e.colors.tooltip.background,
                      color: e.colors.tooltip.text
                    }
                  }
                }
              },
              ((a2 = e.componentStyles) == null ? void 0 : a2.call(e, e).SlashMenu) || {}
            )
          };
        }
      },
      SideMenu: {
        styles: () => {
          var a2;
          return {
            root: import_lodash.default.merge(
              {
                backgroundColor: "transparent",
                ".mantine-UnstyledButton-root": {
                  backgroundColor: "transparent",
                  color: e.colors.sideMenu
                },
                ".mantine-UnstyledButton-root:hover": {
                  backgroundColor: e.colors.hovered.background
                }
              },
              ((a2 = e.componentStyles) == null ? void 0 : a2.call(e, e).SideMenu) || {}
            )
          };
        }
      }
    },
    fontFamily: e.fontFamily,
    other: {
      textColors: i,
      backgroundColors: s
    }
  };
};
var _e = (0, import_react291.forwardRef)((e, n2) => {
  const { classes: o } = createStyles({ root: {} })(void 0, {
    name: "Toolbar"
  });
  return r.jsx(
    Group,
    {
      className: e.className ? `${o.root} ${e.className}` : o.root,
      ref: n2,
      ...e,
      children: e.children
    }
  );
});
var xt = {
  color: void 0,
  size: void 0,
  className: void 0,
  style: void 0,
  attr: void 0
};
var ct = import_react291.default.createContext && import_react291.default.createContext(xt);
var W = globalThis && globalThis.__assign || function() {
  return W = Object.assign || function(e) {
    for (var n2, o = 1, i = arguments.length; o < i; o++) {
      n2 = arguments[o];
      for (var s in n2)
        Object.prototype.hasOwnProperty.call(n2, s) && (e[s] = n2[s]);
    }
    return e;
  }, W.apply(this, arguments);
};
var Br = globalThis && globalThis.__rest || function(e, n2) {
  var o = {};
  for (var i in e)
    Object.prototype.hasOwnProperty.call(e, i) && n2.indexOf(i) < 0 && (o[i] = e[i]);
  if (e != null && typeof Object.getOwnPropertySymbols == "function")
    for (var s = 0, i = Object.getOwnPropertySymbols(e); s < i.length; s++)
      n2.indexOf(i[s]) < 0 && Object.prototype.propertyIsEnumerable.call(e, i[s]) && (o[i[s]] = e[i[s]]);
  return o;
};
function mt(e) {
  return e && e.map(function(n2, o) {
    return import_react291.default.createElement(n2.tag, W({
      key: o
    }, n2.attr), mt(n2.child));
  });
}
function j(e) {
  return function(n2) {
    return import_react291.default.createElement(_r, W({
      attr: W({}, e.attr)
    }, n2), mt(e.child));
  };
}
function _r(e) {
  var n2 = function(o) {
    var i = e.attr, s = e.size, c = e.title, d2 = Br(e, ["attr", "size", "title"]), g = s || o.size || "1em", a2;
    return o.className && (a2 = o.className), e.className && (a2 = (a2 ? a2 + " " : "") + e.className), import_react291.default.createElement("svg", W({
      stroke: "currentColor",
      fill: "currentColor",
      strokeWidth: "0"
    }, o.attr, i, d2, {
      className: a2,
      style: W(W({
        color: e.color || o.color
      }, o.style), e.style),
      height: g,
      width: g,
      xmlns: "http://www.w3.org/2000/svg"
    }), c && import_react291.default.createElement("title", null, c), e.children);
  };
  return ct !== void 0 ? import_react291.default.createElement(ct.Consumer, null, function(o) {
    return n2(o);
  }) : n2(xt);
}
function Dr(e) {
  return j({ tag: "svg", attr: { viewBox: "0 0 24 24" }, child: [{ tag: "g", attr: {}, child: [{ tag: "path", attr: { fill: "none", d: "M0 0h24v24H0z" } }, { tag: "path", attr: { d: "M23 12l-7.071 7.071-1.414-1.414L20.172 12l-5.657-5.657 1.414-1.414L23 12zM3.828 12l5.657 5.657-1.414 1.414L1 12l7.071-7.071 1.414 1.414L3.828 12z" } }] }] })(e);
}
function Pr(e) {
  return j({ tag: "svg", attr: { viewBox: "0 0 24 24" }, child: [{ tag: "g", attr: {}, child: [{ tag: "path", attr: { fill: "none", d: "M0 0h24v24H0z" } }, { tag: "path", attr: { d: "M3 4h18v2H3V4zm2 15h14v2H5v-2zm-2-5h18v2H3v-2zm2-5h14v2H5V9z" } }] }] })(e);
}
function Or(e) {
  return j({ tag: "svg", attr: { viewBox: "0 0 24 24" }, child: [{ tag: "g", attr: {}, child: [{ tag: "path", attr: { fill: "none", d: "M0 0h24v24H0z" } }, { tag: "path", attr: { d: "M3 4h18v2H3V4zm0 15h18v2H3v-2zm0-5h18v2H3v-2zm0-5h18v2H3V9z" } }] }] })(e);
}
function Fr(e) {
  return j({ tag: "svg", attr: { viewBox: "0 0 24 24" }, child: [{ tag: "g", attr: {}, child: [{ tag: "path", attr: { fill: "none", d: "M0 0h24v24H0z" } }, { tag: "path", attr: { d: "M3 4h18v2H3V4zm0 15h14v2H3v-2zm0-5h18v2H3v-2zm0-5h14v2H3V9z" } }] }] })(e);
}
function Ar(e) {
  return j({ tag: "svg", attr: { viewBox: "0 0 24 24" }, child: [{ tag: "g", attr: {}, child: [{ tag: "path", attr: { fill: "none", d: "M0 0h24v24H0z" } }, { tag: "path", attr: { d: "M3 4h18v2H3V4zm4 15h14v2H7v-2zm-4-5h18v2H3v-2zm4-5h14v2H7V9z" } }] }] })(e);
}
function Lr(e) {
  return j({ tag: "svg", attr: { viewBox: "0 0 24 24" }, child: [{ tag: "g", attr: {}, child: [{ tag: "path", attr: { fill: "none", d: "M0 0h24v24H0z" } }, { tag: "path", attr: { d: "M8 11h4.5a2.5 2.5 0 1 0 0-5H8v5zm10 4.5a4.5 4.5 0 0 1-4.5 4.5H6V4h6.5a4.5 4.5 0 0 1 3.256 7.606A4.498 4.498 0 0 1 18 15.5zM8 13v5h5.5a2.5 2.5 0 1 0 0-5H8z" } }] }] })(e);
}
function kt(e) {
  return j({ tag: "svg", attr: { viewBox: "0 0 24 24" }, child: [{ tag: "g", attr: {}, child: [{ tag: "path", attr: { fill: "none", d: "M0 0H24V24H0z" } }, { tag: "path", attr: { d: "M13 20h-2v-7H4v7H2V4h2v7h7V4h2v16zm8-12v12h-2v-9.796l-2 .536V8.67L19.5 8H21z" } }] }] })(e);
}
function yt(e) {
  return j({ tag: "svg", attr: { viewBox: "0 0 24 24" }, child: [{ tag: "g", attr: {}, child: [{ tag: "path", attr: { fill: "none", d: "M0 0H24V24H0z" } }, { tag: "path", attr: { d: "M4 4v7h7V4h2v16h-2v-7H4v7H2V4h2zm14.5 4c2.071 0 3.75 1.679 3.75 3.75 0 .857-.288 1.648-.772 2.28l-.148.18L18.034 18H22v2h-7v-1.556l4.82-5.546c.268-.307.43-.709.43-1.148 0-.966-.784-1.75-1.75-1.75-.918 0-1.671.707-1.744 1.606l-.006.144h-2C14.75 9.679 16.429 8 18.5 8z" } }] }] })(e);
}
function pt(e) {
  return j({ tag: "svg", attr: { viewBox: "0 0 24 24" }, child: [{ tag: "g", attr: {}, child: [{ tag: "path", attr: { fill: "none", d: "M0 0H24V24H0z" } }, { tag: "path", attr: { d: "M22 8l-.002 2-2.505 2.883c1.59.435 2.757 1.89 2.757 3.617 0 2.071-1.679 3.75-3.75 3.75-1.826 0-3.347-1.305-3.682-3.033l1.964-.382c.156.806.866 1.415 1.718 1.415.966 0 1.75-.784 1.75-1.75s-.784-1.75-1.75-1.75c-.286 0-.556.069-.794.19l-1.307-1.547L19.35 10H15V8h7zM4 4v7h7V4h2v16h-2v-7H4v7H2V4h2z" } }] }] })(e);
}
function Vr(e) {
  return j({ tag: "svg", attr: { viewBox: "0 0 24 24" }, child: [{ tag: "g", attr: {}, child: [{ tag: "path", attr: { fill: "none", d: "M0 0h24v24H0z" } }, { tag: "path", attr: { d: "M3 4h18v2H3V4zm0 15h18v2H3v-2zm8-5h10v2H11v-2zm0-5h10v2H11V9zm-8 3.5L7 9v7l-4-3.5z" } }] }] })(e);
}
function Ur(e) {
  return j({ tag: "svg", attr: { viewBox: "0 0 24 24" }, child: [{ tag: "g", attr: {}, child: [{ tag: "path", attr: { fill: "none", d: "M0 0h24v24H0z" } }, { tag: "path", attr: { d: "M3 4h18v2H3V4zm0 15h18v2H3v-2zm8-5h10v2H11v-2zm0-5h10v2H11V9zm-4 3.5L3 16V9l4 3.5z" } }] }] })(e);
}
function Nr(e) {
  return j({ tag: "svg", attr: { viewBox: "0 0 24 24" }, child: [{ tag: "g", attr: {}, child: [{ tag: "path", attr: { fill: "none", d: "M0 0h24v24H0z" } }, { tag: "path", attr: { d: "M15 20H7v-2h2.927l2.116-12H9V4h8v2h-2.927l-2.116 12H15z" } }] }] })(e);
}
function $r(e) {
  return j({ tag: "svg", attr: { viewBox: "0 0 24 24" }, child: [{ tag: "g", attr: {}, child: [{ tag: "path", attr: { fill: "none", d: "M0 0h24v24H0z" } }, { tag: "path", attr: { d: "M17 17h5v2h-3v3h-2v-5zM7 7H2V5h3V2h2v5zm11.364 8.536L16.95 14.12l1.414-1.414a5 5 0 1 0-7.071-7.071L9.879 7.05 8.464 5.636 9.88 4.222a7 7 0 0 1 9.9 9.9l-1.415 1.414zm-2.828 2.828l-1.415 1.414a7 7 0 0 1-9.9-9.9l1.415-1.414L7.05 9.88l-1.414 1.414a5 5 0 1 0 7.071 7.071l1.414-1.414 1.415 1.414zm-.708-10.607l1.415 1.415-7.071 7.07-1.415-1.414 7.071-7.07z" } }] }] })(e);
}
function Ct(e) {
  return j({ tag: "svg", attr: { viewBox: "0 0 24 24" }, child: [{ tag: "g", attr: {}, child: [{ tag: "path", attr: { fill: "none", d: "M0 0h24v24H0z" } }, { tag: "path", attr: { d: "M18.364 15.536L16.95 14.12l1.414-1.414a5 5 0 1 0-7.071-7.071L9.879 7.05 8.464 5.636 9.88 4.222a7 7 0 0 1 9.9 9.9l-1.415 1.414zm-2.828 2.828l-1.415 1.414a7 7 0 0 1-9.9-9.9l1.415-1.414L7.05 9.88l-1.414 1.414a5 5 0 1 0 7.071 7.071l1.414-1.414 1.415 1.414zm-.708-10.607l1.415 1.415-7.071 7.07-1.415-1.414 7.071-7.07z" } }] }] })(e);
}
function jt2(e) {
  return j({ tag: "svg", attr: { viewBox: "0 0 24 24" }, child: [{ tag: "g", attr: {}, child: [{ tag: "path", attr: { fill: "none", d: "M0 0h24v24H0z" } }, { tag: "path", attr: { d: "M8 4h13v2H8V4zM5 3v3h1v1H3V6h1V4H3V3h2zM3 14v-2.5h2V11H3v-1h3v2.5H4v.5h2v1H3zm2 5.5H3v-1h2V18H3v-1h3v4H3v-1h2v-.5zM8 11h13v2H8v-2zm0 7h13v2H8v-2z" } }] }] })(e);
}
function Tt(e) {
  return j({ tag: "svg", attr: { viewBox: "0 0 24 24" }, child: [{ tag: "g", attr: {}, child: [{ tag: "path", attr: { fill: "none", d: "M0 0h24v24H0z" } }, { tag: "path", attr: { d: "M8 4h13v2H8V4zM4.5 6.5a1.5 1.5 0 1 1 0-3 1.5 1.5 0 0 1 0 3zm0 7a1.5 1.5 0 1 1 0-3 1.5 1.5 0 0 1 0 3zm0 6.9a1.5 1.5 0 1 1 0-3 1.5 1.5 0 0 1 0 3zM8 11h13v2H8v-2zm0 7h13v2H8v-2z" } }] }] })(e);
}
function Wr(e) {
  return j({ tag: "svg", attr: { viewBox: "0 0 24 24" }, child: [{ tag: "g", attr: {}, child: [{ tag: "path", attr: { fill: "none", d: "M0 0h24v24H0z" } }, { tag: "path", attr: { d: "M17.154 14c.23.516.346 1.09.346 1.72 0 1.342-.524 2.392-1.571 3.147C14.88 19.622 13.433 20 11.586 20c-1.64 0-3.263-.381-4.87-1.144V16.6c1.52.877 3.075 1.316 4.666 1.316 2.551 0 3.83-.732 3.839-2.197a2.21 2.21 0 0 0-.648-1.603l-.12-.117H3v-2h18v2h-3.846zm-4.078-3H7.629a4.086 4.086 0 0 1-.481-.522C6.716 9.92 6.5 9.246 6.5 8.452c0-1.236.466-2.287 1.397-3.153C8.83 4.433 10.271 4 12.222 4c1.471 0 2.879.328 4.222.984v2.152c-1.2-.687-2.515-1.03-3.946-1.03-2.48 0-3.719.782-3.719 2.346 0 .42.218.786.654 1.099.436.313.974.562 1.613.75.62.18 1.297.414 2.03.699z" } }] }] })(e);
}
function ie(e) {
  return j({ tag: "svg", attr: { viewBox: "0 0 24 24" }, child: [{ tag: "g", attr: {}, child: [{ tag: "path", attr: { fill: "none", d: "M0 0h24v24H0z" } }, { tag: "path", attr: { d: "M13 6v15h-2V6H5V4h14v2z" } }] }] })(e);
}
function Yr(e) {
  return j({ tag: "svg", attr: { viewBox: "0 0 24 24" }, child: [{ tag: "g", attr: {}, child: [{ tag: "path", attr: { fill: "none", d: "M0 0h24v24H0z" } }, { tag: "path", attr: { d: "M8 3v9a4 4 0 1 0 8 0V3h2v9a6 6 0 1 1-12 0V3h2zM4 20h16v2H4v-2z" } }] }] })(e);
}
function qr(e) {
  return j({ tag: "svg", attr: { viewBox: "0 0 24 24" }, child: [{ tag: "g", attr: {}, child: [{ tag: "path", attr: { fill: "none", d: "M0 0h24v24H0z" } }, { tag: "path", attr: { d: "M5 11.1l2-2 5.5 5.5 3.5-3.5 3 3V5H5v6.1zM4 3h16a1 1 0 0 1 1 1v16a1 1 0 0 1-1 1H4a1 1 0 0 1-1-1V4a1 1 0 0 1 1-1zm11.5 7a1.5 1.5 0 1 1 0-3 1.5 1.5 0 0 1 0 3z" } }] }] })(e);
}
function Kr(e) {
  return j({ tag: "svg", attr: { viewBox: "0 0 24 24" }, child: [{ tag: "g", attr: {}, child: [{ tag: "path", attr: { fill: "none", d: "M0 0H24V24H0z" } }, { tag: "path", attr: { d: "M20 3c.552 0 1 .448 1 1v1.757l-2 2V5H5v8.1l4-4 4.328 4.329-1.327 1.327-.006 4.239 4.246.006 1.33-1.33L18.899 19H19v-2.758l2-2V20c0 .552-.448 1-1 1H4c-.552 0-1-.448-1-1V4c0-.552.448-1 1-1h16zm1.778 4.808l1.414 1.414L15.414 17l-1.416-.002.002-1.412 7.778-7.778zM15.5 7c.828 0 1.5.672 1.5 1.5s-.672 1.5-1.5 1.5S14 9.328 14 8.5 14.672 7 15.5 7z" } }] }] })(e);
}
function Gr(e) {
  return j({ tag: "svg", attr: { viewBox: "0 0 24 24" }, child: [{ tag: "g", attr: {}, child: [{ tag: "path", attr: { fill: "none", d: "M0 0h24v24H0z" } }, { tag: "path", attr: { d: "M10 6v2H5v11h11v-5h2v6a1 1 0 0 1-1 1H4a1 1 0 0 1-1-1V7a1 1 0 0 1 1-1h6zm11-3v9l-3.794-3.793-5.999 6-1.414-1.414 5.999-6L12 3h9z" } }] }] })(e);
}
function Re(e) {
  return j({ tag: "svg", attr: { version: "1.2", baseProfile: "tiny", viewBox: "0 0 24 24" }, child: [{ tag: "path", attr: { d: "M16.972 6.251c-.967-.538-2.185-.188-2.72.777l-3.713 6.682-2.125-2.125c-.781-.781-2.047-.781-2.828 0-.781.781-.781 2.047 0 2.828l4 4c.378.379.888.587 1.414.587l.277-.02c.621-.087 1.166-.46 1.471-1.009l5-9c.537-.966.189-2.183-.776-2.72z" } }] })(e);
}
function Jr(e) {
  const n2 = e.icon;
  return r.jsx(
    Menu.Item,
    {
      onClick: e.onClick,
      icon: n2 && r.jsx(n2, { size: 16 }),
      rightSection: e.isSelected ? r.jsx(Re, { size: 16 }) : (
        // Ensures space for tick even if item isn't currently selected.
        r.jsx("div", { style: { width: "16px", padding: "0" } })
      ),
      disabled: e.isDisabled,
      children: e.text
    },
    e.text
  );
}
function Xr(e) {
  return j({ tag: "svg", attr: { viewBox: "0 0 20 20", fill: "currentColor", "aria-hidden": "true" }, child: [{ tag: "path", attr: { fillRule: "evenodd", d: "M5.293 7.293a1 1 0 011.414 0L10 10.586l3.293-3.293a1 1 0 111.414 1.414l-4 4a1 1 0 01-1.414 0l-4-4a1 1 0 010-1.414z", clipRule: "evenodd" } }] })(e);
}
function Zr(e) {
  return j({ tag: "svg", attr: { viewBox: "0 0 20 20", fill: "currentColor", "aria-hidden": "true" }, child: [{ tag: "path", attr: { fillRule: "evenodd", d: "M7.293 14.707a1 1 0 010-1.414L10.586 10 7.293 6.707a1 1 0 011.414-1.414l4 4a1 1 0 010 1.414l-4 4a1 1 0 01-1.414 0z", clipRule: "evenodd" } }] })(e);
}
var Qr = (0, import_react291.forwardRef)((e, n2) => {
  const o = e.icon;
  return r.jsx(
    Button,
    {
      leftIcon: o && r.jsx(o, { size: 16 }),
      rightIcon: r.jsx(Xr, {}),
      size: "xs",
      variant: "subtle",
      disabled: e.isDisabled,
      onClick: e.onClick,
      ref: n2,
      children: e.text
    }
  );
});
function xe() {
  const e = (0, import_react291.useRef)(null);
  return {
    ref: e,
    updateMaxHeight: () => {
      setTimeout(() => {
        if (e.current && e.current.childElementCount > 0) {
          e.current.firstElementChild.style.maxHeight = "none";
          const n2 = e.current.firstElementChild.getBoundingClientRect();
          e.current.firstElementChild.style.maxHeight = `${Math.min(
            n2.top >= 0 ? window.innerHeight - n2.top - 20 : n2.bottom - 20
          )}px`;
        }
      }, 10);
    }
  };
}
function en(e) {
  const n2 = e.items.filter((s) => s.isSelected)[0], { ref: o, updateMaxHeight: i } = xe();
  return n2 ? r.jsxs(
    Menu,
    {
      exitTransitionDuration: 0,
      disabled: e.isDisabled,
      onOpen: i,
      children: [
        r.jsx(Menu.Target, { children: r.jsx(
          Qr,
          {
            text: n2.text,
            icon: n2.icon,
            isDisabled: n2.isDisabled
          }
        ) }),
        r.jsx("div", { ref: o, children: r.jsx(Menu.Dropdown, { children: e.items.map((s) => r.jsx(Jr, { ...s }, s.text)) }) })
      ]
    }
  ) : null;
}
function tn(e, n2) {
  (0, import_react291.useEffect)(() => (e._tiptapEditor.on("update", n2), () => {
    e._tiptapEditor.off("update", n2);
  }), [n2, e._tiptapEditor]);
}
function rn(e, n2) {
  (0, import_react291.useEffect)(() => (e._tiptapEditor.on("selectionUpdate", n2), () => {
    e._tiptapEditor.off("selectionUpdate", n2);
  }), [n2, e._tiptapEditor]);
}
function q(e, n2) {
  tn(e, n2), rn(e, n2);
}
var nn = [
  {
    name: "Paragraph",
    type: "paragraph",
    icon: ie,
    isSelected: (e) => e.type === "paragraph"
  },
  {
    name: "Heading 1",
    type: "heading",
    props: { level: 1 },
    icon: kt,
    isSelected: (e) => e.type === "heading" && "level" in e.props && e.props.level === "1"
  },
  {
    name: "Heading 2",
    type: "heading",
    props: { level: 2 },
    icon: yt,
    isSelected: (e) => e.type === "heading" && "level" in e.props && e.props.level === "2"
  },
  {
    name: "Heading 3",
    type: "heading",
    props: { level: 3 },
    icon: pt,
    isSelected: (e) => e.type === "heading" && "level" in e.props && e.props.level === "3"
  },
  {
    name: "Bullet List",
    type: "bulletListItem",
    icon: Tt,
    isSelected: (e) => e.type === "bulletListItem"
  },
  {
    name: "Numbered List",
    type: "numberedListItem",
    icon: jt2,
    isSelected: (e) => e.type === "numberedListItem"
  }
];
var on = (e) => {
  const [n2, o] = (0, import_react291.useState)(
    e.editor.getTextCursorPosition().block
  ), i = (0, import_react291.useMemo)(() => (e.items || nn).filter((d2) => {
    if (!(d2.type in e.editor.schema))
      return false;
    for (const [g, a2] of Object.entries(d2.props || {})) {
      const y = e.editor.schema[d2.type].propSchema;
      if (!(g in y) || y[g].values !== void 0 && !y[g].values.includes(a2))
        return false;
    }
    return true;
  }), [e.editor, e.items]), s = (0, import_react291.useMemo)(
    () => i.find((d2) => d2.type === n2.type) !== void 0,
    [n2.type, i]
  ), c = (0, import_react291.useMemo)(() => {
    const d2 = (g) => {
      e.editor.focus(), e.editor.updateBlock(n2, {
        type: g.type,
        props: g.props
      });
    };
    return i.map((g) => ({
      text: g.name,
      icon: g.icon,
      onClick: () => d2(g),
      isSelected: g.isSelected(n2)
    }));
  }, [n2, i, e.editor]);
  return q(e.editor, () => {
    o(e.editor.getTextCursorPosition().block);
  }), s ? r.jsx(en, { items: c }) : null;
};
var an = (e) => {
  const { classes: n2 } = createStyles({ root: {} })(void 0, {
    name: "Tooltip"
  });
  return r.jsxs(Stack, { spacing: 0, className: n2.root, children: [
    r.jsx(Text, { size: "sm", children: e.mainTooltip }),
    e.secondaryTooltip && r.jsx(Text, { size: "xs", children: e.secondaryTooltip })
  ] });
};
var P = (0, import_react291.forwardRef)(
  (e, n2) => {
    const o = e.icon;
    return r.jsx(
      tippy_react_esm_default,
      {
        content: r.jsx(
          an,
          {
            mainTooltip: e.mainTooltip,
            secondaryTooltip: e.secondaryTooltip
          }
        ),
        trigger: "mouseenter",
        children: e.children ? r.jsxs(
          Button,
          {
            onClick: e.onClick,
            "data-selected": e.isSelected ? "true" : void 0,
            "data-test": e.mainTooltip.slice(0, 1).toLowerCase() + e.mainTooltip.replace(/\s+/g, "").slice(1),
            size: "xs",
            disabled: e.isDisabled || false,
            ref: n2,
            children: [
              o && r.jsx(o, {}),
              e.children
            ]
          }
        ) : r.jsx(
          ActionIcon,
          {
            onClick: e.onClick,
            "data-selected": e.isSelected ? "true" : void 0,
            "data-test": e.mainTooltip.slice(0, 1).toLowerCase() + e.mainTooltip.replace(/\s+/g, "").slice(1),
            size: 30,
            disabled: e.isDisabled || false,
            ref: n2,
            children: o && r.jsx(o, {})
          }
        )
      }
    );
  }
);
function Q(e) {
  const [n2, o] = (0, import_react291.useState)(
    () => {
      var i;
      return ((i = e.getSelection()) == null ? void 0 : i.blocks) || [e.getTextCursorPosition().block];
    }
  );
  return q(
    e,
    () => {
      var i;
      return o(
        ((i = e.getSelection()) == null ? void 0 : i.blocks) || [e.getTextCursorPosition().block]
      );
    }
  ), n2;
}
var ln = () => typeof navigator < "u" && (/Mac/.test(navigator.platform) || /AppleWebKit/.test(navigator.userAgent) && /Mobile\/\w+/.test(navigator.userAgent));
function F2(e) {
  return ln() ? e.replace("Mod", "⌘") : e.replace("Mod", "Ctrl");
}
var cn = {
  bold: "Mod+B",
  italic: "Mod+I",
  underline: "Mod+U",
  strike: "Mod+Shift+X",
  code: ""
};
var sn = {
  bold: Lr,
  italic: Nr,
  underline: Yr,
  strike: Wr,
  code: Dr
};
var he = (e) => {
  const n2 = Q(e.editor), [o, i] = (0, import_react291.useState)(
    e.toggledStyle in e.editor.getActiveStyles()
  );
  q(e.editor, () => {
    i(e.toggledStyle in e.editor.getActiveStyles());
  });
  const s = (d2) => {
    e.editor.focus(), e.editor.toggleStyles({ [d2]: true });
  };
  return (0, import_react291.useMemo)(() => !!n2.find((d2) => d2.content !== void 0), [n2]) ? r.jsx(
    P,
    {
      onClick: () => s(e.toggledStyle),
      isSelected: o,
      mainTooltip: e.toggledStyle.slice(0, 1).toUpperCase() + e.toggledStyle.slice(1),
      secondaryTooltip: F2(cn[e.toggledStyle]),
      icon: sn[e.toggledStyle]
    }
  ) : null;
};
var dn = {
  left: Fr,
  center: Pr,
  right: Ar,
  justify: Or
};
var Se = (e) => {
  const n2 = Q(e.editor), o = (0, import_react291.useMemo)(() => {
    const c = n2[0];
    if ("textAlignment" in c.props)
      return c.props.textAlignment;
  }, [n2]), i = (0, import_react291.useCallback)(
    (c) => {
      e.editor.focus();
      for (const d2 of n2)
        e.editor.updateBlock(d2, {
          props: { textAlignment: c }
        });
    },
    [e.editor, n2]
  );
  return (0, import_react291.useMemo)(() => !!n2.find((c) => "textAlignment" in c.props), [n2]) ? r.jsx(
    P,
    {
      onClick: () => i(e.textAlignment),
      isSelected: o === e.textAlignment,
      mainTooltip: e.textAlignment === "justify" ? "Justify Text" : "Align Text " + e.textAlignment.slice(0, 1).toUpperCase() + e.textAlignment.slice(1),
      icon: dn[e.textAlignment]
    }
  ) : null;
};
var Ee = (e) => {
  const { classes: n2 } = createStyles({ root: {} })(void 0, {
    name: "ColorIcon"
  }), o = e.textColor || "default", i = e.backgroundColor || "default", s = e.size || 16;
  return r.jsx(
    Box,
    {
      className: n2.root,
      sx: (c) => ({
        pointerEvents: "none",
        backgroundColor: c.other.backgroundColors[i],
        color: c.other.textColors[o],
        fontSize: (s * 0.75).toString() + "px",
        height: s.toString() + "px",
        lineHeight: s.toString() + "px",
        textAlign: "center",
        width: s.toString() + "px"
      }),
      children: "A"
    }
  );
};
var wt = (e) => {
  const n2 = () => e.text ? r.jsxs(r.Fragment, { children: [
    r.jsx(Menu.Label, { children: "Text" }),
    [
      "default",
      "gray",
      "brown",
      "red",
      "orange",
      "yellow",
      "green",
      "blue",
      "purple",
      "pink"
    ].map((i) => r.jsx(
      Menu.Item,
      {
        onClick: () => {
          e.onClick && e.onClick(), e.text.setColor(i);
        },
        component: "div",
        "data-test": "text-color-" + i,
        icon: r.jsx(Ee, { textColor: i, size: e.iconSize }),
        rightSection: e.text.color === i ? r.jsx(Re, { size: 16, style: { paddingLeft: "8px" } }) : r.jsx("div", { style: { width: "24px", padding: "0" } }),
        children: i.charAt(0).toUpperCase() + i.slice(1)
      },
      "text-color-" + i
    ))
  ] }) : null, o = () => e.background ? r.jsxs(r.Fragment, { children: [
    r.jsx(Menu.Label, { children: "Background" }),
    [
      "default",
      "gray",
      "brown",
      "red",
      "orange",
      "yellow",
      "green",
      "blue",
      "purple",
      "pink"
    ].map((i) => r.jsx(
      Menu.Item,
      {
        onClick: () => {
          e.onClick && e.onClick(), e.background.setColor(i);
        },
        component: "div",
        "data-test": "background-color-" + i,
        icon: r.jsx(Ee, { backgroundColor: i, size: e.iconSize }),
        rightSection: e.background.color === i ? r.jsx(Re, { size: 16, style: { paddingLeft: "8px" } }) : r.jsx("div", { style: { width: "24px", padding: "0" } }),
        children: i.charAt(0).toUpperCase() + i.slice(1)
      },
      "background-color-" + i
    ))
  ] }) : null;
  return r.jsxs(r.Fragment, { children: [
    r.jsx(n2, {}),
    r.jsx(o, {})
  ] });
};
var un = (e) => {
  const n2 = Q(e.editor), [o, i] = (0, import_react291.useState)(
    e.editor.getActiveStyles().textColor || "default"
  ), [s, c] = (0, import_react291.useState)(
    e.editor.getActiveStyles().backgroundColor || "default"
  );
  q(e.editor, () => {
    i(e.editor.getActiveStyles().textColor || "default"), c(
      e.editor.getActiveStyles().backgroundColor || "default"
    );
  });
  const { ref: d2, updateMaxHeight: g } = xe(), a2 = (0, import_react291.useCallback)(
    (p) => {
      e.editor.focus(), p === "default" ? e.editor.removeStyles({ textColor: p }) : e.editor.addStyles({ textColor: p });
    },
    [e.editor]
  ), y = (0, import_react291.useCallback)(
    (p) => {
      e.editor.focus(), p === "default" ? e.editor.removeStyles({ backgroundColor: p }) : e.editor.addStyles({ backgroundColor: p });
    },
    [e.editor]
  );
  return (0, import_react291.useMemo)(() => {
    for (const p of n2)
      if (p.content !== void 0)
        return true;
    return false;
  }, [n2]) ? r.jsxs(Menu, { onOpen: g, children: [
    r.jsx(Menu.Target, { children: r.jsx(
      P,
      {
        mainTooltip: "Colors",
        icon: () => r.jsx(
          Ee,
          {
            textColor: o,
            backgroundColor: s,
            size: 20
          }
        )
      }
    ) }),
    r.jsx("div", { ref: d2, children: r.jsx(Menu.Dropdown, { children: r.jsx(
      wt,
      {
        text: {
          color: o,
          setColor: a2
        },
        background: {
          color: s,
          setColor: y
        }
      }
    ) }) })
  ] }) : null;
};
var gn = (e) => {
  const [n2, o] = (0, import_react291.useState)();
  q(e.editor, () => {
    o(e.editor.canNestBlock());
  });
  const i = (0, import_react291.useCallback)(() => {
    e.editor.focus(), e.editor.nestBlock();
  }, [e.editor]);
  return r.jsx(
    P,
    {
      onClick: i,
      isDisabled: !n2,
      mainTooltip: "Nest Block",
      secondaryTooltip: F2("Tab"),
      icon: Ur
    }
  );
};
var fn4 = (e) => {
  const [n2, o] = (0, import_react291.useState)();
  q(e.editor, () => {
    o(e.editor.canUnnestBlock());
  });
  const i = (0, import_react291.useCallback)(() => {
    e.editor.focus(), e.editor.unnestBlock();
  }, [e]);
  return r.jsx(
    P,
    {
      onClick: i,
      isDisabled: !n2,
      mainTooltip: "Unnest Block",
      secondaryTooltip: F2("Shift+Tab"),
      icon: Vr
    }
  );
};
var St = (e) => {
  const [n2, o] = (0, import_react291.useState)(false), i = (0, import_react291.useCallback)(() => {
    o(false);
  }, []), s = (0, import_react291.useCallback)(() => {
    o(true);
  }, []);
  return r.jsx(
    tippy_react_esm_default,
    {
      onShow: (c) => {
        var d2;
        s(), (d2 = e.onShow) == null || d2.call(e, c);
      },
      onHidden: (c) => {
        var d2;
        i(), (d2 = e.onShow) == null || d2.call(e, c);
      },
      content: n2 ? e.children[1] : null,
      trigger: e.visible === void 0 ? "click" : void 0,
      interactive: true,
      maxWidth: 500,
      zIndex: 9e3,
      ...e,
      children: e.children[0]
    }
  );
};
var Mt = (0, import_react291.forwardRef)(({ className: e, ...n2 }, o) => {
  const { classes: i } = createStyles({ root: {} })(void 0, {
    name: "ToolbarInputDropdown"
  });
  return r.jsx(
    Stack,
    {
      ...n2,
      className: S(i.root, e || ""),
      ref: o,
      children: n2.children
    }
  );
});
var hn = {
  text: TextInput,
  file: FileInput
};
var ze = (e) => {
  const n2 = e.icon, o = hn[e.type];
  return r.jsx(Group, { children: r.jsx(o, { size: "xs", icon: r.jsx(n2, {}), ...e.inputProps }) });
};
var Rt = (0, import_react291.forwardRef)(({ url: e, text: n2, update: o, ...i }, s) => {
  const [c, d2] = (0, import_react291.useState)(e), [g, a2] = (0, import_react291.useState)(n2);
  (0, import_react291.useEffect)(() => {
    d2(e), a2(n2);
  }, [n2, e]);
  const y = (0, import_react291.useCallback)(
    (T) => {
      T.key === "Enter" && (T.preventDefault(), o(c, g));
    },
    [o, c, g]
  ), x = (0, import_react291.useCallback)(
    (T) => d2(T.currentTarget.value),
    []
  ), p = (0, import_react291.useCallback)(
    (T) => a2(T.currentTarget.value),
    []
  ), S2 = (0, import_react291.useCallback)(
    () => o(c, g),
    [o, c, g]
  );
  return r.jsxs(Mt, { ...i, ref: s, children: [
    r.jsx(
      ze,
      {
        type: "text",
        icon: Ct,
        inputProps: {
          autoFocus: true,
          placeholder: "Edit URL",
          value: c,
          onKeyDown: y,
          onChange: x,
          onSubmit: S2
        }
      }
    ),
    r.jsx(
      ze,
      {
        type: "text",
        icon: ie,
        inputProps: {
          placeholder: "Edit Title",
          value: g,
          onKeyDown: y,
          onChange: p,
          onSubmit: S2
        }
      }
    )
  ] });
});
var vn = (e) => {
  const n2 = Q(e.editor), [o, i] = (0, import_react291.useState)(
    e.editor.getSelectedLinkUrl() || ""
  ), [s, c] = (0, import_react291.useState)(e.editor.getSelectedText());
  q(e.editor, () => {
    c(e.editor.getSelectedText() || ""), i(e.editor.getSelectedLinkUrl() || "");
  });
  const d2 = (0, import_react291.useCallback)(
    (a2, y) => {
      e.editor.createLink(a2, y), e.editor.focus();
    },
    [e.editor]
  );
  return (0, import_react291.useMemo)(() => {
    for (const a2 of n2)
      if (a2.content === void 0)
        return false;
    return true;
  }, [n2]) ? r.jsxs(St, { children: [
    r.jsx(
      P,
      {
        mainTooltip: "Create Link",
        secondaryTooltip: F2("Mod+K"),
        icon: Ct
      }
    ),
    r.jsx(Rt, { url: o, text: s, update: d2 })
  ] }) : null;
};
var Et = (e) => {
  const [n2, o] = (0, import_react291.useState)(
    e.editor.uploadFile !== void 0 ? "upload" : "embed"
  ), [i, s] = (0, import_react291.useState)(false), [c, d2] = (0, import_react291.useState)(false);
  (0, import_react291.useEffect)(() => {
    c && setTimeout(() => {
      d2(false);
    }, 3e3);
  }, [c]);
  const g = (0, import_react291.useCallback)(
    async (T) => {
      if (s(true), e.editor.uploadFile !== void 0)
        try {
          const D = await e.editor.uploadFile(T);
          e.editor.updateBlock(e.block, {
            type: "image",
            props: {
              url: D
            }
          });
        } catch {
          d2(true);
        } finally {
          s(false);
        }
    },
    [e.block, e.editor]
  ), [a2, y] = (0, import_react291.useState)(""), x = (0, import_react291.useCallback)(
    (T) => {
      y(T.currentTarget.value);
    },
    []
  ), p = (0, import_react291.useCallback)(
    (T) => {
      T.key === "Enter" && (T.preventDefault(), e.editor.updateBlock(e.block, {
        type: "image",
        props: {
          url: a2
        }
      }));
    },
    [a2, e.block, e.editor]
  ), S2 = (0, import_react291.useCallback)(() => {
    e.editor.updateBlock(e.block, {
      type: "image",
      props: {
        url: a2
      }
    });
  }, [a2, e.block, e.editor]);
  return r.jsx(
    _e,
    {
      style: {
        width: "500px"
      },
      children: r.jsxs(Tabs, { value: n2, onTabChange: o, children: [
        i && r.jsx(LoadingOverlay, { visible: i }),
        r.jsxs(Tabs.List, { children: [
          e.editor.uploadFile !== void 0 && r.jsx(Tabs.Tab, { value: "upload", "data-test": "upload-tab", children: "Upload" }),
          r.jsx(Tabs.Tab, { value: "embed", "data-test": "embed-tab", children: "Embed" })
        ] }),
        e.editor.uploadFile !== void 0 && r.jsx(Tabs.Panel, { value: "upload", children: r.jsxs(
          "div",
          {
            style: {
              display: "flex",
              flexDirection: "column",
              alignItems: "stretch",
              gap: "8px"
            },
            children: [
              r.jsx(
                FileInput,
                {
                  placeholder: "Upload Image",
                  size: "xs",
                  value: null,
                  onChange: g,
                  "data-test": "upload-input"
                }
              ),
              c && r.jsx(Text, { color: "red", size: 12, style: { textAlign: "center" }, children: "Error: Upload failed" })
            ]
          }
        ) }),
        r.jsx(Tabs.Panel, { value: "embed", children: r.jsxs(
          "div",
          {
            style: {
              width: "100%",
              display: "flex",
              flexDirection: "column",
              alignItems: "center",
              gap: "8px"
            },
            children: [
              r.jsx(
                TextInput,
                {
                  size: "xs",
                  placeholder: "Enter URL",
                  value: a2,
                  onChange: x,
                  onKeyDown: p,
                  style: { width: "100%" },
                  "data-test": "embed-input"
                }
              ),
              r.jsx(
                Button,
                {
                  onClick: S2,
                  size: "xs",
                  "data-test": "embed-input-button",
                  children: "Embed Image"
                }
              )
            ]
          }
        ) })
      ] })
    }
  );
};
var bn = (e) => {
  const n2 = Q(e.editor), [o, i] = (0, import_react291.useState)(false);
  return (0, import_react291.useEffect)(() => {
    i(false);
  }, [n2]), // Checks if only one block is selected.
  n2.length === 1 && // Checks if the selected block is an image.
  n2[0].type === "image" ? r.jsx(
    tippy_react_esm_default,
    {
      visible: o,
      interactive: true,
      content: r.jsx(
        Et,
        {
          block: n2[0],
          editor: e.editor
        }
      ),
      children: r.jsx(
        P,
        {
          onClick: () => i(!o),
          isSelected: o,
          mainTooltip: "Replace Image",
          icon: Kr
        }
      )
    }
  ) : null;
};
var xn2 = (e) => {
  const n2 = Q(e.editor), o = (0, import_react291.useMemo)(
    () => (
      // Checks if only one block is selected.
      n2.length === 1 && // Checks if the selected block is an image.
      n2[0].type === "image" && // Checks if the block has a `caption` prop which can take any string
      // value.
      "caption" in e.editor.schema.image.propSchema && typeof e.editor.schema.image.propSchema.caption.default == "string" && e.editor.schema.image.propSchema.caption.values === void 0 && // Checks if the block has a `src` prop which can take any string value.
      "src" in e.editor.schema.image.propSchema && typeof e.editor.schema.image.propSchema.src.default == "string" && e.editor.schema.image.propSchema.src.values === void 0 && // Checks if the `src` prop is not set to an empty string.
      n2[0].props.src !== ""
    ),
    [e.editor.schema, n2]
  ), [i, s] = (0, import_react291.useState)(
    o ? n2[0].props.caption : ""
  );
  (0, import_react291.useEffect)(
    () => s(
      o ? n2[0].props.caption : ""
    ),
    [n2, o]
  );
  const c = (0, import_react291.useCallback)(
    (g) => {
      g.key === "Enter" && (g.preventDefault(), e.editor.updateBlock(n2[0], {
        type: "image",
        props: {
          caption: i
        }
      }));
    },
    [i, e.editor, n2]
  ), d2 = (0, import_react291.useCallback)(
    (g) => s(g.currentTarget.value),
    []
  );
  return o ? r.jsxs(St, { children: [
    r.jsx(
      P,
      {
        mainTooltip: "Edit Caption",
        icon: ie,
        isSelected: n2[0].props.caption !== ""
      }
    ),
    r.jsx(Mt, { children: r.jsx(
      ze,
      {
        type: "text",
        icon: ie,
        inputProps: {
          autoFocus: true,
          placeholder: "Edit Caption",
          value: i,
          onKeyDown: c,
          onChange: d2
        }
      }
    ) })
  ] }) : null;
};
var mn = (e) => r.jsxs(_e, { children: [
  r.jsx(on, { ...e, items: e.blockTypeDropdownItems }),
  r.jsx(xn2, { editor: e.editor }),
  r.jsx(bn, { editor: e.editor }),
  r.jsx(he, { editor: e.editor, toggledStyle: "bold" }),
  r.jsx(he, { editor: e.editor, toggledStyle: "italic" }),
  r.jsx(he, { editor: e.editor, toggledStyle: "underline" }),
  r.jsx(he, { editor: e.editor, toggledStyle: "strike" }),
  r.jsx(Se, { editor: e.editor, textAlignment: "left" }),
  r.jsx(Se, { editor: e.editor, textAlignment: "center" }),
  r.jsx(Se, { editor: e.editor, textAlignment: "right" }),
  r.jsx(un, { editor: e.editor }),
  r.jsx(gn, { editor: e.editor }),
  r.jsx(fn4, { editor: e.editor }),
  r.jsx(vn, { editor: e.editor })
] });
var st = (e) => {
  switch (e) {
    case "left":
      return "top-start";
    case "center":
      return "top";
    case "right":
      return "top-end";
    default:
      return "top-start";
  }
};
var kn = (e) => {
  const [n2, o] = (0, import_react291.useState)(false), [i, s] = (0, import_react291.useState)(
    () => {
      const a2 = e.editor.getTextCursorPosition().block;
      return "textAlignment" in a2.props ? st(
        a2.props.textAlignment
      ) : "top-start";
    }
  ), c = (0, import_react291.useRef)();
  (0, import_react291.useEffect)(() => (tippy_esm_default.setDefaultProps({ maxWidth: "" }), e.editor.formattingToolbar.onUpdate((a2) => {
    o(a2.show), c.current = a2.referencePos;
  })), [e.editor]), q(e.editor, () => {
    const a2 = e.editor.getTextCursorPosition().block;
    "textAlignment" in a2.props ? s(
      st(
        a2.props.textAlignment
      )
    ) : s("top-start");
  });
  const d2 = (0, import_react291.useMemo)(
    () => {
      if (c)
        return () => c.current;
    },
    [c.current]
    // eslint-disable-line
  ), g = (0, import_react291.useMemo)(() => {
    const a2 = e.formattingToolbar || mn;
    return r.jsx(a2, { editor: e.editor });
  }, [e.editor, e.formattingToolbar]);
  return r.jsx(
    tippy_react_esm_default,
    {
      appendTo: e.editor.domElement.parentElement,
      content: g,
      getReferenceClientRect: d2,
      interactive: true,
      visible: n2,
      animation: "fade",
      placement: i,
      sticky: true,
      plugins: yn,
      zIndex: 3e3
    }
  );
};
var yn = [sticky];
var pn = (e) => {
  const [n2, o] = (0, import_react291.useState)(false), i = (0, import_react291.useRef)(null);
  return n2 ? r.jsx(
    Rt,
    {
      url: e.url,
      text: e.text,
      update: e.editHyperlink,
      onBlur: (s) => setTimeout(() => {
        var c;
        (c = i.current) != null && c.contains(s.relatedTarget) || o(false);
      }, 500),
      ref: i
    }
  ) : r.jsxs(
    _e,
    {
      onMouseEnter: e.stopHideTimer,
      onMouseLeave: e.startHideTimer,
      children: [
        r.jsx(
          P,
          {
            mainTooltip: "Edit",
            isSelected: false,
            onClick: () => o(true),
            children: "Edit Link"
          }
        ),
        r.jsx(
          P,
          {
            mainTooltip: "Open in new tab",
            isSelected: false,
            onClick: () => {
              window.open(e.url, "_blank");
            },
            icon: Gr
          }
        ),
        r.jsx(
          P,
          {
            mainTooltip: "Remove link",
            isSelected: false,
            onClick: e.deleteHyperlink,
            icon: $r
          }
        )
      ]
    }
  );
};
var Cn = (e) => {
  const [n2, o] = (0, import_react291.useState)(false), [i, s] = (0, import_react291.useState)(), [c, d2] = (0, import_react291.useState)(), g = (0, import_react291.useRef)();
  (0, import_react291.useEffect)(() => e.editor.hyperlinkToolbar.on(
    "update",
    (x) => {
      o(x.show), s(x.url), d2(x.text), g.current = x.referencePos;
    }
  ), [e.editor]);
  const a2 = (0, import_react291.useMemo)(
    () => {
      if (g.current)
        return () => g.current;
    },
    [g.current]
    // eslint-disable-line
  ), y = (0, import_react291.useMemo)(() => {
    if (!i || !c)
      return null;
    const x = e.hyperlinkToolbar || pn;
    return r.jsx(
      x,
      {
        url: i,
        text: c,
        editHyperlink: e.editor.hyperlinkToolbar.editHyperlink,
        deleteHyperlink: e.editor.hyperlinkToolbar.deleteHyperlink,
        startHideTimer: e.editor.hyperlinkToolbar.startHideTimer,
        stopHideTimer: e.editor.hyperlinkToolbar.stopHideTimer
      }
    );
  }, [e.hyperlinkToolbar, e.editor, c, i]);
  return r.jsx(
    tippy_react_esm_default,
    {
      appendTo: e.editor.domElement.parentElement,
      content: y,
      getReferenceClientRect: a2,
      interactive: true,
      visible: n2,
      animation: "fade",
      placement: "top-start",
      zIndex: 4e3
    }
  );
};
var jn = (e) => {
  const { classes: n2 } = createStyles({ root: {} })(void 0, {
    name: "SideMenu"
  });
  return r.jsx(Group, { className: n2.root, spacing: 0, children: e.children });
};
function Tn(e) {
  return j({ tag: "svg", attr: { t: "1551322312294", style: "", viewBox: "0 0 1024 1024", version: "1.1" }, child: [{ tag: "defs", attr: {}, child: [] }, { tag: "path", attr: { d: "M474 152m8 0l60 0q8 0 8 8l0 704q0 8-8 8l-60 0q-8 0-8-8l0-704q0-8 8-8Z" } }, { tag: "path", attr: { d: "M168 474m8 0l672 0q8 0 8 8l0 60q0 8-8 8l-672 0q-8 0-8-8l0-60q0-8 8-8Z" } }] })(e);
}
var zt = (e) => r.jsx(ActionIcon, { size: 24, children: e.children });
var wn = (e) => r.jsx(zt, { children: r.jsx(
  Tn,
  {
    size: 24,
    onClick: e.addBlock,
    "data-test": "dragHandleAdd"
  }
) });
function Sn(e) {
  return j({ tag: "svg", attr: { viewBox: "0 0 24 24" }, child: [{ tag: "path", attr: { fill: "none", d: "M0 0h24v24H0V0z" } }, { tag: "path", attr: { d: "M11 18c0 1.1-.9 2-2 2s-2-.9-2-2 .9-2 2-2 2 .9 2 2zm-2-8c-1.1 0-2 .9-2 2s.9 2 2 2 2-.9 2-2-.9-2-2-2zm0-6c-1.1 0-2 .9-2 2s.9 2 2 2 2-.9 2-2-.9-2-2-2zm6 4c1.1 0 2-.9 2-2s-.9-2-2-2-2 .9-2 2 .9 2 2 2zm0 2c-1.1 0-2 .9-2 2s.9 2 2 2 2-.9 2-2-.9-2-2-2zm0 6c-1.1 0-2 .9-2 2s.9 2 2 2 2-.9 2-2-.9-2-2-2z" } }] })(e);
}
var Mn = (e) => {
  const { classes: n2 } = createStyles({ root: {} })(void 0, {
    name: "DragHandleMenu"
  });
  return r.jsx(Menu.Dropdown, { className: n2.root, style: { overflow: "visible" }, children: e.children });
};
var Ht = (e) => {
  const { children: n2, ...o } = e;
  return r.jsx(Menu.Item, { ...o, children: n2 });
};
var Rn = (e) => r.jsx(
  Ht,
  {
    onClick: () => e.editor.removeBlocks([e.block]),
    children: e.children
  }
);
var En = (e) => {
  const [n2, o] = (0, import_react291.useState)(false), { ref: i, updateMaxHeight: s } = xe(), c = (0, import_react291.useRef)(), d2 = (0, import_react291.useCallback)(() => {
    c.current && clearTimeout(c.current), c.current = setTimeout(() => {
      o(false);
    }, 250);
  }, []), g = (0, import_react291.useCallback)(() => {
    c.current && clearTimeout(c.current), n2 || s(), o(true);
  }, [n2, s]);
  return !("textColor" in e.block.props) && !("backgroundColor" in e.block.props) ? null : r.jsx(
    Ht,
    {
      onMouseLeave: d2,
      onMouseOver: g,
      children: r.jsxs(Menu, { opened: n2, position: "right", children: [
        r.jsx(Menu.Target, { children: r.jsxs("div", { style: { display: "flex", alignItems: "center" }, children: [
          r.jsx("div", { style: { flex: 1 }, children: e.children }),
          r.jsx(Box, { style: { display: "flex", alignItems: "center" }, children: r.jsx(Zr, { size: 15 }) })
        ] }) }),
        r.jsx("div", { ref: i, children: r.jsx(
          Menu.Dropdown,
          {
            onMouseLeave: d2,
            onMouseOver: g,
            style: { marginLeft: "5px" },
            children: r.jsx(
              wt,
              {
                iconSize: 18,
                text: "textColor" in e.block.props && typeof e.block.props.textColor == "string" ? {
                  color: e.block.props.textColor,
                  setColor: (a2) => e.editor.updateBlock(e.block, {
                    props: { textColor: a2 }
                  })
                } : void 0,
                background: "backgroundColor" in e.block.props && typeof e.block.props.backgroundColor == "string" ? {
                  color: e.block.props.backgroundColor,
                  setColor: (a2) => e.editor.updateBlock(e.block, {
                    props: { backgroundColor: a2 }
                  })
                } : void 0
              }
            )
          }
        ) })
      ] })
    }
  );
};
var zn = (e) => r.jsxs(Mn, { children: [
  r.jsx(Rn, { ...e, children: "Delete" }),
  r.jsx(En, { ...e, children: "Colors" })
] });
var Hn = (e) => {
  const n2 = e.dragHandleMenu || zn;
  return r.jsxs(
    Menu,
    {
      trigger: "click",
      onOpen: e.freezeMenu,
      onClose: e.unfreezeMenu,
      width: 100,
      position: "left",
      children: [
        r.jsx(Menu.Target, { children: r.jsx(
          "div",
          {
            draggable: "true",
            onDragStart: e.blockDragStart,
            onDragEnd: e.blockDragEnd,
            children: r.jsx(zt, { children: r.jsx(Sn, { size: 24, "data-test": "dragHandle" }) })
          }
        ) }),
        r.jsx(n2, { editor: e.editor, block: e.block })
      ]
    }
  );
};
var In = (e) => r.jsxs(jn, { children: [
  r.jsx(wn, { ...e }),
  r.jsx(Hn, { ...e })
] });
var Bn2 = (e) => {
  const [n2, o] = (0, import_react291.useState)(false), [i, s] = (0, import_react291.useState)(), c = (0, import_react291.useRef)();
  (0, import_react291.useEffect)(() => e.editor.sideMenu.onUpdate((a2) => {
    o(a2.show), s(a2.block), c.current = a2.referencePos;
  }), [e.editor]);
  const d2 = (0, import_react291.useMemo)(
    () => {
      if (c.current)
        return () => c.current;
    },
    [c.current]
    // eslint-disable-line
  ), g = (0, import_react291.useMemo)(() => {
    if (!i)
      return null;
    const a2 = e.sideMenu || In;
    return r.jsx(
      a2,
      {
        block: i,
        editor: e.editor,
        blockDragStart: e.editor.sideMenu.blockDragStart,
        blockDragEnd: e.editor.sideMenu.blockDragEnd,
        addBlock: e.editor.sideMenu.addBlock,
        freezeMenu: e.editor.sideMenu.freezeMenu,
        unfreezeMenu: e.editor.sideMenu.unfreezeMenu
      }
    );
  }, [i, e.editor, e.sideMenu]);
  return r.jsx(
    tippy_react_esm_default,
    {
      appendTo: e.editor.domElement.parentElement,
      content: g,
      getReferenceClientRect: d2,
      interactive: true,
      visible: n2,
      animation: "fade",
      offset: _n,
      placement: "left",
      popperOptions: Dn,
      zIndex: 1e3
    }
  );
};
var _n = [0, 0];
var Dn = {
  modifiers: [
    {
      name: "flip",
      options: {
        fallbackPlacements: []
      }
    },
    {
      name: "preventOverflow",
      options: {
        mainAxis: false,
        altAxis: false
      }
    }
  ]
};
var Pn = 5;
function On(e) {
  const n2 = (0, import_react291.useRef)(null), { classes: o } = createStyles({ root: {} })(void 0, {
    name: "SuggestionListItem"
  });
  function i() {
    var g;
    const c = e.isSelected, d2 = (g = n2.current) == null ? void 0 : g.matches(":hover");
    return c || d2;
  }
  function s() {
    var c, d2;
    i() ? (c = n2.current) == null || c.setAttribute("data-hovered", "true") : (d2 = n2.current) == null || d2.removeAttribute("data-hovered");
  }
  return (0, import_react291.useEffect)(() => {
    s(), i() && n2.current && n2.current.getBoundingClientRect().left > Pn && n2.current.scrollIntoView({
      behavior: "smooth",
      block: "nearest"
    });
  }), r.jsx(
    Menu.Item,
    {
      className: o.root,
      icon: e.icon,
      onClick: e.set,
      closeMenuOnClick: false,
      onMouseLeave: () => {
        setTimeout(() => {
          s();
        }, 1);
      },
      ref: n2,
      rightSection: e.shortcut && r.jsx(Badge, { size: "xs", children: e.shortcut }),
      children: r.jsxs(Stack, { children: [
        r.jsx(Text, { size: 14, weight: 500, children: e.name }),
        r.jsx(Text, { size: 10, children: e.hint })
      ] })
    }
  );
}
function Fn(e) {
  const { classes: n2 } = createStyles({ root: {} })(void 0, {
    name: "SlashMenu"
  }), o = [];
  let i = 0;
  const s = it.groupBy(e.filteredItems, (c) => c.group);
  return it.forEach(s, (c) => {
    o.push(
      r.jsx(Menu.Label, { children: c[0].group }, c[0].group)
    );
    for (const d2 of c)
      o.push(
        r.jsx(
          On,
          {
            name: d2.name,
            icon: d2.icon,
            hint: d2.hint,
            shortcut: d2.shortcut,
            isSelected: e.keyboardHoveredItemIndex === i,
            set: () => e.itemCallback(d2)
          },
          d2.name
        )
      ), i++;
  }), r.jsx(
    Menu,
    {
      defaultOpened: true,
      trigger: "hover",
      closeDelay: 1e7,
      children: r.jsx(
        Menu.Dropdown,
        {
          onMouseDown: (c) => c.preventDefault(),
          className: n2.root,
          children: o.length > 0 ? o : r.jsx(Menu.Item, { children: "No match found" })
        }
      )
    }
  );
}
var An = (e) => {
  const [n2, o] = (0, import_react291.useState)(false), [i, s] = (0, import_react291.useState)(), [c, d2] = (0, import_react291.useState)(), g = (0, import_react291.useRef)();
  (0, import_react291.useEffect)(() => e.editor.slashMenu.onUpdate((S2) => {
    o(S2.show), s(S2.filteredItems), d2(S2.keyboardHoveredItemIndex), g.current = S2.referencePos;
  }), [e.editor]);
  const a2 = (0, import_react291.useMemo)(
    () => {
      if (g.current)
        return () => g.current;
    },
    [g.current]
    // eslint-disable-line
  ), { ref: y, updateMaxHeight: x } = xe(), p = (0, import_react291.useMemo)(() => {
    if (!i || c === void 0)
      return null;
    const S2 = e.slashMenu || Fn;
    return r.jsx("div", { ref: y, children: r.jsx(
      S2,
      {
        filteredItems: i,
        itemCallback: (T) => e.editor.slashMenu.itemCallback(T),
        keyboardHoveredItemIndex: c
      }
    ) });
  }, [
    i,
    c,
    e.editor.slashMenu,
    e.slashMenu,
    y
  ]);
  return r.jsx(
    tippy_react_esm_default,
    {
      onShow: x,
      appendTo: e.editor.domElement.parentElement,
      content: p,
      getReferenceClientRect: a2,
      interactive: true,
      visible: n2,
      animation: "fade",
      placement: "bottom-start",
      zIndex: 2e3
    }
  );
};
var k = [
  "#FFFFFF",
  "#EFEFEF",
  "#CFCFCF",
  "#AFAFAF",
  "#7F7F7F",
  "#3F3F3F",
  "#1F1F1F",
  "#161616",
  "#0F0F0F",
  "#000000"
];
var ve = {
  colors: {
    editor: {
      text: k[5],
      background: k[0]
    },
    menu: {
      text: k[5],
      background: k[0]
    },
    tooltip: {
      text: k[5],
      background: k[1]
    },
    hovered: {
      text: k[5],
      background: k[1]
    },
    selected: {
      text: k[0],
      background: k[5]
    },
    disabled: {
      text: k[3],
      background: k[1]
    },
    shadow: k[2],
    border: k[1],
    sideMenu: k[2],
    highlightColors: {
      gray: {
        text: "#9b9a97",
        background: "#ebeced"
      },
      brown: {
        text: "#64473a",
        background: "#e9e5e3"
      },
      red: {
        text: "#e03e3e",
        background: "#fbe4e4"
      },
      orange: {
        text: "#d9730d",
        background: "#f6e9d9"
      },
      yellow: {
        text: "#dfab01",
        background: "#fbf3db"
      },
      green: {
        text: "#4d6461",
        background: "#ddedea"
      },
      blue: {
        text: "#0b6e99",
        background: "#ddebf1"
      },
      purple: {
        text: "#6940a5",
        background: "#eae4f2"
      },
      pink: {
        text: "#ad1a72",
        background: "#f4dfeb"
      }
    }
  },
  borderRadius: 6,
  fontFamily: '"Inter", "SF Pro Display", -apple-system, BlinkMacSystemFont, "Open Sans", "Segoe UI", "Roboto", "Oxygen", "Ubuntu", "Cantarell", "Fira Sans", "Droid Sans", "Helvetica Neue", sans-serif'
};
var dt2 = {
  colors: {
    editor: {
      text: k[2],
      background: k[6]
    },
    menu: {
      text: k[2],
      background: k[6]
    },
    tooltip: {
      text: k[2],
      background: k[7]
    },
    hovered: {
      text: k[2],
      background: k[7]
    },
    selected: {
      text: k[2],
      background: k[8]
    },
    disabled: {
      text: k[5],
      background: k[7]
    },
    shadow: k[8],
    border: k[7],
    sideMenu: k[4],
    highlightColors: {
      gray: {
        text: "#bebdb8",
        background: "#9b9a97"
      },
      brown: {
        text: "#8e6552",
        background: "#64473a"
      },
      red: {
        text: "#ec4040",
        background: "#be3434"
      },
      orange: {
        text: "#e3790d",
        background: "#b7600a"
      },
      yellow: {
        text: "#dfab01",
        background: "#b58b00"
      },
      green: {
        text: "#6b8b87",
        background: "#4d6461"
      },
      blue: {
        text: "#0e87bc",
        background: "#0b6e99"
      },
      purple: {
        text: "#8552d7",
        background: "#6940a5"
      },
      pink: {
        text: "#da208f",
        background: "#ad1a72"
      }
    }
  },
  borderRadius: ve.borderRadius,
  fontFamily: ve.fontFamily
};
var Ln = (e) => {
  const [n2, o] = (0, import_react291.useState)(false), [i, s] = (0, import_react291.useState)(), c = (0, import_react291.useRef)();
  (0, import_react291.useEffect)(() => (tippy_esm_default.setDefaultProps({ maxWidth: "" }), e.editor.imageToolbar.onUpdate((a2) => {
    o(a2.show), s(a2.block), c.current = a2.referencePos;
  })), [e.editor]);
  const d2 = (0, import_react291.useMemo)(
    () => {
      if (c)
        return () => c.current;
    },
    [c.current]
    // eslint-disable-line
  ), g = (0, import_react291.useMemo)(() => {
    const a2 = e.imageToolbar || Et;
    return r.jsx(a2, { editor: e.editor, block: i });
  }, [i, e.editor, e.imageToolbar]);
  return r.jsx(
    tippy_react_esm_default,
    {
      appendTo: e.editor.domElement.parentElement,
      content: g,
      getReferenceClientRect: d2,
      interactive: true,
      visible: n2,
      animation: "fade",
      placement: "bottom",
      zIndex: 5e3
    }
  );
};
function Vn(e) {
  var d2;
  const { classes: n2 } = createStyles({ root: {} })(void 0, {
    name: "Editor"
  }), { editor: o, children: i, className: s, ...c } = e;
  return r.jsx(
    EditorContent,
    {
      editor: (d2 = e.editor) == null ? void 0 : d2._tiptapEditor,
      className: S(n2.root, e.className || ""),
      ...c,
      children: e.children || r.jsxs(r.Fragment, { children: [
        r.jsx(kn, { editor: e.editor }),
        r.jsx(Cn, { editor: e.editor }),
        r.jsx(An, { editor: e.editor }),
        r.jsx(Bn2, { editor: e.editor }),
        r.jsx(Ln, { editor: e.editor })
      ] })
    }
  );
}
function eo(e) {
  const {
    theme: n2 = { light: ve, dark: dt2 },
    ...o
  } = e, i = d(), s = (0, import_react291.useMemo)(() => n2 === "light" ? fe(ve) : n2 === "dark" ? fe(dt2) : "light" in n2 && "dark" in n2 ? fe(
    n2[i === "dark" ? "dark" : "light"]
  ) : fe(n2), [i, n2]);
  return r.jsx(MantineProvider, { theme: s, children: r.jsx(Vn, { ...o }) });
}
var Un = {
  Heading: {
    group: "Headings",
    icon: r.jsx(kt, { size: 18 }),
    hint: "Used for a top-level heading",
    shortcut: F2("Mod-Alt-1")
  },
  "Heading 2": {
    group: "Headings",
    icon: r.jsx(yt, { size: 18 }),
    hint: "Used for key sections",
    shortcut: F2("Mod-Alt-2")
  },
  "Heading 3": {
    group: "Headings",
    icon: r.jsx(pt, { size: 18 }),
    hint: "Used for subsections and group headings",
    shortcut: F2("Mod-Alt-3")
  },
  "Numbered List": {
    group: "Basic blocks",
    icon: r.jsx(jt2, { size: 18 }),
    hint: "Used to display a numbered list",
    shortcut: F2("Mod-Alt-7")
  },
  "Bullet List": {
    group: "Basic blocks",
    icon: r.jsx(Tt, { size: 18 }),
    hint: "Used to display an unordered list",
    shortcut: F2("Mod-Alt-9")
  },
  Paragraph: {
    group: "Basic blocks",
    icon: r.jsx(ie, { size: 18 }),
    hint: "Used for the body of your document",
    shortcut: F2("Mod-Alt-0")
  },
  Image: {
    group: "Media",
    icon: r.jsx(qr, {}),
    hint: "Insert an image"
  }
};
function Nn(e = jt) {
  return Ko(e).map((o) => ({
    ...o,
    ...Un[o.name]
  }));
}
var $n = (e) => new xn({
  slashMenuItems: Nn(
    e.blockSchema || jt
  ),
  ...e
});
var to = (e = {}, n2 = []) => {
  const o = (0, import_react291.useRef)();
  return (0, import_react291.useMemo)(() => (o.current && o.current._tiptapEditor.destroy(), o.current = $n(e), o.current), n2);
};
function Wn() {
  const [, e] = (0, import_react291.useState)(0);
  return () => e((n2) => n2 + 1);
}
var ro = (e) => {
  const n2 = Wn();
  (0, import_react291.useEffect)(() => {
    const o = () => {
      requestAnimationFrame(() => {
        requestAnimationFrame(() => {
          n2();
        });
      });
    };
    return e.on("transaction", o), () => {
      e.off("transaction", o);
    };
  }, [e]);
};
var It = (0, import_react291.createContext)({});
var no = (e) => {
  const n2 = (0, import_react291.useContext)(It).inlineContent || {}, o = S(
    e.className || "",
    Bn.inlineContent,
    n2.class
  );
  return r.jsx(
    NodeViewContent,
    {
      ...Object.fromEntries(
        Object.entries(n2).filter(
          ([i]) => i !== "class"
        )
      ),
      ...e,
      className: o
    }
  );
};
function oo(e) {
  return {
    node: F({
      name: e.type,
      content: e.containsInlineContent ? "inline*" : "",
      selectable: true,
      addAttributes() {
        return ho(e);
      },
      parseHTML() {
        return mo(e);
      },
      renderHTML({ HTMLAttributes: o }) {
        return fo(e, o);
      },
      addNodeView() {
        return ReactNodeViewRenderer((i) => {
          var T;
          const s = e.render, c = ((T = this.options.domAttributes) == null ? void 0 : T.blockContent) || {}, d2 = {};
          for (const [D, le] of Object.entries(i.node.attrs))
            D in e.propSchema && le !== e.propSchema[D].default && (d2[dt(D)] = le);
          const g = this.options.editor, a2 = typeof i.getPos == "function" ? i.getPos() : void 0, p = g._tiptapEditor.state.doc.resolve(a2).node().attrs.id, S2 = g.getBlock(p);
          if (S2.type !== e.type)
            throw new Error("Block type does not match");
          return r.jsx(
            NodeViewWrapper,
            {
              ...Object.fromEntries(
                Object.entries(c).filter(
                  ([D]) => D !== "class"
                )
              ),
              className: S(
                Bn.blockContent,
                c.class
              ),
              "data-content-type": e.type,
              ...d2,
              children: r.jsx(
                It.Provider,
                {
                  value: this.options.domAttributes || {},
                  children: r.jsx(s, { block: S2, editor: g })
                }
              )
            }
          );
        }, {
          className: Bn.reactNodeViewRenderer
        });
      }
    }),
    propSchema: e.propSchema
  };
}
export {
  wn as AddBlockButton,
  En as BlockColorsButton,
  eo as BlockNoteView,
  on as BlockTypeDropdown,
  un as ColorStyleButton,
  vn as CreateLinkButton,
  zn as DefaultDragHandleMenu,
  mn as DefaultFormattingToolbar,
  Et as DefaultImageToolbar,
  In as DefaultSideMenu,
  Fn as DefaultSlashMenu,
  Hn as DragHandle,
  Mn as DragHandleMenu,
  Ht as DragHandleMenuItem,
  kn as FormattingToolbarPositioner,
  Cn as HyperlinkToolbarPositioner,
  Ln as ImageToolbarPositioner,
  no as InlineContent,
  gn as NestBlockButton,
  Rn as RemoveBlockButton,
  jn as SideMenu,
  zt as SideMenuButton,
  Bn2 as SideMenuPositioner,
  On as SlashMenuItem,
  An as SlashMenuPositioner,
  Se as TextAlignButton,
  he as ToggledStyleButton,
  _e as Toolbar,
  P as ToolbarButton,
  en as ToolbarDropdown,
  fn4 as UnnestBlockButton,
  fe as blockNoteToMantineTheme,
  oo as createReactBlockSpec,
  dt2 as darkDefaultTheme,
  nn as defaultBlockTypeDropdownItems,
  k as defaultColorScheme,
  Nn as getDefaultReactSlashMenuItems,
  ve as lightDefaultTheme,
  to as useBlockNote,
  q as useEditorChange,
  tn as useEditorContentChange,
  ro as useEditorForceUpdate,
  rn as useEditorSelectionChange,
  Q as useSelectedBlocks
};
/*! Bundled license information:

tabbable/dist/index.esm.js:
  (*!
  * tabbable 6.2.0
  * @license MIT, https://github.com/focus-trap/tabbable/blob/master/LICENSE
  *)

@blocknote/react/dist/blocknote-react.js:
  (**
   * @license React
   * react-jsx-runtime.production.min.js
   *
   * Copyright (c) Facebook, Inc. and its affiliates.
   *
   * This source code is licensed under the MIT license found in the
   * LICENSE file in the root directory of this source tree.
   *)

@blocknote/react/dist/blocknote-react.js:
  (**
   * @license React
   * react-jsx-runtime.development.js
   *
   * Copyright (c) Facebook, Inc. and its affiliates.
   *
   * This source code is licensed under the MIT license found in the
   * LICENSE file in the root directory of this source tree.
   *)
*/
//# sourceMappingURL=@blocknote_react.js.map
